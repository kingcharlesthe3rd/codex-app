const e = require(`./src-1.js`),
  t = require(`./src.js`),
  n = require(`./file-based-logger.js`),
  r = require(`./workspace-root-drop-handler.js`),
  i = require(`./feedback-desktop-log-archive.js`);
let a = require(`electron`);
a = e.o(a);
let o = require(`node:os`);
o = e.o(o);
let s = require(`node:path`);
s = e.o(s);
let c = require(`node:util`),
  l = require(`node:fs`);
l = e.o(l);
let u = require(`node:crypto`),
  d = require(`node:http`),
  f = require(`node:https`),
  p = require(`node:fs/promises`);
p = e.o(p);
let m = require(`node:child_process`),
  h = require(`node:process`);
h = e.o(h);
let g = require(`node:net`);
g = e.o(g);
let _ = require(`node:timers/promises`),
  v = require(`node:string_decoder`),
  y = require(`node:readline`),
  b = require(`node:stream`),
  x = require(`node:url`),
  S = require(`node:worker_threads`),
  C = require(`node:module`),
  w = require(`node:perf_hooks`),
  T = require(`node:inspector`);
T = e.o(T);
var E = /^[a-z0-9-_]+$/i;
function D(e) {
  let t = typeof e;
  if (t === `object`) {
    if (Array.isArray(e)) return `array`;
    if (e instanceof Date) return `date`;
  }
  return t;
}
function O(e) {
  for (let t = 0; t < e.length; t++) if (D(e[t]) !== `object`) return !1;
  return e.length != 0;
}
function k(e) {
  return JSON.stringify(e).replace(/\x7f/g, `\\u007f`);
}
function A(e, t, n, r) {
  if (n === 0) throw Error(`Could not stringify the object: maximum object depth exceeded`);
  if (t === `number`)
    return isNaN(e)
      ? `nan`
      : e === 1 / 0
        ? `inf`
        : e === -1 / 0
          ? `-inf`
          : r && Number.isInteger(e)
            ? e.toFixed(1)
            : e.toString();
  if (t === `bigint` || t === `boolean`) return e.toString();
  if (t === `string`) return k(e);
  if (t === `date`) {
    if (isNaN(e.getTime())) throw TypeError(`cannot serialize invalid date`);
    return e.toISOString();
  }
  if (t === `object`) return j(e, n, r);
  if (t === `array`) return M(e, n, r);
}
function j(e, t, n) {
  let r = Object.keys(e);
  if (r.length === 0) return `{}`;
  let i = `{ `;
  for (let a = 0; a < r.length; a++) {
    let o = r[a];
    (a && (i += `, `),
      (i += E.test(o) ? o : k(o)),
      (i += ` = `),
      (i += A(e[o], D(e[o]), t - 1, n)));
  }
  return i + ` }`;
}
function M(e, t, n) {
  if (e.length === 0) return `[]`;
  let r = `[ `;
  for (let i = 0; i < e.length; i++) {
    if ((i && (r += `, `), e[i] === null || e[i] === void 0))
      throw TypeError(`arrays cannot contain null or undefined values`);
    r += A(e[i], D(e[i]), t - 1, n);
  }
  return r + ` ]`;
}
function N(e, t, n, r) {
  if (n === 0) throw Error(`Could not stringify the object: maximum object depth exceeded`);
  let i = ``;
  for (let a = 0; a < e.length; a++)
    ((i += `${
      i &&
      `
`
    }[[${t}]]\n`),
      (i += P(0, e[a], t, n, r)));
  return i;
}
function P(e, t, n, r, i) {
  if (r === 0) throw Error(`Could not stringify the object: maximum object depth exceeded`);
  let a = ``,
    o = ``,
    s = Object.keys(t);
  for (let e = 0; e < s.length; e++) {
    let c = s[e];
    if (t[c] !== null && t[c] !== void 0) {
      let e = D(t[c]);
      if (e === `symbol` || e === `function`)
        throw TypeError(`cannot serialize values of type '${e}'`);
      let s = E.test(c) ? c : k(c);
      if (e === `array` && O(t[c]))
        o +=
          (o &&
            `
`) + N(t[c], n ? `${n}.${s}` : s, r - 1, i);
      else if (e === `object`) {
        let e = n ? `${n}.${s}` : s;
        o +=
          (o &&
            `
`) + P(e, t[c], e, r - 1, i);
      } else
        ((a += s),
          (a += ` = `),
          (a += A(t[c], e, r, i)),
          (a += `
`));
    }
  }
  return (e && (a || !o) && (a = a ? `[${e}]\n${a}` : `[${e}]`), a && o ? `${a}\n${o}` : a || o);
}
function F(e, { maxDepth: t = 1e3, numbersAsFloat: n = !1 } = {}) {
  if (D(e) !== `object`) throw TypeError(`stringify can only be called with an object`);
  let r = P(0, e, ``, t, n);
  return r[r.length - 1] ===
    `
`
    ? r
    : r +
        `
`;
}
function I(e, n) {
  return n ? t.Nr(e) : e;
}
function ee(e, t) {
  return e.map((e) => I(e, t));
}
function te(e) {
  return ne(e, t.zr());
}
function ne(e, n) {
  return n ? t.Pr(e, t.Fr()) : e;
}
function re(e, t) {
  return e.map((e) => ne(e, t));
}
function ie(e) {
  return e
    ? {
        preferWslPaths: !0,
        convertWindowsPathToWslPath: t.Nr,
        convertWslPathToWindowsPath: (e) => ne(e, !0),
      }
    : { preferWslPaths: !1 };
}
function ae(e, n, r) {
  return t.va(n) ? e : r ? I(e, !0) : me(e) ? ne(e, !0) : e;
}
function oe(e, t, n) {
  return fe(e, (e) => ae(e, t, n));
}
async function se(e, t, n) {
  let r = new Map();
  for (let i of e) {
    let e = ae(i, t, n);
    r.has(e) || r.set(e, i);
  }
  let i = await Promise.all(
    Array.from(r, async ([e, r]) => {
      try {
        return [e, ae(await (0, p.realpath)(le(r, t)), t, n)];
      } catch {
        return null;
      }
    }),
  );
  return Object.fromEntries(i.filter((e) => e != null));
}
function ce(e, t, n) {
  return pe(e, (e) => ae(e, t, n));
}
function le(e, n) {
  return process.platform !== `win32` || t.va(n) || !me(e) ? e : ne(e, !0);
}
function ue(e, t) {
  return fe(e, (e) => le(e, t));
}
function de(e, t) {
  return e ? pe(e, (e) => le(e, t)) : {};
}
function fe(e, t) {
  let n = new Set();
  return e.map(t).filter((e) => (n.has(e) ? !1 : (n.add(e), !0)));
}
function pe(e, t) {
  let n = {};
  return (
    Object.entries(e).forEach(([e, r]) => {
      let i = t(e);
      n[i] ?? (n[i] = r);
    }),
    n
  );
}
function me(e) {
  return t.Lr(e) || e === `/` || /^\/(?!\/)/.test(e);
}
var he = t.Gr(`devicecheck-attestation`),
  ge = `devicecheck.node`,
  _e = `v1`,
  ve = (0, C.createRequire)(__filename);
function ye({ platform: e = process.platform } = {}) {
  return e === `darwin`;
}
function be({ arch: e = process.arch, platform: t = process.platform } = {}) {
  return t === `darwin` && e === `arm64`;
}
function xe({
  addon: e,
  arch: t = process.arch,
  bundleIdentifier: n,
  platform: r = process.platform,
  resourcesPath: i = process.resourcesPath,
}) {
  return (a) =>
    a.method === `attestation/generate`
      ? Se({ addon: e, arch: t, bundleIdentifier: n, platform: r, resourcesPath: i })
      : null;
}
async function Se({
  addon: e,
  arch: t = process.arch,
  bundleIdentifier: n,
  platform: r = process.platform,
  resourcesPath: i = process.resourcesPath,
}) {
  let a = Date.now();
  if (r !== `darwin`) return we(1, a, n);
  if (!be({ arch: t, platform: r })) return we(2, a, n);
  let o;
  try {
    o = await (e ?? Ce(i)).generateToken();
  } catch (e) {
    return we(4, a, n, { error: e });
  }
  return o.supported
    ? o.tokenBase64 == null
      ? we(4, a, n, { deviceCheckLatencyMs: o.latencyMs })
      : (he().info(`DeviceCheck attestation generated`, {
          safe: { durationMs: Date.now() - a, deviceCheckLatencyMs: o.latencyMs ?? null },
          sensitive: {},
        }),
        { token: Te({ bundleIdentifier: n, token: o.tokenBase64, latencyMs: o.latencyMs }) })
    : we(3, a, n, { deviceCheckLatencyMs: o.latencyMs });
}
function Ce(e) {
  if (e == null) throw Error(`DeviceCheck requires resourcesPath`);
  return ve((0, s.join)(e, `native`, ge));
}
function we(e, t, n, r = {}) {
  let i = {
    durationMs: Date.now() - t,
    errorCode: e,
    deviceCheckLatencyMs: r.deviceCheckLatencyMs ?? null,
  };
  return (
    r.error == null
      ? he().info(`DeviceCheck attestation unavailable`, { safe: i, sensitive: {} })
      : he().warning(`DeviceCheck attestation failed`, { safe: i, sensitive: { error: r.error } }),
    { token: Te({ bundleIdentifier: n, errorCode: e, latencyMs: r.deviceCheckLatencyMs }) }
  );
}
function Te(e) {
  let t = `token` in e ? [Ee(`token`, e.token)] : [De(`error_code`, e.errorCode)];
  return (
    t.push(Ee(`bundle_id`, e.bundleIdentifier)),
    e.latencyMs != null && t.push(Oe(`t`, e.latencyMs)),
    `${_e}.${Ne(Buffer.concat([Buffer.from([160 + t.length]), ...t]))}`
  );
}
function Ee(e, t) {
  return Buffer.concat([ke(e), ke(t)]);
}
function De(e, t) {
  return Buffer.concat([ke(e), Ae(t)]);
}
function Oe(e, t) {
  return Buffer.concat([ke(e), je(t)]);
}
function ke(e) {
  let t = Buffer.from(e, `utf8`);
  return Buffer.concat([Me(96, t.length), t]);
}
function Ae(e) {
  if (!Number.isSafeInteger(e) || e < 0) throw Error(`Invalid CBOR unsigned integer: ${e}`);
  return Me(0, e);
}
function je(e) {
  let t = Buffer.allocUnsafe(9);
  return ((t[0] = 251), t.writeDoubleBE(e, 1), t);
}
function Me(e, t) {
  if (t < 24) return Buffer.from([e + t]);
  if (t <= 255) return Buffer.from([e + 24, t]);
  if (t <= 65535) {
    let n = Buffer.allocUnsafe(3);
    return ((n[0] = e + 25), n.writeUInt16BE(t, 1), n);
  }
  if (t <= 4294967295) {
    let n = Buffer.allocUnsafe(5);
    return ((n[0] = e + 26), n.writeUInt32BE(t, 1), n);
  }
  throw Error(`CBOR length too large: ${t}`);
}
function Ne(e) {
  return e.toString(`base64`).replaceAll(`+`, `-`).replaceAll(`/`, `_`).replace(/=+$/u, ``);
}
var Pe = {
    ambientSuggestions: !1,
    appshotsEnabled: !1,
    browserPane: !1,
    inAppBrowserUse: !1,
    inAppBrowserUseAllowed: !1,
    linksDefaultInAppBrowser: !1,
    externalBrowserUse: !1,
    externalBrowserUseAllowed: !1,
    computerUse: !1,
    computerUseNodeRepl: !1,
    sites: !1,
    control: !1,
    deviceAttestation: ye(),
    dil: !1,
    multiBrowserTabs: !1,
    multiWindow: !1,
    processManager: !1,
    remoteControlClientScopedEnvironments: !1,
    visualize: !1,
  },
  Fe = Object.keys(Pe),
  Ie = { ...Pe },
  Le = `CODEX_ELECTRON_DESKTOP_FEATURE_OVERRIDES`;
function Re() {
  return Ie;
}
function ze(e) {
  return Fe.every((t) => typeof e[t] == `boolean`);
}
function Be(e, t) {
  return Fe.some((n) => e[n] !== t[n]);
}
function Ve(e) {
  let t = { ...Ie, ...e, deviceAttestation: ye() };
  return Fe.every((e) => t[e] === Ie[e]) ? !1 : ((Ie = t), !0);
}
function He(
  e,
  { buildFlavor: t = n.i.resolve(), env: r = h.default.env, platform: i = h.default.platform } = {},
) {
  let a =
      i === `darwin` && !n.i.isInternal(t) && e.computerUseNodeRepl != null
        ? { ...e, computerUseNodeRepl: !1 }
        : e,
    o = i === `win32` && e.computerUse === !0 ? { ...a, computerUseNodeRepl: !0 } : a,
    s =
      i === `win32` && r.CODEX_ELECTRON_ENABLE_WINDOWS_COMPUTER_USE === `1`
        ? { ...o, computerUse: !0, computerUseNodeRepl: !0 }
        : o,
    c = t === n.i.Dev ? Ue(r) : null;
  return c == null
    ? { ...s, deviceAttestation: ye({ platform: i }) }
    : { ...s, ...c, deviceAttestation: ye({ platform: i }) };
}
function Ue(e) {
  let t = e[Le]?.trim();
  if (!t) return null;
  let n = JSON.parse(t);
  if (typeof n != `object` || !n || Array.isArray(n)) throw Error(`${Le} must be a JSON object`);
  let r = {},
    i = n,
    a = new Set(Fe),
    o = Object.keys(i).filter((e) => !a.has(e));
  if (o.length > 0) throw Error(`${Le} contains unknown keys: ${o.join(`, `)}`);
  for (let e of Fe) {
    let t = i[e];
    if (t !== void 0) {
      if (typeof t != `boolean`) throw Error(`${Le}.${e} must be a boolean`);
      r[e] = t;
    }
  }
  return r;
}
function We(e) {
  return Re().ambientSuggestions && e.getEffective(t.bi.enabled.key) === !0;
}
async function Ge({ appServerConnection: e, settingsStore: n }) {
  if (!We(n)) return { enabled: !1, staleTimeMs: t.js(null) };
  let r = await e.getAccount();
  return { enabled: t.As(r.account), staleTimeMs: t.js(r.account) };
}
var Ke = t.Gr(`ambient-suggestions-background-refresh`);
function qe({
  appEvent: e,
  appServerClient: n,
  appServerConnectionRegistry: i,
  globalState: a,
  settingsStore: s,
}) {
  let c = new Map(),
    l = t.Br(n.hostConfig),
    u = async (e, r, i, a) => {
      let o = c.get(e.projectRoot);
      if (o != null) return o;
      let s = t.Sr(n.hostConfig.id, e.projectRoot);
      if (s.generatedAtMs != null && Date.now() - s.generatedAtMs < a) return;
      let l = t
        .Cr({
          hostId: n.hostConfig.id,
          projectRoot: e.projectRoot,
          isProjectlessChat: e.isProjectlessChat,
          enabled: !0,
          appServerConnection: i,
          serviceTier: null,
          staleTimeMs: a,
        })
        .then(() => void 0)
        .catch((t) => {
          Ke().warning(`Failed to refresh ambient suggestions`, {
            safe: { trigger: r },
            sensitive: { error: t, projectRoot: e.projectRoot },
          });
        })
        .finally(() => {
          c.delete(e.projectRoot);
        });
      return (c.set(e.projectRoot, l), l);
    },
    d = { projectRoot: t.xa(I((0, o.homedir)(), l)), isProjectlessChat: !0 },
    f = () => {
      let e = r.i(a)[0] ?? null;
      if (e == null || e === `~`) return [d];
      let n = { projectRoot: t.xa(I(e, l)), isProjectlessChat: !1 };
      return d.projectRoot === n.projectRoot ? [d] : [n];
    },
    p = async (e) => {
      try {
        if (!We(s)) return;
        let t = i.getConnection(n.hostConfig.id),
          r = await Ge({ appServerConnection: t, settingsStore: s });
        if (!r.enabled) return;
        let a = f();
        await Promise.all(a.map((n) => u(n, e, t, r.staleTimeMs)));
      } catch (t) {
        Ke().warning(`Failed to refresh ambient suggestions`, {
          safe: { trigger: e },
          sensitive: { error: t },
        });
      }
    },
    m = e.subscribe((e) => {
      switch (e.type) {
        case `background`:
          p(e.type);
          return;
        case `foreground`:
        case `turnComplete`:
          return;
      }
    });
  return {
    dispose: () => {
      (m.dispose(), c.clear());
    },
  };
}
function Je({ setBrowserUseNativePipeEnabled: e }) {
  return {
    setDesktopFeatureAvailability: (t) => {
      t.inAppBrowserUse != null && e(t.inAppBrowserUse);
    },
    dispose: () => {
      e(!1);
    },
  };
}
var Ye = [`099cb5ac09333da8389063d49d03a7e277fc87d858b0526a538f0871af93bf68`],
  Xe = `CODEX_ELECTRON_RESOURCES_PATH`;
function Ze({ env: e, resourcesPath: t }) {
  return e[Xe]?.trim() || t;
}
var Qe = new Set([`about:blank`]);
function $e(e) {
  if (Qe.has(e)) return !0;
  let t;
  try {
    t = new URL(e);
  } catch {
    return !1;
  }
  return t.protocol === `http:` || t.protocol === `https:`;
}
var et = `browser/config.toml`,
  tt = `full_cdp_access_enabled`,
  nt = `BROWSER_USE_AVAILABLE_BACKENDS`,
  rt = `BROWSER_USE_CODEX_APP_BUILD_FLAVOR`,
  it = `BROWSER_USE_CODEX_APP_VERSION`,
  at = `BROWSER_USE_DISABLE_AMBIENT_NETWORK`,
  ot = `BROWSER_USE_DISABLE_BROWSER_CAPABILITIES`,
  st = `BROWSER_USE_DISABLE_TAB_CAPABILITIES`,
  ct = `BROWSER_USE_SECURITY_MODE`;
(function (e) {
  return (
    (e.Default = ``),
    (e.DisabledForLocalTesting = `disabled-for-local-testing`),
    (e.GaasBrowserEnvironment = `gaas-browser-environment`),
    e
  );
})({}).Default;
var lt = class {
    nextId = 1;
    pendingRequests = new Map();
    requestHandlers = new Map();
    eventHandlers = new Map();
    constructor(e) {
      ((this.transport = e),
        this.transport.setMessageCallback((e) => {
          this.handleIncomingMessage(e);
        }),
        this.transport.addCloseListener?.((e) => {
          this.rejectPendingRequests(e?.message ?? `transport closed before response`);
        }));
    }
    registerRequestHandlerObject(e) {
      [...Object.getOwnPropertyNames(e), ...Object.getOwnPropertyNames(Object.getPrototypeOf(e))]
        .filter((t) => t !== `constructor` && typeof e[t] == `function`)
        .forEach((t) => {
          let n = e[t];
          n && this.registerRequestHandler(t, n.bind(e));
        });
    }
    registerRequestHandler(e, t) {
      this.requestHandlers.set(e, t);
    }
    addEventListener(e, t) {
      let n = e.toString(),
        r = this.eventHandlers.get(n) ?? [];
      (r.push(t), this.eventHandlers.set(n, r));
    }
    removeEventListener(e, t) {
      let n = e.toString(),
        r = this.eventHandlers.get(n) ?? [];
      this.eventHandlers.set(
        n,
        r.filter((e) => e !== t),
      );
    }
    sendNotification(e, t) {
      this.transport.sendMessage({ jsonrpc: `2.0`, method: e, params: t });
    }
    sendRequest(e, t) {
      let n = this.nextId++;
      return new Promise((r, i) => {
        this.pendingRequests.set(n, { resolve: r, reject: i });
        try {
          this.transport.sendMessage({ jsonrpc: `2.0`, method: e.toString(), params: t, id: n });
        } catch (e) {
          (this.pendingRequests.delete(n), i(e));
        }
      });
    }
    async handleIncomingMessage(e) {
      if (`method` in e) return this.handleIncomingRequest(e);
      if (e.id === void 0) return;
      let t = this.pendingRequests.get(e.id);
      if (t) {
        if ((this.pendingRequests.delete(e.id), `error` in e)) {
          t.reject(e.error?.message || `Something went wrong`);
          return;
        }
        t.resolve(e.result);
      }
    }
    rejectPendingRequests(e) {
      for (let t of this.pendingRequests.values()) t.reject(e);
      this.pendingRequests.clear();
    }
    async handleIncomingRequest(e) {
      if (e.id === void 0) {
        (this.eventHandlers.get(e.method ?? ``) ?? []).forEach((t) => t(e.params));
        return;
      }
      let t = this.requestHandlers.get(e.method ?? ``);
      if (!t) {
        this.transport.sendMessage({
          jsonrpc: `2.0`,
          id: e.id,
          error: { code: -1, message: `No handler registered for method: ${e.method}` },
        });
        return;
      }
      try {
        let n = await t(e.params);
        this.transport.sendMessage({ jsonrpc: `2.0`, id: e.id, result: n });
      } catch (t) {
        this.transport.sendMessage({
          jsonrpc: `2.0`,
          id: e.id,
          error: { code: 1, message: t instanceof Error ? t.message : String(t) },
        });
      }
    }
  },
  ut = class extends lt {
    constructor(e, t, n = {}) {
      (super(e),
        (this.handler = t),
        (this.options = n),
        this.registerRequestHandlerObject(t),
        this.addEventListener(`moveMouse`, (e) => {
          Promise.resolve(this.handler.moveMouse(e)).catch((t) => {
            this.options.onMoveMouseError?.(t, e);
          });
        }));
    }
    ping() {
      return this.sendRequest(`ping`);
    }
    sendCdpEvent(e) {
      this.sendNotification(`onCDPEvent`, e);
    }
    sendCdpDetach(e) {
      this.sendNotification(`onCDPDetach`, e);
    }
    sendDownloadChange(e) {
      this.sendNotification(`onDownloadChange`, e);
    }
  };
async function dt({ backendName: e, commandSets: t, handlers: n, payload: r }) {
  if (!t.some((e) => Object.prototype.hasOwnProperty.call(e.commands, r.type))) throw ft(e, r);
  let i = n[r.type];
  if (i == null) throw ft(e, r);
  return i(r);
}
function ft(e, t) {
  return Error(`${e} does not support command "${t.type}".`);
}
var pt = (e) => (e === `win32` ? `\\\\.\\pipe\\codex-browser-use` : `/tmp/codex-browser-use`),
  mt = `CODEX_CHROME_USER_DATA_DIR`,
  ht = `NODE_REPL_DISABLE_ANALYTICS`,
  gt = `NODE_REPL_INSTRUCTIONS_USE_CASE_BROWSER`,
  _t = `NODE_REPL_INSTRUCTIONS_USE_CASE_CHROME`,
  vt = `NODE_REPL_UNTRUSTED_ENV_ALLOWLIST`,
  yt = `native_pipe`,
  bt = t
    .$s({
      call_id: t.nc().trim().min(1).optional(),
      item_id: t.nc().trim().min(1).optional(),
      itemId: t.nc().trim().min(1).optional(),
      model: t.nc().trim().min(1).optional(),
      model_reasoning_effort: t.nc().trim().min(1).optional(),
      reasoningEffort: t.nc().trim().min(1).optional(),
      reasoning_effort: t.nc().trim().min(1).optional(),
      session_id: t.nc().trim().min(1).optional(),
      thread_id: t.nc().trim().min(1).optional(),
      threadId: t.nc().trim().min(1).optional(),
      turn_id: t.nc().trim().min(1).optional(),
    })
    .passthrough(),
  xt = t
    .$s({
      app: t
        .rc([
          t.nc().trim().min(1),
          t
            .$s({
              bundleIdentifier: t.nc().trim().min(1).optional(),
              bundle_identifier: t.nc().trim().min(1).optional(),
            })
            .passthrough(),
        ])
        .optional(),
      bundleIdentifier: t.nc().trim().min(1).optional(),
      bundle_identifier: t.nc().trim().min(1).optional(),
      window: t
        .$s({ app: t.nc().trim().min(1).optional() })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  St = t
    .$s({
      meta: t
        .$s({
          method: t.nc().trim().min(1).optional(),
          params: t.ic().optional(),
          tool_params: t.ic().optional(),
        })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  Ct = t
    .$s({
      action: t.Us([`accept`, `decline`, `cancel`]),
      _meta: t
        .$s({ persist: t.Us([`always`, `session`]).optional() })
        .passthrough()
        .optional(),
    })
    .passthrough();
function wt(e, t, n) {
  let r = xt.safeParse(t),
    i = r.success ? kt(r.data) : void 0,
    a = bt.safeParse(n),
    o = a.success ? a.data.model : void 0,
    s = a.success ? At(a.data) : void 0;
  return {
    toolName: e,
    transport: yt,
    ...(i == null ? {} : { bundleIdentifier: i }),
    ...(o == null ? {} : { model: o }),
    ...(s == null ? {} : { reasoningEffort: s }),
  };
}
function Tt(e) {
  let t = bt.safeParse(e),
    n = t.success ? t.data : {},
    r = n.thread_id ?? n.threadId ?? n.session_id,
    i = n.item_id ?? n.itemId ?? n.call_id;
  return {
    pluginId: `computer-use@openai-bundled`,
    mcpServerName: `computer-use`,
    invocationSource: `model`,
    ...(r == null ? {} : { threadId: r }),
    ...(n.turn_id == null ? {} : { turnId: n.turn_id }),
    ...(i == null ? {} : { itemId: i }),
  };
}
function Et(e) {
  let t = St.safeParse(e);
  return t.success
    ? wt(t.data.meta?.method ?? `unknown`, t.data.meta?.params ?? t.data.meta?.tool_params)
    : { toolName: `unknown`, transport: yt };
}
function Dt(e, t) {
  let n = Ct.safeParse(t);
  return n.success
    ? {
        type: `app-approval-resolved`,
        ...e,
        approvalResult: jt(n.data.action),
        ...(n.data._meta?.persist == null ? {} : { approvalPersistence: n.data._meta.persist }),
      }
    : { type: `app-approval-resolved`, ...e, approvalResult: `unknown` };
}
function Ot(e) {
  let t = { transport: e.transport };
  return (e.bundleIdentifier != null && (t.bundleIdentifier = e.bundleIdentifier), t);
}
function kt(e) {
  return (
    e.bundleIdentifier ??
    e.bundle_identifier ??
    (typeof e.app == `string` ? e.app : (e.app?.bundleIdentifier ?? e.app?.bundle_identifier)) ??
    e.window?.app
  );
}
function At(e) {
  return e.reasoning_effort ?? e.reasoningEffort ?? e.model_reasoning_effort;
}
function jt(e) {
  switch (e) {
    case `accept`:
      return `accepted`;
    case `decline`:
      return `declined`;
    case `cancel`:
      return `canceled`;
  }
}
var Mt = [
    `openai-bundled-agent`,
    `openai-bundled-alpha`,
    `openai-bundled-beta`,
    `openai-bundled-dev`,
    `openai-bundled-nightly`,
    `openai-bundled-owl`,
  ],
  Nt = `chrome`,
  Pt = `chrome-dev`;
function Ft({ marketplaceName: e, marketplaces: n }) {
  return [e, t.bo]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((e) => n.filter((t) => t.name === e));
}
function It({ marketplaceName: e, marketplaces: t, pluginName: n }) {
  for (let r of Ft({ marketplaceName: e, marketplaces: t })) {
    let e = r.plugins.find((e) => e.name === n);
    if (r.path != null && e != null) return { marketplace: { ...r, path: r.path }, plugin: e };
  }
  return null;
}
var Lt = `mcp_servers.${t.go}`,
  Rt = `NODE_REPL_INSTRUCTIONS_USE_CASE_COMPUTER_USE`,
  zt = `SKY_CUA_SERVICE_PATH`,
  Bt = `SKY_CUA_NATIVE_PIPE`,
  Vt = `SKY_CUA_NATIVE_PIPE_DIRECTORY`,
  Ht = `Codex Computer Use.app`,
  Ut = `\\\\.\\pipe\\codex-computer-use-${(0, u.randomUUID)()}`;
function Wt({ nativePipeDirectory: e, nativePipeEnabled: t = !1, serviceAppPath: n } = {}) {
  return { ...(n == null ? {} : { [zt]: n }), ...(t ? { [Bt]: `1`, [Vt]: e ?? Ut } : {}) };
}
function Gt({
  env: e = process.env,
  installedPluginServiceAppPath: t,
  pathExists: n = l.existsSync,
}) {
  let r = e[zt]?.trim();
  if (r) return r;
  let i = t?.trim();
  return i && n(i) ? i : null;
}
function Kt({ env: e = process.env, installedPluginRoot: t, pathExists: n = l.existsSync } = {}) {
  let r = t?.trim() || null;
  return {
    serviceAppPath: Gt({
      env: e,
      installedPluginServiceAppPath: r == null ? null : s.default.join(r, Ht),
      pathExists: n,
    }),
  };
}
function qt({
  codexHome: e,
  env: r = process.env,
  marketplaceName: i = t.So(n.i.resolve()),
  marketplaces: a,
  pathExists: o = l.existsSync,
}) {
  for (let n of Ft({ marketplaceName: i, marketplaces: a })) {
    let i = n.plugins.find(
      (e) => e.name === `computer-use` && e.installed && e.enabled && e.source.type === `local`,
    );
    if (i?.source.type === `local`)
      return Kt({
        env: r,
        installedPluginRoot: t.Eo({
          codexHome: e,
          localVersion: i.localVersion,
          marketplaceName: n.name,
          pluginName: i.name,
        }),
        pathExists: o,
      });
  }
  return Kt({ env: r, pathExists: o });
}
var Jt = t.rc([
    t
      .$s({
        id: t.rc([t.Qs(), t.nc()]).optional(),
        jsonrpc: t.Xs(`2.0`),
        method: t.nc(),
        params: t.ic().optional(),
      })
      .passthrough(),
    t.$s({ id: t.rc([t.Qs(), t.nc()]), jsonrpc: t.Xs(`2.0`), result: t.ic() }).passthrough(),
    t
      .$s({
        error: t.$s({ code: t.Qs(), data: t.ic().optional(), message: t.nc() }).passthrough(),
        id: t.rc([t.Qs(), t.nc()]),
        jsonrpc: t.Xs(`2.0`),
      })
      .passthrough(),
  ]),
  Yt = t.$s({
    codexTurnMetadata: t.ic().optional(),
    method: t.nc().trim().min(1),
    params: t.ic().optional(),
  }),
  Xt = t.$s({ session_id: t.nc().trim().min(1), turn_id: t.nc().trim().min(1) }),
  Zt = t.Gr(`computer-use-native-pipe-server`),
  Qt = 4,
  $t = 8 * 1024 * 1024,
  en = `computer-use-approval:`,
  tn = `requestComputerUseApproval`,
  nn = 300 * 1e3;
async function rn({
  codexCliPath: e,
  nativePipeDirectory: n = Ut,
  onAnalyticsEvent: r,
  windowsHelperPath: i,
  windowsHelperTransportModulePath: a,
}) {
  if (i == null || a == null) throw Error(`Windows Computer Use helper paths are unavailable`);
  let o = null,
    s = !1,
    c = new Map(),
    l = new Map(),
    u = (e) => {
      let t = l.get(e);
      t != null && (r?.({ type: `ended`, ...t }), l.delete(e));
    },
    d = async () => (
      (o ??= new (await import((0, x.pathToFileURL)(a).href)).WindowsHelperTransport({
        helperArgs: [`--parent-pid`, String(process.pid)],
        helperCommand: i,
        helperEnv: e == null ? void 0 : { [t.La]: e },
      })),
      o
    ),
    f = async () => {
      let e = o;
      e != null &&
        ((o = null),
        await e.close(),
        Zt().debug(`computer-use native pipe helper transport closed`, { safe: {} }));
    },
    p = ln(r),
    m = await fn({
      onListening: (e) => {
        (Zt().debug(`computer-use native pipe listening`, { safe: {}, sensitive: { pipePath: e } }),
          r?.({ type: `server-launched`, transport: yt }));
      },
      onMessage: (e, t, n) => {
        let i = Jt.safeParse(e);
        if (!i.success || p.handleApprovalResponse(i.data)) return;
        let a = an(i.data);
        if (s && !a) {
          on(i.data, t);
          return;
        }
        (a || (s = !0),
          sn({
            activeTurnBySocket: c,
            activeTurnAnalyticsBySocket: l,
            closeHelperTransport: f,
            getHelperTransport: d,
            approvalBridge: p,
            message: i.data,
            onAnalyticsEvent: r,
            sendResponse: t,
            socket: n,
          }).finally(() => {
            a || (s = !1);
          }));
      },
      onLastSocketClosed: () => {
        (Zt().debug(`computer-use native pipe last socket closed`, { safe: {} }),
          p.rejectPendingApprovals(Error(`Computer Use native pipe client disconnected`)),
          f().catch((e) => {
            Zt().warning(`computer-use native pipe helper close failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }));
      },
      onSocketClose: (e) => {
        (u(e), c.delete(e));
      },
      onSocketError: (e) => {
        Zt().warning(`computer-use native pipe socket error`, {
          safe: {},
          sensitive: { error: e },
        });
      },
      pipePath: n,
    });
  return {
    pipePath: m.pipePath,
    closeActiveTurn: async (e) => {
      let t = mn(e);
      if (!pn(c, e)) return !1;
      for (let [e, n] of c) n === t && c.delete(e);
      return (await f(), !0);
    },
    hasActiveTurn: (e) => pn(c, e),
    dispose: async () => {
      p.rejectPendingApprovals(Error(`Computer Use native pipe is shutting down`));
      for (let e of l.keys()) u(e);
      try {
        await f();
      } finally {
        ((o = null), (s = !1), await m.dispose(), c.clear(), l.clear());
      }
    },
  };
}
function an(e) {
  if (`method` in e && e.method === `close`) return !0;
  if (!(`method` in e) || e.method !== `request`) return !1;
  let t = Yt.safeParse(e.params);
  return t.success && t.data.method === `end_turn`;
}
function on(e, t) {
  e.id != null &&
    t({
      error: { code: -32e3, message: `Computer Use helper already has an active request` },
      id: e.id,
      jsonrpc: `2.0`,
    });
}
async function sn({
  activeTurnBySocket: e,
  activeTurnAnalyticsBySocket: t,
  closeHelperTransport: n,
  getHelperTransport: r,
  approvalBridge: i,
  message: a,
  onAnalyticsEvent: o,
  sendResponse: s,
  socket: c,
}) {
  let l = a.id;
  if (l == null || !(`method` in a)) return;
  let u = null,
    d = null,
    f = null,
    p = null;
  try {
    if (a.method === `ping`) {
      s({ id: l, jsonrpc: `2.0`, result: `pong` });
      return;
    }
    if (a.method === `close`) {
      let r = t.get(c);
      (r != null && o?.({ type: `ended`, ...r }),
        t.delete(c),
        e.delete(c),
        Zt().debug(`computer-use native pipe close requested`, { safe: {} }),
        await n(),
        s({ id: l, jsonrpc: `2.0`, result: null }));
      return;
    }
    if (a.method !== `request`) throw Error(`Unsupported Computer Use native pipe method`);
    let m = cn(a.params),
      h = wt(m.method, m.params, m.codexTurnMetadata);
    ((d = Tt(m.codexTurnMetadata)),
      (f = h),
      un({
        activeTurnAnalyticsBySocket: t,
        activeTurnBySocket: e,
        analyticsFields: h,
        onAnalyticsEvent: o,
        request: m,
        socket: c,
      }),
      (u = w.performance.now()));
    let g = await (
      await r()
    ).request(m.method, m.params, {
      codexTurnMetadata: m.codexTurnMetadata,
      createElicitation: (e) => i.requestApprovalForSender(s, e),
    });
    ((p = `completed`), s({ id: l, jsonrpc: `2.0`, result: g }));
  } catch (e) {
    (u != null && p == null && (p = `failed`),
      s({
        error: { code: -32e3, message: e instanceof Error ? e.message : String(e) },
        id: l,
        jsonrpc: `2.0`,
      }));
  } finally {
    u != null &&
      d != null &&
      f != null &&
      p != null &&
      o?.({
        type: `tool-called`,
        ...f,
        ...d,
        terminalStatus: p,
        mcpErrorPresent: p === `failed`,
        durationMs: Math.round(w.performance.now() - u),
      });
  }
}
function cn(e) {
  let t = Yt.parse(e);
  return { codexTurnMetadata: t.codexTurnMetadata, method: t.method, params: t.params };
}
function ln(e) {
  let t = new Map();
  return {
    handleApprovalResponse: (n) => {
      let r = n.id;
      if (typeof r != `string` || !r.startsWith(en)) return !1;
      let i = t.get(r);
      if (i == null) return !0;
      if ((t.delete(r), clearTimeout(i.timeout), `error` in n && n.error != null))
        return (e?.(Dt(i.tool, void 0)), i.reject(Error(n.error.message)), !0);
      let a = `result` in n ? n.result : void 0;
      return (e?.(Dt(i.tool, a)), i.resolve(a), !0);
    },
    rejectPendingApprovals: (e) => {
      for (let n of t.values()) (clearTimeout(n.timeout), n.reject(e));
      t.clear();
    },
    requestApprovalForSender: (n, r) =>
      new Promise((i, a) => {
        let o = `${en}${(0, u.randomUUID)()}`,
          s = Et(r);
        e?.({ type: `app-approval-requested`, ...s });
        let c = setTimeout(() => {
          (t.delete(o),
            e?.({ type: `app-approval-resolved`, ...s, approvalResult: `timeout` }),
            a(Error(`Computer Use app approval timed out`)));
        }, nn);
        (c.unref(),
          t.set(o, { reject: a, resolve: i, timeout: c, tool: s }),
          n({ id: o, jsonrpc: `2.0`, method: tn, params: r }));
      }),
  };
}
function un({
  activeTurnAnalyticsBySocket: e,
  activeTurnBySocket: t,
  analyticsFields: n,
  onAnalyticsEvent: r,
  request: i,
  socket: a,
}) {
  let o = dn(i);
  if (o == null || t.get(a) === o) return;
  let s = e.get(a);
  (s != null && r?.({ type: `ended`, ...s }), t.set(a, o));
  let c = Ot(n);
  (e.set(a, c), r?.({ type: `started`, ...c }));
}
function dn(e) {
  let t = Xt.safeParse(e.codexTurnMetadata);
  return t.success ? mn({ sessionId: t.data.session_id, turnId: t.data.turn_id }) : null;
}
async function fn({
  onListening: e,
  onLastSocketClosed: t,
  onMessage: n,
  onSocketClose: r,
  onSocketError: i,
  pipePath: a,
}) {
  await _n(a);
  let o = new Set(),
    s = new Map(),
    c = g.default.createServer((e) => {
      (o.add(e), s.set(e, Buffer.alloc(0)));
      let a = (t) => {
        e.destroyed || e.write(hn(JSON.stringify(t)));
      };
      (e.on(`data`, (t) => {
        let r = Buffer.isBuffer(t) ? t : Buffer.from(t),
          i = s.get(e) ?? Buffer.alloc(0),
          o = gn(Buffer.concat([i, r]));
        if (o == null) {
          (s.delete(e), e.destroy());
          return;
        }
        s.set(e, o.remainingData);
        for (let t of o.messages)
          try {
            n(JSON.parse(t), a, e);
          } catch {
            continue;
          }
      }),
        e.on(`error`, i),
        e.on(`close`, () => {
          (r(e), o.delete(e), s.delete(e), o.size === 0 && t());
        }));
    });
  return (
    await new Promise((e, t) => {
      (c.once(`error`, t),
        c.listen(a, () => {
          (c.off(`error`, t), e());
        }));
    }),
    e(a),
    {
      pipePath: a,
      dispose: async () => {
        for (let e of o) e.destroy();
        (o.clear(),
          s.clear(),
          await new Promise((e) => {
            c.close(() => {
              e();
            });
          }),
          vn(a));
      },
    }
  );
}
function pn(e, t) {
  let n = mn(t);
  for (let t of e.values()) if (t === n) return !0;
  return !1;
}
function mn({ sessionId: e, turnId: t }) {
  return `${e}\0${t}`;
}
function hn(e) {
  let t = Buffer.from(e, `utf8`),
    n = Buffer.alloc(Qt + t.length);
  return (
    (0, o.endianness)() === `LE` ? n.writeUInt32LE(t.length, 0) : n.writeUInt32BE(t.length, 0),
    t.copy(n, Qt),
    n
  );
}
function gn(e) {
  let t = [],
    n = 0;
  for (; e.length - n >= Qt; ) {
    let r = (0, o.endianness)() === `LE` ? e.readUInt32LE(n) : e.readUInt32BE(n);
    if (r > $t) return null;
    let i = Qt + r;
    if (e.length - n < i) break;
    (t.push(e.subarray(n + Qt, n + i).toString(`utf8`)), (n += i));
  }
  return { messages: t, remainingData: e.subarray(n) };
}
async function _n(e) {
  (0, o.platform)() === `win32` || !(0, l.existsSync)(e) || (0, l.unlinkSync)(e);
}
function vn(e) {
  (0, o.platform)() !== `win32` && (0, l.existsSync)(e) && (0, l.unlinkSync)(e);
}
var yn = `--previous-notify`,
  bn = `turn-ended`,
  xn = `codex-computer-use.exe`;
async function Sn({ codexHome: e, windowsHelperPath: t }) {
  let n = s.default.join(e, `config.toml`),
    r = await wn(n),
    i = Cn(r, t);
  return i.content === r
    ? { status: `already-present` }
    : (await (0, p.mkdir)(s.default.dirname(n), { recursive: !0 }),
      await (0, p.writeFile)(n, i.content, `utf8`),
      { status: i.hadNotify ? `repaired` : `installed` });
}
function Cn(e, t) {
  let n = Tn(e),
    r = En(n),
    i = [t, bn];
  return (
    r != null && i.push(yn, JSON.stringify(r)),
    jn(n, i)
      ? { content: e, hadNotify: !0 }
      : { content: kn(e, F({ notify: i }).trimEnd()), hadNotify: n != null }
  );
}
async function wn(e) {
  try {
    return await (0, p.readFile)(e, `utf8`);
  } catch (e) {
    if (e instanceof Error && `code` in e && e.code === `ENOENT`) return ``;
    throw e;
  }
}
function Tn(e) {
  if (e.trim() === ``) return null;
  let n = t.Or(e, { integersAsBigInt: !1 });
  if (n.notify == null) return null;
  if (!An(n.notify)) throw Error(`Codex notify config must be a string array`);
  return n.notify;
}
function En(e) {
  return e == null ? null : Dn(e) ? En(On(e)) : e;
}
function Dn(e) {
  let [t, n] = e;
  return n === bn && t?.replaceAll(`\\`, `/`).split(`/`).at(-1)?.toLowerCase() === xn;
}
function On(e) {
  let t = e[e.indexOf(yn) + 1];
  if (t == null) return null;
  try {
    let e = JSON.parse(t);
    return An(e) ? e : null;
  } catch {
    return null;
  }
}
function kn(e, t) {
  let n = [],
    r = !0,
    i = 0;
  for (let t of e.split(/\r?\n/)) {
    let e = t.trimStart(),
      a = t.split(`[`).length - t.split(`]`).length;
    (r && i === 0 && e.startsWith(`[`) && (r = !1),
      r ? (i > 0 ? (i += a) : /^notify\s*=/.test(e) ? (i = a) : n.push(t)) : n.push(t));
  }
  let a = n.findIndex((e) => e.trimStart().startsWith(`[`)),
    o = a === -1 ? n : n.slice(0, a),
    s = a === -1 ? [] : n.slice(a);
  return `${[...o, t, ...s]
    .join(`
`)
    .trim()}\n`;
}
function An(e) {
  return Array.isArray(e) && e.every((e) => typeof e == `string`);
}
function jn(e, t) {
  return e != null && e.length === t.length && e.every((e, n) => e === t[n]);
}
var Mn = t.Gr(`computer-use-native-pipe`),
  Nn = null;
function Pn() {
  return Nn?.ensureReadyPipePath() ?? Promise.resolve(null);
}
function Fn({
  codexHome: e = t.Mr(),
  ensureNotifyConfig: n = Sn,
  codexCliPath: r,
  onAnalyticsEvent: i,
  platform: a = process.platform,
  resourcesPath: o = Ze({ env: process.env, resourcesPath: process.resourcesPath }),
  startServer: s = rn,
}) {
  let c = !1,
    l = null,
    u = null,
    d = null,
    f = null,
    p = {},
    m = async () => {
      let e = l;
      ((l = null), (u = null), e != null && (await e.dispose()));
    },
    h = () => ({ windowsHelperPath: t.Bn(o), windowsHelperTransportModulePath: t.Vn(o) }),
    g = (t) => {
      if (!c || a !== `win32` || l != null) return Promise.resolve();
      if (f != null) return f;
      let o = Promise.resolve()
        .then(async () => {
          try {
            let o = t ?? h(),
              { windowsHelperPath: d, windowsHelperTransportModulePath: f } = o;
            if (!c || a !== `win32`) return;
            if (d == null || f == null) {
              Mn().info(`computer-use native pipe helper paths unavailable`, {
                safe: {
                  missingHelperPath: d == null,
                  missingTransportModulePath: f == null,
                  platform: a,
                },
              });
              return;
            }
            try {
              let t = await n({ codexHome: e, windowsHelperPath: d });
              Mn().info(`computer-use notify config ensure finished`, {
                safe: { platform: a, status: t.status },
              });
            } catch (e) {
              throw (
                Mn().warning(`computer-use notify config ensure failed`, {
                  safe: { platform: a },
                  sensitive: { error: e },
                }),
                e
              );
            }
            let p = await s({
              codexCliPath: r,
              onAnalyticsEvent: i,
              windowsHelperPath: d,
              windowsHelperTransportModulePath: f,
            });
            if (!c || a !== `win32`) {
              await p.dispose();
              return;
            }
            ((l = p),
              (u = o),
              Mn().info(`computer-use native pipe startup ready`, {
                safe: { platform: a },
                sensitive: { pipePath: l.pipePath },
              }));
          } catch (e) {
            Mn().warning(`computer-use native pipe startup failed`, {
              safe: { platform: a },
              sensitive: { error: e },
            });
          }
        })
        .finally(() => {
          f === o && (f = null);
        });
      return ((f = o), o);
    },
    _ = () => {
      if (d != null) return d;
      let e = Promise.resolve()
        .then(async () => {
          try {
            if (!c || a !== `win32` || (f != null && (await f), !c || a !== `win32`)) return;
            if (l == null) {
              await g();
              return;
            }
            let e = h();
            if (
              u?.windowsHelperPath === e.windowsHelperPath &&
              u?.windowsHelperTransportModulePath === e.windowsHelperTransportModulePath
            )
              return;
            (Mn().info(`computer-use native pipe helper paths changed`, { safe: { platform: a } }),
              await m(),
              await g(e));
          } catch (e) {
            Mn().warning(`computer-use native pipe refresh failed`, {
              safe: { platform: a },
              sensitive: { error: e },
            });
          }
        })
        .finally(() => {
          d === e && (d = null);
        });
      return ((d = e), e);
    },
    v = {
      closeActiveTurn: (e) => l?.closeActiveTurn(e) ?? Promise.resolve(!1),
      ensureReadyPipePath: async () => (await g(), l?.pipePath ?? null),
      hasActiveTurn: (e) => l?.hasActiveTurn(e) ?? !1,
      refresh: () => {
        _();
      },
      setDesktopFeatureAvailability: (e) => {
        if (!(e.computerUse == null && e.computerUseNodeRepl == null)) {
          if (
            ((p = { ...p, ...e }), (c = p.computerUse === !0 && p.computerUseNodeRepl === !0), !c)
          ) {
            m();
            return;
          }
          g();
        }
      },
      dispose: () => {
        ((c = !1), m(), Nn === v && (Nn = null));
      },
    };
  return ((Nn = v), v);
}
var In = `NODE_REPL_HOST_SERVICES_PIPE_PATH`;
function Ln() {
  return `/tmp/codex-host-services-${(0, u.randomUUID)()}.sock`;
}
function Rn({
  env: e = process.env,
  isPackaged: n = !0,
  platform: r = process.platform,
  repoRoot: i = process.cwd(),
  resolveCodexPath: a = t.Hn,
  resolveNodePath: o = t.Un,
  resolveNodeReplPath: s = t.Wn,
  resolvePrimaryRuntimeNodePath: c = Hn,
  resourcesPath: l,
}) {
  let u = l ?? Ze({ env: e, resourcesPath: process.resourcesPath }),
    d = c(r),
    f =
      Vn({ platform: r, rawValue: e.CODEX_CLI_PATH, resolveWindowsAppsPath: a }) ??
      Bn({
        devRelativePathSegments: [`extension`, `bin`, `codex`],
        isPackaged: n,
        platform: r,
        repoRoot: i,
        resolveBundledPath: a,
        resourcesPath: u,
      }),
    p = Bn({
      devRelativePathSegments: null,
      isPackaged: n,
      platform: r,
      repoRoot: i,
      resolveBundledPath: o,
      resourcesPath: u,
    }),
    m =
      Vn({ platform: r, rawValue: e.CODEX_BROWSER_USE_NODE_PATH, resolveWindowsAppsPath: o }) ??
      (p.path == null && d != null ? { path: d, source: `primary-runtime` } : p),
    h =
      Vn({ platform: r, rawValue: e.CODEX_NODE_REPL_PATH, resolveWindowsAppsPath: s }) ??
      Bn({
        devRelativePathSegments: null,
        isPackaged: n,
        platform: r,
        repoRoot: i,
        resolveBundledPath: s,
        resourcesPath: u,
      });
  return {
    codexCliPath: f.path,
    codexCliPathSource: f.source,
    nodeModuleDirs: t.zn(u),
    nodePath: m.path,
    nodePathSource: m.source,
    nodeReplPath: h.path,
    nodeReplPathSource: h.source,
    platform: r,
  };
}
function zn({
  devRelativePathSegments: e,
  isPackaged: t,
  platform: n,
  repoRoot: r,
  resolveBundledPath: i,
  resourcesPath: a,
}) {
  let o = i(a);
  if (t || n === `win32` || o != null || e == null) return o;
  let c = s.default.join(r, ...e);
  return Un(c) ? c : null;
}
function Bn(e) {
  let t = zn(e);
  return { path: t, source: t == null ? `missing` : `bundled-or-dev` };
}
function Vn({ platform: e, rawValue: t, resolveWindowsAppsPath: n }) {
  if (t == null) return null;
  let r = t.trim();
  if (r.length === 0) return null;
  if (e === `win32` && Wn(r)) {
    let e = n(s.default.dirname(r));
    return e == null ? null : { path: e, source: `env-override-relocated` };
  }
  return { path: r, source: `env-override` };
}
function Hn(e) {
  if (e !== `win32`) return null;
  let n = s.default.join(
    (0, o.homedir)(),
    `.cache`,
    `codex-runtimes`,
    t.f,
    `dependencies`,
    `node`,
    `bin`,
    `node.exe`,
  );
  return Un(n) ? n : null;
}
function Un(e) {
  if (!(0, l.existsSync)(e)) return !1;
  try {
    return (0, l.statSync)(e).isFile()
      ? (process.platform === `win32` || (0, l.accessSync)(e, l.constants.X_OK), !0)
      : !1;
  } catch {
    return !1;
  }
}
function Wn(e) {
  return e.replaceAll(`/`, `\\`).toLowerCase().includes(`\\program files\\windowsapps\\`);
}
var Gn = t.Wr(`BrowserUseThreadConfig`),
  Kn = (0, c.promisify)(m.execFile),
  qn = { "features.js_repl": !1 },
  Jn = {
    [Lt]: {
      command: `./Codex Computer Use.app/Contents/SharedSupport/SkyComputerUseClient.app/Contents/MacOS/SkyComputerUseClient`,
      args: [`mcp`],
      cwd: `.`,
      enabled: !1,
    },
  },
  Yn = { serviceAppPath: null },
  Xn = [mt, at, ot, st],
  Zn = [ct, ht],
  Qn = new WeakMap(),
  $n = `Control the in-app browser in conjunction with the Browser Plugin.`,
  er = `Control the Chrome browser in conjunction with the Chrome Plugin. Prefer this method of controlling Chrome over alternatives (such as Computer Use) unless the user explicitly mentions an alternative.`,
  tr = `Control desktop apps on macOS through Computer Use.`;
async function nr({
  appServerConnection: e,
  appVersion: n,
  desktopFeatureAvailability: r,
  includePrivateProcessEnv: i = !0,
  isPackaged: a,
  repoRoot: o,
  resourcesPath: s,
  resolveCodexPath: c,
  resolveNodePath: l,
  resolveNodeReplPath: u,
  resolvePrimaryRuntimeNodePath: d,
  shouldUseWslPaths: f,
  platform: p,
  hostServicesPipePath: m = h.default.env.NODE_REPL_HOST_SERVICES_PIPE_PATH?.trim() || null,
  trustedBrowserClientSha256s: g = Ye,
}) {
  let _ = r.inAppBrowserUse || r.externalBrowserUse,
    v = r.computerUse && r.computerUseNodeRepl,
    y = dr(r);
  if (!_ && !v) return null;
  let b = Rn({
      isPackaged: a,
      platform: p,
      repoRoot: o,
      resolveCodexPath: c,
      resolveNodePath: l,
      resolveNodeReplPath: u,
      resolvePrimaryRuntimeNodePath: d,
      resourcesPath: s,
    }),
    x = v && b.platform === `darwin` ? Jn : null,
    S = v && b.platform === `darwin` ? await ir(e, t.Mr()) : Yn,
    C = v && b.platform === `win32` ? await Pn() : null,
    w = v && (b.platform === `darwin` || C != null);
  v &&
    b.platform === `win32` &&
    !w &&
    Gn.info(`computer_use_native_pipe_thread_config_skipped`, {
      safe: { platform: b.platform, reason: `not-ready` },
      sensitive: {},
    });
  let T = _ || (v && (b.platform === `darwin` || w)) ? g : [],
    E = cr({
      appVersion: n,
      computerUse: v,
      computerUsePaths: S,
      runtimePaths: b,
      sentryUserId: i ? lr(e.getCachedAuthToken?.()) : null,
      shouldUseWslPaths: f,
      availableBrowserUseBackends: y,
      computerUseNativePipePath: C,
      hostServicesPipePath: m,
      trustedBrowserClientSha256s: T,
    });
  return E == null ? x : (ar(b), pr([qn, E, x]));
}
function rr(e) {
  Qn.delete(e);
}
async function ir(e, t) {
  let n = Qn.get(e)?.get(t);
  if (n != null) return n;
  let r = Qn.get(e) ?? new Map(),
    i = e
      .listPlugins({})
      .then(({ marketplaces: e }) => qt({ codexHome: t, marketplaces: e }))
      .catch((n) => {
        let r = Qn.get(e);
        return (
          r?.get(t) === i && (r.delete(t), r.size === 0 && Qn.delete(e)),
          Gn.warning(`computer_use_installed_plugin_path_resolve_failed`, {
            safe: {},
            sensitive: { error: n },
          }),
          Kt()
        );
      });
  return (r.set(t, i), Qn.set(e, r), i);
}
async function ar(e) {
  let t = await or(e.nodePath);
  Gn.info(`browser_use_runtime_paths_selected`, {
    safe: {
      codexCliPathSource: e.codexCliPathSource,
      nodePathSource: e.nodePathSource,
      nodeReplPathSource: e.nodeReplPathSource,
      nodeVersionMajor: sr(t.value),
      nodeVersionStatus: t.status,
      platform: e.platform,
    },
    sensitive: {
      codexCliPath: e.codexCliPath,
      nodePath: e.nodePath,
      nodeReplPath: e.nodeReplPath,
      nodeVersionError: t.error,
      nodeVersionValue: t.value,
    },
  });
}
async function or(e) {
  if (e == null) return { status: `missing`, value: null };
  try {
    return {
      status: `ok`,
      value: (await Kn(e, [`--version`], { timeout: 2e3, windowsHide: !0 })).stdout.trim(),
    };
  } catch (e) {
    return { status: `failed`, value: null, error: e };
  }
}
function sr(e) {
  let t = e?.match(/^v?(\d+)/);
  return t?.[1] == null ? null : Number(t[1]);
}
function cr({
  appVersion: e,
  availableBrowserUseBackends: r,
  computerUse: i,
  computerUseNativePipePath: a,
  computerUsePaths: o,
  hostServicesPipePath: s,
  runtimePaths: c,
  sentryUserId: l,
  shouldUseWslPaths: u,
  trustedBrowserClientSha256s: d,
}) {
  if (c.nodeReplPath == null)
    return (
      Gn.info(`browser_use_setup_failed`, {
        safe: {
          backend: `node_repl`,
          phase: `runtime-paths`,
          platform: c.platform,
          reason: `node-repl-missing`,
        },
        sensitive: {},
      }),
      null
    );
  if (c.nodePath == null)
    return (
      Gn.info(`browser_use_setup_failed`, {
        safe: {
          backend: `node_repl`,
          phase: `runtime-paths`,
          platform: c.platform,
          reason: `node-missing`,
        },
        sensitive: {},
      }),
      null
    );
  t.Ua(c.platform) &&
    c.codexCliPath == null &&
    Gn.warning(`browser_use_codex_cli_missing_for_node_repl_sandbox`, { safe: {}, sensitive: {} });
  let f = mr(c.nodeModuleDirs, c.platform),
    p = t.Mr(),
    m = n.i.resolve(),
    g = m === n.i.Dev ? h.default.env[t.Ra] : void 0,
    _ = m === n.i.Dev ? h.default.env[t.Va] : void 0,
    v = n.i.isInternal(m) || h.default.env.NODE_REPL_TRACE_META === `1`,
    y = h.default.env[vt]?.trim(),
    b = {
      [nt]: r.join(`,`),
      ...(r.includes(`iab`) ? { [gt]: $n } : {}),
      ...(r.includes(`chrome`) ? { [_t]: er } : {}),
      ...(i && c.platform === `darwin` ? { [Rt]: tr } : {}),
      [rt]: m,
      [it]: e,
      ...fr(Xn),
      ...(m === n.i.Dev ? fr(Zn) : {}),
      ...(y ? { [vt]: y } : {}),
      ...(i
        ? Wt({
            nativePipeDirectory: a,
            nativePipeEnabled: a != null,
            serviceAppPath: c.platform === `darwin` ? o.serviceAppPath : null,
          })
        : {}),
      ...(i && c.platform === `darwin` && s != null ? { [In]: s } : {}),
    };
  return t.Ha({
    codexCliPath: c.codexCliPath,
    codexHome: p,
    extraEnv: b,
    nodeModuleDirs: f,
    nodePath: c.nodePath,
    nodeReplPath: u ? t.Nr(c.nodeReplPath) : c.nodeReplPath,
    platform: c.platform,
    requestMeta: g,
    sentryUserId: l,
    traceMeta: v,
    trustAllCode: _,
    trustedBrowserClientSha256s: d,
    shouldUseWslPaths: u,
  });
}
function lr(e) {
  let t = e?.split(`.`, 3)[1];
  if (t == null) return null;
  try {
    let e = JSON.parse(Buffer.from(t, `base64url`).toString(`utf8`));
    if (!ur(e)) return null;
    let n = e[`https://api.openai.com/auth`];
    if (!ur(n)) return null;
    let r = n.chatgpt_user_id;
    return typeof r == `string` && r.length > 0 ? r : null;
  } catch {
    return null;
  }
}
function ur(e) {
  return typeof e == `object` && !!e;
}
function dr(e) {
  let t = [];
  return (e.externalBrowserUse && t.push(`chrome`), e.inAppBrowserUse && t.push(`iab`), t);
}
function fr(e) {
  let t = {};
  for (let n of e) {
    let e = h.default.env[n];
    e != null && (t[n] = e);
  }
  return t;
}
function pr(e) {
  let t = {};
  for (let n of e) if (n != null) for (let [e, r] of Object.entries(n)) t[e] = r;
  return Object.keys(t).length === 0 ? null : t;
}
function mr(e, t) {
  return e.join(t === `win32` ? `;` : `:`);
}
var hr = `computerUseOverlay.usingComputer`,
  gr = `Codex is using your computer`,
  _r = `computerUseOverlay.escToCancel`,
  vr = `Esc to cancel`,
  yr = `computer-use`,
  br = `config.json`,
  xr = `#339cff`,
  Sr = `en`,
  Cr = new Map(),
  wr = t.Gr(`computer-use-config`);
function Tr(e) {
  return (
    e === `appearanceTheme` ||
    e === `appearanceLightChromeTheme` ||
    e === `appearanceDarkChromeTheme` ||
    e === `localeOverride`
  );
}
async function Er({ settingsStore: e, isPackaged: n = a.app.isPackaged, repoRoot: r }) {
  await Mr({
    codexHome: t.Mr(),
    config: Or({
      settingsStore: e,
      locale: kr(),
      localesDir: jr({ isPackaged: n, repoRoot: r }),
      shouldUseDarkColors: Ar(),
    }),
  });
}
function Dr({ settingsStore: e, isPackaged: t, repoRoot: n }) {
  Er({ settingsStore: e, isPackaged: t, repoRoot: n }).catch((e) => {
    wr().warning(`Failed to write Computer Use config`, { safe: {}, sensitive: { error: e } });
  });
}
function Or({ settingsStore: e, locale: n, localesDir: r, shouldUseDarkColors: i }) {
  let a = Br(e.getEffective(t._i.localeOverride.key)) ?? Br(n) ?? Sr,
    o = Ir(r, a);
  return {
    accentColor: Pr({ settingsStore: e, shouldUseDarkColors: i }),
    direction: Rr(a),
    locale: a,
    strings: { usingComputer: Lr(o, hr, gr), escToCancel: Lr(o, _r, vr) },
  };
}
function kr() {
  return typeof a.app.getLocale == `function` ? a.app.getLocale() : `en`;
}
function Ar() {
  return a.nativeTheme?.shouldUseDarkColors === !0;
}
function jr({ isPackaged: e, repoRoot: t }) {
  return e
    ? (0, s.join)(a.app.getAppPath(), `native-menu-locales`)
    : (0, s.join)(t, `webview`, `src`, `locales`);
}
async function Mr({ codexHome: e, config: t }) {
  let n = (0, s.join)(e, yr),
    r = (0, s.join)(n, br),
    i = (Cr.get(r) ?? Promise.resolve())
      .catch(() => {})
      .then(() => Nr({ dir: n, configPath: r, config: t }));
  Cr.set(r, i);
  try {
    await i;
  } finally {
    Cr.get(r) === i && Cr.delete(r);
  }
  return r;
}
async function Nr({ dir: e, configPath: t, config: n }) {
  let r = (0, s.join)(e, `${br}.${process.pid}.${Date.now()}.${(0, u.randomUUID)()}.tmp`);
  await (0, p.mkdir)(e, { recursive: !0 });
  try {
    (await (0, p.writeFile)(r, `${JSON.stringify(n)}\n`, `utf8`), await (0, p.rename)(r, t));
  } catch (e) {
    throw (await (0, p.unlink)(r).catch(() => {}), e);
  }
}
function Pr({ settingsStore: e, shouldUseDarkColors: n }) {
  let r = (
    Fr(e.getEffective(t.yi.theme.key) ?? `system`, n) === `light`
      ? e.getEffective(t.yi.lightChromeTheme.key)
      : e.getEffective(t.yi.darkChromeTheme.key)
  )?.accent;
  return zr(r) ? r : xr;
}
function Fr(e, t) {
  return e === `system` ? (t ? `dark` : `light`) : e;
}
function Ir(e, t) {
  let n = Br(e);
  if (n == null) return null;
  let r = t.replaceAll(`_`, `-`),
    i = r.split(`-`)[0],
    a = [(0, s.join)(n, `${r}.json`), i && i !== r ? (0, s.join)(n, `${i}.json`) : null];
  for (let e of a)
    if (e != null)
      try {
        let t = JSON.parse((0, l.readFileSync)(e, `utf8`));
        if (typeof t == `object` && t && !Array.isArray(t)) return t;
      } catch {}
  return null;
}
function Lr(e, t, n) {
  let r = e?.[t];
  return Br(typeof r == `string` ? r : null) ?? n;
}
function Rr(e) {
  try {
    return new Intl.Locale(e.replaceAll(`_`, `-`)).textInfo?.direction === `rtl` ? `rtl` : `ltr`;
  } catch {
    return `ltr`;
  }
}
function zr(e) {
  return /^#[0-9a-fA-F]{6}$/.test(e ?? ``);
}
function Br(e) {
  return e?.trim() || null;
}
var Vr = `electron-persisted-atom-state`,
  Hr = `electron-worktree-cleanup-buffer-start-ms`,
  Ur = `electron-avatar-overlay-open`,
  Wr = `electron-avatar-overlay-bounds`,
  Gr = `electron-remote-control-client-enrollments`,
  Kr = `electron-local-remote-control-environment-id`,
  qr = `electron-local-remote-control-installation-id`,
  Jr = `electron-chrome-extension-sync-managed-plugin-ids`,
  Yr = `electron-integrity-state-envelope`,
  Xr = 500,
  Zr = t.Gr(`global-state`),
  Qr = t.Hs.object({
    left: t.Hs.number(),
    top: t.Hs.number(),
    width: t.Hs.number().positive(),
    height: t.Hs.number().positive(),
  }),
  $r = t.Hs.object({
    x: t.Hs.number(),
    y: t.Hs.number(),
    width: t.Hs.number().positive(),
    height: t.Hs.number().positive(),
  }),
  ei = t.Hs.enum([`top-start`, `top-end`, `bottom-start`, `bottom-end`]),
  ti = t.Hs.object({
    x: t.Hs.number(),
    y: t.Hs.number(),
    width: t.Hs.number().positive(),
    height: t.Hs.number().positive(),
    anchor: $r.optional(),
    displayBounds: $r.optional(),
    displayId: t.Hs.number().optional(),
    mascot: Qr.optional(),
    placement: ei.optional(),
    tray: Qr.nullable().optional(),
  }),
  ni = ri({
    "electron-main-window-bounds": t.Hs.object({
      x: t.Hs.number(),
      y: t.Hs.number(),
      width: t.Hs.number(),
      height: t.Hs.number(),
      isMaximized: t.Hs.boolean().optional(),
    }),
    [Ur]: t.Hs.boolean(),
    [Wr]: ti.extend({
      byDisplayId: t.Hs.record(t.Hs.string(), ti).optional(),
      byResolution: t.Hs.record(t.Hs.string(), ti).optional(),
    }),
    [Vr]: t.Hs.record(t.Hs.string(), t.Hs.unknown()),
    [Gr]: t.Hs.record(
      t.Hs.string(),
      t.Hs.object({
        accountUserId: t.Hs.string(),
        algorithm: t.Hs.literal(`ecdsa_p256_sha256`),
        clientId: t.Hs.string(),
        keyId: t.Hs.string(),
        protectionClass: t.Hs.enum([
          `hardware_secure_enclave`,
          `hardware_tpm`,
          `os_protected_nonextractable`,
        ]),
        publicKeySpkiDerBase64: t.Hs.string(),
      }),
    ),
    [Kr]: t.Hs.string(),
    [qr]: t.Hs.string(),
    [Jr]: t.Hs.array(t.Hs.string()),
    [Yr]: t.Hs.string(),
    [Hr]: t.Hs.number(),
  });
function ri(e) {
  let t = Object.entries(e).map(([e, t]) => [e, t.optional()]);
  return Object.fromEntries(t);
}
var ii = class {
    state;
    filePath;
    persistDebounced;
    persistScheduledForAtoms = !1;
    persistQueued = Promise.resolve();
    constructor(e) {
      ((this.filePath = e),
        (this.state = si(this.filePath)),
        (this.persistDebounced = t.It(() => {
          this.persistScheduledForAtoms &&
            ((this.persistScheduledForAtoms = !1), ui(this.filePath, this.state));
        }, Xr)));
    }
    getStateFilePath() {
      return this.filePath;
    }
    get(e) {
      let n = this.state.get(e);
      return n !== void 0 && n != null ? n : (t.Io(e) ?? null);
    }
    getStored(e) {
      return this.state.get(e);
    }
    set(e, t) {
      (t === void 0 ? this.state.delete(e) : this.state.set(e, t), this.persistForKey(e));
    }
    update(e, t) {
      let n = t(this.state.has(e) ? this.state.get(e) : null);
      (n === void 0 ? this.state.delete(e) : this.state.set(e, n), this.persistForKey(e));
    }
    delete(e) {
      (this.state.delete(e), this.persistForKey(e));
    }
    flush() {
      return (
        (this.persistScheduledForAtoms &&= (this.persistDebounced.cancel(), !1)),
        (this.persistQueued = this.persistQueued
          .catch(() => void 0)
          .then(() => {
            ui(this.filePath, this.state);
          })),
        this.persistQueued
      );
    }
    persistForKey(e) {
      if (e === `electron-persisted-atom-state`) {
        ((this.persistScheduledForAtoms = !0), this.persistDebounced());
        return;
      }
      this.persistQueued = this.persistQueued
        .catch(() => void 0)
        .then(() => di(this.filePath, this.state));
    }
  },
  ai = new Set(Object.keys(ni));
function oi(e) {
  return ai.has(e);
}
function si(e) {
  if (!(0, l.existsSync)(e)) return new Map();
  let t = ci(e);
  if (t != null) return t;
  let n = ci(`${e}.bak`);
  return n == null
    ? new Map()
    : (Zr().warning(`Loaded global state from backup`, { safe: {}, sensitive: {} }), n);
}
function ci(e) {
  if (!(0, l.existsSync)(e)) return null;
  try {
    let t = (0, l.readFileSync)(e, `utf8`),
      n = JSON.parse(t);
    if (!n || typeof n != `object`) throw Error(`Expected global state to be a JSON object`);
    let r = [];
    for (let [e, t] of Object.entries(n)) {
      if (oi(e) && !ni[e].safeParse(t).success) {
        Zr().warning(`Dropping invalid global state for key`, { safe: { key: e }, sensitive: {} });
        continue;
      }
      r.push([e, t]);
    }
    return new Map(r);
  } catch (e) {
    return (
      Zr().warning(`Failed to load global state`, { safe: {}, sensitive: { error: e } }), null
    );
  }
}
function li(e) {
  let t = {};
  for (let [n, r] of e.entries()) r !== void 0 && (t[n] = r);
  return JSON.stringify(t);
}
function ui(e, t) {
  try {
    let n = li(t);
    (pi(e, n), pi(`${e}.bak`, n));
  } catch (e) {
    Zr().warning(`Failed to persist global state`, { safe: {}, sensitive: { error: e } });
  }
}
async function di(e, t) {
  try {
    let n = li(t);
    (await mi(e, n), await mi(`${e}.bak`, n));
  } catch (e) {
    Zr().warning(`Failed to persist global state`, { safe: {}, sensitive: { error: e } });
  }
}
function fi(e) {
  return (0, s.join)(
    (0, s.dirname)(e),
    `.${(0, s.basename)(e)}.tmp-${Date.now()}-${(0, u.randomUUID)()}`,
  );
}
function pi(e, t) {
  let n = fi(e);
  try {
    ((0, l.writeFileSync)(n, t, `utf8`), (0, l.renameSync)(n, e));
  } catch (e) {
    throw ((0, l.rmSync)(n, { force: !0 }), e);
  }
}
async function mi(e, t) {
  let n = fi(e);
  try {
    (await (0, p.writeFile)(n, t, `utf8`), await (0, p.rename)(n, e));
  } catch (e) {
    throw (await (0, p.rm)(n, { force: !0 }), e);
  }
}
async function hi({
  appServerClient: e,
  createSplitDirectories: n,
  directoryName: r,
  homeDirectory: i,
  prompt: a,
}) {
  let o = await e.platformPath();
  return t.Yi({
    createSplitDirectories: n,
    directoryName: r,
    fileSystem: _i(e),
    homeDirectory: i,
    path: o,
    prompt: a,
  });
}
async function gi({ appServerClient: e, homeDirectory: n }) {
  let r = await e.platformPath();
  return t.Xi({ fileSystem: _i(e), homeDirectory: n, path: r });
}
function _i(e) {
  return {
    createDirectory: async ({ path: n, recursive: r }) => {
      await t.jr.mkdir(n, { recursive: r }, e);
    },
    getMetadata: async (n) => {
      let r = await t.jr.lstat(n, e);
      return { isDirectory: r.isDirectory(), isSymlink: r.isSymbolicLink() };
    },
  };
}
var vi = `Response MUST end with a remark-directive block.

## Responding

- Answer the user normally and concisely. Explain what you found, what you did, and what the user should focus on now.
- Automations: use the memory file at \`$CODEX_HOME/automations/<automation_id>/memory.md\` (create it if missing).
  - Read it first (if present) to avoid repeating recent work, especially for "changes since last run" tasks.
  - Memory is important: some tasks must build on prior work, and others must avoid duplicating prior focus.
  - Before returning the directive, write a concise summary of what you did/decided plus the current run time.
  - Use the \`Automation ID:\` value provided in the message to locate/update this file.
- REQUIRED: End with a valid remark-directive block on its own line (not inline).
  - Always include an inbox item directive:
    \`::inbox-item{title="Sample title" summary="Place description here"}\`

## Choosing return value

- For recurring/bg threads (e.g., "pull datadog logs and fix any new bugs", "address the PR comments"):
  - Always return \`::inbox-item{...}\` with the title/summary the user should see.

## Guidelines

- Directives MUST be on their own line.
- Output exactly ONE inbox-item directive.
- Do NOT use invalid remark-directive formatting.
- DO NOT place commas between arguments.
  - Valid: \`::inbox-item{title="Sample title" summary="Place description here"}\`
  - Invalid: \`::inbox-item{title="Sample title",summary="Place description here"}\`
- When referring to files, use full absolute filesystem links in Markdown (not relative paths).
  - Valid: [\`/Users/alice/project/src/main.ts\`](/Users/alice/project/src/main.ts)
  - Invalid: \`src/main.ts\` or \`[main](src/main.ts)\`
- Try not to ask the user for more input if possible to infer.
- If a PR is opened by the automation, add the \`codex-automation\` label when available alongside the normal \`codex\` label.
- Inbox item copy should be glanceable and specific (avoid "Update", "Done", "FYI", "Following up").
  - Title: what this thread now _is_ (state + object). Aim ~4-8 words.
  - Title should explain what was built or what happened.
- Summary: what the user should _do/know next_ (next step, blocker, or waiting-on). Aim ~6-14 words.
- Summary should usually match the general automation name or prompt summary.
- Both title and summary should be fairly short; usually avoid one-word titles/summaries.
  - Prefer concrete nouns + verbs; include a crisp status cue when helpful: "blocked", "needs decision", "ready for review".

## Examples (inbox-item)

- Work needed:
  - \`::inbox-item{title="Fix flaky checkout tests" summary="Repro isolated; needs CI run + patch"}\`
- Waiting on user decision:
  - \`::inbox-item{title="Choose API shape for filters" summary="Two options drafted; pick A vs B"}\`
- Status update with next step:
  - \`::inbox-item{title="PR comments addressed" summary="Ready for re-review; focus on auth edge case"}\`
`,
  yi = `<heartbeat>
  <automation_id>{{AUTOMATION_ID}}</automation_id>
  <current_time_iso>{{NOW_ISO}}</current_time_iso>
  <instructions>
{{AUTOMATION_PROMPT}}
  </instructions>
</heartbeat>
`,
  bi = 3e4,
  xi = 3,
  Si = 2 * 6e4,
  Ci = 6e4,
  wi = 3e4,
  Ti = 10 * 6e4,
  Ei = `set_thread_archived`,
  Di = {
    namespace: `codex_app`,
    name: Ei,
    description: `Archive or unarchive a Codex thread.`,
    inputSchema: {
      type: `object`,
      additionalProperties: !1,
      properties: {
        threadId: {
          type: `string`,
          description: `Thread id to archive or unarchive. Omit to target the calling thread.`,
        },
        archived: { type: `boolean`, description: `Whether the thread should be archived.` },
      },
      required: [`archived`],
    },
  },
  Oi = { model: t.Es, reasoningEffort: t.Ds },
  ki = t.Gr(`automations`),
  Ai = async () => [],
  ji = new Set([`response_item`, `event_msg`, `item`, `unknown`]);
function Mi({ lastRunAt: e, threadUpdatedAt: t }) {
  let n = [e, t].filter((e) => e != null && Number.isFinite(e));
  return n.length === 0 ? null : Math.max(...n);
}
function Ni({ automation: e, thread: n }) {
  let r = t.Zn(e.rrule);
  if (r == null) return null;
  let i = Mi({
    lastRunAt: e.lastRunAt,
    threadUpdatedAt: Number.isFinite(n.updatedAt) ? n.updatedAt * 1e3 : null,
  });
  return i == null ? null : i + r;
}
function Pi({ automation: e, now: n }) {
  let r = t.Zn(e.rrule);
  return r == null ? t.Qn({ automation: e, now: n }) : n + r;
}
function Fi({ nextScheduledRunAt: e, now: t }) {
  return e == null ? t + Ci : Math.min(e, t + Ci);
}
function Ii({ rrule: e, now: n }) {
  let r = t.Zn(e);
  return {
    scheduleKind: r == null ? `wall_clock` : `interval`,
    intervalMs: r,
    nextScheduledRunAt: r == null ? t.$n({ rrule: e, now: n }) : n + r,
  };
}
function Li(e) {
  return e == null
    ? null
    : {
        ...e,
        settings: {
          ...e.settings,
          developer_instructions: t.rn({ baseInstructions: e.settings.developer_instructions }),
        },
      };
}
async function Ri(e, n, r) {
  let i = await n.getWorktreeRepository(e, t.ht(r));
  return i
    ? { type: `branch`, branchName: (await t.st(i))?.branch ?? `HEAD` }
    : { type: `branch`, branchName: `HEAD` };
}
async function zi({
  sourceCwd: e,
  worktreeCwd: n,
  gitManager: r,
  appServerClient: i,
  automationId: a,
}) {
  let o = t.ht(i),
    c = [n, (0, s.join)(t.Mr(), `automations`, a)],
    l = await r.getWorktreeRepository(t.xa(n), o),
    u = l?.root;
  u && u !== n && c.push(u);
  let d = await r.getWorktreeRepository(t.xa(e), o),
    f = d?.root;
  f && f !== n && f !== u && c.push(f);
  let p = (await r.getRepoRepository(t.xa(l?.root ?? e), o))?.getCommonDir();
  if (p) {
    let t = (0, s.isAbsolute)(p) ? p : (0, s.resolve)(f ?? e, p);
    return (c.push(t), c);
  }
  let m = l ?? d,
    h = m == null ? null : await t.dt(m);
  return (h ? c.push((0, s.dirname)(h)) : f && c.push((0, s.join)(f, `.git`)), c);
}
function Bi(e, n, r, i = null) {
  let a;
  a =
    n.sandbox_mode != null || n.approval_policy != null
      ? `custom`
      : t.Rs(t.Ls(n))
        ? `guardian-approvals`
        : `auto`;
  let o = i ?? a,
    s = t.zs(r, n),
    c =
      [a, `auto`, `granular`, `guardian-approvals`, `read-only`].find((e) => s.includes(e)) ??
      s.at(-1) ??
      `read-only`,
    l = s.includes(o) ? o : c,
    u = t.Fs(l, e, n),
    d = u.sandboxPolicy,
    f = u.approvalsReviewer;
  return {
    approvalPolicy:
      l !== `granular` &&
      f === `user` &&
      (r?.allowedApprovalPolicies == null || r.allowedApprovalPolicies.includes(`never`))
        ? `never`
        : u.approvalPolicy,
    approvalsReviewer: f,
    sandboxPolicy: d,
  };
}
function Vi(e, n) {
  let r = e.get(Vr)?.[t.Ns];
  if (typeof r != `object` || !r || Array.isArray(r)) return null;
  let i = Object.getOwnPropertyDescriptor(r, n)?.value,
    a = t.Ps.safeParse(i);
  return a.success ? a.data : null;
}
function Hi(e) {
  return {
    approvalPolicy: e.approvalPolicy,
    approvalsReviewer: e.approvalsReviewer,
    sandboxPolicy: t.Is(e.approvalPolicy).sandboxPolicy,
  };
}
function Ui(e, n, r) {
  let i = t.zs(r, n),
    a = t.Ls(n),
    o =
      n.sandbox_mode != null || n.approval_policy != null
        ? `custom`
        : t.Rs(a)
          ? `guardian-approvals`
          : `auto`,
    s = o;
  return (
    i.includes(s) ||
      (s =
        o === `auto` && i.includes(`guardian-approvals`)
          ? `guardian-approvals`
          : o === `guardian-approvals` && i.includes(`auto`)
            ? `auto`
            : i.includes(`read-only`)
              ? `read-only`
              : (i[0] ?? `read-only`)),
    t.Fs(s, e, n)
  );
}
function Wi(e, t) {
  return e.reduce((e, n, r) => e.then(() => t(n, r)), Promise.resolve());
}
function Gi({
  appVersion: e,
  appServerConnectionRegistry: n,
  getDesktopFeatureAvailability: r = () => Pe,
  gitManager: i,
  globalState: a,
  settingsStore: o,
  appServerClient: s,
  worktreeLifecycle: c,
  requestDynamicToolsForThreadStart: l = Ai,
  isPackaged: u = !0,
  repoRoot: d = process.cwd(),
  tickMs: f = bi,
  maxPerTick: p = xi,
}) {
  let m = !1,
    h = !1,
    g = new Map(),
    _ = new Map(),
    v = new Map(),
    y = async () => {
      if (!m) {
        m = !0;
        try {
          let f = n.getConnection(s.hostConfig.id),
            m = Date.now(),
            y = t.tr(m, p);
          await Promise.all(
            y.map(async (n) => {
              try {
                if (t.ks(n)) {
                  await Zi({
                    automation: n,
                    now: m,
                    appServerConnection: f,
                    heartbeatAutomationsEnabled: h,
                    heartbeatThreadRendererStates: g,
                    heartbeatThreadCollaborationModes: _,
                    heartbeatThreadPermissions: v,
                  });
                  return;
                }
                let p = t.Qn({ automation: n, now: m });
                (t.nr(n.id, p),
                  await qi({
                    appVersion: e,
                    automation: n,
                    appServerConnection: f,
                    desktopFeatureAvailability: r(),
                    gitManager: i,
                    globalState: a,
                    settingsStore: o,
                    appServerClient: s,
                    worktreeLifecycle: c,
                    requestDynamicToolsForThreadStart: l,
                    isPackaged: u,
                    repoRoot: d,
                  }));
              } catch (e) {
                ki().warning(`Scheduled run failed`, {
                  safe: { automationId: n.id, error: e },
                  sensitive: {},
                });
              }
            }),
          );
        } finally {
          m = !1;
        }
      }
    },
    b = () => {
      y().catch((e) => {
        ki().debug(`Automation scheduler tick threw`, { safe: { error: e }, sensitive: {} });
      });
    };
  try {
    t.gr() && n.getConnection(s.hostConfig.id).notifyAutomationRunsUpdated();
  } catch (e) {
    ki().warning(`Failed to settle interrupted automation runs`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  (t.qn(Date.now()), b());
  let x = setInterval(b, f);
  return (
    x.unref(),
    {
      dispose: () => {
        (clearInterval(x), g.clear(), _.clear(), v.clear());
      },
      setHeartbeatAutomationsEnabled: (e) => {
        if (((h = e), !e)) {
          (g.clear(), _.clear(), v.clear());
          return;
        }
        b();
      },
      setHeartbeatThreadRendererState: (e) => {
        let t = e.threadId?.trim() ?? ``;
        t.length !== 0 &&
          (g.set(t, { isEligible: e.isEligible, reason: e.reason, updatedAtMs: Date.now() }),
          e.collaborationMode != null && _.set(t, e.collaborationMode),
          e.permissions == null ? v.delete(t) : v.set(t, e.permissions));
      },
    }
  );
}
async function Ki({
  appVersion: e,
  automationId: n,
  appServerConnection: r,
  desktopFeatureAvailability: i = Pe,
  gitManager: a,
  globalState: o,
  settingsStore: s,
  appServerClient: c,
  worktreeLifecycle: l,
  requestDynamicToolsForThreadStart: u = Ai,
  isPackaged: d = !0,
  repoRoot: f = process.cwd(),
  collaborationMode: p = null,
  permissions: m = null,
}) {
  let h = t.Xn(n);
  if (!h) throw Error(`Automation not found.`);
  if (t.ks(h)) {
    await Qi({ automation: h, appServerConnection: r, collaborationMode: p, permissions: m });
    return;
  }
  let g = t.Qn({ automation: h, now: Date.now() });
  (t.nr(h.id, g),
    await qi({
      appVersion: e,
      automation: h,
      appServerConnection: r,
      desktopFeatureAvailability: i,
      gitManager: a,
      globalState: o,
      settingsStore: s,
      appServerClient: c,
      worktreeLifecycle: l,
      requestDynamicToolsForThreadStart: u,
      isPackaged: d,
      repoRoot: f,
    }));
}
async function qi({
  appVersion: e,
  automation: n,
  appServerConnection: r,
  desktopFeatureAvailability: i,
  gitManager: a,
  globalState: o,
  settingsStore: s,
  appServerClient: c,
  worktreeLifecycle: l,
  requestDynamicToolsForThreadStart: u,
  isPackaged: d,
  repoRoot: f,
}) {
  let p = n.cwds;
  if (p.length === 0) {
    ki().warning(`Scheduled run skipped: no folders configured`, {
      safe: { automationId: n.id },
      sensitive: {},
    });
    return;
  }
  let m = (await r.getConfigRequirements()).requirements,
    h = Oi;
  try {
    ((h = t.Os({
      automation: n,
      models: (await r.listModels({ includeHidden: !0, cursor: null, limit: 100 })).data,
    })),
      n.model != null &&
        n.model !== h.model &&
        ki().warning(`Automation model unavailable; falling back`, {
          safe: { automationId: n.id, requestedModel: n.model, fallbackModel: h.model },
          sensitive: {},
        }));
  } catch (e) {
    ki().warning(`Failed to load models for automation run`, {
      safe: { automationId: n.id, error: e },
      sensitive: {},
    });
  }
  let g = await Ji(u);
  await Wi(p, (t) =>
    Yi({
      appVersion: e,
      automation: n,
      appServerConnection: r,
      desktopFeatureAvailability: i,
      gitManager: a,
      globalState: o,
      settingsStore: s,
      appServerClient: c,
      worktreeLifecycle: l,
      configRequirements: m,
      cwd: t,
      automationModelSettings: h,
      dynamicTools: g,
      isPackaged: d,
      repoRoot: f,
    }).catch((e) => {
      ki().warning(`Scheduled run failed`, {
        safe: { automationId: n.id, error: e },
        sensitive: {},
      });
    }),
  );
}
async function Ji(e) {
  try {
    let t = await e();
    return t.some((e) => e.namespace === `codex_app` && e.name === Ei) ? t : [...t, Di];
  } catch (e) {
    return (
      ki().warning(`Failed to load dynamic tools for automation run`, {
        safe: { error: e },
        sensitive: {},
      }),
      [Di]
    );
  }
}
async function Yi({
  appVersion: e,
  automation: n,
  appServerConnection: r,
  desktopFeatureAvailability: i,
  gitManager: a,
  globalState: s,
  settingsStore: c,
  appServerClient: l,
  worktreeLifecycle: d,
  configRequirements: f,
  cwd: p,
  automationModelSettings: m,
  dynamicTools: h,
  isPackaged: g,
  repoRoot: _,
}) {
  let v = t.ht(l),
    y = `Automation: ${n.name}`,
    b = vi.trim(),
    x = p === `~`,
    S = p,
    C = null,
    w = null;
  if (x) {
    let e = await hi({
        appServerClient: l,
        createSplitDirectories: !0,
        homeDirectory: (0, o.homedir)(),
        prompt: n.prompt,
      }),
      r = t.Br(l.hostConfig);
    ((S = t.xa(I(e.cwd, r))), (C = I(e.outputDirectory, r)), (w = I(e.workspaceRoot, r)));
  }
  let T = await r.getUserSavedConfiguration(S),
    E = Vi(s, l.hostConfig.id),
    D =
      C == null
        ? null
        : t.Zi({ cwd: S, projectlessOutputDirectory: C, projectlessWorkspaceBrowserRoot: w }),
    O = [t.nn({ baseInstructions: b, isNonGitWorkspace: !0, threadToolsEnabled: !0 }), D].filter(
      (e) => !!e,
    ).join(`

`),
    k = Bi(x ? [] : [S], T, f, E),
    A = x ? Hi(k) : k,
    j =
      n.executionEnvironment === `worktree` &&
      !x &&
      (await a.getWorktreeRepository(S, v))?.root != null,
    M = async (n) => {
      let a = l.hostConfig;
      Dr({ settingsStore: c, isPackaged: g, repoRoot: _ });
      let o = await nr({
          appServerConnection: r,
          appVersion: e,
          desktopFeatureAvailability: i,
          cwd: n,
          isPackaged: g,
          repoRoot: _,
          shouldUseWslPaths: t.Br(a),
        }),
        s = {
          model: m.model,
          modelProvider: null,
          cwd: n,
          approvalPolicy: A.approvalPolicy,
          approvalsReviewer: A.approvalsReviewer,
          sandbox:
            A.sandboxPolicy.type === `dangerFullAccess`
              ? `danger-full-access`
              : A.sandboxPolicy.type === `readOnly`
                ? `read-only`
                : `workspace-write`,
          config: o,
          developerInstructions: O,
          personality: null,
          ephemeral: null,
          threadSource: `automation`,
          dynamicTools: h,
          mockExperimentalField: null,
          experimentalRawEvents: !1,
          serviceTier: null,
        };
      return r.startThread(s);
    },
    N = null,
    P = null,
    F = null,
    ee = `pending:${(0, u.randomUUID)()}`;
  (t.ur(n.id, ee, n.name, p), r.notifyAutomationRunsUpdated());
  try {
    if (j) {
      let e = await Ri(S, a, l),
        r = await t.at({
          gitManager: a,
          workspaceRoot: S,
          startingState: e,
          localEnvironmentConfigPath: n.localEnvironmentConfigPath,
          host: v,
        });
      if (!r.success) throw r.error;
      ((F = r.worktreeGitRoot), (N = await M(r.worktreeWorkspaceRoot)));
    } else N = await M(S);
    let e = N.thread.id;
    if (((P = e), F != null))
      try {
        (await d.setOwnerThread({ conversationId: t.Qo(e), worktree: F }), d.requestCleanup());
      } catch (e) {
        ki().warning(
          `Automation worktree thread started, but owner metadata could not be updated`,
          { safe: { automationId: n.id }, sensitive: { error: e } },
        );
      }
    (x &&
      (s.update(t.Fo.PROJECTLESS_THREAD_IDS, (t) => (t?.includes(e) ? t : [...(t ?? []), e])),
      w != null && s.update(t.Fo.THREAD_WORKSPACE_ROOT_HINTS, (t) => ({ ...t, [e]: w })),
      C != null && s.update(t.Fo.THREAD_PROJECTLESS_OUTPUT_DIRECTORIES, (t) => ({ ...t, [e]: C }))),
      t.mr(ee, P) || t.ur(n.id, P, n.name, p),
      r.notifyAutomationRunsUpdated(),
      Xi({ automation: n, threadId: P, appServerConnection: r }));
  } catch (e) {
    throw (t.pr(ee, `auto`), r.notifyAutomationRunsUpdated(), e);
  }
  if (N == null || P == null) throw Error(`Automation thread start failed`);
  let te = Bi(
      x
        ? []
        : await zi({
            sourceCwd: S,
            worktreeCwd: N.cwd,
            gitManager: a,
            appServerClient: l,
            automationId: n.id,
          }),
      T,
      f,
      E,
    ),
    ne = x ? Hi(te) : te,
    re = n.lastRunAt == null ? `never` : `${new Date(n.lastRunAt).toISOString()} (${n.lastRunAt})`,
    ie = `${y}\nAutomation ID: ${n.id}\nAutomation memory: $CODEX_HOME/automations/${n.id}/memory.md\nLast run: ${re}\n\n${n.prompt}`,
    ae = {
      threadId: P,
      input: [{ type: `text`, text: ie, text_elements: [] }],
      cwd: N.cwd,
      approvalPolicy: ne.approvalPolicy,
      approvalsReviewer: ne.approvalsReviewer,
      sandboxPolicy: ne.sandboxPolicy,
      model: m.model,
      effort: m.reasoningEffort,
      serviceTier: null,
      summary: t.Zr,
      personality: null,
      outputSchema: null,
      collaborationMode: null,
    };
  await r.startTurn(ae);
}
async function Xi({ automation: e, threadId: t, appServerConnection: n }) {
  try {
    await n.updateThreadTitle(t, e.name);
  } catch (n) {
    ki().warning(`Failed to set automation thread title`, {
      safe: { automationId: e.id, threadId: t, error: n },
      sensitive: {},
    });
  }
}
async function Zi({
  automation: e,
  now: n,
  appServerConnection: r,
  heartbeatAutomationsEnabled: i,
  heartbeatThreadRendererStates: a,
  heartbeatThreadCollaborationModes: o,
  heartbeatThreadPermissions: s,
}) {
  let c = Ii({ rrule: e.rrule, now: n });
  if (!i) {
    (t.rr(e.id, c.nextScheduledRunAt),
      ki().debug(`Heartbeat automation skipped: feature disabled`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `feature_disabled`,
          nextRunAt: c.nextScheduledRunAt,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let l = await r.readThread(e.targetThreadId).catch(() => null);
  if (l == null) {
    let r = Fi({ nextScheduledRunAt: c.nextScheduledRunAt, now: n });
    (t.rr(e.id, r),
      ki().warning(`Heartbeat automation skipped: thread missing`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `thread_missing`,
          nextRunAt: r,
          nextCooldownAt: null,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let u = Ni({ automation: e, thread: l }),
    d = aa({ expectedThreadId: e.targetThreadId, heartbeatThreadRendererStates: a });
  if (d != null) {
    let r = Fi({ nextScheduledRunAt: c.nextScheduledRunAt, now: n });
    (t.rr(e.id, r),
      ki().debug(`Heartbeat automation blocked by renderer state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: d,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let f = Li(ia(o, e.targetThreadId));
  if (f == null) {
    let r = Fi({ nextScheduledRunAt: c.nextScheduledRunAt, now: n });
    (t.rr(e.id, r),
      ki().debug(`Heartbeat automation waiting for renderer mode state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `missing_collaboration_mode`,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let p = await oa({ status: l.status, rolloutPath: l.path });
  if (p != null) {
    let r = Fi({ nextScheduledRunAt: c.nextScheduledRunAt, now: n });
    (t.rr(e.id, r),
      ki().debug(`Heartbeat automation blocked by thread state`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: p,
          nextRunAt: r,
          nextCooldownAt: u,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  if (u != null && u > n) {
    (t.rr(e.id, u),
      ki().debug(`Heartbeat automation skipped: not due yet`, {
        safe: {
          automationId: e.id,
          targetThreadId: e.targetThreadId,
          blockReason: `cooldown_not_elapsed`,
          nextRunAt: u,
          nextCooldownAt: u,
          remainingMs: u - n,
          ...c,
        },
        sensitive: {},
      }));
    return;
  }
  let m = Pi({ automation: e, now: n });
  (t.nr(e.id, m),
    await $i({
      automation: e,
      thread: l,
      appServerConnection: r,
      collaborationMode: f,
      permissions: s.get(e.targetThreadId) ?? null,
      waitForCompletion: !0,
    }));
}
async function Qi({ automation: e, appServerConnection: n, collaborationMode: r, permissions: i }) {
  let a = await n.readThread(e.targetThreadId).catch(() => null);
  if (a == null) throw Error(`Heartbeat thread not found.`);
  if ((await oa({ status: a.status, rolloutPath: a.path })) != null)
    throw Error(`Heartbeat thread is busy right now.`);
  let o = Pi({ automation: e, now: Date.now() });
  t.nr(e.id, o);
  let s = Li(r);
  if (s == null) throw Error(`Heartbeat thread mode is still loading.`);
  await $i({
    automation: e,
    thread: a,
    appServerConnection: n,
    collaborationMode: s,
    permissions: i,
    waitForCompletion: !1,
  });
}
async function $i({
  automation: e,
  thread: t,
  appServerConnection: n,
  collaborationMode: r,
  permissions: i,
  waitForCompletion: a,
}) {
  let o = await ea({ thread: t, appServerConnection: n }),
    s = i ?? (await ta({ appServerConnection: n, cwd: o.cwd })),
    c = na(e);
  await ca({
    appServerConnection: n,
    threadId: o.id,
    cwd: o.cwd,
    prompt: c,
    collaborationMode: r,
    approvalPolicy: s.approvalPolicy,
    approvalsReviewer: s.approvalsReviewer,
    sandboxPolicy: s.sandboxPolicy,
    model: null,
    effort: null,
    serviceTier: null,
    waitForCompletion: a,
  });
}
async function ea({ thread: e, appServerConnection: t }) {
  let n = await t.resumeThread(e.id, { cwd: e.cwd, path: e.path });
  return { id: n.thread.id, cwd: n.thread.cwd || n.cwd || e.cwd, path: n.thread.path ?? e.path };
}
async function ta({ appServerConnection: e, cwd: t }) {
  let [n, r] = await Promise.all([
    e.getUserSavedConfiguration(t),
    e.getConfigRequirements().catch(() => ({ requirements: null })),
  ]);
  return Ui([t], n, r.requirements);
}
function na(e) {
  return yi
    .replaceAll(`{{AUTOMATION_ID}}`, e.id)
    .replaceAll(`{{NOW_ISO}}`, new Date().toISOString())
    .replaceAll(`{{AUTOMATION_PROMPT}}`, e.prompt);
}
function ra(e, t) {
  let n = e.get(t);
  return n == null ? null : Date.now() - n.updatedAtMs > Si ? (e.delete(t), null) : n;
}
function ia(e, t) {
  return e.get(t) ?? null;
}
function aa({ expectedThreadId: e, heartbeatThreadRendererStates: t }) {
  let n = ra(t, e);
  return n == null || n.isEligible ? null : (n.reason ?? `renderer_ineligible`);
}
async function oa({ status: e, rolloutPath: t }) {
  if (e.type !== `active`) return null;
  if (e.activeFlags.includes(`waitingOnUserInput`)) return `waiting_on_user_input`;
  if (e.activeFlags.includes(`waitingOnApproval`)) return `waiting_on_approval`;
  if (e.activeFlags.length > 0) return `active_with_flags`;
  if (t == null || t.trim().length === 0) return `active_without_rollout_path`;
  let n = await sa(t);
  return n == null
    ? `active_without_terminal_event`
    : n === `task_complete`
      ? null
      : ji.has(n)
        ? `active_recent_rollout_activity`
        : `active_without_terminal_event`;
}
async function sa(e) {
  let { open: t } = await import(`node:fs/promises`),
    n;
  try {
    n = await t(e, `r`);
    let r = await n.stat();
    if (r.size === 0) return null;
    let i = Math.min(r.size, 256 * 1024),
      a = Buffer.alloc(i);
    await n.read(a, 0, i, r.size - i);
    let o = a.toString(`utf8`).split(`
`);
    r.size > i && o.shift();
    let s = null;
    for (let e of o) {
      let t = e.trim();
      if (t.length !== 0)
        try {
          let e = JSON.parse(t);
          (e.event === `task_complete` || e.event === `response_item` || e.event === `event_msg`) &&
            (s = e.event);
        } catch {
          continue;
        }
    }
    return s;
  } catch {
    return null;
  } finally {
    await n?.close().catch(() => void 0);
  }
}
async function ca({
  appServerConnection: e,
  threadId: n,
  cwd: r,
  prompt: i,
  collaborationMode: a,
  approvalPolicy: o,
  approvalsReviewer: s,
  sandboxPolicy: c,
  model: l,
  effort: u,
  serviceTier: d,
  waitForCompletion: f,
}) {
  let p = {
    threadId: n,
    input: [{ type: `text`, text: i, text_elements: [] }],
    cwd: r,
    approvalPolicy: o,
    approvalsReviewer: s,
    sandboxPolicy: c,
    model: l,
    effort: u,
    serviceTier: d,
    summary: t.Zr,
    personality: null,
    outputSchema: null,
    collaborationMode: a,
  };
  if (f) {
    await la({ appServerConnection: e, threadId: n, turnStartParams: p });
    return;
  }
  let m = ua({ appServerConnection: e, threadId: n });
  try {
    let t = await e.startTurn(p);
    await m.waitForTurn(t.turn.id);
  } catch (e) {
    throw (m.dispose(), e);
  }
}
async function la({ appServerConnection: e, threadId: t, turnStartParams: n }) {
  let r = null,
    i = () => {},
    a = new Promise((n, a) => {
      let o = setTimeout(() => {
          (i(), n());
        }, Ti),
        s = e.registerInternalNotificationHandler((e) => {
          da({
            notification: e,
            threadId: t,
            turnId: r,
            onTurnStarted: (e) => {
              r = e;
            },
            onCompleted: (e, t) => {
              if (((r = e), i(), t !== `completed`)) {
                a(Error(`Heartbeat automation did not complete.`));
                return;
              }
              n();
            },
          });
        });
      i = () => {
        (clearTimeout(o), s(), (i = () => {}));
      };
    });
  try {
    ((r = (await e.startTurn(n)).turn.id), await a);
  } catch (e) {
    throw (i(), e);
  }
}
function ua({ appServerConnection: e, threadId: t }) {
  let n = new Set(),
    r = null,
    i = null,
    a = null,
    o = null,
    s = !1,
    c = () => {
      r == null || !n.has(r) || a?.();
    },
    l = e.registerInternalNotificationHandler((e) => {
      da({
        notification: e,
        threadId: t,
        turnId: null,
        onTurnStarted: (e) => {
          (n.add(e), c());
        },
        onCompleted: (e) => {
          (n.add(e), c());
        },
      });
    }),
    u = new Promise((e, t) => {
      ((a = e), (o = t));
    }),
    d = () => {
      s || ((s = !0), i != null && (clearTimeout(i), (i = null)), l());
    };
  return {
    dispose: d,
    waitForTurn: async (e) => {
      ((r = e),
        (i = setTimeout(() => {
          o?.(Error(`Timed out waiting for heartbeat automation to start.`));
        }, wi)),
        c());
      try {
        await u;
      } finally {
        d();
      }
    },
  };
}
function da({ notification: e, threadId: t, turnId: n, onTurnStarted: r, onCompleted: i }) {
  switch (e.method) {
    case `turn/started`: {
      let n = e.params;
      if (n.threadId !== t) return;
      r(n.turn.id);
      return;
    }
    case `turn/completed`: {
      let r = e.params;
      if (r.threadId !== t || (n != null && r.turn.id !== n)) return;
      i(r.turn.id, r.turn.status);
      return;
    }
    default:
      return;
  }
}
var fa = [`features.js_repl`, `mcp_servers.${t.za}`, Lt];
async function pa({
  appServerConnection: e,
  appVersion: n,
  desktopFeatureAvailability: r,
  isPackaged: i,
  platform: a,
  repoRoot: o,
  resolveCodexPath: s,
  resolveNodePath: c,
  resolveNodeReplPath: l,
  resolvePrimaryRuntimeNodePath: u,
  resourcesPath: d,
}) {
  rr(e);
  let f = await nr({
    appServerConnection: e,
    appVersion: n,
    desktopFeatureAvailability: r,
    cwd: null,
    hostServicesPipePath: null,
    includePrivateProcessEnv: !1,
    isPackaged: i,
    platform: a,
    repoRoot: o,
    resolveCodexPath: s,
    resolveNodePath: c,
    resolveNodeReplPath: l,
    resolvePrimaryRuntimeNodePath: u,
    resourcesPath: d,
    shouldUseWslPaths: t.Br(e.hostConfig),
  });
  await e.sendAppServerRequest(`config/batchWrite`, {
    edits: ma(f),
    expectedVersion: null,
    filePath: null,
    reloadUserConfig: !0,
  });
}
function ma(e) {
  return fa.map((t) => ({ keyPath: t, mergeStrategy: `replace`, value: e?.[t] ?? null }));
}
var ha = `CODEX_USE_INTERNAL_CHROME_PLUGIN`,
  ga = (e) => e[ha] === `1`,
  _a = (e, t = process.env) => e === n.i.Dev && !ga(t),
  va = (e, t = process.env) => {
    switch (e) {
      case n.i.Dev:
        return ga(t);
      case n.i.InternalAlpha:
      case n.i.Nightly:
        return !0;
      case n.i.Agent:
      case n.i.Prod:
      case n.i.PublicBeta:
        return !1;
    }
  },
  ya = (e) => e === n.i.PublicBeta || e === n.i.Prod;
function ba(e) {
  return e === n.i.Nightly || e === n.i.InternalAlpha;
}
var xa = t.Wr(`BrowserUseLocalToBundledMigration`),
  Sa = t.Zs({
    name: t.nc(),
    source: t.Zs({ path: t.nc().trim().min(1), source: t.nc() }).optional(),
  }),
  Ca = t.Zs({ plugins: t.Gs(Sa) });
async function wa(e) {
  try {
    await Ta(e);
  } catch (e) {
    xa.warning(`browser_use_local_to_bundled_migration_failed`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
async function Ta(e) {
  let n = e.marketplaceName ?? `openai-bundled`,
    { marketplaces: r } = await e.appServerConnection.listPlugins({}),
    i = It({ marketplaces: r, marketplaceName: n, pluginName: t.wo });
  if (i == null) {
    xa.debug(`bundled_browser_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let a = Ea({ codexHome: e.codexHome, marketplaces: r });
  if (a == null) {
    xa.debug(`local_browser_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  (await e.appServerConnection.uninstallPlugin({ pluginId: a.plugin.id }),
    xa.info(`browser_use_local_to_bundled_migration_uninstalled_local`, { safe: {} }),
    await e.trashItem(a.plugin.source.path),
    xa.info(`browser_use_local_to_bundled_migration_moved_local_to_trash`, {
      safe: {},
      sensitive: { localPluginPath: a.plugin.source.path },
    }),
    await Da({
      codexHome: e.codexHome,
      localPluginPath: a.plugin.source.path,
      marketplacePath: a.marketplace.path,
    }),
    !i.plugin.installed &&
      (await e.appServerConnection.installPlugin({
        marketplacePath: i.marketplace.path,
        pluginName: t.wo,
      }),
      xa.info(`browser_use_local_to_bundled_migration_installed_bundled`, {
        safe: {},
        sensitive: { marketplacePath: i.marketplace.path },
      })));
}
function Ea(e) {
  let n = (0, s.join)(e.codexHome, `plugins`, t.To);
  for (let t of e.marketplaces)
    for (let e of t.plugins)
      if (
        e.name === `browser-use` &&
        e.installed &&
        t.path != null &&
        e.source.type === `local` &&
        ja(e.source.path, n)
      )
        return { marketplace: { ...t, path: t.path }, plugin: { ...e, source: e.source } };
  return null;
}
async function Da(e) {
  try {
    let t = await Oa(e.marketplacePath),
      n = t.plugins.filter(
        (t) => !ka({ codexHome: e.codexHome, localPluginPath: e.localPluginPath, plugin: t }),
      );
    if (n.length === t.plugins.length) {
      xa.debug(`browser_use_local_to_bundled_migration_marketplace_entry_missing`, {
        safe: {},
        sensitive: { marketplacePath: e.marketplacePath },
      });
      return;
    }
    (await p.default.writeFile(
      e.marketplacePath,
      JSON.stringify({ ...t, plugins: n }, null, 2) +
        `
`,
      `utf8`,
    ),
      xa.info(`browser_use_local_to_bundled_migration_removed_marketplace_entry`, {
        safe: {},
        sensitive: { marketplacePath: e.marketplacePath },
      }));
  } catch (t) {
    xa.warning(`browser_use_local_to_bundled_migration_marketplace_cleanup_failed`, {
      safe: {},
      sensitive: { error: t, marketplacePath: e.marketplacePath },
    });
  }
}
async function Oa(e) {
  return Ca.parseAsync(JSON.parse(await p.default.readFile(e, `utf8`)));
}
function ka(e) {
  return (
    e.plugin.name === `browser-use` &&
    e.plugin.source?.source === `local` &&
    Aa({
      codexHome: e.codexHome,
      localPluginPath: e.localPluginPath,
      sourcePath: e.plugin.source.path,
    })
  );
}
function Aa(e) {
  return [
    (0, s.resolve)(e.sourcePath),
    (0, s.resolve)(e.codexHome, e.sourcePath),
    (0, s.resolve)((0, s.dirname)(e.codexHome), e.sourcePath),
  ].some((t) => ja(t, e.localPluginPath));
}
function ja(e, t) {
  return (0, s.resolve)(e) === (0, s.resolve)(t);
}
var Ma = (0, c.promisify)(m.execFile),
  Na = `Codex Computer Use.app`,
  Pa = `--previous-notify`,
  Fa = t.Wr(`ComputerUseLocalToBundledMigration`),
  Ia = t.Zs({ name: t.Xs(t.go), version: t.nc().trim().min(1) }),
  La = t.Zs({
    name: t.nc(),
    source: t.Zs({ path: t.nc().trim().min(1), source: t.nc() }).optional(),
  }),
  Ra = t.Zs({ plugins: t.Gs(La) });
async function za(e) {
  try {
    await Ba(e);
  } catch (e) {
    Fa.warning(`computer_use_local_to_bundled_migration_failed`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
async function Ba(e) {
  let n = e.marketplaceName ?? `openai-bundled`,
    { marketplaces: r } = await e.appServerConnection.listPlugins({}),
    i = It({ marketplaces: r, marketplaceName: n, pluginName: t.go });
  if (i == null) {
    Fa.debug(`bundled_computer_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let a = Va({ codexHome: e.codexHome, marketplaces: r });
  if (a == null) {
    Fa.debug(`local_computer_use_plugin_missing_for_migration`, { safe: {} });
    return;
  }
  let o = await oo(a.plugin.source.path),
    c = o != null && o > 0,
    l = c ? !1 : await (e.isMacAppNotarized ?? lo)((0, s.join)(a.plugin.source.path, Na));
  if (!l && !c) {
    Fa.info(`local_computer_use_plugin_kept_for_development`, {
      safe: { localBuildNumber: o },
      sensitive: { localPluginPath: a.plugin.source.path },
    });
    return;
  }
  (await e.appServerConnection.uninstallPlugin({ pluginId: a.plugin.id }),
    Fa.info(`computer_use_local_to_bundled_migration_uninstalled_local`, {
      safe: { localBuildNumber: o, reason: l ? `gatekeeper_accepted` : `versioned_build` },
    }),
    await e.trashItem(a.plugin.source.path),
    Fa.info(`computer_use_local_to_bundled_migration_moved_local_to_trash`, {
      safe: { localBuildNumber: o, reason: l ? `gatekeeper_accepted` : `versioned_build` },
      sensitive: { localPluginPath: a.plugin.source.path },
    }),
    await Ha({
      codexHome: e.codexHome,
      localPluginPath: a.plugin.source.path,
      marketplacePath: a.marketplace.path,
    }),
    await Wa({ codexHome: e.codexHome, localPluginPath: a.plugin.source.path }),
    !i.plugin.installed &&
      (await e.appServerConnection.installPlugin({
        marketplacePath: i.marketplace.path,
        pluginName: t.go,
      }),
      Fa.info(`computer_use_local_to_bundled_migration_installed_bundled`, {
        safe: {},
        sensitive: { marketplacePath: i.marketplace.path },
      })));
}
function Va(e) {
  let n = (0, s.join)(e.codexHome, `plugins`, t.go);
  for (let t of e.marketplaces)
    for (let e of t.plugins)
      if (
        e.name === `computer-use` &&
        e.installed &&
        t.path != null &&
        e.source.type === `local` &&
        uo(e.source.path, n)
      )
        return { marketplace: { ...t, path: t.path }, plugin: { ...e, source: e.source } };
  return null;
}
async function Ha(e) {
  try {
    let t = await Ua(e.marketplacePath),
      n = t.plugins.filter(
        (t) => !io({ codexHome: e.codexHome, localPluginPath: e.localPluginPath, plugin: t }),
      );
    if (n.length === t.plugins.length) {
      Fa.debug(`computer_use_local_to_bundled_migration_marketplace_entry_missing`, {
        safe: {},
        sensitive: { marketplacePath: e.marketplacePath },
      });
      return;
    }
    (await p.default.writeFile(
      e.marketplacePath,
      JSON.stringify({ ...t, plugins: n }, null, 2) +
        `
`,
      `utf8`,
    ),
      Fa.info(`computer_use_local_to_bundled_migration_removed_marketplace_entry`, {
        safe: {},
        sensitive: { marketplacePath: e.marketplacePath },
      }));
  } catch (t) {
    Fa.warning(`computer_use_local_to_bundled_migration_marketplace_cleanup_failed`, {
      safe: {},
      sensitive: { error: t, marketplacePath: e.marketplacePath },
    });
  }
}
async function Ua(e) {
  return Ra.parseAsync(JSON.parse(await p.default.readFile(e, `utf8`)));
}
async function Wa(e) {
  let t = (0, s.join)(e.codexHome, `config.toml`);
  try {
    let n = await p.default.readFile(t, `utf8`).catch((e) => {
      if (po(e, `ENOENT`)) return null;
      throw e;
    });
    if (n == null) return;
    let r = Ga({ content: n, localPluginPath: e.localPluginPath });
    if (r === n) return;
    (await p.default.writeFile(t, r, `utf8`),
      Fa.info(`computer_use_local_to_bundled_migration_removed_config`, {
        safe: {},
        sensitive: { configPath: t },
      }));
  } catch (e) {
    Fa.warning(`computer_use_local_to_bundled_migration_config_cleanup_failed`, {
      safe: {},
      sensitive: { configPath: t, error: e },
    });
  }
}
function Ga(e) {
  let n = t.Or(e.content),
    r = e.content;
  Ka({ config: n, localPluginPath: e.localPluginPath }) && (r = Za(r, eo));
  let i = qa({ config: n, localPluginPath: e.localPluginPath });
  return (i.changed && (r = Qa(r, i.notify)), ro(r));
}
function Ka(e) {
  let n = e.config.mcp_servers?.[t.go]?.command;
  return typeof n == `string` && fo(n, e.localPluginPath);
}
function qa(e) {
  return Xa(e.config.notify)
    ? Ja({ localPluginPath: e.localPluginPath, notify: e.config.notify })
    : { changed: !1 };
}
function Ja(e) {
  let t = e.notify[0];
  if (t == null || !fo(t, e.localPluginPath)) return { changed: !1 };
  let n = Ya(e.notify);
  if (n == null) return { changed: !0, notify: null };
  let r = Ja({ localPluginPath: e.localPluginPath, notify: n });
  return { changed: !0, notify: r.changed ? r.notify : n };
}
function Ya(e) {
  let t = e.indexOf(Pa);
  if (t < 0 || t + 1 >= e.length) return null;
  try {
    let n = JSON.parse(e[t + 1] ?? ``);
    return Xa(n) ? n : null;
  } catch {
    return null;
  }
}
function Xa(e) {
  return Array.isArray(e) && e.every((e) => typeof e == `string`);
}
function Za(e, t) {
  let n = e.split(/\r?\n/),
    r = [],
    i = !1;
  for (let e of n) (to(e) && ((i = t(e)), i)) || i || r.push(e);
  return r.join(`
`);
}
function Qa(e, t) {
  let n = $a(e.split(/\r?\n/));
  if (t == null)
    return n.join(`
`);
  let r = `notify = ${JSON.stringify(t)}`,
    i = n.findIndex(to);
  if (i < 0)
    return [...n, r].join(`
`);
  let a = [...n];
  return (
    a.splice(i, 0, r),
    i + 1 < a.length && a[i + 1]?.trim() !== `` && a.splice(i + 1, 0, ``),
    a.join(`
`)
  );
}
function $a(e) {
  let t = [],
    n = !1;
  for (let r = 0; r < e.length; r += 1) {
    let i = e[r] ?? ``;
    if (to(i)) return (t.push(...e.slice(r)), t);
    if (n) {
      i.includes(`]`) && (n = !1);
      continue;
    }
    if (no(i)) {
      i.includes(`[`) && !i.includes(`]`) && (n = !0);
      continue;
    }
    t.push(i);
  }
  return t;
}
function eo(e) {
  return /^\s*\[\s*mcp_servers\s*\.\s*(?:"computer-use"|computer-use)\s*\]\s*$/.test(e);
}
function to(e) {
  return /^\s*\[/.test(e);
}
function no(e) {
  let t = e.trimStart();
  return t.startsWith(`notify `) || t.startsWith(`notify=`);
}
function ro(e) {
  let t = e.trimEnd();
  return t === `` ? `` : `${t}\n`;
}
function io(e) {
  return (
    e.plugin.name === `computer-use` &&
    e.plugin.source?.source === `local` &&
    ao({
      codexHome: e.codexHome,
      localPluginPath: e.localPluginPath,
      sourcePath: e.plugin.source.path,
    })
  );
}
function ao(e) {
  return [
    (0, s.resolve)(e.sourcePath),
    (0, s.resolve)(e.codexHome, e.sourcePath),
    (0, s.resolve)((0, s.dirname)(e.codexHome), e.sourcePath),
  ].some((t) => uo(t, e.localPluginPath));
}
async function oo(e) {
  try {
    return co((await so(e)).version);
  } catch (t) {
    return (
      Fa.warning(`computer_use_plugin_manifest_read_failed`, {
        safe: {},
        sensitive: { error: t, pluginPath: e },
      }),
      null
    );
  }
}
async function so(e) {
  let t = (0, s.join)(e, `.codex-plugin`, `plugin.json`);
  return Ia.parseAsync(JSON.parse(await p.default.readFile(t, `utf8`)));
}
function co(e) {
  let t = e.match(/^\d+\.\d+\.(\d+)(?:[-+].*)?$/);
  return t == null ? null : Number(t[1]);
}
async function lo(e) {
  if (process.platform !== `darwin`) return !1;
  try {
    return (
      await Ma(`/usr/sbin/spctl`, [`--assess`, `--type`, `execute`, `--verbose=4`, e], {
        env: t.Vr(process.env),
        timeout: 5e3,
      }),
      !0
    );
  } catch {
    return !1;
  }
}
function uo(e, t) {
  return (0, s.resolve)(e) === (0, s.resolve)(t);
}
function fo(e, t) {
  let n = (0, s.resolve)(e),
    r = (0, s.resolve)(t);
  return n === r || n.startsWith(`${r}${s.sep}`);
}
function po(e, t) {
  return typeof e == `object` && !!e && `code` in e && e.code === t;
}
var mo = [
    {
      autoInstallOptOutKey: t.xo(t.yo),
      installWhenMissing: !0,
      name: t.yo,
      isAvailable: ({ features: e }) => e.sites,
    },
    {
      autoInstallOptOutKey: t.xo(t.mo),
      forceReload: !0,
      installWhenMissing: !0,
      name: t.mo,
      isAvailable: ({ features: e }) => e.inAppBrowserUseAllowed,
      migrate: wa,
    },
    {
      forceReload: !0,
      name: Pt,
      syncInstallStateWithChromeExtension: !0,
      isAvailable: ({ buildFlavor: e, env: t, features: n }) =>
        _a(e, t) && n.externalBrowserUseAllowed,
    },
    {
      forceReload: !0,
      name: t.ho,
      syncInstallStateWithChromeExtension: !0,
      isAvailable: ({ buildFlavor: e, env: t, features: n }) =>
        va(e, t) && n.externalBrowserUseAllowed,
    },
    {
      forceReload: !0,
      name: Nt,
      syncInstallStateWithChromeExtension: !0,
      isAvailable: ({ buildFlavor: e, features: t }) => t.externalBrowserUseAllowed && ya(e),
    },
    {
      name: t.go,
      isAvailable: ({ features: e, platform: t }) => t === `darwin` && e.computerUse,
      migrate: za,
    },
    { name: t.go, isAvailable: ({ features: e, platform: t }) => t === `win32` && e.computerUse },
    {
      name: t.vo,
      isAvailable: ({ buildFlavor: e, features: t, platform: n }) =>
        n === `darwin` && t.computerUse && ba(e),
    },
    { name: t._o, isAvailable: () => !0 },
    ...[],
  ],
  ho = e.n((e) => {
    (Object.defineProperty(e, `__esModule`, { value: !0 }), (e.sync = e.isexe = void 0));
    var t = require(`fs`),
      n = require(`fs/promises`);
    ((e.isexe = async (e, t = {}) => {
      let { ignoreErrors: i = !1 } = t;
      try {
        return r(await (0, n.stat)(e), t);
      } catch (e) {
        let t = e;
        if (i || t.code === `EACCES`) return !1;
        throw t;
      }
    }),
      (e.sync = (e, n = {}) => {
        let { ignoreErrors: i = !1 } = n;
        try {
          return r((0, t.statSync)(e), n);
        } catch (e) {
          let t = e;
          if (i || t.code === `EACCES`) return !1;
          throw t;
        }
      }));
    var r = (e, t) => e.isFile() && i(e, t),
      i = (e, t) => {
        let n = t.uid ?? process.getuid?.(),
          r = t.groups ?? process.getgroups?.() ?? [],
          i = t.gid ?? process.getgid?.() ?? r[0];
        if (n === void 0 || i === void 0) throw Error(`cannot get uid or gid`);
        let a = new Set([i, ...r]),
          o = e.mode,
          s = e.uid,
          c = e.gid;
        return !!(o & 1 || (o & 8 && a.has(c)) || (o & 64 && s === n) || (o & 72 && n === 0));
      };
  }),
  go = e.n((e) => {
    (Object.defineProperty(e, `__esModule`, { value: !0 }), (e.sync = e.isexe = void 0));
    var t = require(`fs`),
      n = require(`fs/promises`);
    ((e.isexe = async (e, t = {}) => {
      let { ignoreErrors: r = !1 } = t;
      try {
        return i(await (0, n.stat)(e), e, t);
      } catch (e) {
        let t = e;
        if (r || t.code === `EACCES`) return !1;
        throw t;
      }
    }),
      (e.sync = (e, n = {}) => {
        let { ignoreErrors: r = !1 } = n;
        try {
          return i((0, t.statSync)(e), e, n);
        } catch (e) {
          let t = e;
          if (r || t.code === `EACCES`) return !1;
          throw t;
        }
      }));
    var r = (e, t) => {
        let { pathExt: n = process.env.PATHEXT || `` } = t,
          r = n.split(`;`);
        if (r.indexOf(``) !== -1) return !0;
        for (let t = 0; t < r.length; t++) {
          let n = r[t].toLowerCase(),
            i = e.substring(e.length - n.length).toLowerCase();
          if (n && i === n) return !0;
        }
        return !1;
      },
      i = (e, t, n) => e.isFile() && r(t, n);
  }),
  _o = e.n((e) => {
    Object.defineProperty(e, `__esModule`, { value: !0 });
  }),
  vo = e.n((e) => {
    var t =
        (e && e.__createBinding) ||
        (Object.create
          ? function (e, t, n, r) {
              r === void 0 && (r = n);
              var i = Object.getOwnPropertyDescriptor(t, n);
              ((!i || (`get` in i ? !t.__esModule : i.writable || i.configurable)) &&
                (i = {
                  enumerable: !0,
                  get: function () {
                    return t[n];
                  },
                }),
                Object.defineProperty(e, r, i));
            }
          : function (e, t, n, r) {
              (r === void 0 && (r = n), (e[r] = t[n]));
            }),
      n =
        (e && e.__setModuleDefault) ||
        (Object.create
          ? function (e, t) {
              Object.defineProperty(e, `default`, { enumerable: !0, value: t });
            }
          : function (e, t) {
              e.default = t;
            }),
      r =
        (e && e.__importStar) ||
        function (e) {
          if (e && e.__esModule) return e;
          var r = {};
          if (e != null)
            for (var i in e)
              i !== `default` && Object.prototype.hasOwnProperty.call(e, i) && t(r, e, i);
          return (n(r, e), r);
        },
      i =
        (e && e.__exportStar) ||
        function (e, n) {
          for (var r in e)
            r !== `default` && !Object.prototype.hasOwnProperty.call(n, r) && t(n, e, r);
        };
    (Object.defineProperty(e, `__esModule`, { value: !0 }),
      (e.sync = e.isexe = e.posix = e.win32 = void 0));
    var a = r(ho());
    e.posix = a;
    var o = r(go());
    ((e.win32 = o), i(_o(), e));
    var s = (process.env._ISEXE_TEST_PLATFORM_ || process.platform) === `win32` ? o : a;
    ((e.isexe = s.isexe), (e.sync = s.sync));
  }),
  yo = e.n((e, t) => {
    var { isexe: n, sync: r } = vo(),
      { join: i, delimiter: a, sep: o, posix: s } = require(`path`),
      c = process.platform === `win32`,
      l = new RegExp(`[${s.sep}${o === s.sep ? `` : o}]`.replace(/(\\)/g, `\\$1`)),
      u = RegExp(`^\\.${l.source}`),
      d = (e) => Object.assign(Error(`not found: ${e}`), { code: `ENOENT` }),
      f = (
        e,
        { path: t = process.env.PATH, pathExt: n = process.env.PATHEXT, delimiter: r = a },
      ) => {
        let i = e.match(l) ? [``] : [...(c ? [process.cwd()] : []), ...(t || ``).split(r)];
        if (c) {
          let t = n || [`.EXE`, `.CMD`, `.BAT`, `.COM`].join(r),
            a = t.split(r).flatMap((e) => [e, e.toLowerCase()]);
          return (
            e.includes(`.`) && a[0] !== `` && a.unshift(``),
            { pathEnv: i, pathExt: a, pathExtExe: t }
          );
        }
        return { pathEnv: i, pathExt: [``] };
      },
      p = (e, t) => {
        let n = /^".*"$/.test(e) ? e.slice(1, -1) : e;
        return (!n && u.test(t) ? t.slice(0, 2) : ``) + i(n, t);
      },
      m = async (e, t = {}) => {
        let { pathEnv: r, pathExt: i, pathExtExe: a } = f(e, t),
          o = [];
        for (let s of r) {
          let r = p(s, e);
          for (let e of i) {
            let i = r + e;
            if (await n(i, { pathExt: a, ignoreErrors: !0 })) {
              if (!t.all) return i;
              o.push(i);
            }
          }
        }
        if (t.all && o.length) return o;
        if (t.nothrow) return null;
        throw d(e);
      };
    ((t.exports = m),
      (m.sync = (e, t = {}) => {
        let { pathEnv: n, pathExt: i, pathExtExe: a } = f(e, t),
          o = [];
        for (let s of n) {
          let n = p(s, e);
          for (let e of i) {
            let i = n + e;
            if (r(i, { pathExt: a, ignoreErrors: !0 })) {
              if (!t.all) return i;
              o.push(i);
            }
          }
        }
        if (t.all && o.length) return o;
        if (t.nothrow) return null;
        throw d(e);
      }));
  }),
  bo = e.o(yo()),
  xo = process.platform === `darwin`,
  So = process.platform === `win32`;
function Co(e) {
  let t = bo.default.sync(e, { nothrow: !0 });
  return typeof t == `string` && (0, l.existsSync)(t) ? t : So ? Oo(e) : null;
}
function wo(e) {
  let t = e;
  for (;;) {
    if ((0, l.existsSync)(t)) return t;
    let e = (0, s.dirname)(t);
    if (e === t) return null;
    t = e;
  }
}
function To(e) {
  if (xo) return [`-R`, e];
  if (So) {
    let t = wo(e);
    return t ? ((0, l.statSync)(t).isFile() ? [`/select,`, t] : [t]) : [e];
  }
  return [e];
}
function Eo(e, t, n, r) {
  return e === `win32` && /\.(cmd|bat)$/i.test(t)
    ? { command: r ?? `cmd.exe`, args: [`/d`, `/s`, `/c`, t, ...n] }
    : { command: t, args: n };
}
function Do(e, n, r) {
  return new Promise((i, a) => {
    let o = Eo(process.platform, e, n, process.env.ComSpec),
      s = (0, m.spawn)(o.command, o.args, { stdio: `ignore`, env: t.Vr(r?.env ?? process.env) });
    (s.on(`error`, a),
      s.on(`close`, (t) => {
        if (!t) {
          i();
          return;
        }
        a(Error(`${e} exited with code ${t ?? `unknown`} (${n.join(` `)})`));
      }));
  });
}
function Oo(e) {
  let n = (0, m.spawnSync)(`where.exe`, [e], {
    windowsHide: !0,
    encoding: `utf8`,
    env: t.Vr(process.env),
  });
  return n.status !== 0 || typeof n.stdout != `string`
    ? null
    : (n.stdout
        .split(/\r?\n/)
        .map((e) => e.trim())
        .find((e) => e.length > 0) ?? null);
}
var ko = process.env.LOCALAPPDATA ?? (0, s.join)((0, o.homedir)(), `AppData`, `Local`),
  Ao = [
    (0, s.join)(ko, `Programs`),
    process.env.ProgramFiles,
    process.env[`ProgramFiles(x86)`],
  ].flatMap((e) => (e ? [e] : [])),
  jo = (0, s.join)(ko, `Microsoft`, `WindowsApps`),
  Mo = [
    process.env.APPDATA
      ? (0, s.join)(process.env.APPDATA, `Microsoft`, `Windows`, `Start Menu`, `Programs`)
      : null,
    process.env.ProgramData
      ? (0, s.join)(process.env.ProgramData, `Microsoft`, `Windows`, `Start Menu`, `Programs`)
      : null,
  ].flatMap((e) => (e ? [e] : [])),
  No = (e) =>
    process.env[e] == null
      ? null
      : (0, s.join)(process.env[e], `Microsoft Visual Studio`, `Installer`, `vswhere.exe`),
  Po = [No(`ProgramFiles(x86)`), No(`ProgramFiles`)].flatMap((e) => (e ? [e] : []));
function Fo(e) {
  for (let t of Ao)
    for (let n of e) {
      let e = (0, s.join)(t, ...n);
      if ((0, l.existsSync)(e)) return e;
    }
  return null;
}
function Io(e) {
  if (!(0, l.existsSync)(e)) return null;
  if ((0, s.extname)(e)) return e;
  for (let t of [`.cmd`, `.bat`, `.exe`]) {
    let n = `${e}${t}`;
    if ((0, l.existsSync)(n)) return n;
  }
  return e;
}
function Lo(e, t) {
  let n = (0, s.dirname)(e);
  if ((0, s.basename)(n).toLowerCase() === `bin`) {
    let e = (0, s.join)((0, s.dirname)(n), t);
    if ((0, l.existsSync)(e)) return e;
  }
  let r = (0, s.join)(n, t);
  return (0, l.existsSync)(r) ? r : null;
}
function Ro(e) {
  return Io((0, s.join)(jo, e));
}
function zo() {
  return jo;
}
function Bo({ pathCommand: e, executableName: t, installDirName: n }) {
  if (e) {
    let n = Io(e);
    if (n) return Lo(n, t) ?? ((0, s.basename)(n).toLowerCase() === t.toLowerCase() ? n : null);
  }
  return Fo([[n, t]]);
}
function Vo(e) {
  let n = Po.find((e) => (0, l.existsSync)(e));
  if (n == null) return null;
  let r = (0, m.spawnSync)(
    n,
    [`-products`, `*`, `-all`, `-find`, e, `-utf8`, `-nologo`, `-prerelease`],
    { windowsHide: !0, encoding: `utf8`, env: t.Vr(process.env) },
  );
  return r.status !== 0 || typeof r.stdout != `string`
    ? null
    : (r.stdout
        .split(/\r?\n/)
        .map((e) => e.trim())
        .find((e) => e.length > 0 && (0, l.existsSync)(e)) ?? null);
}
function Ho(e) {
  let t = new Set(e.map((e) => e.toLowerCase()));
  for (let e of Mo) {
    let n = Uo(e, t);
    if (n) return n;
  }
  return null;
}
function Uo(e, t) {
  if (!(0, l.existsSync)(e)) return null;
  for (let n of (0, l.readdirSync)(e, { withFileTypes: !0 })) {
    let r = (0, s.join)(e, n.name);
    if (n.isFile() && t.has(n.name.toLowerCase())) return r;
    if (!n.isDirectory()) continue;
    let i = Uo(r, t);
    if (i) return i;
  }
  return null;
}
var Wo = `com.google.Chrome`,
  Go = `/usr/bin/open`,
  Ko = /^[a-p]{32}$/;
function qo(e) {
  return `chrome://extensions/?id=${Qo(e)}`;
}
function Jo({
  extensionId: e,
  homeDir: t = (0, o.homedir)(),
  localAppDataDir: n = process.env.LOCALAPPDATA,
  platform: r = process.platform,
}) {
  let i = Qo(e),
    a = $o({ homeDir: t, localAppDataDir: n, platform: r });
  return a == null || !(0, l.existsSync)(a)
    ? !1
    : (0, l.readdirSync)(a, { withFileTypes: !0 }).some(
        (e) => e.isDirectory() && (0, l.existsSync)((0, s.join)(a, e.name, `Extensions`, i)),
      );
}
async function Yo({
  extensionId: e,
  platform: t = process.platform,
  detectChromeCommand: n = Xo,
  runCommand: r = Do,
}) {
  if (t === `darwin`) {
    await r(Go, [`-b`, Wo, qo(e)]);
    return;
  }
  if (t === `win32`) {
    let t = n();
    if (t == null) throw Error(`Google Chrome is not installed`);
    await r(t, [qo(e)]);
    return;
  }
  throw Error(`Opening Chrome extension settings is only supported on macOS and Windows`);
}
function Xo() {
  return (
    Co(`chrome.exe`) ??
    Co(`chrome`) ??
    Fo([[`Google`, `Chrome`, `Application`, `chrome.exe`]]) ??
    Zo()
  );
}
function Zo() {
  let e = (0, s.join)(
    process.env.LOCALAPPDATA ?? (0, s.join)((0, o.homedir)(), `AppData`, `Local`),
    `Google`,
    `Chrome`,
    `Application`,
    `chrome.exe`,
  );
  return (0, l.existsSync)(e) ? e : null;
}
function Qo(e) {
  let t = e.trim();
  if (!Ko.test(t)) throw Error(`Invalid Chrome extension id`);
  return t;
}
function $o({ homeDir: e, localAppDataDir: t, platform: n }) {
  return n === `darwin`
    ? (0, s.join)(e, `Library`, `Application Support`, `Google`, `Chrome`)
    : n === `win32`
      ? (0, s.join)(t ?? (0, s.join)(e, `AppData`, `Local`), `Google`, `Chrome`, `User Data`)
      : null;
}
var es = [
    {
      destination: [`skills`, `control-in-app-browser`, `SKILL.md`],
      source: [`.codex-plugin`, `control-in-app-browser-multi-tab.md`],
    },
    { destination: [`docs`, `api.md`], source: [`.codex-plugin`, `api-multi-tab.md`] },
  ],
  ts = [
    {
      destination: [`skills`, `computer-use`, `SKILL.md`],
      source: [`.codex-plugin`, `computer-use-node-repl.md`],
    },
  ],
  ns = t.Zs({});
async function rs(e) {
  let n = e.pluginName === t.mo ? e.browserSkillVariant : void 0,
    r = e.pluginName === `computer-use` ? e.computerUseSkillVariant : void 0,
    i = n ?? r;
  if (i == null) return;
  let a = n === `multi-tab` ? es : r === `node-repl` ? ts : [];
  await Promise.all(
    a.map(({ destination: t, source: n }) =>
      p.default.copyFile((0, s.join)(e.pluginRoot, ...n), (0, s.join)(e.pluginRoot, ...t)),
    ),
  );
  let o = (0, s.join)(e.pluginRoot, `.codex-plugin`, `plugin.json`),
    c = await ns.parseAsync(JSON.parse(await p.default.readFile(o, `utf8`)));
  await p.default.writeFile(
    o,
    `${JSON.stringify({ ...c, bundledContentVariant: i }, null, 2)}\n`,
    `utf8`,
  );
}
var is = [`plugins`],
  as = [`.agents`, `plugins`, `marketplace.json`],
  os = [`scripts`, `extension-id.json`],
  ss = new Set(Mt),
  cs = t
    .nc()
    .trim()
    .min(1)
    .refine(
      (e) => e !== `.` && e !== `..` && !e.includes(`/`) && !e.includes(`\\`),
      `Expected a single marketplace name.`,
    ),
  ls = t.Zs({
    name: t.nc().trim().min(1),
    source: t.$s({ source: t.Xs(`local`), path: t.nc().trim().min(1) }),
  }),
  us = t.Zs({
    name: cs.refine(t.Co, `Expected an OpenAI bundled marketplace name.`),
    interface: t.ic().optional(),
    plugins: t.Gs(ls),
  }),
  ds = t.Zs({ name: cs, interface: t.ic().optional(), plugins: t.Gs(ls) }),
  fs = t.Zs({
    bundledContentVariant: t.nc().trim().min(1).optional(),
    name: t.nc(),
    version: t.nc().trim().min(1),
  }),
  ps = t.$s({ extensionId: t.nc().trim().min(1) }),
  L = t.Wr(`BundledPluginsMarketplace`),
  ms = (0, c.promisify)(m.execFile),
  hs = (function (e) {
    return (
      (e.LocalPluginSource = `local_plugin_source`),
      (e.Other = `other`),
      (e.PluginCacheWindowsFileLock = `plugin_cache_windows_file_lock`),
      (e.PluginsDisabled = `plugins_disabled`),
      e
    );
  })({});
async function gs(e) {
  let t = e.marketplaceName ?? `openai-bundled`,
    n = e.marketplacePluginNames,
    r = new Set(e.reconcilePluginNames ?? n),
    i = new Set(e.syncInstallStateWithChromeExtensionPluginNames ?? []),
    a = await Ss({
      appServerConnection: e.appServerConnection,
      failOnError: i.size > 0,
      marketplaceName: t,
      marketplacePluginNames: n,
    }),
    o = new Set(n),
    s = a?.plugins.filter((e) => e.installed && !o.has(e.name)) ?? [],
    c,
    l;
  try {
    ((c = await e.appServerConnection.platformFamily()),
      (l = await Cs({
        browserSkillVariant: e.browserSkillVariant,
        computerUseSkillVariant: e.computerUseSkillVariant,
        marketplacePluginNames: n,
        marketplaceName: t,
        platformFamily: c,
        resourcesPath: e.resourcesPath,
        runtimeMarketplaceRoot: e.runtimeMarketplaceRoot,
      })));
  } catch (n) {
    (L.warning(`bundled_plugins_marketplace_resolve_failed`, { safe: {}, sensitive: { error: n } }),
      await Gs({
        appServerConnection: e.appServerConnection,
        chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
        disabledInstalledPlugins: s,
        marketplaceName: t,
      }));
    return;
  }
  if (l == null) {
    (L.warning(`bundled_plugins_marketplace_missing`, {
      safe: { marketplacePluginNames: n },
      sensitive: {},
    }),
      await Gs({
        appServerConnection: e.appServerConnection,
        chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
        disabledInstalledPlugins: s,
        marketplaceName: t,
      }));
    return;
  }
  let u = await ys({
    appServerConnection: e.appServerConnection,
    chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
    devRuntimeRepoRoot: e.devRuntimeRepoRoot,
    forceInstallPluginNames: new Set(e.forceInstallPluginNames ?? []),
    installWhenMissingPluginNames: new Set(e.installWhenMissingPluginNames ?? []),
    isChromeExtensionInstalled: e.isChromeExtensionInstalled ?? Jo,
    installedBundledMarketplace: a,
    materializedMarketplace: l,
    platformFamily: c,
    reconcilePluginNames: r,
    resourcesPath: e.resourcesPath,
    syncInstallStateWithChromeExtensionPluginNames: i,
  });
  (await Gs({
    appServerConnection: e.appServerConnection,
    chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
    disabledInstalledPlugins: s,
    marketplaceName: t,
  }),
    u != null &&
      Ls({
        installedBundledMarketplace: a,
        installedPluginCountAfterWrite: u,
        marketplaceName: t,
        materializedMarketplace: l,
      }));
}
async function _s(e) {
  if (e.marketplacePluginNames.length === 0) return null;
  let t = e.marketplaceName ?? `openai-bundled`;
  try {
    let n = await ws({ marketplaceName: t, resourcesPath: e.resourcesPath });
    if (n == null)
      return (
        L.warning(`bundled_plugins_marketplace_missing`, {
          safe: { marketplacePluginNames: e.marketplacePluginNames },
          sensitive: {},
        }),
        null
      );
    let r = Bs({ marketplacePluginNames: e.marketplacePluginNames, marketplace: await js(n) }),
      i = e.isChromeExtensionInstalled ?? Jo,
      a = await Promise.all(
        r.plugins.map(async (e) => {
          let t = await As(Os({ bundledPlugin: e, localMarketplaceRoot: n }));
          return {
            extensionId: t,
            isExtensionInstalled: i({ extensionId: t }),
            pluginName: e.name,
          };
        }),
      );
    return JSON.stringify(a);
  } catch (t) {
    return (
      L.warning(`bundled_plugins_chrome_extension_sync_state_read_failed`, {
        safe: { marketplacePluginNames: e.marketplacePluginNames },
        sensitive: { error: t },
      }),
      null
    );
  }
}
async function vs(e) {
  let t, n;
  try {
    ((t = await e.appServerConnection.platformFamily()),
      (n = await Ts({ marketplaceRoot: e.marketplaceRoot, platformFamily: t })));
  } catch (t) {
    return (
      L.warning(`bundled_plugins_marketplace_resolve_failed`, {
        safe: {},
        sensitive: { error: t, marketplaceRoot: e.marketplaceRoot },
      }),
      !1
    );
  }
  if (n == null)
    return (
      L.warning(`bundled_plugins_marketplace_missing`, {
        safe: {},
        sensitive: { marketplaceRoot: e.marketplaceRoot },
      }),
      !1
    );
  let r = await Ss({
      appServerConnection: e.appServerConnection,
      failOnError: !1,
      marketplaceName: n.marketplace.name,
      marketplacePluginNames: n.marketplace.plugins.map((e) => e.name),
    }),
    i = await ys({
      appServerConnection: e.appServerConnection,
      devRuntimeRepoRoot: e.devRuntimeRepoRoot,
      forceInstallPluginNames: new Set(),
      installWhenMissingPluginNames: new Set(n.marketplace.plugins.map((e) => e.name)),
      isChromeExtensionInstalled: Jo,
      installedBundledMarketplace: r,
      materializedMarketplace: n,
      platformFamily: t,
      reconcilePluginNames: new Set(n.marketplace.plugins.map((e) => e.name)),
      resourcesPath: e.resourcesPath,
      syncInstallStateWithChromeExtensionPluginNames: new Set(),
    });
  return i == null
    ? !1
    : (Ls({
        installedBundledMarketplace: r,
        installedPluginCountAfterWrite: i,
        marketplaceName: n.marketplace.name,
        materializedMarketplace: n,
      }),
      !0);
}
async function ys(e) {
  let t;
  try {
    t = (
      await e.appServerConnection.addMarketplace({
        source: e.materializedMarketplace.marketplaceRoot,
      })
    ).marketplaceName;
  } catch (t) {
    if (
      (L.warning(`bundled_plugins_marketplace_add_failed`, {
        safe: { marketplaceName: e.materializedMarketplace.marketplace.name },
        sensitive: { error: t, marketplaceRoot: e.materializedMarketplace.marketplaceRoot },
      }),
      e.syncInstallStateWithChromeExtensionPluginNames.size > 0)
    )
      throw t;
    return null;
  }
  L.info(`bundled_plugins_marketplace_added`, {
    safe: {
      marketplacePluginNames: e.materializedMarketplace.marketplace.plugins.map((e) => e.name),
      marketplaceName: t,
    },
    sensitive: { marketplaceRoot: e.materializedMarketplace.marketplaceRoot },
  });
  let n = e.syncInstallStateWithChromeExtensionPluginNames.size > 0,
    r = n
      ? await Ss({
          appServerConnection: e.appServerConnection,
          failOnError: !0,
          marketplaceName: t,
          marketplacePluginNames: [...e.reconcilePluginNames],
        })
      : e.installedBundledMarketplace,
    i;
  try {
    i = await Vs({
      appServerConnection: e.appServerConnection,
      chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
      devRuntimeRepoRoot: e.devRuntimeRepoRoot,
      forceInstallPluginNames: e.forceInstallPluginNames,
      installWhenMissingPluginNames: e.installWhenMissingPluginNames,
      isChromeExtensionInstalled: e.isChromeExtensionInstalled,
      installedBundledMarketplace: r,
      localMarketplaceRoot: e.materializedMarketplace.localMarketplaceRoot,
      marketplace: e.materializedMarketplace.marketplace,
      marketplaceName: t,
      marketplaceRoot: e.materializedMarketplace.marketplaceRoot,
      platformFamily: e.platformFamily,
      reconcilePluginNames: e.reconcilePluginNames,
      resourcesPath: e.resourcesPath,
      syncInstallStateWithChromeExtensionPluginNames:
        e.syncInstallStateWithChromeExtensionPluginNames,
    });
  } catch (r) {
    if (
      (L.warning(`bundled_plugins_marketplace_install_failed`, {
        safe: {
          errorCategory: bs({ error: r, platformFamily: e.platformFamily }),
          marketplaceName: t,
          platformFamily: e.platformFamily,
        },
        sensitive: { error: r, marketplaceRoot: e.materializedMarketplace.marketplaceRoot },
      }),
      n)
    )
      throw r;
    return null;
  }
  if (i.failure != null) {
    if (
      (L.warning(`bundled_plugins_marketplace_install_failed`, {
        safe: {
          errorCategory: bs({ error: i.failure.error, platformFamily: e.platformFamily }),
          marketplaceName: t,
          platformFamily: e.platformFamily,
          ...i.failure.safe,
        },
        sensitive: {
          error: i.failure.error,
          marketplaceRoot: e.materializedMarketplace.marketplaceRoot,
          ...i.failure.sensitive,
        },
      }),
      n)
    )
      throw i.failure.error;
    return null;
  }
  return i.installedPluginCountAfterWrite;
}
function bs(e) {
  let n = xs(e.error).toLowerCase();
  return n.includes(`codex plugins are disabled for this workspace`)
    ? hs.PluginsDisabled
    : e.platformFamily === `windows` &&
        (t.Do(e.error) === `cache_overwrite_failure` ||
          n.includes(`access is denied`) ||
          n.includes(`being used by another process`) ||
          n.includes(`os error 32`) ||
          n.includes(`os error 5`))
      ? hs.PluginCacheWindowsFileLock
      : n.includes(`plugin source path is not a directory`) ||
          n.includes(`missing plugin.json`) ||
          n.includes(`missing or invalid plugin.json`) ||
          n.includes(`local plugin source path`) ||
          n.includes(`failed to read plugin source directory`) ||
          n.includes(`failed to resolve local marketplace source path`) ||
          n.includes(`marketplace root does not contain a supported manifest`) ||
          n.includes(`marketplace file is not in a supported location`)
        ? hs.LocalPluginSource
        : hs.Other;
}
function xs(e) {
  return e instanceof Error ? e.message : String(e);
}
async function Ss(e) {
  try {
    let { marketplaces: t } = await e.appServerConnection.listPlugins({});
    return t.find((t) => t.name === e.marketplaceName);
  } catch (t) {
    if (
      (L.warning(`bundled_plugins_list_failed`, {
        safe: {
          marketplaceName: e.marketplaceName,
          marketplacePluginNames: e.marketplacePluginNames,
        },
        sensitive: { error: t },
      }),
      e.failOnError)
    )
      throw t;
  }
}
async function Cs(e) {
  let n = await ws({ marketplaceName: e.marketplaceName, resourcesPath: e.resourcesPath });
  if (n == null) return null;
  let r = await Es({
      browserSkillVariant: e.browserSkillVariant,
      computerUseSkillVariant: e.computerUseSkillVariant,
      marketplacePluginNames: e.marketplacePluginNames,
      marketplaceName: e.marketplaceName,
      sourceMarketplaceRoot: n,
      targetMarketplaceRoot: e.runtimeMarketplaceRoot,
    }),
    i = await zs(r.marketplaceRoot);
  return {
    localMarketplaceRoot: i,
    marketplace: r.marketplace,
    marketplaceWriteCounts: r.marketplaceWriteCounts,
    marketplaceRoot: t.or({ localPath: i, platformFamily: e.platformFamily }),
  };
}
async function ws(e) {
  let n = [e.marketplaceName, t.bo]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((t) => [
      { marketplaceName: t, marketplaceRoot: (0, s.join)(e.resourcesPath, ...is, t) },
      {
        marketplaceName: t,
        marketplaceRoot: (0, s.join)(e.resourcesPath, `app.asar.unpacked`, ...is, t),
      },
    ]);
  return (
    (
      await Promise.all(
        n.map(async ({ marketplaceName: e, marketplaceRoot: t }) =>
          (await lc({ marketplaceName: e, marketplaceRoot: t })) ? t : null,
        ),
      )
    ).find((e) => e != null) ?? null
  );
}
async function Ts(e) {
  if (!(await uc({ marketplaceRoot: e.marketplaceRoot }))) return null;
  let n = await zs(e.marketplaceRoot);
  return {
    localMarketplaceRoot: n,
    marketplace: await Ms(e.marketplaceRoot),
    marketplaceWriteCounts: null,
    marketplaceRoot: t.or({ localPath: n, platformFamily: e.platformFamily }),
  };
}
async function Es(e) {
  let t;
  try {
    t = await js(e.sourceMarketplaceRoot);
  } catch (t) {
    throw (
      Fs({
        error: t,
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.sourceMarketplaceRoot,
        marketplaceRootKind: `bundled_resource`,
      }),
      t
    );
  }
  let n = {
      ...Bs({ marketplacePluginNames: e.marketplacePluginNames, marketplace: t }),
      name: e.marketplaceName,
    },
    r = {
      pluginCountAfterWrite: n.plugins.length,
      pluginCountBeforeWrite: await Ns({
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.targetMarketplaceRoot,
        marketplaceRootKind: `runtime_marketplace`,
      }),
    },
    i = `${e.targetMarketplaceRoot}.staging-${(0, u.randomUUID)()}`,
    a = `cleanup_staging_before_write`;
  try {
    (await Ps({
      directory: i,
      directoryKind: `staging_marketplace`,
      marketplaceName: e.marketplaceName,
      marketplaceWriteCounts: r,
      removePhase: `cleanup_staging_before_write`,
    }),
      (a = `create_staging`),
      await p.default.mkdir((0, s.join)(i, ...as.slice(0, -1)), { recursive: !0 }),
      (a = `write_manifest`));
    try {
      await p.default.writeFile((0, s.join)(i, ...as), `${JSON.stringify(n, null, 2)}\n`, `utf8`);
    } catch (t) {
      throw (
        L.warning(`plugin_marketplace_manifest_write_failed`, {
          safe: { marketplaceName: e.marketplaceName, ...r },
          sensitive: { error: t, marketplaceRoot: i },
        }),
        t
      );
    }
    ((a = `copy_plugins`),
      await Promise.all(
        n.plugins.map(async (t) => {
          let n = (0, s.resolve)(e.sourceMarketplaceRoot, t.source.path),
            r = (0, s.resolve)(i, t.source.path);
          (pc({ label: `bundled plugin target`, path: r, root: i }),
            await p.default.mkdir((0, s.dirname)(r), { recursive: !0 }),
            await cc(n, r),
            await rs({
              browserSkillVariant: e.browserSkillVariant,
              computerUseSkillVariant: e.computerUseSkillVariant,
              pluginName: t.name,
              pluginRoot: r,
            }));
        }),
      ),
      (a = `replace_target`),
      await Ps({
        directory: e.targetMarketplaceRoot,
        directoryKind: `runtime_marketplace`,
        marketplaceName: e.marketplaceName,
        marketplaceWriteCounts: r,
        removePhase: `replace_target`,
      }),
      (a = `create_target_parent`),
      await p.default.mkdir((0, s.resolve)(e.targetMarketplaceRoot, `..`), { recursive: !0 }),
      (a = `rename_staging`),
      await p.default.rename(i, e.targetMarketplaceRoot));
  } catch (t) {
    throw (
      L.warning(`plugin_marketplace_folder_write_failed`, {
        safe: { folderWritePhase: a, marketplaceName: e.marketplaceName, ...r },
        sensitive: {
          error: t,
          sourceMarketplaceRoot: e.sourceMarketplaceRoot,
          targetMarketplaceRoot: e.targetMarketplaceRoot,
        },
      }),
      t
    );
  } finally {
    await Ps({
      directory: i,
      directoryKind: `staging_marketplace`,
      marketplaceName: e.marketplaceName,
      marketplaceWriteCounts: r,
      removePhase: `cleanup_staging_after_write`,
    });
  }
  return (
    L.info(`plugin_marketplace_folder_write_succeeded`, {
      safe: { marketplaceName: e.marketplaceName, ...r },
      sensitive: {
        sourceMarketplaceRoot: e.sourceMarketplaceRoot,
        targetMarketplaceRoot: e.targetMarketplaceRoot,
      },
    }),
    L.info(`bundled_plugins_runtime_marketplace_written`, {
      safe: { pluginCount: n.plugins.length, pluginNames: n.plugins.map((e) => e.name) },
      sensitive: { targetMarketplaceRoot: e.targetMarketplaceRoot },
    }),
    {
      localMarketplaceRoot: e.targetMarketplaceRoot,
      marketplace: n,
      marketplaceWriteCounts: r,
      marketplaceRoot: e.targetMarketplaceRoot,
    }
  );
}
async function Ds(e) {
  let n = t.pn(e.bundledPlugin.name);
  if (n == null) return;
  let r = Os({ bundledPlugin: e.bundledPlugin, localMarketplaceRoot: e.localMarketplaceRoot }),
    i = nc({
      bundledPluginName: e.bundledPlugin.name,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      platformPath: e.platformPath,
    });
  await t.mn({ latestRoot: i, versionRoot: r });
  let [a, o] = await Promise.all([As(i), ks(i)]);
  await t.yn({
    codexHome: e.codexHome,
    devRuntimeRepoRoot: e.devRuntimeRepoRoot,
    extensionId: a,
    nativeHostName: n,
    pluginRoot: i,
    pluginVersion: o.version,
    resourcesPath: e.resourcesPath,
  });
}
function Os(e) {
  let t = (0, s.resolve)(e.localMarketplaceRoot, e.bundledPlugin.source.path);
  return (pc({ label: `bundled plugin source`, path: t, root: e.localMarketplaceRoot }), t);
}
async function ks(e) {
  return fs.parseAsync(
    JSON.parse(await p.default.readFile((0, s.join)(e, `.codex-plugin`, `plugin.json`), `utf8`)),
  );
}
async function As(e) {
  return ps.parse(JSON.parse(await p.default.readFile((0, s.join)(e, ...os), `utf8`))).extensionId;
}
async function js(e) {
  return us.parseAsync(JSON.parse(await p.default.readFile((0, s.join)(e, ...as), `utf8`)));
}
async function Ms(e) {
  return ds.parseAsync(JSON.parse(await p.default.readFile((0, s.join)(e, ...as), `utf8`)));
}
async function Ns(e) {
  if (!(0, l.existsSync)((0, s.join)(e.marketplaceRoot, ...as))) return null;
  try {
    return (await Ms(e.marketplaceRoot)).plugins.length;
  } catch (t) {
    return (
      Fs({
        error: t,
        marketplaceName: e.marketplaceName,
        marketplaceRoot: e.marketplaceRoot,
        marketplaceRootKind: e.marketplaceRootKind,
      }),
      null
    );
  }
}
async function Ps(e) {
  try {
    await p.default.rm(e.directory, { force: !0, recursive: !0 });
  } catch (t) {
    throw (
      L.warning(`plugin_marketplace_folder_remove_failed`, {
        safe: {
          directoryKind: e.directoryKind,
          marketplaceName: e.marketplaceName,
          removePhase: e.removePhase,
          ...e.marketplaceWriteCounts,
        },
        sensitive: { directory: e.directory, error: t },
      }),
      t
    );
  }
}
function Fs(e) {
  L.warning(`plugin_marketplace_manifest_read_failed`, {
    safe: {
      errorCategory: Is(e.error),
      marketplaceName: e.marketplaceName,
      marketplaceRootKind: e.marketplaceRootKind,
    },
    sensitive: { error: e.error, marketplaceRoot: e.marketplaceRoot },
  });
}
function Is(e) {
  return e instanceof SyntaxError
    ? `corrupt_json`
    : e instanceof t.ac
      ? `invalid_schema`
      : `read_failed`;
}
function Ls(e) {
  let t = new Set(e.materializedMarketplace.marketplace.plugins.map((e) => e.name));
  L.info(`plugin_marketplace_sync_completed`, {
    safe: {
      marketplaceName: e.marketplaceName,
      pluginCount: e.materializedMarketplace.marketplace.plugins.length,
      pluginsInstalledAfterWrite: e.installedPluginCountAfterWrite,
      pluginsInstalledBeforeWrite: Rs(e.installedBundledMarketplace, t),
      ...e.materializedMarketplace.marketplaceWriteCounts,
    },
    sensitive: {},
  });
}
function Rs(e, t) {
  return e?.plugins.filter((e) => e.installed && t.has(e.name)).length ?? 0;
}
async function zs(e) {
  try {
    return await p.default.realpath(e);
  } catch {
    return e;
  }
}
function Bs(e) {
  let t = new Set(e.marketplacePluginNames);
  return { ...e.marketplace, plugins: e.marketplace.plugins.filter((e) => t.has(e.name)) };
}
async function Vs(e) {
  let n = await e.appServerConnection.codexHome(),
    r = t.ar(e.platformFamily),
    i = ac({ marketplaceRoot: e.marketplaceRoot, platformPath: r }),
    a =
      e.syncInstallStateWithChromeExtensionPluginNames.size === 0
        ? new Set()
        : await Xs({
            chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
          }),
    o = e.marketplace.plugins.filter((t) => e.reconcilePluginNames.has(t.name)),
    s = new Set(e.marketplace.plugins.map((e) => e.name)),
    c = new Set(
      e.installedBundledMarketplace?.plugins
        .filter((e) => e.installed && s.has(e.name))
        .map((e) => e.name) ?? [],
    ),
    l = o.length;
  for (let [s, u] of o.entries()) {
    let o = `read_bundled_manifest`,
      d,
      f,
      p,
      m = null,
      h = async () => {
        if (m != null) return m;
        let t = mc({
          label: `bundled plugin source`,
          path: u.source.path,
          platformPath: r,
          root: e.marketplaceRoot,
        });
        return (
          (m = await oc({
            appServerConnection: e.appServerConnection,
            platformPath: r,
            pluginRoot: t,
          })),
          m
        );
      };
    try {
      let s = !1,
        l = e.installedBundledMarketplace?.plugins.find((e) => e.name === u.name),
        m = e.forceInstallPluginNames.has(u.name),
        g = e.syncInstallStateWithChromeExtensionPluginNames.has(u.name),
        _ = t.pn(u.name) != null,
        v = async () => {
          o = `reconcile_chrome_native_host`;
          let t = Ws(e.resourcesPath, u.name);
          await Ds({
            bundledPlugin: u,
            codexHome: n,
            devRuntimeRepoRoot: e.devRuntimeRepoRoot,
            localMarketplaceRoot: e.localMarketplaceRoot,
            marketplaceName: e.marketplaceName,
            platformPath: r,
            resourcesPath: t,
          });
        },
        y = e.installWhenMissingPluginNames.has(u.name);
      if (l != null && l.installed) {
        o = `read_bundled_manifest`;
        let t = await h();
        ((o = `read_installed_manifest`),
          (d = await Qs({
            appServerConnection: e.appServerConnection,
            bundledManifest: t,
            bundledPlugin: u,
            codexHome: n,
            installedPlugin: l,
            marketplaceName: e.marketplaceName,
            marketplaceRoot: e.marketplaceRoot,
            platformPath: r,
          })));
      } else d = `missing`;
      if (g) {
        o = `read_chrome_extension_config`;
        let r = await As(Os({ bundledPlugin: u, localMarketplaceRoot: e.localMarketplaceRoot })),
          i = e.isChromeExtensionInstalled({ extensionId: r }),
          f = l?.installed === !0,
          p = l != null && a.has(l.id),
          m = i
            ? f
              ? `keep_installed`
              : `install_missing`
            : f
              ? p
                ? `uninstall_managed`
                : `keep_user_managed`
              : `keep_missing`;
        if (
          (L.info(`bundled_plugin_chrome_extension_sync_decision`, {
            safe: {
              chromeExtensionSyncAction: m,
              extensionId: r,
              installedPluginId: l?.id ?? null,
              installedPluginStatus: d,
              isExtensionInstalled: i,
              isPluginInstalled: f,
              isPluginSyncManaged: p,
              pluginName: u.name,
            },
            sensitive: {},
          }),
          (y = i),
          (s = i && (!f || p)),
          !i && f)
        ) {
          if (!p) {
            (_ && (await v()),
              L.info(`bundled_plugin_uninstall_skipped_user_managed`, {
                safe: {
                  pluginId: l.id,
                  pluginName: l.name,
                  reason: `chrome_extension_uninstalled`,
                },
                sensitive: {},
              }));
            continue;
          }
          (L.info(`bundled_plugin_uninstall_requested`, {
            safe: { pluginId: l.id, pluginName: l.name, reason: `chrome_extension_uninstalled` },
            sensitive: {},
          }),
            (o = `sync_chrome_extension_state`),
            await e.appServerConnection.uninstallPlugin({ pluginId: l.id }),
            c.delete(u.name),
            _ &&
              ((o = `reconcile_chrome_native_host`),
              await t._n({ codexHome: n, marketplaceName: e.marketplaceName, pluginName: u.name })),
            (o = `write_chrome_extension_sync_marker`),
            await Zs({
              chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
              managed: !1,
              pluginId: l.id,
            }));
          continue;
        }
      }
      if (
        ((f = Us({ forceInstall: m, installWhenMissing: y, installedPluginStatus: d })), f == null)
      ) {
        (L.info(
          d === `missing`
            ? `bundled_plugin_install_skipped_missing`
            : `bundled_plugin_install_skipped_current`,
          { safe: { pluginName: u.name }, sensitive: {} },
        ),
          d === `missing` &&
            _ &&
            ((o = `reconcile_chrome_native_host`),
            await t._n({ codexHome: n, marketplaceName: e.marketplaceName, pluginName: u.name })),
          d === `current` &&
            _ &&
            (await v(),
            await rc({
              codexHome: n,
              getBundledManifest: h,
              marketplaceName: e.marketplaceName,
              platformPath: r,
              pluginName: u.name,
            })));
        continue;
      }
      if (
        ((p = ec({
          bundledPluginName: u.name,
          codexHome: n,
          marketplaceName: e.marketplaceName,
          platformPath: r,
        })),
        _)
      ) {
        o = `stop_chrome_native_host`;
        try {
          await t.vn({ root: p });
        } catch (e) {
          L.warning(`bundled_plugin_chrome_native_host_stop_failed`, {
            safe: { pluginName: u.name },
            sensitive: { error: e },
          });
        }
      }
      ((d === `outdated` || f === `forced`) &&
        l != null &&
        (L.info(`bundled_plugin_reinstall_uninstall_requested`, {
          safe: { pluginId: l.id, pluginName: l.name, reason: f },
          sensitive: {},
        }),
        (o = `uninstall_existing`),
        await e.appServerConnection.uninstallPlugin({ pluginId: l.id }),
        c.delete(u.name)),
        L.info(`bundled_plugin_install_requested`, {
          safe: { ...Hs(u.source.path), pluginName: u.name, reason: f },
          sensitive: {
            bundledPluginSourcePath: u.source.path,
            installedPluginCacheRoot: p,
            marketplacePath: i,
          },
        }),
        (o = `install_plugin`),
        await e.appServerConnection.installPlugin({ marketplacePath: i, pluginName: u.name }),
        c.add(u.name),
        _ && (await v()),
        (o = `read_bundled_manifest`),
        await h(),
        _ &&
          ((o = `reconcile_chrome_native_host`),
          await rc({
            codexHome: n,
            getBundledManifest: h,
            marketplaceName: e.marketplaceName,
            platformPath: r,
            pluginName: u.name,
          })),
        s &&
          ((o = `write_chrome_extension_sync_marker`),
          await Zs({
            chromeExtensionSyncManagedPluginStore: e.chromeExtensionSyncManagedPluginStore,
            managed: !0,
            pluginId: `${u.name}@${e.marketplaceName}`,
          })),
        d !== `missing` &&
          l?.enabled === !1 &&
          (L.info(`bundled_plugin_enabled_config_write_requested`, {
            safe: { enabled: !1, pluginId: `${u.name}@${e.marketplaceName}`, pluginName: u.name },
            sensitive: {},
          }),
          (o = `restore_disabled_config`),
          await Ys({
            appServerConnection: e.appServerConnection,
            enabled: !1,
            pluginId: `${u.name}@${e.marketplaceName}`,
          })));
    } catch (e) {
      let t = {
        ...Hs(u.source.path),
        installPhase: o,
        pluginCount: l,
        pluginIndex: s,
        pluginName: u.name,
      };
      return (
        f != null && (t.installReason = f),
        d != null && (t.installedPluginStatus = d),
        {
          failure: {
            error: e,
            safe: t,
            sensitive: {
              bundledPluginSourcePath: u.source.path,
              installedPluginCacheRoot: p,
              marketplacePath: i,
            },
          },
          installedPluginCountAfterWrite: c.size,
        }
      );
    }
  }
  return { failure: null, installedPluginCountAfterWrite: c.size };
}
function Hs(e) {
  let t = e.split(/[\\/]+/).filter(Boolean);
  return !e.startsWith(`/`) && !e.startsWith(`\\`) && !/^[a-zA-Z]:/.test(e) && !t.includes(`..`)
    ? { bundledPluginSourcePath: e, bundledPluginSourcePathKind: `relative` }
    : { bundledPluginSourcePathKind: `redacted` };
}
function Us(e) {
  return e.forceInstall && e.installedPluginStatus === `current`
    ? `forced`
    : e.installedPluginStatus === `missing`
      ? e.installWhenMissing
        ? `missing`
        : null
      : e.installedPluginStatus === `current`
        ? null
        : e.installedPluginStatus;
}
function Ws(e, t) {
  if (e == null)
    throw Error(`Missing resourcesPath required to sync Chrome native host resources for ${t}.`);
  return e;
}
async function Gs(e) {
  if (e.disabledInstalledPlugins.length === 0) return;
  let n = await e.appServerConnection.codexHome();
  for (let r of e.disabledInstalledPlugins)
    (await t._n({ codexHome: n, marketplaceName: e.marketplaceName, pluginName: r.name }),
      L.info(`bundled_plugin_uninstall_requested`, {
        safe: {
          pluginId: r.id,
          pluginName: r.name,
          reason: `not_in_bundled_marketplace_plugin_names`,
        },
        sensitive: {},
      }),
      await e.appServerConnection.uninstallPlugin({ pluginId: r.id }),
      e.chromeExtensionSyncManagedPluginStore?.setManagedPluginId(r.id, !1));
}
async function Ks(e) {
  if (e.marketplaceName !== `openai-bundled`) return;
  let t = [];
  try {
    ({ marketplaces: t } = await e.appServerConnection.listPlugins({}));
  } catch (e) {
    L.warning(`stale_bundled_marketplaces_list_failed`, { safe: {}, sensitive: { error: e } });
  }
  let n = t.filter((e) => ss.has(e.name));
  for (let t of n)
    try {
      (await e.appServerConnection.sendAppServerRequest(`marketplace/remove`, {
        marketplaceName: t.name,
      }),
        L.info(`stale_bundled_marketplace_remove_requested`, {
          safe: { marketplaceName: t.name },
          sensitive: {},
        }));
    } catch (e) {
      L.warning(`stale_bundled_marketplace_remove_failed`, {
        safe: { marketplaceName: t.name },
        sensitive: { error: e },
      });
    }
  try {
    await qs({ codexHome: await e.appServerConnection.codexHome() });
  } catch (e) {
    L.warning(`stale_bundled_marketplace_directory_cleanup_failed`, {
      safe: { reason: `codex_home_unavailable` },
      sensitive: { error: e },
    });
  }
}
async function qs(e) {
  for (let t of Mt)
    (await Js({
      codexHome: e.codexHome,
      directoryKind: `plugin_cache`,
      marketplaceName: t,
      pathSegments: [`plugins`, `cache`, t],
    }),
      await Js({
        codexHome: e.codexHome,
        directoryKind: `runtime_marketplace`,
        marketplaceName: t,
        pathSegments: [`.tmp`, `bundled-marketplaces`, t],
      }));
}
async function Js(e) {
  let t = (0, s.join)(e.codexHome, ...e.pathSegments);
  if ((0, l.existsSync)(t))
    try {
      (await p.default.rm(t, { recursive: !0, force: !0 }),
        L.info(`stale_bundled_marketplace_directory_removed`, {
          safe: { directoryKind: e.directoryKind, marketplaceName: e.marketplaceName },
          sensitive: {},
        }));
    } catch (n) {
      (L.warning(`stale_bundled_marketplace_directory_remove_failed`, {
        safe: { directoryKind: e.directoryKind, marketplaceName: e.marketplaceName },
        sensitive: { error: n },
      }),
        L.warning(`plugin_marketplace_folder_remove_failed`, {
          safe: {
            directoryKind: e.directoryKind,
            marketplaceName: e.marketplaceName,
            removePhase: `stale_cleanup`,
          },
          sensitive: { directory: t, error: n },
        }));
    }
}
async function Ys(e) {
  await e.appServerConnection.sendAppServerRequest(`config/batchWrite`, {
    edits: [
      { keyPath: `plugins.${e.pluginId}.enabled`, mergeStrategy: `upsert`, value: e.enabled },
    ],
    reloadUserConfig: !0,
  });
}
async function Xs(e) {
  if (e.chromeExtensionSyncManagedPluginStore == null)
    throw Error(`Missing Chrome extension sync managed plugin store.`);
  let t = e.chromeExtensionSyncManagedPluginStore.getManagedPluginIds();
  return (
    L.info(`bundled_plugins_chrome_extension_sync_marker_read`, {
      safe: { managedPluginCount: t.size },
      sensitive: {},
    }),
    t
  );
}
async function Zs(e) {
  if (e.chromeExtensionSyncManagedPluginStore == null)
    throw Error(`Missing Chrome extension sync managed plugin store.`);
  e.chromeExtensionSyncManagedPluginStore.setManagedPluginId(e.pluginId, e.managed);
}
async function Qs(e) {
  try {
    if (e.installedPlugin.source.type !== `local`) return `unknown`;
    let t = ec({
      bundledPluginName: e.bundledPlugin.name,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      platformPath: e.platformPath,
    });
    if (hc({ path: e.installedPlugin.source.path, platformPath: e.platformPath, root: t })) {
      let t = await oc({
        appServerConnection: e.appServerConnection,
        platformPath: e.platformPath,
        pluginRoot: e.installedPlugin.source.path,
      });
      return ic({ bundledManifest: e.bundledManifest, installedManifest: t });
    }
    if (
      hc({
        path: e.installedPlugin.source.path,
        platformPath: e.platformPath,
        root: e.marketplaceRoot,
      })
    )
      return (
        (await $s({
          appServerConnection: e.appServerConnection,
          bundledManifest: e.bundledManifest,
          installedPluginCacheRoot: t,
          platformPath: e.platformPath,
        })) ?? `unknown`
      );
    let n = await oc({
      appServerConnection: e.appServerConnection,
      platformPath: e.platformPath,
      pluginRoot: e.installedPlugin.source.path,
    });
    return ic({ bundledManifest: e.bundledManifest, installedManifest: n });
  } catch {
    return `unknown`;
  }
}
async function $s(e) {
  try {
    let t = mc({
        label: `bundled plugin cache version`,
        path: e.bundledManifest.version,
        platformPath: e.platformPath,
        root: e.installedPluginCacheRoot,
      }),
      n = await oc({
        appServerConnection: e.appServerConnection,
        platformPath: e.platformPath,
        pluginRoot: t,
      });
    return ic({ bundledManifest: e.bundledManifest, installedManifest: n });
  } catch {
    return null;
  }
}
function ec(e) {
  return mc({
    label: `bundled plugin cache`,
    path: e.bundledPluginName,
    platformPath: e.platformPath,
    root: e.platformPath.join(e.codexHome, `plugins`, `cache`, e.marketplaceName),
  });
}
function tc(e) {
  return mc({
    label: `bundled plugin cache version`,
    path: e.version,
    platformPath: e.platformPath,
    root: ec(e),
  });
}
function nc(e) {
  return mc({
    label: `bundled plugin latest cache`,
    path: `latest`,
    platformPath: e.platformPath,
    root: ec(e),
  });
}
async function rc(e) {
  if (t.pn(e.pluginName) == null) return;
  let n = await e.getBundledManifest();
  await t.gn({
    latestRoot: nc({
      bundledPluginName: e.pluginName,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      platformPath: e.platformPath,
    }),
    versionRoot: tc({
      bundledPluginName: e.pluginName,
      codexHome: e.codexHome,
      marketplaceName: e.marketplaceName,
      platformPath: e.platformPath,
      version: n.version,
    }),
    waitForVersionRoot: !1,
  });
}
function ic(e) {
  return e.bundledManifest.name === e.installedManifest.name &&
    e.bundledManifest.bundledContentVariant === e.installedManifest.bundledContentVariant &&
    e.bundledManifest.version === e.installedManifest.version
    ? `current`
    : `outdated`;
}
function ac(e) {
  return e.platformPath.join(e.marketplaceRoot, ...as);
}
async function oc(e) {
  return fs.parseAsync(
    JSON.parse(
      await sc({
        appServerConnection: e.appServerConnection,
        path: e.platformPath.join(e.pluginRoot, `.codex-plugin`, `plugin.json`),
      }),
    ),
  );
}
async function sc(e) {
  let t = await e.appServerConnection.readFile({ path: e.path });
  return Buffer.from(t.dataBase64, `base64`).toString(`utf8`);
}
async function cc(e, t) {
  if (h.default.platform === `darwin`) {
    await ms(`ditto`, [`--noqtn`, e, t]);
    return;
  }
  await p.default.cp(e, t, { recursive: !0, verbatimSymlinks: !0 });
}
async function lc(e) {
  return dc({
    marketplaceName: e.marketplaceName,
    marketplaceRoot: e.marketplaceRoot,
    marketplaceRootKind: `bundled_resource`,
    marketplaceSchema: us,
  });
}
async function uc(e) {
  return dc({ marketplaceRoot: e.marketplaceRoot, marketplaceSchema: ds });
}
async function dc(e) {
  let t = (0, s.join)(e.marketplaceRoot, ...as);
  if (!(0, l.existsSync)(t)) return !1;
  try {
    let n = await e.marketplaceSchema.parseAsync(JSON.parse(await p.default.readFile(t, `utf8`)));
    return (
      await Promise.all(
        n.plugins.map(async (t) =>
          fc({ pluginName: t.name, pluginRoot: (0, s.resolve)(e.marketplaceRoot, t.source.path) }),
        ),
      )
    ).every((e) => e);
  } catch (t) {
    return (
      e.marketplaceName != null &&
        e.marketplaceRootKind != null &&
        Fs({
          error: t,
          marketplaceName: e.marketplaceName,
          marketplaceRoot: e.marketplaceRoot,
          marketplaceRootKind: e.marketplaceRootKind,
        }),
      !1
    );
  }
}
async function fc(e) {
  let t = (0, s.join)(e.pluginRoot, `.codex-plugin`, `plugin.json`);
  if (!(0, l.existsSync)(t)) return !1;
  try {
    return (
      (await fs.parseAsync(JSON.parse(await p.default.readFile(t, `utf8`)))).name === e.pluginName
    );
  } catch {
    return !1;
  }
}
function pc(e) {
  let t = (0, s.resolve)(e.root),
    n = (0, s.resolve)(e.path);
  if (n !== t && !n.startsWith(`${t}${s.sep}`))
    throw Error(`${e.label} path escapes marketplace root: ${n}`);
}
function mc(e) {
  let t = e.platformPath.resolve(e.root),
    n = e.platformPath.resolve(t, e.path);
  if (!_c({ path: n, platformPath: e.platformPath, root: t }))
    throw Error(`${e.label} path escapes marketplace root: ${n}`);
  return n;
}
function hc(e) {
  return _c({
    path: gc(e.path, e.platformPath),
    platformPath: e.platformPath,
    root: gc(e.root, e.platformPath),
  });
}
function gc(e, t) {
  let n = t.resolve(e);
  return t === s.win32 ? t.toNamespacedPath(n).toLowerCase() : n;
}
function _c(e) {
  let t = e.root.endsWith(e.platformPath.sep) ? e.root : `${e.root}${e.platformPath.sep}`;
  return e.path === e.root || e.path.startsWith(t);
}
var vc = `CODEX_ELECTRON_BUNDLED_PLUGINS_RESOURCES_PATH`,
  yc = `CODEX_ELECTRON_BUNDLED_PLUGINS_FORCE_RELOAD`;
function bc(e) {
  return (e?.env ?? process.env)[vc]?.trim() || e?.resourcesPath || process.resourcesPath;
}
function xc(e) {
  return (0, s.join)(
    e.codexHome,
    `.tmp`,
    `bundled-marketplaces`,
    e.marketplaceName ?? `openai-bundled`,
  );
}
function Sc(e) {
  let t = (e?.env ?? process.env)[yc]?.trim();
  return t
    ? new Set(
        t
          .split(/[,\s]+/)
          .map((e) => e.trim())
          .filter((e) => e.length > 0),
      )
    : new Set();
}
var Cc = t.Gr(`bundled-plugins`),
  wc = `CODEX_ENABLE_DEV_BUNDLED_PLUGINS`;
function Tc(e) {
  let r = e.env ?? process.env,
    i = e.resourcesPath ?? bc({ env: r }),
    a = t.So(e.buildFlavor),
    o = e.runtimeMarketplaceRoot ?? xc({ codexHome: e.codexHome, marketplaceName: a }),
    s = r.NODE_ENV === `development` ? e.repoRoot : void 0,
    c = Sc({ env: r }),
    l = {
      getManagedPluginIds: () =>
        new Set(e.globalState.get(`electron-chrome-extension-sync-managed-plugin-ids`) ?? []),
      setManagedPluginId: (t, n) => {
        let r = new Set(
          e.globalState.get(`electron-chrome-extension-sync-managed-plugin-ids`) ?? [],
        );
        (n ? r.add(t) : r.delete(t), e.globalState.set(Jr, r.size === 0 ? void 0 : [...r].sort()));
      },
    },
    u = e.platform ?? process.platform;
  if (e.buildFlavor === n.i.Dev && r[wc]?.trim() !== `1`) {
    let t = null,
      n = Promise.resolve(),
      r = null;
    return {
      reconcileExternalPluginState: async () => {},
      reloadBundledPlugins: async () => {},
      setDesktopFeatureAvailability: (a) => {
        if (t == null) {
          if (!ze(a)) return n;
          t = a;
        } else t = { ...t, ...a };
        let o = t,
          s = JSON.stringify({
            computerUse: o.computerUse,
            computerUseNodeRepl: o.computerUseNodeRepl,
            externalBrowserUse: o.externalBrowserUse,
            inAppBrowserUse: o.inAppBrowserUse,
          });
        return r === s
          ? n
          : ((r = s),
            (n = n
              .catch(() => {})
              .then(async () => {
                try {
                  await pa({
                    appServerConnection: e.getLocalAppServerConnection(),
                    appVersion: e.appVersion,
                    desktopFeatureAvailability: o,
                    isPackaged: e.isPackaged,
                    platform: u,
                    repoRoot: e.repoRoot,
                    resourcesPath: i,
                  });
                } catch (e) {
                  throw (r === s && (r = null), e);
                }
              })
              .catch(() => {})),
            n);
      },
      waitForPendingReconcile: () => n,
    };
  }
  let d = null,
    f = null,
    p = null,
    m = null,
    h = Promise.resolve(),
    g = !1,
    _,
    v,
    y = (t) =>
      mo.filter((n) =>
        n.isAvailable({ buildFlavor: e.buildFlavor, env: r, features: t, platform: u }),
      ),
    b = ({ force: e, reason: t }) => {
      if (d == null)
        return (
          Cc().info(`bundled_plugins_reconcile_skipped_features_unavailable`, {
            safe: { reason: t },
            sensitive: {},
          }),
          h
        );
      let n = d,
        r = y(n),
        i = r.map((e) => e.name),
        a = Ec({ desktopFeatureAvailability: n, marketplacePluginNames: i }),
        o = Dc({ desktopFeatureAvailability: n, marketplacePluginNames: i, platform: u }),
        s = JSON.stringify({
          browserSkillVariant: a,
          computerUseSkillVariant: o,
          browserUseConfig: {
            computerUse: n.computerUse,
            computerUseNodeRepl: n.computerUseNodeRepl,
            externalBrowserUse: n.externalBrowserUse,
            inAppBrowserUse: n.inAppBrowserUse,
          },
          marketplacePluginNames: i,
        });
      if (!e && f === s) return h;
      f = s;
      let c = r.some((e) => e.syncInstallStateWithChromeExtension === !0),
        l = g;
      return (
        (h = h
          .catch(() => {})
          .then(async () => {
            ((g = !0),
              Cc().info(`bundled_plugins_reconcile_started`, {
                safe: { hasExternalPluginStateSync: c, queued: l, reason: t },
                sensitive: {},
              }));
            try {
              (await w({
                desktopFeatureAvailability: n,
                marketplacePluginDescriptors: r,
                marketplacePluginNames: i,
              }),
                Cc().info(`bundled_plugins_reconcile_completed`, {
                  safe: { hasExternalPluginStateSync: c, reason: t },
                  sensitive: {},
                }));
            } catch (e) {
              throw (
                Cc().warning(`bundled_plugins_reconcile_failed`, {
                  safe: { hasExternalPluginStateSync: c, reason: t },
                  sensitive: { error: e },
                }),
                e
              );
            } finally {
              g = !1;
            }
          })
          .catch(() => {})),
        h
      );
    },
    x = (e) => e.filter((e) => e.syncInstallStateWithChromeExtension === !0).map((e) => e.name),
    S = async (e) => _s({ marketplacePluginNames: x(e), marketplaceName: a, resourcesPath: i }),
    C = async (t) => {
      if (d == null)
        return (
          Cc().info(`bundled_plugins_reconcile_skipped_features_unavailable`, {
            safe: { reason: t },
            sensitive: {},
          }),
          h
        );
      let n = y(d),
        r = n.map((e) => e.name),
        c = Ec({ desktopFeatureAvailability: d, marketplacePluginNames: r }),
        f = Dc({ desktopFeatureAvailability: d, marketplacePluginNames: r, platform: u }),
        _ = x(n),
        v = await S(n);
      if (v == null || v === p || v === m) return h;
      m = v;
      let b = g;
      return (
        (h = h
          .catch(() => {})
          .then(async () => {
            ((g = !0),
              Cc().info(`bundled_plugins_reconcile_started`, {
                safe: { hasExternalPluginStateSync: !0, queued: b, reason: t },
                sensitive: {},
              }));
            try {
              (await gs({
                appServerConnection: e.getLocalAppServerConnection(),
                browserSkillVariant: c,
                computerUseSkillVariant: f,
                chromeExtensionSyncManagedPluginStore: l,
                devRuntimeRepoRoot: s,
                marketplacePluginNames: r,
                forceInstallPluginNames: [],
                installWhenMissingPluginNames: [],
                reconcilePluginNames: _,
                syncInstallStateWithChromeExtensionPluginNames: _,
                marketplaceName: a,
                resourcesPath: i,
                runtimeMarketplaceRoot: o,
              }),
                (p = v),
                e.onReconcileComplete?.(),
                Cc().info(`bundled_plugins_reconcile_completed`, {
                  safe: { hasExternalPluginStateSync: !0, reason: t },
                  sensitive: {},
                }));
            } catch (e) {
              throw (
                Cc().warning(`bundled_plugins_reconcile_failed`, {
                  safe: { hasExternalPluginStateSync: !0, reason: t },
                  sensitive: { error: e },
                }),
                e
              );
            } finally {
              (m === v && (m = null), (g = !1));
            }
          })
          .catch(() => {})),
        h
      );
    },
    w = async (t) => {
      let r = e.getLocalAppServerConnection(),
        d = t.marketplacePluginDescriptors
          .filter((e) => e.forceReload === !0 || c.has(e.name))
          .map((e) => e.name),
        f = t.marketplacePluginDescriptors
          .filter((e) => e.installWhenMissing === !0)
          .filter(
            (t) =>
              t.autoInstallOptOutKey == null || e.globalState.get(t.autoInstallOptOutKey) !== !0,
          )
          .map((e) => e.name),
        m = t.marketplacePluginDescriptors
          .filter((e) => e.syncInstallStateWithChromeExtension === !0)
          .map((e) => e.name),
        h = Ec({
          desktopFeatureAvailability: t.desktopFeatureAvailability,
          marketplacePluginNames: t.marketplacePluginNames,
        }),
        g = Dc({
          desktopFeatureAvailability: t.desktopFeatureAvailability,
          marketplacePluginNames: t.marketplacePluginNames,
          platform: u,
        });
      switch (e.buildFlavor) {
        case n.i.Agent:
        case n.i.Dev:
        case n.i.InternalAlpha:
        case n.i.Nightly:
        case n.i.PublicBeta:
          await Ks({ appServerConnection: r, marketplaceName: a });
          break;
        case n.i.Prod:
          break;
      }
      (await gs({
        appServerConnection: r,
        browserSkillVariant: h,
        computerUseSkillVariant: g,
        chromeExtensionSyncManagedPluginStore: l,
        devRuntimeRepoRoot: s,
        marketplacePluginNames: t.marketplacePluginNames,
        forceInstallPluginNames: d,
        installWhenMissingPluginNames: f,
        syncInstallStateWithChromeExtensionPluginNames: m,
        marketplaceName: a,
        resourcesPath: i,
        runtimeMarketplaceRoot: o,
      }),
        await Promise.all(
          t.marketplacePluginDescriptors.map(async (t) => {
            t.migrate != null &&
              (await t.migrate({
                appServerConnection: r,
                codexHome: e.codexHome,
                marketplaceName: a,
                trashItem: e.trashItem,
              }));
          }),
        ),
        await pa({
          appServerConnection: r,
          appVersion: e.appVersion,
          desktopFeatureAvailability: t.desktopFeatureAvailability,
          isPackaged: e.isPackaged,
          platform: u,
          repoRoot: e.repoRoot,
          resourcesPath: i,
        }),
        (p = await S(t.marketplacePluginDescriptors)),
        (h !== _ || g !== v) && e.onSkillsMayHaveChanged?.(),
        (_ = h),
        (v = g),
        e.onReconcileComplete?.());
    };
  return {
    reconcileExternalPluginState: (e = `focus`) => C(e),
    reloadBundledPlugins: () => b({ force: !0, reason: `debug-reload` }),
    setDesktopFeatureAvailability: (e) => {
      if (d == null) {
        if (!ze(e)) return h;
        d = e;
      } else d = { ...d, ...e };
      return b({ force: !1, reason: `startup` });
    },
    waitForPendingReconcile: () => h,
  };
}
function Ec(e) {
  if (e.marketplacePluginNames.includes(t.mo))
    return e.desktopFeatureAvailability.multiBrowserTabs ? `multi-tab` : `single-tab`;
}
function Dc(e) {
  if (!(e.platform !== `darwin` || !e.marketplacePluginNames.includes(`computer-use`)))
    return e.desktopFeatureAvailability.computerUseNodeRepl ? `node-repl` : `legacy-mcp`;
}
var Oc = t.Gr(`codex-app-config`),
  kc = `codex-app`,
  Ac = `config.json`,
  jc = 1,
  Mc = t.$s({ version: t.Qs().int().optional() }),
  Nc = t.$s({ remotePath: t.nc().trim().min(1), label: t.nc().optional() }).strict(),
  Pc = t.$s({ sshAlias: t.nc().trim().min(1), projects: t.Gs(Nc).default([]) }).strict(),
  Fc = t
    .$s({
      version: t.Xs(jc).optional().default(1),
      remoteConnectionMaxRetryAttempts: t.Qs().int().nonnegative().optional(),
      sshConnectTimeoutSeconds: t.Qs().int().nonnegative().optional(),
      remoteConnections: t.Gs(Pc).default([]),
    })
    .strict();
function Ic(e) {
  return (0, s.join)(e, kc, Ac);
}
async function Lc(e) {
  let t = Ic(e);
  if (!(0, l.existsSync)(t)) return { status: `missing`, configPath: t };
  let n;
  try {
    n = await (0, p.readFile)(t, `utf8`);
  } catch (e) {
    return (
      Oc().warning(`failed_to_read_config`, { safe: {}, sensitive: { configPath: t, error: e } }),
      { status: `read-error`, configPath: t, error: e }
    );
  }
  let r;
  try {
    r = JSON.parse(n);
  } catch (e) {
    return (
      Oc().warning(`failed_to_parse_config`, { safe: {}, sensitive: { configPath: t, error: e } }),
      { status: `invalid-json`, configPath: t, error: e }
    );
  }
  let i = Mc.safeParse(r);
  if (i.success && i.data.version != null && i.data.version !== jc)
    return (
      Oc().warning(`unsupported_config_version`, {
        safe: { version: i.data.version, supportedVersion: jc },
        sensitive: { configPath: t },
      }),
      {
        status: `unsupported-version`,
        configPath: t,
        version: i.data.version,
        supportedVersion: jc,
      }
    );
  let a = Fc.safeParse(r);
  return a.success
    ? { status: `loaded`, configPath: t, config: a.data }
    : (Oc().warning(`invalid_config`, { safe: {}, sensitive: { configPath: t, error: a.error } }),
      { status: `invalid-config`, configPath: t, error: a.error });
}
async function Rc(e, t) {
  let n = zc(t);
  n != null &&
    (
      await a.dialog.showMessageBox(e, {
        type: `error`,
        buttons: [`Copy details`, `OK`],
        defaultId: 1,
        cancelId: 1,
        noLink: !0,
        message: n.message,
        detail: n.detail,
      })
    ).response === 0 &&
    a.clipboard.writeText(n.detail);
}
function zc(e) {
  switch (e.status) {
    case `applied`: {
      let { missingSshAliases: t } = e.result;
      return t.length === 0
        ? null
        : {
            message: `Codex couldn't finish remote setup`,
            detail: [
              `Your remote workspace was not set up completely.`,
              ``,
              `Please copy these details and send them to your administrator.`,
              ``,
              `Details:`,
              `Codex could not find these SSH aliases after checking SSH config:`,
              ``,
              ...t.map((e) => `- ${e}`),
              ``,
              `Config file: ${e.configPath}`,
              ``,
              `Ask your administrator to confirm that the setup command completed and that these SSH aliases exist.`,
            ].join(`
`),
          };
    }
    case `missing`:
      return {
        message: `Codex couldn't find remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `Codex could not find the setup config file.`,
          ``,
          `Expected file: ${e.configPath}`,
        ].join(`
`),
      };
    case `read-error`:
      return {
        message: `Codex couldn't read remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `Codex could not read the setup config file.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
    case `invalid-json`:
      return {
        message: `Codex couldn't read remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `The setup config file is not valid JSON.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
    case `unsupported-version`:
      return {
        message: `Codex couldn't apply remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `Setup config version ${e.version} is not supported by this version of Codex.`,
          ``,
          `Supported version: ${e.supportedVersion}`,
          `Config file: ${e.configPath}`,
        ].join(`
`),
      };
    case `invalid-config`:
      return {
        message: `Codex couldn't apply remote setup config`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `The setup config file does not match the format Codex expects.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error.message}`,
        ].join(`
`),
      };
    case `unexpected-error`:
      return {
        message: `Codex couldn't finish remote setup`,
        detail: [
          `Your remote workspace was not set up completely.`,
          ``,
          `Please copy these details and send them to your administrator.`,
          ``,
          `Details:`,
          `Codex hit an unexpected error while applying the setup config.`,
          ``,
          `Config file: ${e.configPath}`,
          `Error: ${e.error instanceof Error ? e.error.message : String(e.error)}`,
        ].join(`
`),
      };
  }
}
var Bc = `CODEX_ELECTRON_COMPUTER_USE_APP_PATH`,
  Vc = `Codex Computer Use.app`;
function Hc() {
  return Uc() ?? Wc(Gc());
}
function Uc() {
  let e = process.env[Bc]?.trim();
  return e ? Kc(e) : null;
}
function Wc(e) {
  for (let t of e) if ((0, l.existsSync)(t)) return Kc(t);
  return null;
}
function Gc() {
  return [t.So(n.i.resolve()), t.bo]
    .filter((e, t, n) => n.indexOf(e) === t)
    .flatMap((e) => [
      s.default.join(bc(), `plugins`, e, `plugins`, t.go, Vc),
      s.default.join(
        a.app.getAppPath(),
        `.generated`,
        `dev-resources`,
        `plugins`,
        e,
        `plugins`,
        t.go,
        Vc,
      ),
    ]);
}
function Kc(e) {
  try {
    return l.realpathSync.native(e);
  } catch {
    return e;
  }
}
var qc = `computer-use`,
  Jc = `Codex Computer Use.app`,
  Yc = `CODEX_ELECTRON_SKIP_COMPUTER_USE_CANONICAL_REFRESH`,
  Xc = (0, c.promisify)(m.execFile);
function Zc(e) {
  return s.default.join(e, qc, Jc);
}
async function Qc({ sourceServiceAppPath: e, codexHome: t }) {
  if (process.platform !== `darwin` || process.env[Yc]?.trim() === `1` || e == null) return;
  let n = Zc(t);
  (await p.default.mkdir(s.default.dirname(n), { recursive: !0 }),
    await p.default.rm(n, { force: !0, recursive: !0 }),
    await Xc(`ditto`, [`--noqtn`, e, n]));
}
var $c = (0, c.promisify)(m.execFile),
  el = 100,
  tl = new Map(),
  nl = `
$ErrorActionPreference = "Stop"
$ProgressPreference = "SilentlyContinue"

$code = @"
using System;
using System.Runtime.InteropServices;

[StructLayout(LayoutKind.Sequential)]
public struct SIZE {
  public int cx;
  public int cy;
  public SIZE(int x, int y) { cx = x; cy = y; }
}

[StructLayout(LayoutKind.Sequential)]
public struct BITMAPINFOHEADER {
  public uint biSize;
  public int biWidth;
  public int biHeight;
  public ushort biPlanes;
  public ushort biBitCount;
  public uint biCompression;
  public uint biSizeImage;
  public int biXPelsPerMeter;
  public int biYPelsPerMeter;
  public uint biClrUsed;
  public uint biClrImportant;
}

[StructLayout(LayoutKind.Sequential)]
public struct BITMAPINFO {
  public BITMAPINFOHEADER bmiHeader;
  public uint bmiColors;
}

[ComImport]
[InterfaceType(ComInterfaceType.InterfaceIsIUnknown)]
[Guid("bcc18b79-ba16-442f-80c4-8a59c30c463b")]
public interface IShellItemImageFactory {
  void GetImage(SIZE size, int flags, out IntPtr phbm);
}

public static class CodexShellIconNative {
  const int BI_RGB = 0;
  const int DIB_RGB_COLORS = 0;
  const int ICON_ONLY = 4;

  [DllImport("shell32.dll", CharSet = CharSet.Unicode, PreserveSig = false)]
  public static extern void SHCreateItemFromParsingName(
    string pszPath,
    IntPtr pbc,
    ref Guid riid,
    [MarshalAs(UnmanagedType.Interface)] out IShellItemImageFactory ppv
  );

  [DllImport("gdi32.dll")]
  public static extern bool DeleteObject(IntPtr hObject);

  [DllImport("gdi32.dll")]
  static extern int GetDIBits(
    IntPtr hdc,
    IntPtr hbmp,
    uint uStartScan,
    uint cScanLines,
    byte[] lpvBits,
    ref BITMAPINFO lpbi,
    uint uUsage
  );

  [DllImport("user32.dll")]
  static extern IntPtr GetDC(IntPtr hWnd);

  [DllImport("user32.dll")]
  static extern int ReleaseDC(IntPtr hWnd, IntPtr hDC);

  public static string GetAppsFolderIconDataUrl(string appId) {
    Guid iid = new Guid("bcc18b79-ba16-442f-80c4-8a59c30c463b");
    IShellItemImageFactory factory;
    SHCreateItemFromParsingName("shell:AppsFolder\\\\" + appId, IntPtr.Zero, ref iid, out factory);

    IntPtr hbitmap;
    factory.GetImage(new SIZE(32, 32), ICON_ONLY, out hbitmap);
    try {
      using (var bitmap = BitmapFromHBitmapPreserveAlpha(hbitmap, 32, 32))
      using (var stream = new System.IO.MemoryStream()) {
        bitmap.Save(stream, System.Drawing.Imaging.ImageFormat.Png);
        return "data:image/png;base64," + Convert.ToBase64String(stream.ToArray());
      }
    } finally {
      DeleteObject(hbitmap);
    }
  }

  static System.Drawing.Bitmap BitmapFromHBitmapPreserveAlpha(IntPtr hbitmap, int width, int height) {
    var info = new BITMAPINFO();
    info.bmiHeader.biSize = (uint)Marshal.SizeOf(typeof(BITMAPINFOHEADER));
    info.bmiHeader.biWidth = width;
    info.bmiHeader.biHeight = -height;
    info.bmiHeader.biPlanes = 1;
    info.bmiHeader.biBitCount = 32;
    info.bmiHeader.biCompression = BI_RGB;
    info.bmiHeader.biSizeImage = (uint)(width * height * 4);

    byte[] pixels = new byte[width * height * 4];
    IntPtr hdc = GetDC(IntPtr.Zero);
    try {
      int scanLines = GetDIBits(hdc, hbitmap, 0, (uint)height, pixels, ref info, DIB_RGB_COLORS);
      if (scanLines == 0) {
        throw new InvalidOperationException("GetDIBits failed.");
      }
    } finally {
      ReleaseDC(IntPtr.Zero, hdc);
    }

    var bitmap = new System.Drawing.Bitmap(width, height, System.Drawing.Imaging.PixelFormat.Format32bppArgb);
    var data = bitmap.LockBits(
      new System.Drawing.Rectangle(0, 0, width, height),
      System.Drawing.Imaging.ImageLockMode.WriteOnly,
      System.Drawing.Imaging.PixelFormat.Format32bppArgb
    );
    try {
      Marshal.Copy(pixels, 0, data.Scan0, pixels.Length);
    } finally {
      bitmap.UnlockBits(data);
    }
    return bitmap;
  }
}
"@

Add-Type -AssemblyName System.Drawing
if (-not ("CodexShellIconNative" -as [type])) {
  Add-Type -TypeDefinition $code -ReferencedAssemblies System.Drawing
}

function Convert-ImageFileToDataUrl([string]$Path) {
  if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
    return $null
  }

  $extension = [System.IO.Path]::GetExtension($Path).ToLowerInvariant()
  $mimeType = switch ($extension) {
    ".png" { "image/png" }
    ".jpg" { "image/jpeg" }
    ".jpeg" { "image/jpeg" }
    ".ico" { "image/x-icon" }
    ".bmp" { "image/bmp" }
    default { $null }
  }
  if ($null -eq $mimeType) {
    return $null
  }

  $bytes = [System.IO.File]::ReadAllBytes($Path)
  return "data:$mimeType;base64,$([Convert]::ToBase64String($bytes))"
}

function Convert-ExeIconToDataUrl([string]$Path) {
  if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
    return $null
  }

  Add-Type -AssemblyName System.Drawing
  $icon = [System.Drawing.Icon]::ExtractAssociatedIcon($Path)
  if ($null -eq $icon) {
    return $null
  }

  $bitmap = $null
  $stream = New-Object System.IO.MemoryStream
  try {
    $bitmap = $icon.ToBitmap()
    $bitmap.Save($stream, [System.Drawing.Imaging.ImageFormat]::Png)
    return "data:image/png;base64,$([Convert]::ToBase64String($stream.ToArray()))"
  } finally {
    if ($null -ne $bitmap) { $bitmap.Dispose() }
    $icon.Dispose()
    $stream.Dispose()
  }
}

$AppPath = $env:CODEX_NATIVE_DESKTOP_APP_ICON_PATH
if ([string]::IsNullOrWhiteSpace($AppPath)) {
  exit 0
}

$trimmedAppPath = $AppPath.Trim()
if (Test-Path -LiteralPath $trimmedAppPath -PathType Leaf) {
  $extension = [System.IO.Path]::GetExtension($trimmedAppPath).ToLowerInvariant()
  $dataUrl = if ($extension -eq ".exe" -or $extension -eq ".dll") {
    Convert-ExeIconToDataUrl $trimmedAppPath
  } else {
    Convert-ImageFileToDataUrl $trimmedAppPath
  }
  if ($null -ne $dataUrl) {
    Write-Output $dataUrl
  }
  exit 0
}

try {
  Write-Output ([CodexShellIconNative]::GetAppsFolderIconDataUrl($trimmedAppPath))
} catch {
  exit 0
}
`;
async function rl(e) {
  let t = il(e);
  if (t === ``) return null;
  let n = al(t),
    r = tl.get(n);
  if (r != null) return (tl.delete(n), tl.set(n, r), r);
  let i = sl(t).then((e) => (e ?? tl.delete(n), e));
  return (tl.set(n, i), ol(), i);
}
function il(e) {
  let t = e.trim();
  if (t.toLowerCase().startsWith(`process:`)) {
    let e = t.slice(8).trim();
    if (e.includes(`\\`) || e.includes(`/`)) return e;
  }
  return t;
}
function al(e) {
  return e.trim().toLowerCase();
}
function ol() {
  for (; tl.size > el; ) {
    let e = tl.keys().next().value;
    if (e == null) return;
    tl.delete(e);
  }
}
async function sl(e) {
  try {
    let { stdout: n } = await $c(
      `powershell.exe`,
      [
        `-NoProfile`,
        `-NonInteractive`,
        `-ExecutionPolicy`,
        `Bypass`,
        `-EncodedCommand`,
        Buffer.from(nl, `utf16le`).toString(`base64`),
      ],
      {
        encoding: `utf8`,
        env: { ...t.Vr(process.env), CODEX_NATIVE_DESKTOP_APP_ICON_PATH: e },
        timeout: 1e4,
        windowsHide: !0,
      },
    );
    return cl(n);
  } catch {
    return null;
  }
}
function cl(e) {
  return e.match(/data:image\/[a-z0-9.+-]+;base64,[A-Za-z0-9+/=]+/gi)?.at(-1) ?? null;
}
var ll = t.$s({ iconSmallForAppPath: t.qs((e) => typeof e == `function`) }),
  ul = `computer-use-app-icons.node`,
  dl = 100,
  fl = `sky.node`,
  pl = 1e4,
  ml = (0, C.createRequire)(__filename),
  hl = new Map(),
  gl = t.$s({
    bundleIdentifier: t.nc(),
    iconSmallDataURL: t
      .nc()
      .nullish()
      .transform((e) => e ?? null),
    name: t.nc(),
    windowTitle: t
      .nc()
      .nullish()
      .transform((e) => e ?? null),
  });
async function _l({
  addon: e,
  electronAppPath: t = a.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return null;
  try {
    let n = null,
      i = await Promise.race([
        (e ?? Ol({ electronAppPath: t, resourcesPath: r })).frontmostWindow(),
        new Promise((e) => {
          ((n = setTimeout(() => {
            e(null);
          }, pl)),
            n.unref());
        }),
      ]).finally(() => {
        n != null && clearTimeout(n);
      });
    return i == null ? null : gl.parse(JSON.parse(i));
  } catch {
    return null;
  }
}
async function vl(
  e,
  {
    addon: t,
    electronAppPath: n = a.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = Ze({ env: process.env, resourcesPath: process.resourcesPath }),
  } = {},
) {
  let o = yl(e, r);
  if (o === ``) return null;
  let s = xl(o, r),
    c = hl.get(s);
  if (c != null) return (hl.delete(s), hl.set(s, c), c);
  let l = bl(o, { addon: t, electronAppPath: n, platform: r, resourcesPath: i }).then(
    (e) => (e ?? hl.delete(s), e),
  );
  return (hl.set(s, l), Sl(), l);
}
function yl(e, t) {
  return t === `win32` ? il(e) : t === `darwin` ? e.trim() : ``;
}
async function bl(e, { addon: t, electronAppPath: n, platform: r, resourcesPath: i }) {
  try {
    return r === `darwin`
      ? await (t ?? Ol({ electronAppPath: n, resourcesPath: i })).iconSmallForAppPath(e)
      : r === `win32`
        ? await (t ?? Cl({ electronAppPath: n, resourcesPath: i })).iconSmallForAppPath(e)
        : null;
  } catch {
    return null;
  }
}
function xl(e, t) {
  return `${t}:${t === `win32` ? e.toLowerCase() : e}`;
}
function Sl() {
  for (; hl.size > dl; ) {
    let e = hl.keys().next().value;
    if (e == null) return;
    hl.delete(e);
  }
}
function Cl({ electronAppPath: e, resourcesPath: t }) {
  for (let n of wl({ electronAppPath: e, resourcesPath: t }))
    if ((0, l.existsSync)(n)) return ll.parse(ml(n));
  throw Error(`Computer Use app icons native addon is unavailable`);
}
function wl({ electronAppPath: e, resourcesPath: t }) {
  return [
    ...(t == null ? [] : [s.default.join(t, `native`, ul)]),
    s.default.join(
      e,
      `native`,
      `computer-use-app-icons-addon`,
      `build`,
      `Release-${process.arch}`,
      `computer_use_app_icons.node`,
    ),
  ];
}
async function Tl(
  e,
  {
    addon: t,
    electronAppPath: n = a.app.getAppPath(),
    platform: r = process.platform,
    resourcesPath: i = process.resourcesPath,
  } = {},
) {
  return r !== `darwin` || e.trim() === ``
    ? null
    : (t ?? Ol({ electronAppPath: n, resourcesPath: i })).spawnComputerUseService(e);
}
function El(
  e,
  t,
  {
    addon: n,
    electronAppPath: r = a.app.getAppPath(),
    platform: i = process.platform,
    resourcesPath: o = process.resourcesPath,
  } = {},
) {
  if (i !== `darwin` || !Number.isSafeInteger(e) || e <= 0 || t.trim() === ``) return !1;
  try {
    return (
      n ?? Ol({ electronAppPath: r, resourcesPath: o })
    ).computerUseServiceProcessMatchesExecutablePath(e, t);
  } catch {
    return !1;
  }
}
function Dl({
  addon: e,
  electronAppPath: t = a.app.getAppPath(),
  platform: n = process.platform,
  resourcesPath: r = process.resourcesPath,
} = {}) {
  if (n !== `darwin`) return !1;
  try {
    return (
      e ?? Ol({ electronAppPath: t, resourcesPath: r })
    ).isPrivacySettingsTerminationRequest();
  } catch {
    return !1;
  }
}
function Ol({ electronAppPath: e, resourcesPath: t }) {
  for (let n of kl({ electronAppPath: e, resourcesPath: t }))
    if ((0, l.existsSync)(n)) return ml(n);
  throw Error(`Sky native addon is unavailable`);
}
function kl({ electronAppPath: e, resourcesPath: t }) {
  return [
    ...(t == null ? [] : [s.default.join(t, `native`, fl)]),
    s.default.join(e, `native`, `sky-addon`, `build`, `Release-${process.arch}`, fl),
  ];
}
var Al = class {
  appshotsEnabled = !1;
  nodeReplEnabled = !1;
  disposed = !1;
  servicePid = null;
  pendingOperation = Promise.resolve();
  isExpectedServiceProcess;
  isProcessRunning;
  serviceExecutablePath;
  spawnService;
  constructor(
    e,
    { isExpectedServiceProcess: t = El, isProcessRunning: n = jl, spawnService: r = Tl } = {},
  ) {
    ((this.isExpectedServiceProcess = t),
      (this.isProcessRunning = n),
      (this.serviceExecutablePath = s.default.join(
        e,
        `Contents`,
        `MacOS`,
        `SkyComputerUseService`,
      )),
      (this.spawnService = r));
  }
  setAppshotsEnabled(e) {
    return this.setEnabledFeatures({ appshotsEnabled: e, nodeReplEnabled: this.nodeReplEnabled });
  }
  setEnabledFeatures({ appshotsEnabled: e, nodeReplEnabled: t }) {
    return this.appshotsEnabled === e && this.nodeReplEnabled === t
      ? Promise.resolve()
      : ((this.appshotsEnabled = e),
        (this.nodeReplEnabled = t),
        this.runSerialized(async () => {
          this.disposed || (this.enabled && (await this.ensureServicePidForEnabledFeatures()));
        }));
  }
  ensureServicePid() {
    return this.runSerialized(async () => {
      if (this.disposed || !this.enabled)
        throw Error(`Managed Computer Use service is not enabled`);
      return this.ensureServicePidForEnabledFeatures();
    });
  }
  invalidateServicePid(e) {
    return this.runSerialized(async () => {
      this.disposed ||
        !this.appshotsEnabled ||
        (this.servicePid === e &&
          ((this.isProcessRunning(e) &&
            this.isExpectedServiceProcess(e, this.serviceExecutablePath)) ||
            (this.servicePid = null)));
    });
  }
  dispose() {
    ((this.disposed = !0), (this.appshotsEnabled = !1), (this.nodeReplEnabled = !1));
  }
  runSerialized(e) {
    let t = this.pendingOperation.then(e, e);
    return (
      (this.pendingOperation = t.then(
        () => {},
        () => {},
      )),
      t
    );
  }
  get enabled() {
    return this.appshotsEnabled || this.nodeReplEnabled;
  }
  async ensureServicePidForEnabledFeatures() {
    let e = this.servicePid;
    if (
      e != null &&
      this.isProcessRunning(e) &&
      this.isExpectedServiceProcess(e, this.serviceExecutablePath)
    )
      return e;
    this.servicePid = null;
    let t = await this.spawnService(this.serviceExecutablePath);
    if (t == null || !Number.isSafeInteger(t) || t <= 0)
      throw Error(`Failed to spawn managed Computer Use service`);
    return ((this.servicePid = t), t);
  }
};
function jl(e, t = process.platform) {
  try {
    process.kill(e, 0);
  } catch (e) {
    return !(typeof e == `object` && e && `code` in e && e.code === `ESRCH`);
  }
  if (t !== `darwin`) return !0;
  try {
    let t = (0, m.execFileSync)(`ps`, [`-o`, `state=`, `-p`, String(e)], {
      encoding: `utf8`,
      stdio: [`ignore`, `pipe`, `ignore`],
    }).trim();
    return t !== `` && !t.startsWith(`Z`);
  } catch {
    return !0;
  }
}
function Ml({ appServerConnection: e, closeActiveTurn: t }) {
  return e.registerInternalNotificationHandler((e) => {
    Nl(e) && t({ sessionId: e.params.threadId, turnId: e.params.turn.id });
  });
}
function Nl(e) {
  return (
    e.method === `turn/completed` &&
    typeof e.params?.threadId == `string` &&
    typeof e.params.turn == `object` &&
    e.params.turn != null &&
    `id` in e.params.turn &&
    typeof e.params.turn.id == `string`
  );
}
var Pl = `codex_desktop:message-from-view`,
  R = `codex_desktop:message-for-view`,
  Fl = `codex_desktop:browser-sidebar-runtime-message`,
  Il = `codex_desktop:mcp-app-sandbox-guest-message`,
  Ll = `codex_desktop:mcp-app-sandbox-host-message`,
  Rl = `codex_desktop:show-context-menu`,
  zl = `codex_desktop:show-application-menu`,
  Bl = `codex_desktop:get-sentry-init-options`,
  Vl = `codex_desktop:get-build-flavor`,
  Hl = `codex_desktop:get-uses-owl-app-shell`,
  Ul = `codex_desktop:get-system-theme-variant`,
  Wl = `codex_desktop:get-shared-object-snapshot`,
  Gl = `codex_desktop:get-fast-mode-rollout-metrics`,
  Kl = `codex_desktop:system-theme-variant-updated`,
  ql = `codex_desktop:trigger-sentry-test`,
  Jl = `codex_desktop:connect-app-host`,
  Yl = `icons`,
  Xl = `https://chatgpt.com/backend-api`,
  Zl = `http://localhost:8000/api`,
  Ql = `Codex Desktop`,
  $l = `codex_desktop`;
function eu(e) {
  return `codex_desktop:worker:${e}:from-view`;
}
function tu(e) {
  return `codex_desktop:worker:${e}:for-view`;
}
var nu = `CODEX_ELECTRON_DEV_PARENT_PID`,
  ru = `CODEX_ELECTRON_DEV_WEBVIEW_PID`,
  iu = 1e3;
function au(e) {
  let t = e?.trim();
  if (!t) return null;
  let n = Number(t);
  return !Number.isInteger(n) || n <= 0 ? null : n;
}
function ou(e) {
  return au(e);
}
function su(e) {
  return au(e);
}
function cu(e) {
  return typeof e == `object` && !!e && `code` in e && e.code === `ESRCH`;
}
function lu(e, t = (e, t) => process.kill(e, t)) {
  try {
    return (t(e, 0), !0);
  } catch (e) {
    return !cu(e);
  }
}
function uu({
  app: e,
  parentPid: t = ou(process.env[nu]),
  pollIntervalMs: n = iu,
  isParentProcessRunning: r = lu,
  setIntervalImpl: i = setInterval,
  clearIntervalImpl: a = clearInterval,
}) {
  if (t == null) return () => {};
  let o = !1,
    s = () => {
      o || ((o = !0), a(c));
    },
    c = i(() => {
      r(t) || (s(), e.quit());
    }, n);
  return (e.once(`will-quit`, s), s);
}
function du({
  app: e,
  webviewPid: t = su(process.env[ru]),
  killImpl: n = (e, t) => process.kill(e, t),
}) {
  if (t == null) return () => {};
  let r = () => {
    try {
      n(-t, `SIGKILL`);
      return;
    } catch {}
    try {
      n(t, `SIGKILL`);
    } catch {}
  };
  return (
    e.once(`will-quit`, r),
    () => {
      e.off(`will-quit`, r);
    }
  );
}
var fu = `trayMenu.openApp`,
  pu = `Open {appName}`,
  mu = `trayMenu.newChat`,
  hu = `New Chat`,
  gu = `trayMenu.pinnedThreads`,
  _u = `Pinned`,
  vu = `trayMenu.runningThreads`,
  yu = `Running`,
  bu = `trayMenu.recentThreads`,
  xu = `Recent`,
  Su = `trayMenu.unreadThreads`,
  Cu = `Unread`,
  wu = `trayMenu.usage`,
  Tu = `Usage`,
  Eu = `trayMenu.more`,
  Du = `More`,
  Ou = `trayMenu.projectlessThreads`,
  ku = `Chats`,
  Au = 3,
  ju = 35,
  Mu = class {
    completedThreads = [];
    unreadThreads = [];
    recentThreads = [];
    constructor(e, t) {
      ((this.onOpenThread = e), (this.nativeIntl = t), this.refreshMenu());
    }
    async handleMessage(e) {
      switch (e.type) {
        case `tray-menu-threads-changed`:
          ((this.unreadThreads = e.trayMenuThreads.unreadThreads),
            (this.recentThreads = e.trayMenuThreads.recentThreads),
            this.refreshMenu());
          return;
      }
    }
    setCompletedThreads(e) {
      ((this.completedThreads = e), this.refreshMenu());
    }
    refreshMenu() {
      let e = this.nativeIntl.formatMessage({ messageId: Eu, defaultMessage: Du }),
        t = this.nativeIntl.formatMessage({ messageId: Ou, defaultMessage: ku }),
        n = Nu(this.unreadThreads, this.completedThreads),
        r = new Set(n.map((e) => e.path)),
        i = [
          Pu({
            label: this.nativeIntl.formatMessage({ messageId: Su, defaultMessage: Cu }),
            moreLabel: e,
            threads: n,
            projectlessLabel: t,
            onOpenThread: this.onOpenThread,
          }),
          Pu({
            label: this.nativeIntl.formatMessage({ messageId: bu, defaultMessage: xu }),
            moreLabel: e,
            threads: this.recentThreads.filter((e) => !r.has(e.path)),
            projectlessLabel: t,
            onOpenThread: this.onOpenThread,
          }),
        ]
          .filter((e) => e.length > 0)
          .flatMap((e, t) => (t === 0 ? e : [{ type: `separator` }, ...e]));
      this.setDockMenu(i);
    }
    setDockMenu(e) {
      process.platform === `darwin` && a.app.dock?.setMenu(a.Menu.buildFromTemplate(e));
    }
  };
function Nu(e, t) {
  let n = new Set(e.map((e) => e.path));
  return [...e, ...t.filter((e) => !n.has(e.path))];
}
function Pu({ label: e, moreLabel: t, threads: n, projectlessLabel: r, onOpenThread: i }) {
  if (n.length === 0) return [];
  let a = n.slice(0, Au),
    o = n.slice(Au);
  return [
    { label: e, enabled: !1 },
    ...a.map((e) => Fu({ thread: e, projectlessLabel: r, onOpenThread: i })),
    ...(o.length > 0
      ? [
          {
            label: t,
            submenu: o.map((e) => Fu({ thread: e, projectlessLabel: r, onOpenThread: i })),
          },
        ]
      : []),
  ];
}
function Fu({ thread: e, projectlessLabel: t, onOpenThread: n }) {
  let r = Array.from(e.title),
    i = e.isProjectless ? t : e.projectLabel;
  return {
    label:
      r.length <= ju
        ? e.title
        : `${r
            .slice(0, ju - 1)
            .join(``)
            .trimEnd()}…`,
    ...(i.length > 0 ? { sublabel: i } : {}),
    click: () => {
      n(e.path);
    },
  };
}
var z = `local`,
  Iu = { id: z, display_name: `Local`, kind: `local` },
  Lu = 4,
  Ru = 8 * 1024 * 1024,
  zu = 200,
  Bu = `__native_pipe_request_`;
async function Vu(e = {}) {
  let t = new Hu(
    e.pipePath ?? (await Qu(e.nativePipeDirectory)),
    e.socketPeerAuthorizer ?? (() => ({ authorized: !0 })),
    e.events ?? {},
  );
  try {
    return (await t.start(), t);
  } catch (e) {
    throw (await t.close(), e);
  }
}
var Hu = class {
  sockets = new Set();
  pendingDataBySocket = new Map();
  socketsByClientId = new Map();
  clientIdsBySocket = new Map();
  server = g.default.createServer((e) => {
    this.handleSocketConnection(e);
  });
  messageCallback = null;
  nextClientId = 1;
  started = !1;
  ownsPipePath = !1;
  constructor(e, t, n) {
    ((this.pipePath = e), (this.socketPeerAuthorizer = t), (this.events = n));
  }
  async start() {
    (await ed(this.pipePath),
      await new Promise((e, t) => {
        (this.server.once(`error`, t),
          this.server.listen(this.pipePath, () => {
            (this.server.off(`error`, t), e());
          }));
      }),
      (this.started = !0),
      (this.ownsPipePath = !0),
      this.events.onListening?.(this.pipePath));
  }
  sendMessage(e) {
    if (qu(e)) {
      let t = Xu(JSON.stringify(e));
      for (let e of this.sockets) e.destroyed || e.write(t);
      return;
    }
    let t = Wu(e);
    if (t == null) return;
    let n = this.socketsByClientId.get(t.clientId);
    if (n == null) return;
    e.id = t.messageId;
    let r = Xu(JSON.stringify(e));
    n.destroyed || n.write(r);
  }
  setMessageCallback(e) {
    this.messageCallback = e;
  }
  async close() {
    let e = this.ownsPipePath;
    for (let e of this.sockets) e.destroy();
    (this.sockets.clear(),
      this.pendingDataBySocket.clear(),
      this.clientIdsBySocket.clear(),
      this.socketsByClientId.clear(),
      (this.started &&=
        (await new Promise((e) => {
          this.server.close(() => {
            e();
          });
        }),
        !1)),
      (this.ownsPipePath = !1),
      e && td(this.pipePath));
  }
  handleSocketConnection(e) {
    let t;
    try {
      t = this.socketPeerAuthorizer(e);
    } catch (t) {
      (this.events.onAuthorizationError?.(t), e.destroy());
      return;
    }
    if (!t.authorized) {
      (this.events.onRejectedSocket?.(t), e.destroy());
      return;
    }
    (this.sockets.add(e), this.pendingDataBySocket.set(e, Buffer.alloc(0)));
    let n = String(this.nextClientId);
    ((this.nextClientId += 1),
      this.clientIdsBySocket.set(e, n),
      this.socketsByClientId.set(n, e),
      e.on(`data`, (t) => {
        this.handleSocketData(e, t);
      }),
      e.on(`error`, (e) => {
        this.events.onSocketError?.(e);
      }),
      e.on(`close`, () => {
        (this.sockets.delete(e), this.pendingDataBySocket.delete(e));
        let t = this.clientIdsBySocket.get(e);
        t != null && (this.clientIdsBySocket.delete(e), this.socketsByClientId.delete(t));
      }));
  }
  handleSocketData(e, t) {
    let n = Buffer.isBuffer(t) ? t : Buffer.from(t),
      r = this.pendingDataBySocket.get(e) ?? Buffer.alloc(0),
      i = Zu(Buffer.concat([r, n]));
    if (i == null) {
      (this.events.onInvalidFrame?.(), this.pendingDataBySocket.delete(e), e.destroy());
      return;
    }
    this.pendingDataBySocket.set(e, i.remainingData);
    for (let t of i.messages) this.handleIncomingMessage(e, t);
  }
  handleIncomingMessage(e, t) {
    let n;
    try {
      n = Gu(t);
    } catch {
      return;
    }
    if (Ku(n)) {
      let t = this.clientIdsBySocket.get(e);
      if (t == null) return;
      n.id = Uu(t, n.id);
    }
    this.messageCallback?.(n);
  }
};
function Uu(e, t) {
  return `${Bu}${e}:${JSON.stringify(t)}`;
}
function Wu(e) {
  if (typeof e.id != `string`) return;
  let t = e.id;
  if (!t.startsWith(Bu)) return;
  let n = t.slice(22),
    r = n.indexOf(`:`);
  if (r < 1) return;
  let i = n.slice(0, r),
    a;
  try {
    a = JSON.parse(n.slice(r + 1));
  } catch {
    return;
  }
  if (Yu(a)) return { clientId: i, messageId: a };
}
function Gu(e) {
  let t = JSON.parse(e);
  if (!Ju(t)) throw Error(`invalid transport message`);
  return t;
}
function Ku(e) {
  return qu(e) && Yu(e.id);
}
function qu(e) {
  return `method` in e && typeof e.method == `string`;
}
function Ju(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function Yu(e) {
  return typeof e == `string` || typeof e == `number`;
}
function Xu(e) {
  let t = Buffer.from(e, `utf8`),
    n = Buffer.alloc(Lu + t.length);
  return (
    (0, o.endianness)() === `LE` ? n.writeUInt32LE(t.length, 0) : n.writeUInt32BE(t.length, 0),
    t.copy(n, Lu),
    n
  );
}
function Zu(e) {
  let t = [],
    n = 0;
  for (; e.length - n >= Lu; ) {
    let r = (0, o.endianness)() === `LE` ? e.readUInt32LE(n) : e.readUInt32BE(n);
    if (r > Ru) return null;
    let i = Lu + r;
    if (e.length - n < i) break;
    (t.push(e.subarray(n + Lu, n + i).toString(`utf8`)), (n += i));
  }
  return { messages: t, remainingData: e.subarray(n) };
}
async function Qu(e) {
  return (
    (e ??= pt((0, o.platform)())),
    (0, o.platform)() === `win32`
      ? `${e}-${(0, u.randomUUID)()}`
      : (await $u(e), s.default.join(e, `${(0, u.randomUUID)()}.sock`))
  );
}
async function $u(e) {
  try {
    await (0, p.mkdir)(e, { recursive: !0 });
    return;
  } catch (e) {
    if (!(e instanceof Error && `code` in e && e.code === `EEXIST`)) throw e;
  }
  let t = await (0, p.lstat)(e);
  if (!t.isDirectory()) {
    if (!t.isSocket())
      throw Error(`Browser-use native pipe directory path exists and is not a directory`);
    if (await nd(e)) throw Error(`Browser-use native pipe directory path is already in use`);
    (await (0, p.unlink)(e), await (0, p.mkdir)(e, { recursive: !0 }));
  }
}
async function ed(e) {
  if (!((0, o.platform)() === `win32` || !(0, l.existsSync)(e))) {
    if (await nd(e)) throw Error(`Browser-use native pipe is already in use`);
    (0, l.unlinkSync)(e);
  }
}
function td(e) {
  (0, o.platform)() === `win32` || !(0, l.existsSync)(e) || (0, l.unlinkSync)(e);
}
async function nd(e) {
  return new Promise((t, n) => {
    let r = g.default.createConnection(e),
      i = !1,
      a = setTimeout(() => {
        o(!0);
      }, zu);
    a.unref();
    let o = (e) => {
      if (!i) {
        if (((i = !0), clearTimeout(a), r.destroy(), e instanceof Error)) {
          n(e);
          return;
        }
        t(e);
      }
    };
    (r.once(`connect`, () => {
      o(!0);
    }),
      r.once(`error`, (e) => {
        if (e.code === `ECONNREFUSED` || e.code === `ENOENT`) {
          o(!1);
          return;
        }
        o(e);
      }));
  });
}
var rd = `CODEX_BROWSER_USE_PEER_AUTHORIZATION`,
  id = `browser-use-peer-authorization.node`;
function ad(e) {
  return e[rd] === `1`;
}
var od = t.Gr(`browser-use-native-pipe-peer-authorizer`),
  sd = (0, C.createRequire)(__filename);
function cd() {
  if (process.platform !== `darwin`) return () => ({ authorized: !0 });
  let e = n.i.readFromPackageMetadata(),
    t = e != null && n.i.shouldIncludeBrowserUsePeerAuthorization(e, process.platform),
    r = !t && ad(process.env);
  if (a.app?.isPackaged === !0 && e == null)
    return (
      od().warning(`browser-use native pipe peer authorization disabled`, {
        safe: { reason: `missing-package-build-flavor` },
        sensitive: {},
      }),
      () => ({ authorized: !1, reason: `missing-package-build-flavor` })
    );
  if (!t && !r) return () => ({ authorized: !0 });
  let i = r
    ? Ze({ env: process.env, resourcesPath: process.resourcesPath })
    : process.resourcesPath;
  if (i == null) throw Error(`Browser-use peer authorization requires resourcesPath`);
  let o;
  try {
    o = sd((0, s.join)(i, `native`, id));
  } catch (e) {
    throw Error(`Failed to load browser-use peer authorization addon`, { cause: e });
  }
  return (
    od().info(`browser-use native pipe peer authorization enabled`, {
      safe: { mode: r ? `dev` : `packaged` },
      sensitive: {},
    }),
    (e) => {
      let t = ld(e);
      return t == null
        ? { authorized: !1, reason: `missing-socket-file-descriptor` }
        : o.authorizeSocketPeer(t, r);
    }
  );
}
function ld(e) {
  let t = e._handle?.fd;
  return typeof t == `number` && Number.isInteger(t) && t >= 0 ? t : null;
}
var ud = t.Gr(`node-repl-host-services-native-pipe`),
  dd = t.$s({
    id: t.rc([t.Qs(), t.nc()]),
    jsonrpc: t.Xs(`2.0`),
    method: t.Xs(`ensureService`),
    params: t.$s({ service: t.nc().trim().min(1).max(128) }),
  });
async function fd({ services: e, pipePath: t = Ln(), socketPeerAuthorizer: n = cd() }) {
  let r = await Vu({
    pipePath: t,
    socketPeerAuthorizer: n,
    events: {
      onListening: (e) => {
        ((0, l.chmodSync)(e, 384),
          ud().info(`node_repl_host_services_listening`, { safe: {}, sensitive: { pipePath: e } }));
      },
      onAuthorizationError: (e) => {
        ud().warning(`node_repl_host_services_peer_authorization_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      },
      onRejectedSocket: (e) => {
        ud().warning(`node_repl_host_services_peer_rejected`, {
          safe: { reason: e.reason ?? `unauthorized` },
          sensitive: {},
        });
      },
      onSocketError: (e) => {
        ud().warning(`node_repl_host_services_socket_error`, { safe: {}, sensitive: { error: e } });
      },
    },
  });
  return (
    r.setMessageCallback((t) => {
      pd(t, e, (e) => {
        r.sendMessage(e);
      });
    }),
    { pipePath: r.pipePath, dispose: () => r.close() }
  );
}
async function pd(e, t, n) {
  let r = dd.safeParse(e);
  if (!r.success) {
    let t = md(e);
    t != null &&
      n({
        error: { code: -32602, message: `Invalid host service request` },
        id: t,
        jsonrpc: `2.0`,
      });
    return;
  }
  let i = t.get(r.data.params.service);
  if (i == null) {
    n({
      error: { code: -32602, message: `Unsupported host service: ${r.data.params.service}` },
      id: r.data.id,
      jsonrpc: `2.0`,
    });
    return;
  }
  try {
    let e = await i();
    n({ id: r.data.id, jsonrpc: `2.0`, result: e ?? {} });
  } catch (e) {
    (ud().warning(`node_repl_host_service_ensure_failed`, {
      safe: { service: r.data.params.service },
      sensitive: { error: e },
    }),
      n({
        error: { code: -32e3, message: `Host service ensure failed` },
        id: r.data.id,
        jsonrpc: `2.0`,
      }));
  }
}
function md(e) {
  if (typeof e != `object` || !e || !(`id` in e)) return null;
  let t = e.id;
  return typeof t == `string` || typeof t == `number` ? t : null;
}
var hd = t.Gr(`react-devtools-extension`),
  gd = `fmkadmapgofadopljbjfkapdkoienihi`;
async function _d(e = {}) {
  let t = vd(e),
    n = e.loadExtension ?? ((e, t) => a.session.defaultSession.loadExtension(e, t));
  if (t == null)
    return (
      hd().trace(`React DevTools extension not found`, {
        safe: { extensionId: gd, platform: process.platform },
      }),
      !1
    );
  try {
    let e = await n(t);
    return (
      hd().info(`Loaded React DevTools extension`, {
        safe: { extensionId: e.id, name: e.name, version: e.version },
      }),
      !0
    );
  } catch (e) {
    return (
      hd().error(`Failed to load React DevTools extension`, {
        safe: {},
        sensitive: { error: String(e) },
      }),
      !1
    );
  }
}
function vd(e = {}) {
  let {
    platform: t = process.platform,
    homeDirectory: n = (0, o.homedir)(),
    localAppData: r = process.env.LOCALAPPDATA,
    userDataDirectory: i = a.app.getPath(`userData`),
  } = e;
  for (let e of yd({ platform: t, homeDirectory: n, localAppData: r, userDataDirectory: i })) {
    let t = Sd((0, s.join)(e, gd));
    if (t != null) return t;
  }
  return null;
}
function yd({ platform: e, homeDirectory: t, localAppData: n, userDataDirectory: r }) {
  return [
    (0, s.join)(r, `extensions`),
    ...bd({ platform: e, homeDirectory: t, localAppData: n }).flatMap(xd),
  ];
}
function bd({ platform: e, homeDirectory: t, localAppData: n }) {
  return e === `darwin`
    ? [(0, s.join)(t, `Library`, `Application Support`, `Google`, `Chrome`)]
    : e === `win32`
      ? n == null
        ? []
        : [(0, s.join)(n, `Google`, `Chrome`, `User Data`)]
      : e === `linux`
        ? [
            (0, s.join)(t, `.config`, `google-chrome`),
            (0, s.join)(t, `.config`, `google-chrome-beta`),
            (0, s.join)(t, `.config`, `google-chrome-canary`),
            (0, s.join)(t, `.config`, `chromium`),
          ]
        : [];
}
function xd(e) {
  return (0, l.existsSync)(e)
    ? (0, l.readdirSync)(e, { withFileTypes: !0 })
        .filter((e) => e.isDirectory())
        .map((t) => (0, s.join)(e, t.name, `Extensions`))
    : [];
}
function Sd(e) {
  if (!(0, l.existsSync)(e)) return null;
  let t = (0, l.readdirSync)(e, { withFileTypes: !0 })
    .filter((e) => e.isDirectory())
    .map((e) => e.name)
    .sort((e, t) => e.localeCompare(t, void 0, { numeric: !0, sensitivity: `base` }))
    .at(-1);
  return t == null ? null : (0, s.join)(e, t);
}
function Cd({ ensureWindow: e, showPrimaryWindow: n }) {
  let r = [],
    i = new Set(),
    a = !1,
    o = null,
    s = () => {
      if (!a) return Promise.resolve();
      if (o != null) return o;
      let t = r.splice(0);
      return (
        (o = (async () => {
          for (let r of t) await wd(r, { ensureWindow: e, showPrimaryWindow: n });
        })().finally(() => {
          ((o = null), r.length > 0 && s());
        })),
        o
      );
    };
  return {
    onRecovery: (e) => {
      let n = `${e.databasePath}\0${e.backupFolder}`;
      i.has(n) ||
        (i.add(n),
        r.push(e),
        t
          .Ur()
          .info(`Queued app-server SQLite recovery confirmation`, {
            safe: { pendingNotifications: r.length },
            sensitive: { backupFolder: e.backupFolder, databasePath: e.databasePath },
          }),
        s());
    },
    enable: async () => {
      ((a = !0), await s());
    },
  };
}
async function wd(e, { ensureWindow: n, showPrimaryWindow: i }) {
  t.Ur().info(`Showing app-server SQLite recovery confirmation`, {
    safe: {},
    sensitive: { backupFolder: e.backupFolder, databasePath: e.databasePath },
  });
  let a = await n();
  (i({ stealFocus: !0 }),
    await r._(e.databasePath, e.backupFolder, a).catch((e) => {
      t.Ur().warning(`Failed to show app-server SQLite recovery confirmation`, {
        safe: {},
        sensitive: { error: e },
      });
    }));
}
var Td = {};
function Ed(e, t) {
  let n = t || Td;
  return Dd(
    e,
    typeof n.includeImageAlt == `boolean` ? n.includeImageAlt : !0,
    typeof n.includeHtml == `boolean` ? n.includeHtml : !0,
  );
}
function Dd(e, t, n) {
  if (kd(e)) {
    if (`value` in e) return e.type === `html` && !n ? `` : e.value;
    if (t && `alt` in e && e.alt) return e.alt;
    if (`children` in e) return Od(e.children, t, n);
  }
  return Array.isArray(e) ? Od(e, t, n) : ``;
}
function Od(e, t, n) {
  let r = [],
    i = -1;
  for (; ++i < e.length; ) r[i] = Dd(e[i], t, n);
  return r.join(``);
}
function kd(e) {
  return !!(e && typeof e == `object`);
}
var Ad = {
    AElig: `Æ`,
    AMP: `&`,
    Aacute: `Á`,
    Abreve: `Ă`,
    Acirc: `Â`,
    Acy: `А`,
    Afr: `𝔄`,
    Agrave: `À`,
    Alpha: `Α`,
    Amacr: `Ā`,
    And: `⩓`,
    Aogon: `Ą`,
    Aopf: `𝔸`,
    ApplyFunction: `⁡`,
    Aring: `Å`,
    Ascr: `𝒜`,
    Assign: `≔`,
    Atilde: `Ã`,
    Auml: `Ä`,
    Backslash: `∖`,
    Barv: `⫧`,
    Barwed: `⌆`,
    Bcy: `Б`,
    Because: `∵`,
    Bernoullis: `ℬ`,
    Beta: `Β`,
    Bfr: `𝔅`,
    Bopf: `𝔹`,
    Breve: `˘`,
    Bscr: `ℬ`,
    Bumpeq: `≎`,
    CHcy: `Ч`,
    COPY: `©`,
    Cacute: `Ć`,
    Cap: `⋒`,
    CapitalDifferentialD: `ⅅ`,
    Cayleys: `ℭ`,
    Ccaron: `Č`,
    Ccedil: `Ç`,
    Ccirc: `Ĉ`,
    Cconint: `∰`,
    Cdot: `Ċ`,
    Cedilla: `¸`,
    CenterDot: `·`,
    Cfr: `ℭ`,
    Chi: `Χ`,
    CircleDot: `⊙`,
    CircleMinus: `⊖`,
    CirclePlus: `⊕`,
    CircleTimes: `⊗`,
    ClockwiseContourIntegral: `∲`,
    CloseCurlyDoubleQuote: `”`,
    CloseCurlyQuote: `’`,
    Colon: `∷`,
    Colone: `⩴`,
    Congruent: `≡`,
    Conint: `∯`,
    ContourIntegral: `∮`,
    Copf: `ℂ`,
    Coproduct: `∐`,
    CounterClockwiseContourIntegral: `∳`,
    Cross: `⨯`,
    Cscr: `𝒞`,
    Cup: `⋓`,
    CupCap: `≍`,
    DD: `ⅅ`,
    DDotrahd: `⤑`,
    DJcy: `Ђ`,
    DScy: `Ѕ`,
    DZcy: `Џ`,
    Dagger: `‡`,
    Darr: `↡`,
    Dashv: `⫤`,
    Dcaron: `Ď`,
    Dcy: `Д`,
    Del: `∇`,
    Delta: `Δ`,
    Dfr: `𝔇`,
    DiacriticalAcute: `´`,
    DiacriticalDot: `˙`,
    DiacriticalDoubleAcute: `˝`,
    DiacriticalGrave: "`",
    DiacriticalTilde: `˜`,
    Diamond: `⋄`,
    DifferentialD: `ⅆ`,
    Dopf: `𝔻`,
    Dot: `¨`,
    DotDot: `⃜`,
    DotEqual: `≐`,
    DoubleContourIntegral: `∯`,
    DoubleDot: `¨`,
    DoubleDownArrow: `⇓`,
    DoubleLeftArrow: `⇐`,
    DoubleLeftRightArrow: `⇔`,
    DoubleLeftTee: `⫤`,
    DoubleLongLeftArrow: `⟸`,
    DoubleLongLeftRightArrow: `⟺`,
    DoubleLongRightArrow: `⟹`,
    DoubleRightArrow: `⇒`,
    DoubleRightTee: `⊨`,
    DoubleUpArrow: `⇑`,
    DoubleUpDownArrow: `⇕`,
    DoubleVerticalBar: `∥`,
    DownArrow: `↓`,
    DownArrowBar: `⤓`,
    DownArrowUpArrow: `⇵`,
    DownBreve: `̑`,
    DownLeftRightVector: `⥐`,
    DownLeftTeeVector: `⥞`,
    DownLeftVector: `↽`,
    DownLeftVectorBar: `⥖`,
    DownRightTeeVector: `⥟`,
    DownRightVector: `⇁`,
    DownRightVectorBar: `⥗`,
    DownTee: `⊤`,
    DownTeeArrow: `↧`,
    Downarrow: `⇓`,
    Dscr: `𝒟`,
    Dstrok: `Đ`,
    ENG: `Ŋ`,
    ETH: `Ð`,
    Eacute: `É`,
    Ecaron: `Ě`,
    Ecirc: `Ê`,
    Ecy: `Э`,
    Edot: `Ė`,
    Efr: `𝔈`,
    Egrave: `È`,
    Element: `∈`,
    Emacr: `Ē`,
    EmptySmallSquare: `◻`,
    EmptyVerySmallSquare: `▫`,
    Eogon: `Ę`,
    Eopf: `𝔼`,
    Epsilon: `Ε`,
    Equal: `⩵`,
    EqualTilde: `≂`,
    Equilibrium: `⇌`,
    Escr: `ℰ`,
    Esim: `⩳`,
    Eta: `Η`,
    Euml: `Ë`,
    Exists: `∃`,
    ExponentialE: `ⅇ`,
    Fcy: `Ф`,
    Ffr: `𝔉`,
    FilledSmallSquare: `◼`,
    FilledVerySmallSquare: `▪`,
    Fopf: `𝔽`,
    ForAll: `∀`,
    Fouriertrf: `ℱ`,
    Fscr: `ℱ`,
    GJcy: `Ѓ`,
    GT: `>`,
    Gamma: `Γ`,
    Gammad: `Ϝ`,
    Gbreve: `Ğ`,
    Gcedil: `Ģ`,
    Gcirc: `Ĝ`,
    Gcy: `Г`,
    Gdot: `Ġ`,
    Gfr: `𝔊`,
    Gg: `⋙`,
    Gopf: `𝔾`,
    GreaterEqual: `≥`,
    GreaterEqualLess: `⋛`,
    GreaterFullEqual: `≧`,
    GreaterGreater: `⪢`,
    GreaterLess: `≷`,
    GreaterSlantEqual: `⩾`,
    GreaterTilde: `≳`,
    Gscr: `𝒢`,
    Gt: `≫`,
    HARDcy: `Ъ`,
    Hacek: `ˇ`,
    Hat: `^`,
    Hcirc: `Ĥ`,
    Hfr: `ℌ`,
    HilbertSpace: `ℋ`,
    Hopf: `ℍ`,
    HorizontalLine: `─`,
    Hscr: `ℋ`,
    Hstrok: `Ħ`,
    HumpDownHump: `≎`,
    HumpEqual: `≏`,
    IEcy: `Е`,
    IJlig: `Ĳ`,
    IOcy: `Ё`,
    Iacute: `Í`,
    Icirc: `Î`,
    Icy: `И`,
    Idot: `İ`,
    Ifr: `ℑ`,
    Igrave: `Ì`,
    Im: `ℑ`,
    Imacr: `Ī`,
    ImaginaryI: `ⅈ`,
    Implies: `⇒`,
    Int: `∬`,
    Integral: `∫`,
    Intersection: `⋂`,
    InvisibleComma: `⁣`,
    InvisibleTimes: `⁢`,
    Iogon: `Į`,
    Iopf: `𝕀`,
    Iota: `Ι`,
    Iscr: `ℐ`,
    Itilde: `Ĩ`,
    Iukcy: `І`,
    Iuml: `Ï`,
    Jcirc: `Ĵ`,
    Jcy: `Й`,
    Jfr: `𝔍`,
    Jopf: `𝕁`,
    Jscr: `𝒥`,
    Jsercy: `Ј`,
    Jukcy: `Є`,
    KHcy: `Х`,
    KJcy: `Ќ`,
    Kappa: `Κ`,
    Kcedil: `Ķ`,
    Kcy: `К`,
    Kfr: `𝔎`,
    Kopf: `𝕂`,
    Kscr: `𝒦`,
    LJcy: `Љ`,
    LT: `<`,
    Lacute: `Ĺ`,
    Lambda: `Λ`,
    Lang: `⟪`,
    Laplacetrf: `ℒ`,
    Larr: `↞`,
    Lcaron: `Ľ`,
    Lcedil: `Ļ`,
    Lcy: `Л`,
    LeftAngleBracket: `⟨`,
    LeftArrow: `←`,
    LeftArrowBar: `⇤`,
    LeftArrowRightArrow: `⇆`,
    LeftCeiling: `⌈`,
    LeftDoubleBracket: `⟦`,
    LeftDownTeeVector: `⥡`,
    LeftDownVector: `⇃`,
    LeftDownVectorBar: `⥙`,
    LeftFloor: `⌊`,
    LeftRightArrow: `↔`,
    LeftRightVector: `⥎`,
    LeftTee: `⊣`,
    LeftTeeArrow: `↤`,
    LeftTeeVector: `⥚`,
    LeftTriangle: `⊲`,
    LeftTriangleBar: `⧏`,
    LeftTriangleEqual: `⊴`,
    LeftUpDownVector: `⥑`,
    LeftUpTeeVector: `⥠`,
    LeftUpVector: `↿`,
    LeftUpVectorBar: `⥘`,
    LeftVector: `↼`,
    LeftVectorBar: `⥒`,
    Leftarrow: `⇐`,
    Leftrightarrow: `⇔`,
    LessEqualGreater: `⋚`,
    LessFullEqual: `≦`,
    LessGreater: `≶`,
    LessLess: `⪡`,
    LessSlantEqual: `⩽`,
    LessTilde: `≲`,
    Lfr: `𝔏`,
    Ll: `⋘`,
    Lleftarrow: `⇚`,
    Lmidot: `Ŀ`,
    LongLeftArrow: `⟵`,
    LongLeftRightArrow: `⟷`,
    LongRightArrow: `⟶`,
    Longleftarrow: `⟸`,
    Longleftrightarrow: `⟺`,
    Longrightarrow: `⟹`,
    Lopf: `𝕃`,
    LowerLeftArrow: `↙`,
    LowerRightArrow: `↘`,
    Lscr: `ℒ`,
    Lsh: `↰`,
    Lstrok: `Ł`,
    Lt: `≪`,
    Map: `⤅`,
    Mcy: `М`,
    MediumSpace: ` `,
    Mellintrf: `ℳ`,
    Mfr: `𝔐`,
    MinusPlus: `∓`,
    Mopf: `𝕄`,
    Mscr: `ℳ`,
    Mu: `Μ`,
    NJcy: `Њ`,
    Nacute: `Ń`,
    Ncaron: `Ň`,
    Ncedil: `Ņ`,
    Ncy: `Н`,
    NegativeMediumSpace: `​`,
    NegativeThickSpace: `​`,
    NegativeThinSpace: `​`,
    NegativeVeryThinSpace: `​`,
    NestedGreaterGreater: `≫`,
    NestedLessLess: `≪`,
    NewLine: `
`,
    Nfr: `𝔑`,
    NoBreak: `⁠`,
    NonBreakingSpace: `\xA0`,
    Nopf: `ℕ`,
    Not: `⫬`,
    NotCongruent: `≢`,
    NotCupCap: `≭`,
    NotDoubleVerticalBar: `∦`,
    NotElement: `∉`,
    NotEqual: `≠`,
    NotEqualTilde: `≂̸`,
    NotExists: `∄`,
    NotGreater: `≯`,
    NotGreaterEqual: `≱`,
    NotGreaterFullEqual: `≧̸`,
    NotGreaterGreater: `≫̸`,
    NotGreaterLess: `≹`,
    NotGreaterSlantEqual: `⩾̸`,
    NotGreaterTilde: `≵`,
    NotHumpDownHump: `≎̸`,
    NotHumpEqual: `≏̸`,
    NotLeftTriangle: `⋪`,
    NotLeftTriangleBar: `⧏̸`,
    NotLeftTriangleEqual: `⋬`,
    NotLess: `≮`,
    NotLessEqual: `≰`,
    NotLessGreater: `≸`,
    NotLessLess: `≪̸`,
    NotLessSlantEqual: `⩽̸`,
    NotLessTilde: `≴`,
    NotNestedGreaterGreater: `⪢̸`,
    NotNestedLessLess: `⪡̸`,
    NotPrecedes: `⊀`,
    NotPrecedesEqual: `⪯̸`,
    NotPrecedesSlantEqual: `⋠`,
    NotReverseElement: `∌`,
    NotRightTriangle: `⋫`,
    NotRightTriangleBar: `⧐̸`,
    NotRightTriangleEqual: `⋭`,
    NotSquareSubset: `⊏̸`,
    NotSquareSubsetEqual: `⋢`,
    NotSquareSuperset: `⊐̸`,
    NotSquareSupersetEqual: `⋣`,
    NotSubset: `⊂⃒`,
    NotSubsetEqual: `⊈`,
    NotSucceeds: `⊁`,
    NotSucceedsEqual: `⪰̸`,
    NotSucceedsSlantEqual: `⋡`,
    NotSucceedsTilde: `≿̸`,
    NotSuperset: `⊃⃒`,
    NotSupersetEqual: `⊉`,
    NotTilde: `≁`,
    NotTildeEqual: `≄`,
    NotTildeFullEqual: `≇`,
    NotTildeTilde: `≉`,
    NotVerticalBar: `∤`,
    Nscr: `𝒩`,
    Ntilde: `Ñ`,
    Nu: `Ν`,
    OElig: `Œ`,
    Oacute: `Ó`,
    Ocirc: `Ô`,
    Ocy: `О`,
    Odblac: `Ő`,
    Ofr: `𝔒`,
    Ograve: `Ò`,
    Omacr: `Ō`,
    Omega: `Ω`,
    Omicron: `Ο`,
    Oopf: `𝕆`,
    OpenCurlyDoubleQuote: `“`,
    OpenCurlyQuote: `‘`,
    Or: `⩔`,
    Oscr: `𝒪`,
    Oslash: `Ø`,
    Otilde: `Õ`,
    Otimes: `⨷`,
    Ouml: `Ö`,
    OverBar: `‾`,
    OverBrace: `⏞`,
    OverBracket: `⎴`,
    OverParenthesis: `⏜`,
    PartialD: `∂`,
    Pcy: `П`,
    Pfr: `𝔓`,
    Phi: `Φ`,
    Pi: `Π`,
    PlusMinus: `±`,
    Poincareplane: `ℌ`,
    Popf: `ℙ`,
    Pr: `⪻`,
    Precedes: `≺`,
    PrecedesEqual: `⪯`,
    PrecedesSlantEqual: `≼`,
    PrecedesTilde: `≾`,
    Prime: `″`,
    Product: `∏`,
    Proportion: `∷`,
    Proportional: `∝`,
    Pscr: `𝒫`,
    Psi: `Ψ`,
    QUOT: `"`,
    Qfr: `𝔔`,
    Qopf: `ℚ`,
    Qscr: `𝒬`,
    RBarr: `⤐`,
    REG: `®`,
    Racute: `Ŕ`,
    Rang: `⟫`,
    Rarr: `↠`,
    Rarrtl: `⤖`,
    Rcaron: `Ř`,
    Rcedil: `Ŗ`,
    Rcy: `Р`,
    Re: `ℜ`,
    ReverseElement: `∋`,
    ReverseEquilibrium: `⇋`,
    ReverseUpEquilibrium: `⥯`,
    Rfr: `ℜ`,
    Rho: `Ρ`,
    RightAngleBracket: `⟩`,
    RightArrow: `→`,
    RightArrowBar: `⇥`,
    RightArrowLeftArrow: `⇄`,
    RightCeiling: `⌉`,
    RightDoubleBracket: `⟧`,
    RightDownTeeVector: `⥝`,
    RightDownVector: `⇂`,
    RightDownVectorBar: `⥕`,
    RightFloor: `⌋`,
    RightTee: `⊢`,
    RightTeeArrow: `↦`,
    RightTeeVector: `⥛`,
    RightTriangle: `⊳`,
    RightTriangleBar: `⧐`,
    RightTriangleEqual: `⊵`,
    RightUpDownVector: `⥏`,
    RightUpTeeVector: `⥜`,
    RightUpVector: `↾`,
    RightUpVectorBar: `⥔`,
    RightVector: `⇀`,
    RightVectorBar: `⥓`,
    Rightarrow: `⇒`,
    Ropf: `ℝ`,
    RoundImplies: `⥰`,
    Rrightarrow: `⇛`,
    Rscr: `ℛ`,
    Rsh: `↱`,
    RuleDelayed: `⧴`,
    SHCHcy: `Щ`,
    SHcy: `Ш`,
    SOFTcy: `Ь`,
    Sacute: `Ś`,
    Sc: `⪼`,
    Scaron: `Š`,
    Scedil: `Ş`,
    Scirc: `Ŝ`,
    Scy: `С`,
    Sfr: `𝔖`,
    ShortDownArrow: `↓`,
    ShortLeftArrow: `←`,
    ShortRightArrow: `→`,
    ShortUpArrow: `↑`,
    Sigma: `Σ`,
    SmallCircle: `∘`,
    Sopf: `𝕊`,
    Sqrt: `√`,
    Square: `□`,
    SquareIntersection: `⊓`,
    SquareSubset: `⊏`,
    SquareSubsetEqual: `⊑`,
    SquareSuperset: `⊐`,
    SquareSupersetEqual: `⊒`,
    SquareUnion: `⊔`,
    Sscr: `𝒮`,
    Star: `⋆`,
    Sub: `⋐`,
    Subset: `⋐`,
    SubsetEqual: `⊆`,
    Succeeds: `≻`,
    SucceedsEqual: `⪰`,
    SucceedsSlantEqual: `≽`,
    SucceedsTilde: `≿`,
    SuchThat: `∋`,
    Sum: `∑`,
    Sup: `⋑`,
    Superset: `⊃`,
    SupersetEqual: `⊇`,
    Supset: `⋑`,
    THORN: `Þ`,
    TRADE: `™`,
    TSHcy: `Ћ`,
    TScy: `Ц`,
    Tab: `	`,
    Tau: `Τ`,
    Tcaron: `Ť`,
    Tcedil: `Ţ`,
    Tcy: `Т`,
    Tfr: `𝔗`,
    Therefore: `∴`,
    Theta: `Θ`,
    ThickSpace: `  `,
    ThinSpace: ` `,
    Tilde: `∼`,
    TildeEqual: `≃`,
    TildeFullEqual: `≅`,
    TildeTilde: `≈`,
    Topf: `𝕋`,
    TripleDot: `⃛`,
    Tscr: `𝒯`,
    Tstrok: `Ŧ`,
    Uacute: `Ú`,
    Uarr: `↟`,
    Uarrocir: `⥉`,
    Ubrcy: `Ў`,
    Ubreve: `Ŭ`,
    Ucirc: `Û`,
    Ucy: `У`,
    Udblac: `Ű`,
    Ufr: `𝔘`,
    Ugrave: `Ù`,
    Umacr: `Ū`,
    UnderBar: `_`,
    UnderBrace: `⏟`,
    UnderBracket: `⎵`,
    UnderParenthesis: `⏝`,
    Union: `⋃`,
    UnionPlus: `⊎`,
    Uogon: `Ų`,
    Uopf: `𝕌`,
    UpArrow: `↑`,
    UpArrowBar: `⤒`,
    UpArrowDownArrow: `⇅`,
    UpDownArrow: `↕`,
    UpEquilibrium: `⥮`,
    UpTee: `⊥`,
    UpTeeArrow: `↥`,
    Uparrow: `⇑`,
    Updownarrow: `⇕`,
    UpperLeftArrow: `↖`,
    UpperRightArrow: `↗`,
    Upsi: `ϒ`,
    Upsilon: `Υ`,
    Uring: `Ů`,
    Uscr: `𝒰`,
    Utilde: `Ũ`,
    Uuml: `Ü`,
    VDash: `⊫`,
    Vbar: `⫫`,
    Vcy: `В`,
    Vdash: `⊩`,
    Vdashl: `⫦`,
    Vee: `⋁`,
    Verbar: `‖`,
    Vert: `‖`,
    VerticalBar: `∣`,
    VerticalLine: `|`,
    VerticalSeparator: `❘`,
    VerticalTilde: `≀`,
    VeryThinSpace: ` `,
    Vfr: `𝔙`,
    Vopf: `𝕍`,
    Vscr: `𝒱`,
    Vvdash: `⊪`,
    Wcirc: `Ŵ`,
    Wedge: `⋀`,
    Wfr: `𝔚`,
    Wopf: `𝕎`,
    Wscr: `𝒲`,
    Xfr: `𝔛`,
    Xi: `Ξ`,
    Xopf: `𝕏`,
    Xscr: `𝒳`,
    YAcy: `Я`,
    YIcy: `Ї`,
    YUcy: `Ю`,
    Yacute: `Ý`,
    Ycirc: `Ŷ`,
    Ycy: `Ы`,
    Yfr: `𝔜`,
    Yopf: `𝕐`,
    Yscr: `𝒴`,
    Yuml: `Ÿ`,
    ZHcy: `Ж`,
    Zacute: `Ź`,
    Zcaron: `Ž`,
    Zcy: `З`,
    Zdot: `Ż`,
    ZeroWidthSpace: `​`,
    Zeta: `Ζ`,
    Zfr: `ℨ`,
    Zopf: `ℤ`,
    Zscr: `𝒵`,
    aacute: `á`,
    abreve: `ă`,
    ac: `∾`,
    acE: `∾̳`,
    acd: `∿`,
    acirc: `â`,
    acute: `´`,
    acy: `а`,
    aelig: `æ`,
    af: `⁡`,
    afr: `𝔞`,
    agrave: `à`,
    alefsym: `ℵ`,
    aleph: `ℵ`,
    alpha: `α`,
    amacr: `ā`,
    amalg: `⨿`,
    amp: `&`,
    and: `∧`,
    andand: `⩕`,
    andd: `⩜`,
    andslope: `⩘`,
    andv: `⩚`,
    ang: `∠`,
    ange: `⦤`,
    angle: `∠`,
    angmsd: `∡`,
    angmsdaa: `⦨`,
    angmsdab: `⦩`,
    angmsdac: `⦪`,
    angmsdad: `⦫`,
    angmsdae: `⦬`,
    angmsdaf: `⦭`,
    angmsdag: `⦮`,
    angmsdah: `⦯`,
    angrt: `∟`,
    angrtvb: `⊾`,
    angrtvbd: `⦝`,
    angsph: `∢`,
    angst: `Å`,
    angzarr: `⍼`,
    aogon: `ą`,
    aopf: `𝕒`,
    ap: `≈`,
    apE: `⩰`,
    apacir: `⩯`,
    ape: `≊`,
    apid: `≋`,
    apos: `'`,
    approx: `≈`,
    approxeq: `≊`,
    aring: `å`,
    ascr: `𝒶`,
    ast: `*`,
    asymp: `≈`,
    asympeq: `≍`,
    atilde: `ã`,
    auml: `ä`,
    awconint: `∳`,
    awint: `⨑`,
    bNot: `⫭`,
    backcong: `≌`,
    backepsilon: `϶`,
    backprime: `‵`,
    backsim: `∽`,
    backsimeq: `⋍`,
    barvee: `⊽`,
    barwed: `⌅`,
    barwedge: `⌅`,
    bbrk: `⎵`,
    bbrktbrk: `⎶`,
    bcong: `≌`,
    bcy: `б`,
    bdquo: `„`,
    becaus: `∵`,
    because: `∵`,
    bemptyv: `⦰`,
    bepsi: `϶`,
    bernou: `ℬ`,
    beta: `β`,
    beth: `ℶ`,
    between: `≬`,
    bfr: `𝔟`,
    bigcap: `⋂`,
    bigcirc: `◯`,
    bigcup: `⋃`,
    bigodot: `⨀`,
    bigoplus: `⨁`,
    bigotimes: `⨂`,
    bigsqcup: `⨆`,
    bigstar: `★`,
    bigtriangledown: `▽`,
    bigtriangleup: `△`,
    biguplus: `⨄`,
    bigvee: `⋁`,
    bigwedge: `⋀`,
    bkarow: `⤍`,
    blacklozenge: `⧫`,
    blacksquare: `▪`,
    blacktriangle: `▴`,
    blacktriangledown: `▾`,
    blacktriangleleft: `◂`,
    blacktriangleright: `▸`,
    blank: `␣`,
    blk12: `▒`,
    blk14: `░`,
    blk34: `▓`,
    block: `█`,
    bne: `=⃥`,
    bnequiv: `≡⃥`,
    bnot: `⌐`,
    bopf: `𝕓`,
    bot: `⊥`,
    bottom: `⊥`,
    bowtie: `⋈`,
    boxDL: `╗`,
    boxDR: `╔`,
    boxDl: `╖`,
    boxDr: `╓`,
    boxH: `═`,
    boxHD: `╦`,
    boxHU: `╩`,
    boxHd: `╤`,
    boxHu: `╧`,
    boxUL: `╝`,
    boxUR: `╚`,
    boxUl: `╜`,
    boxUr: `╙`,
    boxV: `║`,
    boxVH: `╬`,
    boxVL: `╣`,
    boxVR: `╠`,
    boxVh: `╫`,
    boxVl: `╢`,
    boxVr: `╟`,
    boxbox: `⧉`,
    boxdL: `╕`,
    boxdR: `╒`,
    boxdl: `┐`,
    boxdr: `┌`,
    boxh: `─`,
    boxhD: `╥`,
    boxhU: `╨`,
    boxhd: `┬`,
    boxhu: `┴`,
    boxminus: `⊟`,
    boxplus: `⊞`,
    boxtimes: `⊠`,
    boxuL: `╛`,
    boxuR: `╘`,
    boxul: `┘`,
    boxur: `└`,
    boxv: `│`,
    boxvH: `╪`,
    boxvL: `╡`,
    boxvR: `╞`,
    boxvh: `┼`,
    boxvl: `┤`,
    boxvr: `├`,
    bprime: `‵`,
    breve: `˘`,
    brvbar: `¦`,
    bscr: `𝒷`,
    bsemi: `⁏`,
    bsim: `∽`,
    bsime: `⋍`,
    bsol: `\\`,
    bsolb: `⧅`,
    bsolhsub: `⟈`,
    bull: `•`,
    bullet: `•`,
    bump: `≎`,
    bumpE: `⪮`,
    bumpe: `≏`,
    bumpeq: `≏`,
    cacute: `ć`,
    cap: `∩`,
    capand: `⩄`,
    capbrcup: `⩉`,
    capcap: `⩋`,
    capcup: `⩇`,
    capdot: `⩀`,
    caps: `∩︀`,
    caret: `⁁`,
    caron: `ˇ`,
    ccaps: `⩍`,
    ccaron: `č`,
    ccedil: `ç`,
    ccirc: `ĉ`,
    ccups: `⩌`,
    ccupssm: `⩐`,
    cdot: `ċ`,
    cedil: `¸`,
    cemptyv: `⦲`,
    cent: `¢`,
    centerdot: `·`,
    cfr: `𝔠`,
    chcy: `ч`,
    check: `✓`,
    checkmark: `✓`,
    chi: `χ`,
    cir: `○`,
    cirE: `⧃`,
    circ: `ˆ`,
    circeq: `≗`,
    circlearrowleft: `↺`,
    circlearrowright: `↻`,
    circledR: `®`,
    circledS: `Ⓢ`,
    circledast: `⊛`,
    circledcirc: `⊚`,
    circleddash: `⊝`,
    cire: `≗`,
    cirfnint: `⨐`,
    cirmid: `⫯`,
    cirscir: `⧂`,
    clubs: `♣`,
    clubsuit: `♣`,
    colon: `:`,
    colone: `≔`,
    coloneq: `≔`,
    comma: `,`,
    commat: `@`,
    comp: `∁`,
    compfn: `∘`,
    complement: `∁`,
    complexes: `ℂ`,
    cong: `≅`,
    congdot: `⩭`,
    conint: `∮`,
    copf: `𝕔`,
    coprod: `∐`,
    copy: `©`,
    copysr: `℗`,
    crarr: `↵`,
    cross: `✗`,
    cscr: `𝒸`,
    csub: `⫏`,
    csube: `⫑`,
    csup: `⫐`,
    csupe: `⫒`,
    ctdot: `⋯`,
    cudarrl: `⤸`,
    cudarrr: `⤵`,
    cuepr: `⋞`,
    cuesc: `⋟`,
    cularr: `↶`,
    cularrp: `⤽`,
    cup: `∪`,
    cupbrcap: `⩈`,
    cupcap: `⩆`,
    cupcup: `⩊`,
    cupdot: `⊍`,
    cupor: `⩅`,
    cups: `∪︀`,
    curarr: `↷`,
    curarrm: `⤼`,
    curlyeqprec: `⋞`,
    curlyeqsucc: `⋟`,
    curlyvee: `⋎`,
    curlywedge: `⋏`,
    curren: `¤`,
    curvearrowleft: `↶`,
    curvearrowright: `↷`,
    cuvee: `⋎`,
    cuwed: `⋏`,
    cwconint: `∲`,
    cwint: `∱`,
    cylcty: `⌭`,
    dArr: `⇓`,
    dHar: `⥥`,
    dagger: `†`,
    daleth: `ℸ`,
    darr: `↓`,
    dash: `‐`,
    dashv: `⊣`,
    dbkarow: `⤏`,
    dblac: `˝`,
    dcaron: `ď`,
    dcy: `д`,
    dd: `ⅆ`,
    ddagger: `‡`,
    ddarr: `⇊`,
    ddotseq: `⩷`,
    deg: `°`,
    delta: `δ`,
    demptyv: `⦱`,
    dfisht: `⥿`,
    dfr: `𝔡`,
    dharl: `⇃`,
    dharr: `⇂`,
    diam: `⋄`,
    diamond: `⋄`,
    diamondsuit: `♦`,
    diams: `♦`,
    die: `¨`,
    digamma: `ϝ`,
    disin: `⋲`,
    div: `÷`,
    divide: `÷`,
    divideontimes: `⋇`,
    divonx: `⋇`,
    djcy: `ђ`,
    dlcorn: `⌞`,
    dlcrop: `⌍`,
    dollar: `$`,
    dopf: `𝕕`,
    dot: `˙`,
    doteq: `≐`,
    doteqdot: `≑`,
    dotminus: `∸`,
    dotplus: `∔`,
    dotsquare: `⊡`,
    doublebarwedge: `⌆`,
    downarrow: `↓`,
    downdownarrows: `⇊`,
    downharpoonleft: `⇃`,
    downharpoonright: `⇂`,
    drbkarow: `⤐`,
    drcorn: `⌟`,
    drcrop: `⌌`,
    dscr: `𝒹`,
    dscy: `ѕ`,
    dsol: `⧶`,
    dstrok: `đ`,
    dtdot: `⋱`,
    dtri: `▿`,
    dtrif: `▾`,
    duarr: `⇵`,
    duhar: `⥯`,
    dwangle: `⦦`,
    dzcy: `џ`,
    dzigrarr: `⟿`,
    eDDot: `⩷`,
    eDot: `≑`,
    eacute: `é`,
    easter: `⩮`,
    ecaron: `ě`,
    ecir: `≖`,
    ecirc: `ê`,
    ecolon: `≕`,
    ecy: `э`,
    edot: `ė`,
    ee: `ⅇ`,
    efDot: `≒`,
    efr: `𝔢`,
    eg: `⪚`,
    egrave: `è`,
    egs: `⪖`,
    egsdot: `⪘`,
    el: `⪙`,
    elinters: `⏧`,
    ell: `ℓ`,
    els: `⪕`,
    elsdot: `⪗`,
    emacr: `ē`,
    empty: `∅`,
    emptyset: `∅`,
    emptyv: `∅`,
    emsp13: ` `,
    emsp14: ` `,
    emsp: ` `,
    eng: `ŋ`,
    ensp: ` `,
    eogon: `ę`,
    eopf: `𝕖`,
    epar: `⋕`,
    eparsl: `⧣`,
    eplus: `⩱`,
    epsi: `ε`,
    epsilon: `ε`,
    epsiv: `ϵ`,
    eqcirc: `≖`,
    eqcolon: `≕`,
    eqsim: `≂`,
    eqslantgtr: `⪖`,
    eqslantless: `⪕`,
    equals: `=`,
    equest: `≟`,
    equiv: `≡`,
    equivDD: `⩸`,
    eqvparsl: `⧥`,
    erDot: `≓`,
    erarr: `⥱`,
    escr: `ℯ`,
    esdot: `≐`,
    esim: `≂`,
    eta: `η`,
    eth: `ð`,
    euml: `ë`,
    euro: `€`,
    excl: `!`,
    exist: `∃`,
    expectation: `ℰ`,
    exponentiale: `ⅇ`,
    fallingdotseq: `≒`,
    fcy: `ф`,
    female: `♀`,
    ffilig: `ﬃ`,
    fflig: `ﬀ`,
    ffllig: `ﬄ`,
    ffr: `𝔣`,
    filig: `ﬁ`,
    fjlig: `fj`,
    flat: `♭`,
    fllig: `ﬂ`,
    fltns: `▱`,
    fnof: `ƒ`,
    fopf: `𝕗`,
    forall: `∀`,
    fork: `⋔`,
    forkv: `⫙`,
    fpartint: `⨍`,
    frac12: `½`,
    frac13: `⅓`,
    frac14: `¼`,
    frac15: `⅕`,
    frac16: `⅙`,
    frac18: `⅛`,
    frac23: `⅔`,
    frac25: `⅖`,
    frac34: `¾`,
    frac35: `⅗`,
    frac38: `⅜`,
    frac45: `⅘`,
    frac56: `⅚`,
    frac58: `⅝`,
    frac78: `⅞`,
    frasl: `⁄`,
    frown: `⌢`,
    fscr: `𝒻`,
    gE: `≧`,
    gEl: `⪌`,
    gacute: `ǵ`,
    gamma: `γ`,
    gammad: `ϝ`,
    gap: `⪆`,
    gbreve: `ğ`,
    gcirc: `ĝ`,
    gcy: `г`,
    gdot: `ġ`,
    ge: `≥`,
    gel: `⋛`,
    geq: `≥`,
    geqq: `≧`,
    geqslant: `⩾`,
    ges: `⩾`,
    gescc: `⪩`,
    gesdot: `⪀`,
    gesdoto: `⪂`,
    gesdotol: `⪄`,
    gesl: `⋛︀`,
    gesles: `⪔`,
    gfr: `𝔤`,
    gg: `≫`,
    ggg: `⋙`,
    gimel: `ℷ`,
    gjcy: `ѓ`,
    gl: `≷`,
    glE: `⪒`,
    gla: `⪥`,
    glj: `⪤`,
    gnE: `≩`,
    gnap: `⪊`,
    gnapprox: `⪊`,
    gne: `⪈`,
    gneq: `⪈`,
    gneqq: `≩`,
    gnsim: `⋧`,
    gopf: `𝕘`,
    grave: "`",
    gscr: `ℊ`,
    gsim: `≳`,
    gsime: `⪎`,
    gsiml: `⪐`,
    gt: `>`,
    gtcc: `⪧`,
    gtcir: `⩺`,
    gtdot: `⋗`,
    gtlPar: `⦕`,
    gtquest: `⩼`,
    gtrapprox: `⪆`,
    gtrarr: `⥸`,
    gtrdot: `⋗`,
    gtreqless: `⋛`,
    gtreqqless: `⪌`,
    gtrless: `≷`,
    gtrsim: `≳`,
    gvertneqq: `≩︀`,
    gvnE: `≩︀`,
    hArr: `⇔`,
    hairsp: ` `,
    half: `½`,
    hamilt: `ℋ`,
    hardcy: `ъ`,
    harr: `↔`,
    harrcir: `⥈`,
    harrw: `↭`,
    hbar: `ℏ`,
    hcirc: `ĥ`,
    hearts: `♥`,
    heartsuit: `♥`,
    hellip: `…`,
    hercon: `⊹`,
    hfr: `𝔥`,
    hksearow: `⤥`,
    hkswarow: `⤦`,
    hoarr: `⇿`,
    homtht: `∻`,
    hookleftarrow: `↩`,
    hookrightarrow: `↪`,
    hopf: `𝕙`,
    horbar: `―`,
    hscr: `𝒽`,
    hslash: `ℏ`,
    hstrok: `ħ`,
    hybull: `⁃`,
    hyphen: `‐`,
    iacute: `í`,
    ic: `⁣`,
    icirc: `î`,
    icy: `и`,
    iecy: `е`,
    iexcl: `¡`,
    iff: `⇔`,
    ifr: `𝔦`,
    igrave: `ì`,
    ii: `ⅈ`,
    iiiint: `⨌`,
    iiint: `∭`,
    iinfin: `⧜`,
    iiota: `℩`,
    ijlig: `ĳ`,
    imacr: `ī`,
    image: `ℑ`,
    imagline: `ℐ`,
    imagpart: `ℑ`,
    imath: `ı`,
    imof: `⊷`,
    imped: `Ƶ`,
    in: `∈`,
    incare: `℅`,
    infin: `∞`,
    infintie: `⧝`,
    inodot: `ı`,
    int: `∫`,
    intcal: `⊺`,
    integers: `ℤ`,
    intercal: `⊺`,
    intlarhk: `⨗`,
    intprod: `⨼`,
    iocy: `ё`,
    iogon: `į`,
    iopf: `𝕚`,
    iota: `ι`,
    iprod: `⨼`,
    iquest: `¿`,
    iscr: `𝒾`,
    isin: `∈`,
    isinE: `⋹`,
    isindot: `⋵`,
    isins: `⋴`,
    isinsv: `⋳`,
    isinv: `∈`,
    it: `⁢`,
    itilde: `ĩ`,
    iukcy: `і`,
    iuml: `ï`,
    jcirc: `ĵ`,
    jcy: `й`,
    jfr: `𝔧`,
    jmath: `ȷ`,
    jopf: `𝕛`,
    jscr: `𝒿`,
    jsercy: `ј`,
    jukcy: `є`,
    kappa: `κ`,
    kappav: `ϰ`,
    kcedil: `ķ`,
    kcy: `к`,
    kfr: `𝔨`,
    kgreen: `ĸ`,
    khcy: `х`,
    kjcy: `ќ`,
    kopf: `𝕜`,
    kscr: `𝓀`,
    lAarr: `⇚`,
    lArr: `⇐`,
    lAtail: `⤛`,
    lBarr: `⤎`,
    lE: `≦`,
    lEg: `⪋`,
    lHar: `⥢`,
    lacute: `ĺ`,
    laemptyv: `⦴`,
    lagran: `ℒ`,
    lambda: `λ`,
    lang: `⟨`,
    langd: `⦑`,
    langle: `⟨`,
    lap: `⪅`,
    laquo: `«`,
    larr: `←`,
    larrb: `⇤`,
    larrbfs: `⤟`,
    larrfs: `⤝`,
    larrhk: `↩`,
    larrlp: `↫`,
    larrpl: `⤹`,
    larrsim: `⥳`,
    larrtl: `↢`,
    lat: `⪫`,
    latail: `⤙`,
    late: `⪭`,
    lates: `⪭︀`,
    lbarr: `⤌`,
    lbbrk: `❲`,
    lbrace: `{`,
    lbrack: `[`,
    lbrke: `⦋`,
    lbrksld: `⦏`,
    lbrkslu: `⦍`,
    lcaron: `ľ`,
    lcedil: `ļ`,
    lceil: `⌈`,
    lcub: `{`,
    lcy: `л`,
    ldca: `⤶`,
    ldquo: `“`,
    ldquor: `„`,
    ldrdhar: `⥧`,
    ldrushar: `⥋`,
    ldsh: `↲`,
    le: `≤`,
    leftarrow: `←`,
    leftarrowtail: `↢`,
    leftharpoondown: `↽`,
    leftharpoonup: `↼`,
    leftleftarrows: `⇇`,
    leftrightarrow: `↔`,
    leftrightarrows: `⇆`,
    leftrightharpoons: `⇋`,
    leftrightsquigarrow: `↭`,
    leftthreetimes: `⋋`,
    leg: `⋚`,
    leq: `≤`,
    leqq: `≦`,
    leqslant: `⩽`,
    les: `⩽`,
    lescc: `⪨`,
    lesdot: `⩿`,
    lesdoto: `⪁`,
    lesdotor: `⪃`,
    lesg: `⋚︀`,
    lesges: `⪓`,
    lessapprox: `⪅`,
    lessdot: `⋖`,
    lesseqgtr: `⋚`,
    lesseqqgtr: `⪋`,
    lessgtr: `≶`,
    lesssim: `≲`,
    lfisht: `⥼`,
    lfloor: `⌊`,
    lfr: `𝔩`,
    lg: `≶`,
    lgE: `⪑`,
    lhard: `↽`,
    lharu: `↼`,
    lharul: `⥪`,
    lhblk: `▄`,
    ljcy: `љ`,
    ll: `≪`,
    llarr: `⇇`,
    llcorner: `⌞`,
    llhard: `⥫`,
    lltri: `◺`,
    lmidot: `ŀ`,
    lmoust: `⎰`,
    lmoustache: `⎰`,
    lnE: `≨`,
    lnap: `⪉`,
    lnapprox: `⪉`,
    lne: `⪇`,
    lneq: `⪇`,
    lneqq: `≨`,
    lnsim: `⋦`,
    loang: `⟬`,
    loarr: `⇽`,
    lobrk: `⟦`,
    longleftarrow: `⟵`,
    longleftrightarrow: `⟷`,
    longmapsto: `⟼`,
    longrightarrow: `⟶`,
    looparrowleft: `↫`,
    looparrowright: `↬`,
    lopar: `⦅`,
    lopf: `𝕝`,
    loplus: `⨭`,
    lotimes: `⨴`,
    lowast: `∗`,
    lowbar: `_`,
    loz: `◊`,
    lozenge: `◊`,
    lozf: `⧫`,
    lpar: `(`,
    lparlt: `⦓`,
    lrarr: `⇆`,
    lrcorner: `⌟`,
    lrhar: `⇋`,
    lrhard: `⥭`,
    lrm: `‎`,
    lrtri: `⊿`,
    lsaquo: `‹`,
    lscr: `𝓁`,
    lsh: `↰`,
    lsim: `≲`,
    lsime: `⪍`,
    lsimg: `⪏`,
    lsqb: `[`,
    lsquo: `‘`,
    lsquor: `‚`,
    lstrok: `ł`,
    lt: `<`,
    ltcc: `⪦`,
    ltcir: `⩹`,
    ltdot: `⋖`,
    lthree: `⋋`,
    ltimes: `⋉`,
    ltlarr: `⥶`,
    ltquest: `⩻`,
    ltrPar: `⦖`,
    ltri: `◃`,
    ltrie: `⊴`,
    ltrif: `◂`,
    lurdshar: `⥊`,
    luruhar: `⥦`,
    lvertneqq: `≨︀`,
    lvnE: `≨︀`,
    mDDot: `∺`,
    macr: `¯`,
    male: `♂`,
    malt: `✠`,
    maltese: `✠`,
    map: `↦`,
    mapsto: `↦`,
    mapstodown: `↧`,
    mapstoleft: `↤`,
    mapstoup: `↥`,
    marker: `▮`,
    mcomma: `⨩`,
    mcy: `м`,
    mdash: `—`,
    measuredangle: `∡`,
    mfr: `𝔪`,
    mho: `℧`,
    micro: `µ`,
    mid: `∣`,
    midast: `*`,
    midcir: `⫰`,
    middot: `·`,
    minus: `−`,
    minusb: `⊟`,
    minusd: `∸`,
    minusdu: `⨪`,
    mlcp: `⫛`,
    mldr: `…`,
    mnplus: `∓`,
    models: `⊧`,
    mopf: `𝕞`,
    mp: `∓`,
    mscr: `𝓂`,
    mstpos: `∾`,
    mu: `μ`,
    multimap: `⊸`,
    mumap: `⊸`,
    nGg: `⋙̸`,
    nGt: `≫⃒`,
    nGtv: `≫̸`,
    nLeftarrow: `⇍`,
    nLeftrightarrow: `⇎`,
    nLl: `⋘̸`,
    nLt: `≪⃒`,
    nLtv: `≪̸`,
    nRightarrow: `⇏`,
    nVDash: `⊯`,
    nVdash: `⊮`,
    nabla: `∇`,
    nacute: `ń`,
    nang: `∠⃒`,
    nap: `≉`,
    napE: `⩰̸`,
    napid: `≋̸`,
    napos: `ŉ`,
    napprox: `≉`,
    natur: `♮`,
    natural: `♮`,
    naturals: `ℕ`,
    nbsp: `\xA0`,
    nbump: `≎̸`,
    nbumpe: `≏̸`,
    ncap: `⩃`,
    ncaron: `ň`,
    ncedil: `ņ`,
    ncong: `≇`,
    ncongdot: `⩭̸`,
    ncup: `⩂`,
    ncy: `н`,
    ndash: `–`,
    ne: `≠`,
    neArr: `⇗`,
    nearhk: `⤤`,
    nearr: `↗`,
    nearrow: `↗`,
    nedot: `≐̸`,
    nequiv: `≢`,
    nesear: `⤨`,
    nesim: `≂̸`,
    nexist: `∄`,
    nexists: `∄`,
    nfr: `𝔫`,
    ngE: `≧̸`,
    nge: `≱`,
    ngeq: `≱`,
    ngeqq: `≧̸`,
    ngeqslant: `⩾̸`,
    nges: `⩾̸`,
    ngsim: `≵`,
    ngt: `≯`,
    ngtr: `≯`,
    nhArr: `⇎`,
    nharr: `↮`,
    nhpar: `⫲`,
    ni: `∋`,
    nis: `⋼`,
    nisd: `⋺`,
    niv: `∋`,
    njcy: `њ`,
    nlArr: `⇍`,
    nlE: `≦̸`,
    nlarr: `↚`,
    nldr: `‥`,
    nle: `≰`,
    nleftarrow: `↚`,
    nleftrightarrow: `↮`,
    nleq: `≰`,
    nleqq: `≦̸`,
    nleqslant: `⩽̸`,
    nles: `⩽̸`,
    nless: `≮`,
    nlsim: `≴`,
    nlt: `≮`,
    nltri: `⋪`,
    nltrie: `⋬`,
    nmid: `∤`,
    nopf: `𝕟`,
    not: `¬`,
    notin: `∉`,
    notinE: `⋹̸`,
    notindot: `⋵̸`,
    notinva: `∉`,
    notinvb: `⋷`,
    notinvc: `⋶`,
    notni: `∌`,
    notniva: `∌`,
    notnivb: `⋾`,
    notnivc: `⋽`,
    npar: `∦`,
    nparallel: `∦`,
    nparsl: `⫽⃥`,
    npart: `∂̸`,
    npolint: `⨔`,
    npr: `⊀`,
    nprcue: `⋠`,
    npre: `⪯̸`,
    nprec: `⊀`,
    npreceq: `⪯̸`,
    nrArr: `⇏`,
    nrarr: `↛`,
    nrarrc: `⤳̸`,
    nrarrw: `↝̸`,
    nrightarrow: `↛`,
    nrtri: `⋫`,
    nrtrie: `⋭`,
    nsc: `⊁`,
    nsccue: `⋡`,
    nsce: `⪰̸`,
    nscr: `𝓃`,
    nshortmid: `∤`,
    nshortparallel: `∦`,
    nsim: `≁`,
    nsime: `≄`,
    nsimeq: `≄`,
    nsmid: `∤`,
    nspar: `∦`,
    nsqsube: `⋢`,
    nsqsupe: `⋣`,
    nsub: `⊄`,
    nsubE: `⫅̸`,
    nsube: `⊈`,
    nsubset: `⊂⃒`,
    nsubseteq: `⊈`,
    nsubseteqq: `⫅̸`,
    nsucc: `⊁`,
    nsucceq: `⪰̸`,
    nsup: `⊅`,
    nsupE: `⫆̸`,
    nsupe: `⊉`,
    nsupset: `⊃⃒`,
    nsupseteq: `⊉`,
    nsupseteqq: `⫆̸`,
    ntgl: `≹`,
    ntilde: `ñ`,
    ntlg: `≸`,
    ntriangleleft: `⋪`,
    ntrianglelefteq: `⋬`,
    ntriangleright: `⋫`,
    ntrianglerighteq: `⋭`,
    nu: `ν`,
    num: `#`,
    numero: `№`,
    numsp: ` `,
    nvDash: `⊭`,
    nvHarr: `⤄`,
    nvap: `≍⃒`,
    nvdash: `⊬`,
    nvge: `≥⃒`,
    nvgt: `>⃒`,
    nvinfin: `⧞`,
    nvlArr: `⤂`,
    nvle: `≤⃒`,
    nvlt: `<⃒`,
    nvltrie: `⊴⃒`,
    nvrArr: `⤃`,
    nvrtrie: `⊵⃒`,
    nvsim: `∼⃒`,
    nwArr: `⇖`,
    nwarhk: `⤣`,
    nwarr: `↖`,
    nwarrow: `↖`,
    nwnear: `⤧`,
    oS: `Ⓢ`,
    oacute: `ó`,
    oast: `⊛`,
    ocir: `⊚`,
    ocirc: `ô`,
    ocy: `о`,
    odash: `⊝`,
    odblac: `ő`,
    odiv: `⨸`,
    odot: `⊙`,
    odsold: `⦼`,
    oelig: `œ`,
    ofcir: `⦿`,
    ofr: `𝔬`,
    ogon: `˛`,
    ograve: `ò`,
    ogt: `⧁`,
    ohbar: `⦵`,
    ohm: `Ω`,
    oint: `∮`,
    olarr: `↺`,
    olcir: `⦾`,
    olcross: `⦻`,
    oline: `‾`,
    olt: `⧀`,
    omacr: `ō`,
    omega: `ω`,
    omicron: `ο`,
    omid: `⦶`,
    ominus: `⊖`,
    oopf: `𝕠`,
    opar: `⦷`,
    operp: `⦹`,
    oplus: `⊕`,
    or: `∨`,
    orarr: `↻`,
    ord: `⩝`,
    order: `ℴ`,
    orderof: `ℴ`,
    ordf: `ª`,
    ordm: `º`,
    origof: `⊶`,
    oror: `⩖`,
    orslope: `⩗`,
    orv: `⩛`,
    oscr: `ℴ`,
    oslash: `ø`,
    osol: `⊘`,
    otilde: `õ`,
    otimes: `⊗`,
    otimesas: `⨶`,
    ouml: `ö`,
    ovbar: `⌽`,
    par: `∥`,
    para: `¶`,
    parallel: `∥`,
    parsim: `⫳`,
    parsl: `⫽`,
    part: `∂`,
    pcy: `п`,
    percnt: `%`,
    period: `.`,
    permil: `‰`,
    perp: `⊥`,
    pertenk: `‱`,
    pfr: `𝔭`,
    phi: `φ`,
    phiv: `ϕ`,
    phmmat: `ℳ`,
    phone: `☎`,
    pi: `π`,
    pitchfork: `⋔`,
    piv: `ϖ`,
    planck: `ℏ`,
    planckh: `ℎ`,
    plankv: `ℏ`,
    plus: `+`,
    plusacir: `⨣`,
    plusb: `⊞`,
    pluscir: `⨢`,
    plusdo: `∔`,
    plusdu: `⨥`,
    pluse: `⩲`,
    plusmn: `±`,
    plussim: `⨦`,
    plustwo: `⨧`,
    pm: `±`,
    pointint: `⨕`,
    popf: `𝕡`,
    pound: `£`,
    pr: `≺`,
    prE: `⪳`,
    prap: `⪷`,
    prcue: `≼`,
    pre: `⪯`,
    prec: `≺`,
    precapprox: `⪷`,
    preccurlyeq: `≼`,
    preceq: `⪯`,
    precnapprox: `⪹`,
    precneqq: `⪵`,
    precnsim: `⋨`,
    precsim: `≾`,
    prime: `′`,
    primes: `ℙ`,
    prnE: `⪵`,
    prnap: `⪹`,
    prnsim: `⋨`,
    prod: `∏`,
    profalar: `⌮`,
    profline: `⌒`,
    profsurf: `⌓`,
    prop: `∝`,
    propto: `∝`,
    prsim: `≾`,
    prurel: `⊰`,
    pscr: `𝓅`,
    psi: `ψ`,
    puncsp: ` `,
    qfr: `𝔮`,
    qint: `⨌`,
    qopf: `𝕢`,
    qprime: `⁗`,
    qscr: `𝓆`,
    quaternions: `ℍ`,
    quatint: `⨖`,
    quest: `?`,
    questeq: `≟`,
    quot: `"`,
    rAarr: `⇛`,
    rArr: `⇒`,
    rAtail: `⤜`,
    rBarr: `⤏`,
    rHar: `⥤`,
    race: `∽̱`,
    racute: `ŕ`,
    radic: `√`,
    raemptyv: `⦳`,
    rang: `⟩`,
    rangd: `⦒`,
    range: `⦥`,
    rangle: `⟩`,
    raquo: `»`,
    rarr: `→`,
    rarrap: `⥵`,
    rarrb: `⇥`,
    rarrbfs: `⤠`,
    rarrc: `⤳`,
    rarrfs: `⤞`,
    rarrhk: `↪`,
    rarrlp: `↬`,
    rarrpl: `⥅`,
    rarrsim: `⥴`,
    rarrtl: `↣`,
    rarrw: `↝`,
    ratail: `⤚`,
    ratio: `∶`,
    rationals: `ℚ`,
    rbarr: `⤍`,
    rbbrk: `❳`,
    rbrace: `}`,
    rbrack: `]`,
    rbrke: `⦌`,
    rbrksld: `⦎`,
    rbrkslu: `⦐`,
    rcaron: `ř`,
    rcedil: `ŗ`,
    rceil: `⌉`,
    rcub: `}`,
    rcy: `р`,
    rdca: `⤷`,
    rdldhar: `⥩`,
    rdquo: `”`,
    rdquor: `”`,
    rdsh: `↳`,
    real: `ℜ`,
    realine: `ℛ`,
    realpart: `ℜ`,
    reals: `ℝ`,
    rect: `▭`,
    reg: `®`,
    rfisht: `⥽`,
    rfloor: `⌋`,
    rfr: `𝔯`,
    rhard: `⇁`,
    rharu: `⇀`,
    rharul: `⥬`,
    rho: `ρ`,
    rhov: `ϱ`,
    rightarrow: `→`,
    rightarrowtail: `↣`,
    rightharpoondown: `⇁`,
    rightharpoonup: `⇀`,
    rightleftarrows: `⇄`,
    rightleftharpoons: `⇌`,
    rightrightarrows: `⇉`,
    rightsquigarrow: `↝`,
    rightthreetimes: `⋌`,
    ring: `˚`,
    risingdotseq: `≓`,
    rlarr: `⇄`,
    rlhar: `⇌`,
    rlm: `‏`,
    rmoust: `⎱`,
    rmoustache: `⎱`,
    rnmid: `⫮`,
    roang: `⟭`,
    roarr: `⇾`,
    robrk: `⟧`,
    ropar: `⦆`,
    ropf: `𝕣`,
    roplus: `⨮`,
    rotimes: `⨵`,
    rpar: `)`,
    rpargt: `⦔`,
    rppolint: `⨒`,
    rrarr: `⇉`,
    rsaquo: `›`,
    rscr: `𝓇`,
    rsh: `↱`,
    rsqb: `]`,
    rsquo: `’`,
    rsquor: `’`,
    rthree: `⋌`,
    rtimes: `⋊`,
    rtri: `▹`,
    rtrie: `⊵`,
    rtrif: `▸`,
    rtriltri: `⧎`,
    ruluhar: `⥨`,
    rx: `℞`,
    sacute: `ś`,
    sbquo: `‚`,
    sc: `≻`,
    scE: `⪴`,
    scap: `⪸`,
    scaron: `š`,
    sccue: `≽`,
    sce: `⪰`,
    scedil: `ş`,
    scirc: `ŝ`,
    scnE: `⪶`,
    scnap: `⪺`,
    scnsim: `⋩`,
    scpolint: `⨓`,
    scsim: `≿`,
    scy: `с`,
    sdot: `⋅`,
    sdotb: `⊡`,
    sdote: `⩦`,
    seArr: `⇘`,
    searhk: `⤥`,
    searr: `↘`,
    searrow: `↘`,
    sect: `§`,
    semi: `;`,
    seswar: `⤩`,
    setminus: `∖`,
    setmn: `∖`,
    sext: `✶`,
    sfr: `𝔰`,
    sfrown: `⌢`,
    sharp: `♯`,
    shchcy: `щ`,
    shcy: `ш`,
    shortmid: `∣`,
    shortparallel: `∥`,
    shy: `­`,
    sigma: `σ`,
    sigmaf: `ς`,
    sigmav: `ς`,
    sim: `∼`,
    simdot: `⩪`,
    sime: `≃`,
    simeq: `≃`,
    simg: `⪞`,
    simgE: `⪠`,
    siml: `⪝`,
    simlE: `⪟`,
    simne: `≆`,
    simplus: `⨤`,
    simrarr: `⥲`,
    slarr: `←`,
    smallsetminus: `∖`,
    smashp: `⨳`,
    smeparsl: `⧤`,
    smid: `∣`,
    smile: `⌣`,
    smt: `⪪`,
    smte: `⪬`,
    smtes: `⪬︀`,
    softcy: `ь`,
    sol: `/`,
    solb: `⧄`,
    solbar: `⌿`,
    sopf: `𝕤`,
    spades: `♠`,
    spadesuit: `♠`,
    spar: `∥`,
    sqcap: `⊓`,
    sqcaps: `⊓︀`,
    sqcup: `⊔`,
    sqcups: `⊔︀`,
    sqsub: `⊏`,
    sqsube: `⊑`,
    sqsubset: `⊏`,
    sqsubseteq: `⊑`,
    sqsup: `⊐`,
    sqsupe: `⊒`,
    sqsupset: `⊐`,
    sqsupseteq: `⊒`,
    squ: `□`,
    square: `□`,
    squarf: `▪`,
    squf: `▪`,
    srarr: `→`,
    sscr: `𝓈`,
    ssetmn: `∖`,
    ssmile: `⌣`,
    sstarf: `⋆`,
    star: `☆`,
    starf: `★`,
    straightepsilon: `ϵ`,
    straightphi: `ϕ`,
    strns: `¯`,
    sub: `⊂`,
    subE: `⫅`,
    subdot: `⪽`,
    sube: `⊆`,
    subedot: `⫃`,
    submult: `⫁`,
    subnE: `⫋`,
    subne: `⊊`,
    subplus: `⪿`,
    subrarr: `⥹`,
    subset: `⊂`,
    subseteq: `⊆`,
    subseteqq: `⫅`,
    subsetneq: `⊊`,
    subsetneqq: `⫋`,
    subsim: `⫇`,
    subsub: `⫕`,
    subsup: `⫓`,
    succ: `≻`,
    succapprox: `⪸`,
    succcurlyeq: `≽`,
    succeq: `⪰`,
    succnapprox: `⪺`,
    succneqq: `⪶`,
    succnsim: `⋩`,
    succsim: `≿`,
    sum: `∑`,
    sung: `♪`,
    sup1: `¹`,
    sup2: `²`,
    sup3: `³`,
    sup: `⊃`,
    supE: `⫆`,
    supdot: `⪾`,
    supdsub: `⫘`,
    supe: `⊇`,
    supedot: `⫄`,
    suphsol: `⟉`,
    suphsub: `⫗`,
    suplarr: `⥻`,
    supmult: `⫂`,
    supnE: `⫌`,
    supne: `⊋`,
    supplus: `⫀`,
    supset: `⊃`,
    supseteq: `⊇`,
    supseteqq: `⫆`,
    supsetneq: `⊋`,
    supsetneqq: `⫌`,
    supsim: `⫈`,
    supsub: `⫔`,
    supsup: `⫖`,
    swArr: `⇙`,
    swarhk: `⤦`,
    swarr: `↙`,
    swarrow: `↙`,
    swnwar: `⤪`,
    szlig: `ß`,
    target: `⌖`,
    tau: `τ`,
    tbrk: `⎴`,
    tcaron: `ť`,
    tcedil: `ţ`,
    tcy: `т`,
    tdot: `⃛`,
    telrec: `⌕`,
    tfr: `𝔱`,
    there4: `∴`,
    therefore: `∴`,
    theta: `θ`,
    thetasym: `ϑ`,
    thetav: `ϑ`,
    thickapprox: `≈`,
    thicksim: `∼`,
    thinsp: ` `,
    thkap: `≈`,
    thksim: `∼`,
    thorn: `þ`,
    tilde: `˜`,
    times: `×`,
    timesb: `⊠`,
    timesbar: `⨱`,
    timesd: `⨰`,
    tint: `∭`,
    toea: `⤨`,
    top: `⊤`,
    topbot: `⌶`,
    topcir: `⫱`,
    topf: `𝕥`,
    topfork: `⫚`,
    tosa: `⤩`,
    tprime: `‴`,
    trade: `™`,
    triangle: `▵`,
    triangledown: `▿`,
    triangleleft: `◃`,
    trianglelefteq: `⊴`,
    triangleq: `≜`,
    triangleright: `▹`,
    trianglerighteq: `⊵`,
    tridot: `◬`,
    trie: `≜`,
    triminus: `⨺`,
    triplus: `⨹`,
    trisb: `⧍`,
    tritime: `⨻`,
    trpezium: `⏢`,
    tscr: `𝓉`,
    tscy: `ц`,
    tshcy: `ћ`,
    tstrok: `ŧ`,
    twixt: `≬`,
    twoheadleftarrow: `↞`,
    twoheadrightarrow: `↠`,
    uArr: `⇑`,
    uHar: `⥣`,
    uacute: `ú`,
    uarr: `↑`,
    ubrcy: `ў`,
    ubreve: `ŭ`,
    ucirc: `û`,
    ucy: `у`,
    udarr: `⇅`,
    udblac: `ű`,
    udhar: `⥮`,
    ufisht: `⥾`,
    ufr: `𝔲`,
    ugrave: `ù`,
    uharl: `↿`,
    uharr: `↾`,
    uhblk: `▀`,
    ulcorn: `⌜`,
    ulcorner: `⌜`,
    ulcrop: `⌏`,
    ultri: `◸`,
    umacr: `ū`,
    uml: `¨`,
    uogon: `ų`,
    uopf: `𝕦`,
    uparrow: `↑`,
    updownarrow: `↕`,
    upharpoonleft: `↿`,
    upharpoonright: `↾`,
    uplus: `⊎`,
    upsi: `υ`,
    upsih: `ϒ`,
    upsilon: `υ`,
    upuparrows: `⇈`,
    urcorn: `⌝`,
    urcorner: `⌝`,
    urcrop: `⌎`,
    uring: `ů`,
    urtri: `◹`,
    uscr: `𝓊`,
    utdot: `⋰`,
    utilde: `ũ`,
    utri: `▵`,
    utrif: `▴`,
    uuarr: `⇈`,
    uuml: `ü`,
    uwangle: `⦧`,
    vArr: `⇕`,
    vBar: `⫨`,
    vBarv: `⫩`,
    vDash: `⊨`,
    vangrt: `⦜`,
    varepsilon: `ϵ`,
    varkappa: `ϰ`,
    varnothing: `∅`,
    varphi: `ϕ`,
    varpi: `ϖ`,
    varpropto: `∝`,
    varr: `↕`,
    varrho: `ϱ`,
    varsigma: `ς`,
    varsubsetneq: `⊊︀`,
    varsubsetneqq: `⫋︀`,
    varsupsetneq: `⊋︀`,
    varsupsetneqq: `⫌︀`,
    vartheta: `ϑ`,
    vartriangleleft: `⊲`,
    vartriangleright: `⊳`,
    vcy: `в`,
    vdash: `⊢`,
    vee: `∨`,
    veebar: `⊻`,
    veeeq: `≚`,
    vellip: `⋮`,
    verbar: `|`,
    vert: `|`,
    vfr: `𝔳`,
    vltri: `⊲`,
    vnsub: `⊂⃒`,
    vnsup: `⊃⃒`,
    vopf: `𝕧`,
    vprop: `∝`,
    vrtri: `⊳`,
    vscr: `𝓋`,
    vsubnE: `⫋︀`,
    vsubne: `⊊︀`,
    vsupnE: `⫌︀`,
    vsupne: `⊋︀`,
    vzigzag: `⦚`,
    wcirc: `ŵ`,
    wedbar: `⩟`,
    wedge: `∧`,
    wedgeq: `≙`,
    weierp: `℘`,
    wfr: `𝔴`,
    wopf: `𝕨`,
    wp: `℘`,
    wr: `≀`,
    wreath: `≀`,
    wscr: `𝓌`,
    xcap: `⋂`,
    xcirc: `◯`,
    xcup: `⋃`,
    xdtri: `▽`,
    xfr: `𝔵`,
    xhArr: `⟺`,
    xharr: `⟷`,
    xi: `ξ`,
    xlArr: `⟸`,
    xlarr: `⟵`,
    xmap: `⟼`,
    xnis: `⋻`,
    xodot: `⨀`,
    xopf: `𝕩`,
    xoplus: `⨁`,
    xotime: `⨂`,
    xrArr: `⟹`,
    xrarr: `⟶`,
    xscr: `𝓍`,
    xsqcup: `⨆`,
    xuplus: `⨄`,
    xutri: `△`,
    xvee: `⋁`,
    xwedge: `⋀`,
    yacute: `ý`,
    yacy: `я`,
    ycirc: `ŷ`,
    ycy: `ы`,
    yen: `¥`,
    yfr: `𝔶`,
    yicy: `ї`,
    yopf: `𝕪`,
    yscr: `𝓎`,
    yucy: `ю`,
    yuml: `ÿ`,
    zacute: `ź`,
    zcaron: `ž`,
    zcy: `з`,
    zdot: `ż`,
    zeetrf: `ℨ`,
    zeta: `ζ`,
    zfr: `𝔷`,
    zhcy: `ж`,
    zigrarr: `⇝`,
    zopf: `𝕫`,
    zscr: `𝓏`,
    zwj: `‍`,
    zwnj: `‌`,
  },
  jd = {}.hasOwnProperty;
function Md(e) {
  return jd.call(Ad, e) ? Ad[e] : !1;
}
function Nd(e, t, n, r) {
  let i = e.length,
    a = 0,
    o;
  if (((t = t < 0 ? (-t > i ? 0 : i + t) : t > i ? i : t), (n = n > 0 ? n : 0), r.length < 1e4))
    ((o = Array.from(r)), o.unshift(t, n), e.splice(...o));
  else
    for (n && e.splice(t, n); a < r.length; )
      ((o = r.slice(a, a + 1e4)), o.unshift(t, 0), e.splice(...o), (a += 1e4), (t += 1e4));
}
function Pd(e, t) {
  return e.length > 0 ? (Nd(e, e.length, 0, t), e) : t;
}
var Fd = {}.hasOwnProperty;
function Id(e) {
  let t = {},
    n = -1;
  for (; ++n < e.length; ) Ld(t, e[n]);
  return t;
}
function Ld(e, t) {
  let n;
  for (n in t) {
    let r = (Fd.call(e, n) ? e[n] : void 0) || (e[n] = {}),
      i = t[n],
      a;
    if (i)
      for (a in i) {
        Fd.call(r, a) || (r[a] = []);
        let e = i[a];
        Rd(r[a], Array.isArray(e) ? e : e ? [e] : []);
      }
  }
}
function Rd(e, t) {
  let n = -1,
    r = [];
  for (; ++n < t.length; ) (t[n].add === `after` ? e : r).push(t[n]);
  Nd(e, 0, 0, r);
}
function zd(e, t) {
  let n = Number.parseInt(e, t);
  return n < 9 ||
    n === 11 ||
    (n > 13 && n < 32) ||
    (n > 126 && n < 160) ||
    (n > 55295 && n < 57344) ||
    (n > 64975 && n < 65008) ||
    (n & 65535) == 65535 ||
    (n & 65535) == 65534 ||
    n > 1114111
    ? `�`
    : String.fromCodePoint(n);
}
function Bd(e) {
  return e
    .replace(/[\t\n\r ]+/g, ` `)
    .replace(/^ | $/g, ``)
    .toLowerCase()
    .toUpperCase();
}
var Vd = Qd(/[A-Za-z]/),
  Hd = Qd(/[\dA-Za-z]/),
  Ud = Qd(/[#-'*+\--9=?A-Z^-~]/);
function Wd(e) {
  return e !== null && (e < 32 || e === 127);
}
var Gd = Qd(/\d/),
  Kd = Qd(/[\dA-Fa-f]/),
  qd = Qd(/[!-/:-@[-`{-~]/);
function B(e) {
  return e !== null && e < -2;
}
function Jd(e) {
  return e !== null && (e < 0 || e === 32);
}
function Yd(e) {
  return e === -2 || e === -1 || e === 32;
}
var Xd = Qd(/\p{P}|\p{S}/u),
  Zd = Qd(/\s/);
function Qd(e) {
  return t;
  function t(t) {
    return t !== null && t > -1 && e.test(String.fromCharCode(t));
  }
}
function $d(e, t, n, r) {
  let i = r ? r - 1 : 1 / 0,
    a = 0;
  return o;
  function o(r) {
    return Yd(r) ? (e.enter(n), s(r)) : t(r);
  }
  function s(r) {
    return Yd(r) && a++ < i ? (e.consume(r), s) : (e.exit(n), t(r));
  }
}
var ef = { tokenize: tf };
function tf(e) {
  let t = e.attempt(this.parser.constructs.contentInitial, r, i),
    n;
  return t;
  function r(n) {
    if (n === null) {
      e.consume(n);
      return;
    }
    return (e.enter(`lineEnding`), e.consume(n), e.exit(`lineEnding`), $d(e, t, `linePrefix`));
  }
  function i(t) {
    return (e.enter(`paragraph`), a(t));
  }
  function a(t) {
    let r = e.enter(`chunkText`, { contentType: `text`, previous: n });
    return (n && (n.next = r), (n = r), o(t));
  }
  function o(t) {
    if (t === null) {
      (e.exit(`chunkText`), e.exit(`paragraph`), e.consume(t));
      return;
    }
    return B(t) ? (e.consume(t), e.exit(`chunkText`), a) : (e.consume(t), o);
  }
}
var nf = { tokenize: af },
  rf = { tokenize: of };
function af(e) {
  let t = this,
    n = [],
    r = 0,
    i,
    a,
    o;
  return s;
  function s(i) {
    if (r < n.length) {
      let a = n[r];
      return ((t.containerState = a[1]), e.attempt(a[0].continuation, c, l)(i));
    }
    return l(i);
  }
  function c(e) {
    if ((r++, t.containerState._closeFlow)) {
      ((t.containerState._closeFlow = void 0), i && v());
      let n = t.events.length,
        a = n,
        o;
      for (; a--; )
        if (t.events[a][0] === `exit` && t.events[a][1].type === `chunkFlow`) {
          o = t.events[a][1].end;
          break;
        }
      _(r);
      let s = n;
      for (; s < t.events.length; ) ((t.events[s][1].end = { ...o }), s++);
      return (Nd(t.events, a + 1, 0, t.events.slice(n)), (t.events.length = s), l(e));
    }
    return s(e);
  }
  function l(a) {
    if (r === n.length) {
      if (!i) return f(a);
      if (i.currentConstruct && i.currentConstruct.concrete) return m(a);
      t.interrupt = !!(i.currentConstruct && !i._gfmTableDynamicInterruptHack);
    }
    return ((t.containerState = {}), e.check(rf, u, d)(a));
  }
  function u(e) {
    return (i && v(), _(r), f(e));
  }
  function d(e) {
    return ((t.parser.lazy[t.now().line] = r !== n.length), (o = t.now().offset), m(e));
  }
  function f(n) {
    return ((t.containerState = {}), e.attempt(rf, p, m)(n));
  }
  function p(e) {
    return (r++, n.push([t.currentConstruct, t.containerState]), f(e));
  }
  function m(n) {
    if (n === null) {
      (i && v(), _(0), e.consume(n));
      return;
    }
    return (
      (i ||= t.parser.flow(t.now())),
      e.enter(`chunkFlow`, { _tokenizer: i, contentType: `flow`, previous: a }),
      h(n)
    );
  }
  function h(n) {
    if (n === null) {
      (g(e.exit(`chunkFlow`), !0), _(0), e.consume(n));
      return;
    }
    return B(n)
      ? (e.consume(n), g(e.exit(`chunkFlow`)), (r = 0), (t.interrupt = void 0), s)
      : (e.consume(n), h);
  }
  function g(e, n) {
    let s = t.sliceStream(e);
    if (
      (n && s.push(null),
      (e.previous = a),
      a && (a.next = e),
      (a = e),
      i.defineSkip(e.start),
      i.write(s),
      t.parser.lazy[e.start.line])
    ) {
      let e = i.events.length;
      for (; e--; )
        if (
          i.events[e][1].start.offset < o &&
          (!i.events[e][1].end || i.events[e][1].end.offset > o)
        )
          return;
      let n = t.events.length,
        a = n,
        s,
        c;
      for (; a--; )
        if (t.events[a][0] === `exit` && t.events[a][1].type === `chunkFlow`) {
          if (s) {
            c = t.events[a][1].end;
            break;
          }
          s = !0;
        }
      for (_(r), e = n; e < t.events.length; ) ((t.events[e][1].end = { ...c }), e++);
      (Nd(t.events, a + 1, 0, t.events.slice(n)), (t.events.length = e));
    }
  }
  function _(r) {
    let i = n.length;
    for (; i-- > r; ) {
      let r = n[i];
      ((t.containerState = r[1]), r[0].exit.call(t, e));
    }
    n.length = r;
  }
  function v() {
    (i.write([null]), (a = void 0), (i = void 0), (t.containerState._closeFlow = void 0));
  }
}
function of(e, t, n) {
  return $d(
    e,
    e.attempt(this.parser.constructs.document, t, n),
    `linePrefix`,
    this.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
  );
}
function sf(e) {
  if (e === null || Jd(e) || Zd(e)) return 1;
  if (Xd(e)) return 2;
}
function cf(e, t, n) {
  let r = [],
    i = -1;
  for (; ++i < e.length; ) {
    let a = e[i].resolveAll;
    a && !r.includes(a) && ((t = a(t, n)), r.push(a));
  }
  return t;
}
var lf = { name: `attention`, resolveAll: uf, tokenize: df };
function uf(e, t) {
  let n = -1,
    r,
    i,
    a,
    o,
    s,
    c,
    l,
    u;
  for (; ++n < e.length; )
    if (e[n][0] === `enter` && e[n][1].type === `attentionSequence` && e[n][1]._close) {
      for (r = n; r--; )
        if (
          e[r][0] === `exit` &&
          e[r][1].type === `attentionSequence` &&
          e[r][1]._open &&
          t.sliceSerialize(e[r][1]).charCodeAt(0) === t.sliceSerialize(e[n][1]).charCodeAt(0)
        ) {
          if (
            (e[r][1]._close || e[n][1]._open) &&
            (e[n][1].end.offset - e[n][1].start.offset) % 3 &&
            !(
              (e[r][1].end.offset -
                e[r][1].start.offset +
                e[n][1].end.offset -
                e[n][1].start.offset) %
              3
            )
          )
            continue;
          c =
            e[r][1].end.offset - e[r][1].start.offset > 1 &&
            e[n][1].end.offset - e[n][1].start.offset > 1
              ? 2
              : 1;
          let d = { ...e[r][1].end },
            f = { ...e[n][1].start };
          (ff(d, -c),
            ff(f, c),
            (o = {
              type: c > 1 ? `strongSequence` : `emphasisSequence`,
              start: d,
              end: { ...e[r][1].end },
            }),
            (s = {
              type: c > 1 ? `strongSequence` : `emphasisSequence`,
              start: { ...e[n][1].start },
              end: f,
            }),
            (a = {
              type: c > 1 ? `strongText` : `emphasisText`,
              start: { ...e[r][1].end },
              end: { ...e[n][1].start },
            }),
            (i = { type: c > 1 ? `strong` : `emphasis`, start: { ...o.start }, end: { ...s.end } }),
            (e[r][1].end = { ...o.start }),
            (e[n][1].start = { ...s.end }),
            (l = []),
            e[r][1].end.offset - e[r][1].start.offset &&
              (l = Pd(l, [
                [`enter`, e[r][1], t],
                [`exit`, e[r][1], t],
              ])),
            (l = Pd(l, [
              [`enter`, i, t],
              [`enter`, o, t],
              [`exit`, o, t],
              [`enter`, a, t],
            ])),
            (l = Pd(l, cf(t.parser.constructs.insideSpan.null, e.slice(r + 1, n), t))),
            (l = Pd(l, [
              [`exit`, a, t],
              [`enter`, s, t],
              [`exit`, s, t],
              [`exit`, i, t],
            ])),
            e[n][1].end.offset - e[n][1].start.offset
              ? ((u = 2),
                (l = Pd(l, [
                  [`enter`, e[n][1], t],
                  [`exit`, e[n][1], t],
                ])))
              : (u = 0),
            Nd(e, r - 1, n - r + 3, l),
            (n = r + l.length - u - 2));
          break;
        }
    }
  for (n = -1; ++n < e.length; ) e[n][1].type === `attentionSequence` && (e[n][1].type = `data`);
  return e;
}
function df(e, t) {
  let n = this.parser.constructs.attentionMarkers.null,
    r = this.previous,
    i = sf(r),
    a;
  return o;
  function o(t) {
    return ((a = t), e.enter(`attentionSequence`), s(t));
  }
  function s(o) {
    if (o === a) return (e.consume(o), s);
    let c = e.exit(`attentionSequence`),
      l = sf(o),
      u = !l || (l === 2 && i) || n.includes(o),
      d = !i || (i === 2 && l) || n.includes(r);
    return (
      (c._open = !!(a === 42 ? u : u && (i || !d))),
      (c._close = !!(a === 42 ? d : d && (l || !u))),
      t(o)
    );
  }
}
function ff(e, t) {
  ((e.column += t), (e.offset += t), (e._bufferIndex += t));
}
var pf = { name: `autolink`, tokenize: mf };
function mf(e, t, n) {
  let r = 0;
  return i;
  function i(t) {
    return (
      e.enter(`autolink`),
      e.enter(`autolinkMarker`),
      e.consume(t),
      e.exit(`autolinkMarker`),
      e.enter(`autolinkProtocol`),
      a
    );
  }
  function a(t) {
    return Vd(t) ? (e.consume(t), o) : t === 64 ? n(t) : l(t);
  }
  function o(e) {
    return e === 43 || e === 45 || e === 46 || Hd(e) ? ((r = 1), s(e)) : l(e);
  }
  function s(t) {
    return t === 58
      ? (e.consume(t), (r = 0), c)
      : (t === 43 || t === 45 || t === 46 || Hd(t)) && r++ < 32
        ? (e.consume(t), s)
        : ((r = 0), l(t));
  }
  function c(r) {
    return r === 62
      ? (e.exit(`autolinkProtocol`),
        e.enter(`autolinkMarker`),
        e.consume(r),
        e.exit(`autolinkMarker`),
        e.exit(`autolink`),
        t)
      : r === null || r === 32 || r === 60 || Wd(r)
        ? n(r)
        : (e.consume(r), c);
  }
  function l(t) {
    return t === 64 ? (e.consume(t), u) : Ud(t) ? (e.consume(t), l) : n(t);
  }
  function u(e) {
    return Hd(e) ? d(e) : n(e);
  }
  function d(n) {
    return n === 46
      ? (e.consume(n), (r = 0), u)
      : n === 62
        ? ((e.exit(`autolinkProtocol`).type = `autolinkEmail`),
          e.enter(`autolinkMarker`),
          e.consume(n),
          e.exit(`autolinkMarker`),
          e.exit(`autolink`),
          t)
        : f(n);
  }
  function f(t) {
    if ((t === 45 || Hd(t)) && r++ < 63) {
      let n = t === 45 ? f : d;
      return (e.consume(t), n);
    }
    return n(t);
  }
}
var hf = { partial: !0, tokenize: gf };
function gf(e, t, n) {
  return r;
  function r(t) {
    return Yd(t) ? $d(e, i, `linePrefix`)(t) : i(t);
  }
  function i(e) {
    return e === null || B(e) ? t(e) : n(e);
  }
}
var _f = { continuation: { tokenize: yf }, exit: bf, name: `blockQuote`, tokenize: vf };
function vf(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    if (t === 62) {
      let n = r.containerState;
      return (
        (n.open ||= (e.enter(`blockQuote`, { _container: !0 }), !0)),
        e.enter(`blockQuotePrefix`),
        e.enter(`blockQuoteMarker`),
        e.consume(t),
        e.exit(`blockQuoteMarker`),
        a
      );
    }
    return n(t);
  }
  function a(n) {
    return Yd(n)
      ? (e.enter(`blockQuotePrefixWhitespace`),
        e.consume(n),
        e.exit(`blockQuotePrefixWhitespace`),
        e.exit(`blockQuotePrefix`),
        t)
      : (e.exit(`blockQuotePrefix`), t(n));
  }
}
function yf(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return Yd(t)
      ? $d(
          e,
          a,
          `linePrefix`,
          r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
        )(t)
      : a(t);
  }
  function a(r) {
    return e.attempt(_f, t, n)(r);
  }
}
function bf(e) {
  e.exit(`blockQuote`);
}
var xf = { name: `characterEscape`, tokenize: Sf };
function Sf(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`characterEscape`), e.enter(`escapeMarker`), e.consume(t), e.exit(`escapeMarker`), i
    );
  }
  function i(r) {
    return qd(r)
      ? (e.enter(`characterEscapeValue`),
        e.consume(r),
        e.exit(`characterEscapeValue`),
        e.exit(`characterEscape`),
        t)
      : n(r);
  }
}
var Cf = { name: `characterReference`, tokenize: wf };
function wf(e, t, n) {
  let r = this,
    i = 0,
    a,
    o;
  return s;
  function s(t) {
    return (
      e.enter(`characterReference`),
      e.enter(`characterReferenceMarker`),
      e.consume(t),
      e.exit(`characterReferenceMarker`),
      c
    );
  }
  function c(t) {
    return t === 35
      ? (e.enter(`characterReferenceMarkerNumeric`),
        e.consume(t),
        e.exit(`characterReferenceMarkerNumeric`),
        l)
      : (e.enter(`characterReferenceValue`), (a = 31), (o = Hd), u(t));
  }
  function l(t) {
    return t === 88 || t === 120
      ? (e.enter(`characterReferenceMarkerHexadecimal`),
        e.consume(t),
        e.exit(`characterReferenceMarkerHexadecimal`),
        e.enter(`characterReferenceValue`),
        (a = 6),
        (o = Kd),
        u)
      : (e.enter(`characterReferenceValue`), (a = 7), (o = Gd), u(t));
  }
  function u(s) {
    if (s === 59 && i) {
      let i = e.exit(`characterReferenceValue`);
      return o === Hd && !Md(r.sliceSerialize(i))
        ? n(s)
        : (e.enter(`characterReferenceMarker`),
          e.consume(s),
          e.exit(`characterReferenceMarker`),
          e.exit(`characterReference`),
          t);
    }
    return o(s) && i++ < a ? (e.consume(s), u) : n(s);
  }
}
var Tf = { partial: !0, tokenize: Of },
  Ef = { concrete: !0, name: `codeFenced`, tokenize: Df };
function Df(e, t, n) {
  let r = this,
    i = { partial: !0, tokenize: x },
    a = 0,
    o = 0,
    s;
  return c;
  function c(e) {
    return l(e);
  }
  function l(t) {
    let n = r.events[r.events.length - 1];
    return (
      (a = n && n[1].type === `linePrefix` ? n[2].sliceSerialize(n[1], !0).length : 0),
      (s = t),
      e.enter(`codeFenced`),
      e.enter(`codeFencedFence`),
      e.enter(`codeFencedFenceSequence`),
      u(t)
    );
  }
  function u(t) {
    return t === s
      ? (o++, e.consume(t), u)
      : o < 3
        ? n(t)
        : (e.exit(`codeFencedFenceSequence`), Yd(t) ? $d(e, d, `whitespace`)(t) : d(t));
  }
  function d(n) {
    return n === null || B(n)
      ? (e.exit(`codeFencedFence`), r.interrupt ? t(n) : e.check(Tf, h, b)(n))
      : (e.enter(`codeFencedFenceInfo`), e.enter(`chunkString`, { contentType: `string` }), f(n));
  }
  function f(t) {
    return t === null || B(t)
      ? (e.exit(`chunkString`), e.exit(`codeFencedFenceInfo`), d(t))
      : Yd(t)
        ? (e.exit(`chunkString`), e.exit(`codeFencedFenceInfo`), $d(e, p, `whitespace`)(t))
        : t === 96 && t === s
          ? n(t)
          : (e.consume(t), f);
  }
  function p(t) {
    return t === null || B(t)
      ? d(t)
      : (e.enter(`codeFencedFenceMeta`), e.enter(`chunkString`, { contentType: `string` }), m(t));
  }
  function m(t) {
    return t === null || B(t)
      ? (e.exit(`chunkString`), e.exit(`codeFencedFenceMeta`), d(t))
      : t === 96 && t === s
        ? n(t)
        : (e.consume(t), m);
  }
  function h(t) {
    return e.attempt(i, b, g)(t);
  }
  function g(t) {
    return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), _);
  }
  function _(t) {
    return a > 0 && Yd(t) ? $d(e, v, `linePrefix`, a + 1)(t) : v(t);
  }
  function v(t) {
    return t === null || B(t) ? e.check(Tf, h, b)(t) : (e.enter(`codeFlowValue`), y(t));
  }
  function y(t) {
    return t === null || B(t) ? (e.exit(`codeFlowValue`), v(t)) : (e.consume(t), y);
  }
  function b(n) {
    return (e.exit(`codeFenced`), t(n));
  }
  function x(e, t, n) {
    let i = 0;
    return a;
    function a(t) {
      return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), c);
    }
    function c(t) {
      return (
        e.enter(`codeFencedFence`),
        Yd(t)
          ? $d(
              e,
              l,
              `linePrefix`,
              r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
            )(t)
          : l(t)
      );
    }
    function l(t) {
      return t === s ? (e.enter(`codeFencedFenceSequence`), u(t)) : n(t);
    }
    function u(t) {
      return t === s
        ? (i++, e.consume(t), u)
        : i >= o
          ? (e.exit(`codeFencedFenceSequence`), Yd(t) ? $d(e, d, `whitespace`)(t) : d(t))
          : n(t);
    }
    function d(r) {
      return r === null || B(r) ? (e.exit(`codeFencedFence`), t(r)) : n(r);
    }
  }
}
function Of(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return t === null ? n(t) : (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), a);
  }
  function a(e) {
    return r.parser.lazy[r.now().line] ? n(e) : t(e);
  }
}
var kf = { name: `codeIndented`, tokenize: jf },
  Af = { partial: !0, tokenize: Mf };
function jf(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (e.enter(`codeIndented`), $d(e, a, `linePrefix`, 5)(t));
  }
  function a(e) {
    let t = r.events[r.events.length - 1];
    return t && t[1].type === `linePrefix` && t[2].sliceSerialize(t[1], !0).length >= 4
      ? o(e)
      : n(e);
  }
  function o(t) {
    return t === null ? c(t) : B(t) ? e.attempt(Af, o, c)(t) : (e.enter(`codeFlowValue`), s(t));
  }
  function s(t) {
    return t === null || B(t) ? (e.exit(`codeFlowValue`), o(t)) : (e.consume(t), s);
  }
  function c(n) {
    return (e.exit(`codeIndented`), t(n));
  }
}
function Mf(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return r.parser.lazy[r.now().line]
      ? n(t)
      : B(t)
        ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), i)
        : $d(e, a, `linePrefix`, 5)(t);
  }
  function a(e) {
    let a = r.events[r.events.length - 1];
    return a && a[1].type === `linePrefix` && a[2].sliceSerialize(a[1], !0).length >= 4
      ? t(e)
      : B(e)
        ? i(e)
        : n(e);
  }
}
var Nf = { name: `codeText`, previous: Ff, resolve: Pf, tokenize: If };
function Pf(e) {
  let t = e.length - 4,
    n = 3,
    r,
    i;
  if (
    (e[n][1].type === `lineEnding` || e[n][1].type === `space`) &&
    (e[t][1].type === `lineEnding` || e[t][1].type === `space`)
  ) {
    for (r = n; ++r < t; )
      if (e[r][1].type === `codeTextData`) {
        ((e[n][1].type = `codeTextPadding`),
          (e[t][1].type = `codeTextPadding`),
          (n += 2),
          (t -= 2));
        break;
      }
  }
  for (r = n - 1, t++; ++r <= t; )
    i === void 0
      ? r !== t && e[r][1].type !== `lineEnding` && (i = r)
      : (r === t || e[r][1].type === `lineEnding`) &&
        ((e[i][1].type = `codeTextData`),
        r !== i + 2 &&
          ((e[i][1].end = e[r - 1][1].end),
          e.splice(i + 2, r - i - 2),
          (t -= r - i - 2),
          (r = i + 2)),
        (i = void 0));
  return e;
}
function Ff(e) {
  return e !== 96 || this.events[this.events.length - 1][1].type === `characterEscape`;
}
function If(e, t, n) {
  let r = 0,
    i,
    a;
  return o;
  function o(t) {
    return (e.enter(`codeText`), e.enter(`codeTextSequence`), s(t));
  }
  function s(t) {
    return t === 96 ? (e.consume(t), r++, s) : (e.exit(`codeTextSequence`), c(t));
  }
  function c(t) {
    return t === null
      ? n(t)
      : t === 32
        ? (e.enter(`space`), e.consume(t), e.exit(`space`), c)
        : t === 96
          ? ((a = e.enter(`codeTextSequence`)), (i = 0), u(t))
          : B(t)
            ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), c)
            : (e.enter(`codeTextData`), l(t));
  }
  function l(t) {
    return t === null || t === 32 || t === 96 || B(t)
      ? (e.exit(`codeTextData`), c(t))
      : (e.consume(t), l);
  }
  function u(n) {
    return n === 96
      ? (e.consume(n), i++, u)
      : i === r
        ? (e.exit(`codeTextSequence`), e.exit(`codeText`), t(n))
        : ((a.type = `codeTextData`), l(n));
  }
}
var Lf = class {
  constructor(e) {
    ((this.left = e ? [...e] : []), (this.right = []));
  }
  get(e) {
    if (e < 0 || e >= this.left.length + this.right.length)
      throw RangeError(
        "Cannot access index `" +
          e +
          "` in a splice buffer of size `" +
          (this.left.length + this.right.length) +
          "`",
      );
    return e < this.left.length
      ? this.left[e]
      : this.right[this.right.length - e + this.left.length - 1];
  }
  get length() {
    return this.left.length + this.right.length;
  }
  shift() {
    return (this.setCursor(0), this.right.pop());
  }
  slice(e, t) {
    let n = t ?? 1 / 0;
    return n < this.left.length
      ? this.left.slice(e, n)
      : e > this.left.length
        ? this.right
            .slice(
              this.right.length - n + this.left.length,
              this.right.length - e + this.left.length,
            )
            .reverse()
        : this.left
            .slice(e)
            .concat(this.right.slice(this.right.length - n + this.left.length).reverse());
  }
  splice(e, t, n) {
    let r = t || 0;
    this.setCursor(Math.trunc(e));
    let i = this.right.splice(this.right.length - r, 1 / 0);
    return (n && Rf(this.left, n), i.reverse());
  }
  pop() {
    return (this.setCursor(1 / 0), this.left.pop());
  }
  push(e) {
    (this.setCursor(1 / 0), this.left.push(e));
  }
  pushMany(e) {
    (this.setCursor(1 / 0), Rf(this.left, e));
  }
  unshift(e) {
    (this.setCursor(0), this.right.push(e));
  }
  unshiftMany(e) {
    (this.setCursor(0), Rf(this.right, e.reverse()));
  }
  setCursor(e) {
    if (
      !(
        e === this.left.length ||
        (e > this.left.length && this.right.length === 0) ||
        (e < 0 && this.left.length === 0)
      )
    )
      if (e < this.left.length) {
        let t = this.left.splice(e, 1 / 0);
        Rf(this.right, t.reverse());
      } else {
        let t = this.right.splice(this.left.length + this.right.length - e, 1 / 0);
        Rf(this.left, t.reverse());
      }
  }
};
function Rf(e, t) {
  let n = 0;
  if (t.length < 1e4) e.push(...t);
  else for (; n < t.length; ) (e.push(...t.slice(n, n + 1e4)), (n += 1e4));
}
function zf(e) {
  let t = {},
    n = -1,
    r,
    i,
    a,
    o,
    s,
    c,
    l,
    u = new Lf(e);
  for (; ++n < u.length; ) {
    for (; n in t; ) n = t[n];
    if (
      ((r = u.get(n)),
      n &&
        r[1].type === `chunkFlow` &&
        u.get(n - 1)[1].type === `listItemPrefix` &&
        ((c = r[1]._tokenizer.events),
        (a = 0),
        a < c.length && c[a][1].type === `lineEndingBlank` && (a += 2),
        a < c.length && c[a][1].type === `content`))
    )
      for (; ++a < c.length && c[a][1].type !== `content`; )
        c[a][1].type === `chunkText` && ((c[a][1]._isInFirstContentOfListItem = !0), a++);
    if (r[0] === `enter`) r[1].contentType && (Object.assign(t, Bf(u, n)), (n = t[n]), (l = !0));
    else if (r[1]._container) {
      for (a = n, i = void 0; a--; )
        if (((o = u.get(a)), o[1].type === `lineEnding` || o[1].type === `lineEndingBlank`))
          o[0] === `enter` &&
            (i && (u.get(i)[1].type = `lineEndingBlank`), (o[1].type = `lineEnding`), (i = a));
        else if (!(o[1].type === `linePrefix` || o[1].type === `listItemIndent`)) break;
      i &&
        ((r[1].end = { ...u.get(i)[1].start }),
        (s = u.slice(i, n)),
        s.unshift(r),
        u.splice(i, n - i + 1, s));
    }
  }
  return (Nd(e, 0, 1 / 0, u.slice(0)), !l);
}
function Bf(e, t) {
  let n = e.get(t)[1],
    r = e.get(t)[2],
    i = t - 1,
    a = [],
    o = n._tokenizer;
  o ||
    ((o = r.parser[n.contentType](n.start)),
    n._contentTypeTextTrailing && (o._contentTypeTextTrailing = !0));
  let s = o.events,
    c = [],
    l = {},
    u,
    d,
    f = -1,
    p = n,
    m = 0,
    h = 0,
    g = [h];
  for (; p; ) {
    for (; e.get(++i)[1] !== p; );
    (a.push(i),
      p._tokenizer ||
        ((u = r.sliceStream(p)),
        p.next || u.push(null),
        d && o.defineSkip(p.start),
        p._isInFirstContentOfListItem && (o._gfmTasklistFirstContentOfListItem = !0),
        o.write(u),
        p._isInFirstContentOfListItem && (o._gfmTasklistFirstContentOfListItem = void 0)),
      (d = p),
      (p = p.next));
  }
  for (p = n; ++f < s.length; )
    s[f][0] === `exit` &&
      s[f - 1][0] === `enter` &&
      s[f][1].type === s[f - 1][1].type &&
      s[f][1].start.line !== s[f][1].end.line &&
      ((h = f + 1), g.push(h), (p._tokenizer = void 0), (p.previous = void 0), (p = p.next));
  for (
    o.events = [], p ? ((p._tokenizer = void 0), (p.previous = void 0)) : g.pop(), f = g.length;
    f--;
  ) {
    let t = s.slice(g[f], g[f + 1]),
      n = a.pop();
    (c.push([n, n + t.length - 1]), e.splice(n, 2, t));
  }
  for (c.reverse(), f = -1; ++f < c.length; )
    ((l[m + c[f][0]] = m + c[f][1]), (m += c[f][1] - c[f][0] - 1));
  return l;
}
var Vf = { resolve: Uf, tokenize: Wf },
  Hf = { partial: !0, tokenize: Gf };
function Uf(e) {
  return (zf(e), e);
}
function Wf(e, t) {
  let n;
  return r;
  function r(t) {
    return (e.enter(`content`), (n = e.enter(`chunkContent`, { contentType: `content` })), i(t));
  }
  function i(t) {
    return t === null ? a(t) : B(t) ? e.check(Hf, o, a)(t) : (e.consume(t), i);
  }
  function a(n) {
    return (e.exit(`chunkContent`), e.exit(`content`), t(n));
  }
  function o(t) {
    return (
      e.consume(t),
      e.exit(`chunkContent`),
      (n.next = e.enter(`chunkContent`, { contentType: `content`, previous: n })),
      (n = n.next),
      i
    );
  }
}
function Gf(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.exit(`chunkContent`),
      e.enter(`lineEnding`),
      e.consume(t),
      e.exit(`lineEnding`),
      $d(e, a, `linePrefix`)
    );
  }
  function a(i) {
    if (i === null || B(i)) return n(i);
    let a = r.events[r.events.length - 1];
    return !r.parser.constructs.disable.null.includes(`codeIndented`) &&
      a &&
      a[1].type === `linePrefix` &&
      a[2].sliceSerialize(a[1], !0).length >= 4
      ? t(i)
      : e.interrupt(r.parser.constructs.flow, n, t)(i);
  }
}
function Kf(e, t, n, r, i, a, o, s, c) {
  let l = c || 1 / 0,
    u = 0;
  return d;
  function d(t) {
    return t === 60
      ? (e.enter(r), e.enter(i), e.enter(a), e.consume(t), e.exit(a), f)
      : t === null || t === 32 || t === 41 || Wd(t)
        ? n(t)
        : (e.enter(r),
          e.enter(o),
          e.enter(s),
          e.enter(`chunkString`, { contentType: `string` }),
          h(t));
  }
  function f(n) {
    return n === 62
      ? (e.enter(a), e.consume(n), e.exit(a), e.exit(i), e.exit(r), t)
      : (e.enter(s), e.enter(`chunkString`, { contentType: `string` }), p(n));
  }
  function p(t) {
    return t === 62
      ? (e.exit(`chunkString`), e.exit(s), f(t))
      : t === null || t === 60 || B(t)
        ? n(t)
        : (e.consume(t), t === 92 ? m : p);
  }
  function m(t) {
    return t === 60 || t === 62 || t === 92 ? (e.consume(t), p) : p(t);
  }
  function h(i) {
    return !u && (i === null || i === 41 || Jd(i))
      ? (e.exit(`chunkString`), e.exit(s), e.exit(o), e.exit(r), t(i))
      : u < l && i === 40
        ? (e.consume(i), u++, h)
        : i === 41
          ? (e.consume(i), u--, h)
          : i === null || i === 32 || i === 40 || Wd(i)
            ? n(i)
            : (e.consume(i), i === 92 ? g : h);
  }
  function g(t) {
    return t === 40 || t === 41 || t === 92 ? (e.consume(t), h) : h(t);
  }
}
function qf(e, t, n, r, i, a) {
  let o = this,
    s = 0,
    c;
  return l;
  function l(t) {
    return (e.enter(r), e.enter(i), e.consume(t), e.exit(i), e.enter(a), u);
  }
  function u(l) {
    return s > 999 ||
      l === null ||
      l === 91 ||
      (l === 93 && !c) ||
      (l === 94 && !s && `_hiddenFootnoteSupport` in o.parser.constructs)
      ? n(l)
      : l === 93
        ? (e.exit(a), e.enter(i), e.consume(l), e.exit(i), e.exit(r), t)
        : B(l)
          ? (e.enter(`lineEnding`), e.consume(l), e.exit(`lineEnding`), u)
          : (e.enter(`chunkString`, { contentType: `string` }), d(l));
  }
  function d(t) {
    return t === null || t === 91 || t === 93 || B(t) || s++ > 999
      ? (e.exit(`chunkString`), u(t))
      : (e.consume(t), (c ||= !Yd(t)), t === 92 ? f : d);
  }
  function f(t) {
    return t === 91 || t === 92 || t === 93 ? (e.consume(t), s++, d) : d(t);
  }
}
function Jf(e, t, n, r, i, a) {
  let o;
  return s;
  function s(t) {
    return t === 34 || t === 39 || t === 40
      ? (e.enter(r), e.enter(i), e.consume(t), e.exit(i), (o = t === 40 ? 41 : t), c)
      : n(t);
  }
  function c(n) {
    return n === o ? (e.enter(i), e.consume(n), e.exit(i), e.exit(r), t) : (e.enter(a), l(n));
  }
  function l(t) {
    return t === o
      ? (e.exit(a), c(o))
      : t === null
        ? n(t)
        : B(t)
          ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), $d(e, l, `linePrefix`))
          : (e.enter(`chunkString`, { contentType: `string` }), u(t));
  }
  function u(t) {
    return t === o || t === null || B(t)
      ? (e.exit(`chunkString`), l(t))
      : (e.consume(t), t === 92 ? d : u);
  }
  function d(t) {
    return t === o || t === 92 ? (e.consume(t), u) : u(t);
  }
}
function Yf(e, t) {
  let n;
  return r;
  function r(i) {
    return B(i)
      ? (e.enter(`lineEnding`), e.consume(i), e.exit(`lineEnding`), (n = !0), r)
      : Yd(i)
        ? $d(e, r, n ? `linePrefix` : `lineSuffix`)(i)
        : t(i);
  }
}
var Xf = { name: `definition`, tokenize: Qf },
  Zf = { partial: !0, tokenize: $f };
function Qf(e, t, n) {
  let r = this,
    i;
  return a;
  function a(t) {
    return (e.enter(`definition`), o(t));
  }
  function o(t) {
    return qf.call(
      r,
      e,
      s,
      n,
      `definitionLabel`,
      `definitionLabelMarker`,
      `definitionLabelString`,
    )(t);
  }
  function s(t) {
    return (
      (i = Bd(r.sliceSerialize(r.events[r.events.length - 1][1]).slice(1, -1))),
      t === 58 ? (e.enter(`definitionMarker`), e.consume(t), e.exit(`definitionMarker`), c) : n(t)
    );
  }
  function c(t) {
    return Jd(t) ? Yf(e, l)(t) : l(t);
  }
  function l(t) {
    return Kf(
      e,
      u,
      n,
      `definitionDestination`,
      `definitionDestinationLiteral`,
      `definitionDestinationLiteralMarker`,
      `definitionDestinationRaw`,
      `definitionDestinationString`,
    )(t);
  }
  function u(t) {
    return e.attempt(Zf, d, d)(t);
  }
  function d(t) {
    return Yd(t) ? $d(e, f, `whitespace`)(t) : f(t);
  }
  function f(a) {
    return a === null || B(a) ? (e.exit(`definition`), r.parser.defined.push(i), t(a)) : n(a);
  }
}
function $f(e, t, n) {
  return r;
  function r(t) {
    return Jd(t) ? Yf(e, i)(t) : n(t);
  }
  function i(t) {
    return Jf(e, a, n, `definitionTitle`, `definitionTitleMarker`, `definitionTitleString`)(t);
  }
  function a(t) {
    return Yd(t) ? $d(e, o, `whitespace`)(t) : o(t);
  }
  function o(e) {
    return e === null || B(e) ? t(e) : n(e);
  }
}
var ep = { name: `hardBreakEscape`, tokenize: tp };
function tp(e, t, n) {
  return r;
  function r(t) {
    return (e.enter(`hardBreakEscape`), e.consume(t), i);
  }
  function i(r) {
    return B(r) ? (e.exit(`hardBreakEscape`), t(r)) : n(r);
  }
}
var np = { name: `headingAtx`, resolve: rp, tokenize: ip };
function rp(e, t) {
  let n = e.length - 2,
    r = 3,
    i,
    a;
  return (
    e[r][1].type === `whitespace` && (r += 2),
    n - 2 > r && e[n][1].type === `whitespace` && (n -= 2),
    e[n][1].type === `atxHeadingSequence` &&
      (r === n - 1 || (n - 4 > r && e[n - 2][1].type === `whitespace`)) &&
      (n -= r + 1 === n ? 2 : 4),
    n > r &&
      ((i = { type: `atxHeadingText`, start: e[r][1].start, end: e[n][1].end }),
      (a = { type: `chunkText`, start: e[r][1].start, end: e[n][1].end, contentType: `text` }),
      Nd(e, r, n - r + 1, [
        [`enter`, i, t],
        [`enter`, a, t],
        [`exit`, a, t],
        [`exit`, i, t],
      ])),
    e
  );
}
function ip(e, t, n) {
  let r = 0;
  return i;
  function i(t) {
    return (e.enter(`atxHeading`), a(t));
  }
  function a(t) {
    return (e.enter(`atxHeadingSequence`), o(t));
  }
  function o(t) {
    return t === 35 && r++ < 6
      ? (e.consume(t), o)
      : t === null || Jd(t)
        ? (e.exit(`atxHeadingSequence`), s(t))
        : n(t);
  }
  function s(n) {
    return n === 35
      ? (e.enter(`atxHeadingSequence`), c(n))
      : n === null || B(n)
        ? (e.exit(`atxHeading`), t(n))
        : Yd(n)
          ? $d(e, s, `whitespace`)(n)
          : (e.enter(`atxHeadingText`), l(n));
  }
  function c(t) {
    return t === 35 ? (e.consume(t), c) : (e.exit(`atxHeadingSequence`), s(t));
  }
  function l(t) {
    return t === null || t === 35 || Jd(t) ? (e.exit(`atxHeadingText`), s(t)) : (e.consume(t), l);
  }
}
var ap =
    `address.article.aside.base.basefont.blockquote.body.caption.center.col.colgroup.dd.details.dialog.dir.div.dl.dt.fieldset.figcaption.figure.footer.form.frame.frameset.h1.h2.h3.h4.h5.h6.head.header.hr.html.iframe.legend.li.link.main.menu.menuitem.nav.noframes.ol.optgroup.option.p.param.search.section.summary.table.tbody.td.tfoot.th.thead.title.tr.track.ul`.split(
      `.`,
    ),
  op = [`pre`, `script`, `style`, `textarea`],
  sp = { concrete: !0, name: `htmlFlow`, resolveTo: up, tokenize: dp },
  cp = { partial: !0, tokenize: pp },
  lp = { partial: !0, tokenize: fp };
function up(e) {
  let t = e.length;
  for (; t-- && !(e[t][0] === `enter` && e[t][1].type === `htmlFlow`); );
  return (
    t > 1 &&
      e[t - 2][1].type === `linePrefix` &&
      ((e[t][1].start = e[t - 2][1].start),
      (e[t + 1][1].start = e[t - 2][1].start),
      e.splice(t - 2, 2)),
    e
  );
}
function dp(e, t, n) {
  let r = this,
    i,
    a,
    o,
    s,
    c;
  return l;
  function l(e) {
    return u(e);
  }
  function u(t) {
    return (e.enter(`htmlFlow`), e.enter(`htmlFlowData`), e.consume(t), d);
  }
  function d(s) {
    return s === 33
      ? (e.consume(s), f)
      : s === 47
        ? (e.consume(s), (a = !0), h)
        : s === 63
          ? (e.consume(s), (i = 3), r.interrupt ? t : I)
          : Vd(s)
            ? (e.consume(s), (o = String.fromCharCode(s)), g)
            : n(s);
  }
  function f(a) {
    return a === 45
      ? (e.consume(a), (i = 2), p)
      : a === 91
        ? (e.consume(a), (i = 5), (s = 0), m)
        : Vd(a)
          ? (e.consume(a), (i = 4), r.interrupt ? t : I)
          : n(a);
  }
  function p(i) {
    return i === 45 ? (e.consume(i), r.interrupt ? t : I) : n(i);
  }
  function m(i) {
    return i === `CDATA[`.charCodeAt(s++)
      ? (e.consume(i), s === 6 ? (r.interrupt ? t : O) : m)
      : n(i);
  }
  function h(t) {
    return Vd(t) ? (e.consume(t), (o = String.fromCharCode(t)), g) : n(t);
  }
  function g(s) {
    if (s === null || s === 47 || s === 62 || Jd(s)) {
      let c = s === 47,
        l = o.toLowerCase();
      return !c && !a && op.includes(l)
        ? ((i = 1), r.interrupt ? t(s) : O(s))
        : ap.includes(o.toLowerCase())
          ? ((i = 6), c ? (e.consume(s), _) : r.interrupt ? t(s) : O(s))
          : ((i = 7), r.interrupt && !r.parser.lazy[r.now().line] ? n(s) : a ? v(s) : y(s));
    }
    return s === 45 || Hd(s) ? (e.consume(s), (o += String.fromCharCode(s)), g) : n(s);
  }
  function _(i) {
    return i === 62 ? (e.consume(i), r.interrupt ? t : O) : n(i);
  }
  function v(t) {
    return Yd(t) ? (e.consume(t), v) : E(t);
  }
  function y(t) {
    return t === 47
      ? (e.consume(t), E)
      : t === 58 || t === 95 || Vd(t)
        ? (e.consume(t), b)
        : Yd(t)
          ? (e.consume(t), y)
          : E(t);
  }
  function b(t) {
    return t === 45 || t === 46 || t === 58 || t === 95 || Hd(t) ? (e.consume(t), b) : x(t);
  }
  function x(t) {
    return t === 61 ? (e.consume(t), S) : Yd(t) ? (e.consume(t), x) : y(t);
  }
  function S(t) {
    return t === null || t === 60 || t === 61 || t === 62 || t === 96
      ? n(t)
      : t === 34 || t === 39
        ? (e.consume(t), (c = t), C)
        : Yd(t)
          ? (e.consume(t), S)
          : w(t);
  }
  function C(t) {
    return t === c ? (e.consume(t), (c = null), T) : t === null || B(t) ? n(t) : (e.consume(t), C);
  }
  function w(t) {
    return t === null ||
      t === 34 ||
      t === 39 ||
      t === 47 ||
      t === 60 ||
      t === 61 ||
      t === 62 ||
      t === 96 ||
      Jd(t)
      ? x(t)
      : (e.consume(t), w);
  }
  function T(e) {
    return e === 47 || e === 62 || Yd(e) ? y(e) : n(e);
  }
  function E(t) {
    return t === 62 ? (e.consume(t), D) : n(t);
  }
  function D(t) {
    return t === null || B(t) ? O(t) : Yd(t) ? (e.consume(t), D) : n(t);
  }
  function O(t) {
    return t === 45 && i === 2
      ? (e.consume(t), M)
      : t === 60 && i === 1
        ? (e.consume(t), N)
        : t === 62 && i === 4
          ? (e.consume(t), ee)
          : t === 63 && i === 3
            ? (e.consume(t), I)
            : t === 93 && i === 5
              ? (e.consume(t), F)
              : B(t) && (i === 6 || i === 7)
                ? (e.exit(`htmlFlowData`), e.check(cp, te, k)(t))
                : t === null || B(t)
                  ? (e.exit(`htmlFlowData`), k(t))
                  : (e.consume(t), O);
  }
  function k(t) {
    return e.check(lp, A, te)(t);
  }
  function A(t) {
    return (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), j);
  }
  function j(t) {
    return t === null || B(t) ? k(t) : (e.enter(`htmlFlowData`), O(t));
  }
  function M(t) {
    return t === 45 ? (e.consume(t), I) : O(t);
  }
  function N(t) {
    return t === 47 ? (e.consume(t), (o = ``), P) : O(t);
  }
  function P(t) {
    if (t === 62) {
      let n = o.toLowerCase();
      return op.includes(n) ? (e.consume(t), ee) : O(t);
    }
    return Vd(t) && o.length < 8 ? (e.consume(t), (o += String.fromCharCode(t)), P) : O(t);
  }
  function F(t) {
    return t === 93 ? (e.consume(t), I) : O(t);
  }
  function I(t) {
    return t === 62 ? (e.consume(t), ee) : t === 45 && i === 2 ? (e.consume(t), I) : O(t);
  }
  function ee(t) {
    return t === null || B(t) ? (e.exit(`htmlFlowData`), te(t)) : (e.consume(t), ee);
  }
  function te(n) {
    return (e.exit(`htmlFlow`), t(n));
  }
}
function fp(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return B(t) ? (e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), a) : n(t);
  }
  function a(e) {
    return r.parser.lazy[r.now().line] ? n(e) : t(e);
  }
}
function pp(e, t, n) {
  return r;
  function r(r) {
    return (e.enter(`lineEnding`), e.consume(r), e.exit(`lineEnding`), e.attempt(hf, t, n));
  }
}
var mp = { name: `htmlText`, tokenize: hp };
function hp(e, t, n) {
  let r = this,
    i,
    a,
    o;
  return s;
  function s(t) {
    return (e.enter(`htmlText`), e.enter(`htmlTextData`), e.consume(t), c);
  }
  function c(t) {
    return t === 33
      ? (e.consume(t), l)
      : t === 47
        ? (e.consume(t), x)
        : t === 63
          ? (e.consume(t), y)
          : Vd(t)
            ? (e.consume(t), w)
            : n(t);
  }
  function l(t) {
    return t === 45
      ? (e.consume(t), u)
      : t === 91
        ? (e.consume(t), (a = 0), m)
        : Vd(t)
          ? (e.consume(t), v)
          : n(t);
  }
  function u(t) {
    return t === 45 ? (e.consume(t), p) : n(t);
  }
  function d(t) {
    return t === null
      ? n(t)
      : t === 45
        ? (e.consume(t), f)
        : B(t)
          ? ((o = d), N(t))
          : (e.consume(t), d);
  }
  function f(t) {
    return t === 45 ? (e.consume(t), p) : d(t);
  }
  function p(e) {
    return e === 62 ? M(e) : e === 45 ? f(e) : d(e);
  }
  function m(t) {
    return t === `CDATA[`.charCodeAt(a++) ? (e.consume(t), a === 6 ? h : m) : n(t);
  }
  function h(t) {
    return t === null
      ? n(t)
      : t === 93
        ? (e.consume(t), g)
        : B(t)
          ? ((o = h), N(t))
          : (e.consume(t), h);
  }
  function g(t) {
    return t === 93 ? (e.consume(t), _) : h(t);
  }
  function _(t) {
    return t === 62 ? M(t) : t === 93 ? (e.consume(t), _) : h(t);
  }
  function v(t) {
    return t === null || t === 62 ? M(t) : B(t) ? ((o = v), N(t)) : (e.consume(t), v);
  }
  function y(t) {
    return t === null
      ? n(t)
      : t === 63
        ? (e.consume(t), b)
        : B(t)
          ? ((o = y), N(t))
          : (e.consume(t), y);
  }
  function b(e) {
    return e === 62 ? M(e) : y(e);
  }
  function x(t) {
    return Vd(t) ? (e.consume(t), S) : n(t);
  }
  function S(t) {
    return t === 45 || Hd(t) ? (e.consume(t), S) : C(t);
  }
  function C(t) {
    return B(t) ? ((o = C), N(t)) : Yd(t) ? (e.consume(t), C) : M(t);
  }
  function w(t) {
    return t === 45 || Hd(t) ? (e.consume(t), w) : t === 47 || t === 62 || Jd(t) ? T(t) : n(t);
  }
  function T(t) {
    return t === 47
      ? (e.consume(t), M)
      : t === 58 || t === 95 || Vd(t)
        ? (e.consume(t), E)
        : B(t)
          ? ((o = T), N(t))
          : Yd(t)
            ? (e.consume(t), T)
            : M(t);
  }
  function E(t) {
    return t === 45 || t === 46 || t === 58 || t === 95 || Hd(t) ? (e.consume(t), E) : D(t);
  }
  function D(t) {
    return t === 61 ? (e.consume(t), O) : B(t) ? ((o = D), N(t)) : Yd(t) ? (e.consume(t), D) : T(t);
  }
  function O(t) {
    return t === null || t === 60 || t === 61 || t === 62 || t === 96
      ? n(t)
      : t === 34 || t === 39
        ? (e.consume(t), (i = t), k)
        : B(t)
          ? ((o = O), N(t))
          : Yd(t)
            ? (e.consume(t), O)
            : (e.consume(t), A);
  }
  function k(t) {
    return t === i
      ? (e.consume(t), (i = void 0), j)
      : t === null
        ? n(t)
        : B(t)
          ? ((o = k), N(t))
          : (e.consume(t), k);
  }
  function A(t) {
    return t === null || t === 34 || t === 39 || t === 60 || t === 61 || t === 96
      ? n(t)
      : t === 47 || t === 62 || Jd(t)
        ? T(t)
        : (e.consume(t), A);
  }
  function j(e) {
    return e === 47 || e === 62 || Jd(e) ? T(e) : n(e);
  }
  function M(r) {
    return r === 62 ? (e.consume(r), e.exit(`htmlTextData`), e.exit(`htmlText`), t) : n(r);
  }
  function N(t) {
    return (e.exit(`htmlTextData`), e.enter(`lineEnding`), e.consume(t), e.exit(`lineEnding`), P);
  }
  function P(t) {
    return Yd(t)
      ? $d(
          e,
          F,
          `linePrefix`,
          r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
        )(t)
      : F(t);
  }
  function F(t) {
    return (e.enter(`htmlTextData`), o(t));
  }
}
var gp = { name: `labelEnd`, resolveAll: bp, resolveTo: xp, tokenize: Sp },
  _p = { tokenize: Cp },
  vp = { tokenize: wp },
  yp = { tokenize: Tp };
function bp(e) {
  let t = -1,
    n = [];
  for (; ++t < e.length; ) {
    let r = e[t][1];
    if (
      (n.push(e[t]), r.type === `labelImage` || r.type === `labelLink` || r.type === `labelEnd`)
    ) {
      let e = r.type === `labelImage` ? 4 : 2;
      ((r.type = `data`), (t += e));
    }
  }
  return (e.length !== n.length && Nd(e, 0, e.length, n), e);
}
function xp(e, t) {
  let n = e.length,
    r = 0,
    i,
    a,
    o,
    s;
  for (; n--; )
    if (((i = e[n][1]), a)) {
      if (i.type === `link` || (i.type === `labelLink` && i._inactive)) break;
      e[n][0] === `enter` && i.type === `labelLink` && (i._inactive = !0);
    } else if (o) {
      if (
        e[n][0] === `enter` &&
        (i.type === `labelImage` || i.type === `labelLink`) &&
        !i._balanced &&
        ((a = n), i.type !== `labelLink`)
      ) {
        r = 2;
        break;
      }
    } else i.type === `labelEnd` && (o = n);
  let c = {
      type: e[a][1].type === `labelLink` ? `link` : `image`,
      start: { ...e[a][1].start },
      end: { ...e[e.length - 1][1].end },
    },
    l = { type: `label`, start: { ...e[a][1].start }, end: { ...e[o][1].end } },
    u = { type: `labelText`, start: { ...e[a + r + 2][1].end }, end: { ...e[o - 2][1].start } };
  return (
    (s = [
      [`enter`, c, t],
      [`enter`, l, t],
    ]),
    (s = Pd(s, e.slice(a + 1, a + r + 3))),
    (s = Pd(s, [[`enter`, u, t]])),
    (s = Pd(s, cf(t.parser.constructs.insideSpan.null, e.slice(a + r + 4, o - 3), t))),
    (s = Pd(s, [[`exit`, u, t], e[o - 2], e[o - 1], [`exit`, l, t]])),
    (s = Pd(s, e.slice(o + 1))),
    (s = Pd(s, [[`exit`, c, t]])),
    Nd(e, a, e.length, s),
    e
  );
}
function Sp(e, t, n) {
  let r = this,
    i = r.events.length,
    a,
    o;
  for (; i--; )
    if (
      (r.events[i][1].type === `labelImage` || r.events[i][1].type === `labelLink`) &&
      !r.events[i][1]._balanced
    ) {
      a = r.events[i][1];
      break;
    }
  return s;
  function s(t) {
    return a
      ? a._inactive
        ? d(t)
        : ((o = r.parser.defined.includes(Bd(r.sliceSerialize({ start: a.end, end: r.now() })))),
          e.enter(`labelEnd`),
          e.enter(`labelMarker`),
          e.consume(t),
          e.exit(`labelMarker`),
          e.exit(`labelEnd`),
          c)
      : n(t);
  }
  function c(t) {
    return t === 40
      ? e.attempt(_p, u, o ? u : d)(t)
      : t === 91
        ? e.attempt(vp, u, o ? l : d)(t)
        : o
          ? u(t)
          : d(t);
  }
  function l(t) {
    return e.attempt(yp, u, d)(t);
  }
  function u(e) {
    return t(e);
  }
  function d(e) {
    return ((a._balanced = !0), n(e));
  }
}
function Cp(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`resource`), e.enter(`resourceMarker`), e.consume(t), e.exit(`resourceMarker`), i
    );
  }
  function i(t) {
    return Jd(t) ? Yf(e, a)(t) : a(t);
  }
  function a(t) {
    return t === 41
      ? u(t)
      : Kf(
          e,
          o,
          s,
          `resourceDestination`,
          `resourceDestinationLiteral`,
          `resourceDestinationLiteralMarker`,
          `resourceDestinationRaw`,
          `resourceDestinationString`,
          32,
        )(t);
  }
  function o(t) {
    return Jd(t) ? Yf(e, c)(t) : u(t);
  }
  function s(e) {
    return n(e);
  }
  function c(t) {
    return t === 34 || t === 39 || t === 40
      ? Jf(e, l, n, `resourceTitle`, `resourceTitleMarker`, `resourceTitleString`)(t)
      : u(t);
  }
  function l(t) {
    return Jd(t) ? Yf(e, u)(t) : u(t);
  }
  function u(r) {
    return r === 41
      ? (e.enter(`resourceMarker`), e.consume(r), e.exit(`resourceMarker`), e.exit(`resource`), t)
      : n(r);
  }
}
function wp(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return qf.call(r, e, a, o, `reference`, `referenceMarker`, `referenceString`)(t);
  }
  function a(e) {
    return r.parser.defined.includes(
      Bd(r.sliceSerialize(r.events[r.events.length - 1][1]).slice(1, -1)),
    )
      ? t(e)
      : n(e);
  }
  function o(e) {
    return n(e);
  }
}
function Tp(e, t, n) {
  return r;
  function r(t) {
    return (
      e.enter(`reference`), e.enter(`referenceMarker`), e.consume(t), e.exit(`referenceMarker`), i
    );
  }
  function i(r) {
    return r === 93
      ? (e.enter(`referenceMarker`),
        e.consume(r),
        e.exit(`referenceMarker`),
        e.exit(`reference`),
        t)
      : n(r);
  }
}
var Ep = { name: `labelStartImage`, resolveAll: gp.resolveAll, tokenize: Dp };
function Dp(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.enter(`labelImage`),
      e.enter(`labelImageMarker`),
      e.consume(t),
      e.exit(`labelImageMarker`),
      a
    );
  }
  function a(t) {
    return t === 91
      ? (e.enter(`labelMarker`), e.consume(t), e.exit(`labelMarker`), e.exit(`labelImage`), o)
      : n(t);
  }
  function o(e) {
    return e === 94 && `_hiddenFootnoteSupport` in r.parser.constructs ? n(e) : t(e);
  }
}
var Op = { name: `labelStartLink`, resolveAll: gp.resolveAll, tokenize: kp };
function kp(e, t, n) {
  let r = this;
  return i;
  function i(t) {
    return (
      e.enter(`labelLink`),
      e.enter(`labelMarker`),
      e.consume(t),
      e.exit(`labelMarker`),
      e.exit(`labelLink`),
      a
    );
  }
  function a(e) {
    return e === 94 && `_hiddenFootnoteSupport` in r.parser.constructs ? n(e) : t(e);
  }
}
var Ap = { name: `lineEnding`, tokenize: jp };
function jp(e, t) {
  return n;
  function n(n) {
    return (e.enter(`lineEnding`), e.consume(n), e.exit(`lineEnding`), $d(e, t, `linePrefix`));
  }
}
var Mp = { name: `thematicBreak`, tokenize: Np };
function Np(e, t, n) {
  let r = 0,
    i;
  return a;
  function a(t) {
    return (e.enter(`thematicBreak`), o(t));
  }
  function o(e) {
    return ((i = e), s(e));
  }
  function s(a) {
    return a === i
      ? (e.enter(`thematicBreakSequence`), c(a))
      : r >= 3 && (a === null || B(a))
        ? (e.exit(`thematicBreak`), t(a))
        : n(a);
  }
  function c(t) {
    return t === i
      ? (e.consume(t), r++, c)
      : (e.exit(`thematicBreakSequence`), Yd(t) ? $d(e, s, `whitespace`)(t) : s(t));
  }
}
var Pp = { continuation: { tokenize: Rp }, exit: Bp, name: `list`, tokenize: Lp },
  Fp = { partial: !0, tokenize: Vp },
  Ip = { partial: !0, tokenize: zp };
function Lp(e, t, n) {
  let r = this,
    i = r.events[r.events.length - 1],
    a = i && i[1].type === `linePrefix` ? i[2].sliceSerialize(i[1], !0).length : 0,
    o = 0;
  return s;
  function s(t) {
    let i =
      r.containerState.type || (t === 42 || t === 43 || t === 45 ? `listUnordered` : `listOrdered`);
    if (i === `listUnordered` ? !r.containerState.marker || t === r.containerState.marker : Gd(t)) {
      if (
        (r.containerState.type || ((r.containerState.type = i), e.enter(i, { _container: !0 })),
        i === `listUnordered`)
      )
        return (e.enter(`listItemPrefix`), t === 42 || t === 45 ? e.check(Mp, n, l)(t) : l(t));
      if (!r.interrupt || t === 49)
        return (e.enter(`listItemPrefix`), e.enter(`listItemValue`), c(t));
    }
    return n(t);
  }
  function c(t) {
    return Gd(t) && ++o < 10
      ? (e.consume(t), c)
      : (!r.interrupt || o < 2) &&
          (r.containerState.marker ? t === r.containerState.marker : t === 41 || t === 46)
        ? (e.exit(`listItemValue`), l(t))
        : n(t);
  }
  function l(t) {
    return (
      e.enter(`listItemMarker`),
      e.consume(t),
      e.exit(`listItemMarker`),
      (r.containerState.marker = r.containerState.marker || t),
      e.check(hf, r.interrupt ? n : u, e.attempt(Fp, f, d))
    );
  }
  function u(e) {
    return ((r.containerState.initialBlankLine = !0), a++, f(e));
  }
  function d(t) {
    return Yd(t)
      ? (e.enter(`listItemPrefixWhitespace`), e.consume(t), e.exit(`listItemPrefixWhitespace`), f)
      : n(t);
  }
  function f(n) {
    return (
      (r.containerState.size = a + r.sliceSerialize(e.exit(`listItemPrefix`), !0).length), t(n)
    );
  }
}
function Rp(e, t, n) {
  let r = this;
  return ((r.containerState._closeFlow = void 0), e.check(hf, i, a));
  function i(n) {
    return (
      (r.containerState.furtherBlankLines =
        r.containerState.furtherBlankLines || r.containerState.initialBlankLine),
      $d(e, t, `listItemIndent`, r.containerState.size + 1)(n)
    );
  }
  function a(n) {
    return r.containerState.furtherBlankLines || !Yd(n)
      ? ((r.containerState.furtherBlankLines = void 0),
        (r.containerState.initialBlankLine = void 0),
        o(n))
      : ((r.containerState.furtherBlankLines = void 0),
        (r.containerState.initialBlankLine = void 0),
        e.attempt(Ip, t, o)(n));
  }
  function o(i) {
    return (
      (r.containerState._closeFlow = !0),
      (r.interrupt = void 0),
      $d(
        e,
        e.attempt(Pp, t, n),
        `linePrefix`,
        r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 4,
      )(i)
    );
  }
}
function zp(e, t, n) {
  let r = this;
  return $d(e, i, `listItemIndent`, r.containerState.size + 1);
  function i(e) {
    let i = r.events[r.events.length - 1];
    return i &&
      i[1].type === `listItemIndent` &&
      i[2].sliceSerialize(i[1], !0).length === r.containerState.size
      ? t(e)
      : n(e);
  }
}
function Bp(e) {
  e.exit(this.containerState.type);
}
function Vp(e, t, n) {
  let r = this;
  return $d(
    e,
    i,
    `listItemPrefixWhitespace`,
    r.parser.constructs.disable.null.includes(`codeIndented`) ? void 0 : 5,
  );
  function i(e) {
    let i = r.events[r.events.length - 1];
    return !Yd(e) && i && i[1].type === `listItemPrefixWhitespace` ? t(e) : n(e);
  }
}
var Hp = { name: `setextUnderline`, resolveTo: Up, tokenize: Wp };
function Up(e, t) {
  let n = e.length,
    r,
    i,
    a;
  for (; n--; )
    if (e[n][0] === `enter`) {
      if (e[n][1].type === `content`) {
        r = n;
        break;
      }
      e[n][1].type === `paragraph` && (i = n);
    } else
      (e[n][1].type === `content` && e.splice(n, 1),
        !a && e[n][1].type === `definition` && (a = n));
  let o = {
    type: `setextHeading`,
    start: { ...e[r][1].start },
    end: { ...e[e.length - 1][1].end },
  };
  return (
    (e[i][1].type = `setextHeadingText`),
    a
      ? (e.splice(i, 0, [`enter`, o, t]),
        e.splice(a + 1, 0, [`exit`, e[r][1], t]),
        (e[r][1].end = { ...e[a][1].end }))
      : (e[r][1] = o),
    e.push([`exit`, o, t]),
    e
  );
}
function Wp(e, t, n) {
  let r = this,
    i;
  return a;
  function a(t) {
    let a = r.events.length,
      s;
    for (; a--; )
      if (
        r.events[a][1].type !== `lineEnding` &&
        r.events[a][1].type !== `linePrefix` &&
        r.events[a][1].type !== `content`
      ) {
        s = r.events[a][1].type === `paragraph`;
        break;
      }
    return !r.parser.lazy[r.now().line] && (r.interrupt || s)
      ? (e.enter(`setextHeadingLine`), (i = t), o(t))
      : n(t);
  }
  function o(t) {
    return (e.enter(`setextHeadingLineSequence`), s(t));
  }
  function s(t) {
    return t === i
      ? (e.consume(t), s)
      : (e.exit(`setextHeadingLineSequence`), Yd(t) ? $d(e, c, `lineSuffix`)(t) : c(t));
  }
  function c(r) {
    return r === null || B(r) ? (e.exit(`setextHeadingLine`), t(r)) : n(r);
  }
}
var Gp = { tokenize: Kp };
function Kp(e) {
  let t = this,
    n = e.attempt(
      hf,
      r,
      e.attempt(
        this.parser.constructs.flowInitial,
        i,
        $d(e, e.attempt(this.parser.constructs.flow, i, e.attempt(Vf, i)), `linePrefix`),
      ),
    );
  return n;
  function r(r) {
    if (r === null) {
      e.consume(r);
      return;
    }
    return (
      e.enter(`lineEndingBlank`),
      e.consume(r),
      e.exit(`lineEndingBlank`),
      (t.currentConstruct = void 0),
      n
    );
  }
  function i(r) {
    if (r === null) {
      e.consume(r);
      return;
    }
    return (
      e.enter(`lineEnding`), e.consume(r), e.exit(`lineEnding`), (t.currentConstruct = void 0), n
    );
  }
}
var qp = { resolveAll: Zp() },
  Jp = Xp(`string`),
  Yp = Xp(`text`);
function Xp(e) {
  return { resolveAll: Zp(e === `text` ? Qp : void 0), tokenize: t };
  function t(t) {
    let n = this,
      r = this.parser.constructs[e],
      i = t.attempt(r, a, o);
    return a;
    function a(e) {
      return c(e) ? i(e) : o(e);
    }
    function o(e) {
      if (e === null) {
        t.consume(e);
        return;
      }
      return (t.enter(`data`), t.consume(e), s);
    }
    function s(e) {
      return c(e) ? (t.exit(`data`), i(e)) : (t.consume(e), s);
    }
    function c(e) {
      if (e === null) return !0;
      let t = r[e],
        i = -1;
      if (t)
        for (; ++i < t.length; ) {
          let e = t[i];
          if (!e.previous || e.previous.call(n, n.previous)) return !0;
        }
      return !1;
    }
  }
}
function Zp(e) {
  return t;
  function t(t, n) {
    let r = -1,
      i;
    for (; ++r <= t.length; )
      i === void 0
        ? t[r] && t[r][1].type === `data` && ((i = r), r++)
        : (!t[r] || t[r][1].type !== `data`) &&
          (r !== i + 2 &&
            ((t[i][1].end = t[r - 1][1].end), t.splice(i + 2, r - i - 2), (r = i + 2)),
          (i = void 0));
    return e ? e(t, n) : t;
  }
}
function Qp(e, t) {
  let n = 0;
  for (; ++n <= e.length; )
    if ((n === e.length || e[n][1].type === `lineEnding`) && e[n - 1][1].type === `data`) {
      let r = e[n - 1][1],
        i = t.sliceStream(r),
        a = i.length,
        o = -1,
        s = 0,
        c;
      for (; a--; ) {
        let e = i[a];
        if (typeof e == `string`) {
          for (o = e.length; e.charCodeAt(o - 1) === 32; ) (s++, o--);
          if (o) break;
          o = -1;
        } else if (e === -2) ((c = !0), s++);
        else if (e !== -1) {
          a++;
          break;
        }
      }
      if ((t._contentTypeTextTrailing && n === e.length && (s = 0), s)) {
        let i = {
          type: n === e.length || c || s < 2 ? `lineSuffix` : `hardBreakTrailing`,
          start: {
            _bufferIndex: a ? o : r.start._bufferIndex + o,
            _index: r.start._index + a,
            line: r.end.line,
            column: r.end.column - s,
            offset: r.end.offset - s,
          },
          end: { ...r.end },
        };
        ((r.end = { ...i.start }),
          r.start.offset === r.end.offset
            ? Object.assign(r, i)
            : (e.splice(n, 0, [`enter`, i, t], [`exit`, i, t]), (n += 2)));
      }
      n++;
    }
  return e;
}
var $p = e.i({
    attentionMarkers: () => sm,
    contentInitial: () => tm,
    disable: () => cm,
    document: () => em,
    flow: () => rm,
    flowInitial: () => nm,
    insideSpan: () => om,
    string: () => im,
    text: () => am,
  }),
  em = {
    42: Pp,
    43: Pp,
    45: Pp,
    48: Pp,
    49: Pp,
    50: Pp,
    51: Pp,
    52: Pp,
    53: Pp,
    54: Pp,
    55: Pp,
    56: Pp,
    57: Pp,
    62: _f,
  },
  tm = { 91: Xf },
  nm = { [-2]: kf, [-1]: kf, 32: kf },
  rm = { 35: np, 42: Mp, 45: [Hp, Mp], 60: sp, 61: Hp, 95: Mp, 96: Ef, 126: Ef },
  im = { 38: Cf, 92: xf },
  am = {
    [-5]: Ap,
    [-4]: Ap,
    [-3]: Ap,
    33: Ep,
    38: Cf,
    42: lf,
    60: [pf, mp],
    91: Op,
    92: [ep, xf],
    93: gp,
    95: lf,
    96: Nf,
  },
  om = { null: [lf, qp] },
  sm = { null: [42, 95] },
  cm = { null: [] };
function lm(e, t, n) {
  let r = {
      _bufferIndex: -1,
      _index: 0,
      line: (n && n.line) || 1,
      column: (n && n.column) || 1,
      offset: (n && n.offset) || 0,
    },
    i = {},
    a = [],
    o = [],
    s = [],
    c = {
      attempt: C(x),
      check: C(S),
      consume: v,
      enter: y,
      exit: b,
      interrupt: C(S, { interrupt: !0 }),
    },
    l = {
      code: null,
      containerState: {},
      defineSkip: h,
      events: [],
      now: m,
      parser: e,
      previous: null,
      sliceSerialize: f,
      sliceStream: p,
      write: d,
    },
    u = t.tokenize.call(l, c);
  return (t.resolveAll && a.push(t), l);
  function d(e) {
    return (
      (o = Pd(o, e)),
      g(),
      o[o.length - 1] === null ? (w(t, 0), (l.events = cf(a, l.events, l)), l.events) : []
    );
  }
  function f(e, t) {
    return dm(p(e), t);
  }
  function p(e) {
    return um(o, e);
  }
  function m() {
    let { _bufferIndex: e, _index: t, line: n, column: i, offset: a } = r;
    return { _bufferIndex: e, _index: t, line: n, column: i, offset: a };
  }
  function h(e) {
    ((i[e.line] = e.column), E());
  }
  function g() {
    let e;
    for (; r._index < o.length; ) {
      let t = o[r._index];
      if (typeof t == `string`)
        for (
          e = r._index, r._bufferIndex < 0 && (r._bufferIndex = 0);
          r._index === e && r._bufferIndex < t.length;
        )
          _(t.charCodeAt(r._bufferIndex));
      else _(t);
    }
  }
  function _(e) {
    u = u(e);
  }
  function v(e) {
    (B(e)
      ? (r.line++, (r.column = 1), (r.offset += e === -3 ? 2 : 1), E())
      : e !== -1 && (r.column++, r.offset++),
      r._bufferIndex < 0
        ? r._index++
        : (r._bufferIndex++,
          r._bufferIndex === o[r._index].length && ((r._bufferIndex = -1), r._index++)),
      (l.previous = e));
  }
  function y(e, t) {
    let n = t || {};
    return ((n.type = e), (n.start = m()), l.events.push([`enter`, n, l]), s.push(n), n);
  }
  function b(e) {
    let t = s.pop();
    return ((t.end = m()), l.events.push([`exit`, t, l]), t);
  }
  function x(e, t) {
    w(e, t.from);
  }
  function S(e, t) {
    t.restore();
  }
  function C(e, t) {
    return n;
    function n(n, r, i) {
      let a, o, s, u;
      return Array.isArray(n) ? f(n) : `tokenize` in n ? f([n]) : d(n);
      function d(e) {
        return t;
        function t(t) {
          let n = t !== null && e[t],
            r = t !== null && e.null;
          return f([
            ...(Array.isArray(n) ? n : n ? [n] : []),
            ...(Array.isArray(r) ? r : r ? [r] : []),
          ])(t);
        }
      }
      function f(e) {
        return ((a = e), (o = 0), e.length === 0 ? i : p(e[o]));
      }
      function p(e) {
        return n;
        function n(n) {
          return (
            (u = T()),
            (s = e),
            e.partial || (l.currentConstruct = e),
            e.name && l.parser.constructs.disable.null.includes(e.name)
              ? h(n)
              : e.tokenize.call(t ? Object.assign(Object.create(l), t) : l, c, m, h)(n)
          );
        }
      }
      function m(t) {
        return (e(s, u), r);
      }
      function h(e) {
        return (u.restore(), ++o < a.length ? p(a[o]) : i);
      }
    }
  }
  function w(e, t) {
    (e.resolveAll && !a.includes(e) && a.push(e),
      e.resolve && Nd(l.events, t, l.events.length - t, e.resolve(l.events.slice(t), l)),
      e.resolveTo && (l.events = e.resolveTo(l.events, l)));
  }
  function T() {
    let e = m(),
      t = l.previous,
      n = l.currentConstruct,
      i = l.events.length,
      a = Array.from(s);
    return { from: i, restore: o };
    function o() {
      ((r = e), (l.previous = t), (l.currentConstruct = n), (l.events.length = i), (s = a), E());
    }
  }
  function E() {
    r.line in i && r.column < 2 && ((r.column = i[r.line]), (r.offset += i[r.line] - 1));
  }
}
function um(e, t) {
  let n = t.start._index,
    r = t.start._bufferIndex,
    i = t.end._index,
    a = t.end._bufferIndex,
    o;
  if (n === i) o = [e[n].slice(r, a)];
  else {
    if (((o = e.slice(n, i)), r > -1)) {
      let e = o[0];
      typeof e == `string` ? (o[0] = e.slice(r)) : o.shift();
    }
    a > 0 && o.push(e[i].slice(0, a));
  }
  return o;
}
function dm(e, t) {
  let n = -1,
    r = [],
    i;
  for (; ++n < e.length; ) {
    let a = e[n],
      o;
    if (typeof a == `string`) o = a;
    else
      switch (a) {
        case -5:
          o = `\r`;
          break;
        case -4:
          o = `
`;
          break;
        case -3:
          o = `\r
`;
          break;
        case -2:
          o = t ? ` ` : `	`;
          break;
        case -1:
          if (!t && i) continue;
          o = ` `;
          break;
        default:
          o = String.fromCharCode(a);
      }
    ((i = a === -2), r.push(o));
  }
  return r.join(``);
}
function fm(e) {
  let t = {
    constructs: Id([$p, ...((e || {}).extensions || [])]),
    content: n(ef),
    defined: [],
    document: n(nf),
    flow: n(Gp),
    lazy: {},
    string: n(Jp),
    text: n(Yp),
  };
  return t;
  function n(e) {
    return n;
    function n(n) {
      return lm(t, e, n);
    }
  }
}
function pm(e) {
  for (; !zf(e); );
  return e;
}
var mm = /[\0\t\n\r]/g;
function hm() {
  let e = 1,
    t = ``,
    n = !0,
    r;
  return i;
  function i(i, a, o) {
    let s = [],
      c,
      l,
      u,
      d,
      f;
    for (
      i = t + (typeof i == `string` ? i.toString() : new TextDecoder(a || void 0).decode(i)),
        u = 0,
        t = ``,
        n &&= (i.charCodeAt(0) === 65279 && u++, void 0);
      u < i.length;
    ) {
      if (
        ((mm.lastIndex = u),
        (c = mm.exec(i)),
        (d = c && c.index !== void 0 ? c.index : i.length),
        (f = i.charCodeAt(d)),
        !c)
      ) {
        t = i.slice(u);
        break;
      }
      if (f === 10 && u === d && r) (s.push(-3), (r = void 0));
      else
        switch (((r &&= (s.push(-5), void 0)), u < d && (s.push(i.slice(u, d)), (e += d - u)), f)) {
          case 0:
            (s.push(65533), e++);
            break;
          case 9:
            for (l = Math.ceil(e / 4) * 4, s.push(-2); e++ < l; ) s.push(-1);
            break;
          case 10:
            (s.push(-4), (e = 1));
            break;
          default:
            ((r = !0), (e = 1));
        }
      u = d + 1;
    }
    return (o && (r && s.push(-5), t && s.push(t), s.push(null)), s);
  }
}
var gm = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function _m(e) {
  return e.replace(gm, vm);
}
function vm(e, t, n) {
  if (t) return t;
  if (n.charCodeAt(0) === 35) {
    let e = n.charCodeAt(1),
      t = e === 120 || e === 88;
    return zd(n.slice(t ? 2 : 1), t ? 16 : 10);
  }
  return Md(n) || e;
}
function ym(e) {
  return !e || typeof e != `object`
    ? ``
    : `position` in e || `type` in e
      ? xm(e.position)
      : `start` in e || `end` in e
        ? xm(e)
        : `line` in e || `column` in e
          ? bm(e)
          : ``;
}
function bm(e) {
  return Sm(e && e.line) + `:` + Sm(e && e.column);
}
function xm(e) {
  return bm(e && e.start) + `-` + bm(e && e.end);
}
function Sm(e) {
  return e && typeof e == `number` ? e : 1;
}
var Cm = {}.hasOwnProperty;
function wm(e, t, n) {
  return (
    t && typeof t == `object` && ((n = t), (t = void 0)),
    Tm(n)(
      pm(
        fm(n)
          .document()
          .write(hm()(e, t, !0)),
      ),
    )
  );
}
function Tm(e) {
  let t = {
    transforms: [],
    canContainEols: [`emphasis`, `fragment`, `heading`, `paragraph`, `strong`],
    enter: {
      autolink: a(ve),
      autolinkProtocol: T,
      autolinkEmail: T,
      atxHeading: a(me),
      blockQuote: a(le),
      characterEscape: T,
      characterReference: T,
      codeFenced: a(ue),
      codeFencedFenceInfo: o,
      codeFencedFenceMeta: o,
      codeIndented: a(ue, o),
      codeText: a(de, o),
      codeTextData: T,
      data: T,
      codeFlowValue: T,
      definition: a(fe),
      definitionDestinationString: o,
      definitionLabelString: o,
      definitionTitleString: o,
      emphasis: a(pe),
      hardBreakEscape: a(he),
      hardBreakTrailing: a(he),
      htmlFlow: a(ge, o),
      htmlFlowData: T,
      htmlText: a(ge, o),
      htmlTextData: T,
      image: a(_e),
      label: o,
      link: a(ve),
      listItem: a(be),
      listItemValue: f,
      listOrdered: a(ye, d),
      listUnordered: a(ye),
      paragraph: a(xe),
      reference: ne,
      referenceString: o,
      resourceDestinationString: o,
      resourceTitleString: o,
      setextHeading: a(me),
      strong: a(Se),
      thematicBreak: a(we),
    },
    exit: {
      atxHeading: c(),
      atxHeadingSequence: x,
      autolink: c(),
      autolinkEmail: ce,
      autolinkProtocol: se,
      blockQuote: c(),
      characterEscapeValue: E,
      characterReferenceMarkerHexadecimal: ie,
      characterReferenceMarkerNumeric: ie,
      characterReferenceValue: ae,
      characterReference: oe,
      codeFenced: c(g),
      codeFencedFence: h,
      codeFencedFenceInfo: p,
      codeFencedFenceMeta: m,
      codeFlowValue: E,
      codeIndented: c(_),
      codeText: c(j),
      codeTextData: E,
      data: E,
      definition: c(),
      definitionDestinationString: b,
      definitionLabelString: v,
      definitionTitleString: y,
      emphasis: c(),
      hardBreakEscape: c(O),
      hardBreakTrailing: c(O),
      htmlFlow: c(k),
      htmlFlowData: E,
      htmlText: c(A),
      htmlTextData: E,
      image: c(N),
      label: F,
      labelText: P,
      lineEnding: D,
      link: c(M),
      listItem: c(),
      listOrdered: c(),
      listUnordered: c(),
      paragraph: c(),
      referenceString: re,
      resourceDestinationString: I,
      resourceTitleString: ee,
      resource: te,
      setextHeading: c(w),
      setextHeadingLineSequence: C,
      setextHeadingText: S,
      strong: c(),
      thematicBreak: c(),
    },
  };
  Dm(t, (e || {}).mdastExtensions || []);
  let n = {};
  return r;
  function r(e) {
    let r = { type: `root`, children: [] },
      a = {
        stack: [r],
        tokenStack: [],
        config: t,
        enter: s,
        exit: l,
        buffer: o,
        resume: u,
        data: n,
      },
      c = [],
      d = -1;
    for (; ++d < e.length; )
      (e[d][1].type === `listOrdered` || e[d][1].type === `listUnordered`) &&
        (e[d][0] === `enter` ? c.push(d) : (d = i(e, c.pop(), d)));
    for (d = -1; ++d < e.length; ) {
      let n = t[e[d][0]];
      Cm.call(n, e[d][1].type) &&
        n[e[d][1].type].call(Object.assign({ sliceSerialize: e[d][2].sliceSerialize }, a), e[d][1]);
    }
    if (a.tokenStack.length > 0) {
      let e = a.tokenStack[a.tokenStack.length - 1];
      (e[1] || km).call(a, void 0, e[0]);
    }
    for (
      r.position = {
        start: Em(e.length > 0 ? e[0][1].start : { line: 1, column: 1, offset: 0 }),
        end: Em(e.length > 0 ? e[e.length - 2][1].end : { line: 1, column: 1, offset: 0 }),
      },
        d = -1;
      ++d < t.transforms.length;
    )
      r = t.transforms[d](r) || r;
    return r;
  }
  function i(e, t, n) {
    let r = t - 1,
      i = -1,
      a = !1,
      o,
      s,
      c,
      l;
    for (; ++r <= n; ) {
      let t = e[r];
      switch (t[1].type) {
        case `listUnordered`:
        case `listOrdered`:
        case `blockQuote`:
          (t[0] === `enter` ? i++ : i--, (l = void 0));
          break;
        case `lineEndingBlank`:
          t[0] === `enter` && (o && !l && !i && !c && (c = r), (l = void 0));
          break;
        case `linePrefix`:
        case `listItemValue`:
        case `listItemMarker`:
        case `listItemPrefix`:
        case `listItemPrefixWhitespace`:
          break;
        default:
          l = void 0;
      }
      if (
        (!i && t[0] === `enter` && t[1].type === `listItemPrefix`) ||
        (i === -1 &&
          t[0] === `exit` &&
          (t[1].type === `listUnordered` || t[1].type === `listOrdered`))
      ) {
        if (o) {
          let i = r;
          for (s = void 0; i--; ) {
            let t = e[i];
            if (t[1].type === `lineEnding` || t[1].type === `lineEndingBlank`) {
              if (t[0] === `exit`) continue;
              (s && ((e[s][1].type = `lineEndingBlank`), (a = !0)),
                (t[1].type = `lineEnding`),
                (s = i));
            } else if (
              !(
                t[1].type === `linePrefix` ||
                t[1].type === `blockQuotePrefix` ||
                t[1].type === `blockQuotePrefixWhitespace` ||
                t[1].type === `blockQuoteMarker` ||
                t[1].type === `listItemIndent`
              )
            )
              break;
          }
          (c && (!s || c < s) && (o._spread = !0),
            (o.end = Object.assign({}, s ? e[s][1].start : t[1].end)),
            e.splice(s || r, 0, [`exit`, o, t[2]]),
            r++,
            n++);
        }
        if (t[1].type === `listItemPrefix`) {
          let i = {
            type: `listItem`,
            _spread: !1,
            start: Object.assign({}, t[1].start),
            end: void 0,
          };
          ((o = i), e.splice(r, 0, [`enter`, i, t[2]]), r++, n++, (c = void 0), (l = !0));
        }
      }
    }
    return ((e[t][1]._spread = a), n);
  }
  function a(e, t) {
    return n;
    function n(n) {
      (s.call(this, e(n), n), t && t.call(this, n));
    }
  }
  function o() {
    this.stack.push({ type: `fragment`, children: [] });
  }
  function s(e, t, n) {
    (this.stack[this.stack.length - 1].children.push(e),
      this.stack.push(e),
      this.tokenStack.push([t, n || void 0]),
      (e.position = { start: Em(t.start), end: void 0 }));
  }
  function c(e) {
    return t;
    function t(t) {
      (e && e.call(this, t), l.call(this, t));
    }
  }
  function l(e, t) {
    let n = this.stack.pop(),
      r = this.tokenStack.pop();
    if (r) r[0].type !== e.type && (t ? t.call(this, e, r[0]) : (r[1] || km).call(this, e, r[0]));
    else
      throw Error(
        "Cannot close `" + e.type + "` (" + ym({ start: e.start, end: e.end }) + `): it’s not open`,
      );
    n.position.end = Em(e.end);
  }
  function u() {
    return Ed(this.stack.pop());
  }
  function d() {
    this.data.expectingFirstListItemValue = !0;
  }
  function f(e) {
    if (this.data.expectingFirstListItemValue) {
      let t = this.stack[this.stack.length - 2];
      ((t.start = Number.parseInt(this.sliceSerialize(e), 10)),
        (this.data.expectingFirstListItemValue = void 0));
    }
  }
  function p() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.lang = e;
  }
  function m() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.meta = e;
  }
  function h() {
    this.data.flowCodeInside || (this.buffer(), (this.data.flowCodeInside = !0));
  }
  function g() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    ((t.value = e.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, ``)), (this.data.flowCodeInside = void 0));
  }
  function _() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e.replace(/(\r?\n|\r)$/g, ``);
  }
  function v(e) {
    let t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((n.label = t), (n.identifier = Bd(this.sliceSerialize(e)).toLowerCase()));
  }
  function y() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.title = e;
  }
  function b() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.url = e;
  }
  function x(e) {
    let t = this.stack[this.stack.length - 1];
    t.depth ||= this.sliceSerialize(e).length;
  }
  function S() {
    this.data.setextHeadingSlurpLineEnding = !0;
  }
  function C(e) {
    let t = this.stack[this.stack.length - 1];
    t.depth = this.sliceSerialize(e).codePointAt(0) === 61 ? 1 : 2;
  }
  function w() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function T(e) {
    let t = this.stack[this.stack.length - 1].children,
      n = t[t.length - 1];
    ((!n || n.type !== `text`) &&
      ((n = Ce()), (n.position = { start: Em(e.start), end: void 0 }), t.push(n)),
      this.stack.push(n));
  }
  function E(e) {
    let t = this.stack.pop();
    ((t.value += this.sliceSerialize(e)), (t.position.end = Em(e.end)));
  }
  function D(e) {
    let n = this.stack[this.stack.length - 1];
    if (this.data.atHardBreak) {
      let t = n.children[n.children.length - 1];
      ((t.position.end = Em(e.end)), (this.data.atHardBreak = void 0));
      return;
    }
    !this.data.setextHeadingSlurpLineEnding &&
      t.canContainEols.includes(n.type) &&
      (T.call(this, e), E.call(this, e));
  }
  function O() {
    this.data.atHardBreak = !0;
  }
  function k() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function A() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function j() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.value = e;
  }
  function M() {
    let e = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      let t = this.data.referenceType || `shortcut`;
      ((e.type += `Reference`), (e.referenceType = t), delete e.url, delete e.title);
    } else (delete e.identifier, delete e.label);
    this.data.referenceType = void 0;
  }
  function N() {
    let e = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      let t = this.data.referenceType || `shortcut`;
      ((e.type += `Reference`), (e.referenceType = t), delete e.url, delete e.title);
    } else (delete e.identifier, delete e.label);
    this.data.referenceType = void 0;
  }
  function P(e) {
    let t = this.sliceSerialize(e),
      n = this.stack[this.stack.length - 2];
    ((n.label = _m(t)), (n.identifier = Bd(t).toLowerCase()));
  }
  function F() {
    let e = this.stack[this.stack.length - 1],
      t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((this.data.inReference = !0), n.type === `link` ? (n.children = e.children) : (n.alt = t));
  }
  function I() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.url = e;
  }
  function ee() {
    let e = this.resume(),
      t = this.stack[this.stack.length - 1];
    t.title = e;
  }
  function te() {
    this.data.inReference = void 0;
  }
  function ne() {
    this.data.referenceType = `collapsed`;
  }
  function re(e) {
    let t = this.resume(),
      n = this.stack[this.stack.length - 1];
    ((n.label = t),
      (n.identifier = Bd(this.sliceSerialize(e)).toLowerCase()),
      (this.data.referenceType = `full`));
  }
  function ie(e) {
    this.data.characterReferenceType = e.type;
  }
  function ae(e) {
    let t = this.sliceSerialize(e),
      n = this.data.characterReferenceType,
      r;
    n
      ? ((r = zd(t, n === `characterReferenceMarkerNumeric` ? 10 : 16)),
        (this.data.characterReferenceType = void 0))
      : (r = Md(t));
    let i = this.stack[this.stack.length - 1];
    i.value += r;
  }
  function oe(e) {
    let t = this.stack.pop();
    t.position.end = Em(e.end);
  }
  function se(e) {
    E.call(this, e);
    let t = this.stack[this.stack.length - 1];
    t.url = this.sliceSerialize(e);
  }
  function ce(e) {
    E.call(this, e);
    let t = this.stack[this.stack.length - 1];
    t.url = `mailto:` + this.sliceSerialize(e);
  }
  function le() {
    return { type: `blockquote`, children: [] };
  }
  function ue() {
    return { type: `code`, lang: null, meta: null, value: `` };
  }
  function de() {
    return { type: `inlineCode`, value: `` };
  }
  function fe() {
    return { type: `definition`, identifier: ``, label: null, title: null, url: `` };
  }
  function pe() {
    return { type: `emphasis`, children: [] };
  }
  function me() {
    return { type: `heading`, depth: 0, children: [] };
  }
  function he() {
    return { type: `break` };
  }
  function ge() {
    return { type: `html`, value: `` };
  }
  function _e() {
    return { type: `image`, title: null, url: ``, alt: null };
  }
  function ve() {
    return { type: `link`, title: null, url: ``, children: [] };
  }
  function ye(e) {
    return {
      type: `list`,
      ordered: e.type === `listOrdered`,
      start: null,
      spread: e._spread,
      children: [],
    };
  }
  function be(e) {
    return { type: `listItem`, spread: e._spread, checked: null, children: [] };
  }
  function xe() {
    return { type: `paragraph`, children: [] };
  }
  function Se() {
    return { type: `strong`, children: [] };
  }
  function Ce() {
    return { type: `text`, value: `` };
  }
  function we() {
    return { type: `thematicBreak` };
  }
}
function Em(e) {
  return { line: e.line, column: e.column, offset: e.offset };
}
function Dm(e, t) {
  let n = -1;
  for (; ++n < t.length; ) {
    let r = t[n];
    Array.isArray(r) ? Dm(e, r) : Om(e, r);
  }
}
function Om(e, t) {
  let n;
  for (n in t)
    if (Cm.call(t, n))
      switch (n) {
        case `canContainEols`: {
          let r = t[n];
          r && e[n].push(...r);
          break;
        }
        case `transforms`: {
          let r = t[n];
          r && e[n].push(...r);
          break;
        }
        case `enter`:
        case `exit`: {
          let r = t[n];
          r && Object.assign(e[n], r);
          break;
        }
      }
}
function km(e, t) {
  throw Error(
    e
      ? "Cannot close `" +
          e.type +
          "` (" +
          ym({ start: e.start, end: e.end }) +
          "): a different token (`" +
          t.type +
          "`, " +
          ym({ start: t.start, end: t.end }) +
          `) is open`
      : "Cannot close document, a token (`" +
          t.type +
          "`, " +
          ym({ start: t.start, end: t.end }) +
          `) is still open`,
  );
}
var Am = new Set([`root`, `blockquote`, `list`, `listItem`, `table`, `tableRow`, `tableCell`]);
function jm(e) {
  let t = e.trim();
  return t.length === 0 ? `` : Mm(wm(t)).replace(/\s+/g, ` `).trim();
}
function Mm(e) {
  return e.children == null
    ? Ed(e, { includeHtml: !1 })
    : e.children
        .map(Mm)
        .filter((e) => e.length > 0)
        .join(Am.has(e.type) ? ` ` : ``);
}
var Nm = `codex-notification`,
  Pm = `${Nm}.wav`,
  Fm = t.Gr(`desktop-notifications`),
  Im = class {
    isSupported;
    createNotification;
    logger = Fm();
    notificationSoundStaged = !1;
    notifications = new Map();
    constructor(e) {
      ((this.options = e),
        (this.isSupported = e.isSupported ?? (() => a.Notification.isSupported())),
        e.createNotification
          ? (this.createNotification = e.createNotification)
          : (this.createNotification = (e) => {
              let t = new a.Notification(e);
              return {
                show: () => t.show(),
                on: (e, n) => {
                  switch (e) {
                    case `action`:
                      return t.on(`action`, (e, t) => {
                        n(e, t);
                      });
                    case `click`:
                      return t.on(`click`, () => {
                        n(void 0);
                      });
                    case `close`:
                      return t.on(`close`, () => {
                        n(void 0);
                      });
                  }
                },
                close: () => t.close(),
              };
            }));
    }
    showNotification(e, t, n, r) {
      if ((this.stageNotificationSoundIfNeeded(), !this.isSupported())) {
        (this.logger.warning(`Notification API not supported on this host`), r?.());
        return;
      }
      if (t.isDestroyed()) {
        (this.logger.warning(`target webContents destroyed; dropping notification`, {
          safe: { notificationId: e.id },
          sensitive: {},
        }),
          r?.());
        return;
      }
      let i = (e.actions ?? []).slice(0, 4);
      this.logger.info(`show notification`, {
        safe: { notificationId: e.id, kind: e.kind, actionCount: i.length },
        sensitive: {},
      });
      let a = e.kind === `permission` || e.kind === `question` ? `never` : void 0,
        o = e.kind === `turn-complete` && typeof e.replyPlaceholder == `string`,
        s = this.notifications.get(e.id);
      s != null && (this.removeNotification(e.id), s.notification.close?.());
      let c = jm(e.title),
        l = this.createNotification({
          title: c,
          body: jm(e.body),
          silent: !1,
          sound: this.options.platform === `darwin` ? Nm : void 0,
          timeoutType: a,
          hasReply: o,
          replyPlaceholder: o ? jm(e.replyPlaceholder ?? ``) : void 0,
          actions: i.map((e) => ({ type: `button`, text: jm(e.title) })),
        });
      (l.on(`click`, () => {
        (n({ notificationId: e.id, actionId: null, actionType: `open` }),
          this.logger.info(`notification click open`, {
            safe: { notificationId: e.id },
            sensitive: {},
          }));
      }),
        l.on(`action`, (t, r) => {
          let a = i[r ?? -1];
          a &&
            (this.logger.info(`notification action`, {
              safe: { notificationId: e.id, actionId: a.id, actionType: a.actionType },
              sensitive: {},
            }),
            n({ notificationId: e.id, actionId: a.id, actionType: a.actionType }));
        }),
        l.on(`reply`, (t, r) => {
          n({
            notificationId: e.id,
            actionId: null,
            actionType: `reply`,
            reply: typeof r == `string` ? r : r == null ? `` : (JSON.stringify(r) ?? ``),
          });
        }),
        l.on(`close`, () => {
          this.removeNotification(e.id);
        }),
        this.notifications.set(e.id, {
          notification: l,
          conversationId: e.conversationId ?? null,
          navigationPath: e.navigationPath ?? null,
          completedThread:
            e.kind === `turn-complete` && e.navigationPath != null
              ? { title: c, path: e.navigationPath, projectLabel: ``, isProjectless: !1 }
              : null,
          onNotificationRemoved: r,
        }),
        this.emitCompletedThreadsChanged(),
        l.show());
    }
    stageNotificationSoundIfNeeded() {
      if (
        this.notificationSoundStaged ||
        ((this.notificationSoundStaged = !0), this.options.platform !== `darwin`) ||
        typeof process.resourcesPath != `string`
      )
        return;
      let e = s.default.join(process.resourcesPath, Pm),
        t = s.default.join(__dirname, `..`, `assets`, `sounds`, Pm),
        n = (0, l.existsSync)(e) ? e : t;
      if (!(0, l.existsSync)(n)) return;
      let r = s.default.join(o.default.homedir(), `Library`, `Sounds`);
      try {
        ((0, l.mkdirSync)(r, { recursive: !0 }), (0, l.copyFileSync)(n, s.default.join(r, Pm)));
      } catch (e) {
        this.logger.warning(`failed to stage notification sound`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    dismissByConversationId(e) {
      for (let [t, n] of this.notifications.entries())
        n.conversationId === e && (n.notification.close?.(), this.removeNotification(t));
    }
    dismissByNotificationId(e) {
      (this.notifications.get(e)?.notification.close?.(), this.removeNotification(e));
    }
    dismissByNavigationPath(e) {
      for (let [t, n] of this.notifications.entries())
        n.navigationPath === e && (n.notification.close?.(), this.removeNotification(t));
    }
    removeNotification(e) {
      let t = this.notifications.get(e);
      t != null &&
        (this.notifications.delete(e),
        t.onNotificationRemoved?.(),
        this.emitCompletedThreadsChanged());
    }
    emitCompletedThreadsChanged() {
      if (this.options.onCompletedThreadsChanged == null) return;
      let e = new Set(),
        t = [];
      for (let n of [...this.notifications.values()].reverse())
        n.completedThread == null ||
          e.has(n.completedThread.path) ||
          (e.add(n.completedThread.path), t.push(n.completedThread));
      this.options.onCompletedThreadsChanged(t);
    }
  },
  Lm = class {
    constructor(e) {
      this.app = e;
    }
    getVersion() {
      return this.app.getVersion();
    }
    onShutdown(e) {
      return (
        this.app.on(`before-quit`, e),
        {
          dispose: () => {
            this.app.off(`before-quit`, e);
          },
        }
      );
    }
  },
  Rm = class {
    constructor(e) {
      this.broadcaster = e;
    }
    persistAndNotify(e) {
      let n = t.D(e);
      return n.length <= 0 ? 0 : (this.broadcaster.inboxItemsChanged(t.Zo.CHANGED), n.length);
    }
    markRead(e, n) {
      t.w(e, n) && this.broadcaster.inboxItemsChanged(t.Zo.CHANGED);
    }
    markUnread(e) {
      t.w(e, null) && this.broadcaster.inboxItemsChanged(t.Zo.CHANGED);
    }
  },
  zm = t.Wr(`RealtimeSessionRecorder`),
  Bm = t.$s({ threadId: t.nc() }).loose(),
  Vm = t.Js(`method`, [
    t.$s({
      method: t.Xs(`thread/realtime/started`),
      params: Bm.extend({
        sessionId: t.nc().nullable().optional(),
        realtimeSessionId: t.nc().nullable().optional(),
        version: t.Us([`v1`, `v2`]),
      }),
    }),
    t.$s({ method: t.Xs(`thread/realtime/itemAdded`), params: Bm.extend({ item: t.Ys() }) }),
    t.$s({
      method: t.Xs(`thread/realtime/transcript/delta`),
      params: Bm.extend({ role: t.nc(), delta: t.nc() }),
    }),
    t.$s({
      method: t.Xs(`thread/realtime/transcript/done`),
      params: Bm.extend({ role: t.nc(), text: t.nc() }),
    }),
    t.$s({
      method: t.Xs(`thread/realtime/outputAudio/delta`),
      params: Bm.extend({
        audio: t
          .$s({
            data: t.nc(),
            sampleRate: t.Qs(),
            numChannels: t.Qs(),
            samplesPerChannel: t.Qs().nullable(),
            itemId: t.nc().nullable(),
          })
          .loose(),
      }),
    }),
    t.$s({ method: t.Xs(`thread/realtime/sdp`), params: Bm.extend({ sdp: t.nc() }) }),
    t.$s({ method: t.Xs(`thread/realtime/error`), params: Bm.extend({ message: t.nc() }) }),
    t.$s({
      method: t.Xs(`thread/realtime/closed`),
      params: Bm.extend({ reason: t.nc().nullable() }),
    }),
  ]),
  Hm = t.Js(`method`, [
    t.$s({
      method: t.Xs(`turn/started`),
      params: Bm.extend({ turn: t.$s({ id: t.nc() }).loose() }),
    }),
    t.$s({
      method: t.Xs(`turn/completed`),
      params: Bm.extend({ turn: t.$s({ id: t.nc() }).loose() }),
    }),
  ]),
  Um = t.$s({ type: t.Xs(`handoff_request`), handoff_id: t.nc() }).loose(),
  Wm = class {
    enabled = !1;
    generation = 0;
    setEnabled(e) {
      e !== this.enabled && ((this.enabled = e), (this.generation += 1));
    }
    getGeneration() {
      return this.generation;
    }
    isEnabled() {
      return this.enabled;
    }
  },
  Gm = class {
    recordings = new Map();
    recordingGeneration = null;
    pendingWrite = Promise.resolve();
    constructor(e) {
      this.options = e;
    }
    static getRootDirectory({ codexHome: e }) {
      return (0, s.join)(e, `realtime-sessions`);
    }
    record(e) {
      let t = this.options.getRecordingGeneration();
      if (
        (this.recordingGeneration != null &&
          t !== this.recordingGeneration &&
          this.recordings.clear(),
        (this.recordingGeneration = t),
        !this.options.isRecordingEnabled())
      )
        return;
      let n = Vm.safeParse(e);
      if (!n.success) {
        this.recordTurnNotification(e);
        return;
      }
      let r = n.data,
        i = new Date().toISOString(),
        { threadId: a } = r.params,
        o = this.recordings.get(a);
      if (
        ((r.method === `thread/realtime/started` || o == null) &&
          ((o = this.createRecording({
            threadId: a,
            realtimeSessionId:
              r.method === `thread/realtime/started`
                ? (r.params.sessionId ?? r.params.realtimeSessionId ?? null)
                : null,
            timestamp: i,
            version: r.method === `thread/realtime/started` ? r.params.version : null,
          })),
          this.recordings.set(a, o)),
        this.append(o, {
          timestamp: i,
          type: `realtime_event`,
          payload: { method: r.method, params: Xm(r) },
        }),
        r.method === `thread/realtime/itemAdded`)
      ) {
        let e = Um.safeParse(r.params.item);
        e.success && o.pendingHandoffIds.push(e.data.handoff_id);
      }
      r.method === `thread/realtime/closed` &&
        ((o.isClosed = !0), this.deleteFinishedRecording(a, o));
    }
    async flush() {
      await this.pendingWrite;
    }
    async readMostRecentSessionLog(e = new Date()) {
      if (!this.options.isRecordingEnabled()) return null;
      await this.flush();
      let t = this.getDayDirectory(e),
        n = await (0, p.readdir)(t, { withFileTypes: !0 }).catch((e) => {
          if (e instanceof Error && `code` in e && e.code === `ENOENT`) return null;
          throw e;
        });
      if (n == null) return null;
      let r = n
        .filter((e) => e.isFile())
        .map((e) => e.name)
        .filter((e) => e.startsWith(`realtime-`) && e.endsWith(`.jsonl`))
        .sort()
        .at(-1);
      return r == null ? null : (0, p.readFile)((0, s.join)(t, r));
    }
    createRecording({ threadId: e, realtimeSessionId: t, timestamp: n, version: r }) {
      let i = new Date(n),
        a = {
          filePath: (0, s.join)(
            this.getDayDirectory(i),
            [`realtime`, n.replaceAll(`:`, `-`), Zm(e), Zm(t ?? `unknown-session`)].join(`-`) +
              `.jsonl`,
          ),
          pendingHandoffIds: [],
          handoffByTurnId: new Map(),
          embeddedTurnIds: new Set(),
          isClosed: !1,
        };
      return (
        this.append(a, {
          timestamp: n,
          type: `realtime_session_meta`,
          payload: {
            schema_version: 2,
            host_id: this.options.hostId,
            thread_id: e,
            realtime_session_id: t,
            version: r,
          },
        }),
        a
      );
    }
    getDayDirectory(e) {
      return (0, s.join)(
        this.options.rootDirectory,
        String(e.getUTCFullYear()),
        String(e.getUTCMonth() + 1).padStart(2, `0`),
        String(e.getUTCDate()).padStart(2, `0`),
      );
    }
    recordTurnNotification(e) {
      let t = Hm.safeParse(e);
      if (!t.success) return;
      let { threadId: n, turn: r } = t.data.params,
        i = this.recordings.get(n);
      if (i == null) return;
      if (t.data.method === `turn/started`) {
        let e = i.pendingHandoffIds.shift();
        if (e == null) return;
        (i.handoffByTurnId.set(r.id, e),
          this.append(i, {
            timestamp: new Date().toISOString(),
            type: `handoff_turn`,
            payload: { status: `started`, handoff_id: e, thread_id: n, turn_id: r.id },
          }));
        return;
      }
      let a = i.handoffByTurnId.get(r.id);
      a == null ||
        i.embeddedTurnIds.has(r.id) ||
        (i.embeddedTurnIds.add(r.id),
        this.embedRollout({ recording: i, threadId: n, turnId: r.id, handoffId: a }));
    }
    embedRollout({ recording: e, threadId: t, turnId: n, handoffId: r }) {
      this.enqueue(e, async () => {
        try {
          let i = await this.options.getRolloutPath?.(t);
          if (i == null) return;
          let a = await Km(i, n);
          if (a == null) return;
          let o = new Date().toISOString(),
            c = [
              {
                timestamp: o,
                type: `embedded_rollout_meta`,
                payload: {
                  handoff_id: r,
                  thread_id: t,
                  turn_id: n,
                  source_file: (0, s.basename)(i),
                  record_count: a.length,
                },
              },
              ...a.map((e) => ({
                timestamp: Jm(e) ?? o,
                type: `embedded_rollout_event`,
                payload: { handoff_id: r, thread_id: t, turn_id: n, source: e },
              })),
              {
                timestamp: new Date().toISOString(),
                type: `handoff_turn`,
                payload: { status: `completed`, handoff_id: r, thread_id: t, turn_id: n },
              },
            ];
          await (0, p.appendFile)(
            e.filePath,
            c.map((e) => JSON.stringify(e)).join(`
`) +
              `
`,
            `utf8`,
          );
        } finally {
          (e.handoffByTurnId.delete(n), this.deleteFinishedRecording(t, e));
        }
      });
    }
    append(e, t) {
      this.enqueue(e, () => (0, p.appendFile)(e.filePath, `${JSON.stringify(t)}\n`, `utf8`));
    }
    enqueue(e, t) {
      this.pendingWrite = this.pendingWrite
        .then(async () => {
          (await (0, p.mkdir)((0, s.dirname)(e.filePath), { recursive: !0 }), await t());
        })
        .catch((t) => {
          zm.warning(`realtime_session_recording_failed`, {
            safe: {},
            sensitive: { error: t, filePath: e.filePath },
          });
        });
    }
    deleteFinishedRecording(e, t) {
      this.recordings.get(e) === t &&
        t.isClosed &&
        t.pendingHandoffIds.length === 0 &&
        t.handoffByTurnId.size === 0 &&
        this.recordings.delete(e);
    }
  };
async function Km(e, t, n = 0) {
  let r = (await (0, p.readFile)(e, `utf8`))
      .split(/\r?\n/)
      .filter((e) => e.trim().length > 0)
      .flatMap((e) => {
        try {
          let t = JSON.parse(e);
          return Ym(t) ? [t] : [];
        } catch {
          return [];
        }
      }),
    i = r.find((e) => e.type === `session_meta`),
    a = r.findIndex((e) => qm(e, `task_started`, t)),
    o = r.findIndex((e, n) => n >= a && qm(e, `task_complete`, t));
  return a >= 0 && o >= a
    ? [...(i == null ? [] : [i]), ...r.slice(a, o + 1)]
    : n >= 2
      ? null
      : (await (0, _.setTimeout)(100), Km(e, t, n + 1));
}
function qm(e, t, n) {
  return e.type !== `event_msg` || !Ym(e.payload)
    ? !1
    : e.payload.type === t && e.payload.turn_id === n;
}
function Jm(e) {
  return typeof e.timestamp == `string` ? e.timestamp : null;
}
function Ym(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function Xm(e) {
  if (e.method === `thread/realtime/outputAudio/delta`) {
    let { audio: t } = e.params;
    return {
      ...e.params,
      audio: {
        sampleRate: t.sampleRate,
        numChannels: t.numChannels,
        samplesPerChannel: t.samplesPerChannel,
        itemId: t.itemId,
        encodedCharacters: t.data.length,
        decodedBytes: Buffer.byteLength(t.data, `base64`),
      },
    };
  }
  return e.method === `thread/realtime/sdp`
    ? { ...e.params, sdp: { redacted: !0, characters: e.params.sdp.length } }
    : e.params;
}
function Zm(e) {
  return e.replaceAll(/[^a-zA-Z0-9._-]/g, `_`).slice(0, 120);
}
var Qm = Symbol(`workers-module`);
(Symbol.dispose || (Symbol.dispose = Symbol.for(`dispose`)),
  Symbol.asyncDispose || (Symbol.asyncDispose = Symbol.for(`asyncDispose`)),
  Promise.withResolvers ||
    (Promise.withResolvers = function () {
      let e, t;
      return {
        promise: new Promise((n, r) => {
          ((e = n), (t = r));
        }),
        resolve: e,
        reject: t,
      };
    }));
var $m = globalThis[Qm],
  eh = $m ? $m.RpcTarget : class {},
  th = async function () {}.constructor,
  nh = typeof Buffer < `u` ? Buffer.prototype : void 0;
function rh(e) {
  switch (typeof e) {
    case `boolean`:
    case `number`:
    case `string`:
      return `primitive`;
    case `undefined`:
      return `undefined`;
    case `object`:
    case `function`:
      break;
    case `bigint`:
      return `bigint`;
    default:
      return `unsupported`;
  }
  if (e === null) return `primitive`;
  let t = Object.getPrototypeOf(e);
  switch (t) {
    case Object.prototype:
      return `object`;
    case Function.prototype:
    case th.prototype:
      return `function`;
    case Array.prototype:
      return `array`;
    case Date.prototype:
      return `date`;
    case Uint8Array.prototype:
    case nh:
      return `bytes`;
    case WritableStream.prototype:
      return `writable`;
    case ReadableStream.prototype:
      return `readable`;
    case Headers.prototype:
      return `headers`;
    case Request.prototype:
      return `request`;
    case Response.prototype:
      return `response`;
    case hh.prototype:
      return `stub`;
    case gh.prototype:
      return `rpc-promise`;
    default:
      if ($m) {
        if (t == $m.RpcStub.prototype || e instanceof $m.ServiceStub) return `rpc-target`;
        if (t == $m.RpcPromise.prototype || t == $m.RpcProperty.prototype) return `rpc-thenable`;
      }
      return e instanceof eh ? `rpc-target` : e instanceof Error ? `error` : `unsupported`;
  }
}
function ih() {
  throw Error(`RPC map() implementation was not loaded.`);
}
var ah = { applyMap: ih, sendMap: ih };
function oh() {
  throw Error(`Stream implementation was not loaded.`);
}
var sh = {
    createWritableStreamHook: oh,
    createWritableStreamFromHook: oh,
    createReadableStreamHook: oh,
  },
  ch = class {
    stream(e, t) {
      let n = this.call(e, t).pull(),
        r;
      return (
        n instanceof Promise
          ? (r = n.then((e) => {
              e.dispose();
            }))
          : (n.dispose(), (r = Promise.resolve())),
        { promise: r }
      );
    }
  },
  lh = class extends ch {
    constructor(e) {
      (super(), (this.error = e));
    }
    call(e, t) {
      return this;
    }
    map(e, t, n) {
      return this;
    }
    get(e) {
      return this;
    }
    dup() {
      return this;
    }
    pull() {
      return Promise.reject(this.error);
    }
    ignoreUnhandledRejections() {}
    dispose() {}
    onBroken(e) {
      try {
        e(this.error);
      } catch (e) {
        Promise.resolve(e);
      }
    }
  },
  uh = new lh(Error(`Attempted to use RPC stub after it has been disposed.`)),
  dh = (e, t, n) => e.call(t, n);
function fh(e, t) {
  let n = dh;
  dh = e;
  try {
    return t();
  } finally {
    dh = n;
  }
}
var ph = Symbol(`realStub`),
  mh = {
    apply(e, t, n) {
      let r = e.raw;
      return new gh(dh(r.hook, r.pathIfPromise || [], Ch.fromAppParams(n)), []);
    },
    get(e, t, n) {
      let r = e.raw;
      if (t === ph) return r;
      if (t in gh.prototype) return r[t];
      if (typeof t == `string`)
        return new gh(r.hook, r.pathIfPromise ? [...r.pathIfPromise, t] : [t]);
      if (t === Symbol.dispose && (!r.pathIfPromise || r.pathIfPromise.length == 0))
        return () => {
          (r.hook.dispose(), (r.hook = uh));
        };
    },
    has(e, t) {
      let n = e.raw;
      return t === ph
        ? !0
        : t in gh.prototype
          ? t in n
          : typeof t == `string`
            ? !0
            : t === Symbol.dispose && (!n.pathIfPromise || n.pathIfPromise.length == 0);
    },
    construct(e, t) {
      throw Error(`An RPC stub cannot be used as a constructor.`);
    },
    defineProperty(e, t, n) {
      throw Error(`Can't define properties on RPC stubs.`);
    },
    deleteProperty(e, t) {
      throw Error(`Can't delete properties on RPC stubs.`);
    },
    getOwnPropertyDescriptor(e, t) {},
    getPrototypeOf(e) {
      return Object.getPrototypeOf(e.raw);
    },
    isExtensible(e) {
      return !1;
    },
    ownKeys(e) {
      return [];
    },
    preventExtensions(e) {
      return !0;
    },
    set(e, t, n, r) {
      throw Error(`Can't assign properties on RPC stubs.`);
    },
    setPrototypeOf(e, t) {
      throw Error(`Can't override prototype of RPC stubs.`);
    },
  },
  hh = class e extends eh {
    constructor(e, t) {
      if ((super(), !(e instanceof ch))) {
        let n = e;
        if (
          ((e =
            n instanceof eh || n instanceof Function
              ? Oh.create(n, void 0)
              : new Eh(Ch.fromAppReturn(n))),
          t)
        )
          throw TypeError(`RpcStub constructor expected one argument, received two.`);
      }
      ((this.hook = e), (this.pathIfPromise = t));
      let n = () => {};
      return ((n.raw = this), new Proxy(n, mh));
    }
    hook;
    pathIfPromise;
    dup() {
      let t = this[ph];
      return t.pathIfPromise ? new e(t.hook.get(t.pathIfPromise)) : new e(t.hook.dup());
    }
    onRpcBroken(e) {
      this[ph].hook.onBroken(e);
    }
    map(e) {
      let { hook: t, pathIfPromise: n } = this[ph];
      return ah.sendMap(t, n || [], e);
    }
    toString() {
      return `[object RpcStub]`;
    }
  },
  gh = class extends hh {
    constructor(e, t) {
      super(e, t);
    }
    then(e, t) {
      return Sh(this).then(...arguments);
    }
    catch(e) {
      return Sh(this).catch(...arguments);
    }
    finally(e) {
      return Sh(this).finally(...arguments);
    }
    toString() {
      return `[object RpcPromise]`;
    }
  };
function _h(e) {
  let { hook: t, pathIfPromise: n } = e[ph];
  return n && n.length > 0 ? t.get(n) : t;
}
function vh(e) {
  let { hook: t, pathIfPromise: n } = e[ph];
  return n ? t.get(n) : t.dup();
}
function yh(e) {
  let { hook: t, pathIfPromise: n } = e[ph];
  if (!(n && n.length > 0)) return t;
}
function bh(e) {
  return e[ph].hook;
}
function xh(e) {
  return e[ph];
}
async function Sh(e) {
  let { hook: t, pathIfPromise: n } = e[ph];
  return (n.length > 0 && (t = t.get(n)), (await t.pull()).deliverResolve());
}
var Ch = class e {
  constructor(e, t, n, r) {
    ((this.value = e), (this.source = t), (this.hooks = n), (this.promises = r));
  }
  static fromAppParams(t) {
    return new e(t, `params`);
  }
  static fromAppReturn(t) {
    return new e(t, `return`);
  }
  static fromArray(t) {
    let n = [],
      r = [],
      i = [];
    for (let e of t) {
      e.ensureDeepCopied();
      for (let t of e.hooks) n.push(t);
      for (let t of e.promises)
        (t.parent === e && (t = { parent: i, property: i.length, promise: t.promise }), r.push(t));
      i.push(e.value);
    }
    return new e(i, `owned`, n, r);
  }
  static forEvaluate(t, n) {
    return new e(null, `owned`, t, n);
  }
  static deepCopyFrom(t, n, r) {
    let i = new e(null, `owned`, [], []);
    return ((i.value = i.deepCopy(t, n, `value`, i, !0, r)), i);
  }
  rpcTargets;
  getHookForRpcTarget(e, t, n = !0) {
    if (this.source === `params`) {
      if (n) {
        let t = e;
        typeof t.dup == `function` && (e = t.dup());
      }
      return Oh.create(e, t);
    } else if (this.source === `return`) {
      let r = this.rpcTargets?.get(e);
      return r
        ? n
          ? r.dup()
          : (this.rpcTargets?.delete(e), r)
        : ((r = Oh.create(e, t)),
          n ? ((this.rpcTargets ||= new Map()), this.rpcTargets.set(e, r), r.dup()) : r);
    } else throw Error(`owned payload shouldn't contain raw RpcTargets`);
  }
  getHookForWritableStream(e, t, n = !0) {
    if (this.source === `params`) return sh.createWritableStreamHook(e);
    if (this.source === `return`) {
      let t = this.rpcTargets?.get(e);
      return t
        ? n
          ? t.dup()
          : (this.rpcTargets?.delete(e), t)
        : ((t = sh.createWritableStreamHook(e)),
          n ? ((this.rpcTargets ||= new Map()), this.rpcTargets.set(e, t), t.dup()) : t);
    } else throw Error(`owned payload shouldn't contain raw WritableStreams`);
  }
  getHookForReadableStream(e, t, n = !0) {
    if (this.source === `params`) return sh.createReadableStreamHook(e);
    if (this.source === `return`) {
      let t = this.rpcTargets?.get(e);
      return t
        ? n
          ? t.dup()
          : (this.rpcTargets?.delete(e), t)
        : ((t = sh.createReadableStreamHook(e)),
          n ? ((this.rpcTargets ||= new Map()), this.rpcTargets.set(e, t), t.dup()) : t);
    } else throw Error(`owned payload shouldn't contain raw ReadableStreams`);
  }
  deepCopy(e, t, n, r, i, a) {
    switch (rh(e)) {
      case `unsupported`:
        return e;
      case `primitive`:
      case `bigint`:
      case `date`:
      case `bytes`:
      case `error`:
      case `undefined`:
        return e;
      case `array`: {
        let t = e,
          n = t.length,
          r = Array(n);
        for (let e = 0; e < n; e++) r[e] = this.deepCopy(t[e], t, e, r, i, a);
        return r;
      }
      case `object`: {
        let t = {},
          n = e;
        for (let e in n) t[e] = this.deepCopy(n[e], n, e, t, i, a);
        return t;
      }
      case `stub`:
      case `rpc-promise`: {
        let t = e,
          a;
        if (((a = i ? vh(t) : _h(t)), t instanceof gh)) {
          let e = new gh(a, []);
          return (this.promises.push({ parent: r, property: n, promise: e }), e);
        } else return (this.hooks.push(a), new hh(a));
      }
      case `function`:
      case `rpc-target`: {
        let n = e,
          r;
        return (
          (r = a ? a.getHookForRpcTarget(n, t, i) : Oh.create(n, t)), this.hooks.push(r), new hh(r)
        );
      }
      case `rpc-thenable`: {
        let o = e,
          s;
        return (
          (s = a ? new gh(a.getHookForRpcTarget(o, t, i), []) : new gh(Oh.create(o, t), [])),
          this.promises.push({ parent: r, property: n, promise: s }),
          s
        );
      }
      case `writable`: {
        let n = e,
          r;
        return (
          (r = a ? a.getHookForWritableStream(n, t, i) : sh.createWritableStreamHook(n)),
          this.hooks.push(r),
          n
        );
      }
      case `readable`: {
        let n = e,
          r;
        return (
          (r = a ? a.getHookForReadableStream(n, t, i) : sh.createReadableStreamHook(n)),
          this.hooks.push(r),
          n
        );
      }
      case `headers`:
        return new Headers(e);
      case `request`: {
        let t = e;
        return (t.body && this.deepCopy(t.body, t, `body`, t, i, a), new Request(t));
      }
      case `response`: {
        let t = e;
        return (t.body && this.deepCopy(t.body, t, `body`, t, i, a), new Response(t.body, t));
      }
      default:
        throw Error(`unreachable`);
    }
  }
  ensureDeepCopied() {
    if (this.source !== `owned`) {
      let e = this.source === `params`;
      ((this.hooks = []), (this.promises = []));
      try {
        this.value = this.deepCopy(this.value, void 0, `value`, this, e, this);
      } catch (e) {
        throw ((this.hooks = void 0), (this.promises = void 0), e);
      }
      if (((this.source = `owned`), this.rpcTargets && this.rpcTargets.size > 0))
        throw Error(`Not all rpcTargets were accounted for in deep-copy?`);
      this.rpcTargets = void 0;
    }
  }
  deliverTo(t, n, r) {
    if ((this.ensureDeepCopied(), this.value instanceof gh))
      e.deliverRpcPromiseTo(this.value, t, n, r);
    else {
      t[n] = this.value;
      for (let t of this.promises) e.deliverRpcPromiseTo(t.promise, t.parent, t.property, r);
    }
  }
  static deliverRpcPromiseTo(t, n, r, i) {
    let a = yh(t);
    if (!a) throw Error(`property promises should have been resolved earlier`);
    let o = a.pull();
    o instanceof e
      ? o.deliverTo(n, r, i)
      : i.push(
          o.then((e) => {
            let t = [];
            if ((e.deliverTo(n, r, t), t.length > 0)) return Promise.all(t);
          }),
        );
  }
  async deliverCall(t, n) {
    try {
      let r = [];
      (this.deliverTo(this, `value`, r), r.length > 0 && (await Promise.all(r)));
      let i = Function.prototype.apply.call(t, n, this.value);
      return i instanceof gh ? e.fromAppReturn(i) : e.fromAppReturn(await i);
    } finally {
      this.dispose();
    }
  }
  async deliverResolve() {
    try {
      let e = [];
      (this.deliverTo(this, `value`, e), e.length > 0 && (await Promise.all(e)));
      let t = this.value;
      return (
        t instanceof Object &&
          (Symbol.dispose in t ||
            Object.defineProperty(t, Symbol.dispose, {
              value: () => this.dispose(),
              writable: !0,
              enumerable: !1,
              configurable: !0,
            })),
        t
      );
    } catch (e) {
      throw (this.dispose(), e);
    }
  }
  dispose() {
    if (this.source === `owned`)
      (this.hooks.forEach((e) => e.dispose()),
        this.promises.forEach((e) => e.promise[Symbol.dispose]()));
    else if (
      this.source === `return` &&
      (this.disposeImpl(this.value, void 0), this.rpcTargets && this.rpcTargets.size > 0)
    )
      throw Error(`Not all rpcTargets were accounted for in disposeImpl()?`);
    ((this.source = `owned`), (this.hooks = []), (this.promises = []));
  }
  disposeImpl(e, t) {
    switch (rh(e)) {
      case `unsupported`:
      case `primitive`:
      case `bigint`:
      case `bytes`:
      case `date`:
      case `error`:
      case `undefined`:
        return;
      case `array`: {
        let t = e,
          n = t.length;
        for (let e = 0; e < n; e++) this.disposeImpl(t[e], t);
        return;
      }
      case `object`: {
        let t = e;
        for (let e in t) this.disposeImpl(t[e], t);
        return;
      }
      case `stub`:
      case `rpc-promise`: {
        let t = yh(e);
        t && t.dispose();
        return;
      }
      case `function`:
      case `rpc-target`: {
        let t = e,
          n = this.rpcTargets?.get(t);
        n ? (n.dispose(), this.rpcTargets.delete(t)) : Dh(t);
        return;
      }
      case `rpc-thenable`:
        return;
      case `headers`:
        return;
      case `request`: {
        let t = e;
        t.body && this.disposeImpl(t.body, t);
        return;
      }
      case `response`: {
        let t = e;
        t.body && this.disposeImpl(t.body, t);
        return;
      }
      case `writable`: {
        let t = e,
          n = this.rpcTargets?.get(t);
        (n ? this.rpcTargets.delete(t) : (n = sh.createWritableStreamHook(t)), n.dispose());
        return;
      }
      case `readable`: {
        let t = e,
          n = this.rpcTargets?.get(t);
        (n ? this.rpcTargets.delete(t) : (n = sh.createReadableStreamHook(t)), n.dispose());
        return;
      }
      default:
        return;
    }
  }
  ignoreUnhandledRejections() {
    this.hooks
      ? (this.hooks.forEach((e) => {
          e.ignoreUnhandledRejections();
        }),
        this.promises.forEach((e) => bh(e.promise).ignoreUnhandledRejections()))
      : this.ignoreUnhandledRejectionsImpl(this.value);
  }
  ignoreUnhandledRejectionsImpl(e) {
    switch (rh(e)) {
      case `unsupported`:
      case `primitive`:
      case `bigint`:
      case `bytes`:
      case `date`:
      case `error`:
      case `undefined`:
      case `function`:
      case `rpc-target`:
      case `writable`:
      case `readable`:
      case `headers`:
      case `request`:
      case `response`:
        return;
      case `array`: {
        let t = e,
          n = t.length;
        for (let e = 0; e < n; e++) this.ignoreUnhandledRejectionsImpl(t[e]);
        return;
      }
      case `object`: {
        let t = e;
        for (let e in t) this.ignoreUnhandledRejectionsImpl(t[e]);
        return;
      }
      case `stub`:
      case `rpc-promise`:
        bh(e).ignoreUnhandledRejections();
        return;
      case `rpc-thenable`:
        e.then(
          (e) => {},
          (e) => {},
        );
        return;
      default:
        return;
    }
  }
};
function wh(e, t, n, r) {
  for (let i = 0; i < n.length; i++) {
    t = e;
    let a = n[i];
    if (a in Object.prototype) {
      e = void 0;
      continue;
    }
    switch (rh(e)) {
      case `object`:
      case `function`:
        e = Object.hasOwn(e, a) ? e[a] : void 0;
        break;
      case `array`:
        e = Number.isInteger(a) && a >= 0 ? e[a] : void 0;
        break;
      case `rpc-target`:
      case `rpc-thenable`:
        if (Object.hasOwn(e, a))
          throw TypeError(
            `Attempted to access property '${a}', which is an instance property of the RpcTarget. To avoid leaking private internals, instance properties cannot be accessed over RPC. If you want to make this property available over RPC, define it as a method or getter on the class, instead of an instance property.`,
          );
        ((e = e[a]), (r = null));
        break;
      case `stub`:
      case `rpc-promise`: {
        let { hook: t, pathIfPromise: r } = xh(e);
        return { hook: t, remainingPath: r ? r.concat(n.slice(i)) : n.slice(i) };
      }
      case `writable`:
        e = void 0;
        break;
      case `readable`:
        e = void 0;
        break;
      case `primitive`:
      case `bigint`:
      case `bytes`:
      case `date`:
      case `error`:
      case `headers`:
      case `request`:
      case `response`:
        e = void 0;
        break;
      case `undefined`:
        e = e[a];
        break;
      case `unsupported`:
        if (i === 0) throw TypeError(`RPC stub points at a non-serializable type.`);
        {
          let e = n.slice(0, i).join(`.`),
            t = n.slice(0, i).join(`.`);
          throw TypeError(
            `'${e}' is not a serializable type, so property ${t} cannot be accessed.`,
          );
        }
      default:
        throw TypeError(`unreachable`);
    }
  }
  if (e instanceof gh) {
    let { hook: t, pathIfPromise: n } = xh(e);
    return { hook: t, remainingPath: n || [] };
  }
  return { value: e, parent: t, owner: r };
}
var Th = class extends ch {
    call(e, t) {
      try {
        let { value: n, owner: r } = this.getValue(),
          i = wh(n, void 0, e, r);
        if (i.hook) return i.hook.call(i.remainingPath, t);
        if (typeof i.value != `function`) throw TypeError(`'${e.join(`.`)}' is not a function.`);
        return new kh(t.deliverCall(i.value, i.parent).then((e) => new Eh(e)));
      } catch (e) {
        return new lh(e);
      }
    }
    map(e, t, n) {
      try {
        let r;
        try {
          let { value: t, owner: n } = this.getValue();
          r = wh(t, void 0, e, n);
        } catch (e) {
          for (let e of t) e.dispose();
          throw e;
        }
        return r.hook
          ? r.hook.map(r.remainingPath, t, n)
          : ah.applyMap(r.value, r.parent, r.owner, t, n);
      } catch (e) {
        return new lh(e);
      }
    }
    get(e) {
      try {
        let { value: t, owner: n } = this.getValue();
        if (e.length === 0 && n === null) throw Error(`Can't dup an RpcTarget stub as a promise.`);
        let r = wh(t, void 0, e, n);
        return r.hook
          ? r.hook.get(r.remainingPath)
          : new Eh(Ch.deepCopyFrom(r.value, r.parent, r.owner));
      } catch (e) {
        return new lh(e);
      }
    }
  },
  Eh = class e extends Th {
    constructor(e) {
      (super(), (this.payload = e));
    }
    payload;
    getPayload() {
      if (this.payload) return this.payload;
      throw Error(`Attempted to use an RPC StubHook after it was disposed.`);
    }
    getValue() {
      let e = this.getPayload();
      return { value: e.value, owner: e };
    }
    dup() {
      let t = this.getPayload();
      return new e(Ch.deepCopyFrom(t.value, void 0, t));
    }
    pull() {
      return this.getPayload();
    }
    ignoreUnhandledRejections() {
      this.payload && this.payload.ignoreUnhandledRejections();
    }
    dispose() {
      this.payload &&= (this.payload.dispose(), void 0);
    }
    onBroken(e) {
      this.payload && this.payload.value instanceof hh && this.payload.value.onRpcBroken(e);
    }
  };
function Dh(e) {
  if (Symbol.dispose in e)
    try {
      e[Symbol.dispose]();
    } catch (e) {
      Promise.reject(e);
    }
}
var Oh = class e extends Th {
    static create(t, n) {
      return (typeof t != `function` && (n = void 0), new e(t, n));
    }
    constructor(e, t, n) {
      (super(),
        (this.target = e),
        (this.parent = t),
        n
          ? n.refcount && ((this.refcount = n.refcount), ++this.refcount.count)
          : Symbol.dispose in e && (this.refcount = { count: 1 }));
    }
    target;
    parent;
    refcount;
    getTarget() {
      if (this.target) return this.target;
      throw Error(`Attempted to use an RPC StubHook after it was disposed.`);
    }
    getValue() {
      return { value: this.getTarget(), owner: null };
    }
    dup() {
      return new e(this.getTarget(), this.parent, this);
    }
    pull() {
      let e = this.getTarget();
      return `then` in e
        ? Promise.resolve(e).then((e) => Ch.fromAppReturn(e))
        : Promise.reject(Error(`Tried to resolve a non-promise stub.`));
    }
    ignoreUnhandledRejections() {}
    dispose() {
      this.target &&= (this.refcount && --this.refcount.count == 0 && Dh(this.target), void 0);
    }
    onBroken(e) {}
  },
  kh = class e extends ch {
    promise;
    resolution;
    constructor(e) {
      (super(), (this.promise = e.then((e) => ((this.resolution = e), e))));
    }
    call(t, n) {
      return (n.ensureDeepCopied(), new e(this.promise.then((e) => e.call(t, n))));
    }
    stream(e, t) {
      return (t.ensureDeepCopied(), { promise: this.promise.then((n) => n.stream(e, t).promise) });
    }
    map(t, n, r) {
      return new e(
        this.promise.then(
          (e) => e.map(t, n, r),
          (e) => {
            for (let e of n) e.dispose();
            throw e;
          },
        ),
      );
    }
    get(t) {
      return new e(this.promise.then((e) => e.get(t)));
    }
    dup() {
      return this.resolution ? this.resolution.dup() : new e(this.promise.then((e) => e.dup()));
    }
    pull() {
      return this.resolution ? this.resolution.pull() : this.promise.then((e) => e.pull());
    }
    ignoreUnhandledRejections() {
      this.resolution
        ? this.resolution.ignoreUnhandledRejections()
        : this.promise.then(
            (e) => {
              e.ignoreUnhandledRejections();
            },
            (e) => {},
          );
    }
    dispose() {
      this.resolution
        ? this.resolution.dispose()
        : this.promise.then(
            (e) => {
              e.dispose();
            },
            (e) => {},
          );
    }
    onBroken(e) {
      this.resolution
        ? this.resolution.onBroken(e)
        : this.promise.then((t) => {
            t.onBroken(e);
          }, e);
    }
  },
  Ah = new (class {
    exportStub(e) {
      throw Error(`Cannot serialize RPC stubs without an RPC session.`);
    }
    exportPromise(e) {
      throw Error(`Cannot serialize RPC stubs without an RPC session.`);
    }
    getImport(e) {}
    unexport(e) {}
    createPipe(e) {
      throw Error(`Cannot create pipes without an RPC session.`);
    }
    onSendError(e) {}
  })(),
  jh = {
    Error,
    EvalError,
    RangeError,
    ReferenceError,
    SyntaxError,
    TypeError,
    URIError,
    AggregateError,
  },
  Mh = class e {
    constructor(e, t) {
      ((this.exporter = e), (this.source = t));
    }
    static devaluate(t, n, r = Ah, i) {
      let a = new e(r, i);
      try {
        return a.devaluateImpl(t, n, 0);
      } catch (e) {
        if (a.exports)
          try {
            r.unexport(a.exports);
          } catch {}
        throw e;
      }
    }
    exports;
    devaluateImpl(e, t, n) {
      if (n >= 64)
        throw Error(
          `Serialization exceeded maximum allowed depth. (Does the message contain cycles?)`,
        );
      switch (rh(e)) {
        case `unsupported`: {
          let t;
          try {
            t = `Cannot serialize value: ${e}`;
          } catch {
            t = `Cannot serialize value: (couldn't stringify value)`;
          }
          throw TypeError(t);
        }
        case `primitive`:
          return typeof e == `number` && !isFinite(e)
            ? e === 1 / 0
              ? [`inf`]
              : e === -1 / 0
                ? [`-inf`]
                : [`nan`]
            : e;
        case `object`: {
          let t = e,
            r = {};
          for (let e in t) r[e] = this.devaluateImpl(t[e], t, n + 1);
          return r;
        }
        case `array`: {
          let t = e,
            r = t.length,
            i = Array(r);
          for (let e = 0; e < r; e++) i[e] = this.devaluateImpl(t[e], t, n + 1);
          return [i];
        }
        case `bigint`:
          return [`bigint`, e.toString()];
        case `date`:
          return [`date`, e.getTime()];
        case `bytes`: {
          let t = e;
          if (t.toBase64) return [`bytes`, t.toBase64({ omitPadding: !0 })];
          let n;
          if (typeof Buffer < `u`)
            n = (
              t instanceof Buffer ? t : Buffer.from(t.buffer, t.byteOffset, t.byteLength)
            ).toString(`base64`);
          else {
            let e = ``;
            for (let n = 0; n < t.length; n++) e += String.fromCharCode(t[n]);
            n = btoa(e);
          }
          return [`bytes`, n.replace(/=+$/, ``)];
        }
        case `headers`:
          return [`headers`, [...e]];
        case `request`: {
          let t = e,
            r = {};
          t.method !== `GET` && (r.method = t.method);
          let i = [...t.headers];
          if ((i.length > 0 && (r.headers = i), t.body))
            ((r.body = this.devaluateImpl(t.body, t, n + 1)), (r.duplex = t.duplex || `half`));
          else if (
            t.body === void 0 &&
            ![`GET`, `HEAD`, `OPTIONS`, `TRACE`, `DELETE`].includes(t.method)
          ) {
            let e = t.arrayBuffer(),
              n = new ReadableStream({
                async start(t) {
                  try {
                    (t.enqueue(new Uint8Array(await e)), t.close());
                  } catch (e) {
                    t.error(e);
                  }
                },
              }),
              i = sh.createReadableStreamHook(n);
            ((r.body = [`readable`, this.exporter.createPipe(n, i)]),
              (r.duplex = t.duplex || `half`));
          }
          (t.cache && t.cache !== `default` && (r.cache = t.cache),
            t.redirect !== `follow` && (r.redirect = t.redirect),
            t.integrity && (r.integrity = t.integrity),
            t.mode && t.mode !== `cors` && (r.mode = t.mode),
            t.credentials && t.credentials !== `same-origin` && (r.credentials = t.credentials),
            t.referrer && t.referrer !== `about:client` && (r.referrer = t.referrer),
            t.referrerPolicy && (r.referrerPolicy = t.referrerPolicy),
            t.keepalive && (r.keepalive = t.keepalive));
          let a = t;
          return (
            a.cf && (r.cf = a.cf),
            a.encodeResponseBody &&
              a.encodeResponseBody !== `automatic` &&
              (r.encodeResponseBody = a.encodeResponseBody),
            [`request`, t.url, r]
          );
        }
        case `response`: {
          let t = e,
            r = this.devaluateImpl(t.body, t, n + 1),
            i = {};
          (t.status !== 200 && (i.status = t.status),
            t.statusText && (i.statusText = t.statusText));
          let a = [...t.headers];
          a.length > 0 && (i.headers = a);
          let o = t;
          if (
            (o.cf && (i.cf = o.cf),
            o.encodeBody && o.encodeBody !== `automatic` && (i.encodeBody = o.encodeBody),
            o.webSocket)
          )
            throw TypeError(`Can't serialize a Response containing a webSocket.`);
          return [`response`, r, i];
        }
        case `error`: {
          let t = e,
            n = this.exporter.onSendError(t);
          n && (t = n);
          let r = [`error`, t.name, t.message];
          return (n && n.stack && r.push(n.stack), r);
        }
        case `undefined`:
          return [`undefined`];
        case `stub`:
        case `rpc-promise`: {
          if (!this.source) throw Error(`Can't serialize RPC stubs in this context.`);
          let { hook: t, pathIfPromise: n } = xh(e),
            r = this.exporter.getImport(t);
          return r === void 0
            ? ((t = n ? t.get(n) : t.dup()), this.devaluateHook(n ? `promise` : `export`, t))
            : n
              ? n.length > 0
                ? [`pipeline`, r, n]
                : [`pipeline`, r]
              : [`import`, r];
        }
        case `function`:
        case `rpc-target`: {
          if (!this.source) throw Error(`Can't serialize RPC stubs in this context.`);
          let n = this.source.getHookForRpcTarget(e, t);
          return this.devaluateHook(`export`, n);
        }
        case `rpc-thenable`: {
          if (!this.source) throw Error(`Can't serialize RPC stubs in this context.`);
          let n = this.source.getHookForRpcTarget(e, t);
          return this.devaluateHook(`promise`, n);
        }
        case `writable`: {
          if (!this.source) throw Error(`Can't serialize WritableStream in this context.`);
          let n = this.source.getHookForWritableStream(e, t);
          return this.devaluateHook(`writable`, n);
        }
        case `readable`: {
          if (!this.source) throw Error(`Can't serialize ReadableStream in this context.`);
          let n = e,
            r = this.source.getHookForReadableStream(n, t);
          return [`readable`, this.exporter.createPipe(n, r)];
        }
        default:
          throw Error(`unreachable`);
      }
    }
    devaluateHook(e, t) {
      this.exports ||= [];
      let n = e === `promise` ? this.exporter.exportPromise(t) : this.exporter.exportStub(t);
      return (this.exports.push(n), [e, n]);
    }
  };
new (class {
  importStub(e) {
    throw Error(`Cannot deserialize RPC stubs without an RPC session.`);
  }
  importPromise(e) {
    throw Error(`Cannot deserialize RPC stubs without an RPC session.`);
  }
  getExport(e) {}
  getPipeReadable(e) {
    throw Error(`Cannot retrieve pipe readable without an RPC session.`);
  }
})();
function Nh(e, t) {
  return new gh(
    new kh(
      new Response(t).arrayBuffer().then((t) => {
        let n = new Uint8Array(t),
          r = new Request(e, { body: n });
        return new Eh(Ch.fromAppReturn(r));
      }),
    ),
    [],
  );
}
var Ph = class e {
    constructor(e) {
      this.importer = e;
    }
    hooks = [];
    promises = [];
    evaluate(e) {
      let t = Ch.forEvaluate(this.hooks, this.promises);
      try {
        return ((t.value = this.evaluateImpl(e, t, `value`)), t);
      } catch (e) {
        throw (t.dispose(), e);
      }
    }
    evaluateCopy(e) {
      return this.evaluate(structuredClone(e));
    }
    evaluateImpl(t, n, r) {
      if (t instanceof Array) {
        if (t.length == 1 && t[0] instanceof Array) {
          let e = t[0];
          for (let t = 0; t < e.length; t++) e[t] = this.evaluateImpl(e[t], e, t);
          return e;
        } else
          switch (t[0]) {
            case `bigint`:
              if (typeof t[1] == `string`) return BigInt(t[1]);
              break;
            case `date`:
              if (typeof t[1] == `number`) return new Date(t[1]);
              break;
            case `bytes`:
              if (typeof t[1] == `string`) {
                if (typeof Buffer < `u`) return Buffer.from(t[1], `base64`);
                if (Uint8Array.fromBase64) return Uint8Array.fromBase64(t[1]);
                {
                  let e = atob(t[1]),
                    n = e.length,
                    r = new Uint8Array(n);
                  for (let t = 0; t < n; t++) r[t] = e.charCodeAt(t);
                  return r;
                }
              }
              break;
            case `error`:
              if (t.length >= 3 && typeof t[1] == `string` && typeof t[2] == `string`) {
                let e = new (jh[t[1]] || Error)(t[2]);
                return (typeof t[3] == `string` && (e.stack = t[3]), e);
              }
              break;
            case `undefined`:
              if (t.length === 1) return;
              break;
            case `inf`:
              return 1 / 0;
            case `-inf`:
              return -1 / 0;
            case `nan`:
              return NaN;
            case `headers`:
              if (t.length === 2 && t[1] instanceof Array) return new Headers(t[1]);
              break;
            case `request`: {
              if (t.length !== 3 || typeof t[1] != `string`) break;
              let e = t[1],
                i = t[2];
              if (typeof i != `object` || !i) break;
              if (
                i.body &&
                ((i.body = this.evaluateImpl(i.body, i, `body`)),
                !(
                  i.body === null ||
                  typeof i.body == `string` ||
                  i.body instanceof Uint8Array ||
                  i.body instanceof ReadableStream
                ))
              )
                throw TypeError(`Request body must be of type ReadableStream.`);
              if (
                i.signal &&
                ((i.signal = this.evaluateImpl(i.signal, i, `signal`)),
                !(i.signal instanceof AbortSignal))
              )
                throw TypeError(`Request siganl must be of type AbortSignal.`);
              if (i.headers && !(i.headers instanceof Array))
                throw TypeError(`Request headers must be serialized as an array of pairs.`);
              let a = new Request(e, i);
              if (i.body instanceof ReadableStream && a.body === void 0) {
                let e = Nh(a, i.body);
                return (this.promises.push({ promise: e, parent: n, property: r }), e);
              } else return a;
            }
            case `response`: {
              if (t.length !== 3) break;
              let e = this.evaluateImpl(t[1], n, r);
              if (
                !(
                  e === null ||
                  typeof e == `string` ||
                  e instanceof Uint8Array ||
                  e instanceof ReadableStream
                )
              )
                throw TypeError(`Response body must be of type ReadableStream.`);
              let i = t[2];
              if (typeof i != `object` || !i) break;
              if (i.webSocket)
                throw TypeError(`Can't deserialize a Response containing a webSocket.`);
              if (i.headers && !(i.headers instanceof Array))
                throw TypeError(`Request headers must be serialized as an array of pairs.`);
              return new Response(e, i);
            }
            case `import`:
            case `pipeline`: {
              if (t.length < 2 || t.length > 4 || typeof t[1] != `number`) break;
              let i = this.importer.getExport(t[1]);
              if (!i) throw Error(`no such entry on exports table: ${t[1]}`);
              let a = t[0] == `pipeline`,
                o = (e) => {
                  if (a) {
                    let t = new gh(e, []);
                    return (this.promises.push({ promise: t, parent: n, property: r }), t);
                  } else return (this.hooks.push(e), new gh(e, []));
                };
              if (t.length == 2) return o(a ? i.get([]) : i.dup());
              let s = t[2];
              if (
                !(s instanceof Array) ||
                !s.every((e) => typeof e == `string` || typeof e == `number`)
              )
                break;
              if (t.length == 3) return o(i.get(s));
              let c = t[3];
              if (!(c instanceof Array)) break;
              return ((c = new e(this.importer).evaluate([c])), o(i.call(s, c)));
            }
            case `remap`: {
              if (
                t.length !== 5 ||
                typeof t[1] != `number` ||
                !(t[2] instanceof Array) ||
                !(t[3] instanceof Array) ||
                !(t[4] instanceof Array)
              )
                break;
              let e = this.importer.getExport(t[1]);
              if (!e) throw Error(`no such entry on exports table: ${t[1]}`);
              let i = t[2];
              if (!i.every((e) => typeof e == `string` || typeof e == `number`)) break;
              let a = t[3].map((e) => {
                  if (
                    !(e instanceof Array) ||
                    e.length !== 2 ||
                    (e[0] !== `import` && e[0] !== `export`) ||
                    typeof e[1] != `number`
                  )
                    throw TypeError(`unknown map capture: ${JSON.stringify(e)}`);
                  if (e[0] === `export`) return this.importer.importStub(e[1]);
                  {
                    let t = this.importer.getExport(e[1]);
                    if (!t) throw Error(`no such entry on exports table: ${e[1]}`);
                    return t.dup();
                  }
                }),
                o = t[4],
                s = new gh(e.map(i, a, o), []);
              return (this.promises.push({ promise: s, parent: n, property: r }), s);
            }
            case `export`:
            case `promise`:
              if (typeof t[1] == `number`)
                if (t[0] == `promise`) {
                  let e = new gh(this.importer.importPromise(t[1]), []);
                  return (this.promises.push({ parent: n, property: r, promise: e }), e);
                } else {
                  let e = this.importer.importStub(t[1]);
                  return (this.hooks.push(e), new hh(e));
                }
              break;
            case `writable`:
              if (typeof t[1] == `number`) {
                let e = this.importer.importStub(t[1]),
                  n = sh.createWritableStreamFromHook(e);
                return (this.hooks.push(e), n);
              }
              break;
            case `readable`:
              if (typeof t[1] == `number`) {
                let e = this.importer.getPipeReadable(t[1]),
                  n = sh.createReadableStreamHook(e);
                return (this.hooks.push(n), e);
              }
              break;
          }
        throw TypeError(`unknown special value: ${JSON.stringify(t)}`);
      } else if (t instanceof Object) {
        let e = t;
        for (let t in e)
          t in Object.prototype || t === `toJSON`
            ? (this.evaluateImpl(e[t], e, t), delete e[t])
            : (e[t] = this.evaluateImpl(e[t], e, t));
        return e;
      } else return t;
    }
  },
  Fh = class {
    constructor(e, t, n) {
      ((this.session = e), (this.importId = t), n && (this.activePull = Promise.withResolvers()));
    }
    localRefcount = 0;
    remoteRefcount = 1;
    activePull;
    resolution;
    onBrokenRegistrations;
    resolve(e) {
      if (this.localRefcount == 0) {
        e.dispose();
        return;
      }
      if (((this.resolution = e), this.sendRelease(), this.onBrokenRegistrations)) {
        for (let t of this.onBrokenRegistrations) {
          let n = this.session.onBrokenCallbacks[t],
            r = this.session.onBrokenCallbacks.length;
          (e.onBroken(n),
            this.session.onBrokenCallbacks[r] === n
              ? delete this.session.onBrokenCallbacks[r]
              : delete this.session.onBrokenCallbacks[t]);
        }
        this.onBrokenRegistrations = void 0;
      }
      this.activePull &&= (this.activePull.resolve(), void 0);
    }
    async awaitResolution() {
      return (
        (this.activePull ||= (this.session.sendPull(this.importId), Promise.withResolvers())),
        await this.activePull.promise,
        this.resolution.pull()
      );
    }
    dispose() {
      this.resolution
        ? this.resolution.dispose()
        : (this.abort(Error(`RPC was canceled because the RpcPromise was disposed.`)),
          this.sendRelease());
    }
    abort(e) {
      this.resolution ||
        ((this.resolution = new lh(e)),
        (this.activePull &&= (this.activePull.reject(e), void 0)),
        (this.onBrokenRegistrations = void 0));
    }
    onBroken(e) {
      if (this.resolution) this.resolution.onBroken(e);
      else {
        let t = this.session.onBrokenCallbacks.length;
        (this.session.onBrokenCallbacks.push(e),
          (this.onBrokenRegistrations ||= []),
          this.onBrokenRegistrations.push(t));
      }
    }
    sendRelease() {
      this.remoteRefcount > 0 &&
        (this.session.sendRelease(this.importId, this.remoteRefcount), (this.remoteRefcount = 0));
    }
  },
  Ih = class e extends ch {
    constructor(e, t) {
      (super(), (this.isPromise = e), ++t.localRefcount, (this.entry = t));
    }
    entry;
    collectPath(e) {
      return this;
    }
    getEntry() {
      if (this.entry) return this.entry;
      throw Error(`This RpcImportHook was already disposed.`);
    }
    call(e, t) {
      let n = this.getEntry();
      return n.resolution ? n.resolution.call(e, t) : n.session.sendCall(n.importId, e, t);
    }
    stream(e, t) {
      let n = this.getEntry();
      return n.resolution ? n.resolution.stream(e, t) : n.session.sendStream(n.importId, e, t);
    }
    map(e, t, n) {
      let r;
      try {
        r = this.getEntry();
      } catch (e) {
        for (let e of t) e.dispose();
        throw e;
      }
      return r.resolution ? r.resolution.map(e, t, n) : r.session.sendMap(r.importId, e, t, n);
    }
    get(e) {
      let t = this.getEntry();
      return t.resolution ? t.resolution.get(e) : t.session.sendCall(t.importId, e);
    }
    dup() {
      return new e(!1, this.getEntry());
    }
    pull() {
      let e = this.getEntry();
      if (!this.isPromise) throw Error(`Can't pull this hook because it's not a promise hook.`);
      return e.resolution ? e.resolution.pull() : e.awaitResolution();
    }
    ignoreUnhandledRejections() {}
    dispose() {
      let e = this.entry;
      ((this.entry = void 0), e && --e.localRefcount === 0 && e.dispose());
    }
    onBroken(e) {
      this.entry && this.entry.onBroken(e);
    }
  },
  Lh = class extends Ih {
    session;
    constructor(e) {
      (super(!1, e), (this.session = e.session));
    }
    dispose() {
      if (this.session) {
        let e = this.session;
        ((this.session = void 0), e.shutdown());
      }
    }
  },
  Rh = class {
    constructor(e, t, n) {
      ((this.transport = e),
        (this.options = n),
        this.exports.push({ hook: t, refcount: 1 }),
        this.imports.push(new Fh(this, 0, !1)));
      let r,
        i = new Promise((e, t) => {
          r = t;
        });
      ((this.cancelReadLoop = r), this.readLoop(i).catch((e) => this.abort(e)));
    }
    exports = [];
    reverseExports = new Map();
    imports = [];
    abortReason;
    cancelReadLoop;
    nextExportId = -1;
    onBatchDone;
    pullCount = 0;
    onBrokenCallbacks = [];
    getMainImport() {
      return new Lh(this.imports[0]);
    }
    shutdown() {
      this.abort(Error(`RPC session was shut down by disposing the main stub`), !1);
    }
    exportStub(e) {
      if (this.abortReason) throw this.abortReason;
      let t = this.reverseExports.get(e);
      if (t !== void 0) return (++this.exports[t].refcount, t);
      {
        let t = this.nextExportId--;
        return ((this.exports[t] = { hook: e, refcount: 1 }), this.reverseExports.set(e, t), t);
      }
    }
    exportPromise(e) {
      if (this.abortReason) throw this.abortReason;
      let t = this.nextExportId--;
      return (
        (this.exports[t] = { hook: e, refcount: 1 }),
        this.reverseExports.set(e, t),
        this.ensureResolvingExport(t),
        t
      );
    }
    unexport(e) {
      for (let t of e) this.releaseExport(t, 1);
    }
    releaseExport(e, t) {
      let n = this.exports[e];
      if (!n) throw Error(`no such export ID: ${e}`);
      if (n.refcount < t) throw Error(`refcount would go negative: ${n.refcount} < ${t}`);
      ((n.refcount -= t),
        n.refcount === 0 &&
          (delete this.exports[e], this.reverseExports.delete(n.hook), n.hook.dispose()));
    }
    onSendError(e) {
      if (this.options.onSendError) return this.options.onSendError(e);
    }
    ensureResolvingExport(e) {
      let t = this.exports[e];
      if (!t) throw Error(`no such export ID: ${e}`);
      if (!t.pull) {
        let n = async () => {
            let e = t.hook;
            for (;;) {
              let t = await e.pull();
              if (t.value instanceof hh) {
                let { hook: n, pathIfPromise: r } = xh(t.value);
                if (r && r.length == 0 && this.getImport(e) === void 0) {
                  e = n;
                  continue;
                }
              }
              return t;
            }
          },
          r = t.autoRelease;
        (++this.pullCount,
          (t.pull = n()
            .then(
              (t) => {
                let n = Mh.devaluate(t.value, void 0, this, t);
                (this.send([`resolve`, e, n]), r && this.releaseExport(e, 1));
              },
              (t) => {
                (this.send([`reject`, e, Mh.devaluate(t, void 0, this)]),
                  r && this.releaseExport(e, 1));
              },
            )
            .catch((t) => {
              try {
                (this.send([`reject`, e, Mh.devaluate(t, void 0, this)]),
                  r && this.releaseExport(e, 1));
              } catch (e) {
                this.abort(e);
              }
            })
            .finally(() => {
              --this.pullCount === 0 && this.onBatchDone && this.onBatchDone.resolve();
            })));
      }
    }
    getImport(e) {
      if (e instanceof Ih && e.entry && e.entry.session === this) return e.entry.importId;
    }
    importStub(e) {
      if (this.abortReason) throw this.abortReason;
      let t = this.imports[e];
      return (t || ((t = new Fh(this, e, !1)), (this.imports[e] = t)), new Ih(!1, t));
    }
    importPromise(e) {
      if (this.abortReason) throw this.abortReason;
      if (this.imports[e])
        return new lh(
          Error(`Bug in RPC system: The peer sent a promise reusing an existing export ID.`),
        );
      let t = new Fh(this, e, !0);
      return ((this.imports[e] = t), new Ih(!0, t));
    }
    getExport(e) {
      return this.exports[e]?.hook;
    }
    getPipeReadable(e) {
      let t = this.exports[e];
      if (!t || !t.pipeReadable)
        throw Error(`Export ${e} is not a pipe or its readable end was already consumed.`);
      let n = t.pipeReadable;
      return ((t.pipeReadable = void 0), n);
    }
    createPipe(e, t) {
      if (this.abortReason) throw this.abortReason;
      this.send([`pipe`]);
      let n = this.imports.length,
        r = new Fh(this, n, !1);
      this.imports.push(r);
      let i = new Ih(!1, r),
        a = sh.createWritableStreamFromHook(i);
      return (
        e
          .pipeTo(a)
          .catch(() => {})
          .finally(() => t.dispose()),
        n
      );
    }
    send(e) {
      if (this.abortReason !== void 0) return 0;
      let t;
      try {
        t = JSON.stringify(e);
      } catch (e) {
        try {
          this.abort(e);
        } catch {}
        throw e;
      }
      return (this.transport.send(t).catch((e) => this.abort(e, !1)), t.length);
    }
    sendCall(e, t, n) {
      if (this.abortReason) throw this.abortReason;
      let r = [`pipeline`, e, t];
      if (n) {
        let e = Mh.devaluate(n.value, void 0, this, n);
        r.push(e[0]);
      }
      this.send([`push`, r]);
      let i = new Fh(this, this.imports.length, !1);
      return (this.imports.push(i), new Ih(!0, i));
    }
    sendStream(e, t, n) {
      if (this.abortReason) throw this.abortReason;
      let r = [`pipeline`, e, t],
        i = Mh.devaluate(n.value, void 0, this, n);
      r.push(i[0]);
      let a = this.send([`stream`, r]),
        o = this.imports.length,
        s = new Fh(this, o, !0);
      return (
        (s.remoteRefcount = 0),
        (s.localRefcount = 1),
        this.imports.push(s),
        {
          promise: s.awaitResolution().then(
            (e) => {
              (e.dispose(), delete this.imports[o]);
            },
            (e) => {
              throw (delete this.imports[o], e);
            },
          ),
          size: a,
        }
      );
    }
    sendMap(e, t, n, r) {
      if (this.abortReason) {
        for (let e of n) e.dispose();
        throw this.abortReason;
      }
      let i = [
        `remap`,
        e,
        t,
        n.map((e) => {
          let t = this.getImport(e);
          return t === void 0 ? [`export`, this.exportStub(e)] : [`import`, t];
        }),
        r,
      ];
      this.send([`push`, i]);
      let a = new Fh(this, this.imports.length, !1);
      return (this.imports.push(a), new Ih(!0, a));
    }
    sendPull(e) {
      if (this.abortReason) throw this.abortReason;
      this.send([`pull`, e]);
    }
    sendRelease(e, t) {
      this.abortReason || (this.send([`release`, e, t]), delete this.imports[e]);
    }
    abort(e, t = !0) {
      if (this.abortReason === void 0) {
        if ((this.cancelReadLoop(e), t))
          try {
            this.transport
              .send(JSON.stringify([`abort`, Mh.devaluate(e, void 0, this)]))
              .catch((e) => {});
          } catch {}
        if (
          (e === void 0 && (e = `undefined`),
          (this.abortReason = e),
          this.onBatchDone && this.onBatchDone.reject(e),
          this.transport.abort)
        )
          try {
            this.transport.abort(e);
          } catch (e) {
            Promise.resolve(e);
          }
        for (let t in this.onBrokenCallbacks)
          try {
            this.onBrokenCallbacks[t](e);
          } catch (e) {
            Promise.resolve(e);
          }
        for (let t in this.imports) this.imports[t].abort(e);
        for (let e in this.exports) this.exports[e].hook.dispose();
      }
    }
    async readLoop(e) {
      for (; !this.abortReason; ) {
        let t = JSON.parse(await Promise.race([this.transport.receive(), e]));
        if (this.abortReason) break;
        if (t instanceof Array)
          switch (t[0]) {
            case `push`:
              if (t.length > 1) {
                let e = new Eh(new Ph(this).evaluate(t[1]));
                (e.ignoreUnhandledRejections(), this.exports.push({ hook: e, refcount: 1 }));
                continue;
              }
              break;
            case `stream`:
              if (t.length > 1) {
                let e = new Eh(new Ph(this).evaluate(t[1]));
                e.ignoreUnhandledRejections();
                let n = this.exports.length;
                (this.exports.push({ hook: e, refcount: 1, autoRelease: !0 }),
                  this.ensureResolvingExport(n));
                continue;
              }
              break;
            case `pipe`: {
              let { readable: e, writable: t } = new TransformStream(),
                n = sh.createWritableStreamHook(t);
              this.exports.push({ hook: n, refcount: 1, pipeReadable: e });
              continue;
            }
            case `pull`: {
              let e = t[1];
              if (typeof e == `number`) {
                this.ensureResolvingExport(e);
                continue;
              }
              break;
            }
            case `resolve`:
            case `reject`: {
              let e = t[1];
              if (typeof e == `number` && t.length > 2) {
                let n = this.imports[e];
                if (n)
                  if (t[0] == `resolve`) n.resolve(new Eh(new Ph(this).evaluate(t[2])));
                  else {
                    let e = new Ph(this).evaluate(t[2]);
                    (e.dispose(), n.resolve(new lh(e.value)));
                  }
                else t[0] == `resolve` && new Ph(this).evaluate(t[2]).dispose();
                continue;
              }
              break;
            }
            case `release`: {
              let e = t[1],
                n = t[2];
              if (typeof e == `number` && typeof n == `number`) {
                this.releaseExport(e, n);
                continue;
              }
              break;
            }
            case `abort`: {
              let e = new Ph(this).evaluate(t[1]);
              (e.dispose(), this.abort(e, !1));
              break;
            }
          }
        throw Error(`bad RPC message: ${JSON.stringify(t)}`);
      }
    }
    async drain() {
      if (this.abortReason) throw this.abortReason;
      if (this.pullCount > 0) {
        let { promise: e, resolve: t, reject: n } = Promise.withResolvers();
        ((this.onBatchDone = { resolve: t, reject: n }), await e);
      }
    }
    getStats() {
      let e = { imports: 0, exports: 0 };
      for (let t in this.imports) ++e.imports;
      for (let t in this.exports) ++e.exports;
      return e;
    }
  },
  zh = class {
    #e;
    #t;
    constructor(e, t, n = {}) {
      let r;
      ((r = t ? new Eh(Ch.fromAppReturn(t)) : new lh(Error(`This connection has no main object.`))),
        (this.#e = new Rh(e, r, n)),
        (this.#t = new hh(this.#e.getMainImport())));
    }
    getRemoteMain() {
      return this.#t;
    }
    getStats() {
      return this.#e.getStats();
    }
    drain() {
      return this.#e.drain();
    }
  },
  Bh,
  Vh = class {
    context;
    captureMap = new Map();
    instructions = [];
    constructor(e, t) {
      (Bh
        ? (this.context = { parent: Bh, captures: [], subject: Bh.capture(e), path: t })
        : (this.context = { parent: void 0, captures: [], subject: e, path: t }),
        (Bh = this));
    }
    unregister() {
      Bh = this.context.parent;
    }
    makeInput() {
      return new Uh(this, 0);
    }
    makeOutput(e) {
      let t;
      try {
        t = Mh.devaluate(e.value, void 0, this, e);
      } finally {
        e.dispose();
      }
      return (
        this.instructions.push(t),
        this.context.parent
          ? (this.context.parent.instructions.push([
              `remap`,
              this.context.subject,
              this.context.path,
              this.context.captures.map((e) => [`import`, e]),
              this.instructions,
            ]),
            new Uh(this.context.parent, this.context.parent.instructions.length))
          : this.context.subject.map(this.context.path, this.context.captures, this.instructions)
      );
    }
    pushCall(e, t, n) {
      let r = Mh.devaluate(n.value, void 0, this, n);
      r = r[0];
      let i = this.capture(e.dup());
      return (
        this.instructions.push([`pipeline`, i, t, r]), new Uh(this, this.instructions.length)
      );
    }
    pushGet(e, t) {
      let n = this.capture(e.dup());
      return (this.instructions.push([`pipeline`, n, t]), new Uh(this, this.instructions.length));
    }
    capture(e) {
      if (e instanceof Uh && e.mapper === this) return e.idx;
      let t = this.captureMap.get(e);
      if (t === void 0) {
        if (this.context.parent) {
          let t = this.context.parent.capture(e);
          this.context.captures.push(t);
        } else this.context.captures.push(e);
        ((t = -this.context.captures.length), this.captureMap.set(e, t));
      }
      return t;
    }
    exportStub(e) {
      throw Error(
        `Can't construct an RpcTarget or RPC callback inside a mapper function. Try creating a new RpcStub outside the callback first, then using it inside the callback.`,
      );
    }
    exportPromise(e) {
      return this.exportStub(e);
    }
    getImport(e) {
      return this.capture(e);
    }
    unexport(e) {}
    createPipe(e) {
      throw Error(`Cannot send ReadableStream inside a mapper function.`);
    }
    onSendError(e) {}
  };
ah.sendMap = (e, t, n) => {
  let r = new Vh(e, t),
    i;
  try {
    i = Ch.fromAppReturn(fh(r.pushCall.bind(r), () => n(new gh(r.makeInput(), []))));
  } finally {
    r.unregister();
  }
  if (i instanceof Promise)
    throw (i.catch((e) => {}), Error(`RPC map() callbacks cannot be async.`));
  return new gh(r.makeOutput(i), []);
};
function Hh() {
  throw Error(
    `Attempted to use an abstract placeholder from a mapper function. Please make sure your map function has no side effects.`,
  );
}
var Uh = class extends ch {
    constructor(e, t) {
      (super(), (this.mapper = e), (this.idx = t));
    }
    dup() {
      return this;
    }
    dispose() {}
    get(e) {
      if (e.length == 0) return this;
      if (Bh) return Bh.pushGet(this, e);
      Hh();
    }
    call(e, t) {
      Hh();
    }
    map(e, t, n) {
      Hh();
    }
    pull() {
      Hh();
    }
    ignoreUnhandledRejections() {}
    onBroken(e) {
      Hh();
    }
  },
  Wh = class {
    constructor(e, t) {
      ((this.captures = e), (this.variables = [t]));
    }
    variables;
    dispose() {
      for (let e of this.variables) e.dispose();
    }
    apply(e) {
      try {
        if (e.length < 1) throw Error(`Invalid empty mapper function.`);
        for (let t of e.slice(0, -1)) {
          let e = new Ph(this).evaluateCopy(t);
          if (e.value instanceof hh) {
            let t = yh(e.value);
            if (t) {
              this.variables.push(t);
              continue;
            }
          }
          this.variables.push(new Eh(e));
        }
        return new Ph(this).evaluateCopy(e[e.length - 1]);
      } finally {
        for (let e of this.variables) e.dispose();
      }
    }
    importStub(e) {
      throw Error(`A mapper function cannot refer to exports.`);
    }
    importPromise(e) {
      return this.importStub(e);
    }
    getExport(e) {
      return e < 0 ? this.captures[-e - 1] : this.variables[e];
    }
    getPipeReadable(e) {
      throw Error(`A mapper function cannot use pipe readables.`);
    }
  };
function Gh(e, t, n, r, i) {
  let a = new Wh(r, new Eh(Ch.deepCopyFrom(e, t, n)));
  try {
    return a.apply(i);
  } finally {
    a.dispose();
  }
}
ah.applyMap = (e, t, n, r, i) => {
  try {
    let a;
    if (e instanceof gh) throw Error(`applyMap() can't be called on RpcPromise`);
    if (e instanceof Array) {
      let t = [];
      try {
        for (let a of e) t.push(Gh(a, e, n, r, i));
      } catch (e) {
        for (let e of t) e.dispose();
        throw e;
      }
      a = Ch.fromArray(t);
    } else a = e == null ? Ch.fromAppReturn(e) : Gh(e, t, n, r, i);
    return new Eh(a);
  } finally {
    for (let e of r) e.dispose();
  }
};
var Kh = class e extends ch {
    state;
    static create(t) {
      return new e({ refcount: 1, writer: t.getWriter(), closed: !1 });
    }
    constructor(e, t) {
      (super(), (this.state = e), t && ++e.refcount);
    }
    getState() {
      if (this.state) return this.state;
      throw Error(`Attempted to use a WritableStreamStubHook after it was disposed.`);
    }
    call(e, t) {
      try {
        let n = this.getState();
        if (e.length !== 1 || typeof e[0] != `string`)
          throw Error(`WritableStream stub only supports direct method calls`);
        let r = e[0];
        if (r !== `write` && r !== `close` && r !== `abort`)
          throw (t.dispose(), Error(`Unknown WritableStream method: ${r}`));
        (r === `close` || r === `abort`) && (n.closed = !0);
        let i = n.writer[r];
        return new kh(t.deliverCall(i, n.writer).then((e) => new Eh(e)));
      } catch (e) {
        return new lh(e);
      }
    }
    map(e, t, n) {
      for (let e of t) e.dispose();
      return new lh(Error(`Cannot use map() on a WritableStream`));
    }
    get(e) {
      return new lh(Error(`Cannot access properties on a WritableStream stub`));
    }
    dup() {
      return new e(this.getState(), this);
    }
    pull() {
      return Promise.reject(Error(`Cannot pull a WritableStream stub`));
    }
    ignoreUnhandledRejections() {}
    dispose() {
      let e = this.state;
      ((this.state = void 0),
        e &&
          --e.refcount === 0 &&
          (e.closed ||
            e.writer
              .abort(Error(`WritableStream RPC stub was disposed without calling close()`))
              .catch(() => {}),
          e.writer.releaseLock()));
    }
    onBroken(e) {}
  },
  qh = 256 * 1024,
  Jh = 1024 * 1024 * 1024,
  Yh = 64 * 1024,
  Xh = 2,
  Zh = 1.25,
  Qh = 0.9,
  $h = 3,
  eg = class {
    constructor(e) {
      this.now = e;
    }
    window = qh;
    bytesInFlight = 0;
    inStartupPhase = !0;
    delivered = 0;
    deliveredTime = 0;
    firstAckTime = 0;
    firstAckDelivered = 0;
    minRtt = 1 / 0;
    roundsWithoutIncrease = 0;
    lastRoundWindow = 0;
    roundStartTime = 0;
    onSend(e) {
      this.bytesInFlight += e;
      let t = {
        sentTime: this.now(),
        size: e,
        deliveredAtSend: this.delivered,
        deliveredTimeAtSend: this.deliveredTime,
        windowAtSend: this.window,
        windowFullAtSend: this.bytesInFlight >= this.window,
      };
      return { token: t, shouldBlock: t.windowFullAtSend };
    }
    onError(e) {
      this.bytesInFlight -= e.size;
    }
    onAck(e) {
      let t = this.now();
      ((this.delivered += e.size), (this.deliveredTime = t), (this.bytesInFlight -= e.size));
      let n = t - e.sentTime;
      if (((this.minRtt = Math.min(this.minRtt, n)), this.firstAckTime === 0))
        ((this.firstAckTime = t), (this.firstAckDelivered = this.delivered));
      else {
        let n, r;
        e.deliveredTimeAtSend === 0
          ? ((n = this.firstAckTime), (r = this.firstAckDelivered))
          : ((n = e.deliveredTimeAtSend), (r = e.deliveredAtSend));
        let i = t - n,
          a = (this.delivered - r) / i,
          o = this.inStartupPhase ? Xh : Zh,
          s = a * this.minRtt * o;
        ((s = Math.min(s, e.windowAtSend * o)),
          (s = e.windowFullAtSend ? Math.max(s, e.windowAtSend * Qh) : Math.max(s, this.window)),
          (this.window = Math.max(Math.min(s, Jh), Yh)),
          this.inStartupPhase &&
            e.sentTime >= this.roundStartTime &&
            (this.window > this.lastRoundWindow * Zh
              ? (this.roundsWithoutIncrease = 0)
              : ++this.roundsWithoutIncrease >= $h && (this.inStartupPhase = !1),
            (this.roundStartTime = t),
            (this.lastRoundWindow = this.window)));
      }
      return this.bytesInFlight < this.window;
    }
  };
function tg(e) {
  let t,
    n = !1,
    r = new eg(() => performance.now()),
    i,
    a,
    o = () => {
      n || ((n = !0), e.dispose());
    };
  return new WritableStream({
    write(n, s) {
      if (t !== void 0) throw t;
      let c = Ch.fromAppParams([n]),
        { promise: l, size: u } = e.stream([`write`], c);
      if (u === void 0)
        return l.catch((e) => {
          throw (t === void 0 && (t = e), e);
        });
      {
        let { token: e, shouldBlock: n } = r.onSend(u);
        if (
          (l.then(
            () => {
              r.onAck(e) && i && (i(), (i = void 0), (a = void 0));
            },
            (n) => {
              (r.onError(e),
                t === void 0 && ((t = n), s.error(n), o()),
                (a &&= (a(n), (i = void 0), void 0)));
            },
          ),
          n)
        )
          return new Promise((e, t) => {
            ((i = e), (a = t));
          });
      }
    },
    async close() {
      if (t !== void 0) throw (o(), t);
      let { promise: n } = e.stream([`close`], Ch.fromAppParams([]));
      try {
        await n;
      } catch (e) {
        throw t ?? e;
      } finally {
        o();
      }
    },
    abort(n) {
      if (t !== void 0) return;
      ((t = n ?? Error(`WritableStream was aborted`)), (a &&= (a(t), (i = void 0), void 0)));
      let { promise: r } = e.stream([`abort`], Ch.fromAppParams([n]));
      r.then(
        () => o(),
        () => o(),
      );
    },
  });
}
var ng = class e extends ch {
  state;
  static create(t) {
    return new e({ refcount: 1, stream: t, canceled: !1 });
  }
  constructor(e, t) {
    (super(), (this.state = e), t && ++e.refcount);
  }
  call(e, t) {
    return (t.dispose(), new lh(Error(`Cannot call methods on a ReadableStream stub`)));
  }
  map(e, t, n) {
    for (let e of t) e.dispose();
    return new lh(Error(`Cannot use map() on a ReadableStream`));
  }
  get(e) {
    return new lh(Error(`Cannot access properties on a ReadableStream stub`));
  }
  dup() {
    let t = this.state;
    if (!t) throw Error(`Attempted to dup a ReadableStreamStubHook after it was disposed.`);
    return new e(t, this);
  }
  pull() {
    return Promise.reject(Error(`Cannot pull a ReadableStream stub`));
  }
  ignoreUnhandledRejections() {}
  dispose() {
    let e = this.state;
    ((this.state = void 0),
      e &&
        --e.refcount === 0 &&
        (e.canceled ||
          ((e.canceled = !0),
          e.stream.locked ||
            e.stream
              .cancel(Error(`ReadableStream RPC stub was disposed without being consumed`))
              .catch(() => {}))));
  }
  onBroken(e) {}
};
((sh.createWritableStreamHook = Kh.create),
  (sh.createWritableStreamFromHook = tg),
  (sh.createReadableStreamHook = ng.create));
var rg = zh,
  ig = eh,
  ag = class extends ig {
    #e;
    get services() {
      return this.#e;
    }
    constructor(e) {
      (super(), (this.#e = e));
    }
  },
  og = { darwin: `Macintosh; Intel Mac OS X`, linux: `X11; Linux`, win32: `Windows NT 10.0` };
function sg(e) {
  let t = process.env.CODEX_API_BASE_URL;
  return t && t.trim().length > 0
    ? t.replace(/\/+$/, ``)
    : (process.env.CODEX_API_ENDPOINT ?? ``).toLowerCase() === `localhost`
      ? e.devApiBaseUrl
      : e.prodApiBaseUrl;
}
function cg(e, t) {
  return `${sg(e)}/${t.replace(/^\/+/, ``)}`;
}
async function lg({
  action: e,
  appServerClient: t,
  desktopOriginator: n,
  headers: r = {},
  refreshToken: i = !1,
}) {
  let a = await t.getAuthToken({ refreshToken: i });
  if (!a) throw Error(`Sign in to ChatGPT in Codex Desktop to ${e}.`);
  let o = { ...r };
  return (ug(o, a, { desktopOriginator: n }), o);
}
function ug(e, t, { desktopOriginator: n, includeSurfaceHeaders: r = !0 }) {
  hg(e, `Authorization`, `Bearer ${t}`);
  let i = fg(t);
  (i && hg(e, `ChatGPT-Account-Id`, i), r && dg(e, { desktopOriginator: n }));
}
function dg(e, { desktopOriginator: t }) {
  (mg(e, `originator`) || hg(e, `originator`, t), mg(e, `User-Agent`) || hg(e, `User-Agent`, pg()));
}
function fg(e) {
  let t = e.split(`.`);
  if (t.length < 2) return null;
  try {
    let e = JSON.parse(Buffer.from(t[1], `base64url`).toString(`utf8`))[
      `https://api.openai.com/auth`
    ];
    if (e && typeof e == `object` && e) {
      let t = e.chatgpt_account_id;
      return typeof t == `string` ? t : null;
    }
  } catch {}
  return null;
}
function pg() {
  let e = og[process.platform] ?? process.platform;
  return `Codex Desktop/${a.app.getVersion()} (${e}; ${process.arch})`;
}
function mg(e, t) {
  return gg(e, t) != null;
}
function hg(e, t, n) {
  let r = gg(e, t);
  (r && delete e[r], (e[t] = n));
}
function gg(e, t) {
  let n = t.toLowerCase();
  return Object.keys(e).find((e) => e.toLowerCase() === n);
}
var _g = 100,
  vg = t.$s({ detail: t.nc().optional() }),
  yg = class extends Error {},
  bg = class extends Error {},
  xg = class extends Error {};
async function Sg({ appServerClient: e, clientId: t, desktopApiOptions: n }) {
  return Ag({ appServerClient: e, clientId: t, desktopApiOptions: n, cursor: null });
}
async function Cg({ appServerClient: e, desktopApiOptions: t, envId: n, name: r }) {
  return jg(
    await (
      await Tg({
        action: `rename remote control environment`,
        appServerClient: e,
        desktopApiOptions: t,
        method: `PATCH`,
        path: kg(n),
        headers: { "Content-Type": `application/json` },
        body: JSON.stringify({ name: r }),
        mapNotFoundToFeatureUnavailable: !1,
      })
    ).json(),
  );
}
async function wg({ appServerClient: e, desktopApiOptions: t, envId: n }) {
  await Tg({
    action: `delete remote control environment`,
    appServerClient: e,
    desktopApiOptions: t,
    method: `DELETE`,
    path: kg(n),
    mapNotFoundToFeatureUnavailable: !1,
  });
}
async function Tg({
  action: e,
  appServerClient: t,
  desktopApiOptions: n,
  path: r,
  method: i,
  headers: o = {},
  body: s,
  mapNotFoundToFeatureUnavailable: c = !0,
}) {
  let l = cg(n, r),
    u = await Eg({ action: e, appServerClient: t, desktopApiOptions: n, headers: o }),
    d = await a.net.fetch(l, { method: i, headers: u, body: s });
  if (
    (d.status === 401 &&
      ((u = await Eg({
        action: e,
        appServerClient: t,
        desktopApiOptions: n,
        headers: o,
        refreshToken: !0,
      })),
      (d = await a.net.fetch(l, { method: i, headers: u, body: s }))),
    d.status === 404 && c)
  )
    throw new yg();
  if (d.status === 403) throw new xg(await Ng(d));
  if (d.status === 401) throw new bg(Dg(e));
  if (!d.ok) throw Error(`Remote control request failed (${d.status}): ${await Ng(d)}`);
  return d;
}
async function Eg({
  action: e,
  appServerClient: t,
  desktopApiOptions: n,
  headers: r = {},
  refreshToken: i = !1,
}) {
  try {
    return await lg({
      action: e,
      appServerClient: t,
      desktopOriginator: n.desktopOriginator,
      headers: r,
      refreshToken: i,
    });
  } catch (t) {
    throw t instanceof Error && t.message === Dg(e) ? new bg(t.message) : t;
  }
}
function Dg(e) {
  return `Sign in to ChatGPT in Codex Desktop to ${e}.`;
}
function Og(e, t) {
  let n = new URLSearchParams({ limit: String(_g) });
  return (
    t != null && n.set(`cursor`, t),
    e == null
      ? `/codex/remote/control/environments?${n.toString()}`
      : `/codex/remote/control/clients/${encodeURIComponent(e)}/environments?${n.toString()}`
  );
}
function kg(e) {
  return `/codex/remote/control/environments/${encodeURIComponent(e)}`;
}
async function Ag({ appServerClient: e, clientId: t, desktopApiOptions: n, cursor: r }) {
  let i = await (
      await Tg({
        action: `load remote control environments`,
        appServerClient: e,
        desktopApiOptions: n,
        mapNotFoundToFeatureUnavailable: t == null,
        method: `GET`,
        path: Og(t, r),
      })
    ).json(),
    a = i.items.map(jg);
  return i.cursor == null
    ? a
    : a.concat(
        await Ag({ appServerClient: e, clientId: t, desktopApiOptions: n, cursor: i.cursor }),
      );
}
function jg(e) {
  let n = e.env_id,
    r = Mg(e, `display_name`),
    i = Mg(e, `host_name`),
    a = (r ?? e.name ?? i ?? ``).trim(),
    o = (i ?? e.name ?? r ?? ``).trim(),
    s = a || o || n,
    c = o || s;
  return {
    hostId: t.ma(n),
    displayName: s,
    hostName: c,
    autoConnect: !1,
    source: `remote-control`,
    envId: n,
    environmentKind: e.kind,
    clientType: e.client_type ?? `CODEX_UNKNOWN`,
    online: e.online,
    busy: e.busy,
    os: e.os,
    arch: e.arch,
    appServerVersion: e.app_server_version,
    installationId: e.installation_id ?? null,
    lastSeenAt: e.last_seen_at ?? null,
  };
}
function Mg(e, t) {
  let n = e[t];
  return typeof n == `string` ? n : null;
}
async function Ng(e) {
  try {
    let t = await e.text();
    if (!t) return e.statusText || `Unknown error`;
    try {
      let e = vg.safeParse(JSON.parse(t));
      if (e.success && e.data.detail != null) return e.data.detail;
    } catch {}
    return t;
  } catch {
    return e.statusText || `Unknown error`;
  }
}
var Pg = t.Gr(`remote-control-websocket`),
  Fg = [`remote_control_controller_websocket`],
  Ig = class extends Error {
    errorCode = t.ji;
    constructor() {
      super(`Remote control enrollment step-up token does not match current account.`);
    }
  },
  Lg = `codex.remote_control.enroll`,
  Rg = 300,
  zg = `after-first-unlock-device-key-v1`,
  Bg = t
    .$s({
      sub: t.nc().optional(),
      "https://api.openai.com/auth": t
        .$s({
          account_id: t.nc().optional(),
          account_user_id: t.nc().optional(),
          chatgpt_account_id: t.nc().optional(),
          chatgpt_account_user_id: t.nc().optional(),
          user_id: t.nc().optional(),
        })
        .passthrough()
        .optional(),
    })
    .passthrough(),
  Vg = t.tc(t.nc(), t.ic()),
  Hg = class extends t.cn {
    constructor(e) {
      let n = Gg(e.desktopApiOptions),
        r = e.globalState,
        i = e.deviceKeyClient;
      super({
        envId: e.hostConfig.env_id,
        connectionGroup: e.appServerClient,
        connectionKey: n,
        websocketUrl: t.ln(cg(e.desktopApiOptions, `/codex/remote/control/client`)),
        getAuthHeaders: ({ headers: t } = {}) =>
          qg({
            appServerClient: e.appServerClient,
            desktopApiOptions: e.desktopApiOptions,
            headers: t,
          }),
        enrollClient: ({ headers: t }) =>
          Jg({
            appServerClient: e.appServerClient,
            deviceKeyClient: i,
            desktopApiOptions: e.desktopApiOptions,
            enrollmentKey: n,
            globalState: r,
            headers: t,
            requestRemoteControlEnrollmentStepUpToken: e.requestRemoteControlEnrollmentStepUpToken,
          }),
        authorizeDeviceKeyChallenge: (e) =>
          D_({ challenge: e, deviceKeyClient: i, enrollmentKey: n, globalState: r }),
      });
    }
  };
async function Ug({
  appServerClient: e,
  desktopApiOptions: t,
  deviceKeyClient: n,
  globalState: r,
  requestRemoteControlEnrollmentStepUpToken: i,
}) {
  await Jg({
    appServerClient: e,
    deviceKeyClient: n,
    desktopApiOptions: t,
    enrollmentKey: Gg(t),
    globalState: r,
    headers: await qg({
      action: `authorize remote control environments`,
      appServerClient: e,
      desktopApiOptions: t,
    }),
    requestRemoteControlEnrollmentStepUpToken: i,
  });
}
async function Wg({
  appServerClient: e,
  desktopApiOptions: t,
  deviceKeyClient: n,
  globalState: r,
}) {
  let i = t_(
    await qg({
      action: `check remote control authorization`,
      appServerClient: e,
      desktopApiOptions: t,
    }),
  );
  if (i.tokenAccountUserId == null) return { clientAuthorized: !1, clientId: null };
  let a = await h_({ authIdentity: i, connectionKey: Gg(t), deviceKeyClient: n, globalState: r });
  return { clientAuthorized: a != null, clientId: a?.record.clientId ?? null };
}
function Gg(e) {
  return [e.desktopOriginator, e.devApiBaseUrl, e.prodApiBaseUrl, zg].join(`
`);
}
function Kg(e, t) {
  return `${e}\n${t}`;
}
async function qg({
  action: e = `connect remote control environments`,
  appServerClient: t,
  desktopApiOptions: n,
  headers: r,
}) {
  return lg({ action: e, appServerClient: t, desktopOriginator: n.desktopOriginator, headers: r });
}
async function Jg({
  appServerClient: e,
  deviceKeyClient: t,
  desktopApiOptions: n,
  enrollmentKey: r,
  globalState: i,
  headers: a,
  requestRemoteControlEnrollmentStepUpToken: o,
}) {
  let s = t_(a),
    c = s.tokenAccountUserId;
  if (c == null)
    throw Error(`Remote control enrollment requires the current ChatGPT account user id.`);
  let l = s.tokenAccountId ?? s.headerChatGptAccountId,
    u = await h_({ authIdentity: s, connectionKey: r, deviceKeyClient: t, globalState: i }),
    d = u?.record ?? null,
    f = u?.key ?? Kg(r, c),
    p = d,
    m;
  if (p == null) {
    if (o == null)
      throw Error(`Remote control enrollment requires explicit authorization in settings.`);
    Pg().info(`remote_control_client_enrollment_start_request`, {
      ...n_({ authIdentity: s, hasExistingEnrollment: !1 }),
    });
    let u = await u_({ appServerClient: e, body: {}, desktopApiOptions: n, headers: a });
    if (
      (Pg().info(`remote_control_client_enrollment_start_response`, {
        ...n_({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseClientId: u.client_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
        }),
      }),
      u.account_user_id !== c &&
        !(
          s.tokenAccountId != null &&
          s.headerChatGptAccountId === s.tokenAccountId &&
          s.tokenAuthUserId === u.account_user_id
        ))
    )
      throw (
        Pg().warning(`remote_control_client_enrollment_start_account_mismatch`, {
          ...n_({
            authIdentity: s,
            hasExistingEnrollment: !1,
            responseAccountUserId: u.account_user_id,
            responseClientId: u.client_id,
            responseChallengeId: u.device_key_challenge.challenge_id,
          }),
        }),
        Error(`Remote control enrollment start does not match current account.`)
      );
    p = await S_({ accountUserId: u.account_user_id, clientId: u.client_id, deviceKeyClient: t });
    try {
      if (
        (Pg().info(`remote_control_client_enrollment_key_created`, {
          safe: { algorithm: p.algorithm, protectionClass: p.protectionClass },
          sensitive: { accountUserId: p.accountUserId, clientId: p.clientId, keyId: p.keyId },
        }),
        o == null)
      )
        throw Error(`Remote control enrollment requires a step-up authorization flow.`);
      Pg().info(`remote_control_client_enrollment_step_up_requested`, {
        ...n_({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
          responseClientId: u.client_id,
        }),
      });
      let d = await o({ accountId: l }),
        f = i_({ accountUserId: c, stepUpToken: d }),
        h = n_({
          authIdentity: s,
          hasExistingEnrollment: !1,
          responseAccountUserId: u.account_user_id,
          responseChallengeId: u.device_key_challenge.challenge_id,
          responseClientId: u.client_id,
        });
      (Pg().info(`remote_control_client_enrollment_step_up_validated`, {
        safe: { ...h.safe, stepUpTokenScopes: f.scopes },
        sensitive: {
          ...h.sensitive,
          stepUpIssuedAt: f.issuedAt,
          stepUpPasswordAuthTime: f.passwordAuthTime,
          stepUpTokenAccountUserId: f.accountUserId,
        },
      }),
        (m = await d_({
          appServerClient: e,
          body: {
            client_id: p.clientId,
            step_up_token: d,
            device_identity: w_(p),
            device_key_proof: await E_({
              challenge: u.device_key_challenge,
              deviceKeyClient: t,
              desktopApiOptions: n,
              enrollment: p,
              expectedPath: `/codex/remote/control/client/enroll/finish`,
              requireDeviceIdentityHash: !1,
            }),
          },
          desktopApiOptions: n,
          headers: a,
        })),
        Pg().info(`remote_control_client_enrollment_finish_response`, { ...r_(m) }),
        Xg(m, p),
        y_(i, Kg(r, p.accountUserId), p));
    } catch (e) {
      throw (await C_({ deviceKeyClient: t, enrollment: p }), e);
    }
  } else {
    Pg().info(`remote_control_client_refresh_start_request`, {
      ...n_({ authIdentity: s, existingEnrollment: d, hasExistingEnrollment: !0 }),
    });
    let c;
    try {
      c = await f_({
        appServerClient: e,
        body: { client_id: p.clientId },
        desktopApiOptions: n,
        headers: a,
      });
    } catch (s) {
      if (!x_(s)) throw s;
      return (
        await C_({ deviceKeyClient: t, enrollment: p }),
        b_(i, f),
        Jg({
          appServerClient: e,
          deviceKeyClient: t,
          desktopApiOptions: n,
          enrollmentKey: r,
          globalState: i,
          headers: a,
          requestRemoteControlEnrollmentStepUpToken: o,
        })
      );
    }
    if (
      (Pg().info(`remote_control_client_refresh_start_response`, {
        ...n_({
          authIdentity: s,
          existingEnrollment: d,
          hasExistingEnrollment: !0,
          responseAccountUserId: c.account_user_id,
          responseClientId: c.client_id,
          responseChallengeId: c.device_key_challenge.challenge_id,
        }),
      }),
      c.client_id !== p.clientId || c.account_user_id !== p.accountUserId)
    )
      throw (
        Pg().warning(`remote_control_client_refresh_start_mismatch`, {
          ...n_({
            authIdentity: s,
            existingEnrollment: d,
            hasExistingEnrollment: !0,
            responseAccountUserId: c.account_user_id,
            responseClientId: c.client_id,
            responseChallengeId: c.device_key_challenge.challenge_id,
          }),
        }),
        Error(`Remote control refresh challenge does not match local enrollment.`)
      );
    ((m = await p_({
      appServerClient: e,
      body: {
        client_id: p.clientId,
        device_key_proof: await E_({
          challenge: c.device_key_challenge,
          deviceKeyClient: t,
          desktopApiOptions: n,
          enrollment: p,
          expectedPath: `/codex/remote/control/client/refresh/finish`,
          requireDeviceIdentityHash: !0,
        }),
      },
      desktopApiOptions: n,
      headers: a,
    })),
      Pg().info(`remote_control_client_refresh_finish_response`, { ...r_(m) }));
  }
  let h = Xg(m, p);
  return {
    clientId: m.client_id,
    headers: { "x-codex-client-session-token": `Bearer ${m.remote_control_token}` },
    tokenExpiresAt: h.tokenExpiresAt,
    scopes: h.scopes,
    requiresDeviceKeyProof: !0,
  };
}
function Yg(e) {
  if (e == null) return;
  let t = Date.parse(e);
  if (Number.isFinite(t)) return Math.floor(t / 1e3);
}
function Xg(e, t) {
  if (e.client_id !== t.clientId || e.account_user_id !== t.accountUserId)
    throw Error(`Remote control token response does not match local enrollment.`);
  let n = Yg(e.expires_at);
  if (n == null || n <= Math.floor(Date.now() / 1e3))
    throw Error(`Remote control token response has invalid expiration.`);
  if (e.scopes.length !== Fg.length || !Fg.every((t, n) => e.scopes[n] === t))
    throw Error(`Remote control token response has unexpected scopes.`);
  return { tokenExpiresAt: n, scopes: [...Fg] };
}
function Zg(e) {
  return e[`ChatGPT-Account-Id`] ?? e[`chatgpt-account-id`] ?? null;
}
function Qg(e, t) {
  let n = t.toLowerCase();
  for (let [t, r] of Object.entries(e)) if (t.toLowerCase() === n) return r;
  return null;
}
function $g(e) {
  let t = Qg(e, `authorization`);
  if (t == null) return null;
  let [n, r] = t.split(/\s+/, 2);
  return n?.toLowerCase() !== `bearer` || !r ? null : r;
}
function e_(e) {
  try {
    let t = Bg.safeParse(s_(e));
    return t.success ? t.data : null;
  } catch {
    return null;
  }
}
function t_(e) {
  let t = $g(e),
    n = t == null ? null : e_(t),
    r = n?.[`https://api.openai.com/auth`];
  return {
    headerChatGptAccountId: Zg(e),
    tokenAccountId: r?.chatgpt_account_id ?? r?.account_id ?? null,
    tokenAccountUserId: r?.chatgpt_account_user_id ?? r?.account_user_id ?? null,
    tokenAuthUserId: r?.user_id ?? null,
    tokenSubject: n?.sub ?? null,
  };
}
function n_({
  authIdentity: e,
  existingEnrollment: t,
  hasExistingEnrollment: n,
  responseAccountUserId: r,
  responseChallengeId: i,
  responseClientId: a,
}) {
  return {
    safe: { hasExistingEnrollment: n },
    sensitive: {
      headerChatGptAccountId: e.headerChatGptAccountId,
      localEnrollmentAccountUserId: t?.accountUserId ?? null,
      localEnrollmentClientId: t?.clientId ?? null,
      responseAccountUserId: r ?? null,
      responseChallengeId: i ?? null,
      responseClientId: a ?? null,
      selectedAccountUserId: e.tokenAccountUserId,
      tokenAccountId: e.tokenAccountId,
      tokenAccountUserId: e.tokenAccountUserId,
      tokenAuthUserId: e.tokenAuthUserId,
      tokenSubject: e.tokenSubject,
    },
  };
}
function r_(e) {
  return {
    safe: { expiresAt: e.expires_at, scopes: e.scopes },
    sensitive: { accountUserId: e.account_user_id, clientId: e.client_id },
  };
}
function i_({ accountUserId: e, stepUpToken: n }) {
  let r = s_(n);
  o_({ payload: r });
  let i = t.Ni.parse(r),
    a = i[`https://api.openai.com/auth`],
    o = a.chatgpt_account_user_id ?? a.account_user_id,
    s = a_(i);
  if (o !== e) throw new Ig();
  if (Math.floor(Date.now() / 1e3) - i.iat > Rg)
    throw Error(`Remote control enrollment step-up token is not fresh.`);
  if (Date.now() - i.pwd_auth_time > Rg * 1e3)
    throw Error(`Remote control enrollment step-up token does not have fresh password auth.`);
  if (s.length !== 1 || s[0] !== Lg)
    throw Error(`Remote control enrollment step-up token is missing required authorization.`);
  return {
    accountUserId: o ?? null,
    issuedAt: i.iat,
    passwordAuthTime: i.pwd_auth_time,
    scopes: s,
  };
}
function a_({ scope: e, scp: t }) {
  let n = new Set();
  for (let t of e?.split(/\s+/) ?? []) t.length > 0 && n.add(t);
  for (let e of t ?? []) e.length > 0 && n.add(e);
  return [...n];
}
function o_({ payload: e }) {
  let t = Vg.safeParse(e);
  if (!t.success) {
    Pg().warning(`remote_control_step_up_token_payload_unexpected`, {
      safe: { payloadType: typeof e },
      sensitive: {},
    });
    return;
  }
  let n = t.data,
    r = Vg.safeParse(n[`https://api.openai.com/auth`]),
    i = r.success ? r.data : null,
    a = Vg.safeParse(n[`https://api.openai.com/mfa`]),
    o = a.success ? a.data : null;
  Pg().info(`remote_control_step_up_token_payload_decoded`, {
    safe: {
      claimKeys: Object.keys(n).sort(),
      hasScope: typeof n.scope == `string`,
      hasScopeArray: Array.isArray(n.scp),
      hasIssuedAt: typeof n.iat == `number`,
      hasPasswordAuthTime: typeof n.pwd_auth_time == `number`,
      authClaimKeys: i == null ? null : Object.keys(i).sort(),
      hasAuthAmr: Array.isArray(i?.amr),
      hasAuthAccountUserId: typeof i?.chatgpt_account_user_id == `string`,
      hasLegacyAuthAccountUserId: typeof i?.account_user_id == `string`,
      mfaClaimKeys: o == null ? null : Object.keys(o).sort(),
      hasMfaRequired: typeof o?.required == `string`,
    },
    sensitive: {},
  });
}
function s_(e) {
  let t = e.split(`.`);
  if (t.length < 2 || !t[1]) throw Error(`Remote control enrollment step-up token is malformed.`);
  return JSON.parse(Buffer.from(t[1], `base64url`).toString(`utf8`));
}
function c_(e) {
  let t = process.env.CODEX_API_BASE_URL;
  return t && t.trim().length > 0
    ? t.replace(/\/+$/, ``)
    : (process.env.CODEX_API_ENDPOINT ?? ``).toLowerCase() === `localhost`
      ? e.devApiBaseUrl
      : e.prodApiBaseUrl;
}
function l_(e, t) {
  let n = new URL(t.replace(/^\/+/, ``), `${c_(e).replace(/\/+$/, ``)}/`);
  return { targetOrigin: n.origin, targetPath: n.pathname };
}
async function u_({ appServerClient: e, body: t, desktopApiOptions: n, headers: r }) {
  return m_({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/enroll/start`,
  });
}
async function d_({ appServerClient: e, body: t, desktopApiOptions: n, headers: r }) {
  return m_({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/enroll/finish`,
  });
}
async function f_({ appServerClient: e, body: t, desktopApiOptions: n, headers: r }) {
  return m_({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    mapNotFoundToFeatureUnavailable: !1,
    path: `/codex/remote/control/client/refresh/start`,
  });
}
async function p_({ appServerClient: e, body: t, desktopApiOptions: n, headers: r }) {
  return m_({
    appServerClient: e,
    body: t,
    desktopApiOptions: n,
    headers: r,
    path: `/codex/remote/control/client/refresh/finish`,
  });
}
async function m_({
  appServerClient: e,
  body: t,
  desktopApiOptions: n,
  headers: r,
  mapNotFoundToFeatureUnavailable: i,
  path: a,
}) {
  return await (
    await Tg({
      action: `connect remote control environments`,
      appServerClient: e,
      desktopApiOptions: n,
      headers: { ...r, "content-type": `application/json` },
      method: `POST`,
      path: a,
      body: JSON.stringify(t),
      mapNotFoundToFeatureUnavailable: i,
    })
  ).json();
}
async function h_({ authIdentity: e, connectionKey: t, deviceKeyClient: n, globalState: r }) {
  let i = (
    await Promise.all(
      g_(e).map(async (e) => {
        let i = Kg(t, e);
        return {
          key: i,
          record: await v_({ deviceKeyClient: n, enrollmentKey: i, globalState: r }),
        };
      }),
    )
  ).find((e) => e.record != null);
  return i?.record == null ? null : { key: i.key, record: i.record };
}
function g_(e) {
  if (e.tokenAccountUserId == null) return [];
  let t = [e.tokenAccountUserId];
  return (
    e.tokenAccountId != null &&
      e.headerChatGptAccountId === e.tokenAccountId &&
      e.tokenAuthUserId != null &&
      e.tokenAuthUserId !== e.tokenAccountUserId &&
      t.push(e.tokenAuthUserId),
    t
  );
}
function __(e, n) {
  let r = e.get(`electron-remote-control-client-enrollments`) ?? {},
    i = t.Mi.safeParse(r[n]);
  return i.success ? i.data : null;
}
async function v_({ deviceKeyClient: e, enrollmentKey: t, globalState: n }) {
  let r = __(n, t);
  if (r == null) return null;
  try {
    let t = await e.getDeviceKeyPublic(r.keyId);
    return t.keyId === r.keyId &&
      t.publicKeySpkiDerBase64 === r.publicKeySpkiDerBase64 &&
      t.algorithm === r.algorithm &&
      t.protectionClass === r.protectionClass
      ? r
      : null;
  } catch {
    return null;
  }
}
function y_(e, t, n) {
  e.update(Gr, (e) => ({ ...e, [t]: n }));
}
function b_(e, t) {
  e.update(Gr, (e) => {
    let n = { ...e };
    return (delete n[t], n);
  });
}
function x_(e) {
  return e instanceof Error
    ? e.message.startsWith(`Remote control request failed (404):`) ||
        e.message === `Remote-control client has been revoked` ||
        e.message ===
          `Remote control request failed (401): Remote-control client enrollment is incomplete` ||
        e.message ===
          `Remote control request failed (403): Remote-control client key material missing`
    : !1;
}
async function S_({ accountUserId: e, clientId: t, deviceKeyClient: n }) {
  let r = await n.createDeviceKey(`allow_os_protected_nonextractable`);
  return {
    accountUserId: e,
    algorithm: r.algorithm,
    clientId: t,
    keyId: r.keyId,
    protectionClass: r.protectionClass,
    publicKeySpkiDerBase64: r.publicKeySpkiDerBase64,
  };
}
async function C_({ deviceKeyClient: e, enrollment: t }) {
  try {
    await e.deleteDeviceKey(t.keyId);
  } catch (e) {
    Pg().warning(`remote_control_client_enrollment_key_cleanup_failed`, {
      safe: {},
      sensitive: { accountUserId: t.accountUserId, clientId: t.clientId, keyId: t.keyId, error: e },
    });
  }
}
function w_(e) {
  return {
    key_id: e.keyId,
    public_key_spki_der_base64: e.publicKeySpkiDerBase64,
    algorithm: e.algorithm,
    protection_class: e.protectionClass,
  };
}
function T_(e) {
  return (0, u.createHash)(`sha256`)
    .update(
      JSON.stringify({
        algorithm: e.algorithm,
        keyId: e.keyId,
        protectionClass: e.protectionClass,
        publicKeySpkiDerBase64: e.publicKeySpkiDerBase64,
      }),
    )
    .digest(`base64url`);
}
async function E_({
  challenge: e,
  deviceKeyClient: t,
  desktopApiOptions: n,
  enrollment: r,
  expectedPath: i,
  requireDeviceIdentityHash: a,
}) {
  let o = l_(n, i);
  if (
    e.purpose !== `remote_control_client_enrollment` ||
    e.audience !== `remote_control_client_enrollment` ||
    e.account_user_id !== r.accountUserId ||
    e.client_id !== r.clientId ||
    e.target_origin !== o.targetOrigin ||
    e.target_path !== o.targetPath
  )
    throw (
      Pg().warning(`remote_control_client_enrollment_challenge_mismatch`, {
        safe: {},
        sensitive: {
          accountUserId: e.account_user_id,
          clientId: e.client_id,
          enrollmentAccountUserId: r.accountUserId,
          enrollmentClientId: r.clientId,
          expectedTargetOrigin: o.targetOrigin,
          expectedTargetPath: o.targetPath,
          targetOrigin: e.target_origin,
          targetPath: e.target_path,
        },
      }),
      Error(`Remote control enrollment challenge does not match local enrollment.`)
    );
  if (a && e.device_identity_hash == null)
    throw Error(`Remote control enrollment challenge is missing device identity hash.`);
  let s = e.device_identity_hash ?? T_(r);
  if (s !== T_(r))
    throw Error(`Remote control enrollment challenge does not match local device identity.`);
  Pg().info(`remote_control_client_enrollment_challenge_signing`, {
    safe: {
      challengeExpiresAt: e.challenge_expires_at,
      purpose: e.purpose,
      requireDeviceIdentityHash: a,
    },
    sensitive: {
      accountUserId: e.account_user_id,
      challengeId: e.challenge_id,
      clientId: e.client_id,
      expectedTargetOrigin: o.targetOrigin,
      expectedTargetPath: o.targetPath,
      keyId: r.keyId,
      targetOrigin: e.target_origin,
      targetPath: e.target_path,
    },
  });
  let c = await t.signDeviceKey(r.keyId, {
    type: `remoteControlClientEnrollment`,
    nonce: e.nonce,
    audience: `remote_control_client_enrollment`,
    challengeId: e.challenge_id,
    targetOrigin: e.target_origin,
    targetPath: e.target_path,
    accountUserId: e.account_user_id,
    clientId: e.client_id,
    deviceIdentitySha256Base64url: s,
    challengeExpiresAt: e.challenge_expires_at,
  });
  return (
    Pg().info(`remote_control_client_enrollment_challenge_signed`, {
      safe: { algorithm: c.algorithm },
      sensitive: {
        accountUserId: e.account_user_id,
        challengeId: e.challenge_id,
        clientId: e.client_id,
        keyId: r.keyId,
      },
    }),
    {
      challenge_token: e.challenge_token,
      key_id: r.keyId,
      signature_der_base64: c.signatureDerBase64,
      signed_payload_base64: c.signedPayloadBase64,
      algorithm: c.algorithm,
    }
  );
}
async function D_({ challenge: e, deviceKeyClient: t, enrollmentKey: n, globalState: r }) {
  let i = await v_({ deviceKeyClient: t, enrollmentKey: Kg(n, e.accountUserId), globalState: r });
  if (
    (Pg().debug(`remote_control_websocket_device_key_challenge`, {
      safe: { audience: e.audience, hasLocalEnrollment: i != null },
      sensitive: {
        accountUserId: e.accountUserId,
        clientId: e.clientId,
        localEnrollmentAccountUserId: i?.accountUserId ?? null,
        localEnrollmentClientId: i?.clientId ?? null,
        sessionId: e.sessionId,
        targetOrigin: e.targetOrigin,
        targetPath: e.targetPath,
      },
    }),
    i == null || i.accountUserId !== e.accountUserId || i.clientId !== e.clientId)
  )
    throw (
      Pg().warning(`remote_control_websocket_device_key_challenge_mismatch`, {
        safe: { hasLocalEnrollment: i != null },
        sensitive: {
          accountUserId: e.accountUserId,
          clientId: e.clientId,
          localEnrollmentAccountUserId: i?.accountUserId ?? null,
          localEnrollmentClientId: i?.clientId ?? null,
          sessionId: e.sessionId,
        },
      }),
      Error(`Remote control device-key challenge has no matching local key.`)
    );
  let a = {
      type: `remoteControlClientConnection`,
      nonce: e.nonce,
      audience: e.audience,
      sessionId: e.sessionId,
      targetOrigin: e.targetOrigin,
      targetPath: e.targetPath,
      accountUserId: e.accountUserId,
      clientId: e.clientId,
      tokenSha256Base64url: e.tokenSha256Base64url,
      tokenExpiresAt: e.tokenExpiresAt,
      scopes: e.scopes,
    },
    o = await t.signDeviceKey(i.keyId, a);
  return (
    Pg().info(`remote_control_websocket_device_key_challenge_signed`, {
      safe: { algorithm: o.algorithm, scopes: e.scopes, tokenExpiresAt: e.tokenExpiresAt },
      sensitive: {
        accountUserId: e.accountUserId,
        clientId: e.clientId,
        keyId: i.keyId,
        sessionId: e.sessionId,
        targetOrigin: e.targetOrigin,
        targetPath: e.targetPath,
      },
    }),
    {
      type: `device_key_proof`,
      keyId: i.keyId,
      signatureDerBase64: o.signatureDerBase64,
      signedPayloadBase64: o.signedPayloadBase64,
      algorithm: o.algorithm,
    }
  );
}
var O_ = `https://chatgpt.com/codex/install.sh`,
  k_ = `https://chatgpt.com/codex/install.ps1`;
function A_(e) {
  let t = e.toLowerCase();
  if (t.startsWith(`cygwin`) || t.startsWith(`mingw`) || t.startsWith(`msys`)) return `windows`;
  switch (t) {
    case `darwin`:
    case `linux`:
      return `posix`;
    default:
      throw Error(`Codex install is not supported on ${e}`);
  }
}
function j_(e) {
  switch (e) {
    case `posix`:
      return `if command -v curl >/dev/null 2>&1; then installer_script="$(curl -fsSL ${O_})" || exit; elif command -v wget >/dev/null 2>&1; then installer_script="$(wget -qO- ${O_})" || exit; else echo 'curl or wget is required to install Codex' >&2; exit 127; fi; printf '%s\\n' "$installer_script" | CODEX_RELEASE=latest CODEX_NON_INTERACTIVE=1 sh`;
    case `windows`:
      return `install_dir="$(cygpath -w "\${CODEX_INSTALL_DIR:-$HOME/.local/bin}")" || exit; CODEX_INSTALL_DIR="$install_dir" powershell -ExecutionPolicy ByPass -c ${t.Nn(`$env:CODEX_RELEASE = "latest"; $env:CODEX_NON_INTERACTIVE = "1"; irm ${k_} | iex`)}`;
  }
}
var M_ = `codex`,
  N_ = 'PATH="${CODEX_INSTALL_DIR:-$HOME/.local/bin}:$PATH"; export PATH';
function P_(e) {
  return `'${e.replace(/'/g, `'\\''`)}'`;
}
function F_(e, t) {
  let n = `${N_}; ${e}`,
    r = t == null ? n : `printf '%b' ${P_(V_(t))}; ${n}`;
  return `sh -c ${P_([`if [ -z "$SHELL" ] || [ ! -x "$SHELL" ]; then echo "Codex remote SSH requires SHELL to point to an executable login shell" >&2; exit 127; fi;`, `CODEX_REMOTE_PAYLOAD="$1"; export CODEX_REMOTE_PAYLOAD;`, 'case "${SHELL##*/}" in', `csh|tcsh) exec "$SHELL" -i -c ${P_([`set loginsh=1`, `if ( -r /etc/csh.login ) source /etc/csh.login`, `if ( -r ~/.login ) source ~/.login`, `exec /bin/sh -c "$CODEX_REMOTE_PAYLOAD"`].join(`; `))} ;;`, `nu) exec "$SHELL" -l -i -c ${P_(`exec /bin/sh -c $env.CODEX_REMOTE_PAYLOAD`)} ;;`, `*) exec "$SHELL" -l -i -c ${P_(`exec /bin/sh -c "$CODEX_REMOTE_PAYLOAD"`)} ;;`, `esac`].join(` `))} sh ${P_(r)}`;
}
function I_() {
  return [
    `sh`,
    `-c`,
    `login_shell="\${SHELL:-}"
if [ -z "$login_shell" ] && command -v getent >/dev/null 2>&1; then
  login_shell="$(getent passwd "$(id -un)" | cut -d: -f7 || true)"
fi
if [ -z "$login_shell" ] || [ ! -x "$login_shell" ]; then
  login_shell="/bin/bash"
fi
exec "$login_shell" -il`,
  ];
}
function L_() {
  let e = process.env.CODEX_CLI_PATH;
  if (e == null) return null;
  let t = e.trim();
  return t.length === 0 ? null : t;
}
function R_(e) {
  return /[\\/]/.test(e) || /^[a-zA-Z]:/.test(e);
}
function z_() {
  let e = L_();
  return e == null || R_(e) ? null : e;
}
function B_() {
  return z_() ?? M_;
}
function V_(e) {
  return Array.from(e, (e) => `\\${e.toString(8).padStart(3, `0`)}`).join(``);
}
var H_ = `ssh_websocket_v0`,
  U_ = new Set([
    `INIT_CWD`,
    `npm_command`,
    `npm_execpath`,
    `npm_node_execpath`,
    `PNPM_PACKAGE_NAME`,
    `PNPM_SCRIPT_SRC_DIR`,
  ]),
  W_ = [`npm_config_`, `npm_lifecycle_`, `npm_package_`];
function G_(e) {
  for (let t of Object.keys(e)) {
    let n = t.toLowerCase();
    (U_.has(t) || W_.some((e) => n.startsWith(e))) && delete e[t];
  }
}
var K_ = 5e3,
  q_ = 1500,
  J_ = null,
  Y_ = null,
  X_ = null;
async function Z_() {
  let e = await Q_({ caller: `startup`, retryFailed: !1 });
  return (e.status !== `loaded` && (await ev()), e);
}
async function Q_({ caller: e = `startup`, retryFailed: t = !0, timeoutMs: n = K_ } = {}) {
  if (X_?.status === `loaded` || (X_ != null && !t)) return rv(X_, e);
  if (J_ != null && Y_ != null && Y_ >= n) return J_.then((t) => ({ ...t, caller: e }));
  let r = $_(e, n);
  ((J_ = r), (Y_ = n));
  try {
    let e = await r;
    return (iv(e), e);
  } finally {
    J_ === r && ((J_ = null), (Y_ = null));
  }
}
async function $_(e, n) {
  let r = Date.now();
  a.app.isPackaged || G_(process.env);
  let i = new AbortController(),
    o = null,
    s = await Promise.race([
      t
        .In({ interactive: !0, extraEnv: { [t.Ln]: `1` }, signal: i.signal })
        .then((e) => ({ status: `loaded`, userEnv: e }))
        .catch((e) => ({ status: `failed`, error: e })),
      new Promise((e) => {
        ((o = setTimeout(() => {
          (i.abort(), e({ status: `timed_out` }));
        }, n)),
          o.unref());
      }),
    ]).finally(() => {
      o != null && clearTimeout(o);
    });
  if (s.status === `loaded`)
    return (
      Object.assign(process.env, s.userEnv),
      a.app.isPackaged || G_(process.env),
      { status: `loaded`, path: process.env.PATH ?? null, ...tv(r, e, n) }
    );
  let c =
    s.status === `timed_out`
      ? `Timed out after ${n}ms.`
      : s.error instanceof Error
        ? s.error.message
        : String(s.error);
  return (
    t
      .Ur()
      .warning(`Failed to load shell env`, {
        safe: {
          caller: e,
          durationMs: Date.now() - r,
          pathEntryCount: nv(process.env.PATH ?? null),
          resultSource: `load`,
          status: s.status,
          timeoutMs: n,
        },
        sensitive: { detail: c, path: process.env.PATH ?? null },
      }),
    { status: s.status, detail: c, path: process.env.PATH ?? null, ...tv(r, e, n) }
  );
}
function ev() {
  return new Promise((e) => {
    setTimeout(e, q_).unref();
  });
}
function tv(e, t, n) {
  let r = process.env.PATH ?? null;
  return {
    caller: t,
    durationMs: Date.now() - e,
    pathEntryCount: nv(r),
    resultSource: `load`,
    timeoutMs: n,
  };
}
function nv(e) {
  return e == null || e === `` ? 0 : e.split(s.delimiter).length;
}
function rv(e, t) {
  return { ...e, caller: t, resultSource: `cache` };
}
function iv(e) {
  (X_?.status === `loaded` && e.status !== `loaded`) || (X_ = e);
}
var av = {
    localSshConfigProbe: 5e3,
    proxyWebSocketOpen: 3e4,
    remoteBootstrapCommand: 3e4,
    remoteCodexInstall: 6e5,
    shellEnvLoad: 1e4,
  },
  ov = `ws://codex-app-server/rpc`,
  sv = '"${CODEX_HOME:-$HOME/.codex}/app-server-control"',
  cv = '"${CODEX_HOME:-$HOME/.codex}/app-server-control/app-server.log"',
  lv = { batchMode: `BatchMode=yes`, serverAliveIntervalSeconds: 15, serverAliveCountMax: 12 },
  uv = 86,
  dv = 8,
  fv = new Map();
function pv(e) {
  let t = [`-v`, `-o`, lv.batchMode];
  return (
    e != null && t.push(`-o`, `ConnectTimeout=${e}`),
    t.push(
      `-o`,
      `ServerAliveInterval=${lv.serverAliveIntervalSeconds}`,
      `-o`,
      `ServerAliveCountMax=${lv.serverAliveCountMax}`,
    ),
    t
  );
}
function mv(e) {
  return e
    .split(/\r?\n/u)
    .map((e) => e.trim())
    .filter((e) => e !== `` && !/^debug\d+:/u.test(e))
    .join(`
`)
    .trim();
}
function hv(e) {
  if (e.alias?.trim()) return [e.alias.trim()];
  let t = [];
  return (
    e.identity?.trim() && t.push(`-i`, e.identity.trim()),
    e.port != null && t.push(`-p`, String(e.port)),
    t.push(e.host),
    t
  );
}
function gv(e) {
  let t = e[H_];
  return t?.sshHost ? t : null;
}
var _v = class {
  kind = `websocket`;
  logger = t.Wr(`AppServerTransportSshWebsocket`);
  proxyStreams = new Set();
  installedCodexVersion;
  constructor(e) {
    this.options = e;
  }
  supportsReconnect() {
    return !0;
  }
  dispose() {
    for (let e of this.proxyStreams) e.destroy();
    this.proxyStreams.clear();
  }
  async connect() {
    this.installedCodexVersion = void 0;
    let e = this.startShellEnvLoadForSsh();
    return (
      e.promise
        .then((e) => {
          this.logShellEnvLoadForSsh({ result: e, sshAttempted: !0, sshPhase: `connect` });
        })
        .catch((t) => {
          this.logger.warning(`ssh_websocket_v0.shell_env_load_unexpected_error`, {
            safe: { ...Sv(e.getState()), sshAttempted: !0, sshPhase: `connect` },
            sensitive: {
              error: t,
              path: process.env.PATH ?? null,
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
            },
          });
        }),
      await this.waitForShellEnvIfProxyCommandNeedsPath(e, `connect`),
      await this.ensureRemoteAppServer({ phase: `connect`, shellEnv: e.getState() }),
      this.runWithSshStartupGate(async () => {
        let n = { current: null },
          r = new t.dn(ov, {
            perMessageDeflate: !1,
            createConnection: () => (
              (n.current = this.createSshProxyStream({ phase: `connect`, shellEnv: e.getState() })),
              n.current
            ),
          });
        r.once(`close`, () => {
          n.current?.destroy();
        });
        try {
          await vv(r);
        } catch (e) {
          throw (n.current?.destroy(), r.terminate(), e);
        }
        return (
          t.un(r, {
            onPongTimeout: () => {
              r.terminate();
            },
          }),
          new t.fn(r)
        );
      })
    );
  }
  startShellEnvLoadForSsh() {
    let e = {
      caller: `ssh_connect`,
      pathEntryCount: Cv(process.env.PATH ?? null),
      resultSource: `pending`,
      status: `loading`,
    };
    return {
      getState: () => e,
      promise: Q_({ caller: `ssh_connect`, timeoutMs: av.shellEnvLoad }).then(
        (t) => ((e = xv(t)), t),
      ),
    };
  }
  async waitForShellEnvIfProxyCommandNeedsPath(e, t) {
    if ((await Promise.resolve(), e.getState().status !== `loading`)) return;
    let n = await this.getSshProxyCommandShellEnvDependency();
    if (
      (this.logger.info(`ssh_websocket_v0.shell_env_wait_decision`, {
        safe: {
          needsShellEnv: n.needsShellEnv,
          pathEntryCount: n.pathEntryCount,
          reason: n.reason,
          sshPhase: t,
          timeoutMs: av.localSshConfigProbe,
        },
        sensitive: {
          commandName: n.commandName,
          path: process.env.PATH ?? null,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      }),
      !n.needsShellEnv)
    )
      return;
    let r = Date.now();
    try {
      let i = await e.promise;
      this.logger.info(`ssh_websocket_v0.shell_env_wait_completed`, {
        safe: {
          durationMs: Date.now() - r,
          pathEntryCount: i.pathEntryCount,
          reason: n.reason,
          resultSource: i.resultSource,
          sshPhase: t,
          status: i.status,
          timeoutMs: i.timeoutMs,
        },
        sensitive: {
          detail: i.status === `loaded` ? null : i.detail,
          path: i.path,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    } catch (e) {
      this.logger.warning(`ssh_websocket_v0.shell_env_wait_unexpected_error`, {
        safe: { durationMs: Date.now() - r, reason: n.reason, sshPhase: t },
        sensitive: {
          error: e,
          proxyCommand: n.proxyCommand,
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    }
  }
  async getSshProxyCommandShellEnvDependency() {
    let e = Date.now();
    try {
      let n = await Lv({
        process: t.An({
          args: [`ssh`, `-G`, ...hv(this.options.sshConnection)],
          spawnInsideWsl: !1,
        }),
        timeoutMs: av.localSshConfigProbe,
        timeoutMessage: `SSH: local ssh config probe timed out`,
      });
      return n.code === 0
        ? wv(n.stdout, process.env.PATH ?? null)
        : (this.logger.warning(`ssh_websocket_v0.ssh_config_probe_failed`, {
            safe: { code: n.code, durationMs: Date.now() - e, timeoutMs: av.localSshConfigProbe },
            sensitive: {
              sshAlias: this.options.sshConnection.alias,
              sshHost: this.options.sshConnection.host,
              sshPort: this.options.sshConnection.port,
              stderr: n.stderr,
              stdout: n.stdout,
            },
          }),
          Tv());
    } catch (t) {
      return (
        this.logger.warning(`ssh_websocket_v0.ssh_config_probe_failed`, {
          safe: { durationMs: Date.now() - e, timeoutMs: av.localSshConfigProbe },
          sensitive: {
            error: t,
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
          },
        }),
        Tv()
      );
    }
  }
  logShellEnvLoadForSsh({ result: e, sshAttempted: t, sshPhase: n }) {
    this.logger.info(`ssh_websocket_v0.shell_env_load_completed`, {
      safe: {
        caller: e.caller,
        durationMs: e.durationMs,
        pathEntryCount: e.pathEntryCount,
        resultSource: e.resultSource,
        sshAttempted: t,
        sshPhase: n,
        status: e.status,
        timeoutMs: e.timeoutMs,
      },
      sensitive: {
        detail: e.status === `loaded` ? null : e.detail,
        path: e.path,
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
  }
  async ensureRemoteAppServer(e) {
    (this.logger.info(`ssh_websocket_v0.ensure_remote_app_server`, {
      safe: { ...Sv(e.shellEnv), sshAttempted: !0, sshPhase: e.phase },
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    }),
      await this.startRemoteAppServer(e));
  }
  async startRemoteAppServer(e) {
    let n = B_(),
      r;
    try {
      r = await this.runRemoteLoginShellCommand({
        command: [
          `if command -v `,
          t.Nn(n),
          ` >/dev/null 2>&1; then exit 0; fi; exit `,
          String(uv),
        ].join(``),
        context: e,
        operation: `codex_path_probe`,
        timeoutMessage: `SSH: codex path probe timed out`,
      });
    } catch (e) {
      throw this.createSshSetupError(`remote_codex_lookup`, e);
    }
    if (r.code === uv)
      throw new t.kn({
        failureReason: `remote-codex-not-found`,
        message: "No `codex` found in PATH. Please install Codex on the remote machine.",
        stage: `remote_codex_lookup`,
      });
    if (r.code !== 0)
      throw this.createSshSetupError(
        `remote_codex_lookup`,
        Error(await this.getSshCommandFailureMessage(r)),
      );
    let i = await this.getInstalledCodexVersion(e);
    if (i != null && !t.Ss(i)) throw Error(t.bs + i);
    let a = [
        `umask 077; mkdir -p -- `,
        sv,
        ` && (pkill -9 -U "$(id -u)" -f `,
        t.Nn(`${n}.*[d]esktop-ssh-websocket-v0.sock`),
        ` || true) && nohup `,
        t.Nn(n),
        ` app-server --listen `,
        t.Nn(`unix://`),
        ` >${cv} 2>&1 &`,
      ].join(``),
      o;
    try {
      o = await this.runRemoteLoginShellCommand({
        command: a,
        context: e,
        operation: `app_server_bootstrap`,
        timeoutMessage: `SSH: app-server bootstrap timed out`,
      });
    } catch (e) {
      throw this.createSshSetupError(`remote_app_server_start`, e);
    }
    let { code: s, stdout: c, stderr: l } = o;
    if (s !== 0) {
      this.logger.warning(`ssh_websocket_v0.remote_app_server_bootstrap_failed`, {
        safe: { code: s },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
          identity: this.options.sshConnection.identity,
          remoteLogPath: cv,
          stdout: c,
          stderr: l,
        },
      });
      let e = await this.getSshCommandFailureMessage(o);
      throw this.createSshSetupError(`remote_app_server_start`, Error(e));
    }
  }
  createSshSetupError(e, n) {
    return n instanceof t.kn
      ? n
      : new t.kn({
          message:
            n instanceof Error ? n.message : `SSH-backed app-server setup failed: ${String(n)}`,
          stage: e,
        });
  }
  async getSshCommandFailureMessage(e) {
    return (
      mv(
        [e.stderr, e.stdout].join(`
`),
      ) ||
      (e.code == null
        ? `Remote SSH failed without an exit code or stderr`
        : `Remote SSH failed with exit code ${e.code} and did not return stderr`)
    );
  }
  async getInstalledCodexVersion(e) {
    if (this.installedCodexVersion !== void 0) return this.installedCodexVersion;
    let {
      code: n,
      stdout: r,
      stderr: i,
    } = await this.runRemoteLoginShellCommand({
      command: `${t.Nn(B_())} --version`,
      context: e,
      operation: `codex_version_probe`,
      timeoutMessage: `SSH: codex --version check timed out`,
    });
    return n === 0
      ? ((this.installedCodexVersion = t.xs(r || i)), this.installedCodexVersion)
      : (this.logger.warning(`ssh_websocket_v0.remote_codex_version_probe_failed`, {
          safe: { code: n },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
            stdout: r,
            stderr: i,
          },
        }),
        (this.installedCodexVersion = null),
        null);
  }
  async installCodex() {
    let e = Date.now(),
      t = `platform_probe`,
      n = e,
      r = null;
    this.logger.info(`ssh_websocket_v0.remote_codex_install_started`, {
      safe: {},
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
    try {
      ((r = await this.getRemoteCodexInstallPlatform()),
        this.logger.info(`ssh_websocket_v0.remote_codex_install_platform_ready`, {
          safe: { durationMs: Date.now() - n, platform: r },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
          },
        }),
        (t = `remote_install_command`),
        (n = Date.now()),
        this.logger.info(`ssh_websocket_v0.remote_codex_install_remote_command_started`, {
          safe: { platform: r, timeoutMs: av.remoteCodexInstall },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
          },
        }));
      let i = await this.runRemoteLoginShellCommand({
        command: j_(r),
        operation: `remote_codex_install`,
        timeoutMessage: `SSH: codex install timed out`,
        timeoutMs: av.remoteCodexInstall,
      });
      if (i.code !== 0) throw Error(await this.getSshCommandFailureMessage(i));
      this.logger.info(`ssh_websocket_v0.remote_codex_install_completed`, {
        safe: { durationMs: Date.now() - e, platform: r, remoteCommandDurationMs: Date.now() - n },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
        },
      });
    } catch (i) {
      throw (
        this.logRemoteCodexInstallFailed({
          error: i,
          platform: r,
          stage: t,
          stageStartedAtMs: n,
          startedAtMs: e,
        }),
        i
      );
    }
  }
  logRemoteCodexInstallFailed({
    error: e,
    platform: t,
    stage: n,
    stageStartedAtMs: r,
    startedAtMs: i,
  }) {
    let a = Date.now();
    this.logger.warning(`ssh_websocket_v0.remote_codex_install_failed`, {
      safe: { durationMs: a - i, platform: t, stage: n, stageDurationMs: a - r },
      sensitive: {
        error: e,
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
  }
  async getRemoteCodexInstallPlatform() {
    let e = await this.runRemoteLoginShellCommand({
      command: `uname -s`,
      operation: `remote_codex_platform_probe`,
      timeoutMessage: `SSH: remote platform probe timed out`,
    });
    if (e.code !== 0) throw Error(await this.getSshCommandFailureMessage(e));
    let t = e.stdout.trim();
    if (!t) throw Error(`Remote platform probe did not return an OS`);
    return A_(t);
  }
  async killCodexProcess() {
    let {
      code: e,
      stdout: n,
      stderr: r,
    } = await this.runRemoteLoginShellCommand({
      command: `pkill -9 -U "$(id -u)" -f ${t.Nn(`${B_()}.* app-server.* --listen`)}`,
      operation: `remote_codex_kill`,
      timeoutMessage: `SSH: codex pkill timed out`,
    });
    if (e === 0 || e === 1) {
      this.logger.info(`ssh_websocket_v0.remote_codex_kill_completed`, {
        safe: { matchedProcess: e === 0 },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
          stdout: n,
          stderr: r,
        },
      });
      return;
    }
    throw Error(r || n || `pkill failed with exit code ${e}`);
  }
  async runRemoteLoginShellCommand({
    command: e,
    context: t,
    operation: n,
    timeoutMessage: r,
    timeoutMs: i = av.remoteBootstrapCommand,
  }) {
    return this.runWithSshStartupGate(async () =>
      this.runRemoteLoginShellCommandWithoutGate({
        command: e,
        context: t,
        operation: n,
        timeoutMessage: r,
        timeoutMs: i,
      }),
    );
  }
  async runRemoteLoginShellCommandWithoutGate({
    command: e,
    context: n,
    operation: r,
    timeoutMessage: i,
    timeoutMs: a = av.remoteBootstrapCommand,
  }) {
    let o = Date.now(),
      s = (0, u.randomBytes)(dv),
      c = t.An({
        args: [
          `ssh`,
          ...pv(this.options.getConnectTimeoutSeconds?.()),
          ...hv(this.options.sshConnection),
          F_(e, s),
        ],
        spawnInsideWsl: !1,
      }),
      l = await Lv({ process: c, timeoutMs: a, timeoutMessage: i }),
      d = Iv(c.getStdout(), s);
    return (
      this.logger.info(`ssh_websocket_v0.remote_login_shell_command_completed`, {
        safe: {
          code: l.code,
          durationMs: Date.now() - o,
          operation: r,
          shellEnvCaller: n?.shellEnv.caller ?? `unknown`,
          shellEnvPathEntryCount: n?.shellEnv.pathEntryCount ?? Cv(process.env.PATH ?? null),
          shellEnvResultSource: n?.shellEnv.resultSource ?? `unknown`,
          shellEnvStatus: n?.shellEnv.status ?? `unknown`,
          sshCommandKind: `remote_login_shell`,
          sshPhase: n?.phase ?? `standalone`,
          timeoutMs: a,
        },
        sensitive: {
          sshAlias: this.options.sshConnection.alias,
          sshHost: this.options.sshConnection.host,
          sshPort: this.options.sshConnection.port,
          stdout: d,
          stderr: l.stderr,
        },
      }),
      { ...l, stdout: d }
    );
  }
  createSshProxyStream(e) {
    let n = `${t.Nn(B_())} app-server proxy`,
      r = (0, u.randomBytes)(dv);
    this.logger.info(`ssh_websocket_v0.proxy_command_starting`, {
      safe: {
        operation: `app_server_proxy`,
        ...Sv(e.shellEnv),
        sshCommandKind: `app_server_proxy`,
        sshPhase: e.phase,
      },
      sensitive: {
        sshAlias: this.options.sshConnection.alias,
        sshHost: this.options.sshConnection.host,
        sshPort: this.options.sshConnection.port,
      },
    });
    let i = (0, m.spawn)(
        `ssh`,
        [
          `-T`,
          ...pv(this.options.getConnectTimeoutSeconds?.()),
          ...hv(this.options.sshConnection),
          F_(n, r),
        ],
        { env: t.Vr(process.env), stdio: [`pipe`, `pipe`, `pipe`] },
      ),
      { stdin: a, stdout: o, stderr: s } = i;
    if (a == null || o == null || s == null)
      throw (i.kill(), Error(`ssh proxy stdio was unavailable`));
    let c = ``;
    s.on(`data`, (e) => {
      c = `${c}${e.toString(`utf8`)}`.slice(-4e3);
    });
    let l = new b.Duplex({
      read() {
        o.resume();
      },
      write(e, t, n) {
        a.write(e, t, n);
      },
      final(e) {
        (a.end(), e());
      },
      destroy(e, t) {
        (i.kill(), t(e));
      },
    });
    Object.assign(l, { setKeepAlive: () => l, setNoDelay: () => l, setTimeout: () => l });
    let d = (e) => {
      l.destroy(e);
    };
    (a.on(`error`, d),
      this.proxyStreams.add(l),
      l.once(`close`, () => {
        this.proxyStreams.delete(l);
      }));
    let f = !1,
      p = Buffer.alloc(0);
    return (
      o.on(`data`, (e) => {
        let t = e;
        if (!f) {
          let n = Buffer.concat([p, e]),
            i = n.indexOf(r);
          if (i === -1) {
            p = n.subarray(Math.max(0, n.length - (r.length - 1)));
            return;
          }
          ((f = !0), (t = n.subarray(i + r.length)), (p = Buffer.alloc(0)));
        }
        t.length !== 0 && (l.push(t) || o.pause());
      }),
      o.on(`end`, () => {
        l.push(null);
      }),
      i.on(`error`, (e) => {
        l.destroy(e);
      }),
      i.on(`close`, (t, n) => {
        if ((a.removeListener(`error`, d), t === 0)) {
          l.push(null);
          return;
        }
        let r = mv(c);
        (this.logger.warning(`ssh_websocket_v0.proxy_command_failed`, {
          safe: {
            code: t,
            operation: `app_server_proxy`,
            ...Sv(e.shellEnv),
            signal: n,
            sshCommandKind: `app_server_proxy`,
            sshPhase: e.phase,
          },
          sensitive: {
            sshAlias: this.options.sshConnection.alias,
            sshHost: this.options.sshConnection.host,
            sshPort: this.options.sshConnection.port,
            stderr: c,
          },
        }),
          l.destroy(Error(`ssh app-server proxy exited with code ${t}, signal ${n}: ${r}`)));
      }),
      queueMicrotask(() => {
        l.emit(`connect`);
      }),
      l
    );
  }
  async runWithSshStartupGate(e) {
    let t = bv(this.options.sshConnection),
      n = fv.get(t),
      r = () => {},
      i = new Promise((e) => {
        r = e;
      }),
      a = (n ?? Promise.resolve()).catch(() => void 0).then(() => i);
    (fv.set(t, a), await n?.catch(() => void 0));
    try {
      return await e();
    } finally {
      (r(), fv.get(t) === a && fv.delete(t));
    }
  }
};
function vv(e) {
  return e.readyState === t.dn.OPEN
    ? Promise.resolve()
    : new Promise((t, n) => {
        let r = null,
          i = () => {
            (r != null && clearTimeout(r), e.off(`open`, a), e.off(`error`, o), e.off(`close`, s));
          },
          a = () => {
            (i(), t());
          },
          o = (e) => {
            (i(), n(e));
          },
          s = () => {
            (i(), n(Error(`SSH websocket closed before opening`)));
          };
        (e.once(`open`, a),
          e.once(`error`, o),
          e.once(`close`, s),
          (r = setTimeout(() => {
            (i(), n(Error(`SSH websocket open timed out`)));
          }, av.proxyWebSocketOpen)),
          r.unref());
      });
}
function yv(e) {
  if (process.env.CODEX_APP_SERVER_FORCE_CLI === `1` || e.kind !== `ssh`) return null;
  let t = gv(e);
  return t
    ? {
        sshConnection: {
          alias: t.sshAlias,
          host: t.sshHost,
          port: t.sshPort,
          identity: t.identity,
        },
      }
    : null;
}
function bv(e) {
  let t = e.alias?.trim();
  return t
    ? `alias:${t}`
    : [`direct`, e.host, String(e.port ?? ``), e.identity?.trim() ?? ``].join(`
`);
}
function xv(e) {
  return {
    caller: e.caller,
    durationMs: e.durationMs,
    pathEntryCount: e.pathEntryCount,
    resultSource: e.resultSource,
    status: e.status,
    timeoutMs: e.timeoutMs,
  };
}
function Sv(e) {
  return {
    shellEnvCaller: e.caller,
    shellEnvPathEntryCount: e.pathEntryCount,
    shellEnvResultSource: e.resultSource,
    shellEnvStatus: e.status,
  };
}
function Cv(e) {
  return e == null || e === `` ? 0 : e.split(s.delimiter).length;
}
function wv(e, t) {
  let n = Ev(e),
    r = Cv(t);
  if (n == null)
    return {
      commandName: null,
      needsShellEnv: !1,
      pathEntryCount: r,
      proxyCommand: null,
      reason: `no_proxy_command`,
    };
  if (n.toLowerCase() === `none`)
    return {
      commandName: null,
      needsShellEnv: !1,
      pathEntryCount: r,
      proxyCommand: n,
      reason: `proxy_command_none`,
    };
  let i = Dv(n);
  return i == null
    ? {
        commandName: null,
        needsShellEnv: !1,
        pathEntryCount: r,
        proxyCommand: n,
        reason: `proxy_command_unparsed`,
      }
    : Pv(i)
      ? {
          commandName: i,
          needsShellEnv: !1,
          pathEntryCount: r,
          proxyCommand: n,
          reason: `proxy_command_has_path`,
        }
      : Fv(i, t)
        ? {
            commandName: i,
            needsShellEnv: !1,
            pathEntryCount: r,
            proxyCommand: n,
            reason: `proxy_command_resolved_in_path`,
          }
        : {
            commandName: i,
            needsShellEnv: !0,
            pathEntryCount: r,
            proxyCommand: n,
            reason: `proxy_command_unresolved_in_path`,
          };
}
function Tv() {
  return {
    commandName: null,
    needsShellEnv: !1,
    pathEntryCount: Cv(process.env.PATH ?? null),
    proxyCommand: null,
    reason: `ssh_config_probe_failed`,
  };
}
function Ev(e) {
  let t = null;
  for (let n of e.split(/\r?\n/u)) {
    let e = /^proxycommand\s+(?<command>.*)$/iu.exec(n.trim());
    e?.groups?.command != null && (t = e.groups.command.trim());
  }
  return t;
}
function Dv(e) {
  return Ov(e, 0);
}
function Ov(e, t) {
  let n = jv(e);
  if (n == null) return null;
  for (let e = 0; e < n.length; e += 1) {
    let r = n[e];
    if (!Nv(r) && !(r === `exec` || r === `command`) && r !== `env`) {
      if (t < 2 && kv(r)) {
        let r = Av(n, e + 1);
        if (r != null) return Ov(r, t + 1);
      }
      return r;
    }
  }
  return null;
}
function kv(e) {
  let t = e.replace(/\\/gu, `/`).toLowerCase(),
    n = t.slice(t.lastIndexOf(`/`) + 1);
  return n === `sh` || n === `bash` || n === `zsh` || n === `dash` || n === `ksh`;
}
function Av(e, t) {
  for (let n = t; n < e.length; n += 1) {
    let t = e[n];
    if (t === `-c` || /^-[^-]*c/u.test(t)) return e[n + 1] ?? null;
    if (!t.startsWith(`-`) || t === `--`) return null;
  }
  return null;
}
function jv(e) {
  if (Mv(e)) return null;
  try {
    return t.Pn(e);
  } catch {
    return null;
  }
}
function Mv(e) {
  let t = null,
    n = !1;
  for (let r = 0; r < e.length; r += 1) {
    let i = e[r];
    if (n) {
      n = !1;
      continue;
    }
    if (i === `\\` && t !== `'`) {
      n = !0;
      continue;
    }
    if (t != null) {
      i === t && (t = null);
      continue;
    }
    if (i === `'` || i === `"`) {
      t = i;
      continue;
    }
    if (/[;&|<>]/u.test(i)) return !0;
  }
  return !1;
}
function Nv(e) {
  return /^[A-Za-z_][A-Za-z0-9_]*=/u.test(e);
}
function Pv(e) {
  return e.includes(`/`) || e.includes(`\\`) || /^[A-Za-z]:/u.test(e);
}
function Fv(e, t) {
  return t == null || t === ``
    ? !1
    : bo.default.sync(e, { nothrow: !0, path: t, pathExt: process.env.PATHEXT }) != null;
}
function Iv(e, t) {
  let n = e.indexOf(t);
  return n === -1 ? e.toString(`utf8`) : e.subarray(n + t.length).toString(`utf8`);
}
async function Lv({ process: e, timeoutMs: t, timeoutMessage: n }) {
  let r = null;
  try {
    return await Promise.race([
      e.wait(),
      new Promise((i, a) => {
        r = setTimeout(() => {
          (e.kill(), a(Error(`${n} after ${t}ms`)));
        }, t);
      }),
    ]);
  } finally {
    r != null && clearTimeout(r);
  }
}
var Rv = `appHeader.installUpdate.confirmTitle`,
  zv = `Update Codex now?`,
  Bv = `appHeader.installUpdate.confirmSubtitle`,
  Vv = `Codex will quit to install the update, which will interrupt active local sessions on this machine`,
  Hv = `appHeader.installUpdate.confirmCancel`,
  Uv = `Cancel`,
  Wv = `appHeader.installUpdate.confirmInstall`,
  Gv = `Update`,
  Kv = !1;
function qv() {
  return Kv;
}
function Jv(e) {
  Kv = e;
}
function Yv({ getPrimaryWindow: e, origin: t, sparkleManager: n }) {
  return {
    checkForUpdates: () => {
      n.checkForUpdates();
    },
    installUpdate: async () => {
      if (process.platform === `darwin` && qv()) {
        let n = a.BrowserWindow.fromWebContents(t) ?? e(),
          i = r.V(),
          o = i.formatMessage({ messageId: Rv, defaultMessage: zv }),
          s = {
            type: `warning`,
            buttons: [
              i.formatMessage({ messageId: Wv, defaultMessage: Gv }),
              i.formatMessage({ messageId: Hv, defaultMessage: Uv }),
            ],
            defaultId: 0,
            cancelId: 1,
            noLink: !0,
            title: o,
            message: o,
            detail: i.formatMessage({ messageId: Bv, defaultMessage: Vv }),
          };
        if (
          (n == null ? await a.dialog.showMessageBox(s) : await a.dialog.showMessageBox(n, s))
            .response !== 0
        )
          return;
      }
      n.installUpdatesIfAvailable();
    },
  };
}
var Xv = t.Gr(`codex-app-config/apply`);
function Zv({ config: e, globalState: n }) {
  let r = n.get(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS) ?? [],
    i = new Set(
      r.flatMap((e) => {
        let t = e.alias?.trim();
        return t ? [t] : [];
      }),
    ),
    a = [];
  for (let n of e.remoteConnections)
    i.has(n.sshAlias) ||
      (i.add(n.sshAlias),
      a.push({
        hostId: t.pa(n.sshAlias),
        displayName: n.sshAlias,
        source: `discovered`,
        alias: n.sshAlias,
        hostname: null,
        sshPort: null,
        identity: null,
      }));
  return a.length === 0 ? !1 : (n.set(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS, [...r, ...a]), !0);
}
function Qv({
  config: e,
  createRemoteProjectId: n = u.randomUUID,
  globalState: r,
  remoteConnections: i,
}) {
  let a = ny(i),
    o = new Set(),
    s = [];
  for (let t of e.remoteConnections) {
    let e = a.get(t.sshAlias);
    if (e == null) {
      o.add(t.sshAlias);
      continue;
    }
    s.push({ connectionConfig: t, remoteConnection: e });
  }
  let c = new Set();
  ($v({ config: e, globalState: r }) && c.add(t.Fo.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS),
    ey({ globalState: r, remoteConnectionConfigMatches: s }) &&
      c.add(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID));
  let l = ty({ createRemoteProjectId: n, globalState: r, remoteConnectionConfigMatches: s });
  return (
    l.remoteProjectsChanged && c.add(t.Fo.REMOTE_PROJECTS),
    l.projectOrderChanged && c.add(t.Fo.PROJECT_ORDER),
    o.size > 0 &&
      Xv().warning(`skipped_missing_ssh_aliases`, {
        safe: { missingSshAliasCount: o.size },
        sensitive: { sshAliases: Array.from(o) },
      }),
    {
      changedGlobalStateKeys: Array.from(c),
      remoteProjectsChanged: l.remoteProjectsChanged,
      missingSshAliases: Array.from(o),
      remoteConnectionHostIdsToConnect: Array.from(
        new Set(s.map(({ remoteConnection: e }) => e.hostId)),
      ),
    }
  );
}
function $v({ config: e, globalState: n }) {
  let r = t.Fo.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS,
    i = e.remoteConnectionMaxRetryAttempts;
  return (n.get(r) ?? void 0) === i ? !1 : (n.set(r, i), !0);
}
function ey({ globalState: e, remoteConnectionConfigMatches: n }) {
  let r = e.get(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID) ?? {},
    i = r,
    a = !1;
  for (let { remoteConnection: e } of n)
    r[e.hostId] !== !0 && ((a ||= ((i = { ...r }), !0)), (i[e.hostId] = !0));
  return a ? (e.set(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, i), !0) : !1;
}
function ty({ createRemoteProjectId: e, globalState: n, remoteConnectionConfigMatches: r }) {
  let i = n.get(t.Fo.REMOTE_PROJECTS) ?? [],
    a = new Map();
  for (let { connectionConfig: n, remoteConnection: o } of r)
    for (let r of n.projects) {
      let n = t.ei(r.remotePath),
        s = `${o.hostId}:${t.$r(n)}`,
        c = a.get(s) ?? ry(i, o.hostId, n);
      a.set(s, {
        id: c?.id ?? e(),
        hostId: o.hostId,
        remotePath: n,
        label: r.label?.trim() || c?.label || t.Qr(n),
      });
    }
  if (a.size === 0) return { remoteProjectsChanged: !1, projectOrderChanged: !1 };
  let o = Array.from(a.values()),
    s = n.get(t.Fo.PROJECT_ORDER) ?? [],
    c = o.map((e) => e.id),
    l = [...o, ...i.filter((e) => !a.has(`${e.hostId}:${t.$r(e.remotePath)}`))],
    u = [...c, ...s.filter((e) => !c.includes(e))],
    d = !iy(i, l),
    f = !ay(s, u);
  return (
    d && n.set(t.Fo.REMOTE_PROJECTS, l),
    f && n.set(t.Fo.PROJECT_ORDER, u),
    { remoteProjectsChanged: d, projectOrderChanged: f }
  );
}
function ny(e) {
  let t = new Map();
  for (let n of e) {
    let e = n.sshAlias?.trim();
    !e || t.has(e) || t.set(e, n);
  }
  return t;
}
function ry(e, n, r) {
  let i = t.$r(r);
  return e.find((e) => e.hostId === n && t.$r(e.remotePath) === i) ?? null;
}
function iy(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          r != null &&
          e.id === r.id &&
          e.hostId === r.hostId &&
          e.remotePath === r.remotePath &&
          e.label === r.label
        );
      })
    : !1;
}
function ay(e, t) {
  return e.length === t.length ? e.every((e, n) => e === t[n]) : !1;
}
var oy = `persist:codex-browser-`,
  sy = [
    `filesystem`,
    `indexdb`,
    `localstorage`,
    `shadercache`,
    `websql`,
    `serviceworkers`,
    `cachestorage`,
  ];
function cy(e) {
  return `${oy}${encodeURIComponent(e)}`;
}
async function ly(e) {
  let t = uy(`app`),
    n = [];
  (e.includes(`siteData`) && n.push(t.clearStorageData({ storages: sy })),
    e.includes(`cookies`) && n.push(t.clearStorageData({ storages: [`cookies`] })),
    e.includes(`cache`) && n.push(t.clearCache()),
    await Promise.all(n));
}
function uy(e) {
  return a.session.fromPartition(cy(e));
}
var dy = cy(`app`),
  fy = t.$s({
    owlProfileImporter: t.$s({
      listImportableProfiles: t.qs((e) => typeof e == `function`),
      importProfile: t.qs((e) => typeof e == `function`),
    }),
  }),
  py = t.qs((e) => fy.safeParse(e).success);
function my() {
  return _y() != null;
}
async function hy() {
  let e = _y();
  if (e == null) throw Error(`Owl browser profile importer is unavailable`);
  let n = await e.listImportableProfiles();
  return t.Gs(t.so).parse(n);
}
async function gy(e) {
  let n = _y();
  if (n == null) throw Error(`Owl browser profile importer is unavailable`);
  let r = t.co.parse(e);
  if (!r.importCookies && !r.importPasswords)
    throw Error(`Select cookies, passwords, or both to import`);
  if (!r.importCookies && r.cookieDomainAllowlist)
    throw Error(`Cookie domain selection requires cookie import`);
  if (
    t
      .Gs(t.so)
      .parse(await n.listImportableProfiles())
      .find((e) => e.source === r.source && e.profilePath === r.profilePath) == null
  )
    throw Error(`Browser profile is no longer importable`);
  let i = await n.importProfile({ ...r, targetPartition: dy });
  return t.lo.parse(i);
}
function _y() {
  if ((process.platform !== `darwin` && process.platform !== `win32`) || !n.a()) return null;
  let e = process._linkedBinding;
  if (typeof e != `function`) return null;
  let t;
  try {
    t = e.call(process, `electron_browser_owl_profile_importer`);
  } catch {
    return null;
  }
  let r = py.safeParse(t);
  return r.success ? r.data.owlProfileImporter : null;
}
function vy() {
  if (my()) return { importBrowserProfile: gy, listImportableBrowserProfiles: hy };
}
var yy = t.Wr(`BrowserUseOriginState`),
  by = { origin: `origin`, download: `download`, upload: `upload`, fullCdp: `fullCdp` },
  xy = { origin: `origins`, download: `downloads`, upload: `uploads`, fullCdp: `full_cdp` },
  Sy = t.Gs(
    t.$s({
      action: t.Us([`add`, `remove`]),
      kind: t.Us([`allowed`, `denied`]),
      origin: t.nc().min(1),
      resource: t.Us(by),
    }),
  );
async function Cy(e = t.Mr()) {
  return zy(await Fy(e));
}
async function wy(e, n = t.Mr()) {
  let r = await Fy(n);
  return ((r.approval_mode = Wy(e)), await Ly(n, r), zy(r));
}
async function Ty(e, n = t.Mr()) {
  let r = await Fy(n);
  return ((r.history_approval_mode = Wy(e)), await Ly(n, r), zy(r));
}
async function Ey(e, n, r = t.Mr()) {
  let i = await Fy(r);
  return ((i[Gy(e)] = Wy(n)), await Ly(r, i), zy(i));
}
async function Dy(e, n = t.Mr()) {
  let r = await Fy(n);
  return ((r[tt] = e), await Ly(n, r), zy(r));
}
async function Oy(e, n, r = t.Mr()) {
  return My([{ action: `add`, kind: e, origin: n, resource: `origin` }], r);
}
async function ky(e, n, r, i = t.Mr()) {
  return My([{ action: `add`, kind: n, origin: r, resource: e }], i);
}
async function Ay(e, n, r = t.Mr()) {
  return My([{ action: `remove`, kind: e, origin: n, resource: `origin` }], r);
}
async function jy(e, n, r, i = t.Mr()) {
  return My([{ action: `remove`, kind: n, origin: r, resource: e }], i);
}
async function My(e, n = t.Mr()) {
  let r = Sy.parse(e),
    i = await Fy(n),
    a = r.map((e) => ({
      ...e,
      origin: e.action === `add` ? Py(i, e.resource, e.origin) : e.origin,
    })),
    o = !1;
  for (let e of a) o = Ny(i, e) || o;
  return (o && (await Ly(n, i)), zy(i));
}
function Ny(e, t) {
  let { action: n, kind: r, origin: i, resource: a } = t,
    o = xy[a];
  if (n === `add`) {
    let t = By(e, o),
      n = qy(t[r]),
      a = Vy(r),
      s = qy(t[a]);
    return (
      (t[r] = n.includes(i) ? n : [...n, i]),
      (t[a] = s.filter((e) => e !== i)),
      !n.includes(i) || s.includes(i)
    );
  }
  let s = e[o];
  if (!Jy(s)) return !1;
  let c = qy(s[r]);
  return c.includes(i) ? ((s[r] = c.filter((e) => e !== i)), !0) : !1;
}
function Py(e, t, n) {
  let r = e[xy[t]];
  if (Jy(r) && (qy(r.allowed).includes(n) || qy(r.denied).includes(n))) return n;
  let i = Ky(n);
  if (i == null) throw Error(`Invalid Browser Use origin`);
  return i;
}
async function Fy(e) {
  let t = Ry(e);
  return (0, l.existsSync)(t) ? Iy(t) : {};
}
async function Iy(e) {
  try {
    let n = t.Or(await (0, p.readFile)(e, `utf8`));
    return Jy(n) ? n : {};
  } catch (t) {
    return (
      yy.warning(`browser_use_origin_state_parse_failed`, {
        safe: {},
        sensitive: { error: t, filePath: e },
      }),
      {}
    );
  }
}
async function Ly(e, t) {
  let n = Ry(e);
  await (0, p.mkdir)(s.default.dirname(n), { recursive: !0 });
  let r = F(t);
  await (0, p.writeFile)(
    n,
    r.endsWith(`
`)
      ? r
      : `${r}\n`,
    `utf8`,
  );
}
function Ry(e) {
  return s.default.join(e, et);
}
function zy(e) {
  let t = e.origins;
  return {
    fullCdpAccessEnabled: e[tt] === !0,
    approvalMode: Hy(e.approval_mode),
    historyApprovalMode: Hy(e.history_approval_mode),
    downloadApprovalMode: Hy(e.download_approval_mode),
    uploadApprovalMode: Hy(e.upload_approval_mode),
    allowedOrigins: Uy(t, `allowed`),
    deniedOrigins: Uy(t, `denied`),
    allowedDownloadOrigins: Uy(e.downloads, `allowed`),
    deniedDownloadOrigins: Uy(e.downloads, `denied`),
    allowedUploadOrigins: Uy(e.uploads, `allowed`),
    deniedUploadOrigins: Uy(e.uploads, `denied`),
    allowedFullCdpOrigins: Uy(e.full_cdp, `allowed`),
    deniedFullCdpOrigins: Uy(e.full_cdp, `denied`),
  };
}
function By(e, t) {
  if (Jy(e[t])) return e[t];
  let n = {};
  return ((e[t] = n), n);
}
function Vy(e) {
  return e === `allowed` ? `denied` : `allowed`;
}
function Hy(e) {
  return e === `never_ask` ? `neverAsk` : `alwaysAsk`;
}
function Uy(e, t) {
  return Jy(e) ? qy(e[t]) : [];
}
function Wy(e) {
  return e === `neverAsk` ? `never_ask` : `always_ask`;
}
function Gy(e) {
  return e === `download` ? `download_approval_mode` : `upload_approval_mode`;
}
function Ky(e) {
  let t = e.trim();
  if (t.length === 0) return null;
  let n = /^[a-zA-Z][a-zA-Z\d+.-]*:\/\//.test(t) ? t : `https://${t}`;
  try {
    let e = new URL(n);
    return e.protocol !== `http:` && e.protocol !== `https:` ? null : e.origin;
  } catch {
    return null;
  }
}
function qy(e) {
  if (!Array.isArray(e)) return [];
  let t = new Set(),
    n = [];
  for (let r of e) {
    if (typeof r != `string`) continue;
    let e = r.trim();
    e === `` || t.has(e) || (t.add(e), n.push(e));
  }
  return n;
}
function Jy(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
var Yy = t.$s({
    hostId: t.nc().trim().min(1),
    marketplacePath: t.nc().nullable(),
    pluginName: t.nc().trim().min(1),
  }),
  Xy = t.$s({
    hostId: t.nc().trim().min(1),
    marketplaceName: t.nc().trim().min(1),
    pluginName: t.nc().trim().min(1),
  });
function Zy({
  devRuntimeRepoRoot: e,
  getCodexHome: n,
  getHostConfig: r,
  getResourcesPath: i = Qy,
}) {
  return {
    install: async (a) => {
      let { hostId: o, marketplacePath: s, pluginName: c } = $y(a);
      if (t.va(r(o))) return;
      let l = i();
      if (l == null) throw Error(`Chrome native host resource sync requires resourcesPath`);
      await t.hn({
        codexHome: await n(o),
        devRuntimeRepoRoot: e,
        marketplacePath: s,
        pluginName: c,
        resourcesPath: l,
      });
    },
    uninstall: async (e) => {
      let { hostId: i, marketplaceName: a, pluginName: o } = eb(e);
      t.va(r(i)) || (await t._n({ codexHome: await n(i), marketplaceName: a, pluginName: o }));
    },
  };
}
function Qy() {
  return Ze({ env: process.env, resourcesPath: process.resourcesPath });
}
function $y(e) {
  let t = Yy.safeParse(e);
  if (!t.success) throw Error(`Invalid Chrome native host install request`);
  return t.data;
}
function eb(e) {
  let t = Xy.safeParse(e);
  if (!t.success) throw Error(`Invalid Chrome native host uninstall request`);
  return t.data;
}
var tb = t.Gr(`export-logs`);
function nb(e) {
  return e.toString().padStart(2, `0`);
}
function rb(e, t) {
  return (0, s.join)(e, t.getUTCFullYear().toString(), nb(t.getUTCMonth() + 1), nb(t.getUTCDate()));
}
function ib(e) {
  return new Date(Date.UTC(e.getUTCFullYear(), e.getUTCMonth(), e.getUTCDate()));
}
function ab(e, t) {
  return Array.from({ length: t }, (t, n) => {
    let r = new Date(e);
    return (r.setUTCDate(r.getUTCDate() - n), r);
  });
}
function ob(e, t) {
  let n = ib(e);
  switch (t) {
    case `today`:
      return [n];
    case `last7days`:
    case `session`:
      return ab(n, 7);
  }
}
function sb(e, t, n) {
  switch (e) {
    case `today`:
    case `last7days`:
      return !0;
    case `session`:
      return n.startsWith(`codex-desktop-${t}-`);
  }
}
async function cb(e) {
  let t = ob(e.now, e.scope);
  return (
    await Promise.all(
      t.map(async (t) => {
        let n = rb(e.rootDir, t);
        try {
          return (await (0, p.readdir)(n, { withFileTypes: !0 }))
            .filter((t) => t.isFile() && sb(e.scope, e.appSessionId, t.name))
            .map((e) => (0, s.join)(n, e.name));
        } catch (e) {
          return (
            tb().error(`Failed to read log dir`, { safe: {}, sensitive: { dir: n, error: e } }), []
          );
        }
      }),
    )
  )
    .flat()
    .sort();
}
function lb(e, t) {
  switch (e) {
    case `today`:
      return `codex-logs-today.txt`;
    case `last7days`:
      return `codex-logs-last7days.txt`;
    case `session`:
      return `codex-logs-session-${t.slice(0, 8)}.txt`;
  }
}
async function ub(e) {
  let t = { defaultPath: e.suggestedName, filters: [{ name: `Text`, extensions: [`txt`] }] },
    n = e.parentWindow
      ? await a.dialog.showSaveDialog(e.parentWindow, t)
      : await a.dialog.showSaveDialog(t);
  return n.canceled || !n.filePath
    ? null
    : (0, s.extname)(n.filePath).toLowerCase() === `.txt`
      ? n.filePath
      : `${n.filePath}.txt`;
}
async function db(e) {
  try {
    return await (0, p.readFile)(e, `utf8`);
  } catch (e) {
    return (tb().error(`Failed to read log file`, { safe: {}, sensitive: { error: e } }), ``);
  }
}
function fb(e) {
  let t = e.indexOf(` `);
  if (t <= 0) return null;
  let n = e.slice(0, t),
    r = Date.parse(n);
  return Number.isFinite(r) ? r : null;
}
async function pb(e) {
  let t = await Promise.all(e.sort().map((e) => db(e))),
    n = [],
    r = 0;
  for (let e of t)
    for (let t of e.split(/\r?\n/))
      t.length !== 0 && (n.push({ order: r, timestampMs: fb(t), text: t }), (r += 1));
  return (
    n.sort((e, t) => {
      let n = e.timestampMs,
        r = t.timestampMs;
      return n != null && r != null
        ? n - r || e.order - t.order
        : n == null
          ? r == null
            ? e.order - t.order
            : 1
          : -1;
    }),
    n.length > 0
      ? `${n.map((e) => e.text).join(`
`)}\n`
      : ``
  );
}
async function mb(e) {
  let t = new Date(),
    r = await cb({ rootDir: n.n(), now: t, scope: e.scope, appSessionId: e.appSessionId }),
    i = await ub({ parentWindow: e.parentWindow, suggestedName: lb(e.scope, e.appSessionId) });
  i && (await (0, p.writeFile)(i, await pb(r), `utf8`));
}
var hb = class extends ig {
  constructor(e) {
    (super(), (this.options = e));
  }
  async exportLogs({ scope: e }) {
    await mb({
      appSessionId: r.N,
      parentWindow: a.BrowserWindow.fromWebContents(this.options.origin),
      scope: e,
    });
  }
  async getBrowserSnapshot() {
    return this.options.getBrowserSnapshot();
  }
  async openBrowserTabOwner(e) {
    return this.options.openBrowserTabOwner(e);
  }
  async openWindow() {
    await this.options.debugWindowManager?.show(this.options.origin);
  }
};
function gb(e) {
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`;
  } catch {
    return !1;
  }
}
function _b({
  browserPaneEnabled: e,
  link: n,
  openLinkInTargetPreference: r = t.Vs,
  openLocalUrlInTargetPreference: i = t.Vs,
  webLinksDefaultInAppBrowser: a = !1,
}) {
  if (!e) return !1;
  switch (n.type) {
    case `file`:
      return n.target == null && n.location == null && t.fo(n.path);
    case `url`:
      return vb({
        openLinkInTargetPreference: r,
        openLocalUrlInTargetPreference: i,
        url: n.url,
        webLinksDefaultInAppBrowser: a,
      });
  }
}
function vb({
  openLinkInTargetPreference: e,
  openLocalUrlInTargetPreference: n,
  url: r,
  webLinksDefaultInAppBrowser: i,
}) {
  return t.po(r) ? n === `in-app-browser` : e === `in-app-browser` && i && gb(r);
}
function yb(e) {
  try {
    let { protocol: t } = new URL(e);
    return t === `file:` || t === `http:` || t === `https:` || t === `mailto:`;
  } catch {
    return !1;
  }
}
function bb(e) {
  try {
    let { protocol: n } = new URL(e);
    return n === `https:` || (n === `http:` && t.po(e));
  } catch {
    return !1;
  }
}
function xb(e, t) {
  let n = Cb(e, t);
  return n != null && Tb(n, e) === Tb(n, t);
}
function Sb(e, t) {
  let n = Cb(e, t);
  if (n == null) return !1;
  let r = n.relative(n.resolve(t), n.resolve(e));
  return r === `` || (!r.startsWith(`..`) && !n.isAbsolute(r));
}
function Cb(e, t) {
  let n = wb(e);
  return n === wb(t) ? (n ? s.default.win32 : s.default) : null;
}
function wb(e) {
  return /^[A-Za-z]:[\\/]/.test(e) || /^(?:\\\\|\/\/)/.test(e);
}
function Tb(e, t) {
  let n = e.resolve(t);
  return e === s.default.win32 ? n.toLowerCase() : n;
}
var Eb = t.$s({ name: t.nc().optional() }).passthrough(),
  Db = t.Gs(Eb);
function Ob(e) {
  return e.trim().toLowerCase();
}
function kb(e) {
  let t = Db.safeParse(e);
  if (!t.success) return [];
  let n = new Map();
  for (let e of t.data) {
    let t = e.name?.trim();
    if (t == null || t.length === 0) continue;
    let r = Ob(t);
    n.has(r) || n.set(r, t);
  }
  return [...n.values()].sort((e, t) => e.localeCompare(t));
}
function Ab(e) {
  let t = new Map();
  for (let n of e) {
    let e = new Map();
    for (let t of n.remoteMcpServerNames) {
      let n = t.trim();
      if (n.length === 0) continue;
      let r = Ob(n);
      e.has(r) || e.set(r, n);
    }
    for (let [r, i] of e) {
      let e = t.get(r);
      t.set(r, {
        lastSeenAtMs:
          e?.lastSeenAtMs == null
            ? n.lastActivityAtMs
            : n.lastActivityAtMs == null
              ? e.lastSeenAtMs
              : Math.max(e.lastSeenAtMs, n.lastActivityAtMs),
        name: e?.name ?? i,
        sessionCount: (e?.sessionCount ?? 0) + 1,
        source: `remoteMcpServersConfig`,
      });
    }
  }
  return [...t.values()].sort((e, t) => e.name.localeCompare(t.name));
}
async function jb(e, t) {
  let n = await Mb(e);
  if (n == null) return null;
  let r;
  try {
    r = JSON.parse(n);
  } catch {
    return null;
  }
  let i = t.safeParse(r);
  return i.success ? i.data : null;
}
async function Mb(e) {
  try {
    return await (0, p.readFile)(e, `utf8`);
  } catch {
    return null;
  }
}
async function Nb(e, t) {
  (await (0, p.mkdir)(s.default.dirname(e), { recursive: !0 }),
    await (0, p.writeFile)(
      e,
      t.endsWith(`
`)
        ? t
        : `${t}\n`,
      `utf8`,
    ));
}
async function Pb(e) {
  if (!(await Ib(e))) return [];
  let t = await (0, p.readdir)(e, { withFileTypes: !0 });
  return (
    await Promise.all(
      t.map(async (t) => {
        let n = s.default.join(e, t.name);
        return t.isDirectory() ? Pb(n) : t.isFile() ? [n] : [];
      }),
    )
  ).flat();
}
async function Fb(e) {
  try {
    return (await (0, p.stat)(e)).isFile();
  } catch {
    return !1;
  }
}
async function Ib(e) {
  try {
    return (await (0, p.stat)(e)).isDirectory();
  } catch {
    return !1;
  }
}
async function Lb(e) {
  try {
    return (await (0, p.lstat)(e), !0);
  } catch {
    return !1;
  }
}
var Rb = `claude-cowork-import-history.json`,
  zb = t
    .$s({
      contentSha256: t.nc(),
      importedAtMs: t.Qs(),
      importedThreadId: t.nc(),
      projectRoot: t.nc().optional(),
      sessionId: t.nc().optional(),
      sourceId: t.nc().optional(),
      sourcePath: t.nc(),
      spaceId: t.nc().optional(),
    })
    .transform((e) => ({
      contentSha256: e.contentSha256,
      importedAtMs: e.importedAtMs,
      importedThreadId: e.importedThreadId,
      projectRoot: e.projectRoot ?? e.spaceId ?? ``,
      sourceId: e.sourceId ?? e.sessionId ?? ``,
      sourcePath: e.sourcePath,
    })),
  Bb = t.$s({ importedAtMs: t.Qs(), sourceRoot: t.nc(), spaceId: t.nc() }),
  Vb = t.$s({ accountOrgDir: t.nc(), importedAtMs: t.Qs(), itemType: t.Us([`CONFIG`, `PLUGINS`]) }),
  Hb = t.$s({
    importedAtMs: t.Qs(),
    lastSeenAtMs: t.Qs().nullable(),
    name: t.nc(),
    sessionCount: t.Qs(),
    source: t.Xs(`remoteMcpServersConfig`),
  }),
  Ub = t
    .$s({
      accountSetupItems: t.Gs(Vb).optional(),
      connectorCandidates: t.Gs(Hb).optional(),
      projects: t.Gs(Bb).optional(),
      records: t.Gs(zb),
      version: t.Xs(1),
    })
    .transform((e) => ({
      accountSetupItems: e.accountSetupItems ?? [],
      connectorCandidates: e.connectorCandidates ?? [],
      projects: e.projects ?? [],
      records: e.records,
      version: e.version,
    }));
async function Wb(e) {
  return (
    (await jb(s.default.join(e, `claude-cowork-import-history.json`), Ub)) ?? {
      accountSetupItems: [],
      connectorCandidates: [],
      projects: [],
      records: [],
      version: 1,
    }
  );
}
async function Gb(e) {
  let t = await Wb(e);
  return {
    importedSessionCount: t.records.length,
    latestImportedAtMs: [
      ...t.records.map((e) => e.importedAtMs),
      ...t.projects.map((e) => e.importedAtMs),
      ...t.accountSetupItems.map((e) => e.importedAtMs),
      ...t.connectorCandidates.map((e) => e.importedAtMs),
    ].reduce((e, t) => (e == null ? t : Math.max(e, t)), null),
  };
}
async function Kb({ codexHome: e, importedAtMs: t = Date.now(), transcripts: n }) {
  let r = n.map(({ importedThreadId: e, transcript: n }) => ({
    contentSha256: n.contentSha256,
    importedAtMs: t,
    importedThreadId: e,
    projectRoot: n.projectRoot,
    sourceId: n.sourceId,
    sourcePath: n.sourceJsonlPath,
  }));
  if (r.length === 0) return;
  let i = await Wb(e);
  await Qb(e, { ...i, records: $b(i.records, r, ix) });
}
async function qb({ codexHome: e, importedAtMs: t = Date.now(), items: n }) {
  if (n.length === 0) return;
  let r = await Wb(e),
    i = n.map((e) => ({ accountOrgDir: e.accountOrgDir, importedAtMs: t, itemType: e.itemType }));
  await Qb(e, { ...r, accountSetupItems: $b(r.accountSetupItems, i, ex) });
}
async function Jb({ codexHome: e, importedAtMs: t = Date.now(), projects: n }) {
  if (n.length === 0) return;
  let r = await Wb(e),
    i = n.map((e) => ({ importedAtMs: t, sourceRoot: e.sourceRoot, spaceId: e.spaceId }));
  await Qb(e, { ...r, projects: $b(r.projects, i, nx) });
}
async function Yb({ candidates: e, codexHome: t }) {
  if (e.length === 0) return;
  let n = await Wb(t);
  await Qb(t, {
    ...n,
    connectorCandidates: ax({ existingRecords: n.connectorCandidates, nextCandidates: e }),
  });
}
function Xb(e, t) {
  return e.projects.some((e) => rx(e, t));
}
function Zb(e, t) {
  return e.accountSetupItems.some((e) => tx(e, t));
}
async function Qb(e, t) {
  (await (0, p.mkdir)(e, { recursive: !0 }),
    await (0, p.writeFile)(s.default.join(e, Rb), `${JSON.stringify(t, null, 2)}\n`, `utf8`));
}
function $b(e, t, n) {
  return [...e.filter((e) => !t.some((t) => n(e, t))), ...t];
}
function ex(e, t) {
  return tx(e, t);
}
function tx(e, t) {
  return e.itemType === t.itemType && xb(e.accountOrgDir, t.accountOrgDir);
}
function nx(e, t) {
  return rx(e, t);
}
function rx(e, t) {
  return e.spaceId === t.spaceId && xb(e.sourceRoot, t.sourceRoot);
}
function ix(e, t) {
  return (
    xb(e.projectRoot, t.projectRoot) &&
    e.sourceId === t.sourceId &&
    e.contentSha256 === t.contentSha256
  );
}
function ax({ existingRecords: e, nextCandidates: t }) {
  let n = Date.now(),
    r = new Map(e.map((e) => [ox(e.name, e.source), e]));
  for (let e of t) {
    let t = ox(e.name, e.source),
      i = r.get(t);
    r.set(t, {
      importedAtMs: n,
      lastSeenAtMs:
        i?.lastSeenAtMs == null
          ? e.lastSeenAtMs
          : e.lastSeenAtMs == null
            ? i.lastSeenAtMs
            : Math.max(i.lastSeenAtMs, e.lastSeenAtMs),
      name: sx(i?.name, e.name),
      sessionCount: (i?.sessionCount ?? 0) + e.sessionCount,
      source: e.source,
    });
  }
  return [...r.values()].sort((e, t) => e.name.localeCompare(t.name));
}
function ox(e, t) {
  return `${t}\u001f${Ob(e)}`;
}
function sx(e, t) {
  let n = e?.trim();
  return n != null && n.length > 0 ? n : t.trim();
}
function cx(e, t) {
  return {
    ...e,
    spaceConfigPath: t(e.spaceConfigPath),
    sourceFolders: e.sourceFolders.map((e) => t(e)),
  };
}
var lx = `## Imported Claude Cowork project instructions`,
  ux = `claude-cowork`,
  dx = { agents: !0, config: !0, sessions: !0 },
  fx = [`AGENTS_MD`],
  px = new Set([
    ...[
      `spaces.json`,
      `scheduled-tasks.json`,
      `artifacts.json`,
      `mcp.json`,
      `mcp-servers.json`,
      `plugins.json`,
      `plugins-mcp-skills.json`,
      `skills.json`,
      `agents.json`,
      `cowork_settings.json`,
      `cowork_account_settings.json`,
    ],
    `cowork-clientdata-cache.json`,
    `cowork-gb-cache.json`,
  ]),
  mx = t.tc(t.nc(), t.ic()),
  hx = t.rc([t.nc(), t.$s({ path: t.nc() }).passthrough()]),
  gx = t
    .$s({
      folders: t.Gs(hx).optional(),
      id: t.nc(),
      instructions: t.nc().optional(),
      name: t.nc().optional(),
      title: t.nc().optional(),
      userSelectedFolders: t.Gs(hx).optional(),
    })
    .passthrough(),
  _x = t.rc([t.Gs(gx), t.$s({ spaces: t.Gs(gx) }).passthrough()]),
  vx = t
    .$s({
      fileName: t.nc(),
      hostPath: t.nc(),
      timestamp: t.rc([t.Qs(), t.nc()]).nullable().optional(),
    })
    .passthrough(),
  yx = t
    .$s({
      cliSessionId: t.nc().nullable().optional(),
      createdAt: t.rc([t.Qs(), t.nc()]).nullable().optional(),
      cwd: t.nc().nullable().optional(),
      fsDetectedFiles: t.Gs(vx).optional(),
      id: t.nc().optional(),
      lastActivityAt: t.rc([t.Qs(), t.nc()]).nullable().optional(),
      remoteMcpServersConfig: t.ic().optional(),
      scheduledTaskId: t.nc().nullable().optional(),
      sessionId: t.nc().optional(),
      spaceId: t.nc().nullable().optional(),
      title: t.nc().nullable().optional(),
      updatedAt: t.rc([t.Qs(), t.nc()]).nullable().optional(),
      userSelectedFolders: t.Gs(hx).optional(),
    })
    .passthrough(),
  bx = t
    .$s({
      id: t.nc().optional(),
      name: t.nc().nullable().optional(),
      spaceId: t.nc().nullable().optional(),
      title: t.nc().nullable().optional(),
    })
    .passthrough(),
  xx = t.rc([t.Gs(bx), t.$s({ tasks: t.Gs(bx) }).passthrough()]),
  Sx = t
    .$s({
      args: t.Gs(t.nc()).optional(),
      command: t.nc().optional(),
      disabled: t.Ks().optional(),
      enabled: t.Ks().optional(),
      env: t.tc(t.nc(), t.ic()).optional(),
      headers: t.tc(t.nc(), t.ic()).optional(),
      type: t.nc().optional(),
      url: t.nc().optional(),
    })
    .passthrough(),
  Cx = t
    .$s({ mcp_servers: t.tc(t.nc(), Sx).optional(), mcpServers: t.tc(t.nc(), Sx).optional() })
    .passthrough(),
  wx = t.tc(t.nc(), t.ic()),
  Tx = t
    .$s({ id: t.nc().optional(), name: t.nc().optional(), path: t.nc().optional() })
    .passthrough(),
  Ex = t.rc([
    t.Gs(Tx),
    t.$s({ agents: t.Gs(Tx).optional(), skills: t.Gs(Tx).optional() }).passthrough(),
  ]);
async function Dx(e, n) {
  let r = await Mb(e);
  if (r == null) return {};
  try {
    let e = mx.safeParse(t.Or(r));
    if (e.success) return e.data;
  } catch (t) {
    return (n.push(`Could not read Codex config.toml ${e}: ${kx(t)}`), null);
  }
  return (n.push(`Could not read Codex config.toml ${e}`), null);
}
async function Ox(e, t) {
  await Nb(e, F(t));
}
function kx(e) {
  return e instanceof Error ? e.message : String(e);
}
function Ax(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function jx(e) {
  return typeof e == `string` && e.trim() !== `` ? e : null;
}
function Mx(e) {
  if (typeof e == `number`) return e > 1e10 ? e : e * 1e3;
  if (typeof e == `string`) {
    let t = Date.parse(e);
    return Number.isNaN(t) ? null : t;
  }
  return null;
}
var Nx = t
    .$s({ description: t.nc().optional(), name: t.nc().optional(), version: t.nc().optional() })
    .passthrough(),
  Px = `local`,
  Fx = `skills`,
  Ix = new Set([`.mcp.json`, `mcp.json`]),
  Lx = new Set();
async function Rx({ callbacks: e, codexHome: t, migration: n, pluginImports: r = [] }) {
  let i = [];
  try {
    let a = await Bx({
      callbacks: e,
      claimedRolloutImportKeys: i,
      codexHome: t,
      preparedRollouts: n.preparedRollouts,
    });
    return (
      await Vx({ codexHome: t, pluginImports: r }),
      await qb({ codexHome: t, items: [...n.accountSetupItemsToRecord, ...zx(r)] }),
      await Jb({ codexHome: t, projects: n.projectsToRecord }),
      { importedRollouts: a }
    );
  } finally {
    eS(i);
  }
}
function zx(e) {
  return [...new Set(e.map((e) => e.accountOrgDir))].map((e) => ({
    accountOrgDir: e,
    itemType: `PLUGINS`,
  }));
}
async function Bx({
  callbacks: e,
  claimedRolloutImportKeys: t,
  codexHome: n,
  preparedRollouts: r,
}) {
  if (r.length === 0) return [];
  let i = await Promise.allSettled(
      r.map(async (r) => {
        let i = Qx(n, r);
        i != null && t.push(i);
        try {
          if (i == null) return { importedRollout: null };
          let t = await e.forkThread({
              cwd: tS(e, r.cwd),
              path: tS(e, r.rolloutPath),
              threadId: r.sourceThreadId,
            }),
            n = { importedThreadId: t.threadId, rollout: r };
          if (
            r.workspaceKind === `projectless` &&
            r.projectlessOutputDirectory != null &&
            r.projectlessWorkspaceRoot != null
          )
            try {
              await e.recordProjectlessThread?.({
                outputDirectory: tS(e, r.projectlessOutputDirectory),
                threadId: t.threadId,
                workspaceRoot: tS(e, r.projectlessWorkspaceRoot),
              });
            } catch (e) {
              return { error: e, importedRollout: n };
            }
          try {
            r.title != null &&
              r.title !== `` &&
              (await e.setThreadName({ name: r.title, threadId: t.threadId }));
          } catch (e) {
            return { error: e, importedRollout: n };
          }
          return { importedRollout: n };
        } finally {
          await (0, p.rm)(r.rolloutRoot, { force: !0, recursive: !0 });
        }
      }),
    ),
    a = i.flatMap((e) => (e.status === `fulfilled` ? [e.value] : [])),
    o = a.flatMap((e) => (e.importedRollout == null ? [] : [e.importedRollout]));
  (await Kb({
    codexHome: n,
    transcripts: o.map(({ importedThreadId: e, rollout: t }) => ({
      importedThreadId: e,
      transcript: t,
    })),
  }),
    await Yb({ candidates: Ab(o.map(({ rollout: e }) => e)), codexHome: n }));
  let s = i.find((e) => e.status === `rejected`);
  if (s?.status === `rejected`) throw s.reason;
  let c = a.find((e) => e.error != null);
  if (c?.error != null) throw c.error;
  return o;
}
async function Vx({ codexHome: e, pluginImports: t }) {
  let n = Hx(t);
  (await Promise.all(n.map((t) => Wx({ codexHome: e, pluginImport: t }))),
    await Yx({ codexHome: e, pluginImports: n }));
}
function Hx(e) {
  let t = new Map();
  for (let n of e) {
    let e = n.version ?? Px,
      r = [ux, n.pluginName, e].join(``),
      i = t.get(r);
    if (i == null) {
      t.set(r, { ...n, skillPaths: [...n.skillPaths] });
      continue;
    }
    let a = new Map(i.skillPaths.map((e) => [Ux(e), e]));
    for (let e of n.skillPaths) {
      let t = Ux(e);
      a.has(t) || a.set(t, e);
    }
    i.skillPaths = [...a.values()];
  }
  return [...t.values()];
}
function Ux(e) {
  let t = Zx(s.default.dirname(e));
  return process.platform === `win32` ? t.toLowerCase() : t;
}
async function Wx({ codexHome: e, pluginImport: t }) {
  let n = t.version ?? Px,
    r = s.default.join(e, `plugins`, `cache`, ux, t.pluginName, n);
  (await (0, p.rm)(r, { force: !0, recursive: !0 }),
    await (0, p.mkdir)(s.default.dirname(r), { recursive: !0 }),
    await (0, p.cp)(t.pluginRoot, r, { filter: qx, recursive: !0 }),
    await Gx({ pluginImport: t, targetRoot: r }),
    await Kx({ pluginImport: t, targetRoot: r, version: n }));
}
async function Gx({ pluginImport: e, targetRoot: t }) {
  await Promise.all(
    e.skillPaths.map(async (n) => {
      if (s.default.relative(e.pluginRoot, n).split(s.default.sep)[0] === Fx) return;
      let r = s.default.dirname(n),
        i = s.default.join(t, Fx, Zx(r));
      (await (0, p.rm)(i, { force: !0, recursive: !0 }),
        await (0, p.mkdir)(s.default.dirname(i), { recursive: !0 }),
        await (0, p.cp)(r, i, { recursive: !0 }));
    }),
  );
}
async function Kx({ pluginImport: e, targetRoot: t, version: n }) {
  let r = await Jx(e.manifestPath);
  (delete r.skills, delete r.mcpServers, delete r.mcp_servers);
  let i = s.default.join(t, `.codex-plugin`, `plugin.json`);
  (await (0, p.mkdir)(s.default.dirname(i), { recursive: !0 }),
    await (0, p.writeFile)(
      i,
      `${JSON.stringify({ ...r, name: e.pluginName, version: n }, null, 2)}\n`,
      `utf8`,
    ));
}
function qx(e) {
  return !Ix.has(s.default.basename(e));
}
async function Jx(e) {
  let t = await (0, p.readFile)(e, `utf8`).catch(() => `{}`);
  try {
    let e = Nx.safeParse(JSON.parse(t));
    return e.success ? e.data : {};
  } catch {
    return {};
  }
}
async function Yx({ codexHome: e, pluginImports: t }) {
  if (t.length === 0) return;
  let n = s.default.join(e, `config.toml`),
    r = await Dx(n, []);
  if (r == null) throw Error(`Could not read Codex plugin config`);
  let i = Xx(r, `plugins`),
    a = !1;
  for (let e of t) {
    let t = `${e.pluginName}@${ux}`,
      n = i[t];
    (Ax(n) && n.enabled === !0) || ((i[t] = { ...(Ax(n) ? n : {}), enabled: !0 }), (a = !0));
  }
  a && (await Ox(n, r));
}
function Xx(e, t) {
  let n = e[t];
  if (Ax(n)) return n;
  let r = {};
  return ((e[t] = r), r);
}
function Zx(e) {
  return (
    s.default
      .basename(e)
      .replace(/[^A-Za-z0-9_-]+/g, `-`)
      .replace(/^-+|-+$/g, ``) || `cowork-skill`
  );
}
function Qx(e, t) {
  let n = $x(e, t);
  return Lx.has(n) ? null : (Lx.add(n), n);
}
function $x(e, t) {
  return [e, t.projectRoot, t.sourceId, t.contentSha256].join(``);
}
function eS(e) {
  for (let t of e) Lx.delete(t);
}
function tS(e, t) {
  return e.pathForCodexHost?.(t) ?? t;
}
async function nS(e, t) {
  return rS(e.map((e) => s.default.join(e, t)));
}
async function rS(e) {
  let t = await Promise.all(e.map((e) => Lb(e)));
  return e.filter((e, n) => t[n]);
}
async function iS(e, t) {
  let n = {},
    r = await rS(
      [`cowork_account_settings.json`, `cowork_settings.json`].flatMap((t) =>
        [...e.accountOrgDirs].reverse().map((e) => s.default.join(e, t)),
      ),
    ),
    i = await Promise.all(r.map(async (e) => ({ parsed: await jb(e, wx), sourcePath: e })));
  for (let { parsed: e, sourcePath: r } of i) {
    if (e == null) {
      t.push(`Could not read Cowork settings file: ${r}`);
      continue;
    }
    Object.assign(n, e);
  }
  return n;
}
async function aS(e, t, n) {
  let r = sS(n ?? (await iS(e, t))),
    i = new Map(),
    a = new Set(),
    o = await oS(e);
  for (let { parsed: e, sourcePath: n } of o) {
    if (e == null) {
      t.push(`Could not read Cowork MCP config file: ${n}`);
      continue;
    }
    for (let [o, s] of Object.entries({ ...e.mcpServers, ...e.mcp_servers })) {
      let e = o.trim();
      if (e === ``) {
        t.push(`Cowork MCP config ${n} had an empty server name`);
        continue;
      }
      a.has(e) ||
        (a.add(e),
        !cS({ disabledServerNames: r, serverConfig: s, serverName: e }) &&
          i.set(e, { config: s, sourcePath: n }));
    }
  }
  return i;
}
async function oS(e) {
  let t = await rS(
    e.userDataDirs.flatMap((t) => {
      let n = e.accountOrgDirs.find((e) => Sb(e, t));
      return [
        ...(n == null
          ? []
          : [s.default.join(n, `mcp.json`), s.default.join(n, `mcp-servers.json`)]),
        s.default.join(t, `claude_desktop_config.json`),
      ];
    }),
  );
  return Promise.all(t.map(async (e) => ({ parsed: await jb(e, Cx), sourcePath: e })));
}
function sS(e) {
  let t = e.disabledMcpjsonServers ?? e.disabledMcpServers;
  return Array.isArray(t)
    ? new Set(
        t
          .filter((e) => typeof e == `string`)
          .map((e) => e.trim())
          .filter((e) => e !== ``),
      )
    : new Set();
}
function cS({ disabledServerNames: e, serverConfig: t, serverName: n }) {
  return e.has(n) || t.disabled === !0 || t.enabled === !1;
}
var lS = new Set([
    `bash`,
    `bun`,
    `deno`,
    `node`,
    `perl`,
    `php`,
    `python`,
    `python3`,
    `ruby`,
    `sh`,
    `tsx`,
    `zsh`,
  ]),
  uS = /^\$\{([A-Za-z_][A-Za-z0-9_]*)\}$/,
  dS = /^Bearer\s+\$\{([A-Za-z_][A-Za-z0-9_]*)\}$/;
async function fS({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await iS(n.project, n.warnings),
    i = await aS(n.project, n.warnings, r),
    a = pS(r);
  if (i.size === 0 && Object.keys(a).length === 0) return [];
  await hS({ servers: i, warnings: n.warnings });
  let o = s.default.join(e, `config.toml`),
    c = await Dx(o, n.warnings);
  if (c == null) return [];
  let l = !1;
  for (let [e, t] of Object.entries(a)) c[e] ?? ((c[e] = t), (l = !0));
  if (i.size > 0) {
    let e = AS(c, `mcp_servers`);
    for (let [t, r] of [...i.entries()].sort(([e], [t]) => e.localeCompare(t))) {
      if (e[t] != null) continue;
      let i = mS({ serverEntry: r, serverName: t, warnings: n.warnings });
      i != null && ((e[t] = i), (l = !0));
    }
  }
  return l ? (await Ox(o, c), t.push(o), [o]) : [];
}
function pS(e) {
  let t = {},
    n = jx(e.model);
  n != null && (t.model = ES(n));
  let r = DS(jx(e.permissionMode));
  return (r != null && (t.sandbox_mode = r), t);
}
function mS({ serverEntry: e, serverName: t, warnings: n }) {
  let r = gS(e),
    i = { enabled: !0 },
    a = r.command?.trim(),
    o = r.url?.trim(),
    s = r.type,
    c = [],
    l;
  if (a != null && a !== ``) {
    if (s != null && s !== `stdio`)
      return (
        n.push(
          `Cowork MCP server ${t} skipped because command transport is incompatible with type ${s}`,
        ),
        null
      );
    ((i.command = a),
      r.args != null && r.args.length > 0 && (i.args = r.args),
      wS(i, r.env),
      r.headers != null && Object.keys(r.headers).length > 0 && c.push(`headers`),
      o != null && o !== `` && c.push(`url`),
      (l = `command`));
  } else if (o != null && o !== ``) {
    if (s != null && s !== `http` && s !== `streamable_http` && s !== `streamable-http`)
      return (
        n.push(
          `Cowork MCP server ${t} skipped because HTTP transport is incompatible with type ${s}`,
        ),
        null
      );
    ((i.url = o),
      CS(i, r.headers),
      r.args != null && r.args.length > 0 && c.push(`args`),
      r.env != null && Object.keys(r.env).length > 0 && c.push(`env`),
      (l = `HTTP`));
  } else return (n.push(`Cowork MCP server ${t} skipped because it has no command or URL`), null);
  c.length > 0 &&
    n.push(
      `Cowork MCP server ${t} ignored fields incompatible with ${l} transport: ${c.join(`, `)}`,
    );
  let u = TS(r);
  return (
    u.length > 0 && n.push(`Cowork MCP server ${t} has unsupported fields: ${u.join(`, `)}`), i
  );
}
async function hS({ servers: e, warnings: t }) {
  let n = await Promise.all(
    [...e.entries()].map(async ([e, t]) => {
      let n = _S(t);
      return n == null || (await Fb(n)) ? null : { executablePath: n, serverName: e };
    }),
  );
  for (let e of n) {
    if (e == null) continue;
    let n = `Cowork MCP server ${e.serverName} config migrated, but executable not found: ${e.executablePath}`;
    t.includes(n) || t.push(n);
  }
}
function gS(e) {
  let { config: t } = e,
    n = t.command?.trim();
  if (n == null || n === ``) return t;
  let r = s.default.dirname(e.sourcePath),
    i = yS(r, n),
    a = vS(t.args, r);
  return i == null
    ? !SS(n) || a === t.args
      ? t
      : { ...t, args: a }
    : a === t.args
      ? { ...t, command: i }
      : { ...t, args: a, command: i };
}
function _S(e) {
  let { config: t } = e,
    n = t.command?.trim();
  if (n == null || n === ``) return null;
  let r = s.default.dirname(e.sourcePath),
    i = yS(r, n);
  if (i != null) return i;
  if (!SS(n)) return null;
  let a = t.args?.find(xS) ?? null;
  return a == null ? null : yS(r, a);
}
function vS(e, t) {
  if (e == null) return;
  let n = !1,
    r = e.map((e) => {
      let r = yS(t, e);
      return r == null ? e : ((n = !0), r);
    });
  return n ? r : e;
}
function yS(e, t) {
  if (!bS(t)) return null;
  let n = t.replaceAll(`\\`, s.default.sep);
  return s.default.win32.isAbsolute(t) && !s.default.isAbsolute(t)
    ? null
    : s.default.isAbsolute(n)
      ? n
      : s.default.resolve(e, n);
}
function bS(e) {
  return s.default.isAbsolute(e) || s.default.win32.isAbsolute(e) || xS(e);
}
function xS(e) {
  return e.startsWith(`./`) || e.startsWith(`../`) || e.startsWith(`.\\`) || e.startsWith(`..\\`);
}
function SS(e) {
  let t = s.default
    .basename(e)
    .replace(/\.(?:bat|cmd|exe)$/i, ``)
    .toLowerCase();
  return lS.has(t);
}
function CS(e, t) {
  if (t == null) return;
  let n = {},
    r = {};
  for (let [i, a] of Object.entries(t)) {
    let t = String(a),
      o = kS(t);
    if (i.toLowerCase() === `authorization` && o != null) {
      e.bearer_token_env_var = o;
      continue;
    }
    let s = OS(t);
    if (s != null) {
      r[i] = s;
      continue;
    }
    n[i] = t;
  }
  (Object.keys(n).length > 0 && (e.http_headers = n),
    Object.keys(r).length > 0 && (e.env_http_headers = r));
}
function wS(e, t) {
  if (t == null) return;
  let n = {},
    r = [];
  for (let [e, i] of Object.entries(t)) {
    let t = String(i);
    if (OS(t) === e) {
      r.push(e);
      continue;
    }
    n[e] = t;
  }
  (r.length > 0 && (e.env_vars = r), Object.keys(n).length > 0 && (e.env = n));
}
function TS(e) {
  let t = new Set([
    `args`,
    `command`,
    `disabled`,
    `enabled`,
    `env`,
    `headers`,
    `name`,
    `scope`,
    `type`,
    `url`,
  ]);
  return Object.keys(e)
    .filter((e) => !t.has(e))
    .sort();
}
function ES(e) {
  return e.startsWith(`claude-opus`)
    ? `gpt-5.4`
    : e.startsWith(`claude-sonnet`) || e.startsWith(`claude-haiku`)
      ? `gpt-5.4-mini`
      : e;
}
function DS(e) {
  return e === `acceptEdits` ? `workspace-write` : e === `readOnly` ? `read-only` : null;
}
function OS(e) {
  return uS.exec(e)?.[1] ?? null;
}
function kS(e) {
  return dS.exec(e)?.[1] ?? null;
}
function AS(e, t) {
  if (Ax(e[t])) return e[t];
  let n = {};
  return ((e[t] = n), n);
}
var jS = `## Claude Cowork Migration`;
async function MS({ projectImport: e }) {
  let t = s.default.join(e.project.sourceRoot, `AGENTS.md`),
    n = await Mb(t);
  if (NS(n, `## Imported Claude Cowork project instructions`) || NS(n, jS)) return t;
  let r = e.project.instructions,
    i = r == null || r.trim() === `` ? null : r.trim(),
    a =
      i == null
        ? lx
        : [lx, ``, i].join(`
`),
    o = `

`;
  return (
    n == null ||
    n === `` ||
    n.endsWith(`

`)
      ? (o = ``)
      : n.endsWith(`
`) &&
        (o = `
`),
    await (0, p.writeFile)(t, `${n ?? ``}${o}${a}\n`, `utf8`),
    t
  );
}
function NS(e, t) {
  return (
    e
      ?.split(`
`)
      .some((e) => e.trim() === t) === !0
  );
}
function PS(e) {
  return (
    e
      .trim()
      .toLowerCase()
      .replace(/[^a-z0-9_-]+/g, `-`)
      .replace(/^-+|-+$/g, ``) || `cowork-item`
  );
}
var FS = t.$s({ name: t.nc().optional(), version: t.nc().optional() }).passthrough(),
  IS = [
    s.default.join(`.codex-plugin`, `plugin.json`),
    s.default.join(`.claude-plugin`, `plugin.json`),
  ];
async function LS(e) {
  let t = (
      await Promise.all(
        US(e).map(async ({ root: e, userDataDir: t }) =>
          (await HS(e)).map((e) => ({ skillPath: e, userDataDir: t })),
        ),
      )
    ).flat(),
    n = new Map(),
    r = [],
    i = await Promise.all(
      t.map(async ({ skillPath: e, userDataDir: t }) => {
        let n = await WS(e);
        return {
          manifest: n == null ? null : await jb(n, FS),
          manifestPath: n,
          skillPath: e,
          userDataDir: t,
        };
      }),
    );
  for (let { manifest: t, manifestPath: a, skillPath: o, userDataDir: c } of i) {
    if (a == null) {
      let t = s.default.dirname(o);
      r.push({
        accountOrgDir: e.accountOrgDir,
        name: s.default.basename(t),
        skillPath: o,
        skillRoot: t,
      });
      continue;
    }
    let i = s.default.dirname(s.default.dirname(a)),
      l = s.default.relative(c, i),
      u = process.platform === `win32` ? l.toLowerCase() : l,
      d = n.get(u);
    if (d != null) {
      let e = PS(s.default.basename(s.default.dirname(o)));
      d.skillPaths.some((t) => PS(s.default.basename(s.default.dirname(t))) === e) ||
        d.skillPaths.push(o);
      continue;
    }
    let f = qS({ manifestName: t?.name, pluginRoot: i });
    n.set(u, {
      accountOrgDir: e.accountOrgDir,
      manifestPath: a,
      pluginName: f,
      pluginRoot: i,
      skillPaths: [o],
      version: JS(t?.version),
    });
  }
  return { looseSkills: ZS(r), pluginBundles: VS([...n.values()]) };
}
async function RS(e) {
  return (await LS(e)).pluginBundles;
}
function zS(e) {
  return BS(e.accountOrgDir, e.pluginName);
}
function BS(e, t) {
  return [e, t].join(``);
}
function VS(e) {
  return [
    ...new Map(
      e.map((e) => [
        [e.accountOrgDir, e.pluginName, e.pluginRoot].join(``),
        { ...e, skillPaths: [...new Set(e.skillPaths)].sort((e, t) => e.localeCompare(t)) },
      ]),
    ).values(),
  ].sort((e, t) =>
    `${e.accountOrgDir}\u001f${e.pluginName}`.localeCompare(
      `${t.accountOrgDir}\u001f${t.pluginName}`,
    ),
  );
}
async function HS(e) {
  return (await Pb(e))
    .filter((e) => s.default.basename(e) === `SKILL.md`)
    .sort((e, t) => e.localeCompare(t));
}
function US(e) {
  let t = s.default.basename(e.accountOrgDir),
    n = s.default.basename(s.default.dirname(e.accountOrgDir));
  return e.userDataDirs.flatMap((r) => {
    let i = s.default.join(r, s.default.relative(e.userDataDir, e.accountOrgDir));
    return [
      { root: s.default.join(i, `rpm`), userDataDir: r },
      { root: s.default.join(i, `skills`), userDataDir: r },
      { root: s.default.join(i, `cowork_plugins`), userDataDir: r },
      { root: s.default.join(i, `.local-plugins`), userDataDir: r },
      { root: s.default.join(i, `.remote-plugins`), userDataDir: r },
      {
        root: s.default.join(r, `local-agent-mode-sessions`, `skills-plugin`, t, n),
        userDataDir: r,
      },
    ];
  });
}
async function WS(e) {
  return (
    (await Promise.all(GS(s.default.dirname(e)).map((e) => KS(e)))).find((e) => e != null) ?? null
  );
}
function GS(e) {
  let t = [];
  for (let n = e; ; n = s.default.dirname(n)) if ((t.push(n), s.default.dirname(n) === n)) return t;
}
async function KS(e) {
  return (
    (
      await Promise.all(
        IS.map(async (t) => {
          let n = s.default.join(e, t);
          return (await Fb(n)) ? n : null;
        }),
      )
    ).find((e) => e != null) ?? null
  );
}
function qS({ manifestName: e, pluginRoot: t }) {
  let n = e?.trim();
  return YS(n == null || n === `` ? s.default.basename(t) : n, `cowork-plugin`);
}
function JS(e) {
  let t = e?.trim();
  return t == null || t === `` ? null : XS(t);
}
function YS(e, t) {
  return e.replace(/[^A-Za-z0-9_-]+/g, `-`).replace(/^-+|-+$/g, ``) || t;
}
function XS(e) {
  return e.replace(/[^A-Za-z0-9.+_-]+/g, `-`).replace(/^[.+-]+|[.+-]+$/g, ``) || `local`;
}
function ZS(e) {
  let t = new Map();
  for (let n of e) {
    let e = [n.accountOrgDir, PS(n.name)].join(``);
    t.has(e) || t.set(e, n);
  }
  return [...t.values()].sort((e, t) =>
    `${e.accountOrgDir}\u001f${e.name}`.localeCompare(`${t.accountOrgDir}\u001f${t.name}`),
  );
}
async function QS({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await LS(n.project),
    i = await rC(n.project.accountOrgDirs, `skills`),
    a = await nC(n.project.accountOrgDirs, i.length, `skills`),
    o = new Map();
  for (let t of i) {
    let n = s.default.basename(t, s.default.extname(t)),
      r = a.get(n) ?? a.get(`fallback`) ?? n,
      i = s.default.join(e, `skills`, PS(r), `SKILL.md`);
    o.has(i) || o.set(i, { skillName: r, sourcePath: t });
  }
  let c = await Promise.all(
      [...o].map(async ([e, { skillName: n, sourcePath: r }]) =>
        (await Fb(e))
          ? null
          : (await Nb(e, eC({ content: await (0, p.readFile)(r, `utf8`), name: n })), t.push(e), e),
      ),
    ).then((e) => e.filter((e) => e != null)),
    l = await Promise.all(
      r.looseSkills.map(async (n) => {
        let r = s.default.join(e, `skills`, PS(n.name)),
          i = s.default.join(r, `SKILL.md`);
        return (await Fb(i))
          ? null
          : (await (0, p.cp)(n.skillRoot, r, { recursive: !0 }), t.push(i), i);
      }),
    ).then((e) => e.filter((e) => e != null));
  return [...c, ...l];
}
async function $S({ codexHome: e, copiedFiles: t, projectImport: n }) {
  let r = await rC(n.project.accountOrgDirs, `agents`);
  if (r.length === 0) return [];
  let i = await nC(n.project.accountOrgDirs, r.length, `agents`),
    a = new Map();
  for (let t of r) {
    let n = s.default.basename(t, s.default.extname(t)),
      r = i.get(n) ?? i.get(`fallback`) ?? iC(n),
      o = s.default.join(e, `agents`, `${PS(r)}.toml`);
    a.has(o) || a.set(o, { agentName: r, sourcePath: t });
  }
  return Promise.all(
    [...a].map(async ([e, { agentName: n, sourcePath: r }]) => {
      if (await Fb(e)) return null;
      let i = await (0, p.readFile)(r, `utf8`);
      return (
        await Ox(e, {
          name: n,
          description: `Migrated Claude Cowork agent ${n}.`,
          developer_instructions: i.trim(),
        }),
        t.push(e),
        e
      );
    }),
  ).then((e) => e.filter((e) => e != null));
}
function eC({ content: e, name: t }) {
  return tC(e)
    ? e
    : [
        `---`,
        `name: ${JSON.stringify(t)}`,
        `description: ${JSON.stringify(`Migrated Claude Cowork skill ${t}.`)}`,
        `---`,
        ``,
        e.trim(),
        ``,
      ].join(`
`);
}
function tC(e) {
  let t = /^---\n([\s\S]*?)\n---\n?/.exec(e);
  return t == null ? !1 : /\bname\s*:/.test(t[1]) && /\bdescription\s*:/.test(t[1]);
}
async function nC(e, t, n) {
  let r = (await Promise.all(e.map((e) => jb(s.default.join(e, `${n}.json`), Ex)))).flatMap((e) =>
      e == null ? [] : Array.isArray(e) ? e : (e[n] ?? []),
    ),
    i = new Map();
  for (let e of r) {
    let t = e.name ?? e.id;
    if (t == null) continue;
    let n = e.path == null ? PS(t) : s.default.basename(e.path, s.default.extname(e.path));
    i.has(n) || i.set(n, t);
  }
  if (r.length === 1 && t === 1) {
    let [e] = r,
      t = e?.name ?? e?.id;
    t != null && i.set(`fallback`, t);
  }
  return i;
}
async function rC(e, t) {
  let n = await Promise.all(
      e.map(async (e) => ({
        entries: await (0, p.readdir)(s.default.join(e, t), { withFileTypes: !0 }).catch(() => []),
        root: e,
      })),
    ),
    r = new Map();
  for (let { entries: e, root: i } of n)
    for (let n of e.sort((e, t) => e.name.localeCompare(t.name))) {
      let e = process.platform === `win32` ? n.name.toLowerCase() : n.name;
      n.isFile() && n.name.endsWith(`.md`) && !r.has(e) && r.set(e, s.default.join(i, t, n.name));
    }
  return [...r.values()];
}
function iC(e) {
  let t = e
    .split(/[-_]+/g)
    .filter((e) => e !== ``)
    .map((e) => `${e.slice(0, 1).toUpperCase()}${e.slice(1)}`)
    .join(` `);
  return t === `` ? `Cowork Agent` : t;
}
async function aC({ codexHome: e, projects: t }) {
  let n = [];
  for (let r of t) {
    let t = await oC({ codexHome: e, projectImport: r });
    n.push(t);
  }
  return { projects: n };
}
async function oC({ codexHome: e, projectImport: t }) {
  let n = [],
    r = [],
    i = null;
  return (
    t.accountSetupSelected &&
      r.push(
        ...(await fS({ codexHome: e, copiedFiles: n, projectImport: t })),
        ...(await QS({ codexHome: e, copiedFiles: n, projectImport: t })),
        ...(await $S({ codexHome: e, copiedFiles: n, projectImport: t })),
      ),
    t.itemTypes.has(`AGENTS_MD`) && (i = await MS({ projectImport: t })),
    {
      agentsPath: i,
      copiedFiles: n,
      liveConfigPaths: r,
      projectImport: t,
      transcriptCandidates: t.itemTypes.has(`SESSIONS`) ? t.transcriptCandidates : [],
    }
  );
}
var sC = `claude-cowork`;
function cC(e) {
  return [
    ...new Set(
      e.flatMap((e) => [
        ...(e.cwd == null || e.cwd === `` ? [] : [e.cwd]),
        ...(e.details?.sessions ?? [])
          .filter((e) => e.workspaceKind !== `projectless`)
          .map((e) => e.cwd),
      ]),
    ),
  ].sort((e, t) => e.localeCompare(t));
}
function lC(e, t) {
  return {
    ...e,
    cwd: e.cwd == null || e.cwd === `` ? e.cwd : t(e.cwd),
    ...(e.sourcePath == null ? {} : { sourcePath: t(e.sourcePath) }),
    details:
      e.details == null
        ? null
        : {
            ...e.details,
            sessions: e.details.sessions.map((e) => ({ ...e, cwd: t(e.cwd), path: t(e.path) })),
          },
  };
}
function uC({ projectlessSpaces: e = [], projects: t }) {
  let n = dC(t),
    r = fC(t),
    i = t.flatMap((e) =>
      e.itemTypes.has(`AGENTS_MD`)
        ? [
            mC({
              cwd: e.project.sourceRoot,
              description: `Append imported Cowork project instructions for ${e.project.name}`,
              itemType: `AGENTS_MD`,
            }),
          ]
        : [],
    ),
    a = [
      ...t.flatMap((e) =>
        e.transcriptCandidates.map((e) => ({
          cwd: e.projectRoot,
          path: e.sourceJsonlPath,
          title: e.title,
          ...(e.workspaceKind === `projectless` ? { workspaceKind: e.workspaceKind } : {}),
        })),
      ),
      ...e.flatMap((e) =>
        e.transcriptCandidates.map((e) => ({
          cwd: e.projectRoot,
          path: e.sourceJsonlPath,
          title: e.title,
          ...(e.workspaceKind === `projectless` ? { workspaceKind: e.workspaceKind } : {}),
        })),
      ),
    ],
    o =
      a.length === 1
        ? `Import 1 Cowork JSONL transcript`
        : `Import ${a.length} Cowork JSONL transcripts`;
  return [
    ...n,
    ...r,
    ...i,
    ...(a.length === 0
      ? []
      : [
          mC({
            cwd: null,
            description: o,
            details: {
              commands: [],
              hooks: [],
              mcpServers: [],
              plugins: [],
              sessions: a,
              subagents: [],
            },
            itemType: `SESSIONS`,
          }),
        ]),
  ];
}
function dC(e) {
  let t = new Set();
  for (let n of e)
    !n.accountSetupSelected || n.configSurfaces.length === 0 || t.add(n.project.accountOrgDir);
  return [...t]
    .sort((e, t) => e.localeCompare(t))
    .map((e) =>
      mC({
        cwd: null,
        description: `Migrate supported Cowork account/org setup`,
        itemType: `CONFIG`,
        sourcePath: e,
      }),
    );
}
function fC(e) {
  let t = new Map();
  for (let n of e) {
    if (!n.pluginSetupSelected || n.pluginCandidates.length === 0) continue;
    let e = n.project.accountOrgDir,
      r = t.get(e) ?? [];
    t.set(e, [...r, ...n.pluginCandidates]);
  }
  return [...t.entries()]
    .map(([e, t]) => {
      let n = new Set();
      for (let e of t) n.add(e.pluginName);
      return mC({
        cwd: null,
        description: `Install supported Cowork plugins`,
        details: {
          commands: [],
          hooks: [],
          mcpServers: [],
          plugins: [...n].sort((e, t) => e.localeCompare(t)).map((e) => ({ name: e })),
          sessions: [],
          subagents: [],
        },
        itemType: `PLUGINS`,
        sourcePath: e,
      });
    })
    .sort((e, t) => (e.sourcePath ?? ``).localeCompare(t.sourcePath ?? ``));
}
function pC({ skippedProjects: e, workspaceRoots: t }) {
  return t == null ? e : e.filter((e) => e.sourceFolders.some((e) => t.some((t) => xb(t, e))));
}
function mC({ cwd: e, description: t, details: n = null, itemType: r, sourcePath: i }) {
  return {
    cwd: e,
    description: t,
    details: n,
    itemType: r,
    providerId: sC,
    ...(i == null ? {} : { sourcePath: i }),
  };
}
async function hC(e) {
  let t = [],
    n = _C(e),
    r = (
      await Promise.all(
        (await gC(n)).map((r) => xC({ locations: r, options: e, userDataDirs: n, warnings: t })),
      )
    ).flat(),
    i = r.map((e) => e.project).filter((e) => e != null),
    a = r.map((e) => e.projectlessSpace).filter((e) => e != null),
    o = r.map((e) => e.skippedProject).filter((e) => e != null);
  return (
    i.length === 0 &&
      a.length === 0 &&
      o.length === 0 &&
      t.length === 0 &&
      t.push(`No Claude Cowork spaces were found`),
    { projectlessSpaces: a, projects: i, skippedProjects: o, warnings: t }
  );
}
async function gC(e) {
  let t = new Map(),
    n = await Promise.all(
      e.map(async (e) => {
        let t = s.default.join(e, `local-agent-mode-sessions`),
          n = await yC(t);
        return (
          await Promise.all(
            n.map(async (n) => {
              let r = s.default.join(t, n);
              return (await yC(r)).map((t) => ({
                accountOrgDir: s.default.join(r, t),
                accountOrgKey:
                  process.platform === `win32` ? `${n}\u001f${t}`.toLowerCase() : `${n}\u001f${t}`,
                userDataDir: e,
              }));
            }),
          )
        ).flat();
      }),
    );
  for (let { accountOrgDir: e, accountOrgKey: r, userDataDir: i } of n.flat()) {
    let n = t.get(r) ?? [];
    (n.push({ accountOrgDir: e, userDataDir: i }), t.set(r, n));
  }
  return [...t.values()];
}
function _C(e) {
  let t = [e.userDataDir, ...(e.fallbackUserDataDirs ?? [])];
  return t.filter((e, n) => t.findIndex((t) => xb(t, e)) === n);
}
function vC(e) {
  return e == null ? [] : e.map((e) => (typeof e == `string` ? e : e.path)).filter((e) => e !== ``);
}
async function yC(e) {
  return (await Ib(e))
    ? (await (0, p.readdir)(e, { withFileTypes: !0 }))
        .filter((e) => e.isDirectory())
        .map((e) => e.name)
        .sort((e, t) => e.localeCompare(t))
    : [];
}
function bC(e, t) {
  let n = s.default.join(t.documentsDir, `Claude`, `Artifacts`);
  return _C(t).some((t) => Sb(e, t)) || Sb(e, n);
}
async function xC({ locations: e, options: t, userDataDirs: n, warnings: r }) {
  let i = SC(e, t),
    a = e.map((e) => e.accountOrgDir),
    o = await nS(a, `spaces.json`),
    c = await Promise.all(o.map(async (e) => ({ parsed: await jb(e, _x), sourcePath: e }))),
    l = new Map();
  for (let e of c) {
    if (e.parsed == null) {
      r.push(`Could not read Claude Cowork spaces file: ${e.sourcePath}`);
      continue;
    }
    let t = Array.isArray(e.parsed) ? e.parsed : e.parsed.spaces;
    for (let n of t) l.has(n.id) || l.set(n.id, { sourcePath: e.sourcePath, space: n });
  }
  let u =
      c.find((e) => e.parsed != null)?.sourcePath ??
      o[0] ??
      s.default.join(e[0].accountOrgDir, `spaces.json`),
    d = (await CC(a))
      ? TC({ accountOrgDirs: a, identityLocation: i, options: t, spaceConfigPath: u })
      : null;
  if (l.size === 0)
    return d == null ? [] : [{ project: null, projectlessSpace: d, skippedProject: null }];
  let f = await Promise.all(
    [...l.values()].map(async ({ sourcePath: e, space: r }) =>
      EC({
        accountOrgDirs: a,
        identityLocation: i,
        options: t,
        space: r,
        spaceConfigPath: e,
        userDataDirs: n,
      }),
    ),
  );
  return d == null ? f : [...f, { project: null, projectlessSpace: d, skippedProject: null }];
}
function SC(e, t) {
  let n = t.fallbackUserDataDirs?.find((e) => !xb(e, t.userDataDir)) ?? t.userDataDir,
    r = e.find((e) => xb(e.userDataDir, n)) ?? e[0];
  return {
    accountOrgDir: s.default.join(n, s.default.relative(r.userDataDir, r.accountOrgDir)),
    userDataDir: n,
  };
}
async function CC(e) {
  return (await Promise.all(e.flatMap((e) => [wC(e), wC(s.default.join(e, `agent`))]))).some(
    (e) => e.length > 0,
  );
}
async function wC(e) {
  return (await Ib(e))
    ? (await (0, p.readdir)(e, { withFileTypes: !0 }))
        .filter((e) => e.isFile() && e.name.endsWith(`.json`) && !px.has(e.name))
        .map((e) => e.name)
    : [];
}
function TC({ accountOrgDirs: e, identityLocation: t, options: n, spaceConfigPath: r }) {
  return {
    accountOrgDir: t.accountOrgDir,
    accountOrgDirs: e,
    documentsDir: n.documentsDir,
    homeDir: n.homeDir ?? null,
    name: `Cowork recents`,
    projectlessImportRoot: s.default.join(t.accountOrgDir, `projectless-spaces`, `account`),
    spaceConfigPath: r,
    spaceId: null,
    userDataDir: t.userDataDir,
  };
}
async function EC({
  accountOrgDirs: e,
  identityLocation: t,
  options: n,
  space: r,
  spaceConfigPath: i,
  userDataDirs: a,
}) {
  let o = r.name ?? r.title ?? r.id,
    c = vC(r.folders ?? r.userSelectedFolders),
    l = c.map((e) => s.default.resolve(e));
  if (c.length === 0)
    return {
      project: null,
      projectlessSpace: {
        accountOrgDir: t.accountOrgDir,
        accountOrgDirs: e,
        documentsDir: n.documentsDir,
        homeDir: n.homeDir ?? null,
        name: o,
        projectlessImportRoot: s.default.join(t.accountOrgDir, `projectless-spaces`, r.id),
        spaceConfigPath: i,
        spaceId: r.id,
        userDataDir: t.userDataDir,
      },
      skippedProject: null,
    };
  if (c.length !== 1)
    return {
      project: null,
      projectlessSpace: null,
      skippedProject: {
        name: o,
        reason: `multiple-source-folders`,
        spaceConfigPath: i,
        sourceFolders: l,
        spaceId: r.id,
      },
    };
  let u = l[0];
  return (await Ib(u))
    ? bC(u, n)
      ? {
          project: null,
          projectlessSpace: null,
          skippedProject: {
            name: o,
            reason: `claude-managed-folder`,
            spaceConfigPath: i,
            sourceFolders: [u],
            spaceId: r.id,
          },
        }
      : {
          project: {
            accountOrgDir: t.accountOrgDir,
            accountOrgDirs: e,
            documentsDir: n.documentsDir,
            homeDir: n.homeDir ?? null,
            instructions: r.instructions ?? null,
            name: o,
            sourceRoot: u,
            spaceId: r.id,
            userDataDir: t.userDataDir,
            userDataDirs: a,
          },
          projectlessSpace: null,
          skippedProject: null,
        }
    : {
        project: null,
        projectlessSpace: null,
        skippedProject: {
          name: o,
          reason: `missing-source-folder`,
          spaceConfigPath: i,
          sourceFolders: [u],
          spaceId: r.id,
        },
      };
}
async function DC({ items: e, options: t }) {
  let n = OC(e);
  if (n.size === 0) return [];
  let r = await hC(t);
  return VS((await Promise.all(r.projects.map((e) => RS(e)))).flat().filter((e) => n.has(zS(e))));
}
function OC(e) {
  return new Set(
    e
      .filter((e) => e.itemType === `PLUGINS`)
      .flatMap((e) => {
        let t = e.sourcePath;
        return t == null || t === ``
          ? []
          : (e.details?.plugins ?? []).flatMap((e) => [BS(t, e.name)]);
      }),
  );
}
var kC = [
    { kind: `plugins`, relativePath: `plugins.json` },
    { kind: `plugins`, relativePath: `plugins-mcp-skills.json` },
    { kind: `plugins`, relativePath: `.local-plugins` },
    { kind: `plugins`, relativePath: `.remote-plugins` },
    { kind: `skills`, relativePath: `skills.json` },
    { kind: `skills`, relativePath: `skills` },
    { kind: `agents`, relativePath: `agents.json` },
    { kind: `agents`, relativePath: `agents` },
    { kind: `settings`, relativePath: `cowork_account_settings.json` },
    { kind: `settings`, relativePath: `cowork_settings.json` },
  ],
  AC = [
    { kind: `extensions`, relativePath: `Claude Extensions` },
    { kind: `extensions`, relativePath: `Claude Extensions Settings` },
    { kind: `extensions`, relativePath: `Extensions Update Cache` },
    { kind: `extensions`, relativePath: `extensions-blocklist.json` },
    { kind: `extensions`, relativePath: `extensions-installations.json` },
  ];
async function jC(e) {
  let t = new Map(),
    n = (e, n) => {
      let r = t.get(e);
      if (r != null) return r;
      let i = NC(n);
      return (t.set(e, i), i);
    },
    r = await Promise.all(
      e.projects.map(async (e) => {
        let t = [],
          r = await iS(e, t),
          [i, a, o, s] = await Promise.all([
            n(e.accountOrgDir, e.accountOrgDirs),
            MC(e, t),
            RC(e, t, r),
            RS(e),
          ]);
        return {
          configSurfaces: o,
          pluginCandidates: s,
          project: e,
          scheduledTasks: a,
          sessionManifests: i,
          warnings: t,
        };
      }),
    ),
    i = await Promise.all(
      e.projectlessSpaces.map(async (e) => ({
        projectlessSpace: e,
        sessionManifests: await n(e.accountOrgDir, e.accountOrgDirs),
      })),
    );
  return {
    configSurfacesByProjectRoot: new Map(r.map((e) => [e.project.sourceRoot, e.configSurfaces])),
    pluginCandidatesByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.pluginCandidates]),
    ),
    projectlessSpaces: e.projectlessSpaces,
    projects: e.projects,
    scheduledTasksByProjectRoot: new Map(r.map((e) => [e.project.sourceRoot, e.scheduledTasks])),
    sessionManifestsByProjectlessSpaceRoot: new Map(
      i.map((e) => [e.projectlessSpace.projectlessImportRoot, e.sessionManifests]),
    ),
    sessionManifestsByProjectRoot: new Map(
      r.map((e) => [e.project.sourceRoot, e.sessionManifests]),
    ),
    skippedProjects: e.skippedProjects,
    warnings: [...e.warnings, ...r.flatMap((e) => e.warnings)],
  };
}
async function MC(e, t) {
  let n = await nS(e.accountOrgDirs, `scheduled-tasks.json`);
  if (n.length === 0) return [];
  let r = await Promise.all(n.map(async (e) => ({ parsed: await jb(e, xx), sourcePath: e })));
  for (let e of r)
    e.parsed == null &&
      t != null &&
      t.push(`Could not read Claude Cowork scheduled tasks file: ${e.sourcePath}`);
  let i = r.find((e) => e.parsed != null);
  if (i?.parsed == null) return [];
  let a = i.sourcePath,
    o = i.parsed;
  return (Array.isArray(o) ? o : o.tasks)
    .filter((t) => t.spaceId == null || t.spaceId === e.spaceId)
    .map((e, t) => ({
      id: e.id ?? `scheduled-task-${t + 1}`,
      name: e.name ?? e.title ?? null,
      sourcePath: a,
    }));
}
async function NC(e) {
  let t = await Promise.all(
      e.map(async (e) => {
        let t = [...(await BC(e)), ...(await BC(s.default.join(e, `agent`)))].filter(
          (e) => !px.has(s.default.basename(e)),
        );
        return (await Promise.all(t.map((e) => LC(e)))).filter((e) => e != null);
      }),
    ),
    n = new Map();
  for (let e of t.flat()) {
    let t = n.get(e.sessionId);
    if (t == null) {
      n.set(e.sessionId, e);
      continue;
    }
    n.set(e.sessionId, PC(t, e));
  }
  return [...n.values()];
}
function PC(e, t) {
  return {
    ...e,
    cliSessionIds: FC(e.cliSessionIds, t.cliSessionIds),
    cwd: e.cwd ?? t.cwd,
    detectedFiles: IC(e.detectedFiles, t.detectedFiles),
    lastActivityAtMs: e.lastActivityAtMs ?? t.lastActivityAtMs,
    remoteMcpServerNames: FC(e.remoteMcpServerNames, t.remoteMcpServerNames),
    scheduledTaskId: e.scheduledTaskId ?? t.scheduledTaskId,
    spaceId: e.spaceId ?? t.spaceId,
    storageDirs: [...e.storageDirs, ...t.storageDirs],
    title: e.title ?? t.title,
    userSelectedFolders: FC(e.userSelectedFolders, t.userSelectedFolders),
  };
}
function FC(e, t) {
  return [...new Set([...e, ...t])];
}
function IC(e, t) {
  let n = new Map(e.map((e) => [e.hostPath, e]));
  for (let e of t) n.has(e.hostPath) || n.set(e.hostPath, e);
  return [...n.values()];
}
async function LC(e) {
  let t = await jb(e, yx);
  if (t == null) return null;
  let n = t.sessionId ?? t.id ?? s.default.basename(e, `.json`);
  return {
    cliSessionIds: t.cliSessionId == null ? [] : [t.cliSessionId],
    cwd: t.cwd ?? null,
    detectedFiles: (t.fsDetectedFiles ?? []).map((e) => ({
      fileName: e.fileName,
      hostPath: e.hostPath,
      timestampMs: Mx(e.timestamp ?? null),
    })),
    lastActivityAtMs: Mx(t.lastActivityAt ?? t.updatedAt ?? t.createdAt ?? null),
    manifestPath: e,
    remoteMcpServerNames: kb(t.remoteMcpServersConfig),
    scheduledTaskId: t.scheduledTaskId ?? null,
    sessionId: n,
    spaceId: t.spaceId ?? null,
    storageDirs: [e.slice(0, -s.default.extname(e).length)],
    title: t.title ?? null,
    userSelectedFolders: vC(t.userSelectedFolders),
  };
}
async function RC(e, t, n) {
  let r = await zC({
      disabledServerNames: sS(n ?? (t == null ? {} : await iS(e, t))),
      project: e,
      warnings: t,
    }),
    i = (
      await Promise.all(
        [
          ...kC.map((t) => ({ ...t, roots: e.accountOrgDirs })),
          ...AC.map((t) => ({ ...t, roots: e.userDataDirs })),
        ].map(async ({ kind: e, relativePath: t, roots: n }) => {
          let [r] = await nS(n, t);
          return r == null ? null : { kind: e, path: r };
        }),
      )
    ).filter((e) => e != null);
  if (e.homeDir != null) {
    let t = s.default.join(e.homeDir, `.operon`);
    (await Lb(t)) && i.push({ kind: `operon`, path: t });
  }
  return [...r, ...i];
}
async function zC({ disabledServerNames: e, project: t, warnings: n }) {
  let r = await oS(t);
  return (
    await Promise.all(
      r.map(async ({ parsed: r, sourcePath: i }) =>
        r == null
          ? (n?.push(`Could not read Cowork MCP config file: ${i}`), null)
          : Object.entries({ ...r.mcpServers, ...r.mcp_servers }).some(([t, n]) => {
                let r = t.trim();
                return r !== `` && !cS({ disabledServerNames: e, serverConfig: n, serverName: r });
              })
            ? {
                kind: t.userDataDirs.some(
                  (e) => i === s.default.join(e, `claude_desktop_config.json`),
                )
                  ? `settings`
                  : `mcp`,
                path: i,
              }
            : null,
      ),
    )
  ).filter((e) => e != null);
}
async function BC(e) {
  return (await Ib(e))
    ? (await (0, p.readdir)(e, { withFileTypes: !0 }))
        .filter((e) => e.isFile() && e.name.endsWith(`.json`))
        .map((t) => s.default.join(e, t.name))
    : [];
}
var VC = new Set([
    `csv`,
    `doc`,
    `docx`,
    `htm`,
    `html`,
    `jpeg`,
    `jpg`,
    `jsx`,
    `md`,
    `mdx`,
    `mermaid`,
    `pdf`,
    `png`,
    `ppt`,
    `pptx`,
    `svg`,
    `tsv`,
    `txt`,
    `webp`,
    `xls`,
    `xlsm`,
    `xlsx`,
  ]),
  HC = t
    .$s({
      createdBySessionId: t.nc().nullable().optional(),
      id: t.nc(),
      lastModifiedBySessionId: t.nc().nullable().optional(),
      name: t.nc().nullable().optional(),
    })
    .passthrough(),
  UC = t.rc([t.Gs(HC), t.$s({ artifacts: t.Gs(HC) }).passthrough()]);
async function WC(e) {
  let [t, n] = await Promise.all([KC(e), YC(e)]);
  return [...t, ...n];
}
async function GC({ candidate: e, outputDirectory: t, sources: n }) {
  if (n.length === 0) return [];
  await (0, p.mkdir)(t, { recursive: !0 });
  let r = new Set();
  return (
    await Promise.all(
      n.map((n) =>
        n.kind === `output`
          ? XC({ candidate: e, outputDirectory: t, source: n, usedRelativePaths: r })
          : ZC({ outputDirectory: t, source: n, usedRelativePaths: r }),
      ),
    )
  ).filter((e) => e != null);
}
async function KC(e) {
  let t = await QC(e.sessionStorageDirs),
    n = await qC(e);
  return nw(
    (
      await Promise.all(
        n.map(async (e) =>
          !JC(e.relativePath) || (await $C(e.sourcePath, t)) || !(await Fb(e.sourcePath))
            ? null
            : {
                kind: `output`,
                label: s.default.basename(e.relativePath),
                relativePath: e.relativePath,
                sourcePath: e.sourcePath,
              },
        ),
      )
    ).filter((e) => e != null),
  );
}
async function qC(e) {
  let t = e.detectedFiles.flatMap((e) => {
      let t = iw(e.fileName, e.hostPath);
      return t == null ? [] : [{ relativePath: t, sourcePath: e.hostPath }];
    }),
    n = (
      await Promise.all(
        e.sessionStorageDirs.map(async (e) => {
          let t = s.default.join(e, `outputs`);
          return (await Pb(t)).flatMap((e) => {
            let n = iw(s.default.relative(t, e), e);
            return n == null ? [] : [{ relativePath: n, sourcePath: e }];
          });
        }),
      )
    ).flat(),
    r = new Map();
  for (let e of n) {
    let t = rw(e.relativePath);
    r.has(t) || r.set(t, e);
  }
  return [...r.values(), ...t];
}
function JC(e) {
  let t = s.default.extname(e).slice(1).toLowerCase();
  return VC.has(t);
}
async function YC(e) {
  let t = s.default.join(e.documentsDir, `Claude`, `Artifacts`),
    n = new Map(),
    r = await Promise.all(e.accountOrgDirs.map((e) => jb(s.default.join(e, `artifacts.json`), UC)));
  for (let e of r.flatMap((e) => (e == null ? [] : Array.isArray(e) ? e : e.artifacts))) {
    let r = rw(s.default.join(t, e.id));
    n.has(r) || n.set(r, e);
  }
  return (
    await Promise.all(
      [...n.values()].map(async (n) => {
        if (n.createdBySessionId !== e.sourceId && n.lastModifiedBySessionId !== e.sourceId)
          return null;
        let r = s.default.join(t, n.id);
        return n.id.trim() === `` || !Sb(r, t) || !(await Ib(r))
          ? null
          : { artifactId: n.id, kind: `artifact`, label: n.name ?? n.id, sourceDirectory: r };
      }),
    )
  ).filter((e) => e != null);
}
async function XC({ candidate: e, outputDirectory: t, source: n, usedRelativePaths: r }) {
  if (e.workspaceKind === `project` && Sb(n.sourcePath, e.projectRoot))
    return { kind: `output`, label: n.label, targetPath: n.sourcePath };
  let i = ow(n.relativePath, r),
    a = s.default.join(t, i);
  return Sb(a, t)
    ? (xb(n.sourcePath, a) ||
        (await (0, p.mkdir)(s.default.dirname(a), { recursive: !0 }),
        await (0, p.copyFile)(n.sourcePath, a)),
      { kind: `output`, label: n.label, targetPath: a })
    : null;
}
async function ZC({ outputDirectory: e, source: t, usedRelativePaths: n }) {
  let r = ow(s.default.join(`artifacts`, aw(t.artifactId)), n),
    i = s.default.join(e, r);
  if (!Sb(i, e)) return null;
  (await (0, p.mkdir)(s.default.dirname(i), { recursive: !0 }),
    await (0, p.cp)(t.sourceDirectory, i, { force: !0, recursive: !0 }));
  let a = s.default.join(i, `index.html`);
  return { kind: `artifact`, label: t.label, targetPath: (await Fb(a)) ? a : i };
}
async function QC(e) {
  let t = e.map((e) => s.default.join(e, `uploads`)),
    n = (await Promise.all(t.map((e) => Pb(e)))).flat(),
    r = await Promise.all(n.map(async (e) => ({ fileKey: await ew(e), realPath: await tw(e) })));
  return {
    fileKeys: new Set(r.flatMap(({ fileKey: e }) => e ?? [])),
    realPaths: new Set(r.flatMap(({ realPath: e }) => e ?? [])),
    roots: t,
  };
}
async function $C(e, t) {
  if (t.roots.some((t) => Sb(e, t))) return !0;
  let [n, r] = await Promise.all([ew(e), tw(e)]);
  return (n != null && t.fileKeys.has(n)) || (r != null && t.realPaths.has(r));
}
async function ew(e) {
  try {
    let t = await (0, p.stat)(e);
    return `${t.dev}:${t.ino}`;
  } catch {
    return null;
  }
}
async function tw(e) {
  try {
    return await (0, p.realpath)(e);
  } catch {
    return null;
  }
}
function nw(e) {
  let t = new Set(),
    n = [];
  for (let r of e) {
    let e = rw(s.default.resolve(r.sourcePath));
    t.has(e) || (t.add(e), n.push(r));
  }
  return n;
}
function rw(e) {
  let t = s.default.normalize(e);
  return process.platform === `win32` ? t.toLowerCase() : t;
}
function iw(e, t) {
  let n = e.replaceAll(`\\`, `/`);
  if (n.startsWith(`/`) || /^[A-Za-z]:\//.test(n)) return aw(s.default.basename(t));
  let r = n.split(`/`).filter((e) => e !== ``);
  return r.length === 0 || r.some((e) => e === `..`) ? null : s.default.join(...r.map(aw));
}
function aw(e) {
  let t = Array.from(e)
    .map((e) => (e.charCodeAt(0) <= 31 || `<>:"|?*`.includes(e) ? `-` : e))
    .join(``)
    .trim();
  return t === `` || t === `.` || t === `..` ? `file` : t;
}
function ow(e, t) {
  let n = s.default.normalize(e),
    r = s.default.extname(n),
    i = n.slice(0, n.length - r.length),
    a = n,
    o = 2;
  for (; t.has(rw(a)); ) ((a = `${i}-${o}${r}`), (o += 1));
  return (t.add(rw(a)), a);
}
function sw(e) {
  let t = cw(e);
  if (t.length === 0) return null;
  let n = t.filter((e) => e.kind === `output`),
    r = t.filter((e) => e.kind === `artifact`);
  return [
    ...(n.length === 0 ? [] : [`Imported Cowork generated files:`, ...n.map((e) => lw(e))]),
    ...(r.length === 0
      ? []
      : [
          ...(n.length === 0 ? [] : [``]),
          `Imported Cowork live artifacts:`,
          ...r.map((e) => lw(e)),
        ]),
  ].join(`
`);
}
function cw(e) {
  let t = new Set(),
    n = [];
  for (let r of e) {
    let e = `${r.kind}\u001f${r.targetPath}`;
    t.has(e) || (t.add(e), n.push(r));
  }
  return n;
}
function lw(e) {
  return `- [${uw(e.label)}](<${e.targetPath}>)`;
}
function uw(e) {
  return e.replaceAll(`\\`, `\\\\`).replaceAll(`[`, `\\[`).replaceAll(`]`, `\\]`);
}
var dw = 2e3,
  fw = 4e3,
  pw = `external_agent_tool_call`,
  mw = `external_agent_tool_result`,
  hw = t
    .$s({
      content: t.ic().optional(),
      isMeta: t.Ks().optional(),
      isSidechain: t.Ks().optional(),
      message: t.ic().optional(),
      role: t.nc().optional(),
      timestamp: t.rc([t.Qs(), t.nc()]).nullable().optional(),
      type: t.nc().optional(),
    })
    .passthrough();
async function gw(e) {
  let t = await (0, p.readFile)(e, `utf8`),
    n = [],
    r = null,
    i = !1;
  for (let e of t.split(/\r?\n/)) {
    if (e.trim() === ``) continue;
    let t = vw(e);
    if (t == null || t.isMeta === !0 || t.isSidechain === !0) continue;
    let a = yw(t);
    if (a == null) continue;
    let o = Mx(t.timestamp ?? null);
    ((i ||= ((r = o), !0)), n.push({ ...a, timestampMs: o }));
  }
  return { firstTimestampMs: r, messages: n };
}
async function _w(e) {
  return (await gw(e)).messages.some((e) => e.role === `user`);
}
function vw(e) {
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return null;
  }
  let n = hw.safeParse(t);
  return n.success ? n.data : null;
}
function yw(e) {
  let t = bw(e);
  if (t == null) return null;
  let n = xw(Ax(e.message) ? e.message.content : e.content);
  return n.text === `` && n.images.length === 0
    ? null
    : {
        images: t === `user` ? n.images : [],
        role: t === `assistant` || n.onlyToolResult ? `assistant` : `user`,
        text: n.text,
      };
}
function bw(e) {
  let t = e.role ?? e.type;
  return t === `user` ? `user` : t === `assistant` ? `assistant` : null;
}
function xw(e) {
  if (typeof e == `string`) return { images: [], onlyToolResult: !1, text: e.trim() };
  if (!Array.isArray(e)) return { images: [], onlyToolResult: !1, text: `` };
  let t = [],
    n = [],
    r = e.length > 0;
  for (let i of e) {
    let e = Sw(i);
    (t.push(...e.images), e.onlyToolResult || (r = !1), e.text !== `` && n.push(e.text));
  }
  return {
    images: t,
    onlyToolResult: r,
    text: n
      .join(`

`)
      .trim(),
  };
}
function Sw(e) {
  if (typeof e == `string`) return { images: [], onlyToolResult: !1, text: e };
  if (!Ax(e)) return { images: [], onlyToolResult: !0, text: `` };
  let t = jx(e.type);
  return t === `text`
    ? { images: [], onlyToolResult: !1, text: jx(e.text) ?? `` }
    : t === `tool_use`
      ? { images: [], onlyToolResult: !1, text: Tw(e) }
      : t === `tool_result`
        ? { images: [], onlyToolResult: !0, text: Ew(e) }
        : t === `image`
          ? Cw(e)
          : t === `thinking`
            ? { images: [], onlyToolResult: !0, text: `` }
            : t == null
              ? { images: [], onlyToolResult: !1, text: jx(e.text) ?? `` }
              : { images: [], onlyToolResult: !1, text: `[external unsupported block: ${t}]` };
}
function Cw(e) {
  let t = Ax(e.source) ? e.source : null,
    n = t == null ? null : jx(t.type),
    r = t == null ? null : jx(t.media_type),
    i = t == null ? null : jx(t.data);
  return n !== `base64` || r == null || !r.toLowerCase().startsWith(`image/`) || i == null
    ? { images: [], onlyToolResult: !1, text: `[external unsupported block: image]` }
    : { images: [`data:${ww(r)};base64,${i}`], onlyToolResult: !1, text: `` };
}
function ww(e) {
  let t = e.toLowerCase().split(`;`)[0]?.trim() ?? ``;
  return t.startsWith(`image/`) ? t : `image/png`;
}
function Tw(e) {
  let t = [`[${pw}: ${jx(e.name) ?? `unknown`}]`],
    n = e.input;
  if (Ax(n)) {
    let e = jx(n.description);
    e != null && t.push(`description: ${e}`);
    let r = jx(n.command);
    r != null && t.push(`command: ${r}`);
    let i = jx(n.file_path ?? n.file);
    (i != null && t.push(`file: ${i}`), t.length === 1 && t.push(`input: ${Aw(kw(n), dw)}`));
  } else n != null && t.push(`input: ${Aw(kw(n), dw)}`);
  return (
    t.push(`[/${pw}]`),
    t.join(`
`)
  );
}
function Ew(e) {
  let t = e.is_error === !0 ? `[${mw}: error]` : `[${mw}]`,
    n = Dw(e.content);
  return n === `` ? `${t}\n[/${mw}]` : `${t}\n${Aw(n, fw)}\n[/${mw}]`;
}
function Dw(e) {
  return typeof e == `string`
    ? e
    : Array.isArray(e)
      ? Ow(e).join(`
`)
      : ``;
}
function Ow(e) {
  return e
    .flatMap((e) => {
      if (typeof e == `string`) return [e];
      if (!Ax(e)) return [];
      let t = jx(e.text);
      return [...(t == null ? [] : [t]), ...(Array.isArray(e.content) ? Ow(e.content) : [])];
    })
    .filter((e) => e !== ``);
}
function kw(e) {
  let t = JSON.stringify(e);
  return typeof t == `string` ? t : String(e);
}
function Aw(e, t) {
  return e.length <= t ? e : `${e.slice(0, t - 1)}…`;
}
async function jw({
  candidate: e,
  codexHome: t,
  cwd: n,
  projectlessOutputDirectory: r,
  projectlessWorkspaceRoot: i,
  stagedGeneratedFiles: a,
}) {
  let o = (0, u.randomUUID)(),
    c = s.default.join(t, `claude-cowork-transcript-imports`, o),
    l = await Nw(e.sourceJsonlPath, { stagedGeneratedFiles: a });
  if (l.items.length === 0) return null;
  let d = Pw({ firstTimestampMs: l.firstTimestampMs, lastActivityAtMs: e.lastActivityAtMs }),
    f = s.default.join(c, `rollout-${o}.jsonl`),
    m = [
      {
        payload: {
          agent_nickname: null,
          agent_path: null,
          agent_role: null,
          base_instructions:
            e.workspaceKind === `projectless` ? Mw({ cwd: n, outputDirectory: r ?? n }) : null,
          cli_version: `0.0.0`,
          cwd: n,
          dynamic_tools: null,
          id: o,
          memory_mode: null,
          model_provider: null,
          originator: `Claude Cowork`,
          source: `vscode`,
          timestamp: d,
        },
        timestamp: d,
        type: `session_meta`,
      },
      ...l.items.map((e) => ({ payload: e.payload, timestamp: d, type: e.type })),
    ];
  return (
    await (0, p.mkdir)(c, { recursive: !0 }),
    await (0, p.writeFile)(
      f,
      `${m.map((e) => JSON.stringify(e)).join(`
`)}\n`,
      `utf8`,
    ),
    {
      contentSha256: e.contentSha256,
      cwd: n,
      lastActivityAtMs: e.lastActivityAtMs,
      projectlessOutputDirectory: r,
      projectlessWorkspaceRoot: i,
      projectRoot: e.projectRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      rolloutPath: f,
      rolloutRoot: c,
      sourceId: e.sourceId,
      sourceJsonlPath: e.sourceJsonlPath,
      sourceThreadId: o,
      stagedGeneratedFiles: a,
      title: e.title,
      workspaceKind: e.workspaceKind,
    }
  );
}
function Mw({ cwd: e, outputDirectory: t }) {
  return {
    text: [
      `### Projectless Chat`,
      `This projectless thread starts in a generated directory under the user's Documents/Codex folder.`,
      `Prefer answering inline in chat unless using local files would make the result more useful.`,
      ...(t === e
        ? [
            `When using local files for this projectless thread, write scratch files, drafts, generated assets, and other outputs under ${t}. Do not write directly in the home directory unless the user explicitly asks.`,
          ]
        : [
            `Use work/ for intermediate files, scratch analysis, scripts, drafts, and temporary assets.`,
            `Use ${t} only for user-facing deliverables that should appear as outputs.`,
            `When referring to saved deliverables in the final response, link only files from ${t}. Do not write directly in the home directory unless the user explicitly asks.`,
          ]),
    ].join(`
`),
  };
}
async function Nw(e, t = {}) {
  let n = await gw(e),
    r = Fw();
  r.firstTimestampMs = n.firstTimestampMs;
  for (let e of n.messages)
    ((r.lastMessageTimestampMs = e.timestampMs),
      Iw({ images: e.images, role: e.role, state: r, text: e.text, timestampMs: e.timestampMs }));
  return (
    Lw(r, t.stagedGeneratedFiles ?? []),
    Rw(r),
    { firstTimestampMs: r.firstTimestampMs, items: r.items }
  );
}
function Pw({ firstTimestampMs: e, lastActivityAtMs: t }) {
  return new Date(e ?? t ?? 0).toISOString();
}
function Fw() {
  return {
    currentTurn: null,
    firstTimestampMs: null,
    items: [],
    lastAssistantAgentMessagePayload: null,
    lastAssistantResponseItem: null,
    lastAssistantResponseItemCount: 0,
    lastMessageTimestampMs: null,
    responseItems: [],
    userTurnCount: 0,
  };
}
function Iw({ images: e, role: t, state: n, text: r, timestampMs: i }) {
  if (t === `user`) {
    (n.currentTurn != null && n.items.push(Hw(n.currentTurn, null)), (n.userTurnCount += 1));
    let a = `external-import-turn-${n.userTurnCount}`,
      o = Uw(i);
    n.items.push({
      payload: {
        collaboration_mode_kind: `default`,
        model_context_window: null,
        ...(o == null ? {} : { started_at: o }),
        turn_id: a,
        type: `task_started`,
      },
      type: `event_msg`,
    });
    let s = zw({ role: t, text: r });
    (n.responseItems.push(s),
      n.items.push({ payload: s, type: `response_item` }),
      n.items.push({
        payload: {
          ...(e.length === 0 ? {} : { images: e }),
          local_images: [],
          message: r,
          text_elements: [],
          type: `user_message`,
        },
        type: `event_msg`,
      }),
      (n.currentTurn = { lastAgentMessage: null, turnId: a }));
    return;
  }
  if (n.currentTurn == null) return;
  let a = zw({ role: t, text: r });
  (n.responseItems.push(a),
    (n.lastAssistantResponseItem = a),
    (n.lastAssistantResponseItemCount = n.responseItems.length),
    n.items.push({ payload: a, type: `response_item` }));
  let o = { memory_citation: null, message: r, phase: null, type: `agent_message` };
  (n.items.push({ payload: o, type: `event_msg` }),
    (n.lastAssistantAgentMessagePayload = o),
    (n.currentTurn.lastAgentMessage = r));
}
function Lw(e, t) {
  let n = sw(t);
  if (
    n == null ||
    e.currentTurn == null ||
    e.lastAssistantAgentMessagePayload == null ||
    e.lastAssistantResponseItem == null
  )
    return;
  let r = `${e.lastAssistantAgentMessagePayload.message.trimEnd()}\n\n${n}`;
  e.lastAssistantAgentMessagePayload.message = r;
  let i = e.lastAssistantResponseItem.content[0];
  (i != null && (i.text = r), (e.currentTurn.lastAgentMessage = r));
}
function Rw(e) {
  e.currentTurn != null &&
    (e.items.push(Bw(e.responseItems.slice(0, e.lastAssistantResponseItemCount))),
    e.items.push(Hw(e.currentTurn, e.lastMessageTimestampMs)));
}
function zw(e) {
  return {
    content: [
      e.role === `assistant`
        ? { text: e.text, type: `output_text` }
        : { text: e.text, type: `input_text` },
    ],
    role: e.role,
    type: `message`,
  };
}
function Bw(e) {
  let t = {
    cached_input_tokens: 0,
    input_tokens: 0,
    output_tokens: 0,
    reasoning_output_tokens: 0,
    total_tokens: e.map(Vw).reduce((e, t) => e + t, 0),
  };
  return {
    payload: {
      info: { last_token_usage: t, model_context_window: null, total_token_usage: t },
      rate_limits: null,
      type: `token_count`,
    },
    type: `event_msg`,
  };
}
function Vw(e) {
  return Math.ceil(JSON.stringify(e).length / 4);
}
function Hw(e, t) {
  let n = Uw(t);
  return {
    payload: {
      ...(n == null ? {} : { completed_at: n }),
      last_agent_message: e.lastAgentMessage,
      turn_id: e.turnId,
      type: `task_complete`,
    },
    type: `event_msg`,
  };
}
function Uw(e) {
  return e == null ? null : Math.floor(e / 1e3);
}
async function Ww({ importHistory: e, limits: t, project: n, sessions: r }) {
  return Kw({
    getCandidate: (e) => ({
      accountOrgDir: n.accountOrgDir,
      accountOrgDirs: n.accountOrgDirs,
      contentSha256: e.contentSha256,
      detectedFiles: e.detectedFiles,
      documentsDir: n.documentsDir,
      lastActivityAtMs: e.lastActivityAtMs,
      projectRoot: n.sourceRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      sessionStorageDirs: e.storageDirs,
      sourceId: e.sessionId,
      sourceJsonlPath: e.rawTranscriptPath,
      title: e.title,
      workspaceKind: `project`,
    }),
    importHistory: e,
    importedProjectRoot: n.sourceRoot,
    selectSession: (e) =>
      qw(e, n) ? (Qw(e.lastActivityAtMs, t) ? `too-old` : `include`) : `outside-project`,
    sessions: r,
  });
}
async function Gw({ importHistory: e, limits: t, projectlessSpace: n, sessions: r }) {
  return Kw({
    getCandidate: (e) => ({
      accountOrgDir: n.accountOrgDir,
      accountOrgDirs: n.accountOrgDirs,
      contentSha256: e.contentSha256,
      detectedFiles: e.detectedFiles,
      documentsDir: n.documentsDir,
      lastActivityAtMs: e.lastActivityAtMs,
      projectRoot: n.projectlessImportRoot,
      remoteMcpServerNames: e.remoteMcpServerNames,
      sessionStorageDirs: e.storageDirs,
      sourceId: e.sessionId,
      sourceJsonlPath: e.rawTranscriptPath,
      title: e.title ?? n.name,
      workspaceKind: `projectless`,
    }),
    importHistory: e,
    importedProjectRoot: n.projectlessImportRoot,
    selectSession: (e) =>
      Jw(e, n) ? (Qw(e.lastActivityAtMs, t) ? `too-old` : `include`) : `ignore`,
    sessions: r,
  });
}
async function Kw({
  getCandidate: e,
  importHistory: t,
  importedProjectRoot: n,
  selectSession: r,
  sessions: i,
}) {
  let a = [],
    o = [];
  for (let e of i) {
    let t = r(e);
    if (t === `include`) {
      o.push(e);
      continue;
    }
    t !== `ignore` && a.push({ manifestPath: e.manifestPath, reason: t, sessionId: e.sessionId });
  }
  let s = (await Promise.all(o.map((e) => Xw(e)))).flatMap((r) =>
    r.rawTranscriptPath == null
      ? (a.push({ manifestPath: r.manifestPath, reason: r.skippedReason, sessionId: r.sessionId }),
        [])
      : t != null && Zw(t, n, r)
        ? (a.push({
            manifestPath: r.manifestPath,
            reason: `already-imported`,
            sessionId: r.sessionId,
          }),
          [])
        : [e(r)],
  );
  return (s.sort($w), { skippedSessions: a, transcriptCandidates: s });
}
function qw(e, t) {
  return e.spaceId === t.spaceId || (e.cwd != null && Sb(e.cwd, t.sourceRoot))
    ? !0
    : e.userSelectedFolders.some((e) => Sb(e, t.sourceRoot));
}
function Jw(e, t) {
  return (
    (t.spaceId == null ? e.spaceId == null : e.spaceId === t.spaceId) &&
    Yw(e) &&
    e.userSelectedFolders.length === 0
  );
}
function Yw(e) {
  if (e.cwd == null || e.cwd === ``) return !0;
  let t = s.default.posix.normalize(e.cwd);
  if (
    e.storageDirs.some((t) => Sb(e.cwd ?? ``, t)) ||
    t === `/sessions` ||
    t.startsWith(`/sessions/`)
  )
    return !0;
  let n = s.default.win32.isAbsolute(e.cwd) ? s.default.win32 : s.default.posix,
    r = n.normalize(e.cwd).split(n.sep),
    i = r.findIndex((e) => e.toLowerCase() === `local-agent-mode-sessions`);
  return i >= 0 && r.lastIndexOf(e.sessionId) > i;
}
async function Xw(e) {
  let t = await eT(e.storageDirs, e.cliSessionIds);
  if (t.length === 0)
    return {
      ...e,
      contentSha256: null,
      rawTranscriptPath: null,
      skippedReason: `missing-transcript`,
    };
  let n = (await Promise.all(t.map(_w))).findIndex((e) => e);
  if (n >= 0) {
    let r = t[n];
    return { ...e, contentSha256: await tT(r), rawTranscriptPath: r };
  }
  return {
    ...e,
    contentSha256: null,
    rawTranscriptPath: null,
    skippedReason: `no-importable-turns`,
  };
}
function Zw(e, t, n) {
  return e.records.some(
    (e) =>
      xb(e.projectRoot, t) && e.sourceId === n.sessionId && e.contentSha256 === n.contentSha256,
  );
}
function Qw(e, t) {
  return e != null && e < t.nowMs - t.maxSessionAgeMs;
}
function $w(e, t) {
  return (t.lastActivityAtMs ?? 0) - (e.lastActivityAtMs ?? 0);
}
async function eT(e, t) {
  let n = (
    await Promise.all(
      e.flatMap((e) => [`transcripts`, `projects`].map((t) => Pb(s.default.join(e, `.claude`, t)))),
    )
  ).flat();
  return t.flatMap((e) => n.filter((t) => s.default.basename(t) === `${e}.jsonl`));
}
async function tT(e) {
  return (0, u.createHash)(`sha256`)
    .update(await (0, p.readFile)(e))
    .digest(`hex`);
}
async function nT({ detection: e, importHistory: t, include: n = dx, items: r, limits: i }) {
  let a = gT(i),
    [o, s] = await Promise.all([
      Promise.all(
        e.projects.map(async (i) => {
          let o = sT({ importHistory: t, include: n, items: r, project: i }),
            s = cT({ importHistory: t, include: n, items: r, project: i }),
            c = lT({ importHistory: t, include: n, items: r, project: i });
          if (o.size === 0 && !s && !c) return null;
          let { skippedSessions: l, transcriptCandidates: u } = o.has(`SESSIONS`)
              ? await Ww({
                  importHistory: t,
                  limits: a,
                  project: i,
                  sessions: e.sessionManifestsByProjectRoot.get(i.sourceRoot) ?? [],
                })
              : { skippedSessions: [], transcriptCandidates: [] },
            d = mT({ items: r, transcriptCandidates: u, workspaceKind: `project` });
          return o.size === 0 && !s && !c && d.length === 0
            ? null
            : {
                accountSetupSelected: s,
                configSurfaces: s ? (e.configSurfacesByProjectRoot.get(i.sourceRoot) ?? []) : [],
                itemTypes: o,
                pluginCandidates: c
                  ? (e.pluginCandidatesByProjectRoot.get(i.sourceRoot) ?? [])
                  : [],
                pluginSetupSelected: c,
                project: i,
                scheduledTasks: s ? (e.scheduledTasksByProjectRoot.get(i.sourceRoot) ?? []) : [],
                shouldRecordProjectImport: r != null && hT(r, i),
                skippedSessions: l,
                transcriptCandidates: d.slice(0, a.maxSessions),
                warnings: [...e.warnings],
              };
        }),
      ),
      fT({ detection: e, importHistory: t, include: n, items: r, limits: a }),
    ]);
  return { projectlessSpaces: s, projects: o.filter((e) => e != null) };
}
function rT(e) {
  return cC(e);
}
function iT(e) {
  return e.some(dT);
}
function aT(e) {
  return e.some((e) => e.itemType === `SESSIONS`);
}
function oT(e) {
  return e.some(
    (e) =>
      e.itemType === `SESSIONS` &&
      (e.details?.sessions ?? []).some((e) => e.workspaceKind === `projectless`),
  );
}
function sT({ importHistory: e, include: t, items: n, project: r }) {
  if (n != null) {
    let e = new Set(
      n
        .filter((e) => e.cwd != null && e.cwd !== `` && xb(e.cwd, r.sourceRoot))
        .map((e) => e.itemType),
    );
    return (
      n.some(
        (e) =>
          e.itemType === `SESSIONS` &&
          (e.details?.sessions ?? []).some(
            (e) => e.workspaceKind !== `projectless` && xb(e.cwd, r.sourceRoot),
          ),
      ) && e.add(`SESSIONS`),
      e
    );
  }
  let i = [],
    a = e != null && Xb(e, r);
  return (t.agents && !a && i.push(`AGENTS_MD`), t.sessions && i.push(`SESSIONS`), new Set(i));
}
function cT({ importHistory: e, include: t, items: n, project: r }) {
  return n == null
    ? t.config && (e == null || !Zb(e, { accountOrgDir: r.accountOrgDir, itemType: `CONFIG` }))
    : n.some(
        (e) =>
          dT(e) && e.sourcePath != null && e.sourcePath !== `` && xb(e.sourcePath, r.accountOrgDir),
      );
}
function lT({ importHistory: e, include: t, items: n, project: r }) {
  return n == null
    ? t.config && (e == null || !Zb(e, { accountOrgDir: r.accountOrgDir, itemType: `PLUGINS` }))
    : n.some(
        (e) =>
          e.itemType === `PLUGINS` &&
          e.sourcePath != null &&
          e.sourcePath !== `` &&
          xb(e.sourcePath, r.accountOrgDir),
      );
}
function uT(e) {
  return e?.some((e) => e.itemType === `PLUGINS`) ?? !1;
}
function dT(e) {
  return (e.cwd == null || e.cwd === ``) && e.itemType === `CONFIG`;
}
async function fT({ detection: e, importHistory: t, include: n, items: r, limits: i }) {
  return pT({ include: n, items: r })
    ? (
        await Promise.all(
          e.projectlessSpaces.map(async (n) => {
            let { skippedSessions: a, transcriptCandidates: o } = await Gw({
                importHistory: t,
                limits: i,
                projectlessSpace: n,
                sessions:
                  e.sessionManifestsByProjectlessSpaceRoot.get(n.projectlessImportRoot) ?? [],
              }),
              s = mT({ items: r, transcriptCandidates: o, workspaceKind: `projectless` });
            return s.length === 0
              ? null
              : {
                  projectlessSpace: n,
                  skippedSessions: a,
                  transcriptCandidates: s.slice(0, i.maxSessions),
                };
          }),
        )
      ).filter((e) => e != null)
    : [];
}
function pT({ include: e, items: t }) {
  return t == null ? e.sessions : oT(t);
}
function mT({ items: e, transcriptCandidates: t, workspaceKind: n }) {
  if (e == null) return t;
  let r = e.flatMap(
    (e) =>
      e.details?.sessions
        .filter((e) =>
          n === `project` ? e.workspaceKind !== `projectless` : e.workspaceKind === `projectless`,
        )
        .map((e) => e.path) ?? [],
  );
  return t.filter((e) => r.some((t) => xb(t, e.sourceJsonlPath)));
}
function hT(e, t) {
  let n = sT({ importHistory: void 0, include: dx, items: e, project: t });
  return fx.every((e) => n.has(e));
}
function gT(e) {
  return {
    maxSessionAgeMs: e?.maxSessionAgeMs ?? 2592e6,
    maxSessions: e?.maxSessions ?? 50,
    nowMs: e?.nowMs ?? Date.now(),
  };
}
async function _T({
  importHistory: e,
  include: t,
  items: n,
  limits: r,
  options: i,
  project: a,
  workspaceRoots: o = null,
}) {
  let s = xT({
    sourceIndex:
      a == null
        ? await hC(i)
        : { projectlessSpaces: [], projects: [a], skippedProjects: [], warnings: [] },
    workspaceRoots: CT({ items: n, workspaceRoots: o }),
    includeProjectlessSpaces: ST({ items: n, workspaceRoots: o }),
  });
  return {
    importSelection: await nT({
      detection: await jC(s),
      importHistory: e,
      include: t,
      items: n,
      limits: r,
    }),
    sourceIndex: s,
  };
}
async function vT({ codexHome: e, includeHome: t = !0, options: n, workspaceRoots: r = null }) {
  let i = hC(n),
    a = Wb(e),
    [{ projectlessSpaces: o, projects: s, skippedProjects: c, warnings: l }, u] = await Promise.all(
      [i, a],
    ),
    d = await jC(
      xT({
        sourceIndex: { projectlessSpaces: o, projects: s, skippedProjects: c, warnings: l },
        workspaceRoots: r,
        includeProjectlessSpaces: r == null,
      }),
    ),
    f = await nT({
      detection: d,
      include: t ? void 0 : { agents: !0, config: !1, sessions: !1 },
      importHistory: u,
      limits: n,
    });
  return {
    items: uC({ projectlessSpaces: f.projectlessSpaces, projects: f.projects }),
    unsupportedProjects: pC({ skippedProjects: c, workspaceRoots: r }),
    warnings: d.warnings,
  };
}
async function yT({ codexHome: e, items: t, options: n, projectlessThreadPaths: r }) {
  if (rT(t).length === 0 && !iT(t) && !uT(t) && !aT(t))
    return {
      accountSetupItemsToRecord: [],
      importedProjectRoots: [],
      preparedRollouts: [],
      projectsToRecord: [],
    };
  let { importSelection: i } = await _T({
      importHistory: await Wb(e),
      items: t,
      limits: n,
      options: n,
    }),
    a = await aC({ codexHome: e, projects: i.projects }),
    o = [
      ...a.projects.flatMap((e) => e.transcriptCandidates),
      ...i.projectlessSpaces.flatMap((e) => e.transcriptCandidates),
    ],
    s = (
      await Promise.all(o.map((t) => wT({ candidate: t, codexHome: e, projectlessThreadPaths: r })))
    ).filter((e) => e != null);
  return {
    accountSetupItemsToRecord: a.projects.flatMap((e) =>
      AT(e) ? [{ accountOrgDir: e.projectImport.project.accountOrgDir, itemType: `CONFIG` }] : [],
    ),
    importedProjectRoots: OT(a.projects),
    preparedRollouts: s,
    projectsToRecord: a.projects.flatMap((e) =>
      e.projectImport.shouldRecordProjectImport ? [e.projectImport.project] : [],
    ),
  };
}
async function bT({ callbacks: e, codexHome: t, items: n, options: r }) {
  let [i, a] = await Promise.all([
      yT({
        codexHome: t,
        items: n,
        options: r,
        projectlessThreadPaths: e.createProjectlessThreadPaths,
      }),
      DC({ items: n, options: r }),
    ]),
    { importedRollouts: o } = await Rx({
      callbacks: e,
      codexHome: t,
      migration: i,
      pluginImports: a,
    });
  return { importedProjectRoots: i.importedProjectRoots, importedRollouts: o };
}
function xT({ includeProjectlessSpaces: e, sourceIndex: t, workspaceRoots: n }) {
  return n == null
    ? { ...t, projectlessSpaces: e ? t.projectlessSpaces : [] }
    : {
        ...t,
        projectlessSpaces: e ? t.projectlessSpaces : [],
        projects: t.projects.filter((e) => n.some((t) => xb(t, e.sourceRoot))),
      };
}
function ST({ items: e, workspaceRoots: t }) {
  return e == null ? t == null : oT(e);
}
function CT({ items: e, workspaceRoots: t }) {
  if (e != null) {
    let n = rT(e);
    return n.length > 0 ? n : iT(e) ? t : [];
  }
  return t;
}
async function wT({ candidate: e, codexHome: t, projectlessThreadPaths: n }) {
  let r = await WC(e);
  if (e.workspaceKind !== `projectless`) {
    let i =
        r.length === 0
          ? null
          : (await TT({ candidate: e, codexHome: t, projectlessThreadPaths: n })).outputDirectory,
      a = i == null ? [] : await GC({ candidate: e, outputDirectory: i, sources: r });
    return jw({
      candidate: e,
      codexHome: t,
      cwd: e.projectRoot,
      projectlessOutputDirectory: null,
      projectlessWorkspaceRoot: null,
      stagedGeneratedFiles: a,
    });
  }
  if (n == null)
    throw Error(`Projectless Cowork transcript import requires projectless thread paths`);
  let i;
  try {
    i = await n(e);
  } catch (e) {
    if (DT(e)) return null;
    throw e;
  }
  let a = await GC({ candidate: e, outputDirectory: i.outputDirectory, sources: r });
  return jw({
    candidate: e,
    codexHome: t,
    cwd: i.cwd,
    projectlessOutputDirectory: i.outputDirectory,
    projectlessWorkspaceRoot: i.workspaceRoot,
    stagedGeneratedFiles: a,
  });
}
async function TT({ candidate: e, codexHome: t, projectlessThreadPaths: n }) {
  if (n != null) return n(e);
  let r = s.default.join(t, `claude-cowork-generated-files`, ET(e));
  return { cwd: r, outputDirectory: r, workspaceRoot: r };
}
function ET(e) {
  let t = e.sourceId === `` ? `cowork-session` : e.sourceId,
    n =
      t
        .toLowerCase()
        .replace(/[^a-z0-9]+/g, `-`)
        .replace(/^-+|-+$/g, ``)
        .slice(0, 80) || `cowork-session`;
  return `${
    ((e.title?.trim() === `` ? null : e.title) ?? t)
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, `-`)
      .replace(/^-+|-+$/g, ``)
      .slice(0, 80) || n
  }-${n}`;
}
function DT(e) {
  return (
    e instanceof Error && e.message === `Unable to create a unique projectless thread directory`
  );
}
function OT(e) {
  return [...new Set(e.flatMap((e) => (kT(e) ? [e.projectImport.project.sourceRoot] : [])))].sort(
    (e, t) => e.localeCompare(t),
  );
}
function kT(e) {
  return (
    e.projectImport.accountSetupSelected ||
    e.projectImport.pluginSetupSelected ||
    e.projectImport.itemTypes.has(`AGENTS_MD`) ||
    e.projectImport.itemTypes.has(`SESSIONS`)
  );
}
function AT(e) {
  return e.projectImport.accountSetupSelected && e.projectImport.configSurfaces.length > 0;
}
function jT() {
  return {
    pathForAgentEnvironment: (e, n) => (n ? t.Nr(e) : e),
    pathForDesktopHost: (e, n) => (n ? t.Pr(e, t.Fr()) : e),
  };
}
function MT(e) {
  return {
    detect: ({ hostId: t, includeHome: n, workspaceRoots: r }) =>
      NT({
        getMigrationOptions: e.getMigrationOptions,
        getRequestAppServerClient: e.getRequestAppServerClient,
        hostId: t,
        includeHome: n,
        pathAdapters: e.pathAdapters,
        workspaceRoots: r,
      }),
    import: ({ hostId: t, items: n }) => PT({ ...e, hostId: t, items: n }),
  };
}
async function NT({
  getMigrationOptions: e,
  getRequestAppServerClient: t,
  hostId: n,
  includeHome: r = !0,
  pathAdapters: i,
  workspaceRoots: a,
}) {
  let o = await zT({
    getMigrationOptions: e,
    getRequestAppServerClient: t,
    hostId: n,
    pathAdapters: i,
  });
  if (o == null) return { items: [], unsupportedProjects: [] };
  let s = a == null ? null : a.map((e) => i.pathForDesktopHost(e, o.shouldUseWslPaths)),
    c = await vT({ codexHome: o.codexHome, includeHome: r, options: o.options, workspaceRoots: s });
  return {
    items: c.items.map((e) => FT(lC(e, (e) => i.pathForAgentEnvironment(e, o.shouldUseWslPaths)))),
    unsupportedProjects: c.unsupportedProjects.map((e) => ({
      ...cx(e, (e) => i.pathForAgentEnvironment(e, o.shouldUseWslPaths)),
      providerId: sC,
    })),
  };
}
async function PT({
  getAppServerConnection: e,
  getMigrationOptions: t,
  getRequestAppServerClient: n,
  hostId: r,
  items: i,
  pathAdapters: a,
  recordProjectlessThread: o,
}) {
  let s = await zT({
    getMigrationOptions: t,
    getRequestAppServerClient: n,
    hostId: r,
    pathAdapters: a,
  });
  if (s == null) return { projectRoots: [] };
  let c = i.map((e) => lC(IT(e), (e) => a.pathForDesktopHost(e, s.shouldUseWslPaths)));
  return {
    projectRoots: (
      await bT({
        callbacks: LT({
          appServerConnection: e(r),
          appServerClient: n(r),
          homeDirectory: s.options.homeDir,
          hostId: r,
          pathAdapters: a,
          recordProjectlessThread: o,
          shouldUseWslPaths: s.shouldUseWslPaths,
        }),
        codexHome: s.codexHome,
        items: c,
        options: s.options,
      })
    ).importedProjectRoots.map((e) => a.pathForAgentEnvironment(e, s.shouldUseWslPaths)),
  };
}
function FT(e) {
  return {
    ...e,
    details:
      e.details == null
        ? null
        : {
            ...e.details,
            plugins:
              e.details.plugins.length === 0
                ? []
                : [{ marketplaceName: t.Yo, pluginNames: e.details.plugins.map((e) => e.name) }],
          },
  };
}
function IT(e) {
  return {
    ...e,
    details:
      e.details == null
        ? null
        : {
            ...e.details,
            plugins: e.details.plugins.flatMap((e) => e.pluginNames.map((e) => ({ name: e }))),
          },
  };
}
function LT({
  appServerConnection: e,
  appServerClient: t,
  homeDirectory: n,
  hostId: r,
  pathAdapters: i,
  recordProjectlessThread: a,
  shouldUseWslPaths: o,
}) {
  return {
    createProjectlessThreadPaths: async (e) => {
      let r = await hi({
        appServerClient: t,
        createSplitDirectories: !0,
        homeDirectory: RT({
          appServerClient: t,
          homeDirectory: n,
          pathAdapters: i,
          shouldUseWslPaths: o,
        }),
        prompt: e.title,
      });
      return {
        cwd: i.pathForDesktopHost(r.cwd, o),
        outputDirectory: i.pathForDesktopHost(r.outputDirectory, o),
        workspaceRoot: i.pathForDesktopHost(r.workspaceRoot, o),
      };
    },
    forkThread: async (t) => ({ threadId: (await e.forkThread(t)).thread.id }),
    pathForCodexHost: (e) => i.pathForAgentEnvironment(e, o),
    ...(a == null
      ? {}
      : {
          recordProjectlessThread: async ({
            outputDirectory: e,
            threadId: t,
            workspaceRoot: n,
          }) => {
            await a({ hostId: r, outputDirectory: e, threadId: t, workspaceRoot: n });
          },
        }),
    setThreadName: async ({ name: t, threadId: n }) => {
      await e.setThreadName(n, t);
    },
  };
}
function RT({ appServerClient: e, homeDirectory: t, pathAdapters: n, shouldUseWslPaths: r }) {
  if (t != null && t !== ``) return n.pathForAgentEnvironment(t, r);
  if (e.hostConfig.home_dir == null || e.hostConfig.home_dir === ``)
    throw Error(`Projectless Cowork import requires a home directory`);
  return e.hostConfig.home_dir;
}
async function zT({
  getMigrationOptions: e,
  getRequestAppServerClient: n,
  hostId: r,
  pathAdapters: i,
}) {
  let a = n(r);
  if (t.va(a.hostConfig)) return null;
  let o = t.Br(a.hostConfig);
  return {
    codexHome: i.pathForDesktopHost(await a.codexHome(), o),
    options: e(),
    shouldUseWslPaths: o,
  };
}
function BT({
  desktopPaths: e,
  env: t = process.env,
  homeDir: n = (0, o.homedir)(),
  platform: r = process.platform,
}) {
  let i = VT({ desktopPaths: e, homeDir: n, platform: r }),
    a = HT({ env: t, homeDir: n, platform: r });
  return r === `win32`
    ? {
        documentsDir: i,
        fallbackUserDataDirs: [a],
        homeDir: n,
        userDataDir: UT({ env: t, homeDir: n }),
      }
    : { documentsDir: i, homeDir: n, userDataDir: a };
}
function VT({ desktopPaths: e, homeDir: t, platform: n }) {
  return GT(t, e.getPath(`home`), n) ? e.getPath(`documents`) : KT(n).join(t, `Documents`);
}
function HT({ env: e, homeDir: t, platform: n }) {
  if (n === `darwin`) return s.default.posix.join(t, `Library`, `Application Support`, `Claude`);
  if (n === `win32`) {
    let n = e.APPDATA?.trim();
    return n != null && s.default.win32.isAbsolute(n)
      ? s.default.win32.join(n, `Claude`)
      : s.default.win32.join(WT({ env: e, homeDir: t }), `AppData`, `Roaming`, `Claude`);
  }
  return s.default.posix.join(t, `.config`, `Claude`);
}
function UT({ env: e, homeDir: t }) {
  let n = e.LOCALAPPDATA?.trim(),
    r =
      n != null && s.default.win32.isAbsolute(n)
        ? n
        : s.default.win32.join(WT({ env: e, homeDir: t }), `AppData`, `Local`);
  return s.default.win32.join(
    r,
    `Packages`,
    `Claude_pzs8sxrjxfjjc`,
    `LocalCache`,
    `Roaming`,
    `Claude`,
  );
}
function WT({ env: e, homeDir: t }) {
  let n = e.USERPROFILE?.trim();
  return n != null && s.default.win32.isAbsolute(n) ? n : t;
}
function GT(e, t, n) {
  let r = KT(n),
    i = r.resolve(e),
    a = r.resolve(t);
  return n === `win32` ? i.toLowerCase() === a.toLowerCase() : i === a;
}
function KT(e) {
  return e === `win32` ? s.default.win32 : s.default.posix;
}
function qT(e) {
  return MT({ ...e, getMigrationOptions: () => BT({ desktopPaths: a.app }), pathAdapters: jT() });
}
var JT = `ComputerUseAppApprovals.json`,
  YT = `computer-use`,
  XT = `config.toml`,
  ZT = `apps`,
  QT = `allowed`,
  $T = `denied`,
  eE = s.default.join(`Library`, `Application Support`, `Software`),
  tE = s.default.join(`Library`, `Group Containers`),
  nE = `2DC432GLL2.com.openai.sky.CUAService`,
  rE = t.$s({ approvedBundleIdentifiers: t.Gs(t.nc()) }),
  iE = new Map();
async function aE(e = {}) {
  let t = lE(e);
  if (!(0, l.existsSync)(t.filePath)) return null;
  let n = vE(await uE(t));
  return { approvedApps: await xE(n, e), approvedBundleIdentifiers: n };
}
async function oE(e = {}) {
  return (0, l.existsSync)(lE(e).filePath);
}
async function sE(e, t = {}) {
  let n = lE(t),
    r = vE(e);
  return (
    n.format === `toml` ? await mE(n.filePath, r) : await fE(n.filePath, r),
    { approvedApps: await xE(r, t), approvedBundleIdentifiers: r }
  );
}
async function cE(e, t = {}) {
  let n = await aE(t);
  return n == null
    ? null
    : sE(
        n.approvedBundleIdentifiers.filter((t) => t !== e),
        t,
      );
}
function lE(e = {}) {
  let { codexHome: n, homeDirectory: r = (0, o.homedir)(), platform: i = process.platform } = e;
  if (i === `win32`) return { filePath: s.default.join(n ?? t.Mr(), YT, XT), format: `toml` };
  let a = s.default.join(r, tE, nE, eE);
  return { filePath: s.default.join(a, JT), format: `json` };
}
async function uE(e) {
  return e.format === `toml` ? pE(e.filePath) : dE(e.filePath);
}
async function dE(e) {
  let t = {};
  if ((0, l.existsSync)(e))
    try {
      t = JSON.parse(await (0, p.readFile)(e, `utf8`));
    } catch {
      t = {};
    }
  let n = rE.safeParse(t);
  return n.success ? n.data.approvedBundleIdentifiers : [];
}
async function fE(e, t) {
  (await (0, p.mkdir)(s.default.dirname(e), { recursive: !0 }),
    await (0, p.writeFile)(e, JSON.stringify({ approvedBundleIdentifiers: t }, null, 2), `utf8`));
}
async function pE(e) {
  let t = (await hE(e))[ZT];
  return bE(t) ? yE(t[QT]) : [];
}
async function mE(e, t) {
  let n = await hE(e),
    r = _E(n, ZT);
  ((r[QT] = t), (r[$T] = yE(r[$T]).filter((e) => !t.includes(e))), await gE(e, n));
}
async function hE(e) {
  if (!(0, l.existsSync)(e)) return {};
  try {
    let n = t.Or(await (0, p.readFile)(e, `utf8`));
    return bE(n) ? n : {};
  } catch {
    return {};
  }
}
async function gE(e, t) {
  await (0, p.mkdir)(s.default.dirname(e), { recursive: !0 });
  let n = F(t);
  await (0, p.writeFile)(
    e,
    n.endsWith(`
`)
      ? n
      : `${n}\n`,
    `utf8`,
  );
}
function _E(e, t) {
  if (bE(e[t])) return e[t];
  let n = {};
  return ((e[t] = n), n);
}
function vE(e) {
  let t = new Set(),
    n = [];
  for (let r of e) {
    let e = r.trim();
    e !== `` && (t.has(e) || (t.add(e), n.push(e)));
  }
  return n;
}
function yE(e) {
  return Array.isArray(e) ? vE(e.filter((e) => typeof e == `string`)) : [];
}
function bE(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
async function xE(e, t = {}) {
  let n = TE(t.platform);
  return Promise.all(e.map((e) => SE(e, n)));
}
async function SE(e, t) {
  let n = wE(e, t),
    r = iE.get(n);
  if (r != null) return r;
  let i = (await CE(e)) ?? EE(e);
  return (t !== `win32` || i.iconDataURL != null ? iE.set(n, i) : iE.delete(n), i);
}
async function CE(e) {
  try {
    let n = await t.bt(e);
    return n == null
      ? null
      : { bundleIdentifier: e, displayName: n.displayName, iconDataURL: await vl(n.appPath) };
  } catch {
    return null;
  }
}
function wE(e, t) {
  return `${t}:${e}`;
}
function TE(e) {
  return e === `darwin` || e === `win32` ? e : `other`;
}
function EE(e) {
  return { bundleIdentifier: e, displayName: e, iconDataURL: null };
}
var DE = (0, c.promisify)(m.execFile);
async function OE(e) {
  if (process.platform !== `darwin`) return null;
  let t = await AE(e);
  return t == null ? null : kE(t);
}
async function kE(e) {
  let n = await (0, p.mkdtemp)(s.default.join((0, o.tmpdir)(), `codex-app-icon-`)),
    r = s.default.join(n, `icon.png`);
  try {
    await DE(`/usr/bin/sips`, [`-s`, `format`, `png`, e, `--out`, r], {
      env: t.Vr(process.env),
      timeout: 5e3,
    });
    let n = await (0, p.readFile)(r);
    return n.byteLength === 0 ? null : `data:image/png;base64,${n.toString(`base64`)}`;
  } catch {
    return null;
  } finally {
    await (0, p.rm)(n, { force: !0, recursive: !0 });
  }
}
async function AE(e) {
  let n = s.default.join(e, `Contents`, `Info.plist`),
    r = s.default.join(e, `Contents`, `Resources`),
    i = (await t.wt(n, `CFBundleIconFile`)) ?? (await t.wt(n, `CFBundleIconName`)),
    a = i == null ? [] : [i, jE(i)];
  for (let e of [...a, `AppIcon.icns`]) {
    let t = s.default.join(r, e);
    if ((0, l.existsSync)(t)) return t;
  }
  return null;
}
function jE(e) {
  return s.default.extname(e) === `` ? `${e}.icns` : e;
}
var ME = (0, c.promisify)(m.execFile),
  NE = [
    `computer-use`,
    `Codex Computer Use.app`,
    `Contents`,
    `SharedSupport`,
    `Codex Computer Use Installer.app`,
    `Contents`,
    `MacOS`,
    `Codex Computer Use Installer`,
  ],
  PE = s.default.join(
    `/System`,
    `Library`,
    `CoreServices`,
    `CoreTypes.bundle`,
    `Contents`,
    `Resources`,
  ),
  FE = s.default.join(PE, `LockedIcon.icns`),
  IE = `NSComputer`;
async function LE() {
  if (process.platform !== `darwin`) return null;
  try {
    return await BE();
  } catch {
    return !1;
  }
}
async function RE() {
  if (process.platform !== `darwin`) return { computerIconDataURL: null, lockIconDataURL: null };
  let e = a.nativeImage.createFromNamedImage(IE);
  return { computerIconDataURL: e.isEmpty() ? null : e.toDataURL(), lockIconDataURL: await kE(FE) };
}
async function zE(e) {
  return process.platform === `darwin` ? (await VE(e ? `install` : `uninstall`), BE()) : null;
}
async function BE() {
  return (await VE(`status`)).trim() === `OK: installed`;
}
async function VE(e) {
  let { stdout: n } = await ME(HE(), [e], { env: t.Vr(process.env), timeout: 12e4 });
  return n;
}
function HE() {
  return s.default.join(t.Mr(), ...NE);
}
var UE = (0, c.promisify)(m.execFile),
  WE = `com.openai.sky.CUAService`,
  GE = `computerUseSoundMode`;
async function KE() {
  if (process.platform !== `darwin`) return null;
  try {
    let { stdout: e } = await UE(`/usr/bin/defaults`, [`read`, WE, GE]),
      t = e.trim();
    return t === `` ? null : t;
  } catch {
    return null;
  }
}
async function qE(e) {
  return (
    process.platform === `darwin` && (await UE(`/usr/bin/defaults`, [`write`, WE, GE, e])), e
  );
}
var JE = new Map(),
  YE = t.Gr(`computer-use-capture-handler`),
  XE = 12e4,
  ZE = `-10014`,
  QE = /^Codex Computer Use Apple Event error (-?\d+):/,
  $E = /^AE.+ failed with (-?\d+)(?:\s|$)/;
async function eD({
  animationTarget: e,
  bundleIdentifier: t,
  origin: n,
  requestComputerUseCaptureWorker: r,
  requestId: i,
  subscribeComputerUseCaptureWorkerEvent: a,
}) {
  if (t.trim().length === 0 || i.trim().length === 0) return null;
  if (JE.has(i))
    return (
      YE().warning(`Rejected duplicate Codex Computer Use capture request`, {
        safe: { requestId: i },
        sensitive: {},
      }),
      null
    );
  let o = Date.now(),
    s = new AbortController(),
    c = JE.values().next().value?.permissionRequestId ?? i,
    l = aD(i),
    u = a(`update`, (e) => {
      e.requestId === i && tD(e.requestId, e.update);
    });
  (JE.set(i, {
    origin: n,
    abortController: s,
    permissionRequestId: c,
    startedAt: o,
    timeoutId: l,
    unsubscribe: u,
  }),
    YE().debug(`Starting Codex Computer Use capture`, {
      safe: { requestId: i, pendingCaptureCount: JE.size },
      sensitive: {},
    }));
  let d = async () => {
    if (!JE.has(i)) return null;
    try {
      let n = await r({
          method: `start`,
          params: { animationTarget: e, bundleIdentifier: t, permissionRequestId: c, requestId: i },
          signal: s.signal,
        }),
        a = JE.get(i);
      return (
        a != null && (clearTimeout(a.timeoutId), (a.timeoutId = aD(i))),
        YE().debug(`Codex Computer Use capture worker acknowledged start`, {
          safe: { requestId: i, elapsedMs: Date.now() - o, hasResponse: n != null },
          sensitive: {},
        }),
        n
      );
    } catch (e) {
      let t = JE.get(i);
      if (t == null) return null;
      if (oD(e))
        return (
          YE().debug(`Codex Computer Use capture permissions are still pending; retrying start`, {
            safe: { requestId: i },
            sensitive: {},
          }),
          clearTimeout(t.timeoutId),
          (t.timeoutId = aD(i)),
          d()
        );
      let n = sD(e),
        r = n == null ? `start_request_failed` : `start_request_failed:${n.source}:${n.code}`;
      return (
        YE().warning(`Codex Computer Use capture worker failed`, {
          safe: { failureReason: r, requestId: i },
          sensitive: { error: e },
        }),
        iD(i, t, r),
        null
      );
    }
  };
  return d();
}
function tD(e, t) {
  let n = JE.get(e);
  n != null &&
    (YE().debug(`Codex Computer Use capture update received`, {
      safe: { requestId: e, updateType: t.type, elapsedMs: Date.now() - n.startedAt },
      sensitive: {},
    }),
    nD(n.origin, { requestId: e, type: `computer-use-capture-updated`, update: t }),
    (t.type === `completed` || t.type === `failed`) && rD(e, n));
}
function nD(e, t) {
  e.isDestroyed() || e.send(R, t);
}
function rD(e, t) {
  (JE.delete(e), clearTimeout(t.timeoutId), t.abortController.abort(), t.unsubscribe());
}
function iD(e, t, n) {
  (rD(e, t),
    nD(t.origin, {
      requestId: e,
      type: `computer-use-capture-updated`,
      update: { type: `failed`, failureReason: n },
    }));
}
function aD(e) {
  let t = setTimeout(() => {
    let t = JE.get(e);
    t != null &&
      (YE().warning(`Timed out waiting for Codex Computer Use capture`, {
        safe: { requestId: e },
        sensitive: {},
      }),
      iD(e, t, `capture_timed_out`));
  }, XE);
  return (t.unref(), t);
}
function oD(e) {
  let t = sD(e);
  return t?.source === `computer_use` && t.code === ZE;
}
function sD(e) {
  if (!(e instanceof Error)) return null;
  let t = e.message.match(QE)?.[1];
  if (t != null) return { code: t, source: `computer_use` };
  let n = e.message.match($E)?.[1];
  return n == null ? null : { code: n, source: `os_status` };
}
var cD = 12e4,
  lD = t.Gr(`external-agent-import`);
function uD(e) {
  return {
    "external-agent-import-detect": ({
      hostId: t,
      includeHome: n,
      providers: r,
      workspaceRoots: i,
    }) =>
      dD({
        getAppServerConnection: e.getAppServerConnection,
        hostId: t,
        includeHome: n,
        providerAdapters: e.providerAdapters,
        providers: r,
        workspaceRoots: i,
      }),
    "external-agent-import-import": (t) => fD({ ...e, ...t }),
    "external-agent-import-status": (t) => e.getStatus(t),
  };
}
async function dD({
  getAppServerConnection: e,
  hostId: t,
  includeHome: n = !0,
  providerAdapters: r,
  providers: i,
  workspaceRoots: a,
}) {
  let o = Date.now();
  try {
    let s = new Set(i),
      c = r?.[`claude-cowork`],
      [l, u] = await Promise.all([
        s.has(`claude-code`)
          ? vD({
              detect: () =>
                pD({ getAppServerConnection: e, hostId: t, includeHome: n, workspaceRoots: a }),
              providerId: `claude-code`,
              stage: `detect`,
            })
          : Promise.resolve({ items: [] }),
        c != null && SD({ includeHome: n, providerSet: s, workspaceRoots: a })
          ? vD({
              detect: () => c.detect({ hostId: t, includeHome: n, workspaceRoots: a }),
              providerId: `claude-cowork`,
              stage: `detect`,
            }).catch(() => ({ items: [], unsupportedProjects: [] }))
          : Promise.resolve({ items: [], unsupportedProjects: [] }),
      ]),
      d = [...l.items, ...u.items],
      f = {
        items: [
          ...(s.has(`claude-code`) && n && a == null && cC(d).length > 0
            ? await vD({
                detect: async () => ({
                  items: await mD({
                    getAppServerConnection: e,
                    hostId: t,
                    items: l.items,
                    rootItems: d,
                  }),
                }),
                providerId: `claude-code`,
                stage: `detect_project_items`,
              }).then((e) => e.items)
            : l.items),
          ...u.items,
        ],
        unsupportedProjects: u.unsupportedProjects,
      };
    return (
      lD().info(`external_agent_import_detect_finished`, {
        safe: {
          status: `success`,
          durationMs: Date.now() - o,
          includeHome: n,
          requestedProviderIds: i.join(`,`),
          unsupportedProjectCount: f.unsupportedProjects.length,
          workspaceRootCount: a?.length ?? 0,
          ...xD(f.items),
        },
      }),
      f
    );
  } catch (e) {
    throw (
      lD().warning(`external_agent_import_detect_finished`, {
        safe: {
          status: `failure`,
          durationMs: Date.now() - o,
          failureKind: `detect_failed`,
          includeHome: n,
          requestedProviderIds: i.join(`,`),
          workspaceRootCount: a?.length ?? 0,
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function fD({ getAppServerConnection: e, hostId: t, items: n, providerAdapters: r }) {
  if (n.length === 0) return { projectRoots: [] };
  let i = Date.now(),
    a = n.filter(CD),
    o = n.filter(wD),
    s = r?.[`claude-cowork`],
    c = null;
  try {
    a.length > 0 &&
      ((c = `claude-code`),
      await yD({
        importItems: async () => {
          await hD({ appServerConnection: e(t), items: a });
        },
        items: a,
        providerId: `claude-code`,
      }));
    let r;
    o.length > 0 &&
      s != null &&
      ((c = `claude-cowork`),
      (r = await yD({
        importItems: () => s.import({ hostId: t, items: o }),
        items: o,
        providerId: `claude-cowork`,
      })));
    let l = { projectRoots: [...new Set([...cC(a), ...(r?.projectRoots ?? [])])] };
    return (
      lD().info(`external_agent_import_finished`, {
        safe: {
          status: `success`,
          durationMs: Date.now() - i,
          projectRootsReturnedCount: l.projectRoots.length,
          ...xD(n),
        },
      }),
      l
    );
  } catch (e) {
    throw (
      lD().warning(`external_agent_import_finished`, {
        safe: {
          status: `failure`,
          durationMs: Date.now() - i,
          failedProviderId: c,
          failureKind: bD(c),
          ...xD(n),
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function pD({ getAppServerConnection: e, hostId: t, includeHome: n, workspaceRoots: r }) {
  return !n && r == null
    ? { items: [] }
    : {
        items: (
          await e(t).sendAppServerRequest(`externalAgentConfig/detect`, {
            includeHome: n,
            ...(r == null ? {} : { cwds: r }),
          })
        ).items.map(TD),
      };
}
async function mD({ getAppServerConnection: e, hostId: t, items: n, rootItems: r }) {
  let i = cC(r);
  if (i.length === 0) return n;
  let a = await e(t).sendAppServerRequest(`externalAgentConfig/detect`, {
    includeHome: !1,
    cwds: i,
  });
  return DD([...n, ...a.items.map(TD)]);
}
async function hD({ appServerConnection: e, items: t }) {
  let n = t.map(ED),
    r = gD(n) ? _D(e) : null;
  try {
    if (
      (await e.sendAppServerRequest(`externalAgentConfig/import`, { migrationItems: n }), r != null)
    ) {
      let e = Date.now(),
        t = n.filter((e) => e.itemType === `SESSIONS`).length;
      try {
        (await r.promise,
          lD().info(`external_agent_import_session_completion_finished`, {
            safe: { status: `success`, durationMs: Date.now() - e, sessionItemCount: t },
          }));
      } catch (n) {
        throw (
          lD().warning(`external_agent_import_session_completion_finished`, {
            safe: {
              status: `failure`,
              durationMs: Date.now() - e,
              failureKind: `session_completion_timeout`,
              sessionItemCount: t,
            },
            sensitive: { error: n },
          }),
          n
        );
      }
    }
  } catch (e) {
    throw (r?.cancel(), e);
  }
}
function gD(e) {
  return e.some((e) => e.itemType === `SESSIONS`);
}
function _D(e) {
  let t = null,
    n = null,
    r = () => {
      (t != null && (clearTimeout(t), (t = null)), n?.(), (n = null));
    };
  return {
    cancel: r,
    promise: new Promise((i, a) => {
      ((t = setTimeout(() => {
        (r(), a(Error(`Timed out waiting for external agent import completion`)));
      }, cD)),
        (n = e.registerInternalNotificationHandler((e) => {
          e.method === `externalAgentConfig/import/completed` && (r(), i());
        })));
    }),
  };
}
async function vD({ detect: e, providerId: t, stage: n }) {
  let r = Date.now();
  try {
    let i = await e();
    return (
      lD().info(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `success`,
          stage: n,
          providerId: t,
          durationMs: Date.now() - r,
          unsupportedProjectCount: i.unsupportedProjects?.length ?? 0,
          ...xD(i.items),
        },
      }),
      { items: i.items, unsupportedProjects: i.unsupportedProjects ?? [] }
    );
  } catch (e) {
    throw (
      lD().warning(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `failure`,
          stage: n,
          providerId: t,
          durationMs: Date.now() - r,
          failureKind: `detect_failed`,
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
async function yD({ importItems: e, items: t, providerId: n }) {
  let r = Date.now();
  try {
    let i = await e();
    return (
      lD().info(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `success`,
          stage: `import`,
          providerId: n,
          durationMs: Date.now() - r,
          projectRootsReturnedCount: i?.projectRoots.length ?? 0,
          ...xD(t),
        },
      }),
      i
    );
  } catch (e) {
    throw (
      lD().warning(`external_agent_import_provider_stage_finished`, {
        safe: {
          status: `failure`,
          stage: `import`,
          providerId: n,
          durationMs: Date.now() - r,
          failureKind: bD(n),
          ...xD(t),
        },
        sensitive: { error: e },
      }),
      e
    );
  }
}
function bD(e) {
  switch (e) {
    case `claude-code`:
      return `claude_code_import_failed`;
    case `claude-cowork`:
      return `cowork_import_failed`;
    case null:
      return `unknown`;
  }
}
function xD(e) {
  let t = 0,
    n = 0,
    r = 0,
    i = 0,
    a = 0,
    o = new Set();
  for (let s of e)
    (o.add(s.providerId),
      s.providerId === `claude-code` && (t += 1),
      s.providerId === `claude-cowork` && (n += 1),
      s.cwd == null || s.cwd === `` ? (r += 1) : (i += 1),
      s.itemType === `SESSIONS` && (a += 1));
  return {
    claudeCodeItemCount: t,
    claudeCoworkItemCount: n,
    homeItemCount: r,
    itemCount: e.length,
    projectItemCount: i,
    providerIds: [...o].sort().join(`,`),
    sessionItemCount: a,
  };
}
function SD({ includeHome: e, providerSet: t, workspaceRoots: n }) {
  return t.has(`claude-cowork`) && (e || n != null);
}
function CD(e) {
  return e.providerId === `claude-code`;
}
function wD(e) {
  return e.providerId === `claude-cowork`;
}
function TD(e) {
  return {
    cwd: e.cwd,
    description: e.description,
    details: e.details,
    itemType: e.itemType,
    providerId: `claude-code`,
  };
}
function ED(e) {
  return { cwd: e.cwd, description: e.description, details: e.details, itemType: e.itemType };
}
function DD(e) {
  let t = new Set();
  return e.filter((e) => {
    let n = JSON.stringify({
      cwd: e.cwd ?? ``,
      description: e.description,
      details: e.details,
      itemType: e.itemType,
      providerId: e.providerId,
    });
    return t.has(n) ? !1 : (t.add(n), !0);
  });
}
var OD = `external_agent_session_imports.json`,
  kD = t
    .$s({ records: t.Gs(t.$s({ imported_at: t.Qs().optional() }).passthrough()).default([]) })
    .passthrough();
async function AD({ appServerClient: e }) {
  if (t.va(e.hostConfig)) return jD;
  let n = await e.codexHome(),
    r = (await e.platformPath()).join(n, OD),
    i = await t.jr.readFile(r, e).catch(() => null);
  return i == null ? jD : MD(i);
}
var jD = { importedSessionCount: 0, latestImportedAtMs: null };
function MD(e) {
  let t;
  try {
    t = JSON.parse(e);
  } catch {
    return jD;
  }
  let n = kD.safeParse(t);
  if (!n.success) return jD;
  let r = null;
  for (let e of n.data.records) {
    if (e.imported_at == null) continue;
    let t = e.imported_at * 1e3;
    r = r == null ? t : Math.max(r, t);
  }
  return { importedSessionCount: n.data.records.length, latestImportedAtMs: r };
}
var ND = { importedSessionCount: 0, latestImportedAtMs: null };
async function PD({
  appServerClient: e,
  codexHomeForLocalFs: t,
  includeClaudeCode: n,
  includeCowork: r,
}) {
  let [i, a] = await Promise.all([
    n ? AD({ appServerClient: e }) : ND,
    r ? LD({ appServerClient: e, codexHomeForLocalFs: t }) : ND,
  ]);
  return ID([i, a]);
}
async function FD({ appServerClient: e, providers: t }) {
  let n = new Set(t);
  return PD({
    appServerClient: e,
    includeClaudeCode: n.has(`claude-code`),
    includeCowork: n.has(`claude-cowork`),
  });
}
function ID(e) {
  return {
    importedSessionCount: e.reduce((e, t) => e + t.importedSessionCount, 0),
    latestImportedAtMs: e.reduce(
      (e, t) =>
        t.latestImportedAtMs == null
          ? e
          : Math.max(e ?? t.latestImportedAtMs, t.latestImportedAtMs),
      null,
    ),
  };
}
async function LD({ appServerClient: e, codexHomeForLocalFs: n }) {
  return t.va(e.hostConfig) ? ND : Gb(n ?? (await RD(e)));
}
async function RD(e) {
  let n = await e.codexHome();
  return t.Br(e.hostConfig) ? t.Pr(n, t.Fr()) : n;
}
var zD = new Map([
  [`figma`, `connector_figma`],
  [`github`, `connector_github`],
  [`gmail`, `connector_gmail`],
  [`google calendar`, `connector_google_calendar`],
  [`google drive`, `connector_google_drive`],
  [`linear`, `connector_linear`],
  [`notion`, `connector_notion`],
  [`slack`, `connector_slack`],
]);
async function BD({ codexHome: e, providers: t }) {
  return t.includes(`claude-cowork`)
    ? { connectors: (await Wb(e)).connectorCandidates.map(VD) }
    : { connectors: [] };
}
function VD(e) {
  return {
    codexAppId: zD.get(e.name.trim().toLowerCase()) ?? null,
    name: e.name,
    sessionCount: e.sessionCount,
    source: e.source,
  };
}
var HD = 6e4,
  UD = [`scripts`, `compile_latex.py`],
  WD = process.platform === `win32` ? [`python`, `python3`] : [`python3`, `python`];
async function GD(e, t) {
  if (t == null) return { contentsBase64: null };
  let n = new AbortController(),
    r = setTimeout(() => {
      n.abort();
    }, HD),
    i = null;
  try {
    ((i = await (0, p.mkdtemp)(s.default.join(o.default.tmpdir(), `codex-latex-artifact-`))),
      await (0, p.mkdir)(i, { recursive: !0 }));
    let r = await KD({
      abortSignal: n.signal,
      latexPluginPath: t,
      outputDirectory: i,
      sourcePath: e,
    });
    if (r.contentsBase64 != null) return r;
    let a = ZD(t);
    return await YD({
      abortSignal: n.signal,
      outputDirectory: i,
      sourcePath: e,
      tectonicExecutablePath: a,
    });
  } catch {
    return { contentsBase64: null };
  } finally {
    (clearTimeout(r),
      i != null && (await (0, p.rm)(i, { recursive: !0, force: !0 }).catch(() => void 0)));
  }
}
async function KD({ abortSignal: e, latexPluginPath: t, outputDirectory: n, sourcePath: r }) {
  let i = s.default.join(t, ...UD);
  try {
    await (0, p.access)(i);
  } catch {
    return { contentsBase64: null };
  }
  return qD({
    abortSignal: e,
    latexPluginPath: t,
    outputDirectory: n,
    pythonExecutables: WD,
    scriptPath: i,
    sourcePath: r,
  });
}
async function qD({
  abortSignal: e,
  latexPluginPath: t,
  outputDirectory: n,
  pythonExecutables: r,
  scriptPath: i,
  sourcePath: a,
}) {
  let [o, ...s] = r;
  if (o == null) return { contentsBase64: null };
  let c = await JD({
    abortSignal: e,
    latexPluginPath: t,
    outputDirectory: n,
    pythonExecutable: o,
    scriptPath: i,
    sourcePath: a,
  });
  return c.contentsBase64 == null
    ? qD({
        abortSignal: e,
        latexPluginPath: t,
        outputDirectory: n,
        pythonExecutables: s,
        scriptPath: i,
        sourcePath: a,
      })
    : c;
}
async function JD({
  abortSignal: e,
  latexPluginPath: n,
  outputDirectory: r,
  pythonExecutable: i,
  scriptPath: a,
  sourcePath: o,
}) {
  try {
    let s = await t
      .An({
        args: [i, a, o, `--output-directory`, r, `--json`],
        cwd: n,
        spawnInsideWsl: !1,
        signal: e,
      })
      .wait();
    if (s.code !== 0) return { contentsBase64: null };
    let c = XD(s.stdout);
    return c?.pdfExists !== !0 || typeof c.pdfPath != `string`
      ? { contentsBase64: null }
      : { contentsBase64: (await (0, p.readFile)(c.pdfPath)).toString(`base64`) };
  } catch {
    return { contentsBase64: null };
  }
}
async function YD({
  abortSignal: e,
  outputDirectory: n,
  sourcePath: r,
  tectonicExecutablePath: i,
}) {
  let a = s.default.dirname(r),
    o = s.default.basename(r);
  try {
    return (
      await (0, p.access)(i),
      (
        await t
          .An({
            args: [
              i,
              `-X`,
              `compile`,
              `--outdir`,
              n,
              `--outfmt`,
              `pdf`,
              `--print`,
              `--untrusted`,
              o,
            ],
            cwd: a,
            env: { TECTONIC_UNTRUSTED_MODE: `1` },
            collectOutput: !1,
            spawnInsideWsl: !1,
            signal: e,
          })
          .wait()
      ).code === 0
        ? {
            contentsBase64: (
              await (0, p.readFile)(s.default.join(n, o.replace(/\.[^.]+$/u, `.pdf`)))
            ).toString(`base64`),
          }
        : { contentsBase64: null }
    );
  } catch {
    return { contentsBase64: null };
  }
}
function XD(e) {
  try {
    return JSON.parse(e.trim());
  } catch {
    return null;
  }
}
function ZD(e, t = process.platform) {
  return (t === `win32` ? s.default.win32 : s.default.posix).join(
    e,
    `bin`,
    t === `win32` ? `tectonic.exe` : `tectonic`,
  );
}
async function QD(e) {
  try {
    let [t, { marketplaces: n }] = await Promise.all([e.codexHome(), e.listPlugins({})]),
      r = $D({ codexHome: t, marketplaces: n });
    return r == null ? null : (await (0, p.access)(r), r);
  } catch {
    return null;
  }
}
function $D({ codexHome: e, marketplaces: r, marketplaceName: i = t.So(n.i.resolve()) }) {
  for (let n of Ft({ marketplaceName: i, marketplaces: r })) {
    let r = n.plugins.find(
      (e) => e.name === `latex` && e.installed && e.enabled && e.source.type === `local`,
    );
    if (r?.source.type === `local`)
      return t.Eo({
        codexHome: e,
        localVersion: r.localVersion,
        marketplaceName: n.name,
        pluginName: r.name,
      });
  }
  return null;
}
var eO = t.Gr(`download-internal-plugins`);
async function tO({
  config: e,
  largeInternalPluginsConfig: n,
  downloadPlugins: r = t.Ot,
  errorReporter: i,
}) {
  try {
    n == null ? await r(e) : await r(e, void 0, n);
    return;
  } catch (t) {
    throw (
      (t instanceof TypeError && t.message === `fetch failed`) ||
        i.reportNonFatal(t instanceof Error ? t : Error(String(t)), {
          kind: `download-internal-plugins`,
          tags: { version: e.version },
        }),
      eO().error(`Failed to download internal plugins`, {
        safe: { version: e.version },
        sensitive: { error: t },
      }),
      t
    );
  }
}
function nO(e) {
  if (e !== `local`)
    throw Error(`Internal plugins download is currently only supported on the local host.`);
}
async function rO({ query: e, cwd: n, limit: r }) {
  let i = e.trim();
  if (i.length === 0 || !n) return [];
  let a = s.default.resolve(n),
    o = aO();
  return (
    (await t.jt(i, [a], r, { rgPath: o ?? void 0, runCommand: o == null ? iO : void 0 })) ?? []
  );
}
function iO() {
  return Promise.resolve({ stdout: ``, code: 2 });
}
function aO() {
  return t.Kn(process.resourcesPath);
}
function oO(e, t) {
  let n = e.split(`+`).filter(Boolean),
    r = n.findIndex(sO);
  if (r === -1) return e;
  let i = n[r];
  if (i == null) return e;
  let a = cO(i, t);
  return a == null ? e : n.map((e, t) => (t === r ? a : e)).join(`+`);
}
function sO(e) {
  return /^[A-Z0-9]$/i.test(e);
}
function cO(e, t) {
  let n = e.toLowerCase();
  for (let [e, r] of Object.entries(t))
    if (r.toLowerCase() === n) {
      if (/^Key[A-Z]$/.test(e)) return e.slice(3);
      if (/^Digit[0-9]$/.test(e)) return e.slice(5);
    }
  return null;
}
var lO = `bare-modifier-monitor`,
  uO = new Set(),
  dO = new Map([
    [`fn`, `Fn`],
    [`leftoption`, `LeftOption`],
    [`leftalt`, `LeftOption`],
    [`rightoption`, `RightOption`],
    [`rightalt`, `RightOption`],
    [`doubleoption`, `DoubleOption`],
    [`doublealt`, `DoubleOption`],
    [`leftoption+rightoption`, `DoubleOption`],
    [`leftalt+rightalt`, `DoubleOption`],
    [`leftcommand`, `LeftCommand`],
    [`leftcmd`, `LeftCommand`],
    [`leftmeta`, `LeftCommand`],
    [`doublecommand`, `DoubleCommand`],
    [`leftcommand+rightcommand`, `DoubleCommand`],
    [`leftcmd+rightcmd`, `DoubleCommand`],
    [`leftmeta+rightmeta`, `DoubleCommand`],
    [`rightcommand`, `RightCommand`],
    [`rightcmd`, `RightCommand`],
    [`rightmeta`, `RightCommand`],
    [`leftcontrol`, `LeftControl`],
    [`leftctrl`, `LeftControl`],
    [`doubleshift`, `DoubleShift`],
    [`leftshift+rightshift`, `DoubleShift`],
  ]),
  fO = new Set([`rightcontrol`, `rightctrl`, `leftshift`, `rightshift`]);
function pO(e, t, n = `press`) {
  if (process.platform !== `darwin`) return null;
  let r = bO(e);
  if (r == null) return null;
  let i = OO();
  return i == null || uO.has(r) ? null : (uO.add(r), SO(wO(i, r, { trigger: n }), r, t));
}
function mO(e, t = `press`) {
  process.platform !== `darwin` || bO(e) == null || t === `immediatePress` || gO();
}
function hO(e, n = 3e4) {
  if (process.platform !== `darwin`) return Promise.resolve(null);
  let r = bO(e),
    i = OO();
  return r == null || i == null
    ? Promise.resolve(null)
    : (mO(r, `immediatePress`),
      new Promise((e) => {
        let a = !1,
          o = wO(i, r, { immediate: !0 }),
          s = null,
          c = (t) => {
            a || ((a = !0), s != null && clearTimeout(s), o.kill(), e(t));
          };
        ((s = setTimeout(() => {
          c(null);
        }, n)),
          s.unref(),
          DO(o, (e) => {
            switch (e) {
              case `down`:
                c(r);
                return;
              case `permission-denied`:
                (t.Ur().warning(`Bare modifier hotkey monitor needs input monitoring access`),
                  c(null));
                return;
              case `ready`:
              case `up`:
              case ``:
                return;
              default:
                return;
            }
          }),
          o.once(`error`, (e) => {
            (t
              .Ur()
              .warning(`Bare modifier hotkey capture failed`, {
                safe: {},
                sensitive: { error: e },
              }),
              c(null));
          }),
          o.once(`exit`, () => {
            c(null);
          }));
      }));
}
function gO() {
  if (process.platform !== `darwin`) return;
  let e = OO();
  if (e != null)
    try {
      (0, m.spawnSync)(e, [`--request-permission`], { stdio: `ignore`, timeout: 1e4 });
    } catch (e) {
      t.Ur().warning(`Failed to request input monitoring access`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
function _O(e, t) {
  if (process.platform !== `darwin` || e.length === 0) return null;
  let n = OO();
  return n == null ? null : EO(TO(n, e), t);
}
function vO(e, t = process.platform) {
  return t === `darwin` && bO(e) != null;
}
function yO(e) {
  let t = xO(e);
  return dO.has(t) || fO.has(t);
}
function bO(e) {
  return dO.get(xO(e)) ?? null;
}
function xO(e) {
  return e.replace(/[\s_-]/g, ``).toLowerCase();
}
function SO(e, n, r) {
  let i = !1;
  return (
    DO(e, (e) => {
      CO(e, r);
    }),
    e.once(`error`, (e) => {
      i ||
        t
          .Ur()
          .warning(`Bare modifier hotkey monitor failed`, { safe: {}, sensitive: { error: e } });
    }),
    e.once(`exit`, () => {
      (uO.delete(n), !i && t.Ur().warning(`Bare modifier hotkey monitor exited`));
    }),
    {
      handlesRelease: !0,
      unregister: () => {
        ((i = !0), uO.delete(n), e.kill());
      },
    }
  );
}
function CO(e, n) {
  switch (e) {
    case `ready`:
      return;
    case `down`:
      n.onPressed();
      return;
    case `up`:
      n.onReleased?.();
      return;
    case `permission-denied`:
      t.Ur().warning(`Bare modifier hotkey monitor needs input monitoring access`);
      return;
    case ``:
      return;
    default:
      return;
  }
}
function wO(e, t, n) {
  let r = [`--key`, t];
  return (
    n?.immediate === !0 || n?.trigger === `immediatePress`
      ? r.push(`--immediate`)
      : n?.trigger === `release` && r.push(`--trigger-on-release`),
    (0, m.spawn)(e, r, { stdio: [`pipe`, `pipe`, `ignore`] })
  );
}
function TO(e, t) {
  return (0, m.spawn)(e, [`--release-modifiers`, t.join(`,`)], {
    stdio: [`pipe`, `pipe`, `ignore`],
  });
}
function EO(e, n) {
  let r = !1,
    i = (i) => {
      r ||
        ((r = !0),
        e.kill(),
        i != null &&
          t
            .Ur()
            .warning(`Mac hotkey release monitor failed`, { safe: {}, sensitive: { error: i } }),
        n());
    };
  return (
    DO(e, (e) => {
      switch (e) {
        case `up`:
        case `permission-denied`:
          i();
          return;
        case `ready`:
        case `down`:
        case ``:
          return;
        default:
          return;
      }
    }),
    e.once(`error`, i),
    e.once(`exit`, () => {
      i();
    }),
    {
      dispose: () => {
        ((r = !0), e.kill());
      },
    }
  );
}
function DO(e, t) {
  let n = ``;
  e.stdout?.on(`data`, (e) => {
    n += e.toString(`utf8`);
    let r = n.indexOf(`
`);
    for (; r !== -1; )
      (t(n.slice(0, r).trim()),
        (n = n.slice(r + 1)),
        (r = n.indexOf(`
`)));
  });
}
function OO() {
  for (let e of kO()) if ((0, l.existsSync)(e)) return e;
  return null;
}
function kO() {
  if (typeof a.app.getAppPath != `function`) return [];
  let e = s.default.join(a.app.getAppPath(), `native`, lO, `build`, `Release-${process.arch}`, lO);
  return process.resourcesPath == null
    ? [e]
    : [s.default.join(process.resourcesPath, `native`, lO), e];
}
var AO = new Set([`cmdorctrl`, `command`, `cmd`, `control`, `ctrl`, `alt`, `option`]),
  jO = new Set([...AO, `shift`]),
  MO = new Set(),
  NO = t.Gr(`global-hotkey`),
  PO = null;
function FO(e, t, n) {
  if (yO(e)) return vO(e) ? pO(e, t, n?.bareModifierTrigger) : null;
  let r = VO(e),
    i = () => {
      (NO().debug(`global_hotkey_pressed`, { safe: { hotkey: e }, sensitive: {} }), t.onPressed());
    },
    o = a.globalShortcut.register(r, i);
  return (
    NO().debug(`register_global_hotkey`, {
      safe: { hotkey: e, registrationHotkey: r, platform: process.platform, registered: o },
      sensitive: {},
    }),
    o
      ? process.platform === `darwin`
        ? HO({ hotkey: e, onPressed: i, registrationHotkey: r })
        : {
            handlesRelease: !1,
            unregister: () => {
              a.globalShortcut.unregister(r);
            },
          }
      : null
  );
}
function IO(e) {
  process.platform === `darwin` &&
    ((PO = e),
    NO().debug(`updated_mac_keyboard_layout_map`, {
      safe: { entryCount: Object.keys(e).length },
      sensitive: {},
    }),
    UO());
}
function LO(e, t = process.platform) {
  let n = BO(e);
  if (vO(e, t)) return null;
  if (n.some(yO))
    return n.length === 1
      ? t === `darwin`
        ? vO(e, t)
          ? null
          : `This shortcut key is not supported.`
        : `Choose a shortcut with Ctrl or Alt plus another key.`
      : `Use Ctrl, Alt, or Command when combining with another key.`;
  if (n.length === 0) return `Shortcut cannot be empty.`;
  let r = !1,
    i = null;
  for (let e of n) {
    let t = e.toLowerCase();
    if (jO.has(t)) {
      AO.has(t) && (r = !0);
      continue;
    }
    if (i != null) return `Shortcut must include exactly one non-modifier key.`;
    i = e;
  }
  return i == null
    ? `Shortcut must include a non-modifier key.`
    : r
      ? null
      : `Shortcut must include Cmd/Ctrl or Alt.`;
}
function RO(e, t) {
  mO(e, t?.bareModifierTrigger);
}
function zO(e) {
  if (process.platform === `darwin`) {
    gO();
    return;
  }
  RO(e);
}
function BO(e) {
  return e
    .split(`+`)
    .map((e) => e.trim())
    .filter((e) => e.length > 0);
}
function VO(e) {
  return process.platform !== `darwin` || PO == null ? e : oO(e, PO);
}
function HO(e) {
  return (
    MO.add(e),
    {
      handlesRelease: !1,
      unregister: () => {
        (MO.delete(e), a.globalShortcut.unregister(e.registrationHotkey));
      },
    }
  );
}
function UO() {
  if (PO != null)
    for (let e of MO) {
      let t = oO(e.hotkey, PO);
      if (t === e.registrationHotkey) continue;
      let n = a.globalShortcut.register(t, e.onPressed);
      (NO().debug(`refresh_mac_global_hotkey`, {
        safe: {
          hotkey: e.hotkey,
          previousRegistrationHotkey: e.registrationHotkey,
          registrationHotkey: t,
          registered: n,
        },
        sensitive: {},
      }),
        n && (a.globalShortcut.unregister(e.registrationHotkey), (e.registrationHotkey = t)));
    }
}
var WO = `
$ErrorActionPreference = "Stop"

Add-Type @"
using System;
using System.Runtime.InteropServices;

public static class CodexKeyboardState {
  [DllImport("user32.dll")]
  private static extern short GetAsyncKeyState(int key);

  public static bool IsDown(int key) {
    return (GetAsyncKeyState(key) & 0x8000) != 0;
  }
}
"@

$keyGroupSpec = __KEY_GROUP_SPEC__
if ([string]::IsNullOrWhiteSpace($keyGroupSpec)) {
  throw "Missing hotkey release key groups."
}

$keyGroups = $keyGroupSpec.Split(";") | ForEach-Object {
  ,($_.Split(",") | ForEach-Object { [int]$_ })
}

while ($true) {
  foreach ($group in $keyGroups) {
    $groupDown = $false
    foreach ($key in $group) {
      if ([CodexKeyboardState]::IsDown($key)) {
        $groupDown = $true
        break
      }
    }
    if (-not $groupDown) {
      exit 0
    }
  }
  Start-Sleep -Milliseconds 40
}
`,
  GO = new Map([
    [`cmdorctrl`, `command`],
    [`command`, `command`],
    [`cmd`, `command`],
    [`control`, `control`],
    [`ctrl`, `control`],
    [`alt`, `alternate`],
    [`option`, `alternate`],
    [`shift`, `shift`],
  ]),
  KO = new Map([
    [`cmdorctrl`, [17]],
    [`command`, [91, 92]],
    [`cmd`, [91, 92]],
    [`control`, [17]],
    [`ctrl`, [17]],
    [`alt`, [18]],
    [`option`, [18]],
    [`shift`, [16]],
  ]);
function qO(e) {
  return (
    LO(e) ??
    (vO(e) || tk(e, process.platform)
      ? null
      : `Shortcut key is not supported for global dictation.`)
  );
}
function JO(e, t) {
  switch (process.platform) {
    case `darwin`: {
      let n = _O(YO(e), t);
      if (n == null) throw Error(`Global dictation hotkey release watching is not supported.`);
      return n;
    }
    case `win32`: {
      let n = ZO(e, process.platform);
      if (n == null) throw Error(`Global dictation hotkey release watching is not supported.`);
      return QO(
        (0, m.spawn)(
          `powershell.exe`,
          [
            `-NoProfile`,
            `-NonInteractive`,
            `-ExecutionPolicy`,
            `Bypass`,
            `-Command`,
            XO(n.map((e) => e.join(`,`)).join(`;`)),
          ],
          { stdio: `ignore`, windowsHide: !0 },
        ),
        t,
      );
    }
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      throw Error(`Global dictation hotkey release watching is not supported.`);
  }
}
function YO(e) {
  let t = [];
  for (let n of e.split(`+`)) {
    let e = GO.get(n.trim().toLowerCase());
    e != null && !t.includes(e) && t.push(e);
  }
  return t;
}
function XO(e) {
  return WO.replace(`__KEY_GROUP_SPEC__`, ek(e));
}
function ZO(e, t) {
  let n = [];
  for (let r of e.split(`+`)) {
    let e = $O(r, t);
    e != null && n.push(e);
  }
  return n.length === 0 ? null : n;
}
function QO(e, n) {
  let r = !1,
    i = (e) => {
      r ||
        ((r = !0),
        e != null &&
          t
            .Ur()
            .warning(`Global dictation hotkey release watching failed`, {
              safe: {},
              sensitive: { error: e },
            }),
        n());
    };
  return (
    e.once(`error`, i),
    e.once(`exit`, () => i()),
    {
      dispose: () => {
        ((r = !0), e.kill());
      },
    }
  );
}
function $O(e, t) {
  let n = e.trim().toLowerCase();
  switch (t) {
    case `darwin`:
      return null;
    case `win32`:
      return KO.get(n) ?? null;
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      return null;
  }
}
function ek(e) {
  return `'${e.replace(/'/g, `''`)}'`;
}
function tk(e, t) {
  return t === `darwin` ? YO(e).length > 0 : ZO(e, t) != null;
}
var nk = (0, c.promisify)(m.execFile),
  rk = Promise.resolve(),
  ik = t.$s({ appPath: t.nc(), bundleId: t.nc(), displayName: t.nc() });
async function ak(e) {
  if (process.platform !== `darwin`) return null;
  let n = sk();
  if (n == null) return null;
  try {
    let { stdout: r } = await nk(n, [e], { timeout: 2e3 });
    return t.Gs(ik).parse(JSON.parse(r));
  } catch {
    return null;
  }
}
function ok({ preference: e, resourceName: t }) {
  if (process.platform !== `darwin` || !a.app.isPackaged) return Promise.resolve(!1);
  let n = sk(),
    r = lk();
  if (n == null || r == null) return Promise.resolve(!1);
  let i = rk.then(async () => {
    try {
      return (await nk(n, [`set-app-icon`, r, e, t ?? ``], { timeout: 5e3 }), !0);
    } catch {
      return !1;
    }
  });
  return ((rk = i.then(() => void 0)), i);
}
function sk() {
  for (let e of ck()) if ((0, l.existsSync)(e)) return e;
  return null;
}
function ck() {
  return [
    s.default.join(process.resourcesPath, `native`, `launch-services-helper`),
    s.default.join(
      a.app.getAppPath(),
      `native`,
      `launch-services-helper`,
      `build`,
      `Release-${process.arch}`,
      `launch-services-helper`,
    ),
  ];
}
function lk() {
  let e = s.default.dirname(s.default.dirname(process.execPath)),
    t = s.default.dirname(e);
  return t.endsWith(`.app`) ? t : null;
}
var uk = (0, c.promisify)(m.execFile);
async function dk(e) {
  return e.trim() === ``
    ? null
    : process.platform === `darwin`
      ? fk(e)
      : process.platform === `win32`
        ? rl(e)
        : null;
}
async function fk(e) {
  let t = await pk(e);
  if (t == null) return null;
  try {
    if (t.endsWith(`.icns`)) return await hk(t);
    let e = a.nativeImage.createFromPath(t);
    return e.isEmpty() ? null : e.toDataURL();
  } catch {
    return null;
  }
}
async function pk(e) {
  if (!e.endsWith(`.app`)) return null;
  let t = s.join(e, `Contents`, `Resources`),
    n = s.join(e, `Contents`, `Info.plist`),
    r = new Set(),
    i = await mk(n);
  return (
    i != null &&
      i !== `` &&
      (r.add(s.join(t, i.endsWith(`.icns`) ? i : `${i}.icns`)), r.add(s.join(t, i))),
    r.add(s.join(t, `AppIcon.icns`)),
    (
      await Promise.all(
        [...r].map(async (e) => {
          try {
            return (await (0, p.access)(e), e);
          } catch {
            return null;
          }
        }),
      )
    ).find((e) => e != null) ?? null
  );
}
async function mk(e) {
  try {
    let { stdout: n } = await uk(`plutil`, [`-extract`, `CFBundleIconFile`, `raw`, `-o`, `-`, e], {
        encoding: `utf8`,
        env: t.Vr(process.env),
      }),
      r = n.trim();
    return r === `` ? null : r;
  } catch {
    return null;
  }
}
async function hk(e) {
  let n = await (0, p.mkdtemp)(s.join((0, o.tmpdir)(), `codex-app-icon-`)),
    r = s.join(n, `${s.basename(e)}.png`);
  try {
    return (
      await uk(`qlmanage`, [`-t`, `-s`, `32`, `-o`, n, e], { env: t.Vr(process.env) }),
      `data:image/png;base64,${(await (0, p.readFile)(r)).toString(`base64`)}`
    );
  } catch {
    return null;
  } finally {
    await (0, p.rm)(n, { recursive: !0, force: !0 });
  }
}
var gk = new Map([
    [`pdf`, [`com.apple.Preview`]],
    [`png`, [`com.apple.Preview`]],
    [`jpg`, [`com.apple.Preview`]],
    [`jpeg`, [`com.apple.Preview`]],
    [`gif`, [`com.apple.Preview`]],
    [`bmp`, [`com.apple.Preview`]],
    [`tiff`, [`com.apple.Preview`]],
    [`ico`, [`com.apple.Preview`]],
    [`webp`, [`com.apple.Preview`]],
    [`mov`, [`com.apple.QuickTimePlayerX`]],
    [`mp4`, [`com.apple.QuickTimePlayerX`]],
    [`m4v`, [`com.apple.QuickTimePlayerX`]],
    [`avi`, [`com.apple.QuickTimePlayerX`]],
    [`mkv`, [`com.apple.QuickTimePlayerX`]],
    [`webm`, [`com.apple.QuickTimePlayerX`]],
    [`mpg`, [`com.apple.QuickTimePlayerX`]],
    [`mpeg`, [`com.apple.QuickTimePlayerX`]],
    [`mp3`, [`com.apple.QuickTimePlayerX`]],
    [`wav`, [`com.apple.QuickTimePlayerX`]],
    [`m4a`, [`com.apple.QuickTimePlayerX`]],
    [`aac`, [`com.apple.QuickTimePlayerX`]],
    [`aiff`, [`com.apple.QuickTimePlayerX`]],
    [`aif`, [`com.apple.QuickTimePlayerX`]],
    [`flac`, [`com.apple.QuickTimePlayerX`]],
    [`caf`, [`com.apple.QuickTimePlayerX`]],
    [`zip`, [`com.apple.archiveutility`]],
    [`tar`, [`com.apple.archiveutility`]],
    [`gz`, [`com.apple.archiveutility`]],
    [`tgz`, [`com.apple.archiveutility`]],
    [`bz2`, [`com.apple.archiveutility`]],
    [`tbz`, [`com.apple.archiveutility`]],
    [`xz`, [`com.apple.archiveutility`]],
    [`txz`, [`com.apple.archiveutility`]],
    [`7z`, [`com.apple.archiveutility`]],
    [`rar`, [`com.apple.archiveutility`]],
    [`sketch`, [`com.bohemiancoding.sketch3`]],
    [`fig`, [`com.figma.Desktop`]],
    [`psd`, [`com.adobe.Photoshop`]],
    [`ai`, [`com.adobe.illustrator`]],
    [`indd`, [`com.adobe.InDesign`]],
    [`xd`, [`com.adobe.xd`]],
    [`doc`, [`com.microsoft.Word`, `com.apple.iWork.Pages`]],
    [`docx`, [`com.microsoft.Word`, `com.apple.iWork.Pages`]],
    [`xls`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`xlsx`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`csv`, [`com.microsoft.Excel`, `com.apple.iWork.Numbers`]],
    [`ppt`, [`com.microsoft.Powerpoint`, `com.apple.iWork.Keynote`]],
    [`pptx`, [`com.microsoft.Powerpoint`, `com.apple.iWork.Keynote`]],
    [`key`, [`com.apple.iWork.Keynote`]],
    [`pages`, [`com.apple.iWork.Pages`]],
    [`numbers`, [`com.apple.iWork.Numbers`]],
  ]),
  _k = new Set([...gk.keys(), `html`]);
function vk(e) {
  let t = s.default.extname(e).slice(1).toLowerCase();
  return gk.get(t) ?? [];
}
function yk(e, t = process.platform) {
  return t === `darwin` && Sk(e);
}
function bk(e, t = process.platform) {
  return t !== `darwin` && Sk(e);
}
function xk(e, t = process.platform) {
  return yk(e, t) || bk(e, t);
}
function Sk(e) {
  if (!(0, l.existsSync)(e) || !(0, l.statSync)(e).isFile()) return !1;
  let t = s.default.extname(e).toLowerCase();
  return t ? _k.has(t.slice(1)) : !1;
}
function Ck({
  requestedTarget: e,
  requestedTargetWasFiltered: t,
  preferredTarget: n,
  shouldUseFileManager: r,
  shouldUsePlatformDefaultViewer: i,
  hasExplicitTarget: a,
  allowPlatformFallback: o = !0,
  platform: s,
}) {
  if (t) return wk({ target: null, hasExplicitTarget: a, allowPlatformFallback: o, platform: s });
  let c = e ?? n;
  return (
    !a && e == null && (r ? (c = `fileManager`) : i && (c = `systemDefault`)),
    wk({ target: c, hasExplicitTarget: a, allowPlatformFallback: o, platform: s })
  );
}
function wk({ target: e, hasExplicitTarget: t, allowPlatformFallback: n = !0, platform: r }) {
  return e || (!t && n && r === `win32` ? `fileManager` : null);
}
var Tk = `/Applications`,
  Ek = (0, s.join)((0, o.homedir)(), `Applications`),
  Dk = [Tk, Ek];
function Ok(e) {
  return e.startsWith(`${Tk}/`) ? [e, (0, s.join)(Ek, e.slice(`${Tk}/`.length))] : [e];
}
function kk(e) {
  if (process.platform !== `darwin`) return null;
  for (let t of e) for (let e of Ok(t)) if ((0, l.existsSync)(e)) return e;
  return null;
}
function Ak(e, t) {
  if (process.platform !== `darwin`) return null;
  let n = e.toLowerCase();
  for (let e of Dk) {
    let r;
    try {
      r = (0, l.readdirSync)(e);
    } catch {
      continue;
    }
    for (let i of r) {
      let r = i.toLowerCase();
      if (!r.startsWith(n) || !r.endsWith(`.app`)) continue;
      let a = (0, s.join)(e, i, `Contents`, `MacOS`, t);
      if ((0, l.existsSync)(a)) return a;
    }
  }
  return null;
}
function jk(e) {
  if (process.platform !== `darwin`) return null;
  let t = e.toLowerCase();
  for (let e of Dk) {
    let n;
    try {
      n = (0, l.readdirSync)(e);
    } catch {
      continue;
    }
    for (let r of n) {
      let n = r.toLowerCase();
      if (!n.startsWith(t) || !n.endsWith(`.app`)) continue;
      let i = (0, s.join)(e, r);
      if ((0, l.existsSync)(i)) return i;
    }
  }
  return null;
}
var Mk = t.$s({
  sshAlias: t.nc().nullable(),
  sshHost: t.nc(),
  sshPort: t.Qs().nullable(),
  identity: t.nc().nullable(),
});
function Nk(e) {
  if (e.kind === `ssh`) {
    let t = Lk(Fk(e));
    if (t) return t;
    let n = Rk(Array.isArray(e.terminal_command) ? e.terminal_command : []);
    if (n) return n;
  }
  return e.name?.trim() || Ik(e.id);
}
function Pk(e) {
  let t = Fk(e),
    n = Lk(t);
  if (n) return n;
  if (t != null) {
    if (t.identity?.trim()) return null;
    let e = t.sshHost.trim();
    if (e.length > 0) return Vk(e, t.sshPort);
  }
  let r = Array.isArray(e.terminal_command) ? e.terminal_command : [];
  if (Bk(r)) return null;
  let i = Rk(r);
  return i ? Vk(i, zk(r)) : e.name?.trim() || Ik(e.id);
}
function Fk(e) {
  let t = Mk.safeParse(e[H_]);
  return t.success ? t.data : null;
}
function Ik(e) {
  let t = e.trim();
  return t.split(/[:/]/).at(-1)?.trim() || t;
}
function Lk(e) {
  return e?.sshAlias?.trim() || null;
}
function Rk(e) {
  return e.at(-1)?.trim() || null;
}
function zk(e) {
  let t = e.find((e) => /^-p\d+$/.test(e));
  if (t != null) return Number(t.slice(2));
  let n = e.indexOf(`-p`),
    r = n >= 0 ? e[n + 1] : null;
  return r == null || !/^\d+$/.test(r) ? null : Number(r);
}
function Bk(e) {
  return e.some((e) => e === `-i` || /^-i\S+/.test(e));
}
function Vk(e, t) {
  let n = Hk(e);
  return t == null ? n : `${n}:${t}`;
}
function Hk(e) {
  let t = e.lastIndexOf(`@`),
    n = t >= 0 ? e.slice(0, t + 1) : ``,
    r = t >= 0 ? e.slice(t + 1) : e;
  return !r.includes(`:`) || r.startsWith(`[`) ? e : `${n}[${r}]`;
}
function Uk(e, t) {
  return t
    ? [`--goto`, `${e}:${t.line}:${t.column}`]
    : process.platform === `win32`
      ? [e]
      : [`--goto`, e];
}
function Wk({ hostConfig: e, remoteWorkspaceRoot: t, remotePath: n, location: r }) {
  if (e.kind === `remote-control`)
    throw Error(`Remote control does not support VS Code remote open yet.`);
  let i = `ssh-remote+${Nk(e)}`,
    a = t?.trim() || void 0,
    o = n?.trim() || void 0;
  if (o != null && r != null)
    return Gk({ remoteAuthority: i, remoteWorkspaceRoot: a, path: o, location: r });
  let s = [];
  return (
    a != null && s.push(`--folder-uri`, Kk(i, a)), o != null && s.push(`--file-uri`, Kk(i, o)), s
  );
}
function Gk({ remoteAuthority: e, remoteWorkspaceRoot: t, path: n, location: r }) {
  let i = [];
  return (
    t != null && i.push(`--folder-uri`, Kk(e, t)),
    i.push(`--remote`, e, `--goto`, `${n}:${r.line}:${r.column}`),
    i
  );
}
function Kk(e, t) {
  return `vscode-remote://${e}${(t.startsWith(`/`) ? t : `/${t}`).split(`/`).map(encodeURIComponent).join(`/`)}`;
}
function qk({ label: e, icon: t, kind: n, platform: r }) {
  return {
    label: r.label ?? e,
    icon: r.icon ?? t,
    kind: r.kind ?? n,
    hidden: r.hidden,
    detect: r.detect,
    iconPath: r.iconPath,
    args: r.args,
    env: r.env,
    open: r.open,
  };
}
function Jk({ id: e, label: t, icon: n, kind: r, hidden: i, darwin: a, win32: o, linux: s }) {
  return {
    id: e,
    platforms: {
      darwin: a ? qk({ label: t, icon: n, kind: r, platform: { hidden: i, ...a } }) : void 0,
      win32: o ? qk({ label: t, icon: n, kind: r, platform: { hidden: i, ...o } }) : void 0,
      linux: s ? qk({ label: t, icon: n, kind: r, platform: { hidden: i, ...s } }) : void 0,
    },
  };
}
function Yk({
  id: e,
  label: t,
  icon: n,
  darwinDetect: r,
  win32Detect: i,
  darwinEnv: a,
  darwinArgs: o,
  hidden: s,
}) {
  return {
    id: e,
    platforms: {
      darwin: r
        ? {
            label: t,
            icon: n,
            kind: `editor`,
            hidden: s,
            detect: r,
            env: a,
            args: o ?? Xk,
            supportsSsh: !0,
          }
        : void 0,
      win32: i
        ? { label: t, icon: n, kind: `editor`, hidden: s, detect: i, args: Xk, supportsSsh: !0 }
        : void 0,
    },
  };
}
var Xk = (e, n, r, i, a) =>
    r != null && t.va(r) && (i != null || a != null)
      ? Wk({ hostConfig: r, location: n, remotePath: a, remoteWorkspaceRoot: i })
      : Uk(e, n),
  Zk = Yk({
    id: `antigravity`,
    label: `Antigravity`,
    icon: `apps/antigravity.png`,
    darwinDetect: () =>
      kk([`/Applications/Antigravity.app/Contents/Resources/app/bin/antigravity`]),
    win32Detect: Qk,
  });
function Qk() {
  let e = Co(`antigravity.exe`) ?? Co(`antigravity.cmd`) ?? Co(`antigravity`);
  return e
    ? Io(e)
    : Fo([
        [`Antigravity`, `Antigravity.exe`],
        [`antigravity`, `Antigravity.exe`],
        [`Antigravity`, `bin`, `antigravity.cmd`],
        [`antigravity`, `bin`, `antigravity.cmd`],
        [`Antigravity`, `resources`, `app`, `bin`, `antigravity.cmd`],
        [`antigravity`, `resources`, `app`, `bin`, `antigravity.cmd`],
      ]);
}
var $k = {
    id: `bbedit`,
    platforms: {
      darwin: {
        label: `BBEdit`,
        icon: `apps/bbedit.png`,
        kind: `editor`,
        detect: () => (jk(`BBEdit`) || kk([`/Applications/BBEdit.app`]) ? `open` : null),
        args: (e) => [`-a`, `BBEdit`, e],
      },
    },
  },
  eA = t.Gr(`open-in-targets`),
  tA = {
    id: `terminal`,
    platforms: {
      ...nA({
        id: `terminal`,
        label: `Terminal`,
        icon: `apps/terminal.png`,
        appPaths: [`/System/Applications/Utilities/Terminal.app`],
        appName: `Terminal`,
      }).platforms,
      win32: {
        label: `Terminal`,
        icon: `apps/microsoft-terminal.png`,
        kind: `terminal`,
        detect: lA,
        iconPath: () => null,
        args: uA,
        open: ({ command: e, path: t }) => dA(e, uA(t)),
      },
    },
  };
function nA({ id: e, label: t, icon: n, appPaths: r, appName: i }) {
  return {
    id: e,
    platforms: {
      darwin: {
        label: t,
        icon: n,
        kind: `terminal`,
        detect: () => (kk(r) ? `open` : null),
        args: (e) => [`-a`, i, aA(e)],
        open: async ({ command: t, path: n, location: r }) => {
          (await yA(e, n, r)) || (await Do(t, [`-a`, i, aA(n)]));
        },
      },
    },
  };
}
function rA(e) {
  let t = process.env.VISUAL?.trim();
  if (t) return t;
  let n = process.env.EDITOR?.trim();
  if (n) return n;
  for (let t of oA) {
    let n = e(t);
    if (n) return n;
  }
  return null;
}
function iA(e) {
  return [`-na`, `Ghostty.app`, `--args`, `-e`, process.env.SHELL?.trim() || `/bin/zsh`, `-lc`, e];
}
function aA(e) {
  return (0, l.existsSync)(e) && (0, l.statSync)(e).isDirectory() ? e : (0, s.dirname)(e);
}
var oA = [`nvim`, `vim`, `nano`, `less`],
  sA = `wt.exe`,
  cA = [`Terminal.lnk`, `Windows Terminal.lnk`];
function lA() {
  if (Co(sA) || Ro(sA)) return sA;
  let e = Ho(cA);
  return e ? (Io(a.shell.readShortcutLink(e).target) ?? sA) : null;
}
function uA(e) {
  return [`-d`, aA(e)];
}
async function dA(e, t) {
  await Do(`cmd.exe`, [`/d`, `/s`, `/c`, `start`, ``, e, ...t], { env: fA() });
}
function fA() {
  let e = zo(),
    t = process.env.Path == null ? `PATH` : `Path`,
    n = (process.env[t] ?? ``).split(`;`).filter(Boolean);
  return (
    n.some((t) => t.toLowerCase() === e.toLowerCase()) || n.unshift(e),
    { ...process.env, [t]: n.join(`;`) }
  );
}
function pA() {
  return rA(Co);
}
function mA(e) {
  return e.replace(/\\/g, `\\\\`).replace(/"/g, `\\"`);
}
function hA(e) {
  try {
    let n = t.Pn(e),
      r = n.at(0)?.trim();
    return r
      ? {
          args: n,
          executableName: (0, s.basename)(r)
            .replace(/\.exe$/i, ``)
            .toLowerCase(),
        }
      : null;
  } catch {
    return null;
  }
}
function gA(e, t) {
  return t
    ? e === `nvim` || e === `vim`
      ? [`+call cursor(${t.line},${t.column})`]
      : e === `nano`
        ? [`+${t.line},${t.column}`]
        : e === `less`
          ? [`+${t.line}g`]
          : []
    : [];
}
function _A(e, n, r) {
  let i = (0, s.dirname)(n),
    a = hA(e);
  if (a == null) return `cd ${t.Nn(i)} && ${e} ${t.Nn(n)}`;
  let o = gA(a.executableName, r),
    c = a.args.indexOf(`--`),
    l = c < 0 ? [...a.args, ...o, n] : [...a.args.slice(0, c), ...o, ...a.args.slice(c), n];
  return `cd ${t.Nn(i)} && ${t.Mn(l)}`;
}
function vA(e, t) {
  let n = mA(t);
  return e === `terminal`
    ? `tell application "Terminal" to do script "${n}"`
    : e === `iterm2`
      ? [
          `tell application "iTerm"`,
          `create window with default profile`,
          `tell current session of current window to write text "` + n + `"`,
          `end tell`,
        ].join(`
`)
      : null;
}
async function yA(e, t, n) {
  let r = pA();
  if (!r || !(0, l.existsSync)(t) || (0, l.statSync)(t).isDirectory()) return !1;
  let i = _A(r, t, n);
  if (e === `ghostty`)
    try {
      return (await Do(`open`, iA(i)), !0);
    } catch (e) {
      return (
        eA().warning(
          `Failed to run $EDITOR command for Ghostty; falling back to opening directory`,
          { safe: {}, sensitive: { error: e, path: t } },
        ),
        !1
      );
    }
  let a = vA(e, i);
  if (!a) return !1;
  try {
    return (await Do(`osascript`, [`-e`, a]), !0);
  } catch (n) {
    return (
      eA().warning(
        `Failed to run $EDITOR command for terminal target; falling back to opening directory`,
        { safe: { targetId: e }, sensitive: { error: n, path: t } },
      ),
      !1
    );
  }
}
var bA = Jk({
  id: `cmder`,
  label: `Cmder`,
  icon: `apps/cmder.png`,
  kind: `terminal`,
  win32: { detect: xA, args: (e) => [`/START`, aA(e)] },
});
function xA() {
  let e = process.env.CMDER_ROOT?.trim();
  if (e) {
    let t = (0, s.join)(e, `Cmder.exe`);
    if ((0, l.existsSync)(t)) return t;
  }
  let t = Co(`cmder.exe`) ?? Co(`cmder`);
  if (t) return Io(t);
  let n = Ho([`Cmder.lnk`]);
  return n ? Io(a.shell.readShortcutLink(n).target) : null;
}
var SA = Yk({
    id: `cursor`,
    label: `Cursor`,
    icon: `apps/cursor.png`,
    darwinDetect: () => wA()?.electronBin ?? null,
    win32Detect: TA,
    darwinEnv: () => {
      let e = { ...process.env };
      return (
        (e.VSCODE_NODE_OPTIONS = e.NODE_OPTIONS),
        (e.VSCODE_NODE_REPL_EXTERNAL_MODULE = e.NODE_REPL_EXTERNAL_MODULE),
        delete e.NODE_OPTIONS,
        delete e.NODE_REPL_EXTERNAL_MODULE,
        (e.ELECTRON_RUN_AS_NODE = `1`),
        e
      );
    },
    darwinArgs: (...e) => {
      let t = wA();
      if (!t) throw Error(`Cursor CLI entrypoint not available`);
      return [t.cliJs, ...CA(...e)];
    },
  }),
  CA = (e, n, r, i, a) =>
    r != null && t.va(r) && (i != null || a != null)
      ? Wk({ hostConfig: r, location: n, remotePath: a, remoteWorkspaceRoot: i })
      : Uk(e, n);
function wA() {
  if (process.platform !== `darwin`) return null;
  let e = [
      `/Applications/Cursor.app`,
      `/Applications/Cursor Preview.app`,
      `/Applications/Cursor Nightly.app`,
    ],
    t = jk(`Cursor`);
  t && e.push(t);
  for (let t of e)
    for (let e of Ok(t)) {
      let t = (0, s.join)(e, `Contents`, `MacOS`, `Cursor`),
        n = (0, s.join)(e, `Contents`, `Resources`, `app`, `out`, `cli.js`);
      if (!(!(0, l.existsSync)(t) || !(0, l.existsSync)(n))) return { electronBin: t, cliJs: n };
    }
  return null;
}
function TA() {
  let e = Co(`cursor`);
  if (e) {
    let t = Io(e);
    if (t) {
      if ((0, s.basename)(t).toLowerCase() === `cursor.exe`) return t;
      let e = (0, s.dirname)(t);
      if ((0, s.basename)(e).toLowerCase() === `bin`) {
        let t = (0, s.dirname)(e);
        if ((0, s.basename)(t).toLowerCase() === `app`) {
          let e = (0, s.dirname)(t);
          if ((0, s.basename)(e).toLowerCase() === `resources`) {
            let t = (0, s.join)((0, s.dirname)(e), `Cursor.exe`);
            if ((0, l.existsSync)(t)) return t;
          }
        }
      }
    }
  }
  return Fo([[`Cursor`, `Cursor.exe`]]);
}
var EA = {
    id: `emacs`,
    platforms: {
      darwin: {
        label: `Emacs`,
        icon: `apps/emacs.png`,
        kind: `editor`,
        detect: () => (jk(`Emacs`) || kk([`/Applications/Emacs.app`]) ? `open` : null),
        args: (e) => [`-a`, `Emacs`, e],
      },
    },
  },
  DA = Jk({
    id: `fileManager`,
    label: `Finder`,
    icon: `apps/finder.png`,
    kind: `fileManager`,
    darwin: { detect: () => `open`, args: (e) => To(e) },
    win32: {
      label: `File Explorer`,
      icon: `apps/file-explorer.png`,
      detect: OA,
      args: (e) => To(e),
      open: async ({ path: e }) => kA(e),
    },
  });
function OA() {
  let e = process.env.SystemRoot ?? process.env.windir;
  if (e) {
    let t = (0, s.join)(e, `explorer.exe`);
    if ((0, l.existsSync)(t)) return t;
  }
  return `explorer.exe`;
}
async function kA(e) {
  let t = AA(e);
  if (t && (0, l.statSync)(t).isFile()) {
    a.shell.showItemInFolder(t);
    return;
  }
  let n = t ?? e,
    r = await a.shell.openPath(n);
  if (r) throw Error(r);
}
function AA(e) {
  let t = e;
  for (;;) {
    if ((0, l.existsSync)(t)) return t;
    let e = (0, s.dirname)(t);
    if (e === t) return null;
    t = e;
  }
}
var jA = nA({
    id: `ghostty`,
    label: `Ghostty`,
    icon: `apps/ghostty.png`,
    appPaths: [`/Applications/Ghostty.app`],
    appName: `Ghostty`,
  }),
  MA = Jk({
    id: `gitBash`,
    label: `Git Bash`,
    icon: `apps/vscode.png`,
    kind: `terminal`,
    win32: { detect: NA, iconPath: PA, args: (e) => [`--cd=${aA(e)}`] },
  });
function NA() {
  let e = Co(`git-bash.exe`) ?? Co(`git-bash`);
  if (e) {
    let t = Io(e);
    if (t) return Lo(t, `git-bash.exe`) ?? t;
  }
  return Fo([
    [`Git`, `git-bash.exe`],
    [`Git`, `bin`, `bash.exe`],
  ]);
}
function PA(e) {
  return (
    Ho([`Git Bash.lnk`]) ||
    Fo([[`Git`, `git-bash.exe`]]) ||
    (e ? (Lo(e, `git-bash.exe`) ?? e) : null)
  );
}
var FA = Jk({
  id: `githubDesktop`,
  label: `GitHub Desktop`,
  icon: `apps/vscode.png`,
  kind: `editor`,
  win32: { detect: IA, args: (e) => [aA(e)] },
});
function IA() {
  let e = Co(`github.exe`) ?? Co(`github`);
  if (e) {
    let t = Io(e);
    if (t) return Lo(t, `GitHubDesktop.exe`) ?? t;
  }
  let t = process.env.LOCALAPPDATA ?? `${(0, o.homedir)()}\\AppData\\Local`;
  for (let e of [
    [`GitHubDesktop`, `GitHubDesktop.exe`],
    [`GitHub Desktop`, `GitHubDesktop.exe`],
  ]) {
    let n = (0, s.join)(t, ...e);
    if ((0, l.existsSync)(n)) return n;
  }
  return Fo([
    [`GitHub Desktop`, `GitHubDesktop.exe`],
    [`GitHubDesktop`, `GitHubDesktop.exe`],
  ]);
}
var LA = nA({
    id: `iterm2`,
    label: `iTerm2`,
    icon: `apps/iterm2.png`,
    appPaths: [`/Applications/iTerm.app`, `/Applications/iTerm2.app`],
    appName: `iTerm`,
  }),
  RA = t.Gr(`open-in-targets`),
  zA = (0, s.join)(
    (0, o.homedir)(),
    `Library`,
    `Application Support`,
    `JetBrains`,
    `Toolbox`,
    `apps`,
  ),
  BA = [process.env.ProgramFiles, process.env[`ProgramFiles(x86)`]].flatMap((e) =>
    e ? [(0, s.join)(e, `JetBrains`)] : [],
  ),
  VA = [
    { target: `androidStudio`, bundlePrefixes: [`android studio`], executable: `studio` },
    { target: `intellij`, bundlePrefixes: [`intellij idea`], executable: `idea` },
    { target: `rider`, bundlePrefixes: [`rider`], executable: `rider` },
    { target: `goland`, bundlePrefixes: [`goland`], executable: `goland` },
    { target: `rustrover`, bundlePrefixes: [`rustrover`], executable: `rustrover` },
    { target: `pycharm`, bundlePrefixes: [`pycharm`], executable: `pycharm` },
    { target: `webstorm`, bundlePrefixes: [`webstorm`], executable: `webstorm` },
    { target: `phpstorm`, bundlePrefixes: [`phpstorm`], executable: `phpstorm` },
  ],
  HA = null,
  UA = QA({
    id: `androidStudio`,
    label: `Android Studio`,
    icon: `apps/android-studio.png`,
    toolboxTarget: `androidStudio`,
    macExecutable: `studio`,
    windowsPathCommands: [`studio64.exe`, `studio.exe`, `studio`],
    windowsInstallDirPrefixes: [`android studio`],
    windowsInstallExecutables: [`studio64.exe`, `studio.exe`],
    windowsFallbackPaths: [
      [`Android`, `Android Studio`, `bin`, `studio64.exe`],
      [`Android`, `Android Studio`, `bin`, `studio.exe`],
    ],
  }),
  WA = QA({
    id: `intellij`,
    label: `IntelliJ IDEA`,
    icon: `apps/intellij.png`,
    toolboxTarget: `intellij`,
    macExecutable: `idea`,
    windowsPathCommands: [`idea64.exe`, `idea.exe`, `idea`],
    windowsInstallDirPrefixes: [`intellij idea`, `idea`],
    windowsInstallExecutables: [`idea64.exe`, `idea.exe`],
  }),
  GA = QA({
    id: `rider`,
    label: `Rider`,
    icon: `apps/rider.png`,
    toolboxTarget: `rider`,
    macExecutable: `rider`,
    windowsPathCommands: [`rider64.exe`, `rider.exe`, `rider`],
    windowsInstallDirPrefixes: [`rider`],
    windowsInstallExecutables: [`rider64.exe`, `rider.exe`],
  }),
  KA = QA({
    id: `goland`,
    label: `GoLand`,
    icon: `apps/goland.png`,
    toolboxTarget: `goland`,
    macExecutable: `goland`,
  }),
  qA = QA({
    id: `rustrover`,
    label: `RustRover`,
    icon: `apps/rustrover.png`,
    toolboxTarget: `rustrover`,
    macExecutable: `rustrover`,
  }),
  JA = QA({
    id: `pycharm`,
    label: `PyCharm`,
    icon: `apps/pycharm.png`,
    toolboxTarget: `pycharm`,
    macExecutable: `pycharm`,
    windowsPathCommands: [`pycharm64.exe`, `pycharm.exe`, `pycharm`],
    windowsInstallDirPrefixes: [`pycharm`],
    windowsInstallExecutables: [`pycharm64.exe`, `pycharm.exe`],
  }),
  YA = QA({
    id: `webstorm`,
    label: `WebStorm`,
    icon: `apps/webstorm.svg`,
    toolboxTarget: `webstorm`,
    macExecutable: `webstorm`,
    windowsPathCommands: [`webstorm64.exe`, `webstorm.exe`, `webstorm`],
    windowsInstallDirPrefixes: [`webstorm`],
    windowsInstallExecutables: [`webstorm64.exe`, `webstorm.exe`],
  }),
  XA = QA({
    id: `phpstorm`,
    label: `PhpStorm`,
    icon: `apps/phpstorm.png`,
    toolboxTarget: `phpstorm`,
    macExecutable: `phpstorm`,
    windowsPathCommands: [`phpstorm64.exe`, `phpstorm.exe`, `phpstorm`],
    windowsInstallDirPrefixes: [`phpstorm`],
    windowsInstallExecutables: [`phpstorm64.exe`, `phpstorm.exe`],
  });
function ZA(e) {
  let t = new Map();
  if (process.platform !== `darwin`) return t;
  let n;
  try {
    n = (0, l.readdirSync)(e);
  } catch {
    return t;
  }
  for (let r of $A(n)) {
    let n = (0, s.join)(e, r),
      i;
    try {
      i = (0, l.readdirSync)(n);
    } catch {
      continue;
    }
    for (let e of $A(i)) {
      let r = (0, s.join)(n, e),
        i;
      try {
        i = (0, l.readdirSync)(r);
      } catch {
        continue;
      }
      for (let e of $A(i)) {
        let n = (0, s.join)(r, e),
          i;
        try {
          i = (0, l.readdirSync)(n);
        } catch {
          continue;
        }
        for (let e of $A(i)) {
          let r = e.toLowerCase();
          if (r.endsWith(`.app`))
            for (let i of VA) {
              if (t.has(i.target) || !i.bundlePrefixes.some((e) => r.startsWith(e))) continue;
              let a = (0, s.join)(n, e, `Contents`, `MacOS`, i.executable);
              (0, l.existsSync)(a) && t.set(i.target, a);
            }
        }
      }
    }
  }
  return t;
}
function QA({
  id: e,
  label: t,
  icon: n,
  toolboxTarget: r,
  macExecutable: i,
  windowsPathCommands: a,
  windowsInstallDirPrefixes: o,
  windowsInstallExecutables: s,
  windowsFallbackPaths: c,
}) {
  return {
    id: e,
    platforms: {
      darwin: {
        label: t,
        icon: n,
        kind: `editor`,
        detect: () => nj(r, [`/Applications/${t}.app/Contents/MacOS/${i}`], t, i),
        args: aj,
      },
      win32:
        a && o && s
          ? {
              label: t,
              icon: n,
              kind: `editor`,
              detect: () =>
                rj({
                  pathCommands: a,
                  installDirPrefixes: o,
                  installExecutables: s,
                  fallbackPaths: c,
                }),
              args: aj,
            }
          : void 0,
    },
  };
}
function $A(e) {
  return [...e].sort((e, t) => t.localeCompare(e));
}
function ej() {
  if (HA) return HA;
  try {
    HA = ZA(zA);
  } catch (e) {
    (RA().debug(`Failed to discover JetBrains Toolbox installations`, {
      safe: {},
      sensitive: { error: e },
    }),
      (HA = new Map()));
  }
  return HA;
}
function tj(e, t, n) {
  return kk(e) ?? Ak(t, n);
}
function nj(e, t, n, r) {
  return tj(t, n, r) ?? ej().get(e) ?? null;
}
function rj({ pathCommands: e, installDirPrefixes: t, installExecutables: n, fallbackPaths: r }) {
  for (let t of e) {
    let e = Co(t);
    if (!e) continue;
    let n = Io(e);
    if (n) return n;
  }
  return ij({ installDirPrefixes: t, installExecutables: n }) || (r ? Fo(r) : null);
}
function ij({ installDirPrefixes: e, installExecutables: t }) {
  let n = e.map((e) => e.toLowerCase());
  for (let e of BA) {
    let r;
    try {
      r = $A((0, l.readdirSync)(e));
    } catch {
      continue;
    }
    for (let i of r) {
      let r = i.toLowerCase();
      if (n.some((e) => r.startsWith(e)))
        for (let n of t) {
          let t = (0, s.join)(e, i, `bin`, n);
          if ((0, l.existsSync)(t)) return t;
        }
    }
  }
  return null;
}
function aj(e, t) {
  return t ? [`--line`, t.line.toString(), `--column`, t.column.toString(), e] : [e];
}
function oj(e, n, r, i, a) {
  if (r != null && t.ya(r)) {
    let e = Pk(r);
    if (e == null) throw Error(`Zed SSH open is not available for this host.`);
    let t = sj(e, a?.trim() || i);
    if (t != null) return [cj(t, n)];
    throw Error(`Missing remote path for Zed SSH open.`);
  }
  return [cj(e, n)];
}
function sj(e, t) {
  let n = e.trim(),
    r = t?.trim();
  return !n || !r
    ? null
    : `ssh://${n}${(r.startsWith(`/`) ? r : `/${r}`).split(`/`).map(encodeURIComponent).join(`/`)}`;
}
function cj(e, t) {
  return t ? `${e}:${t.line}:${t.column}` : e;
}
var lj = Jk({
  id: `sublimeText`,
  label: `Sublime Text`,
  icon: `apps/sublime-text.png`,
  kind: `editor`,
  darwin: { detect: uj, args: oj },
  win32: { detect: dj, args: oj },
});
function uj() {
  let e = Co(`subl`);
  if (e) return e;
  let t = jk(`Sublime Text`);
  if (t) {
    let e = (0, s.join)(t, `Contents`, `SharedSupport`, `bin`, `subl`);
    if ((0, l.existsSync)(e)) return e;
  }
  return kk([`/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl`]);
}
function dj() {
  let e = Co(`subl.exe`) ?? Co(`subl`);
  if (e) {
    let t = Io(e);
    if (t) return Lo(t, `sublime_text.exe`) ?? t;
  }
  return Fo([
    [`Sublime Text`, `sublime_text.exe`],
    [`Sublime Text`, `subl.exe`],
  ]);
}
var fj = Jk({
  id: `systemDefault`,
  label: `Default app`,
  icon: `apps/file-explorer.png`,
  kind: `systemDefault`,
  hidden: !0,
  darwin: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ appPath: e, path: t }) => pj(t, e),
  },
  win32: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ path: e }) => mj(e),
  },
  linux: {
    detect: () => `system-default`,
    iconPath: () => null,
    args: (e) => [e],
    open: async ({ path: e }) => mj(e),
  },
});
async function pj(e, t) {
  await Do(`open`, t == null ? [e] : [`-a`, t, e]);
}
async function mj(e) {
  let t = await a.shell.openPath(e);
  if (t) throw Error(t);
}
var hj = {
  id: `textmate`,
  platforms: {
    darwin: {
      label: `TextMate`,
      icon: `apps/textmate.png`,
      kind: `editor`,
      detect: () => (jk(`TextMate`) || kk([`/Applications/TextMate.app`]) ? `open` : null),
      args: gj,
    },
  },
};
function gj(e, t) {
  if (!t) return [`-a`, `TextMate`, e];
  let n = new URL(`txmt://open/`);
  return (
    n.searchParams.set(`url`, (0, x.pathToFileURL)(e).toString()),
    n.searchParams.set(`line`, t.line.toString()),
    n.searchParams.set(`column`, t.column.toString()),
    [`-a`, `TextMate`, n.toString()]
  );
}
var _j = [`2022`, `2019`, `2017`],
  vj = [`Community`, `Professional`, `Enterprise`, `Preview`, `BuildTools`],
  yj = Jk({
    id: `visualStudio`,
    label: `Visual Studio`,
    icon: `apps/vscode.png`,
    kind: `editor`,
    win32: { detect: bj, args: (e) => [e] },
  });
function bj() {
  let e = Co(`devenv.exe`) ?? Co(`devenv`);
  return e
    ? Io(e)
    : Fo(
        _j.flatMap((e) =>
          vj.map((t) => [`Microsoft Visual Studio`, e, t, `Common7`, `IDE`, `devenv.exe`]),
        ),
      ) || Vo(`**\\Common7\\IDE\\devenv.exe`);
}
var xj = Yk({
  id: `vscode`,
  label: `VS Code`,
  icon: `apps/vscode.png`,
  darwinDetect: () =>
    kk([
      `/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code`,
      `/Applications/Code.app/Contents/Resources/app/bin/code`,
    ]),
  win32Detect: Sj,
});
function Sj() {
  return Bo({
    pathCommand: Co(`code`),
    executableName: `Code.exe`,
    installDirName: `Microsoft VS Code`,
  });
}
var Cj = Yk({
  id: `vscodeInsiders`,
  label: `VS Code Insiders`,
  icon: `apps/vscode-insiders.png`,
  darwinDetect: () =>
    kk([
      `/Applications/Visual Studio Code - Insiders.app/Contents/Resources/app/bin/code`,
      `/Applications/Code - Insiders.app/Contents/Resources/app/bin/code`,
    ]),
  win32Detect: wj,
});
function wj() {
  return Bo({
    pathCommand: Co(`code-insiders`),
    executableName: `Code - Insiders.exe`,
    installDirName: `Microsoft VS Code Insiders`,
  });
}
var Tj = nA({
    id: `warp`,
    label: `Warp`,
    icon: `apps/warp.png`,
    appPaths: [`/Applications/Warp.app`],
    appName: `Warp`,
  }),
  Ej = Yk({
    id: `windsurf`,
    label: `Windsurf`,
    icon: `apps/windsurf.png`,
    darwinDetect: () => kk([`/Applications/Windsurf.app/Contents/Resources/app/bin/windsurf`]),
  }),
  Dj = `wsl.exe`,
  Oj = Jk({
    id: `wsl`,
    label: `WSL`,
    icon: `apps/terminal.png`,
    kind: `terminal`,
    win32: { detect: jj, iconPath: Aj, args: kj, open: ({ command: e, path: t }) => dA(e, kj(t)) },
  });
function kj(e) {
  let n = t.Nr(aA(e));
  return n.startsWith(`\\\\`) ? [...uA(e), Dj] : [...uA(e), Dj, `--cd`, n];
}
function Aj() {
  return Ho([`WSL.lnk`]) ?? Ro(Dj);
}
function jj() {
  return t.Fr() == null ? null : lA();
}
var Mj = {
    id: `xcode`,
    platforms: {
      darwin: {
        label: `Xcode`,
        icon: `apps/xcode.png`,
        kind: `editor`,
        detect: () => {
          let e = Pj();
          return e?.xedPath ?? e?.appPath ?? null;
        },
        args: (e) => [e],
        open: async ({ path: e, location: t }) => {
          await Ij(e, t);
        },
      },
    },
  },
  Nj = t.Gr(`open-in-targets`);
function Pj() {
  if (process.platform !== `darwin`) return null;
  let e = jk(`Xcode`) ?? kk([`/Applications/Xcode.app`]),
    n = null;
  try {
    let e = (0, m.spawnSync)(`xcode-select`, [`-p`], {
        encoding: `utf8`,
        env: t.Vr(process.env),
        timeout: 1e3,
      }),
      r = e.stdout?.trim();
    if (e.status === 0 && r) {
      let e = (0, s.join)(r, `usr`, `bin`, `xed`);
      (0, l.existsSync)(e) && (n = e);
    }
  } catch (e) {
    Nj().debug(`Failed to resolve Xcode via xcode-select`, { safe: {}, sensitive: { error: e } });
  }
  if (n == null && e) {
    let t = (0, s.join)(e, `Contents`, `Developer`, `usr`, `bin`, `xed`);
    (0, l.existsSync)(t) && (n = t);
  }
  return n == null && e == null ? null : { appPath: e, xedPath: n };
}
function Fj(e) {
  let t = e;
  (!(0, l.existsSync)(e) || !(0, l.statSync)(e).isDirectory()) && (t = (0, s.dirname)(e));
  let n = null;
  for (;;) {
    let e;
    try {
      e = (0, l.readdirSync)(t);
    } catch {
      e = [];
    }
    let r = e.find((e) => e.endsWith(`.xcworkspace`));
    if (r) return (0, s.join)(t, r);
    let i = e.find((e) => e.endsWith(`.xcodeproj`));
    if (i) return (0, s.join)(t, i);
    n == null && e.includes(`Package.swift`) && (n = t);
    let a = (0, s.dirname)(t);
    if (a === t) return n;
    t = a;
  }
}
async function Ij(e, t) {
  let n = Pj();
  if (!n) throw Error(`Open target "xcode" is not available`);
  if (n.xedPath) {
    let r = Fj(e),
      i = [];
    (r && i.push(`--project`, r),
      t && i.push(`--line`, t.line.toString()),
      i.push(e),
      Nj().debug(`Opening path in Xcode via xed`, {
        safe: {},
        sensitive: { xedPath: n.xedPath, args: i, xcodeContainerPath: r, path: e },
      }),
      await Do(n.xedPath, i));
    return;
  }
  if (n.appPath == null) throw Error(`Open target "xcode" is not available`);
  (Nj().debug(`Opening path in Xcode via LaunchServices fallback`, {
    safe: {},
    sensitive: { appPath: n.appPath, path: e },
  }),
    await Do(`open`, [`-a`, n.appPath, e]));
}
var Lj = {
  id: `zed`,
  platforms: {
    darwin: { label: `Zed`, icon: `apps/zed.png`, kind: `editor`, detect: Rj, args: oj, open: Vj },
    win32: { label: `Zed`, icon: `apps/zed.png`, kind: `editor`, detect: zj, args: oj },
  },
};
function Rj() {
  return (
    Co(`zed`) ??
    kk([
      `/Applications/Zed.app/Contents/MacOS/zed`,
      `/Applications/Zed Preview.app/Contents/MacOS/zed`,
      `/Applications/Zed Nightly.app/Contents/MacOS/zed`,
    ]) ??
    Ak(`Zed`, `zed`)
  );
}
function zj() {
  let e = Co(`zed.exe`) ?? Co(`zed`);
  return e ? Io(e) : Fo([[`Zed`, `Zed.exe`]]);
}
function Bj(e) {
  let t = e.indexOf(`.app/Contents/MacOS/`);
  return t === -1 ? null : e.slice(0, t + 4);
}
async function Vj({
  command: e,
  path: t,
  location: n,
  hostConfig: r,
  remoteWorkspaceRoot: i,
  remotePath: a,
}) {
  let o = oj(t, n, r, i, a),
    s = o[0] ?? t,
    c = n == null ? s : (oj(t, null, r, i, a)[0] ?? s),
    l = Bj(e);
  if (l) {
    if ((await Do(`open`, [`-a`, l, c]), !n)) return;
    let t = Co(`zed`) ?? e;
    try {
      await Do(t, o);
    } catch {}
    return;
  }
  await Do(e, o);
}
var Hj = [
    xj,
    Cj,
    yj,
    SA,
    $k,
    EA,
    lj,
    Lj,
    Ej,
    Zk,
    FA,
    fj,
    DA,
    tA,
    MA,
    bA,
    Oj,
    LA,
    jA,
    Tj,
    Mj,
    UA,
    WA,
    GA,
    KA,
    qA,
    JA,
    YA,
    XA,
    hj,
  ],
  Uj = t.Gr(`open-in-targets`);
function Wj(e) {
  return Hj.flatMap((t) => {
    let n = t.platforms[e];
    return n ? [{ id: t.id, ...n }] : [];
  });
}
var Gj = Wj(process.platform),
  Kj = tM(Gj),
  qj = new Set(Gj.filter((e) => e.kind === `editor`).map((e) => e.id)),
  Jj = null,
  Yj = null;
function Xj(e) {
  return qj.has(e);
}
async function Zj() {
  return (
    Yj ||
    (process.platform === `win32` && !a.app.isReady()
      ? Kj
      : ((Yj = await eM(process.platform, Gj, nM())), Yj))
  );
}
async function Qj() {
  return Array.from(nM().keys());
}
async function $j(e, t, n, r, i, a, o) {
  if (o != null && i?.kind === `remote-control`)
    throw Error(`Remote control does not support open in ${e} yet.`);
  let s = Gj.find((t) => t.id === e);
  if (!s) throw Error(`Unknown open target "${e}"`);
  let c = s.detect();
  if (!c) throw Error(`Open target "${e}" is not available`);
  if (s.open) {
    await s.open({
      command: c,
      path: t,
      appPath: n,
      location: r,
      hostConfig: i,
      remoteWorkspaceRoot: a,
      remotePath: o,
    });
    return;
  }
  await Do(c, s.args(t, r, i, a, o), { env: s.env?.() });
}
async function eM(e, t, n) {
  return e === `win32`
    ? Promise.all(
        t.map(async (e) => {
          let t = n?.get(e.id) ?? null,
            r = e.iconPath ? e.iconPath(t) : t;
          return {
            id: e.id,
            label: e.label,
            icon: await rM(r, e.icon),
            kind: e.kind,
            hidden: e.hidden,
            supportsSsh: e.supportsSsh,
          };
        }),
      )
    : tM(t);
}
function tM(e) {
  return e.map(({ id: e, label: t, icon: n, kind: r, hidden: i, supportsSsh: a }) => ({
    id: e,
    label: t,
    icon: n,
    kind: r,
    hidden: i,
    supportsSsh: a,
  }));
}
function nM() {
  if (Jj) return Jj;
  let e = new Map();
  for (let t of Gj)
    try {
      let n = t.detect();
      n && e.set(t.id, n);
    } catch (e) {
      Uj().error(`Failed to detect open target`, { safe: {}, sensitive: { id: t.id, error: e } });
    }
  return ((Jj = e), e);
}
async function rM(e, t) {
  if (!e) return t;
  try {
    let n = e.toLowerCase().endsWith(`.lnk`)
      ? await iM(e)
      : await a.app.getFileIcon(e, { size: `normal` });
    return !n || n.isEmpty() ? t : n.toDataURL();
  } catch (e) {
    return (
      Uj().warning(`Failed to resolve open target icon`, { safe: {}, sensitive: { error: e } }), t
    );
  }
}
async function iM(e) {
  let t = a.shell.readShortcutLink(e),
    n = aM(t.icon) ?? t.target ?? null;
  if (!n) return null;
  if (/\.(ico|png|jpe?g|bmp)$/i.test(n)) {
    let e = a.nativeImage.createFromPath(n);
    if (!e.isEmpty()) return e;
  }
  let r = await a.app.getFileIcon(n, { size: `normal` });
  return r.isEmpty() ? null : r;
}
function aM(e) {
  if (!e) return null;
  let t = e.trim().replace(/^"(.*)"$/, `$1`),
    n = t.lastIndexOf(`,`);
  if (n < 0) return t;
  let r = Number(t.slice(n + 1));
  return Number.isInteger(r) ? t.slice(0, n) : t;
}
async function oM(e) {
  let [n, r] = await Promise.all([Qj(), Zj()]);
  return t.va(e)
    ? t.ya(e)
      ? n.filter((t) => (t === `zed` ? Pk(e) != null : !!r.find((e) => e.id === t)?.supportsSsh))
      : []
    : n;
}
function sM(e) {
  return Xj(e);
}
function cM(e) {
  let n = e.getEffective(t.vi.openInTargetPreferences.key) ?? {},
    r = lM(n.global) ?? void 0,
    i = Object.fromEntries(
      Object.entries(n.perPath ?? {}).flatMap(([e, t]) => {
        let n = lM(t);
        return n ? [[e, n]] : [];
      }),
    );
  return { global: r, perPath: Object.keys(i).length > 0 ? i : void 0 };
}
function lM(e) {
  return e === `finder`
    ? `fileManager`
    : typeof e == `string` && Gj.some((t) => t.id === e)
      ? e
      : null;
}
function uM(e, t, n) {
  let r = cM(e),
    i = (t && r.perPath?.[t]) ?? r.global ?? null;
  return i && n.has(i) ? i : (n.values().next().value ?? null);
}
function dM(e, t) {
  let n = cM(e);
  return !!((t && n.perPath?.[t]) ?? n.global ?? null);
}
function fM(e, n, r) {
  let i = cM(e);
  ((i.global = r),
    n && ((i.perPath = i.perPath ?? {}), (i.perPath[n] = r)),
    e.set(t.vi.openInTargetPreferences.key, i));
}
function pM(e) {
  return mM(e, t.Gt());
}
function mM(e, t) {
  let n = t.bindings.find((t) => t.command === e);
  return n == null ? { hasBinding: !1, hotkey: null } : { hasBinding: !0, hotkey: n.key };
}
async function hM(e, n) {
  return t.qt({
    commandId: e,
    update: n == null ? { type: `clear` } : { type: `set`, accelerator: n },
  });
}
async function gM({ commandId: e, hotkey: t }) {
  return t == null || pM(e).hasBinding ? null : hM(e, t);
}
function _M({ keymapHotkey: e, legacyHotkey: t }) {
  return e.hasBinding ? e.hotkey : t;
}
function vM() {
  return typeof a.BrowserWindow.isSystemBackdropSupported == `function`
    ? a.BrowserWindow.isSystemBackdropSupported()
    : !0;
}
var yM = new Set([301, 302, 303, 307, 308]),
  bM = new Set([`image/png`, `image/webp`]),
  xM = t.$s({
    name: t.nc().trim().min(1),
    description: t
      .nc()
      .nullish()
      .transform((e) => e?.trim() || null),
    imageUrl: t
      .nc()
      .url()
      .transform((e, n) => {
        let r = new URL(e);
        return r.protocol !== `https:` || r.hostname === `localhost`
          ? (n.addIssue({ code: `custom`, message: `Pet spritesheet URL is invalid` }), t.oc)
          : r.toString();
      }),
  }),
  SM = class {
    fetchAsset;
    installPreparedPet;
    constructor({
      appServerClient: e,
      fetchAsset: n = fetch,
      installPreparedPet: r = t.Rt,
      preferWsl: i = !1,
    }) {
      ((this.appServerClient = e),
        (this.fetchAsset = n),
        (this.installPreparedPet = r),
        (this.preferWsl = i));
    }
    appServerClient;
    preferWsl;
    async preview({ name: e, description: t, imageUrl: n }) {
      let r = xM.parse({ name: e, description: t, imageUrl: n }),
        i = await CM(r.imageUrl, this.fetchAsset);
      return {
        displayName: r.name,
        description: r.description,
        spritesheetDataUrl: i.spritesheetDataUrl,
      };
    }
    async install({ name: e, description: t, imageUrl: n }) {
      let r = xM.parse({ name: e, description: t, imageUrl: n }),
        i = await CM(r.imageUrl, this.fetchAsset);
      return {
        id: await this.installPreparedPet({
          appServerClient: this.appServerClient,
          preferWsl: this.preferWsl,
          name: r.name,
          description: r.description,
          spritesheet: i,
        }),
      };
    }
  };
async function CM(e, n) {
  let r = await n(e, { redirect: `manual` });
  if (yM.has(r.status)) throw Error(`Pet spritesheet redirects are not allowed`);
  if (!r.ok) throw Error(`Pet spritesheet download failed (${r.status})`);
  let i = await wM(r),
    a = t.Bt(i);
  if (a == null) throw Error(`Pet spritesheet is invalid`);
  return { ...a, bytes: i };
}
async function wM(e) {
  let t = e.headers.get(`content-type`)?.split(`;`)[0]?.trim().toLowerCase();
  if (t == null || !bM.has(t)) throw Error(`Pet spritesheet response must be an image`);
  let n = Number(e.headers.get(`content-length`));
  if (Number.isFinite(n) && n > 20971520) throw Error(`Pet spritesheet is too large`);
  if (e.body == null) throw Error(`Pet spritesheet response had no body`);
  let r = e.body.getReader(),
    i = [],
    a = 0;
  for (;;) {
    let { done: e, value: t } = await r.read();
    if (e) break;
    if (((a += t.byteLength), a > 20971520))
      throw (r.cancel(), Error(`Pet spritesheet is too large`));
    i.push(t);
  }
  return Buffer.concat(i, a);
}
function TM(e) {
  let n = e.get(t.Fo.PINNED_THREAD_IDS);
  return Array.isArray(n) ? n.filter((e) => typeof e == `string`) : [];
}
function EM(e) {
  e === `light` || e === `dark`
    ? (a.nativeTheme.themeSource = e)
    : (a.nativeTheme.themeSource = `system`);
}
var DM = (0, c.promisify)(m.execFile),
  OM = t.Gr(`setting-mirrors`);
function kM(e, t) {
  for (let n of e.mirrors ?? [])
    AM(n, t).catch((t) => {
      OM().warning(`Failed to write setting mirror`, {
        safe: { kind: n.kind, settingKey: e.key },
        sensitive: { error: t },
      });
    });
}
async function AM(e, t) {
  switch (e.kind) {
    case `macos-user-defaults`:
      await jM(e, t);
      return;
  }
}
async function jM(e, t) {
  process.platform === `darwin` &&
    (await DM(`/usr/bin/defaults`, [`write`, e.domain, e.key, ...MM(t)]));
}
function MM(e) {
  if (typeof e == `boolean`) return [`-bool`, e ? `YES` : `NO`];
  if (typeof e == `number`) return Number.isInteger(e) ? [`-int`, `${e}`] : [`-float`, `${e}`];
  if (typeof e == `string`) return [`-string`, e];
  throw Error(`Cannot mirror non-primitive setting value`);
}
var NM = t.Gr(`remote-project-migration`);
function PM({ remoteCwdsByHostAndWorkspace: e, remoteProjects: t, workspaceRootOptions: n }) {
  if (e == null) return;
  let r = new Set(t.map((e) => `${e.hostId}::${e.remotePath}`)),
    i = new Set(n),
    a = {};
  for (let [t, n] of Object.entries(e)) {
    let e = {};
    for (let [a, o] of Object.entries(n))
      a === o && (i.has(a) || r.has(`${t}::${o}`)) && (e[a] = o);
    Object.keys(e).length !== 0 && (a[t] = e);
  }
  return Object.keys(a).length > 0 ? a : void 0;
}
function FM(e) {
  let n = r.s(e),
    i = e.get(t.Fo.REMOTE_CWDS_BY_HOST_AND_WORKSPACE) ?? void 0;
  if (n.length === 0 || i == null) return;
  let a = e.get(t.Fo.SELECTED_REMOTE_HOST_ID) ?? null,
    o = r.o(e),
    c = r.i(e),
    l = e.get(t.Fo.PROJECT_ORDER) ?? null,
    d = e.get(t.Fo.ACTIVE_REMOTE_PROJECT_ID) ?? null,
    f = e.get(t.Fo.REMOTE_PROJECTS) ?? [],
    p = new Set(f.map((e) => `${e.hostId}::${e.remotePath}`)),
    m = [],
    h = [],
    g = 0;
  for (let [e, t] of Object.entries(i))
    for (let [r, i] of Object.entries(t)) {
      if (r !== i) {
        g += 1;
        continue;
      }
      if (!n.includes(r)) {
        h.push({ hostId: e, workspaceRoot: r });
        continue;
      }
      let t = o[r]?.trim() || s.default.posix.basename(i) || i;
      m.push({ hostId: e, workspaceRoot: r, remotePath: i, label: t });
    }
  if (m.length === 0) {
    let r = PM({ remoteCwdsByHostAndWorkspace: i, remoteProjects: f, workspaceRootOptions: n });
    (JSON.stringify(r) !== JSON.stringify(i) && e.set(t.Fo.REMOTE_CWDS_BY_HOST_AND_WORKSPACE, r),
      NM().info(`[remote-project-migration] no string-based remote projects found`, {
        safe: { ignoredLocalProjectMappings: g, selfMappingsMissingWorkspaceRootCount: h.length },
        sensitive: { selfMappingsMissingWorkspaceRoot: h },
      }));
    return;
  }
  NM().info(`[remote-project-migration] found string-based remote projects`, {
    safe: {
      candidateCount: m.length,
      ignoredLocalProjectMappings: g,
      selfMappingsMissingWorkspaceRootCount: h.length,
    },
    sensitive: { stringBasedRemoteProjects: m, selfMappingsMissingWorkspaceRoot: h },
  });
  let _ = new Set(m.map((e) => e.workspaceRoot)),
    v = [],
    y = [];
  for (let e of m) {
    let t = `${e.hostId}::${e.remotePath}`;
    if (p.has(t)) {
      y.push({ hostId: e.hostId, remotePath: e.remotePath });
      continue;
    }
    (v.push({
      id: (0, u.randomUUID)(),
      hostId: e.hostId,
      remotePath: e.remotePath,
      label: e.label,
    }),
      p.add(t));
  }
  NM().info(`[remote-project-migration] prepared remote project objects`, {
    safe: {
      createdRemoteProjectsCount: v.length,
      reusedRemoteProjectsCount: y.length,
      migratedWorkspaceRootCount: _.size,
    },
    sensitive: { newRemoteProjects: v, reusedRemoteProjects: y },
  });
  let b = v.length > 0 ? [...f, ...v] : f,
    x = new Map();
  for (let e of b) {
    let t = x.get(e.remotePath) ?? [];
    (t.push(e), x.set(e.remotePath, t));
  }
  let S = n.filter((e) => _.has(e)),
    C = new Map();
  for (let e of S) {
    let t = [...(x.get(e) ?? [])];
    (t.sort((e, t) => {
      let n = e.hostId === a ? 0 : 1,
        r = t.hostId === a ? 0 : 1;
      return n === r ? e.hostId.localeCompare(t.hostId) : n - r;
    }),
      C.set(
        e,
        t.map((e) => e.id),
      ));
  }
  let w = n.filter((e) => !_.has(e)),
    T = c.filter((e) => !_.has(e)),
    E = c.find((e) => _.has(e)),
    D = l ?? n,
    O = [],
    k = new Set();
  for (let e of D) {
    if (!_.has(e)) {
      k.has(e) || (O.push(e), k.add(e));
      continue;
    }
    let t = C.get(e) ?? [];
    for (let e of t) k.has(e) || (O.push(e), k.add(e));
  }
  for (let e of S) {
    let t = C.get(e) ?? [];
    for (let e of t) k.has(e) || (O.push(e), k.add(e));
  }
  let A = E == null ? d : (C.get(E)?.[0] ?? d);
  (NM().info(`[remote-project-migration] rewriting project state`, {
    safe: {
      nextWorkspaceRootOptionsCount: w.length,
      nextActiveWorkspaceRootsCount: T.length,
      nextProjectOrderCount: O.length,
      hasNextActiveRemoteProjectId: A != null,
    },
    sensitive: {
      migratedWorkspaceRoots: [..._],
      replacementRemoteProjectIdsByWorkspaceRoot: Object.fromEntries(C),
      nextWorkspaceRootOptions: w,
      nextActiveWorkspaceRoots: T,
      nextProjectOrder: O,
      nextActiveRemoteProjectId: A,
    },
  }),
    v.length > 0 && e.set(t.Fo.REMOTE_PROJECTS, b),
    w.length > 0 ? r.d(e, w) : e.set(t.Fo.WORKSPACE_ROOT_OPTIONS, void 0),
    r.f(e, (e) => {
      if (e == null) return;
      let t = Object.fromEntries(Object.entries(e).filter(([e]) => !_.has(e)));
      return Object.keys(t).length > 0 ? t : void 0;
    }),
    T.length > 0
      ? r.u(e, T)
      : w.length > 0
        ? r.u(e, [w[0]])
        : e.set(t.Fo.ACTIVE_WORKSPACE_ROOTS, void 0),
    e.set(t.Fo.PROJECT_ORDER, O.length > 0 ? O : void 0),
    e.set(t.Fo.ACTIVE_REMOTE_PROJECT_ID, A ?? void 0),
    e.set(
      t.Fo.REMOTE_CWDS_BY_HOST_AND_WORKSPACE,
      PM({ remoteCwdsByHostAndWorkspace: i, remoteProjects: b, workspaceRootOptions: w }),
    ));
}
var IM = [`powershell`, `commandPrompt`],
  LM = [`--login`, `-i`];
function RM() {
  if (process.platform !== `win32`) return [];
  let e = [...IM];
  return (UM() != null && e.push(`gitBash`), WM() != null && e.push(`wsl`), e);
}
function zM(e) {
  if (process.platform !== `win32`) return [t.Fn()];
  if (e === `powershell`) {
    let e = VM();
    if (e != null) return [e];
  }
  if (e === `commandPrompt`) return [HM()];
  if (e === `gitBash`) {
    let e = UM();
    if (e != null) return [e, ...LM];
  }
  if (e === `wsl`) {
    let e = WM();
    if (e != null) return [e];
  }
  let n = VM();
  return n == null ? [HM()] : [n];
}
function BM(e) {
  let n = e[0];
  if (n == null || n.length === 0) return `Shell`;
  let r = n.split(/[/\\]/).at(-1)?.toLowerCase();
  if (process.platform === `win32`) {
    if (r === `wsl` || r === `wsl.exe`) return t.ii.wsl;
    if (r === `pwsh` || r === `pwsh.exe` || r === `powershell` || r === `powershell.exe`)
      return t.ii.powershell;
    if (r === `cmd` || r === `cmd.exe`) return t.ii.commandPrompt;
    if (r === `git-bash.exe` || (r === `bash.exe` && /[\\/]Git[\\/]/i.test(n))) return t.ii.gitBash;
  }
  let i = r?.replace(/\.exe$/i, ``) ?? n;
  return i != null && i.length > 0 ? i : `Shell`;
}
function VM() {
  for (let e of [`pwsh.exe`, `powershell.exe`]) {
    let t = Co(e);
    if (t != null) return t;
  }
  return null;
}
function HM() {
  let e = process.env.COMSPEC?.trim();
  return e != null && e.length > 0 ? e : `cmd.exe`;
}
function UM() {
  let e = Co(`git-bash.exe`);
  if (e != null) return e;
  let t = Co(`git.exe`);
  if (t != null) {
    let e = (0, s.dirname)(t),
      n = [(0, s.join)(e, `bash.exe`), (0, s.join)((0, s.dirname)(e), `bin`, `bash.exe`)];
    for (let e of n) if ((0, l.existsSync)(e)) return e;
  }
  return Fo([[`Git`, `bin`, `bash.exe`]]);
}
function WM() {
  return Co(`wsl.exe`);
}
var GM = `THIRD_PARTY_NOTICES.txt`,
  KM = (0, c.promisify)(m.execFile),
  qM = () => {
    let e = process.cwd(),
      t = [];
    return (
      process.resourcesPath && t.push(s.default.join(process.resourcesPath, GM)),
      t.push(s.default.join(e, `assets`, GM)),
      t.push(s.default.join(e, `electron`, `assets`, GM)),
      t
    );
  };
function JM() {
  let e = process.cwd();
  for (;;) {
    if ((0, l.existsSync)(s.default.join(e, `pnpm-workspace.yaml`))) return e;
    let t = s.default.dirname(e);
    if (t === e) return null;
    e = t;
  }
}
async function YM() {
  let e = JM();
  if (!e) return;
  let n = s.default.join(e, `electron`, `assets`, GM);
  (await l.promises.mkdir(s.default.dirname(n), { recursive: !0 }),
    await KM(`node`, [`./scripts/generate-third-party-notices.mjs`, `--output-file`, n], {
      cwd: e,
      env: t.Vr(process.env),
      timeout: 12e4,
    }));
}
async function XM() {
  for (let e of qM()) if ((0, l.existsSync)(e)) return l.promises.readFile(e, `utf8`);
  if (process.env.NODE_ENV !== `production`) {
    try {
      await YM();
    } catch {
      return null;
    }
    for (let e of qM()) if ((0, l.existsSync)(e)) return l.promises.readFile(e, `utf8`);
  }
  return null;
}
async function ZM({ prompt: e, cwd: n, serviceTier: r, appServerConnection: i }) {
  return t.t({
    prompt: e,
    cwd: n,
    serviceTier: r,
    client: {
      startThread: (e) => i.startThread(e),
      startTurn: (e) => i.startTurn(e),
      unsubscribeThread: (e) => i.unsubscribeThread(e),
      interruptTurn: (e) => i.interruptTurn(e),
      registerInternalNotificationHandler: (e) => i.registerInternalNotificationHandler(e),
    },
  });
}
var QM = null,
  $M = null;
function eN() {
  if (QM != null) throw Error(`Trace recording start confirmation is already pending`);
  return new Promise((e) => {
    QM = e;
  });
}
function tN() {
  if (QM == null) return !1;
  let e = QM;
  return ((QM = null), e(!0), !0);
}
function nN() {
  if (QM == null) return !1;
  let e = QM;
  return ((QM = null), e(!1), !0);
}
function rN() {
  if ($M != null) throw Error(`Trace recording upload details are already pending`);
  return new Promise((e) => {
    $M = e;
  });
}
function iN(e) {
  if ($M == null) return !1;
  let t = $M;
  return (($M = null), t(e), !0);
}
async function aN({
  appServerClient: e,
  directoryPath: n,
  directoriesOnly: r,
  includeHidden: i,
  workspaceRoot: a,
}) {
  if (a.trim().length === 0) throw Error(`workspaceRoot is required`);
  if (!(await t.jr.stat(a, e)).isDirectory()) throw Error(`workspaceRoot must be a directory`);
  let o = cN(n),
    c = t.va(e.hostConfig) ? await e.platformPath() : s,
    {
      canonicalDirectoryPath: l,
      realWorkspaceRoot: u,
      resolvedDirectoryPath: d,
      resolvedWorkspaceRoot: f,
    } = await oN({ appServerClient: e, directoryPath: o, path: c, workspaceRoot: a });
  if (!(await t.jr.stat(d, e)).isDirectory())
    throw Error(`directoryPath must point to a directory`);
  let p = await t.jr.readdir(d, { withFileTypes: !0 }, e);
  return {
    directoryPath: l,
    entries: (
      await Promise.all(
        p
          .filter((e) => i || !e.name.startsWith(`.`))
          .map((t) =>
            sN({
              appServerClient: e,
              entry: t,
              path: c,
              realWorkspaceRoot: u,
              resolvedDirectoryPath: d,
              resolvedWorkspaceRoot: f,
            }),
          ),
      )
    )
      .filter((e) => e != null)
      .filter((e) => !r || e.type === `directory`)
      .sort((e, t) =>
        e.type === t.type ? e.name.localeCompare(t.name) : e.type === `directory` ? -1 : 1,
      ),
    parentPath: l.length === 0 ? null : uN(l),
  };
}
async function oN({ appServerClient: e, directoryPath: n, path: r, workspaceRoot: i }) {
  let a = r.resolve(i),
    o = r.resolve(a, n);
  if (!dN({ path: r, targetPath: o, workspaceRoot: a }))
    throw Error(`directoryPath must stay within workspaceRoot`);
  if (t.va(e.hostConfig))
    return {
      canonicalDirectoryPath: lN(r.relative(a, o)),
      realWorkspaceRoot: a,
      resolvedDirectoryPath: o,
      resolvedWorkspaceRoot: a,
    };
  let s = await l.promises.realpath(a);
  if (!dN({ path: r, targetPath: await l.promises.realpath(o), workspaceRoot: s }))
    throw Error(`directoryPath must stay within workspaceRoot`);
  return {
    canonicalDirectoryPath: lN(r.relative(a, o)),
    realWorkspaceRoot: s,
    resolvedDirectoryPath: o,
    resolvedWorkspaceRoot: a,
  };
}
async function sN({
  appServerClient: e,
  entry: n,
  path: r,
  realWorkspaceRoot: i,
  resolvedDirectoryPath: a,
  resolvedWorkspaceRoot: o,
}) {
  let s = r.join(a, n.name),
    c = typeof n.isSymbolicLink == `function` && n.isSymbolicLink(),
    u = n.isDirectory() ? `directory` : `file`;
  if (c)
    try {
      if ((await t.jr.stat(s, e)).isDirectory()) {
        if (
          t.va(e.hostConfig) ||
          !dN({ path: r, targetPath: await l.promises.realpath(s), workspaceRoot: i })
        )
          return null;
        u = `directory`;
      }
    } catch {}
  return { isSymlink: c, name: n.name, path: lN(r.relative(o, s)), type: u };
}
function cN(e) {
  let t = e?.trim() ?? ``;
  return t.length === 0 || t === `.` ? `` : lN(t);
}
function lN(e) {
  return e
    .replace(/\\/g, `/`)
    .replace(/^\.\/+/, ``)
    .replace(/\/+$/, ``);
}
function uN(e) {
  let t = lN(e);
  if (t.length === 0) return null;
  let n = t.split(`/`).filter(Boolean);
  return n.length === 1 ? `` : n.slice(0, -1).join(`/`);
}
function dN({ path: e, targetPath: t, workspaceRoot: n }) {
  let r = e.relative(n, t);
  return !(r === `..` || r.startsWith(`..${e.sep}`) || e.isAbsolute(r));
}
var fN = !!process.env.WSL_DISTRO_NAME,
  pN = 1e3,
  mN = t.Gr(`electron-fetch-handler`),
  hN = { supported: !1, configuredHotkey: null, configuredToggleHotkey: null, keepVisible: !1 },
  gN = {
    getState: () => hN,
    setHotkey: () => ({ success: !1, error: `Not supported.`, state: hN }),
    setKeepVisible: () => hN,
    setToggleHotkey: () => ({ success: !1, error: `Not supported.`, state: hN }),
    syncCommandKeybindings: () => void 0,
    getHistory: () => [],
    copyHistoryItem: () => !1,
  },
  _N = (e) => ({ preferWsl: fN, appServerClient: e }),
  vN = 5,
  yN = 5,
  bN = new Set(n.i.values.map((e) => n.r(e)));
async function xN({ appPath: e, bundleId: t, displayName: n }) {
  return {
    id: `app:${e}`,
    target: `systemDefault`,
    label: n,
    icon: (await dk(e)) ?? `apps/finder.png`,
    kind: `native`,
    appPath: e,
    bundleId: t,
    available: !0,
  };
}
async function SN({ filePath: e }) {
  let n = !r.m(e),
    i = await t.St(
      { filePath: e, ...(n ? {} : { limit: vN }), order: `usage`, prioritizedBundleIds: vk(e) },
      { listApplicationsForFile: ak },
    );
  return Promise.all(
    i
      .filter((e) => !n || !bN.has(e.bundleId))
      .slice(0, vN)
      .map(xN),
  );
}
async function CN({ nativeBrowserDiscovery: e }) {
  let n =
      process.platform === `darwin` || process.platform === `win32`
        ? a.app.getApplicationInfoForProtocol(`https://`).catch(() => null)
        : Promise.resolve(null),
    [r, i] = await Promise.all([t.Ct({ discovery: e, limit: yN, order: `usage` }), n]);
  return Promise.all(
    r.map(async (e) => ({ ...(await xN(e)), default: e.appPath === i?.path || void 0 })),
  );
}
function wN(e) {
  if (t.po(e)) return !0;
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`;
  } catch {
    return !1;
  }
}
function TN({ persistPreferredTargetPath: e, hasExplicitTarget: t, hasExistingPreference: n }) {
  return e ? (t ? !0 : !n) : !1;
}
var EN = class extends Error {};
function DN({ globalState: e, threadId: n, pinned: r }) {
  let i = TM(e),
    a = r ? [...i.filter((e) => e !== n), n] : i.filter((e) => e !== n);
  return i.length !== a.length || i.some((e, t) => e !== a[t])
    ? (e.set(t.Fo.PINNED_THREAD_IDS, a), !0)
    : !1;
}
function ON({ globalState: e, threadIds: n }) {
  let r = TM(e);
  return r.length !== n.length || r.some((e, t) => e !== n[t])
    ? (e.set(t.Fo.PINNED_THREAD_IDS, n), !0)
    : !1;
}
var kN = class {
  unsubscribeChatProcessNotificationHandler;
  bundledSkillsRoot;
  chatProcessNotificationHandler;
  chatProcessRegistry;
  pendingProcessManagerRootPids = new Set();
  petInstallManager;
  processManagerSnapshot = null;
  processManagerSnapshotExpiresAtMs = 0;
  processManagerSnapshotPromise = null;
  queuedFollowUpSendLocks = new t.l();
  constructor(
    e,
    n,
    r,
    i,
    a = r.getConnection(z),
    o,
    s,
    c,
    l,
    u,
    d = (e) => {
      throw Error(`App server state for host ID 1 ${e} not found`);
    },
    f = () => {
      throw Error(`Worktree service unavailable`);
    },
    p,
    m,
    h = t.Nt,
    g = process.cwd(),
    _ = gN,
    v = () => void 0,
    y = () => {},
    b,
    x,
    S = () => {},
    C,
    w = () => {},
    T,
  ) {
    ((this.globalState = e),
      (this.getIpcClientForOrigin = n),
      (this.appServerConnectionRegistry = r),
      (this.gitManager = i),
      (this.appServerClient = a),
      (this.remoteConnectionsHandler = o),
      (this.hotkeyWindowLifecycleManager = s),
      (this.windowManager = c),
      (this.terminalManager = l),
      (this.isWorkspaceDependenciesFeatureEnabled = u),
      (this.getAppServerClientForHostId = d),
      (this.getWorktreeService = f),
      (this.requestGitWorker = p),
      (this.sharedObjectRepository = m),
      (this.errorReporter = h),
      (this.repoRoot = g),
      (this.globalDictationHotkeyController = _),
      (this.reloadOpenBrowserPages = v),
      (this.refreshApplicationMenu = y),
      (this.requestComputerUseCaptureWorker = b),
      (this.subscribeComputerUseCaptureWorkerEvent = x),
      (this.refreshBrowserSidebarOwnerCommandAccelerators = S),
      (this.settingsStore = C),
      (this.updateDockIcon = w),
      (this.realtimeSessionRecorder = T),
      (this.bundledSkillsRoot = WN()),
      (this.chatProcessRegistry = new t.$t({
        codexHome: t.Mr({ preferWsl: t.Br(Iu) }),
        rootPid: process.pid,
      })),
      (this.chatProcessNotificationHandler = new t.tn(this.chatProcessRegistry)),
      (this.unsubscribeChatProcessNotificationHandler = this.appServerConnectionRegistry
        .getConnection(z)
        .registerInternalNotificationHandler((e) => {
          this.chatProcessNotificationHandler.handle(e).catch((t) => {
            mN().warning(`Failed to register chat process notification`, {
              safe: { method: e.method },
              sensitive: { error: t },
            });
          });
        })),
      (this.petInstallManager = new SM({ appServerClient: this.appServerClient, preferWsl: fN })),
      t
        .a({
          refresh: !1,
          preferWsl: fN,
          bundledRepoRoot: this.bundledSkillsRoot,
          appServerClient: this.appServerClient,
        })
        .catch((e) => {
          mN().warning(`Failed to warm recommended skills cache`, {
            safe: {},
            sensitive: { error: e },
          });
        }));
  }
  dispose() {
    this.unsubscribeChatProcessNotificationHandler();
  }
  notifyPinnedThreadsUpdated() {
    this.windowManager.sendMessageToAllRegisteredWindows({ type: `pinned-threads-updated` });
  }
  async listProcessManagerSnapshot(e = []) {
    for (let t of e) this.pendingProcessManagerRootPids.add(t);
    if (this.processManagerSnapshot != null && Date.now() < this.processManagerSnapshotExpiresAtMs)
      return this.processManagerSnapshot;
    if (this.processManagerSnapshotPromise != null) return this.processManagerSnapshotPromise;
    let n = Date.now(),
      r = this.chatProcessRegistry.list().then(async (e) => {
        let n = Array.from(this.pendingProcessManagerRootPids),
          r = await t.wn(GN(process.pid, Array.from(new Set([...qN(e), ...n]))));
        for (let e of n) this.pendingProcessManagerRootPids.delete(e);
        return r;
      });
    this.processManagerSnapshotPromise = r;
    try {
      return (
        (this.processManagerSnapshot = await r),
        (this.processManagerSnapshotExpiresAtMs = n + pN),
        this.processManagerSnapshot
      );
    } finally {
      this.processManagerSnapshotPromise === r && (this.processManagerSnapshotPromise = null);
    }
  }
  handlers = {
    "active-workspace-roots": async () => ({
      roots: oe(r.i(this.globalState), Iu, this.shouldUseWslPaths()),
    }),
    "workspace-root-options": async ({ hostId: e }) => {
      let n = e === `local` ? Iu : this.getAppServerClientForHostIdOrThrow(e).hostConfig;
      if (n.id !== `local` && t.va(n)) return (FM(this.globalState), r.a(this.globalState, n.id));
      let i = n.id === `local` && this.shouldUseWslPaths(),
        a = ce(r.o(this.globalState), n, i),
        o = r.s(this.globalState),
        s = await se(o, n, i);
      return {
        ...(Object.keys(s).length === 0 ? {} : { canonicalPathByRoot: s }),
        roots: oe(o, n, i),
        labels: a,
      };
    },
    "add-workspace-root-option": async ({ root: e, label: t, setActive: n, origin: i }) => {
      let a = ne(e, this.shouldUseWslPaths());
      return (
        t && r.f(this.globalState, (e) => ({ ...e, [a]: t })),
        r.p(this.globalState, (e) => [a, ...(e ?? []).filter((e) => e !== a)]),
        i.send(R, { type: `workspace-root-options-updated` }),
        n && (r.u(this.globalState, [a]), i.send(R, { type: `active-workspace-roots-updated` })),
        { success: !0 }
      );
    },
    "electron-clone-workspace-repo": async () => ({
      success: !1,
      canceled: !1,
      error: `Not implemented in Electron.`,
    }),
    "worktree-create-managed": async ({
      hostId: e,
      cwd: t,
      startingState: n,
      localEnvironmentConfigPath: r,
      streamId: i,
    }) =>
      this.getWorktreeService(e).createManagedWorktree({
        hostId: e,
        cwd: t,
        startingState: n,
        localEnvironmentConfigPath: r,
        streamId: i,
      }),
    "worktree-delete": async ({ hostId: e, worktree: t, reason: n }) => {
      await this.getWorktreeService(e).deleteWorktree(t, n);
    },
    "worktree-set-owner-thread": async ({ hostId: e, worktree: t, conversationId: n }) => {
      await this.getWorktreeService(e).setOwnerThread({ worktree: t, conversationId: n });
    },
    "codex-home": async (e) => {
      mN().info(`codex-home request`, { safe: { params: e }, sensitive: { hostId: e.hostId } });
      let n = this.getRequestAppServerClient(e.hostId),
        r = n.hostConfig,
        i = r.id === `local` && this.shouldUseWslPaths(),
        a = await n.platformPath(),
        o = t.va(r) ? await n.codexHome() : t.Mr({ preferWsl: fN });
      return { codexHome: I(o, i), worktreesSegment: I(a.join(o, `worktrees`), i) };
    },
    "projectless-thread-cwd": async (e) => {
      let t = await hi({
          appServerClient: this.appServerClient,
          createSplitDirectories: e?.createSplitDirectories !== !1,
          directoryName: e?.directoryName ?? null,
          homeDirectory: (0, o.homedir)(),
          prompt: e?.prompt ?? null,
        }),
        n = this.shouldUseWslPaths();
      return {
        cwd: I(t.cwd, n),
        outputDirectory: I(t.outputDirectory, n),
        workspaceRoot: I(t.workspaceRoot, n),
      };
    },
    "projectless-workspace-root": async () => ({
      workspaceRoot: I(
        await gi({ appServerClient: this.appServerClient, homeDirectory: (0, o.homedir)() }),
        this.shouldUseWslPaths(),
      ),
    }),
    "home-directory": async (e) => ({ homeDirectory: await this.getHomeDirectory(e?.hostId) }),
    "auto-deny-some-permissions": async ({ hostId: e, permissions: n, threadDetailLevel: r }) => ({
      permissions: t.an({
        homeDirectory: await this.getHomeDirectory(e),
        permissions: n,
        threadDetailLevel: r,
      }),
    }),
    "download-internal-plugins": async ({
      config: e,
      hostId: t,
      largeInternalPluginsConfig: n,
    }) => (
      nO(t),
      tO({ config: e, errorReporter: this.errorReporter, largeInternalPluginsConfig: n })
    ),
    "app-connect-oauth-callback-url": async () => ({
      callbackUrl: `codex://connector/oauth_callback`,
    }),
    "fast-mode-rollout-metrics": async (e) =>
      t.At({ codexHome: t.Mr({ preferWsl: fN }), params: e }),
    "refresh-remote-connections": async () =>
      this.remoteConnectionsHandler.refreshRemoteConnections(),
    "discover-remote-ssh-connections": async () =>
      this.remoteConnectionsHandler.discoverRemoteConnections(),
    "refresh-remote-control-connections": async () =>
      this.remoteConnectionsHandler.refreshRemoteControlConnections(),
    "rename-remote-control-environment": async ({ envId: e, name: t }) =>
      this.remoteConnectionsHandler.renameRemoteControlEnvironment({ envId: e, name: t }),
    "delete-remote-control-environment": async ({ envId: e }) =>
      this.remoteConnectionsHandler.deleteRemoteControlEnvironment({ envId: e }),
    "authorize-remote-control-connections": async () =>
      this.remoteConnectionsHandler.authorizeRemoteControlConnections(),
    "set-remote-control-connections-enabled": async ({ enabled: e, origin: t }) => {
      if (!this.windowManager.getPrimaryWindows().some((e) => e.webContents === t)) {
        let e = this.sharedObjectRepository?.get(`remote_control_connections`);
        if (e == null) throw Error(`Remote control connections are unavailable`);
        return { remoteControlConnections: e };
      }
      return this.remoteConnectionsHandler.setRemoteControlConnectionsEnabled(e);
    },
    "set-local-remote-control-enabled": async ({ enabled: e }) => {
      let n = t.Dt({ enabled: e });
      return (this.sharedObjectRepository?.set(`local_remote_control_enabled`, n), { enabled: n });
    },
    "save-codex-managed-remote-ssh-connections": async ({ remoteConnections: e }) =>
      this.remoteConnectionsHandler.saveCodexManagedRemoteSshConnections(e),
    "set-remote-connection-auto-connect": async ({ hostId: e, autoConnect: t }) =>
      this.remoteConnectionsHandler.setRemoteConnectionAutoConnect(e, t),
    "install-remote-codex": async ({ hostId: e }) =>
      this.remoteConnectionsHandler.installRemoteCodex(e),
    "start-remote-chatgpt-login-port-forward": async ({ hostId: e, loginId: t }) =>
      this.remoteConnectionsHandler.startRemoteChatgptLoginPortForward(e, t),
    "stop-remote-chatgpt-login-port-forward": async ({ hostId: e, loginId: t }) =>
      this.remoteConnectionsHandler.stopRemoteChatgptLoginPortForward(e, t),
    "app-server-connection-state": async ({ hostId: e }) =>
      this.remoteConnectionsHandler.appServerConnectionState(e),
    "remote-workspace-directory-entries": async ({
      hostId: e,
      directoryPath: t,
      directoriesOnly: n,
    }) => this.remoteConnectionsHandler.remoteWorkspaceDirectoryEntries(e, t, n),
    "workspace-directory-entries": async ({
      hostId: e,
      workspaceRoot: n,
      directoryPath: r,
      directoriesOnly: i,
      includeHidden: a,
    }) => {
      let o = this.getRequestAppServerClient(e ?? void 0),
        s = o.hostConfig,
        c = s.id === `local` && this.shouldUseWslPaths(),
        l = t.va(s) ? n : (this.mapAgentPathToLocalPath(n, s) ?? n),
        {
          directoryPath: u,
          entries: d,
          parentPath: f,
        } = await aN({
          appServerClient: o,
          directoryPath: r,
          directoriesOnly: i,
          includeHidden: a,
          workspaceRoot: l,
        });
      return { workspaceRoot: I(l, c), directoryPath: u, parentPath: f, entries: d };
    },
    "has-custom-cli-executable": async () => ({
      hasCustomCliExecutable: this.appServerConnectionRegistry
        .getConnection(z)
        .hasCustomCliExecutable(),
    }),
    "account-info": async () => {
      try {
        let e = await this.appServerConnectionRegistry
          .getConnection(z)
          .getAuthToken({ refreshToken: !1 });
        if (!e)
          return { accountId: null, userId: null, plan: null, email: null, computeResidency: null };
        let [, t] = e.split(`.`);
        if (!t) throw Error(`Missing token payload`);
        let n = JSON.parse(Buffer.from(t, `base64url`).toString(`utf8`)),
          r = n[`https://api.openai.com/auth`],
          i = n[`https://api.openai.com/profile`],
          a = r && typeof r == `object` && r ? r.chatgpt_account_id : null,
          o = r && typeof r == `object` && r ? r.chatgpt_user_id : null,
          s = r && typeof r == `object` && r ? r.chatgpt_plan_type : null,
          c = r && typeof r == `object` && r ? r.chatgpt_compute_residency : null,
          l = i && typeof i == `object` && i ? i.email : null;
        if (typeof a == `string` && typeof o == `string` && typeof s == `string`)
          return {
            accountId: a,
            userId: o,
            plan: s,
            email: typeof l == `string` ? l : null,
            computeResidency: typeof c == `string` ? c : null,
          };
      } catch (e) {
        mN().error(`Unable to extract account id and plan from auth token`, {
          safe: {},
          sensitive: { error: e },
        });
      }
      return { accountId: null, userId: null, plan: null, email: null, computeResidency: null };
    },
    "email-domain-mail-provider": async ({ domain: e }) => ({ provider: await t.Ft(e) }),
    "extension-info": async () => {
      let e = n.i.resolve();
      return {
        version: t._s(a.app.getVersion()).version,
        buildNumber: r.M.value,
        buildFlavor: e,
        osName: t.ra(process.platform) ?? null,
        systemVersion:
          typeof process.getSystemVersion == `function` ? process.getSystemVersion() : null,
        appName: a.app.getName(),
        appBrand: r.H(),
        appIconMedium: await BN(),
      };
    },
    "third-party-notices": async () => ({ text: await XM() }),
    "locale-info": async () => ({
      ideLocale: a.app.getLocale(),
      systemLocale: a.app.getSystemLocale(),
    }),
    "os-info": async () => ({
      platform: process.platform,
      osVersion: await t.y(),
      osRelease: o.default.release(),
      isSystemBackdropSupported: vM(),
      hasWsl: t.Fr() != null,
      isVsCodeRunningInsideWsl: !1,
    }),
    "wsl-bash-availability": async () => t.Ir(),
    "chronicle-permissions": async () => {
      let e = UN(this.repoRoot),
        t = this.appServerConnectionRegistry
          .getMaybeConnection(z)
          ?.getChronicleSidecarControlState();
      return {
        accessibility: VN(),
        screenRecording: HN(),
        chronicleSidecarPresent: e,
        chronicleSidecarProcessState: t?.state ?? `disabled`,
      };
    },
    "computer-use-app-approvals-visibility": async () => ({ hasApprovalStore: await oE() }),
    "computer-use-app-approvals-read": async () => aE(),
    "computer-use-app-approval-remove": async ({ bundleIdentifier: e }) => cE(e),
    "computer-use-sound-mode-read": async () => ({ value: await KE() }),
    "computer-use-sound-mode-write": async ({ value: e }) => ({ value: await qE(e) }),
    "computer-use-background-auth-read": async () => {
      let [e, t] = await Promise.all([LE(), RE()]);
      return { enabled: e, ...t };
    },
    "computer-use-background-auth-write": async ({ enabled: e }) => ({ enabled: await zE(e) }),
    "browser-browsing-data-clear": async ({ dataTypes: e }) => {
      (await ly(e), this.reloadOpenBrowserPages());
    },
    "browser-use-origin-state-read": async () => Cy(),
    "browser-use-approval-mode-write": async ({ approvalMode: e }) => wy(e),
    "browser-use-history-approval-mode-write": async ({ approvalMode: e }) => Ty(e),
    "browser-use-file-transfer-approval-mode-write": async ({ approvalMode: e, kind: t }) =>
      Ey(t, e),
    "browser-use-full-cdp-access-enabled-write": async ({ enabled: e }) => Dy(e),
    "browser-use-origin-add": async ({ kind: e, targetOrigin: t }) => Oy(e, t),
    "browser-use-origin-remove": async ({ kind: e, targetOrigin: t }) => Ay(e, t),
    "browser-use-file-transfer-origin-add": async ({ kind: e, targetOrigin: t, transferKind: n }) =>
      ky(n, e, t),
    "browser-use-file-transfer-origin-remove": async ({
      kind: e,
      targetOrigin: t,
      transferKind: n,
    }) => jy(n, e, t),
    "chrome-extension-settings-open": async ({ extensionId: e }) => {
      await Yo({ extensionId: e });
    },
    "chrome-extension-installed-read": async ({ extensionId: e }) => ({
      installed: Jo({ extensionId: e }),
    }),
    "child-processes": async () => {
      let e = t.xn();
      try {
        let t = await this.listProcessManagerSnapshot(),
          n = await this.chatProcessRegistry.listLiveOsPids(t);
        return { isAvailable: !0, rootProcess: e, processes: KN(t, process.pid, n) };
      } catch (t) {
        return (
          mN().error(`Failed to list child processes`, { safe: {}, sensitive: { error: t } }),
          { isAvailable: !1, rootProcess: e, processes: [] }
        );
      }
    },
    "child-process-kill": async ({ pid: e }) => {
      try {
        return {
          killed: await t.Sn(process.pid, e, {
            listChildProcesses: async (e) => {
              let n = await t.wn(GN(e, qN(await this.chatProcessRegistry.list())));
              return KN(n, e, await this.chatProcessRegistry.listLiveOsPids(n));
            },
          }),
        };
      } catch (e) {
        return (
          mN().error(`Failed to kill child process`, { safe: {}, sensitive: { error: e } }),
          { killed: !1 }
        );
      }
    },
    "chat-process-live-pids": async ({ conversationId: e, processTargets: n = [], turnId: r }) => {
      try {
        let i = new Set(n.flatMap((e) => (e.processId == null ? [] : [e.processId]))),
          a = (await this.chatProcessRegistry.list()).filter(
            (t) =>
              t.conversationId === e &&
              (t.turnId === r || (t.processId != null && i.has(t.processId))),
          ),
          o = JN(n);
        return a.length === 0 && o.length === 0 && !YN(n)
          ? { processes: [] }
          : {
              processes: t.en({
                childProcesses: await this.listProcessManagerSnapshot(o),
                conversationId: e,
                processTargets: n,
                records: a,
                turnId: r,
              }),
            };
      } catch (e) {
        return (
          mN().error(`Failed to list live chat process PIDs`, {
            safe: {},
            sensitive: { error: e },
          }),
          { processes: [] }
        );
      }
    },
    "chat-process-register": async ({ persistIfUnmatched: e, record: t }) => {
      await this.chatProcessRegistry.upsert(t, { persistIfUnmatched: e });
    },
    "chat-processes": async () => ({ processes: await this.chatProcessRegistry.list() }),
    "node-repl-active-execs-kill": async ({ sessionId: e, turnId: n }) =>
      t.yt({ codexHome: t.Mr({ preferWsl: this.shouldUseWslPaths() }), sessionId: e, turnId: n }),
    "is-packaged": async () => ({ isPackaged: a.app.isPackaged }),
    "global-dictation-hotkey-state": async () => this.globalDictationHotkeyController.getState(),
    "global-dictation-set-hotkey": async ({ hotkey: e }) => {
      let n = this.globalDictationHotkeyController.setHotkey(e);
      return (
        n.success &&
          (await t.qt({
            commandId: `globalDictationHold`,
            update: e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        n
      );
    },
    "global-dictation-set-toggle-hotkey": async ({ hotkey: e }) => {
      let n = this.globalDictationHotkeyController.setToggleHotkey(e);
      return (
        n.success &&
          (await t.qt({
            commandId: `globalDictationToggle`,
            update: e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        n
      );
    },
    "global-dictation-set-keep-visible": async ({ keepVisible: e }) =>
      this.globalDictationHotkeyController.setKeepVisible(e),
    "global-dictation-capture-fn-hotkey": async () => ({ hotkey: await hO(`Fn`) }),
    "global-dictation-history": async () => ({
      items: this.globalDictationHotkeyController.getHistory(),
    }),
    "global-dictation-copy-history-item": async ({ id: e }) => ({
      success: this.globalDictationHotkeyController.copyHistoryItem(e),
    }),
    "codex-command-keymap-state": async () => {
      await gM({
        commandId: `hotkeyWindow`,
        hotkey: this.hotkeyWindowLifecycleManager.getState().configuredHotkey,
      });
      let e = this.globalDictationHotkeyController.getState();
      return (
        await gM({ commandId: `globalDictationHold`, hotkey: e.configuredHotkey }),
        await gM({ commandId: `globalDictationToggle`, hotkey: e.configuredToggleHotkey }),
        t.Wt()
      );
    },
    "set-codex-command-keybinding": async ({ commandId: e, update: n }) => {
      zN(e, n);
      let r = await t.qt({ commandId: e, update: n });
      return (
        e === `hotkeyWindow` &&
          this.hotkeyWindowLifecycleManager.syncCommandKeybinding(mM(e, r).hotkey),
        (e === `globalDictationHold` || e === `globalDictationToggle`) &&
          this.globalDictationHotkeyController.syncCommandKeybindings({
            holdHotkey: mM(`globalDictationHold`, r).hotkey,
            toggleHotkey: mM(`globalDictationToggle`, r).hotkey,
          }),
        this.refreshApplicationMenu(),
        this.refreshBrowserSidebarOwnerCommandAccelerators(),
        r
      );
    },
    "reset-codex-command-keybindings": async () => {
      let e = await t.Kt();
      return (
        this.hotkeyWindowLifecycleManager.syncCommandKeybinding(mM(`hotkeyWindow`, e).hotkey),
        this.globalDictationHotkeyController.syncCommandKeybindings({
          holdHotkey: mM(`globalDictationHold`, e).hotkey,
          toggleHotkey: mM(`globalDictationToggle`, e).hotkey,
        }),
        this.refreshApplicationMenu(),
        this.refreshBrowserSidebarOwnerCommandAccelerators(),
        e
      );
    },
    "feedback-desktop-log-archive": async () =>
      (await i.t(new Date(), this.realtimeSessionRecorder)) ?? {
        archiveId: null,
        archivePath: null,
      },
    "delete-feedback-desktop-log-archive": async ({ archiveId: e }) => ({ success: await i.n(e) }),
    "openai-api-key": async () => ({ value: process.env.OPENAI_API_KEY ?? null }),
    "recommended-skills": async ({ hostId: e, refresh: n }) => {
      let r = this.getRequestAppServerClient(e);
      return t.a({
        refresh: n,
        preferWsl: fN,
        bundledRepoRoot: this.bundledSkillsRoot,
        appServerClient: r,
      });
    },
    "local-custom-agents": async ({ roots: e }) =>
      t.Er({ roots: e ?? [], preferWsl: fN, appServerClient: this.appServerClient }),
    "install-recommended-skill": async ({
      hostId: e,
      skillId: n,
      repoPath: r,
      installRoot: i,
      skillStatsigOverride: a,
      forceReinstall: o,
    }) => {
      try {
        let s = this.getRequestAppServerClient(e),
          { destination: c } = await t.i({
            skillId: n,
            repoPath: r,
            installRoot: i,
            skillStatsigOverride: a,
            forceReinstall: o,
            preferWsl: fN,
            bundledRepoRoot: this.bundledSkillsRoot,
            appServerClient: s,
          });
        return { success: !0, destination: c, error: null };
      } catch (e) {
        let t = e instanceof Error ? e.message : String(e);
        return (
          mN().warning(`Failed to install recommended skill`, {
            safe: {},
            sensitive: { error: t },
          }),
          { success: !1, destination: null, error: t }
        );
      }
    },
    "remove-skill": async ({ hostId: e, skillPath: n }) => {
      try {
        let { deletedPath: r } = await t.r({
          skillPath: n,
          preferWsl: fN,
          appServerClient: this.getRequestAppServerClient(e),
        });
        return { success: !0, deletedPath: r, error: null };
      } catch (e) {
        let t = e instanceof Error ? e.message : String(e);
        return (
          mN().warning(`Failed to remove skill`, { safe: {}, sensitive: { error: t } }),
          { success: !1, deletedPath: null, error: t }
        );
      }
    },
    "read-file": async ({ path: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n ?? void 0),
        i = await this.getFileRequestPath(e, r);
      return { contents: await t.jr.readFile(i, r) };
    },
    "read-file-metadata": async ({
      contentSampleByteLimit: e,
      contentSampleMaxFileBytes: n,
      path: r,
      hostId: i,
    }) => {
      let a = this.getRequestAppServerClient(i),
        o = await this.getFileRequestPath(r, a),
        s = await t.jr.stat(o, a),
        c = s.isFile(),
        l = await t.Ea(
          e != null && c && (n == null || typeof s.size != `number` || s.size <= n)
            ? await t.jr.readFileSampleBytes(o, a, e)
            : null,
        );
      return {
        ...(l == null ? {} : { contentKind: l }),
        isFile: c,
        mtimeMs: typeof s.mtimeMs == `number` ? s.mtimeMs : null,
        sizeBytes: typeof s.size == `number` ? s.size : null,
      };
    },
    "write-file": async ({ content: e, expectedMtimeMs: n, hostId: r, path: i }) => {
      let a = this.getRequestAppServerClient(r),
        o = await this.getFileRequestPath(i, a),
        s = null;
      try {
        let e = await t.jr.stat(o, a, { bypassCache: !0 });
        s = typeof e.mtimeMs == `number` ? e.mtimeMs : null;
      } catch (e) {
        let t = e;
        if (!(`code` in t) || t.code !== `ENOENT`) throw e;
      }
      if (n !== s) return { outcome: `conflict`, mtimeMs: s };
      await t.jr.writeFile(o, e, a);
      let c = await t.jr.stat(o, a);
      return { outcome: `saved`, mtimeMs: typeof c.mtimeMs == `number` ? c.mtimeMs : null };
    },
    "ensure-directory": async ({ hostId: e, path: n }) => {
      let r = this.getRequestAppServerClient(e),
        i = await this.getFileRequestPath(n, r);
      return (await t.jr.mkdir(i, { recursive: !0 }, r), {});
    },
    "read-file-binary": async ({ path: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n),
        i = await t.O({ appServerClient: r, url: e });
      if (i != null) return i;
      if (e.startsWith(`https://`)) return { contentsBase64: null };
      let a = await this.getFileRequestPath(e, r),
        o = await t.jr.readFileBase64(a, r),
        s = typeof o == `string` ? o : o.toString(`base64`),
        c = await t.Da(typeof o == `string` ? Buffer.from(o, `base64`) : o);
      return { contentsBase64: s, ...(c == null ? {} : { mimeType: c }) };
    },
    "upload-local-file-over-sftp": async ({ sourcePath: e, targetPath: t, hostId: n }) => {
      await PN({
        sourcePath: e,
        targetPath: t,
        hostConfig: this.getRequestAppServerClient(n).hostConfig,
      });
    },
    "compile-latex-artifact": async ({ path: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n);
      return t.va(r.hostConfig)
        ? { contentsBase64: null }
        : GD(
            await this.getFileRequestPath(e, r),
            await QD(this.appServerConnectionRegistry.getConnection(r.hostConfig.id)),
          );
    },
    "read-git-file-binary": async ({ cwd: e, hostId: n, path: r, ref: i }) => {
      let a = this.getRequestAppServerClient(n),
        o = a.hostConfig,
        s = t.ht(a),
        c = this.mapAgentPathToLocalPath(e, o);
      if (c == null) return { contentsBase64: null };
      try {
        let e = (await this.gitManager.getWorktreeRepository(t.xa(c), s))?.root;
        if (!e) return { contentsBase64: null };
        let n = await a.platformPath(),
          o = t.lt(e, c, r);
        if (n.isAbsolute(o) || o.startsWith(`..`)) return { contentsBase64: null };
        let l = await t.ft(e, [`show`, i === `head` ? `HEAD:${o}` : `:${o}`], s);
        if (l.code !== 0) return { contentsBase64: null };
        let u = l.stdoutBuffer;
        if (u == null || u.length === 0) return { contentsBase64: null };
        let d = await t.Da(u);
        return {
          contentsBase64: Buffer.from(u).toString(`base64`),
          ...(d == null ? {} : { mimeType: d }),
        };
      } catch {
        return { contentsBase64: null };
      }
    },
    "codex-agents-md": async ({ hostId: e }) => t.kr(_N(this.getRequestAppServerClient(e))),
    "codex-agents-md-save": async ({ hostId: e, contents: n }) =>
      t.Ar(n, _N(this.getRequestAppServerClient(e))),
    "pet-install-preview": async ({ name: e, description: t, imageUrl: n }) =>
      this.petInstallManager.preview({ name: e, description: t, imageUrl: n }),
    "pet-install": async ({ name: e, description: t, imageUrl: n }) =>
      this.petInstallManager.install({ name: e, description: t, imageUrl: n }),
    "generate-thread-title": async ({ hostId: e, prompt: t, cwd: n }) => {
      try {
        return {
          title: await ZM({
            prompt: t,
            cwd: n ?? null,
            serviceTier: null,
            appServerConnection: this.appServerConnectionRegistry.getConnection(e),
          }),
        };
      } catch (e) {
        return (
          mN().warning(`Failed to generate thread title`, { safe: {}, sensitive: { error: e } }),
          { title: null }
        );
      }
    },
    "generate-commit-message": async ({ hostId: e, prompt: n, cwd: r, signal: i }) => {
      try {
        return {
          message: await t.Pt({
            prompt: n,
            cwd: r ?? null,
            model: t.Tr,
            serviceTier: null,
            client: this.appServerConnectionRegistry.getConnection(e),
            timeoutMs: t.K,
            signal: i,
            ...t.Y,
          }),
        };
      } catch (e) {
        return (
          mN().warning(`Failed to generate commit message`, { safe: {}, sensitive: { error: e } }),
          { message: null }
        );
      }
    },
    "generate-commit-pull-request-message": async ({ hostId: e, prompt: n, cwd: r, signal: i }) => {
      try {
        let a = await t.Pt({
          prompt: n,
          cwd: r ?? null,
          model: t.Tr,
          serviceTier: null,
          client: this.appServerConnectionRegistry.getConnection(e),
          timeoutMs: t.q,
          signal: i,
          ...t.X,
        });
        return { message: a?.message ?? null, title: a?.title ?? null, body: a?.body ?? null };
      } catch (e) {
        return (
          mN().warning(`Failed to generate commit and pull request messages`, {
            safe: {},
            sensitive: { error: e },
          }),
          { message: null, title: null, body: null }
        );
      }
    },
    "generate-pull-request-message": async ({ hostId: e, prompt: n, cwd: r, signal: i }) => {
      try {
        let a = await t.Pt({
          prompt: n,
          cwd: r ?? null,
          model: t.Tr,
          serviceTier: null,
          client: this.appServerConnectionRegistry.getConnection(e),
          timeoutMs: t.J,
          signal: i,
          ...t.Z,
        });
        return { title: a?.title ?? null, body: a?.body ?? null };
      } catch {
        return { title: null, body: null };
      }
    },
    "inbox-items": async ({ limit: e }) => ({ items: t.E(e ?? 200), unreadRunCounts: t.T() }),
    "list-automations": async () => ({ items: t.er() }),
    "ambient-suggestions": async ({ hostId: e, projectRoot: n }) => {
      let r = await this.getAmbientSuggestionsProjectRoot(n);
      return r.projectRoot == null ? { file: t.Ms } : { file: t.Sr(e, r.projectRoot) };
    },
    "ambient-suggestions-refresh": async ({ hostId: e, projectRoot: n, mode: r }) => {
      let i = await this.getAmbientSuggestionsProjectRoot(n);
      if (i.projectRoot != null)
        try {
          let n = this.appServerConnectionRegistry.getConnection(e),
            a = await Ge({ appServerConnection: n, settingsStore: this.getSettingsStore() });
          await t.Cr({
            hostId: e,
            projectRoot: i.projectRoot,
            isProjectlessChat: i.isProjectlessChat,
            enabled: a.enabled,
            appServerConnection: n,
            serviceTier: null,
            staleTimeMs: a.staleTimeMs,
            mode: r,
          });
        } catch (e) {
          mN().warning(`Failed to refresh ambient suggestions`, {
            safe: {},
            sensitive: { error: e, projectRoot: i.projectRoot },
          });
        }
    },
    "ambient-suggestions-generation-statuses": async () => ({ statuses: t.xr() }),
    "ambient-suggestion-set-status": async ({
      hostId: e,
      projectRoot: n,
      suggestionId: r,
      status: i,
    }) => {
      let a = await this.getAmbientSuggestionsProjectRoot(n);
      return a.projectRoot == null
        ? { file: t.Ms }
        : { file: t.wr({ hostId: e, projectRoot: a.projectRoot, suggestionId: r, status: i }) };
    },
    "developer-instructions": async ({
      baseInstructions: e,
      cwd: t,
      hostId: n,
      instructionOverrides: r,
      threadToolsEnabled: i,
      threadId: a,
    }) => ({
      instructions: await this.getProjectAwareDeveloperInstructions({
        baseInstructions: e,
        cwd: t,
        hostId: n ?? `local`,
        instructionOverrides: r,
        threadToolsEnabled: i,
        threadId: a,
      }),
    }),
    "automation-run-archive": async ({
      threadId: e,
      archivedAssistantMessage: n,
      archivedUserMessage: r,
      archivedReason: i,
    }) => {
      if (n != null || r != null) t.hr(e, r ?? null, n ?? null);
      else
        try {
          await this.appServerConnectionRegistry
            .getConnection(z)
            .captureAutomationArchiveMessages(e);
        } catch (e) {
          mN().warning(`Failed to capture automation archive messages`, {
            safe: { error: e instanceof Error ? e.message : String(e) },
            sensitive: {},
          });
        }
      let a = t.pr(e, i ?? void 0);
      return (
        a && this.appServerConnectionRegistry.getConnection(z).notifyAutomationRunsUpdated(),
        { success: a }
      );
    },
    "automation-run-delete": async ({ threadId: e }) => {
      let n = t.dr(e);
      return (
        n && this.appServerConnectionRegistry.getConnection(z).notifyAutomationRunsUpdated(),
        { success: n }
      );
    },
    "delete-archived-thread": async ({ hostId: e, threadId: n }) => {
      let r = this.appServerConnectionRegistry.getConnection(e),
        i = await t.lr({ appServer: r, threadId: n });
      return (r.notifyAutomationRunsUpdated(), { deletedThreadIds: i });
    },
    "delete-all-archived-threads": async ({ hostId: e }) => {
      let n = this.appServerConnectionRegistry.getConnection(e),
        r = await t.cr({ appServer: n });
      return (r.length > 0 && n.notifyAutomationRunsUpdated(), { deletedThreadIds: r });
    },
    "list-pinned-threads": async () => ({ threadIds: TM(this.globalState) }),
    "set-thread-pinned": async ({ threadId: e, pinned: n, beforeThreadId: r }) => {
      let i =
        n && r !== void 0
          ? ON({
              globalState: this.globalState,
              threadIds: t.ta({ threadIds: TM(this.globalState), threadId: e, beforeThreadId: r }),
            })
          : DN({ globalState: this.globalState, threadId: e, pinned: n });
      return (this.notifyPinnedThreadsUpdated(), await this.globalState.flush(), { success: i });
    },
    "set-pinned-threads-order": async ({ threadIds: e }) => {
      let t = ON({ globalState: this.globalState, threadIds: e });
      return (this.notifyPinnedThreadsUpdated(), await this.globalState.flush(), { success: t });
    },
    "automation-create": async (e) => {
      let n = t.Jn(e);
      if (!n) throw Error(`Automation create failed.`);
      return { item: n };
    },
    "automation-update": async (e) => {
      let n = t.ir(e);
      if (!n) throw Error(t.Ts);
      return { item: n };
    },
    "automation-delete": async ({ id: e }) => {
      let n = t.Xn(e),
        r = t.Yn(e),
        i = r === `deleted` || r === `not_found`;
      if (i)
        try {
          (t.fr(e),
            this.appServerConnectionRegistry.getConnection(z).notifyAutomationRunsUpdated());
        } catch {}
      return { item: n, success: i, status: r };
    },
    "automation-run-now": async ({ id: e, collaborationMode: t, permissions: n }) => (
      await Ki({
        appVersion: a.app.getVersion(),
        automationId: e,
        appServerConnection: this.appServerConnectionRegistry.getConnection(z),
        desktopFeatureAvailability: Re(),
        gitManager: this.gitManager,
        globalState: this.globalState,
        settingsStore: this.getSettingsStore(),
        appServerClient: this.appServerClient,
        worktreeLifecycle: {
          requestCleanup: () => {
            this.getWorktreeService(z).requestCleanup();
          },
          setOwnerThread: ({ conversationId: e, worktree: t }) =>
            this.getWorktreeService(z).setOwnerThread({ conversationId: e, worktree: t }),
        },
        isPackaged: a.app.isPackaged,
        repoRoot: this.repoRoot,
        collaborationMode: t,
        permissions: n,
      }),
      { success: !0 }
    ),
    "find-files": async ({ query: e, cwd: t }) => {
      let n = typeof t == `string` && t.trim().length > 0 ? this.mapAgentPathToLocalPath(t) : null;
      return {
        files: (await rO({ query: e ?? ``, cwd: n ?? this.getWorkspaceRoot() })).map((e) => ({
          ...e,
          path: I(e.path, this.shouldUseWslPaths()),
          fsPath: I(e.fsPath, this.shouldUseWslPaths()),
        })),
      };
    },
    "paths-exist": async ({ hostId: e, paths: n }) => {
      let r = this.getRequestAppServerClient(e),
        i = r.hostConfig.id === `local` && this.shouldUseWslPaths();
      return { existingPaths: ee(await t.Dr(re(n ?? [], i), r), i) };
    },
    "claude-code-import-status": async ({ hostId: e }) =>
      FD({ appServerClient: this.getRequestAppServerClient(e), providers: [`claude-code`] }),
    "external-agent-imported-connectors": async ({ hostId: e, providers: n }) => {
      let r = this.getRequestAppServerClient(e);
      if (t.va(r.hostConfig)) return { connectors: [] };
      let i = await r.codexHome();
      return BD({ codexHome: this.mapAgentPathToLocalPath(i, r.hostConfig) ?? i, providers: n });
    },
    ...uD({
      getAppServerConnection: (e) => this.appServerConnectionRegistry.getConnection(e),
      getStatus: (e) =>
        FD({ appServerClient: this.getRequestAppServerClient(e.hostId), providers: e.providers }),
      providerAdapters: {
        "claude-cowork": qT({
          getAppServerConnection: (e) => this.appServerConnectionRegistry.getConnection(e),
          getRequestAppServerClient: (e) => this.getRequestAppServerClient(e),
          recordProjectlessThread: async ({
            outputDirectory: e,
            threadId: n,
            workspaceRoot: r,
          }) => {
            (this.globalState.update(t.Fo.PROJECTLESS_THREAD_IDS, (e) =>
              e?.includes(n) ? e : [...(e ?? []), n],
            ),
              this.globalState.update(t.Fo.THREAD_WORKSPACE_ROOT_HINTS, (e) => ({ ...e, [n]: r })),
              this.globalState.update(t.Fo.THREAD_PROJECTLESS_OUTPUT_DIRECTORIES, (t) => ({
                ...t,
                [n]: e,
              })));
          },
        }),
      },
    }),
    "local-environment": async ({ configPath: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths(),
        o = await t.vt(t.va(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e), t.ht(r));
      return { environment: { ...o, configPath: I(o.configPath, a) } };
    },
    "local-environments": async ({ hostId: e, workspaceRoot: n }) => {
      let r = this.getRequestAppServerClient(e),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths();
      return {
        environments: (
          await t._t(t.va(i) ? n : (this.mapAgentPathToLocalPath(n, i) ?? n), t.ht(r))
        ).map((e) => ({ ...e, configPath: I(e.configPath, a) })),
      };
    },
    "local-environment-config": async ({ configPath: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n),
        i = r.hostConfig,
        a = i.id === `local` && this.shouldUseWslPaths(),
        o = t.va(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e);
      try {
        let e = await t.jr.readFile(o, r);
        return { configPath: I(o, a), exists: !0, raw: e };
      } catch (e) {
        let t = e;
        if (`code` in t && t.code === `ENOENT`)
          return { configPath: I(o, a), exists: !1, raw: null };
        throw e;
      }
    },
    "local-environment-config-save": async ({ configPath: e, hostId: n, raw: r }) => {
      let i = this.getRequestAppServerClient(n),
        a = i.hostConfig,
        o = a.id === `local` && this.shouldUseWslPaths(),
        c = t.va(a) ? e : (this.mapAgentPathToLocalPath(e, a) ?? e);
      return (
        await t.jr.mkdir(s.dirname(c), { recursive: !0 }, i),
        await t.jr.writeFile(c, r, i),
        { configPath: I(c, o), success: !0 }
      );
    },
    "is-copilot-api-available": async () => ({ available: !1 }),
    "get-copilot-api-proxy-info": async () => null,
    "get-global-state": async ({ key: e }) => (
      (e === t.Fo.REMOTE_PROJECTS ||
        e === t.Fo.ACTIVE_REMOTE_PROJECT_ID ||
        e === t.Fo.WORKSPACE_ROOT_OPTIONS ||
        e === t.Fo.WORKSPACE_ROOT_LABELS ||
        e === t.Fo.ACTIVE_WORKSPACE_ROOTS ||
        e === t.Fo.PROJECT_ORDER ||
        e === t.Fo.REMOTE_CWDS_BY_HOST_AND_WORKSPACE) &&
        FM(this.globalState),
      { value: this.getGlobalStateValue(e) }
    ),
    "set-global-state": async ({ key: e, value: t, origin: n }) => (
      this.setGlobalStateValue(e, t, n),
      { success: !0 }
    ),
    "queued-follow-up-send-lock-acquire": async ({
      conversationId: e,
      messageId: t,
      lockId: n,
    }) => ({
      acquired: this.queuedFollowUpSendLocks.tryAcquire({
        conversationId: e,
        messageId: t,
        lockId: n,
      }),
    }),
    "queued-follow-up-send-lock-release": async ({
      conversationId: e,
      messageId: t,
      lockId: n,
      sent: r,
    }) => {
      this.queuedFollowUpSendLocks.release({ conversationId: e, messageId: t, lockId: n, sent: r });
    },
    "get-configuration": async ({ key: e }) => ({ value: this.getConfigurationValue(e) }),
    "set-configuration": async ({ key: e, value: t }) => (
      this.setConfigurationValue(e, t),
      { success: !0 }
    ),
    "get-setting": async ({ key: e }) => ({ value: this.getSettingValue(e) }),
    "get-settings": async () => ({ values: this.getSettingValues() }),
    "set-setting": async ({ key: e, value: t }) => (this.setSettingValue(e, t), { success: !0 }),
    "settings-read": async () => ({
      filePath: this.getSettingsStore().getStateFilePath(),
      settings: this.getToolVisibleSettings(),
      effectiveSettings: this.getToolVisibleEffectiveSettings(),
      definitions: t.ci,
    }),
    "settings-write": async ({ settings: e }) => {
      let n = Object.entries(e).map(([e, n]) => {
        let r = t.ai(e);
        if (r == null) throw Error(`Unknown setting: ${e}`);
        if (r.agentAccess !== `read-write`) throw Error(`Setting cannot be written by Codex: ${e}`);
        return { definition: r, value: t.oi(r.key).parse(n) };
      });
      for (let { definition: e, value: t } of n) this.setSettingValue(e.key, t);
      return (
        await this.getSettingsStore().flush(),
        {
          settings: this.getToolVisibleSettings(),
          effectiveSettings: this.getToolVisibleEffectiveSettings(),
        }
      );
    },
    "apply-patch": async ({ origin: e, ...n }) => {
      let r = n.hostConfig,
        i = this.getAppServerClientForHostIdOrThrow(r.id);
      return t.ct(n, this.gitManager, t.ht(i), ie(t.Br(r)));
    },
    "mcp-codex-config": async ({ cwd: e }) => ({ config: await this.buildMcpCodexConfig(e) }),
    "worktree-shell-environment-config": async ({ cwd: e, hostId: n }) => {
      let r = this.getRequestAppServerClient(n),
        i = r.hostConfig,
        a = t.va(i) ? e : (this.mapAgentPathToLocalPath(e, i) ?? e),
        o = await this.gitManager.getWorktreeRepository(t.xa(a), t.ht(r));
      return { shellEnvironment: o == null ? null : await t.ot(o) };
    },
    "ide-context": async ({ workspaceRoot: e, origin: t }) => {
      if (!e) throw Error(`workspaceRoot is required`);
      let n = this.getIpcClientForOrigin(t);
      if (!n) throw Error(`Missing IPC client for window`);
      let r = await n.sendRequest(`ide-context`, { workspaceRoot: e });
      if (r.resultType === `error`) throw Error(`Failed to get ide context: ${r.error}`);
      return { ideContext: r.result.ideContext };
    },
    "ipc-request": async ({ method: e, params: t, targetClientId: n, timeoutMs: r, origin: i }) => {
      let a = this.getIpcClientForOrigin(i);
      if (!a) throw Error(`Missing IPC client for window`);
      return await a.sendRequest(e, t, { targetClientId: n, timeoutMs: r });
    },
    "open-in-targets": async ({
      cwd: e,
      hostId: n,
      nativeBrowserDiscovery: r = `scan`,
      path: i,
    }) => {
      let a = this.getRequestAppServerClient(n ?? void 0),
        o = await oM(a.hostConfig),
        s = await Zj(),
        c = new Set(o),
        l = uM(this.getSettingsStore(), e, c),
        u = i?.replace(/^([ab])[\\/]/, ``) ?? null,
        d = u != null && wN(u) && !t.va(a.hostConfig),
        f =
          u == null || d || t.va(a.hostConfig)
            ? null
            : this.resolveOpenFilePath(
                this.mapAgentPathToLocalPath(u, a.hostConfig) ?? u,
                this.mapAgentPathToLocalPath(e, a.hostConfig) ?? this.getWorkspaceRoot(),
              ),
        p = d || (f != null && t.fo(f)),
        m = f != null && yk(f),
        h = f != null && xk(f),
        g = p ? await CN({ nativeBrowserDiscovery: r }) : m ? await SN({ filePath: f }) : [];
      return {
        preferredTarget: l,
        availableTargets: Array.from(c),
        mode: p || h ? `native` : `editor`,
        targets: [
          ...s.map(({ id: e, label: t, icon: n, kind: r, hidden: i }) => ({
            id: e,
            target: e,
            label: t,
            icon: n,
            kind: r,
            hidden: i,
            available: c.has(e),
            default: l === e || void 0,
          })),
          ...g,
        ],
      };
    },
    "set-preferred-app": async ({ target: e }) => (
      fM(this.getSettingsStore(), null, e),
      { success: !0 }
    ),
    "terminal-shell-options": async () => ({ availableShells: RM() }),
    "thread-terminal-snapshot": async ({ threadId: e, origin: t }) => ({
      session: this.terminalManager.getSnapshotForConversationId(e, t.id),
    }),
    "pick-file": async ({ pickerTitle: e }) => ({
      file: (await this.pickFilesFromDialog({ allowMultiple: !1, title: e }))[0] ?? null,
    }),
    "pick-files": async ({ imagesOnly: e, pickerTitle: t }) => ({
      files: await this.pickFilesFromDialog({ allowMultiple: !0, imagesOnly: e, title: t }),
    }),
    "save-file": async (e) => {
      let t = await a.dialog.showSaveDialog({ defaultPath: e.suggestedFilename });
      if (t.canceled || !t.filePath) return { path: null };
      if (e.kind === `contents`)
        return (
          await (0, p.writeFile)(t.filePath, Buffer.from(e.contentsBase64, `base64`)),
          { path: t.filePath }
        );
      let n = this.getRequestAppServerClient(e.hostId);
      return (
        await FN({
          sourcePath: await this.getFileRequestPath(e.sourcePath, n),
          targetPath: t.filePath,
          hostConfig: n.hostConfig,
        }),
        { path: t.filePath }
      );
    },
    "add-context-file": async ({ hostId: e, path: t, origin: n }) => {
      let r = {
        label: (await this.getAppServerClientForHostIdOrThrow(e).platformPath()).basename(t),
        path: t,
        fsPath: t,
      };
      return (n.send(R, { type: `add-context-file`, file: r }), { success: !0 });
    },
    "open-file": async ({
      path: e,
      target: n,
      appPath: r,
      line: i,
      column: o,
      cwd: s,
      persistPreferredTargetPath: c,
      hostId: l,
      openMode: u,
      origin: d,
    }) => {
      try {
        let f = this.getRequestAppServerClient(l ?? void 0),
          p = f.hostConfig;
        if (n === `systemDefault` && wN(e) && !t.va(p))
          return (
            r == null ? await a.shell.openExternal(e) : await $j(`systemDefault`, e, r, null, p),
            { success: !0 }
          );
        let m = e.replace(/^([ab])[\\/]/, ``),
          h = this.mapAgentPathToLocalPath(m, p) ?? m,
          g = this.mapAgentPathToLocalPath(s, p) ?? this.getWorkspaceRoot(),
          _ = t.va(p),
          v = _ ? s : null,
          y = n != null,
          b = await f.platformPath(),
          S = this.resolveOpenFilePath(h, g),
          C = (_ ? null : await this.getWorkspacePathType(f, S)) === `directory`,
          w = typeof i == `number` ? { line: i, column: typeof o == `number` ? o : 1 } : null,
          T = v != null || (_ && t.S(m, b)) ? this.resolveOpenFilePath(m, v, b) : null;
        if (
          !_ &&
          !C &&
          _b({
            browserPaneEnabled: Re().browserPane,
            link: { type: `file`, location: w, path: S, target: n },
          })
        )
          return (
            d.send(R, {
              open: !0,
              type: `toggle-browser-panel`,
              url: (0, x.pathToFileURL)(S).toString(),
              source: `manual`,
              initiator: `open_in_browser_bridge`,
            }),
            { success: !0 }
          );
        let E = await oM(p),
          D = new Set(E),
          O = n ?? null,
          k = O != null && _ && !D.has(O);
        if (
          ((O = Ck({
            requestedTarget: O,
            requestedTargetWasFiltered: k,
            preferredTarget: uM(this.getSettingsStore(), v ?? g, D),
            shouldUsePlatformDefaultViewer: !_ && !C && bk(S),
            shouldUseFileManager: C && D.has(`fileManager`),
            hasExplicitTarget: y,
            allowPlatformFallback: !_,
            platform: process.platform,
          })),
          !O)
        )
          throw Error(`No available open target`);
        let A = !_ && yk(S);
        TN({
          persistPreferredTargetPath: c,
          hasExplicitTarget: y,
          hasExistingPreference: c != null && dM(this.getSettingsStore(), c),
        }) && fM(this.getSettingsStore(), c ?? null, O);
        let j = u === `workspace` && w == null ? null : T;
        return (
          A && w == null && !y && sM(O) && (O = `fileManager`),
          await $j(O, S, r, w, p, v, j),
          { success: !0 }
        );
      } catch (e) {
        return (
          mN().error(`Failed to open file`, { safe: {}, sensitive: { error: e } }), { success: !1 }
        );
      }
    },
    "git-origins": async ({ dirs: e, hostId: n }) => {
      if (!this.requestGitWorker) throw Error(`Git worker request function is not configured`);
      let i = this.getRequestAppServerClient(n).hostConfig,
        a = t.Br(i),
        s = re(e ?? [], a).map((e) => t.Sa(e)),
        c = I((0, o.homedir)(), a),
        l = r.s(this.globalState),
        u = r.i(this.globalState),
        d = l.length > 0 ? l : (u ?? []),
        f = s && s.length > 0 ? s : d.filter((e) => e !== `~`).map((e) => t.Sa(e)),
        p = t.pt(),
        { origins: m } = await this.requestGitWorker({
          method: `git-origins`,
          params: { dirs: f, hostConfig: i, operationSource: p.source },
        });
      return {
        origins: m.map((e) => ({
          ...e,
          dir: t.xa(I(e.dir, a)),
          root: t.Sa(I(e.root, a)),
          commonDir: e.commonDir == null ? null : I(e.commonDir, a),
        })),
        homeDir: c,
      };
    },
    "git-push": async ({ signal: e, ...n }) =>
      t.G(n, this.gitManager, t.ht(this.getRequestAppServerClient(n.hostId)), e),
    "git-create-branch": async ({ signal: e, ...n }) =>
      t.et(n, this.gitManager, t.ht(this.getRequestAppServerClient(n.hostId)), e),
    "git-checkout-branch": ({ signal: e, ...n }) =>
      t.tt(n, t.ht(this.getRequestAppServerClient(n.hostId)), { signal: e }),
    "gh-cli-status": async (e) => (
      (e?.hostId == null || e.hostId === `local`) && (await Q_()),
      t.z(this.getRequestAppServerClient(e?.hostId))
    ),
    "gh-current-user": async (e) => t.B(this.getRequestAppServerClient(e?.hostId)),
    "gh-pr-create": async ({ signal: e, ...n }) =>
      t.R(n, this.gitManager, this.getRequestAppServerClient(n.hostId), e),
    "gh-pr-board": (e) => t.I(e, this.gitManager, (e) => this.getRequestAppServerClient(e)),
    "gh-pr-body": (e) => t.M(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-checks": (e) => t.N(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-comments": (e) => t.P(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-status": (e) => t.F(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-diff": (e) => t.j(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-comment": async (e) => t.L(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-merge": async (e) => t.A(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "gh-pr-update": async (e) => t.k(e, this.gitManager, this.getRequestAppServerClient(e.hostId)),
    "git-merge-base": async ({ gitRoot: e, baseBranch: n, hostId: r }) => {
      let i = this.getRequestAppServerClient(r),
        a = await this.resolveGitRootFromRequest(e, i);
      if (!a || !n) return { mergeBaseSha: null };
      let o = await this.gitManager.getWorktreeRepository(t.xa(a), t.ht(i));
      return { mergeBaseSha: o == null ? null : await t.nt(o, n) };
    },
    "prepare-worktree-snapshot": async ({ gitRoot: e, snapshotBranch: n }) => {
      if (!e) throw Error(`gitRoot is required`);
      let r = await this.gitManager.getWorktreeRepository(t.xa(e), t.ht(this.appServerClient));
      if (r == null) throw Error(`gitRoot must belong to a Git worktree`);
      return t.W(r, n ?? null);
    },
    "upload-worktree-snapshot": async ({
      tarballPath: e,
      uploadUrl: n,
      contentLength: r,
      contentType: i,
    }) => {
      try {
        return (
          await t.U({ tarballPath: e, uploadUrl: n, contentLength: r, contentType: i }),
          { success: !0 }
        );
      } catch (e) {
        return (
          mN().error(`Failed to upload worktree snapshot`, { safe: {}, sensitive: { error: e } }),
          { success: !1 }
        );
      }
    },
    "confirm-trace-recording-start": async () => ({ success: tN() }),
    "cancel-trace-recording-start": async () => ({ success: nN() }),
    "submit-trace-recording-details": async ({ note: e, conversationId: t }) => ({
      success: iN({ note: e, conversationId: t }),
    }),
    "set-vs-context": async () => {
      throw new EN();
    },
    "native-desktop-apps": async (e) => ({ apps: await t.xt(e) }),
    "native-desktop-app-by-bundle-id": async ({ bundleId: e }) => ({ app: await t.bt(e) }),
    "computer-use-frontmost-window": async () => (process.platform === `darwin` ? _l() : null),
    "computer-use-start-capture": async ({
      animationDestination: e,
      bundleIdentifier: t,
      origin: n,
      requestId: r,
    }) => {
      if (
        process.platform !== `darwin` ||
        this.requestComputerUseCaptureWorker == null ||
        this.subscribeComputerUseCaptureWorkerEvent == null
      )
        return null;
      let i = AN({
        backgroundColor: e.backgroundColor,
        cornerRadius: e.cornerRadius,
        primaryTextColor: e.primaryTextColor,
        viewportFrame: e.viewportFrame,
        webContents: n,
      });
      return i == null
        ? null
        : eD({
            animationTarget: i,
            bundleIdentifier: t,
            origin: n,
            requestComputerUseCaptureWorker: this.requestComputerUseCaptureWorker,
            requestId: r,
            subscribeComputerUseCaptureWorkerEvent: this.subscribeComputerUseCaptureWorkerEvent,
          });
    },
    "native-desktop-app-icon": async ({ appPath: e }) => ({ iconSmall: await dk(e) }),
    "computer-use-native-desktop-app-icon": async ({ appPath: e }) => ({ iconSmall: await vl(e) }),
  };
  async pickFilesFromDialog({ allowMultiple: e, imagesOnly: n = !1, title: r }) {
    let i = await a.dialog.showOpenDialog({
      ...(n ? { filters: [{ extensions: [...t.ti], name: `Images` }] } : {}),
      properties: [`openFile`, ...(e ? [`multiSelections`] : [])],
      title: r,
    });
    return i.canceled || i.filePaths.length === 0
      ? []
      : i.filePaths.map((e) => ({ label: s.basename(e), path: e, fsPath: e }));
  }
  async getWorkspaceDirectoryPath(e, n) {
    let r = n?.trim() || (await this.getDefaultWorkspaceDirectoryPath(e));
    if (!(await t.jr.stat(r, e)).isDirectory()) throw Error(`Directory not found: ${r}`);
    return r;
  }
  async getDefaultWorkspaceDirectoryPath(e) {
    let n = e.hostConfig;
    if (!t.va(n)) return (0, o.homedir)();
    let r = await e.platformPath(),
      i = await e.codexHome();
    return r.dirname(i);
  }
  async getParentDirectoryPath(e, t) {
    let n = (await e.platformPath()).dirname(t);
    return n === t ? null : n;
  }
  async getWorkspacePathType(e, n) {
    try {
      return (await t.jr.stat(n, e)).isDirectory() ? `directory` : `file`;
    } catch {
      return `missing`;
    }
  }
  resolveOpenFilePath(e, n, r = s) {
    let i = e.replace(/^([ab])[\\/]/, ``),
      a = r.normalize(process.platform === `win32` ? t.Po(i) : i);
    if (t.S(a, r) || !n) return a;
    let o = a.split(/[\\/]+/).filter(Boolean);
    if (o.length === 0) return n;
    let c = r.join(n, ...o);
    if ((0, l.existsSync)(c)) return c;
    let u = r.basename(n),
      d = o.indexOf(u);
    if (d !== -1) {
      let e = r.join(n, ...o.slice(d + 1));
      return ((0, l.existsSync)(e), e);
    }
    return c;
  }
  async getFileRequestPath(e, n) {
    return t.va(n.hostConfig)
      ? this.resolveOpenFilePath(e, null, await n.platformPath())
      : this.resolveOpenFilePath(
          this.mapAgentPathToLocalPath(e, n.hostConfig) ?? e,
          this.getWorkspaceRoot(),
        );
  }
  mapRemoteHomeToLocalPath(e, n = Iu) {
    if (!e) return e ?? null;
    if (!t.va(n)) return e;
    let r = n.home_dir;
    if (!r) return e;
    let i = (0, o.homedir)();
    return e === r ? i : e.startsWith(`${r}/`) ? `${i}${e.slice(r.length)}` : e;
  }
  mapAgentPathToLocalPath(e, n = Iu) {
    let r = this.mapRemoteHomeToLocalPath(e, n);
    return r && ne(r, t.Br(n));
  }
  async getProjectAwareDeveloperInstructions({
    baseInstructions: e,
    cwd: n,
    hostId: r,
    instructionOverrides: i,
    threadToolsEnabled: a,
    threadId: o,
  }) {
    let s = this.getAppServerClientForHostIdOrThrow(r),
      c = await this.isWorkspaceDependenciesFeatureEnabled(r).catch(
        (e) => (
          mN().warning(
            `Failed to check workspace dependencies feature for developer instructions`,
            { safe: {}, sensitive: { error: e } },
          ),
          !1
        ),
      ),
      l = await this.isNonGitWorkspace(n, s),
      u = this.getSettingsStore(),
      d = u.getEffective(t.pi.conversationDetailMode.key) === `STEPS_PROSE`;
    return t.in({
      baseInstructions: e,
      gitSettings: {
        branchPrefix: u.getEffective(t.gi.branchPrefix.key),
        commitInstructions: u.getEffective(t.gi.commitInstructions.key),
        pullRequestInstructions: u.getEffective(t.gi.pullRequestInstructions.key),
      },
      isNonGitWorkspace: l,
      instructionOverrides: i,
      threadToolsEnabled: a,
      workspaceDependenciesEnabled: c,
      includeProseDetailLevelInstructions: d,
      threadId: o,
    });
  }
  async confirmPrimaryRuntimeDependencyReset(e) {
    return (
      (
        await a.dialog.showMessageBox({
          type: `warning`,
          buttons: [`Reset and install`, `Cancel`],
          cancelId: 1,
          defaultId: 1,
          message: `Reset Codex Workspace dependencies?`,
          detail: `Codex will delete this directory and install Workspace dependencies again:\n\n${e}`,
        })
      ).response === 0
    );
  }
  async isNonGitWorkspace(e, n) {
    let r = t.va(n.hostConfig) ? e : (this.mapAgentPathToLocalPath(e, n.hostConfig) ?? e);
    if (!r) return !1;
    try {
      return (await this.gitManager.getWorktreeRepository(t.xa(r), t.ht(n))) == null;
    } catch (e) {
      return (
        mN().warning(
          `Failed to determine whether developer instructions should omit git guidance for a non-git workspace`,
          { safe: { error: e instanceof Error ? e.message : String(e) }, sensitive: { error: e } },
        ),
        !1
      );
    }
  }
  shouldUseWslPaths() {
    return t.Br(Iu);
  }
  async getHomeDirectory(e) {
    let n = this.getRequestAppServerClient(e),
      r = n.hostConfig;
    if (t.va(r)) {
      let e = await n.platformPath();
      return r.home_dir ?? e.dirname(await n.codexHome());
    }
    return I((0, o.homedir)(), t.Br(r));
  }
  async getAmbientSuggestionsProjectRoot(e) {
    return e == null
      ? { projectRoot: null, isProjectlessChat: !1 }
      : e === `~`
        ? {
            projectRoot: t.xa(I((0, o.homedir)(), this.shouldUseWslPaths())),
            isProjectlessChat: !0,
          }
        : { projectRoot: e, isProjectlessChat: !1 };
  }
  getWorkspaceRoot() {
    return (r.i(this.globalState)[0] ?? null) || (r.s(this.globalState)[0] ?? null ?? null);
  }
  async getSelectedGitRoot(e) {
    let n = r.i(this.globalState)[0];
    return n
      ? ((await this.gitManager.getWorktreeRepository(t.xa(n), t.ht(e)))?.root ?? null)
      : null;
  }
  async resolveGitRootFromRequest(e, n) {
    if (e) {
      let r = e.trim();
      if (r.length > 0) {
        let e = (await this.gitManager.getWorktreeRepository(t.xa(r), t.ht(n)))?.root;
        if (e) return e;
      }
    }
    return this.getSelectedGitRoot(n);
  }
  getRequestAppServerClient(e) {
    return e == null || e === `local` ? this.appServerClient : this.getAppServerClientForHostId(e);
  }
  getAppServerClientForHostIdOrThrow(e) {
    return e === `local` ? this.appServerClient : this.getAppServerClientForHostId(e);
  }
  async buildMcpCodexConfig(e) {
    let t = this.appServerConnectionRegistry.getConnection(z);
    return (
      Dr({ settingsStore: this.getSettingsStore(), repoRoot: this.repoRoot }),
      nr({
        appServerConnection: t,
        appVersion: a.app.getVersion(),
        desktopFeatureAvailability: Re(),
        cwd: e,
        isPackaged: a.app.isPackaged,
        repoRoot: this.repoRoot,
        shouldUseWslPaths: this.shouldUseWslPaths(),
      })
    );
  }
  getGlobalStateValue(e) {
    return this.globalState.get(e);
  }
  setGlobalStateValue(e, n, r) {
    (this.globalState.set(e, n),
      this.windowManager.sendMessageToAllWindows({ type: `global-state-updated`, keys: [e] }),
      e === t.Fo.ACTIVE_WORKSPACE_ROOTS &&
        this.windowManager.sendMessageToAllWindows({ type: `active-workspace-roots-updated` }),
      e === t.Fo.REMOTE_PROJECTS && r.send(R, { type: `workspace-root-options-updated` }));
  }
  getConfigurationValue(e) {
    return this.globalState.get(e);
  }
  setConfigurationValue(e, t) {
    (this.globalState.set(e, t), this.applySettingSideEffects(e, t));
  }
  getSettingValue(e) {
    return this.getSettingsStore().getEffective(e);
  }
  getSettingValues() {
    return Object.fromEntries(t.di.map((e) => [e.key, this.getSettingValue(e.key)]));
  }
  getToolVisibleSettings() {
    let e = this.getSettingsStore();
    return Object.fromEntries(
      t.si.flatMap((t) => {
        let n = e.get(t.key);
        return n === void 0 ? [] : [[t.key, n]];
      }),
    );
  }
  getToolVisibleEffectiveSettings() {
    let e = this.getSettingsStore();
    return Object.fromEntries(
      t.si.flatMap((t) => {
        let n = e.getEffective(t.key);
        return n === void 0 ? [] : [[t.key, n]];
      }),
    );
  }
  setSettingValue(e, n) {
    let r = t.oi(e).parse(n);
    (this.getSettingsStore().set(e, r),
      kM(t.ai(e), r),
      this.applySettingSideEffects(e, r),
      Tr(e) && Dr({ settingsStore: this.getSettingsStore(), repoRoot: this.repoRoot }));
  }
  applySettingSideEffects(e, n) {
    (e === `appearanceTheme` && EM(n),
      (e === `appearanceTheme` ||
        e === `appearanceLightChromeTheme` ||
        e === `appearanceDarkChromeTheme`) &&
        this.windowManager.refreshWindowBackdrops(),
      e === t.yi.dockIconPreference.key && this.updateDockIcon());
  }
  getSettingsStore() {
    if (this.settingsStore == null) throw Error(`Settings store unavailable`);
    return this.settingsStore;
  }
  handleVSCodeRequest(e, n, r, i, a) {
    try {
      let o = n,
        s = this.handlers[o];
      if (typeof s != `function`)
        throw Error(
          `${n} not implemented in the current Electron process. Restart Codex to load the latest Electron handlers.`,
        );
      let c = () => s({ ...r, origin: e, signal: a });
      if (i == null) {
        if (t.Ca(n)) throw Error(`Missing git operation source for ${n}`);
        return c();
      }
      return t.mt({ source: i, requestKind: n }, c);
    } catch (e) {
      throw e instanceof EN ? Error(`${n} not implemented in Electron.`) : e;
    }
  }
};
function AN({
  backgroundColor: e,
  cornerRadius: t,
  primaryTextColor: n,
  viewportFrame: r,
  webContents: i,
}) {
  if (!MN(r) || !jN(t)) return;
  let o = a.BrowserWindow.fromWebContents(i);
  if (o == null || o.isDestroyed()) return;
  let s = i.getZoomFactor(),
    c = o.getContentBounds(),
    l = { x: c.x + r.x * s, y: c.y + r.y * s, width: r.width * s, height: r.height * s },
    u = a.screen.getDisplayMatching(l);
  return {
    codexDisplay: {
      bounds: NN(u.bounds),
      id: u.id,
      scaleFactor: u.scaleFactor,
      workArea: NN(u.workArea),
    },
    destinationBackgroundColor: e,
    destinationCornerRadius: t * s,
    destinationFrame: l,
    destinationPrimaryTextColor: n,
  };
}
function jN(e) {
  return e >= 0;
}
function MN(e) {
  return e.width > 0 && e.height > 0;
}
function NN(e) {
  return { height: e.height, width: e.width, x: e.x, y: e.y };
}
async function PN({ sourcePath: e, targetPath: t, hostConfig: n }) {
  await IN({ command: `put`, operation: `upload`, sourcePath: e, targetPath: t, hostConfig: n });
}
async function FN({ sourcePath: e, targetPath: t, hostConfig: n }) {
  await IN({ command: `get`, operation: `download`, sourcePath: e, targetPath: t, hostConfig: n });
}
async function IN({ command: e, operation: n, sourcePath: r, targetPath: i, hostConfig: a }) {
  if (!t.ya(a)) throw Error(`SFTP ${n}s require an SSH host`);
  let [o, ...s] = a.terminal_command;
  if (o !== `ssh`) throw Error(`SSH host is missing an ssh terminal command`);
  let c = RN(r),
    l = RN(i),
    u = (0, m.spawn)(`sftp`, [`-b`, `-`, ...LN(s)], { stdio: [`pipe`, `pipe`, `pipe`] });
  u.stdin?.end(`${e} ${c} ${l}\n`);
  let d = [];
  if (
    (u.stderr?.on(`data`, (e) => d.push(e)),
    (await new Promise((e) => {
      (u.on(`close`, (t) => e(t)), u.on(`error`, () => e(null)));
    })) !== 0)
  ) {
    let e = Buffer.concat(d).toString(`utf8`).trim();
    throw Error(`SFTP ${n} failed${e.length > 0 ? `: ${e}` : ``}`);
  }
}
function LN(e) {
  let t = [`-o`, `BatchMode=yes`];
  for (let n = 0; n < e.length; n += 1) {
    let r = e[n];
    if (r === `-p`) {
      let r = e[n + 1];
      if (r == null) throw Error(`SSH terminal command is missing a port value`);
      (t.push(`-P`, r), (n += 1));
      continue;
    }
    t.push(r);
  }
  return t;
}
function RN(e) {
  if (/[\r\n]/.test(e)) throw Error(`SFTP batch paths cannot contain newlines`);
  return `"${e.replace(/[\\"]/g, `\\$&`)}"`;
}
function zN(e, t) {
  if (e !== `hotkeyWindow` && e !== `globalDictationHold` && e !== `globalDictationToggle`) return;
  if (t.type === `append`) throw Error(`OS-global shortcuts support one keybinding.`);
  if (t.type !== `set` && t.type !== `replace`) return;
  let n = e === `hotkeyWindow` ? LO(t.accelerator) : qO(t.accelerator);
  if (n != null) throw Error(n);
}
async function BN() {
  return process.platform !== `darwin` || !a.app.isPackaged ? null : OE(r.E());
}
function VN() {
  return process.platform === `darwin`
    ? a.systemPreferences.isTrustedAccessibilityClient(!1)
      ? `granted`
      : `denied`
    : `unknown`;
}
function HN() {
  return process.platform === `darwin`
    ? a.systemPreferences.getMediaAccessStatus(`screen`)
    : `unknown`;
}
function UN(e) {
  return (
    t.Gn({
      buildFlavor: n.i.resolve(),
      repoRoot: e,
      resourcesPath: Ze({ env: process.env, resourcesPath: process.resourcesPath }),
    }) != null
  );
}
function WN() {
  let e = a.app.getAppPath();
  if (a.app.isPackaged) return s.join(e, `skills`);
  let t = s.join(e, `assets`, `skills`);
  if ((0, l.existsSync)(t)) return t;
  let n = s.join(e, `..`, `assets`, `skills`);
  return (0, l.existsSync)(n) ? n : null;
}
function GN(e, t) {
  return [{ pid: e, includeRoot: !1 }, ...t.map((e) => ({ pid: e, includeRoot: !0 }))];
}
function KN(e, n, r) {
  let i = t.bn(e, [{ pid: n, includeRoot: !1 }]),
    a = new Set(i.map((e) => e.pid));
  return t.bn(
    e,
    GN(
      n,
      r.filter((e) => !a.has(e)),
    ),
  );
}
function qN(e) {
  return Array.from(
    new Set(
      e.flatMap((e) => {
        let t = XN(e.processId);
        return [...(e.osPid == null ? [] : [e.osPid]), ...(t == null ? [] : [t])];
      }),
    ),
  );
}
function JN(e) {
  return Array.from(
    new Set(
      e.flatMap((e) => {
        let t = XN(e.processId);
        return t == null ? [] : [t];
      }),
    ),
  );
}
function YN(e) {
  return e.some((e) => e.command.trim().length > 0 && XN(e.processId) == null);
}
function XN(e) {
  if (e == null || !/^\d+$/u.test(e)) return null;
  let t = Number(e);
  return Number.isSafeInteger(t) ? t : null;
}
var ZN = class extends Error {
  constructor(e, t, n) {
    (super(e), (this.status = t), (this.cause = n));
  }
};
function QN(e, { inferAuth: n = !1 } = {}) {
  let r = (t.Oa(e) || n) && !t.ja(e, `authorization`);
  return { attachAuth: r, tokenSource: r ? `pending` : `not-required`, token: null };
}
async function $N({
  appServerClient: e,
  errorStatus: t,
  failureMessage: n,
  refreshToken: r,
  state: i,
}) {
  if (!i.attachAuth) return i;
  if (!r) {
    let t = e.getCachedAuthToken?.();
    if (t !== void 0) return { ...i, tokenSource: `cached`, token: t };
  }
  try {
    let t = await e.getAuthToken({ refreshToken: r });
    return { ...i, tokenSource: r ? `refreshed` : `loaded`, token: t };
  } catch (e) {
    throw new ZN(n, t, e);
  }
}
function eP({ desktopOriginator: e, headers: t, state: n }) {
  !n.attachAuth ||
    n.token == null ||
    ug(t, n.token, { desktopOriginator: e, includeSurfaceHeaders: !1 });
}
function tP(e, t) {
  return e.attachAuth && e.token != null && t.status === 401;
}
var nP = `X-OAI-IS`,
  rP = `X-OAI-IS-Update`,
  iP = /^ois1\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+$/;
function aP(e) {
  return typeof e != `string` || e.length === 0 || e.length > 2048 || e.trim() !== e
    ? !1
    : iP.test(e);
}
function oP(e) {
  let t = e.get(rP);
  return aP(t) ? t : null;
}
function sP({ currentState: e, expectedState: t, nextState: n }) {
  return aP(n) ? (aP(e) ? e : null) === t : !1;
}
function cP(e) {
  return {
    getRequestState() {
      let t = e.get(Yr);
      return aP(t) ? t : null;
    },
    clear() {
      e.delete(Yr);
    },
    storeResponseUpdate(t, n) {
      return lP(e, oP(t), n);
    },
  };
}
function lP(e, t, n) {
  if (!aP(t)) return !1;
  let r = !1;
  return (
    e.update(Yr, (e) =>
      sP({ currentState: e, expectedState: n, nextState: t }) ? ((r = !0), t) : (e ?? void 0),
    ),
    r
  );
}
var uP = 6,
  dP = new Set([
    `accounts`,
    `backend-api`,
    `by-repo`,
    `conversation`,
    `environments`,
    `account`,
    `announcement`,
    `data-controls`,
    `first-run`,
    `general-settings`,
    `git-settings`,
    `local-environments`,
    `mcp-settings`,
    `open-source-licenses`,
    `personalization`,
    `plan-summary`,
    `select-workspace`,
    `settings`,
    `usage-analytics`,
    `worktree-init-v2`,
    `worktrees`,
  ]);
function fP(e) {
  let t = e.length > 0 ? e : `/`;
  return t === `/`
    ? `/`
    : `/${t
        .split(`/`)
        .filter((e) => e.length > 0)
        .map((e) => {
          let t = null;
          try {
            t = decodeURIComponent(e);
          } catch {
            t = null;
          }
          let n = (t ?? e).toLowerCase();
          return n.length <= uP || dP.has(n) ? n : `:param`;
        })
        .join(`/`)}`;
}
function pP(e, t) {
  try {
    let n = new URL(t);
    if (n.protocol !== `http:` && n.protocol !== `https:`) return `${e} ${n.protocol}`;
    let r = fP(n.pathname);
    return `${e} ${n.origin}${r}`;
  } catch {
    return `${e} ${t}`;
  }
}
function mP(e) {
  if (!URL.canParse(e)) return;
  let t = new URL(e);
  if (t.protocol !== `http:` && t.protocol !== `https:`) return t.protocol;
  let n = fP(t.pathname);
  return `${t.origin}${n}`;
}
var hP = t.Gr(`electron-fetch-wrapper`),
  gP = `vscode://codex/`,
  _P = `/codex/dictation-stream-connect-info`,
  vP = `chatgpt-dictation`,
  yP = `openai-bearer.`,
  bP = class {
    abortControllers = new Map();
    constructor(e, t) {
      ((this.fetchHandler = e), (this.options = t));
    }
    getAppServerConnection(e) {
      return this.options.appServerConnectionRegistry.getConnection(e);
    }
    getAbortControllerKey(e, t) {
      return `${e.id}:${t}`;
    }
    async handleRequest(e, t) {
      if (t.url === _P) {
        await this.handleDictationStreamConnectInfoRequest(e, t);
        return;
      }
      let n = new AbortController(),
        r = this.getAbortControllerKey(e, t.requestId);
      if ((this.abortControllers.set(r, n), this.isVsCodeFetchRequest(t.url))) {
        try {
          await this.handleVsCodeFetchRequest(e, t, n.signal);
        } finally {
          this.abortControllers.delete(r);
        }
        return;
      }
      let i = this.ensureAbsoluteUrl(t.url),
        a = pP(t.method, i),
        o = performance.now(),
        s = null;
      try {
        let { headers: r, body: c } = this.prepareFetchInit(t);
        hP().debug(`Fetch`, {
          safe: {
            requestId: t.requestId,
            target: a,
            body: t.body == null ? `<no-body>` : `<body-redacted>`,
          },
          sensitive: {},
        });
        let l = await this.performDesktopFetch({
          body: c,
          headers: r,
          method: t.method,
          resolvedUrl: i,
          signal: n.signal,
        });
        s = l.metrics;
        let u = l.response,
          d = SP(u);
        if (u.ok) {
          let n = u.headers.get(`content-type`) ?? ``,
            r = `null`,
            i = 0,
            c = performance.now();
          if (u.status !== 204)
            if (n.includes(`application/json`)) ((r = await u.text()), (i = Buffer.byteLength(r)));
            else {
              let e = await u.arrayBuffer();
              i = e.byteLength;
              let t = Buffer.from(e).toString(`base64`);
              r = JSON.stringify({ base64: t, contentType: n });
            }
          ((s.timings.bodyReadMs = xP(c)),
            e.send(this.options.messageChannel, {
              type: `fetch-response`,
              responseType: `success`,
              requestId: t.requestId,
              status: u.status,
              headers: d,
              bodyJsonString: r,
            }),
            this.logFetchTiming({
              bodyBytes: i,
              metrics: s,
              requestId: t.requestId,
              responseType: `success`,
              status: u.status,
              target: a,
              totalMs: xP(o),
            }));
        } else {
          let n,
            r = 0,
            i = performance.now();
          try {
            let e = await u.text();
            ((r = Buffer.byteLength(e)),
              (n = e || u.statusText || `Request failed with status ${u.status}`));
          } catch {
            n = u.statusText || `Request failed with status ${u.status}`;
          }
          ((s.timings.bodyReadMs = xP(i)),
            e.send(this.options.messageChannel, {
              type: `fetch-response`,
              responseType: `error`,
              requestId: t.requestId,
              status: u.status,
              error: n,
            }),
            this.logFetchTiming({
              bodyBytes: r,
              metrics: s,
              requestId: t.requestId,
              responseType: `error`,
              status: u.status,
              target: a,
              totalMs: xP(o),
            }));
        }
      } catch (n) {
        let r = n instanceof Error && n.name === `AbortError`,
          i = wP(n),
          c = n instanceof ZN ? n : null;
        (e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: t.requestId,
          status: c?.status ?? (r ? 499 : 500),
          error: c?.message ?? (n instanceof Error ? n.message : `Unknown error`),
          ...(i == null ? {} : { errorCode: i }),
        }),
          this.logFetchTiming({
            bodyBytes: 0,
            metrics: s,
            requestId: t.requestId,
            responseType: `exception`,
            status: c?.status ?? (r ? 499 : 500),
            target: a,
            totalMs: xP(o),
          }));
      } finally {
        this.abortControllers.delete(r);
      }
    }
    cancelRequest(e, t) {
      let n = this.getAbortControllerKey(e, t.requestId),
        r = this.abortControllers.get(n);
      r && (r.abort(), this.abortControllers.delete(n));
    }
    handleStreamRequest(e, t) {
      let n = new AbortController();
      (this.abortControllers.set(this.getAbortControllerKey(e, t.requestId), n),
        this.runStreamRequest(e, t, n));
    }
    cancelStreamRequest(e, t) {
      hP().debug(`Cancel-fetch-stream received with requestId`, {
        safe: { requestId: t.requestId },
        sensitive: {},
      });
      let n = this.getAbortControllerKey(e, t.requestId),
        r = this.abortControllers.get(n);
      r && (r.abort(), this.abortControllers.delete(n));
    }
    async runStreamRequest(e, n, r) {
      let i = this.ensureAbsoluteUrl(n.url),
        a = pP(n.method, i),
        o = performance.now(),
        s = null;
      try {
        let { headers: c, body: l } = this.prepareFetchInit(n);
        hP().debug(`Fetch-stream`, { safe: { requestId: n.requestId, target: a }, sensitive: {} });
        let u = await this.performDesktopFetch({
          body: l,
          headers: c,
          method: n.method,
          resolvedUrl: i,
          signal: r.signal,
        });
        s = u.metrics;
        let d = u.response;
        if (!d.ok) {
          let t = SP(d),
            r = null,
            i = 0,
            c = performance.now();
          try {
            ((r = await d.text()), (i = Buffer.byteLength(r)));
          } catch (e) {
            hP().warning(`Fetch-stream error response body read failed`, {
              safe: { requestId: n.requestId, status: d.status },
              sensitive: { error: e, target: a },
            });
          }
          ((s.timings.bodyReadMs = xP(c)),
            this.logFetchTiming({
              bodyBytes: i,
              metrics: s,
              requestId: n.requestId,
              responseType: `stream-error`,
              status: d.status,
              target: a,
              totalMs: xP(o),
            }),
            hP().warning(`Fetch-stream error response`, {
              safe: {
                bodyBytes: i,
                requestId: n.requestId,
                responseHeaderNames: Object.keys(t).sort(),
                status: d.status,
                statusText: d.statusText || null,
              },
              sensitive: {
                cfRay: CP(t, `cf-ray`),
                requestIdHeader: CP(t, `request-id`),
                responseBody: r,
                responseHeaders: t,
                target: a,
                xOaiRequestId: CP(t, `x-oai-request-id`),
              },
            }));
          let l =
            d.statusText.length > 0
              ? `${d.status} ${d.statusText}`
              : `Request failed with status ${d.status}`;
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: n.requestId,
            error: l,
          });
          return;
        }
        this.logFetchTiming({
          bodyBytes: 0,
          metrics: s,
          requestId: n.requestId,
          responseType: `stream-open`,
          status: d.status,
          target: a,
          totalMs: xP(o),
        });
        let f = d.body;
        if (f == null) {
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: n.requestId,
            error: `Streaming response had no body.`,
          });
          return;
        }
        if (n.format === `ndjson`)
          for await (let i of t.Ma(f, r.signal))
            e.send(this.options.messageChannel, {
              type: `fetch-stream-event`,
              requestId: n.requestId,
              data: i,
            });
        else
          for await (let i of t.c(f, r.signal))
            e.send(this.options.messageChannel, {
              type: `fetch-stream-event`,
              requestId: n.requestId,
              event: i.event,
              data: i.data,
            });
        e.send(this.options.messageChannel, {
          type: `fetch-stream-complete`,
          requestId: n.requestId,
        });
      } catch (i) {
        if (r.signal.aborted || t.s(i)) {
          e.send(this.options.messageChannel, {
            type: `fetch-stream-complete`,
            requestId: n.requestId,
          });
          return;
        }
        (this.logFetchTiming({
          bodyBytes: 0,
          metrics: s,
          requestId: n.requestId,
          responseType: `stream-exception`,
          status: 500,
          target: a,
          totalMs: xP(o),
        }),
          hP().warning(`Fetch-stream failed`, {
            safe: { requestId: n.requestId, target: a },
            sensitive: { error: i },
          }),
          e.send(this.options.messageChannel, {
            type: `fetch-stream-error`,
            requestId: n.requestId,
            error: i instanceof Error ? i.message : `Unknown error`,
          }));
      } finally {
        this.abortControllers.delete(this.getAbortControllerKey(e, n.requestId));
      }
    }
    async performDesktopFetch({ body: e, headers: n, method: r, resolvedUrl: i, signal: o }) {
      let s = t.ka(n),
        c = t.Aa(n),
        l = QN(n, { inferAuth: this.shouldInferCodexApiAuth(i) }),
        u = {},
        d = {
          attachAuth: l.attachAuth,
          attachDesktopSurface: s,
          attachIntegrityState: c,
          hasToken: l.token != null,
          retriedAfter401: !1,
          timings: u,
          tokenSource: l.tokenSource,
        };
      if (l.attachAuth && !this.isDesktopAuthAllowedUrl(i))
        throw new ZN(`Refusing to attach authentication to non-OpenAI URL`, 400, null);
      if (s && !this.isDesktopAuthAllowedUrl(i))
        throw new ZN(`Refusing to attach desktop surface to non-OpenAI URL`, 400, null);
      if (c && !this.isDesktopAuthAllowedUrl(i))
        throw new ZN(`Refusing to attach integrity state to non-OpenAI URL`, 400, null);
      let f = () => {
          if (typeof e == `string`) return e;
          if (e == null) return;
          let t = new Uint8Array(e.byteLength);
          return (t.set(e), t.buffer);
        },
        p = async (e) => {
          let t = this.cloneHeaders(n);
          this.deleteHeader(t, nP);
          let l = c ? this.options.integrityStateStore.getRequestState() : null;
          (eP({ desktopOriginator: this.options.desktopOriginator, headers: t, state: e }),
            s && dg(t, { desktopOriginator: this.options.desktopOriginator }),
            c && l != null && this.setHeader(t, nP, l));
          let u = await a.net.fetch(i, { method: r, headers: t, body: f(), signal: o });
          return (c && this.options.integrityStateStore.storeResponseUpdate(u.headers, l), u);
        },
        m = performance.now();
      ((l = await $N({
        appServerClient: this.getAppServerConnection(z),
        errorStatus: 432,
        failureMessage: `Failed to retrieve authentication token`,
        refreshToken: !1,
        state: l,
      })),
        (u.authMs = xP(m)),
        (d.hasToken = l.token != null),
        (d.tokenSource = l.tokenSource));
      let h = performance.now(),
        g = await p(l);
      u.fetchMs = xP(h);
      let _ = tP(l, g);
      if (
        (g.status === 401 &&
          l.attachAuth &&
          hP().warning(`desktop_fetch_auth_401`, {
            safe: {
              hadToken: l.token != null,
              skipRetryReason: _ ? null : `no_token_attached`,
              target: pP(r, i),
              tokenSource: l.tokenSource,
              willRetry: _,
            },
            sensitive: {},
          }),
        _)
      ) {
        d.retriedAfter401 = !0;
        let e = performance.now();
        ((l = await $N({
          appServerClient: this.getAppServerConnection(z),
          errorStatus: 401,
          failureMessage: `Failed to refresh authentication token`,
          refreshToken: !0,
          state: l,
        })),
          (u.retryAuthMs = xP(e)),
          (d.hasToken = l.token != null),
          (d.tokenSource = l.tokenSource));
        let t = performance.now();
        ((g = await p(l)), (u.retryFetchMs = xP(t)));
      }
      return { metrics: d, response: g };
    }
    logFetchTiming({
      bodyBytes: e,
      metrics: t,
      requestId: n,
      responseType: r,
      status: i,
      target: a,
      totalMs: o,
    }) {
      hP().debug(`Fetch timing`, {
        safe: {
          requestId: n,
          target: a,
          responseType: r,
          status: i,
          totalMs: o,
          bodyBytes: e,
          attachAuth: t?.attachAuth ?? !1,
          attachDesktopSurface: t?.attachDesktopSurface ?? !1,
          attachIntegrityState: t?.attachIntegrityState ?? !1,
          hasToken: t?.hasToken ?? !1,
          retriedAfter401: t?.retriedAfter401 ?? !1,
          tokenSource: t?.tokenSource ?? `unknown`,
          authMs: t?.timings.authMs,
          fetchMs: t?.timings.fetchMs,
          retryAuthMs: t?.timings.retryAuthMs,
          retryFetchMs: t?.timings.retryFetchMs,
          bodyReadMs: t?.timings.bodyReadMs,
        },
        sensitive: {},
      });
    }
    prepareFetchInit(e) {
      let t = {},
        n = e.body,
        r = !1;
      if (e.headers)
        for (let [n, i] of Object.entries(e.headers)) {
          if (n.toLowerCase() === `x-codex-base64`) {
            r = i === `1`;
            continue;
          }
          t[n] = i;
        }
      if (r && typeof n == `string`)
        try {
          n = Buffer.from(n, `base64`);
        } catch (e) {
          hP().warning(`Failed to decode base64 request body`, {
            safe: {},
            sensitive: { body: n, error: e },
          });
        }
      if (!r && typeof n == `string` && !this.hasHeader(t, `Content-Type`)) {
        let e = n.trim();
        if (e.startsWith(`{`) || e.startsWith(`[`))
          try {
            (JSON.parse(n), this.setHeader(t, `Content-Type`, `application/json`));
          } catch {}
      }
      return { headers: t, body: n };
    }
    ensureAbsoluteUrl(e) {
      return /^https?:\/\//i.test(e) || e.startsWith(`data:`) ? e : cg(this.options, e);
    }
    cloneHeaders(e) {
      let t = {};
      for (let [n, r] of Object.entries(e)) t[n] = r;
      return t;
    }
    findHeaderKey(e, t) {
      let n = t.toLowerCase();
      return Object.keys(e).find((e) => e.toLowerCase() === n);
    }
    hasHeader(e, t) {
      return this.findHeaderKey(e, t) != null;
    }
    setHeader(e, t, n) {
      (this.deleteHeader(e, t), (e[t] = n));
    }
    deleteHeader(e, t) {
      let n = this.findHeaderKey(e, t);
      n && delete e[n];
    }
    shouldInferCodexApiAuth(e) {
      if (!this.isDesktopAuthAllowedUrl(e)) return !1;
      let t;
      try {
        t = new URL(e);
      } catch {
        return !1;
      }
      let n = t.pathname.replace(/\/+$/, ``);
      return (
        n === `/wham` ||
        n.startsWith(`/wham/`) ||
        n === `/api/wham` ||
        n.startsWith(`/api/wham/`) ||
        n === `/backend-api/wham` ||
        n.startsWith(`/backend-api/wham/`)
      );
    }
    isDesktopAuthAllowedUrl(e) {
      let t;
      try {
        t = new URL(e);
      } catch {
        return !1;
      }
      if (t.protocol !== `http:` && t.protocol !== `https:`) return !1;
      let n = t.host.toLowerCase();
      return n === `localhost` ||
        n === `localhost:8000` ||
        n === `openai.com` ||
        n.endsWith(`.openai.com`) ||
        n === `chatgpt.com`
        ? !0
        : n.endsWith(`.chatgpt.com`) && !n.startsWith(`ab.`);
    }
    isVsCodeFetchRequest(e) {
      return e.startsWith(gP);
    }
    async handleVsCodeFetchRequest(e, n, r) {
      let i = n.url.slice(15);
      try {
        let a = await this.fetchHandler.handleVSCodeRequest(
          e,
          i,
          n.body ? JSON.parse(n.body) : void 0,
          t.wa(n.headers?.[`x-codex-git-source`]) ? n.headers[t.ba] : null,
          r,
        );
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `success`,
          requestId: n.requestId,
          status: 200,
          headers: { "content-type": `application/json` },
          bodyJsonString: JSON.stringify(a ?? null),
        });
      } catch (t) {
        let r = wP(t);
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: n.requestId,
          status: 432,
          error: t instanceof Error ? t.message : `VS Code bridge request failed`,
          ...(r == null ? {} : { errorCode: r }),
        });
      }
    }
    async handleDictationStreamConnectInfoRequest(e, t) {
      try {
        let n = await this.getAppServerConnection(z).getAuthToken({ refreshToken: !1 });
        if (!n) throw Error(`Sign in to ChatGPT in Codex Desktop to use dictation.`);
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `success`,
          requestId: t.requestId,
          status: 200,
          headers: { "content-type": `application/json` },
          bodyJsonString: JSON.stringify({
            websocketUrl: this.getDictationStreamWebsocketUrl(),
            protocols: [vP, `${yP}${n}`],
          }),
        });
      } catch (n) {
        e.send(this.options.messageChannel, {
          type: `fetch-response`,
          responseType: `error`,
          requestId: t.requestId,
          status: 432,
          error: n instanceof Error ? n.message : `Failed to prepare dictation stream connection.`,
        });
      }
    }
    getDictationStreamWebsocketUrl() {
      let e = new URL(cg(this.options, `/dictation/stream`));
      return ((e.protocol = e.protocol === `https:` ? `wss:` : `ws:`), e.toString());
    }
  };
function xP(e) {
  return Math.round(performance.now() - e);
}
function SP(e) {
  let t = {};
  return (
    e.headers.forEach((e, n) => {
      t[n] = e;
    }),
    t
  );
}
function CP(e, t) {
  let n = t.toLowerCase(),
    r = Object.keys(e).find((e) => e.toLowerCase() === n);
  return r == null ? void 0 : e[r];
}
function wP(e) {
  if (!(typeof e != `object` || !e || !(`errorCode` in e) || typeof e.errorCode != `string`))
    return e.errorCode;
}
var TP = e.o(t.Bs());
function EP(e) {
  return e.type.startsWith(`avatar-overlay-`);
}
function DP(e) {
  let t = `pointerScreenX` in e ? e.pointerScreenX : void 0,
    n = `pointerScreenY` in e ? e.pointerScreenY : void 0;
  return typeof t != `number` || typeof n != `number`
    ? null
    : { pointerScreenX: t, pointerScreenY: n };
}
async function OP(e, t, n, r = () => {}) {
  switch (n.type) {
    case `avatar-overlay-open`:
      await e.toggle(t);
      return;
    case `avatar-overlay-open-state-request`:
      r({ type: `avatar-overlay-open-state-changed`, isOpen: e.isOpen() });
      return;
    case `avatar-overlay-drag-start`: {
      let r = DP(n),
        i =
          r == null
            ? { pointerWindowX: n.pointerWindowX, pointerWindowY: n.pointerWindowY }
            : { ...r, pointerWindowX: n.pointerWindowX, pointerWindowY: n.pointerWindowY };
      e.startDrag(t.id, i);
      return;
    }
    case `avatar-overlay-drag-move`: {
      let r = DP(n);
      r ? e.moveDrag(t.id, r) : e.moveDrag(t.id);
      return;
    }
    case `avatar-overlay-drag-end`: {
      let r = DP(n);
      r ? e.endDrag(t.id, r) : e.endDrag(t.id);
      return;
    }
    case `avatar-overlay-drag-release`:
      e.throwWithVelocity(t.id, n.velocityX, n.velocityY);
      return;
    case `avatar-overlay-mascot-resize-start`:
      e.startMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-mascot-resize-move`:
      e.moveMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-mascot-resize-end`:
      e.endMascotResize(t.id, n.width);
      return;
    case `avatar-overlay-element-size-changed`:
      e.setElementSize(t.id, {
        ...(n.elementSizeRevision == null ? {} : { elementSizeRevision: n.elementSizeRevision }),
        isTrayVisible: n.isTrayVisible,
        mascot: n.mascot,
        tray: n.tray,
      });
      return;
    case `avatar-overlay-composition-changed`:
      e.setCompositionState(t.id, n.state);
      return;
    case `avatar-overlay-composition-surface-action`:
      e.handleCompositionSurfaceAction(t.id, n.action);
      return;
    case `avatar-overlay-pointer-interaction-changed`:
      e.setPointerInteraction(t.id, n.isInteractive);
      return;
    case `avatar-overlay-keyboard-interaction-changed`:
      e.setKeyboardInteraction(t.id, n.isInteractive);
      return;
  }
}
var kP = t.$s({ feature_status: t.tc(t.nc(), t.Ks()) }),
  AP = t.Gr(`browser-sidebar-comment-mode-site-status`),
  jP = 1440 * 60 * 1e3,
  MP = `agent`,
  NP = { desktopOriginator: Ql, devApiBaseUrl: Zl, prodApiBaseUrl: Xl };
function PP({ appServerClient: e, desktopApiOptions: t = NP, now: n = Date.now }) {
  let r = new Map(),
    i = new Map();
  function o(e) {
    let t = IP(e);
    if (t == null) return !1;
    let i = r.get(t);
    return i == null || n() - i.timestampMs >= jP ? (i != null && r.delete(t), null) : i.blocked;
  }
  async function s(e) {
    let t = o(e);
    if (t != null) return t;
    let n = IP(e);
    if (n == null) return !1;
    let r = i.get(n);
    if (r != null) return r;
    let a = c(e, n).finally(() => {
      i.get(n) === a && i.delete(n);
    });
    return (i.set(n, a), a);
  }
  async function c(e, t) {
    try {
      let i = await l(e);
      if (i == null) return !1;
      let a = i.feature_status[MP] === !0;
      return (r.set(t, { blocked: a, timestampMs: n() }), a);
    } catch (e) {
      return (
        AP().warning(`failed to load browser sidebar comment mode site status`, {
          safe: {},
          sensitive: { error: e },
        }),
        !1
      );
    }
  }
  async function l(e) {
    let n = LP(e),
      r = await u(!1),
      i = await a.net.fetch(cg(t, n), { method: `GET`, headers: r });
    return (
      i.status === 401 &&
        ((r = await u(!0)), (i = await a.net.fetch(cg(t, n), { method: `GET`, headers: r }))),
      i.ok
        ? kP.parse(JSON.parse(await i.text()))
        : (AP().warning(`browser sidebar comment mode site status request failed`, {
            safe: { status: i.status },
            sensitive: {},
          }),
          null)
    );
  }
  function u(n) {
    return lg({
      action: `load browser site status`,
      appServerClient: e,
      desktopOriginator: t.desktopOriginator,
      refreshToken: n,
    });
  }
  return { cachedCommentModeBlocked: o, isCommentModeBlocked: s };
}
function FP() {
  return { cachedCommentModeBlocked: () => !1, isCommentModeBlocked: async () => !1 };
}
function IP(e) {
  try {
    let t = new URL(e);
    if (t.protocol !== `http:` && t.protocol !== `https:`) return null;
    let n = t.hostname.trim().toLowerCase();
    return n.length === 0 ? null : n.startsWith(`www.`) ? n.slice(4) : n;
  } catch {
    return null;
  }
}
function LP(e) {
  return `/aura/site_status?${new URLSearchParams({ site_url: e }).toString()}`;
}
function RP(e) {
  return zP(e)
    ? { type: `step-zoom`, delta: 1 }
    : BP(e)
      ? { type: `step-zoom`, delta: -1 }
      : VP(e)
        ? { type: `reset-zoom` }
        : null;
}
function zP(e) {
  return HP(e) && (e.code === `NumpadAdd` || e.code === `Equal` || e.key === `+` || e.key === `=`);
}
function BP(e) {
  return (
    HP(e) && (e.code === `NumpadSubtract` || e.code === `Minus` || e.key === `-` || e.key === `_`)
  );
}
function VP(e) {
  return HP(e) && e.shift !== !0 && (e.code === `Digit0` || e.code === `Numpad0` || e.key === `0`);
}
function HP(e) {
  let t = process.platform === `darwin`;
  return (
    e.type === `keyDown` &&
    e.alt !== !0 &&
    (t ? e.meta === !0 && e.control !== !0 : e.control === !0 && e.meta !== !0)
  );
}
var UP = new Set([`Meta`, `Control`, `Alt`, `AltGraph`, `Shift`]),
  WP = new Map([
    [`Escape`, `Esc`],
    [`ArrowUp`, `Up`],
    [`ArrowDown`, `Down`],
    [`ArrowLeft`, `Left`],
    [`ArrowRight`, `Right`],
  ]),
  GP = new Map([
    [`1`, `!`],
    [`2`, `@`],
    [`3`, `#`],
    [`4`, `$`],
    [`5`, `%`],
    [`6`, `^`],
    [`7`, `&`],
    [`8`, `*`],
    [`9`, `(`],
    [`0`, `)`],
    [`-`, `_`],
    [`=`, `+`],
    [`[`, `{`],
    [`]`, `}`],
    [`\\`, `|`],
    [`;`, `:`],
    [`'`, `"`],
    [`,`, `<`],
    [`.`, `>`],
    [`/`, `?`],
    ["`", `~`],
  ]);
function KP(e, t) {
  return e.type !== `keyDown` || t.includes(` `)
    ? !1
    : qP(
        {
          altKey: e.alt === !0,
          code: e.code,
          ctrlKey: e.control === !0,
          key: e.key,
          metaKey: e.meta === !0,
          repeat: e.isAutoRepeat === !0,
          shiftKey: e.shift === !0,
        },
        t,
      );
}
function qP(e, t) {
  let n = JP(t);
  return (
    n.key.length > 0 &&
    XP(e, n) &&
    e.ctrlKey === n.requireCtrl &&
    e.metaKey === n.requireMeta &&
    e.altKey === n.requireAlt &&
    e.shiftKey === n.requireShift
  );
}
function JP(e) {
  let t = e.split(`+`).filter(Boolean),
    n = process.platform === `darwin`,
    r = ``,
    i = !1,
    a = !1,
    o = !1,
    s = !1;
  for (let e of t)
    switch (e) {
      case `CmdOrCtrl`:
        n ? (o = !0) : (a = !0);
        break;
      case `Command`:
      case `Cmd`:
        o = !0;
        break;
      case `Control`:
      case `Ctrl`:
        a = !0;
        break;
      case `Alt`:
      case `Option`:
        i = !0;
        break;
      case `Shift`:
        s = !0;
        break;
      default:
        r = YP(e);
        break;
    }
  return { key: r, requireAlt: i, requireCtrl: a, requireMeta: o, requireShift: s };
}
function YP(e) {
  switch (e.toLowerCase()) {
    case `space`:
      return ` `;
    case `plus`:
      return `+`;
    case `esc`:
    case `escape`:
      return `escape`;
    case `up`:
    case `arrowup`:
      return `arrowup`;
    case `down`:
    case `arrowdown`:
      return `arrowdown`;
    case `left`:
    case `arrowleft`:
      return `arrowleft`;
    case `right`:
    case `arrowright`:
      return `arrowright`;
    case `pgup`:
    case `pageup`:
      return `pageup`;
    case `pgdn`:
    case `pagedown`:
      return `pagedown`;
    default:
      return e.toLowerCase();
  }
}
function XP(e, t) {
  let n = ZP(e);
  return n === t.key || (t.requireShift && n === (GP.get(t.key) ?? t.key));
}
function ZP(e) {
  return YP(QP(e) ?? ``);
}
function QP(e) {
  let { key: t, code: n, altKey: r } = e;
  if (UP.has(t)) return null;
  if (r) {
    let e = $P(n);
    if (e != null) return e;
  }
  return t === ` `
    ? `Space`
    : t === `+`
      ? `Plus`
      : (WP.get(t) ?? (/^f\d{1,2}$/i.test(t) || t.length === 1 ? t.toUpperCase() : ($P(n) ?? t)));
}
function $P(e) {
  return e == null
    ? null
    : /^Key[A-Z]$/.test(e)
      ? e.slice(3)
      : /^Digit[0-9]$/.test(e)
        ? e.slice(5)
        : e === `Space`
          ? `Space`
          : null;
}
function eF({ allowDebugMenu: e, getCloseTabAccelerators: t = uF, getState: n, sendAction: r }) {
  return (i, a) => {
    let o = tF(a, { allowDebugMenu: e, closeTabAccelerators: t(), state: n() });
    o != null && (i.preventDefault(), r(o));
  };
}
function tF(e, { allowDebugMenu: t, closeTabAccelerators: n = uF(), state: r }) {
  if (t && cF(e)) return { type: `toggle-debug-modal` };
  if (r == null) return null;
  if (r.imagePreviewOpen) {
    let t = RP(e);
    if (t != null) return { type: `image-preview-zoom-command`, command: t };
  }
  let i = null,
    a = null;
  switch (r.focusArea) {
    case `bottom-panel`:
      r.bottomPanelBrowserCanZoom &&
        ((i = r.bottomPanelBrowserConversationId), (a = r.bottomPanelBrowserTabId));
      break;
    case `main`:
      break;
    case `right-panel`:
      r.rightPanelBrowserCanZoom &&
        ((i = r.rightPanelBrowserConversationId), (a = r.rightPanelBrowserTabId));
      break;
  }
  if (i != null) {
    let t = RP(e);
    if (t != null)
      return {
        type: `browser-sidebar-command`,
        conversationId: i,
        ...(a == null ? {} : { browserTabId: a }),
        command: t,
      };
  }
  return r.terminalFocused === !0 && lF(e, `k`)
    ? { type: `clear-active-terminal` }
    : r.terminalFocused === !0 && lF(e, `t`)
      ? { type: `open-browser-tab`, source: `manual`, initiator: `toggle_browser_command` }
      : n.some((t) => KP(e, t))
        ? nF(r)
        : null;
}
function nF(e) {
  if (e == null) return null;
  switch (aF(e)) {
    case `bottom`:
      return e.bottomPanelCanCloseActiveTab
        ? { panelId: `bottom`, type: `close-active-app-shell-tab` }
        : null;
    case `right`:
      return e.rightPanelCanCloseActiveTab
        ? { panelId: `right`, type: `close-active-app-shell-tab` }
        : null;
    case null:
      return null;
  }
}
function rF(e) {
  if (e == null) return null;
  switch (e.focusArea) {
    case `bottom-panel`:
      return `bottom`;
    case `main`:
      return `right`;
    case `right-panel`:
      return `right`;
  }
}
function iF(e, t, n, r) {
  switch (aF(e, t, n, r)) {
    case `bottom`:
      return e?.bottomPanelCanCloseActiveTab === !0
        ? { panelId: `bottom`, type: `close-active-app-shell-tab` }
        : null;
    case `right`:
      return e?.rightPanelCanCloseActiveTab === !0
        ? { panelId: `right`, type: `close-active-app-shell-tab` }
        : null;
    case null:
      return null;
  }
}
function aF(e, t, n, r) {
  if (r != null) return r;
  if (t != null && n != null) {
    let r = sF(e, t, n);
    if (r != null) return r;
  }
  return rF(e);
}
function oF(e) {
  switch (e) {
    case `bottom-panel`:
      return `bottom`;
    case `right-panel`:
      return `right`;
    case `hidden-browser-use`:
    case null:
      return null;
  }
}
function sF(e, t, n) {
  return e?.bottomPanelBrowserConversationId === t && e.bottomPanelBrowserTabId === n
    ? `bottom`
    : e?.rightPanelBrowserConversationId === t && e.rightPanelBrowserTabId === n
      ? `right`
      : null;
}
function cF(e) {
  return (
    e.type === `keyDown` &&
    (e.key.toLowerCase() === `d` || e.code === `KeyD`) &&
    e.alt === !0 &&
    e.control !== !0 &&
    e.meta !== !0 &&
    e.shift !== !0
  );
}
function lF(e, t) {
  let n = process.platform === `darwin`;
  return (
    e.type === `keyDown` &&
    e.key.toLowerCase() === t &&
    e.alt !== !0 &&
    e.shift !== !0 &&
    (n ? e.meta === !0 && e.control !== !0 : e.control === !0 && e.meta !== !0)
  );
}
function uF() {
  return t.Xt({ commandId: `closeTab`, isMacOS: process.platform === `darwin` });
}
var dF = t.Gr(`browser-sidebar-manager`);
async function fF({
  browserTabId: e,
  browserPageUrl: t,
  conversationId: n,
  owner: r,
  sourceUrl: i,
  webContents: a,
  windowManager: o,
}) {
  if (URL.canParse(i))
    try {
      let s = await hF(await a.session.fetch(i, { referrer: t }), i);
      if (s == null) return;
      o.sendMessageToWebContents(r, {
        type: `browser-sidebar-image-attachment`,
        browserTabId: e,
        conversationId: n,
        image: s,
      });
    } catch (e) {
      dF().warning(`Failed to attach browser image to thread`, {
        safe: { conversationId: n },
        sensitive: { error: e },
      });
    }
}
function pF(e) {
  return (
    e.mediaType === `image` && e.hasImageContents && e.linkURL.length === 0 && e.srcURL.length > 0
  );
}
function mF(e, t, n, r, i) {
  let a = e.get(r.id) ?? null;
  a?.conversationId !== n ||
    a.browserTabId !== t ||
    (e.delete(r.id),
    i.sendMessageToWebContents(r, {
      type: `browser-sidebar-image-drag-state`,
      browserTabId: t,
      conversationId: n,
      isActive: !1,
    }));
}
async function hF(e, n) {
  if (!e.ok) return null;
  let r = e.headers.get(`content-type`)?.split(`;`)[0];
  if (r == null || !t.ni({ name: n, type: r })) return null;
  let i = Buffer.from(await e.arrayBuffer());
  return i.length === 0
    ? null
    : { dataUrl: `data:${r};base64,${i.toString(`base64`)}`, filename: gF(n, r) };
}
function gF(e, n) {
  try {
    let n = new URL(e).pathname.split(`/`).at(-1);
    if (n && t.ri(n)) return n;
  } catch {}
  return `browser-image.${n === `image/jpeg` ? `jpg` : n.slice(6)}`;
}
var _F = { maxHeight: 4096, maxWidth: 4096, minHeight: 160, minWidth: 240 };
function vF(e) {
  return e === `about:blank` ? `` : e;
}
function yF(e) {
  return (e.annotationEditorMode === `design` && e.isTweaksEditorOpen === !0) || t.ps(e.comments);
}
function bF(e, t) {
  return t ? e.comments.at(-1)?.designChange != null : !1;
}
function xF(e, t, n) {
  return {
    ...e,
    ...(t == null ? {} : { annotationEditorMode: `comment`, interactionMode: t }),
    ...(n == null ? {} : { comments: n }),
    isAnnotationAddModifierPressed: !1,
    isDesignModifierPressed: !1,
    isOriginalViewEnabled: !1,
    isTweaksEditorOpen: !1,
  };
}
function SF(e, t) {
  return e.isAnnotationModeEnabled === t ||
    ((e.isAnnotationModeEnabled = t), t) ||
    ((e.pendingCommentModeActivation = null), e.snapshot.interactionMode !== `comment`)
    ? !1
    : ((e.snapshot = xF(e.snapshot, `browse`)), !0);
}
var CF = `data-browser-sidebar-conversation-id`,
  wF = `data-browser-sidebar-browser-tab-id`,
  TF = `persist:codex-browser-app-route:`,
  EF = `about:blank#codex-browser-sidebar-attach-token=`;
function DF(e) {
  let n = e[CF] ?? e[`data-conversation-id`] ?? null,
    r = e[wF] ?? null;
  if (typeof n == `string` && n.length > 0)
    return {
      browserTabId: typeof r == `string` && r.length > 0 ? t.rs(r) : OF(n),
      conversationId: n,
    };
  let i = e.partition;
  if (typeof i != `string` || !i.startsWith(TF)) return null;
  try {
    let e = decodeURIComponent(i.slice(32));
    if (e.length === 0) return null;
    let [n, r] = e.split(`\0`);
    return n == null || n.length === 0
      ? null
      : { browserTabId: r == null || r.length === 0 ? OF(n) : t.rs(r), conversationId: n };
  } catch {
    return null;
  }
}
function OF(e) {
  return t.rs(`${e}:legacy`);
}
function kF(e) {
  return `${EF}${encodeURIComponent(e)}`;
}
function AF(e) {
  return e.startsWith(EF);
}
function jF(e) {
  if (!AF(e)) return null;
  let t = decodeURIComponent(e.slice(47));
  return t.length === 0 ? null : t;
}
var MF = class {
  constructor(e) {
    this.options = e;
  }
  activate(e) {
    let t = this.options.findPageStateForPageKey(e);
    t != null && (t.threadState.isBrowserUseNavigationRestrictionActive = !0);
  }
  assertAllowed(e, t) {
    let n = this.options.findPageStateForPageKey(e);
    if (n == null) return;
    let r = this.getRestrictionError(n.threadState, t);
    if (r != null) throw (this.options.onNavigationBlocked?.(n, r, t), r);
  }
  isRestrictedForWebContentsId(e, t) {
    if (e == null) return !1;
    let n = this.options.findPageStateForWebContentsId(e);
    return n == null ? !1 : this.isRestrictedForPageEntry(n, t);
  }
  isRestrictedForPageEntry(e, t) {
    let n = this.getRestrictionError(e.threadState, t);
    return n == null ? !1 : (this.options.onNavigationBlocked?.(e, n, t), !0);
  }
  getRestrictionError(e, t) {
    return NF(e, t, this.options.isEnabled?.() !== !1);
  }
};
function NF(e, t, n = !0) {
  return !e.isBrowserUseNavigationRestrictionActive || AF(t)
    ? null
    : $e(t)
      ? n
        ? FF(t)
          ? null
          : Error(`Blocked browser navigation to non-local URL: ${t}`)
        : null
      : Error(`Blocked browser navigation by Browser Use URL policy: ${t}`);
}
function PF(e) {
  return e === `mainFrame` || e === `subFrame`;
}
function FF(e) {
  let t = e.length === 0 ? `about:blank` : e;
  if (t === `about:blank` || AF(t)) return !0;
  let n;
  try {
    n = new URL(t);
  } catch {
    return !1;
  }
  return n.protocol === `file:`
    ? IF(n)
    : n.protocol !== `http:` && n.protocol !== `https:`
      ? !1
      : LF(n.hostname);
}
function IF(e) {
  return e.hostname.length > 0
    ? !1
    : t.jo(t.Po(e.pathname.startsWith(`//`) ? e.pathname.slice(1) : e.pathname))
      ? !0
      : !t.Mo(e.pathname);
}
function LF(e) {
  let t = e.toLowerCase();
  return (
    t === `localhost` ||
    t.endsWith(`.localhost`) ||
    t === `127.0.0.1` ||
    t === `[::1]` ||
    t === `::1`
  );
}
var RF = t.Gr(`browser-sidebar-manager`),
  zF = 15e3,
  BF = class {
    requests = new Set();
    constructor(e = () => void 0) {
      this.recordDebugEvent = e;
    }
    getDebugSnapshot() {
      return Array.from(this.requests).map((e) => ({
        activeBrowserTabId: null,
        activeConversationId: null,
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        guestWebContentsId: null,
        hasPage: null,
        ownerWebContentsId: e.owner.id,
        ownerRoutePath: null,
        pendingAttachCount: null,
        pendingBrowserUsePanelOpen: null,
        routeActive: null,
        windowId: null,
      }));
    }
    waitForOpen(e, t, n = null, r) {
      return new Promise((i, a) => {
        let o = {
          browserTabId: n,
          conversationId: t,
          owner: e,
          reject: a,
          resolve: i,
          timer: setTimeout(() => {
            let i = Error(
              `Timed out waiting for the Browser webview to attach for this browser-use page`,
            );
            (this.recordDebugEvent({
              browserTabId: n,
              conversationId: t,
              guestWebContentsId: null,
              kind: `browser-open-wait-timeout`,
              message: `Timed out waiting for Browser webview attach`,
              ownerWebContentsId: e.id,
              pageKey: null,
              windowId: null,
            }),
              RF().warning(`IAB_LIFECYCLE browser use open wait timed out`, {
                safe: { conversationId: t, ownerWebContentsId: e.id },
                sensitive: {},
              }),
              this.reject(o, i),
              r?.(i));
          }, zF),
        };
        (o.timer.unref(),
          this.requests.add(o),
          RF().info(`IAB_LIFECYCLE browser use open wait started`, {
            safe: { browserTabId: n, conversationId: t, ownerWebContentsId: e.id },
            sensitive: {},
          }),
          this.recordDebugEvent({
            browserTabId: n,
            conversationId: t,
            guestWebContentsId: null,
            kind: `browser-open-wait-started`,
            message: `Waiting for Browser webview attach`,
            ownerWebContentsId: e.id,
            pageKey: null,
            windowId: null,
          }));
      });
    }
    resolveForWindow(e, t) {
      let n = Array.from(this.requests).filter((t) => t.owner.id === e.owner.id);
      for (let e of n) {
        let n = t(e);
        n != null && this.resolve(e, n);
      }
    }
    rejectForOwner(e, t) {
      for (let n of Array.from(this.requests)) n.owner.id === e.id && this.reject(n, t);
    }
    rejectForRoute({ browserTabId: e, conversationId: t, error: n, owner: r }) {
      for (let i of Array.from(this.requests))
        i.owner.id === r.id && i.conversationId === t && i.browserTabId === e && this.reject(i, n);
    }
    resolve(e, t) {
      (RF().info(`IAB_LIFECYCLE browser use open wait resolved`, {
        safe: {
          browserTabId: t.browserTabId ?? e.browserTabId,
          conversationId: e.conversationId,
          guestWebContentsId: t.webContents.id,
          ownerWebContentsId: e.owner.id,
          pageKey: t.pageKey,
        },
        sensitive: {},
      }),
        this.recordDebugEvent({
          browserTabId: t.browserTabId ?? e.browserTabId,
          conversationId: e.conversationId,
          guestWebContentsId: t.webContents.id,
          kind: `browser-open-wait-resolved`,
          message: `Resolved Browser webview attach wait`,
          ownerWebContentsId: e.owner.id,
          pageKey: t.pageKey,
          windowId: null,
        }),
        this.requests.delete(e),
        clearTimeout(e.timer),
        e.resolve(t));
    }
    reject(e, t) {
      (this.recordDebugEvent({
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        details: [{ label: `errorMessage`, value: t.message }],
        guestWebContentsId: null,
        kind: `browser-open-wait-rejected`,
        message: `Rejected Browser webview attach wait`,
        ownerWebContentsId: e.owner.id,
        pageKey: null,
        windowId: null,
      }),
        RF().warning(`IAB_LIFECYCLE browser use open wait rejected`, {
          safe: {
            conversationId: e.conversationId,
            errorMessage: t.message,
            ownerWebContentsId: e.owner.id,
          },
          sensitive: {},
        }),
        this.requests.delete(e),
        clearTimeout(e.timer),
        e.reject(t));
    }
  },
  VF = `No focused Codex window is available to open a Browser tab`;
function HF({ getPreferredWindowState: e, getWindowStates: t, getWindowId: n, getActivePage: r }) {
  let i = e();
  if (i != null) {
    let e = r(i);
    if (e != null) return e;
  }
  let a = i == null ? null : n(i);
  for (let e of t()) {
    if (a != null && n(e) === a) continue;
    let t = r(e);
    if (t != null) return t;
  }
  return null;
}
async function UF(e) {
  let t = e.getPreferredWindowState();
  if (t != null) {
    let n = e.getOrCreateActiveVisiblePage(t);
    if (n != null) return n;
  }
  let n = e.getFocusedOwner();
  if (n == null || e.isOwnerDestroyed(n)) throw Error(VF);
  e.sendOpenBrowserPanelMessage(n, { startingUrl: e.startingUrl });
  let r = e.getWindowStateForOwner(n);
  if (r != null) {
    let t = e.getOrCreateActiveVisiblePage(r);
    if (t != null) return t;
  }
  return e.waitForOpenRequest(n);
}
function WF({ currentUrl: e, requestUrl: n, resourceType: r }) {
  return r === `subFrame` && !t.po(n) && e != null && t.po(e);
}
var GF = 16,
  KF = 12,
  qF = 8,
  JF = 8,
  YF = 10,
  XF = class {
    nextSessionId = 0;
    overlaysByKey = new Map();
    constructor(e, t) {
      ((this.windowManager = e), (this.errorReporter = t));
    }
    async prepare(e) {
      let t = a.BrowserWindow.fromWebContents(e.owner),
        n = this.getOverlayState(e.owner, e.conversationId, e.browserTabId);
      if (n != null) {
        let r = eI(n, t);
        if (n.wantsToBeVisible) {
          if (n.window == null || r) {
            ((n.owner = e.owner), (n.hostId = e.hostId), (n.browserBounds = e.browserBounds));
            let t = this.createFrame({
              owner: e.owner,
              browserBounds: e.browserBounds,
              viewportScale: e.viewportScale,
              anchorState: n.session.anchorState,
              forceFallback: n.session.placementStrategy === `fallback`,
              body: n.session.body,
              placementSurfaceSize: n.placementSurfaceSize,
              previewSurfaceSize: n.previewSurfaceSize,
              surfaceMode: n.session.surfaceMode,
            });
            ((n.session = {
              ...n.session,
              cwd: e.cwd,
              placementStrategy: t.placementStrategy,
              previewAlignment: t.previewAlignment,
            }),
              (n.viewportScale = e.viewportScale),
              this.recordStateEvent(n, `prepare`),
              this.attachOwnerWindowListeners(n),
              this.positionOverlayWindow(n),
              this.syncOwnerRenderer(n));
            return;
          }
        } else if (r) return;
        else if (n.window == null) {
          let t = sI(e.browserBounds, e.viewportScale);
          ((n.owner = e.owner),
            (n.hostId = e.hostId),
            (n.browserBounds = e.browserBounds),
            (n.viewportScale = e.viewportScale),
            (n.session = {
              ...this.createSession({
                ...e,
                target: { mode: `create` },
                anchorState: t,
                body: ``,
                surfaceMode: `editor`,
              }),
              sessionId: n.session.sessionId,
            }),
            (n.mountedSessionId = null),
            (n.placementSurfaceSize = null),
            (n.previewSurfaceSize = null),
            (n.isPreviewOpen = !1),
            this.recordStateEvent(n, `prepare`),
            this.attachOwnerWindowListeners(n),
            this.positionOverlayWindow(n),
            this.syncOwnerRenderer(n, { shouldPrewarm: !0 }),
            nI(n));
          return;
        }
      }
      let r = sI(e.browserBounds, e.viewportScale),
        i = this.ensureOverlayState({
          ...e,
          target: { mode: `create` },
          anchorState: r,
          body: ``,
          surfaceMode: `editor`,
        });
      i.wantsToBeVisible ||
        ((i.owner = e.owner),
        (i.hostId = e.hostId),
        (i.browserBounds = e.browserBounds),
        (i.viewportScale = e.viewportScale),
        (i.session = this.createSession({
          ...e,
          target: { mode: `create` },
          anchorState: r,
          body: ``,
          surfaceMode: `editor`,
        })),
        (i.mountedSessionId = null),
        (i.isPreviewOpen = !1),
        (i.wantsToBeVisible = !1),
        (i.shouldFocusWhenVisible = !1),
        this.recordStateEvent(i, `prepare`),
        this.attachOwnerWindowListeners(i),
        this.positionOverlayWindow(i),
        this.syncOwnerRenderer(i, { shouldPrewarm: !0 }),
        nI(i));
    }
    async open(e) {
      let t = this.ensureOverlayState(e);
      ((t.owner = e.owner),
        (t.hostId = e.hostId),
        (t.browserBounds = e.browserBounds),
        (t.viewportScale = e.viewportScale),
        (t.session = this.createSession(e)));
      let n = t.session.sessionId;
      if (
        ((t.mountedSessionId = null),
        (t.placementSurfaceSize = null),
        (t.previewSurfaceSize = null),
        (t.isPreviewOpen = !1),
        (t.wantsToBeVisible = !0),
        (t.shouldFocusWhenVisible = e.surfaceMode === `editor`),
        (t.lifetimeCounts.openCount += 1),
        this.recordStateEvent(t, `open`),
        nI(t),
        this.attachOwnerWindowListeners(t),
        this.positionOverlayWindow(t),
        this.syncOwnerRenderer(t),
        this.applyWindowInteractivityPolicy(t),
        !this.isCurrentOverlayState(t) ||
          (t.window != null && t.window.isDestroyed()) ||
          !t.wantsToBeVisible ||
          t.session.sessionId !== n)
      ) {
        nI(t);
        return;
      }
      if ((this.positionOverlayWindow(t), !(t.window == null || t.window.isDestroyed()))) {
        if (!$F(t)) {
          nI(t);
          return;
        }
        if (e.surfaceMode === `preview`) {
          this.showPreviewWindow(t);
          return;
        }
        ((t.shouldFocusWhenVisible = !1),
          this.recordStateEvent(t, `show-editor`),
          QF(t.window),
          this.scheduleEditorShowVerification(t, `open`));
      }
    }
    async openPreview(e) {
      await this.open({ ...e, surfaceMode: `preview` });
    }
    closePreview(e, t, n, r) {
      let i = this.getOverlayState(e, t, n);
      i == null ||
        i.session.surfaceMode !== `preview` ||
        i.session.target.mode !== `edit` ||
        i.session.target.commentId !== r ||
        (xI(i),
        (i.wantsToBeVisible = !1),
        (i.shouldFocusWhenVisible = !1),
        this.recordStateEvent(i, `close-preview`),
        this.applyWindowInteractivityPolicy(i),
        nI(i));
    }
    updateAnchor({
      owner: e,
      conversationId: n,
      browserTabId: r,
      target: i,
      anchorState: a,
      browserBounds: o,
      viewportScale: s,
    }) {
      let c = this.getOverlayState(e, n, r);
      if (c == null || !t.os(c.session.target, i)) return;
      c.browserBounds = o;
      let l =
        a == null
          ? null
          : this.createFrame({
              owner: c.owner,
              browserBounds: o,
              viewportScale: s,
              anchorState: a,
              body: c.session.body,
              placementSurfaceSize: c.placementSurfaceSize,
              previewSurfaceSize: c.previewSurfaceSize,
              surfaceMode: c.session.surfaceMode,
            });
      ((c.session = {
        ...c.session,
        anchorState: a ?? c.session.anchorState,
        ...(l == null
          ? { placementStrategy: `fallback` }
          : { placementStrategy: l.placementStrategy, previewAlignment: l.previewAlignment }),
      }),
        (c.viewportScale = s),
        this.positionOverlayWindow(c),
        this.syncOwnerRenderer(c),
        this.applyWindowInteractivityPolicy(c));
    }
    dismiss(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      if (r == null) return;
      xI(r);
      let i = r.window != null && !r.window.isDestroyed() && r.window.isFocused();
      ((r.wantsToBeVisible = !1),
        (r.shouldFocusWhenVisible = !1),
        this.recordStateEvent(r, `dismiss`),
        r.window != null &&
          !r.window.isDestroyed() &&
          r.window.setIgnoreMouseEvents(!0, { forward: !0 }),
        nI(r),
        i &&
          r.ownerWindow != null &&
          !r.ownerWindow.isDestroyed() &&
          r.ownerWindow.isVisible() &&
          !r.ownerWindow.isMinimized() &&
          r.ownerWindow.focus());
    }
    focus(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      if (
        r == null ||
        r.session.surfaceMode === `preview` ||
        r.window == null ||
        r.window.isDestroyed()
      )
        return;
      let i = a.BrowserWindow.fromWebContents(r.owner);
      i == null ||
        i.isDestroyed() ||
        !i.isVisible() ||
        i.isMinimized() ||
        (xI(r),
        r.wantsToBeVisible ||
          ((r.wantsToBeVisible = !0),
          (r.shouldFocusWhenVisible = !1),
          this.positionOverlayWindow(r),
          this.syncOwnerRenderer(r),
          this.applyWindowInteractivityPolicy(r)),
        $F(r) &&
          (this.recordStateEvent(r, `focus-request`),
          QF(r.window),
          this.scheduleEditorShowVerification(r, `focus`)));
    }
    close(e, t, n) {
      let r = this.getOverlayState(e, t, n);
      r != null && this.closeOverlayState(r);
    }
    transferConversation(e, t, n, r, i) {
      if (t === r && n === i) return;
      let a = this.getOverlayState(e, t, n);
      if (a == null) return;
      let o = this.getOverlayState(e, r, i);
      (o != null && o !== a && this.closeOverlayState(o),
        this.overlaysByKey.delete(tI(e, t, n)),
        (a.browserTabId = i),
        (a.conversationId = r),
        (a.session = { ...a.session, sessionId: String(this.nextSessionId++), conversationId: r }),
        (a.mountedSessionId = null),
        (a.shouldFocusWhenVisible = a.wantsToBeVisible),
        this.overlaysByKey.set(tI(e, r, i), a),
        nI(a),
        oI(a),
        this.syncOwnerRenderer(a));
    }
    claimRendererOpenedWindow({
      browserTabId: e,
      conversationId: t,
      hostId: n,
      owner: r,
      sessionId: i,
      window: a,
    }) {
      let o = this.getOverlayState(r, t, e);
      if (o == null || o.hostId !== n || o.session.sessionId !== i || a.isDestroyed()) {
        a.close();
        return;
      }
      (o.window != null && o.window !== a && oI(o),
        a.hide(),
        (o.window = a),
        (o.mountedSessionId = null),
        (o.previewSurfaceSize = null),
        (o.lifetimeCounts.claimCount += 1),
        this.recordStateEvent(o, `claim-window`),
        this.attachOwnerWindowListeners(o),
        a.on(`blur`, () => {
          this.scheduleOverlayVisibilitySync(o);
        }),
        a.on(`focus`, () => {
          xI(o);
        }),
        a.on(`closed`, () => {
          (o.window === a && ((o.window = null), (o.mountedSessionId = null)),
            this.scheduleOverlayVisibilitySync(o));
        }),
        this.positionOverlayWindow(o),
        this.applyWindowInteractivityPolicy(o),
        this.showOverlayWindowIfOwnerFocused(o),
        this.scheduleOverlayVisibilitySync(o));
    }
    handleOverlayMounted({
      owner: e,
      browserTabId: t,
      conversationId: n,
      sessionId: r,
      placementSurfaceSize: i,
      surfaceSize: a,
    }) {
      let o = this.getOverlayState(e, n, t);
      o == null ||
        r !== o.session.sessionId ||
        ((i == null ? o.placementSurfaceSize != null : bI(o.placementSurfaceSize, i)) &&
          (o.placementSurfaceSize = i ?? null),
        a != null && bI(o.previewSurfaceSize, a) && (o.previewSurfaceSize = a),
        (o.mountedSessionId = r),
        (o.lifetimeCounts.mountCount += 1),
        this.recordStateEvent(o, `mounted`),
        this.positionOverlayWindow(o),
        this.syncOwnerRenderer(o),
        this.applyWindowInteractivityPolicy(o),
        this.showOverlayWindowIfOwnerFocused(o),
        this.scheduleOverlayVisibilitySync(o));
    }
    handleOverlayPreviewOpenChanged(e, t) {
      let n = this.getOverlayState(e, t.conversationId, t.browserTabId);
      n == null ||
        t.sessionId !== n.session.sessionId ||
        ((n.isPreviewOpen = t.previewOpen),
        this.positionOverlayWindow(n),
        this.syncOwnerRenderer(n));
    }
    getOverlayStateForOwner(e, t, n, { includeHidden: r = !1 } = {}) {
      let i = this.getOverlayState(e, t, n);
      return i == null || (!r && !i.wantsToBeVisible)
        ? null
        : {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          };
    }
    updateSessionForOwner(e, t, n, r) {
      let i = this.getOverlayState(e, t, n);
      return i == null
        ? null
        : ((i.session = r(i.session)),
          {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          });
    }
    getOverlayStateForSession(e, t, n, { includeHidden: r = !1 } = {}) {
      let i = [...this.overlaysByKey.values()].find(
        (r) => r.owner === e && r.conversationId === t && r.session.sessionId === n,
      );
      return i == null || (!r && !i.wantsToBeVisible)
        ? null
        : {
            browserTabId: i.browserTabId,
            conversationId: i.conversationId,
            owner: i.owner,
            session: i.session,
          };
    }
    getOverlayState(e, t, n) {
      return this.overlaysByKey.get(tI(e, t, n)) ?? null;
    }
    ensureOverlayState(e) {
      let t = tI(e.owner, e.conversationId, e.browserTabId),
        n = this.getOverlayState(e.owner, e.conversationId, e.browserTabId),
        r = a.BrowserWindow.fromWebContents(e.owner);
      if (n != null) {
        if (n.window == null) return n;
        if (n.window.isDestroyed()) this.overlaysByKey.delete(t);
        else {
          if (eI(n, r)) return n;
          this.closeOverlayState(n);
        }
      }
      let i = {
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        hostId: e.hostId,
        owner: e.owner,
        window: null,
        mountedSessionId: null,
        wantsToBeVisible: !1,
        shouldFocusWhenVisible: !1,
        isPreviewOpen: !1,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        session: this.createSession(e),
        ownerWindow: r,
        ownerWindowListenersCleanup: null,
        ownerDestroyedCleanup: null,
        visibilitySyncTimeout: null,
        previewSurfaceSize: null,
        placementSurfaceSize: null,
        showVerificationTimeout: null,
        eventSequence: 0,
        recentEvents: [],
        lifetimeCounts: { claimCount: 0, hideCount: 0, mountCount: 0, openCount: 0 },
        reportedAnomalyKeys: new Set(),
      };
      (this.overlaysByKey.set(t, i), this.attachOwnerWindowListeners(i));
      let o = () => {
        ((i.ownerDestroyedCleanup = null), this.closeOverlayState(i));
      };
      return (
        (i.ownerDestroyedCleanup = () => {
          e.owner.removeListener(`destroyed`, o);
        }),
        e.owner.once(`destroyed`, o),
        i
      );
    }
    closeOverlayState(e) {
      (this.overlaysByKey.delete(tI(e.owner, e.conversationId, e.browserTabId)),
        (e.wantsToBeVisible = !1),
        (e.shouldFocusWhenVisible = !1),
        this.recordStateEvent(e, `close-state`),
        e.window != null && oI(e),
        this.detachOwnerWindowListeners(e),
        e.ownerDestroyedCleanup?.(),
        (e.ownerDestroyedCleanup = null),
        xI(e),
        rI(e));
    }
    attachOwnerWindowListeners(e) {
      let t = a.BrowserWindow.fromWebContents(e.owner);
      if (
        (e.ownerWindow === t && e.ownerWindowListenersCleanup != null) ||
        (this.detachOwnerWindowListeners(e), (e.ownerWindow = t), t == null)
      )
        return;
      let n = () => {
          (this.positionOverlayWindow(e), this.syncOwnerRenderer(e));
        },
        r = () => {
          (this.positionOverlayWindow(e),
            this.syncOwnerRenderer(e),
            this.scheduleOverlayVisibilitySync(e));
        },
        i = () => {
          nI(e);
        },
        o = () => {
          this.scheduleOverlayVisibilitySync(e);
        };
      (t.on(`move`, n),
        t.on(`resize`, n),
        t.on(`focus`, r),
        t.on(`show`, r),
        t.on(`restore`, r),
        t.on(`blur`, o),
        t.on(`hide`, i),
        t.on(`minimize`, i),
        (e.ownerWindowListenersCleanup = () => {
          t.isDestroyed() ||
            (t.removeListener(`move`, n),
            t.removeListener(`resize`, n),
            t.removeListener(`focus`, r),
            t.removeListener(`show`, r),
            t.removeListener(`restore`, r),
            t.removeListener(`blur`, o),
            t.removeListener(`hide`, i),
            t.removeListener(`minimize`, i));
        }));
    }
    detachOwnerWindowListeners(e) {
      (e.ownerWindowListenersCleanup != null && e.ownerWindowListenersCleanup(),
        (e.ownerWindow = null),
        (e.ownerWindowListenersCleanup = null));
    }
    isCurrentOverlayState(e) {
      return this.getOverlayState(e.owner, e.conversationId, e.browserTabId) === e;
    }
    scheduleOverlayVisibilitySync(e) {
      (xI(e),
        (e.visibilitySyncTimeout = setTimeout(() => {
          if (((e.visibilitySyncTimeout = null), e.window == null || e.window.isDestroyed())) {
            nI(e);
            return;
          }
          if (!e.wantsToBeVisible) {
            nI(e);
            return;
          }
          if (!$F(e)) {
            nI(e);
            return;
          }
          let t = a.BrowserWindow.fromWebContents(e.owner),
            n = a.BrowserWindow.getFocusedWindow();
          if (!(t == null || t.isDestroyed())) {
            if (!t.isVisible() || t.isMinimized()) {
              nI(e);
              return;
            }
            if (n !== e.window) {
              if (n === t) {
                this.showOverlayWindowIfOwnerFocused(e);
                return;
              }
              nI(e);
            }
          }
        }, 0)));
    }
    createSession(e) {
      let t = this.createFrame({
        owner: e.owner,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        anchorState: e.anchorState,
        body: e.body,
        placementSurfaceSize: null,
        previewSurfaceSize: null,
        surfaceMode: e.surfaceMode,
      });
      return {
        sessionId: String(this.nextSessionId++),
        conversationId: e.conversationId,
        target: e.target,
        anchorState: e.anchorState,
        body: e.body,
        ...(e.defaultDesignEditorOpen === !0 ? { defaultDesignEditorOpen: !0 } : {}),
        cwd: e.cwd,
        ...(e.attachedImages == null ? {} : { attachedImages: e.attachedImages }),
        ...(e.designChange == null ? {} : { designChange: e.designChange }),
        ...(e.designEditorState == null ? {} : { designEditorState: e.designEditorState }),
        placementStrategy: t.placementStrategy,
        previewAlignment: t.previewAlignment,
        surfaceMode: e.surfaceMode,
        screenshot: e.screenshot,
      };
    }
    positionOverlayWindow(e) {
      let t = this.createGeometry({
        owner: e.owner,
        browserBounds: e.browserBounds,
        viewportScale: e.viewportScale,
        anchorState: e.session.anchorState,
        forceFallback: e.session.placementStrategy === `fallback`,
        body: e.session.body,
        placementSurfaceSize: e.placementSurfaceSize,
        previewSurfaceSize: e.previewSurfaceSize,
        surfaceMode: e.session.surfaceMode,
        fullWindow: e.isPreviewOpen,
      });
      e.window != null && !e.window.isDestroyed() && e.window.setBounds(t.overlayWindowBounds, !1);
    }
    syncOwnerRenderer(e, { shouldPrewarm: t = !1 } = {}) {
      if (e.owner.isDestroyed()) return;
      let n = $F(e),
        r = this.createGeometry({
          owner: e.owner,
          browserBounds: e.browserBounds,
          viewportScale: e.viewportScale,
          anchorState: e.session.anchorState,
          forceFallback: e.session.placementStrategy === `fallback`,
          body: e.session.body,
          placementSurfaceSize: e.placementSurfaceSize,
          previewSurfaceSize: e.previewSurfaceSize,
          surfaceMode: e.session.surfaceMode,
          fullWindow: e.isPreviewOpen,
        });
      this.windowManager.sendMessageToWebContents(e.owner, {
        type: `browser-sidebar-comment-overlay-session`,
        browserTabId: e.browserTabId,
        conversationId: e.conversationId,
        session: { ...e.session, previewAlignment: r.previewAlignment },
        overlayWindowBounds: r.overlayWindowBounds,
        editorFrame: r.editorFrame,
        visible: e.wantsToBeVisible && n,
        shouldPrewarm: t || (e.wantsToBeVisible && !n),
      });
    }
    createGeometry({
      owner: e,
      browserBounds: t,
      viewportScale: n,
      anchorState: r,
      forceFallback: i = !1,
      body: o = ``,
      placementSurfaceSize: s,
      previewSurfaceSize: c,
      surfaceMode: l = `editor`,
      fullWindow: u = !1,
    }) {
      return cI({
        ownerWindow: a.BrowserWindow.fromWebContents(e),
        browserBounds: t,
        viewportScale: n,
        anchorState: r,
        forceFallback: i,
        body: o,
        placementSurfaceSize: s,
        previewSurfaceSize: c,
        surfaceMode: l,
        fullWindow: u,
      });
    }
    createFrame({
      owner: e,
      browserBounds: t,
      viewportScale: n,
      anchorState: r,
      forceFallback: i = !1,
      body: o = ``,
      placementSurfaceSize: s,
      previewSurfaceSize: c,
      surfaceMode: l = `editor`,
    }) {
      return lI({
        ownerWindow: a.BrowserWindow.fromWebContents(e),
        browserBounds: t,
        viewportScale: n,
        anchorState: r,
        forceFallback: i,
        body: o,
        placementSurfaceSize: s,
        previewSurfaceSize: c,
        surfaceMode: l,
      });
    }
    showOverlayWindowIfOwnerFocused(e) {
      if (!e.wantsToBeVisible || !$F(e)) return;
      let t = a.BrowserWindow.fromWebContents(e.owner),
        n = a.BrowserWindow.getFocusedWindow();
      if (e.window != null && (t?.isFocused() || n === e.window)) {
        if ((this.positionOverlayWindow(e), e.session.surfaceMode === `preview`)) {
          this.showPreviewWindow(e);
          return;
        }
        if (!e.shouldFocusWhenVisible) {
          ZF(e.window);
          return;
        }
        ((e.shouldFocusWhenVisible = !1),
          this.recordStateEvent(e, `show-editor`),
          QF(e.window),
          this.scheduleEditorShowVerification(e, `owner-focus-sync`));
      }
    }
    applyWindowInteractivityPolicy(e) {
      let t = e.window;
      t == null ||
        t.isDestroyed() ||
        t.setIgnoreMouseEvents(e.session.surfaceMode === `preview`, {
          forward: e.session.surfaceMode === `preview`,
        });
    }
    showPreviewWindow(e) {
      let t = e.window;
      t == null ||
        t.isDestroyed() ||
        (this.applyWindowInteractivityPolicy(e),
        !t.isVisible() && (this.recordStateEvent(e, `show-preview`), ZF(t)));
    }
    recordStateEvent(e, t) {
      aI(e, t);
    }
    scheduleEditorShowVerification(e, t) {
      rI(e);
      let n = e.session.sessionId;
      e.showVerificationTimeout = setTimeout(() => {
        if (
          ((e.showVerificationTimeout = null),
          !this.isCurrentOverlayState(e) ||
            e.session.sessionId !== n ||
            e.window == null ||
            e.window.isDestroyed() ||
            e.session.surfaceMode !== `editor` ||
            !e.wantsToBeVisible ||
            !$F(e))
        )
          return;
        let r = a.BrowserWindow.fromWebContents(e.owner);
        if (r == null || r.isDestroyed() || !r.isVisible() || r.isMinimized()) return;
        let i = iI(e, r, a.BrowserWindow.getFocusedWindow()),
          o = e.window.isVisible();
        if (o && i === `overlay`) return;
        let s = e.window.getParentWindow() === r;
        this.reportOverlayAnomaly(e, `post-show-verification-failed`, {
          focusedWindowKind: i,
          hasParentOwner: s,
          ownerFocused: r.isFocused(),
          ownerVisible: r.isVisible(),
          showReason: t,
          windowVisible: o,
        });
      }, 0);
    }
    reportOverlayAnomaly(e, t, n) {
      let r = `${t}:${e.session.sessionId}`;
      e.reportedAnomalyKeys.has(r) ||
        (e.reportedAnomalyKeys.add(r),
        this.errorReporter.reportNonFatal(Error(`Browser comment overlay lifecycle anomaly`), {
          kind: `browser-sidebar-comment-overlay-anomaly`,
          tags: { anomaly: t, surfaceMode: e.session.surfaceMode },
          extra: {
            ...n,
            counts: e.lifetimeCounts,
            hasMountedSession: e.mountedSessionId != null,
            mountedMatchesSession: $F(e),
            recentEvents: e.recentEvents,
            shouldFocusWhenVisible: e.shouldFocusWhenVisible,
            wantsToBeVisible: e.wantsToBeVisible,
          },
        }));
    }
  };
function ZF(e) {
  (e.moveTop(), e.showInactive());
}
function QF(e) {
  (e.moveTop(), e.show(), e.focus());
}
function $F(e) {
  return e.window != null && !e.window.isDestroyed() && e.mountedSessionId === e.session.sessionId;
}
function eI(e, t) {
  return e.window != null && !e.window.isDestroyed() && e.window.getParentWindow() === t;
}
function tI(e, t, n) {
  return `${e.id}:${t}\0${n}`;
}
function nI(e) {
  (rI(e),
    e.window != null &&
      !e.window.isDestroyed() &&
      ((e.lifetimeCounts.hideCount += 1), aI(e, `hide`), e.window.hide()));
}
function rI(e) {
  e.showVerificationTimeout != null &&
    (clearTimeout(e.showVerificationTimeout), (e.showVerificationTimeout = null));
}
function iI(e, t, n) {
  return n == null ? `none` : n === e.window ? `overlay` : n === t ? `owner` : `other`;
}
function aI(e, t) {
  ((e.eventSequence += 1),
    e.recentEvents.push({
      event: t,
      mounted: $F(e),
      sequence: e.eventSequence,
      sessionId: e.session.sessionId,
      surfaceMode: e.session.surfaceMode,
      wantsToBeVisible: e.wantsToBeVisible,
      windowVisible: e.window == null || e.window.isDestroyed() ? null : e.window.isVisible(),
    }),
    e.recentEvents.length > YF && e.recentEvents.shift());
}
function oI(e) {
  (e.window != null && !e.window.isDestroyed() && e.window.close(),
    (e.window = null),
    (e.mountedSessionId = null));
}
function sI(e, t) {
  let n = { x: e.width / t / 2, y: e.height / t / 2 };
  return {
    anchor: {
      kind: `element`,
      pageUrl: ``,
      frameUrl: null,
      title: ``,
      elementPath: ``,
      point: { xPercent: 50, y: n.y },
      rect: { x: n.x, y: n.y, width: 0, height: 0 },
      isFixed: !1,
      role: null,
      name: null,
      selector: null,
      framePath: [],
      nearbyText: null,
    },
    viewportRect: null,
    viewportPoint: n,
  };
}
function cI({
  ownerWindow: e,
  browserBounds: t,
  viewportScale: n = 1,
  anchorState: r,
  forceFallback: i = !1,
  body: a = ``,
  placementSurfaceSize: o,
  previewSurfaceSize: s,
  surfaceMode: c = `editor`,
  fullWindow: l = !1,
}) {
  let u = lI({
    ownerWindow: e,
    browserBounds: t,
    viewportScale: n,
    anchorState: r,
    forceFallback: i,
    body: a,
    placementSurfaceSize: o,
    previewSurfaceSize: s,
    surfaceMode: c,
  });
  if (c === `preview`) {
    let e = pI(u.editorWindowBounds, JF);
    return {
      placementStrategy: u.placementStrategy,
      previewAlignment: u.previewAlignment,
      overlayWindowBounds: e,
      editorFrame: {
        x: JF,
        y: JF,
        width: u.editorWindowBounds.width,
        height: u.editorWindowBounds.height,
      },
    };
  }
  let d = dI({ ownerWindow: e, browserBounds: t }),
    f = l && e != null && !e.isDestroyed() ? e.getContentBounds() : fI(d, u.editorWindowBounds);
  return {
    placementStrategy: u.placementStrategy,
    previewAlignment: u.previewAlignment,
    overlayWindowBounds: f,
    editorFrame: {
      x: u.editorWindowBounds.x - f.x,
      y: u.editorWindowBounds.y - f.y,
      width: u.editorWindowBounds.width,
      height: u.editorWindowBounds.height,
    },
  };
}
function lI({
  ownerWindow: e,
  browserBounds: n,
  viewportScale: r = 1,
  anchorState: i,
  forceFallback: o = !1,
  body: s = ``,
  placementSurfaceSize: c,
  previewSurfaceSize: l,
  surfaceMode: u = `editor`,
}) {
  let d = uI({ ownerWindow: e, browserBounds: n }),
    f = t.us(s),
    p = Math.min(294, Math.max(1, d.width - GF * 2)),
    m = vI(l?.width ?? p, p),
    h = yI(l?.height ?? f, d.height - GF * 2),
    g = Math.min(Math.max(1, d.width - GF * 2), Math.max(294, c?.width ?? 294, l?.width ?? 294)),
    _ = Math.max(1, d.height - GF * 2),
    v = vI(Math.max(l?.width ?? 294, c?.width ?? 0), g),
    y = yI(l?.height ?? 120, _),
    b = u === `preview` ? m : v,
    x = u === `preview` ? h : y,
    S = u === `preview` ? b : vI(c?.width ?? b, g),
    C = u === `preview` ? x : yI(c?.height ?? x, _),
    w = a.screen.getDisplayMatching(d).workArea,
    T =
      u === `preview`
        ? gI({
            browserViewportBounds: d,
            viewportScale: r,
            anchorState: i,
            previewWidth: m,
            previewHeight: h,
          })
        : null,
    E = u === `preview` ? null : mI({ browserViewportBounds: d, cardWidth: b, cardHeight: x }),
    D =
      T ??
      (o
        ? null
        : hI({
            browserViewportBounds: d,
            viewportScale: r,
            anchorState: i,
            cardWidth: b,
            cardHeight: x,
            placementCardWidth: S,
            placementCardHeight: C,
            sideAnchorHeight: 44,
          })),
    O = D?.bounds ?? E;
  if (O == null) throw Error(`Expected preview card bounds`);
  let k = T != null || D != null ? `anchored` : `fallback`,
    A = D?.previewAlignment ?? `end`,
    j = SI(O.x, w.x, w.x + w.width - b),
    M = SI(O.y, w.y, w.y + w.height - x),
    N = u === `preview` ? 0 : Math.min(88, Math.max(M - w.y, 0)),
    P = u === `preview` ? f : Math.max(x, 120) + N;
  return {
    cardWindowBounds: { x: j, y: M, width: b, height: x },
    placementStrategy: k,
    previewAlignment: A,
    editorWindowBounds: { x: j, y: u === `preview` ? M : M - N, width: b, height: P },
  };
}
function uI({ ownerWindow: e, browserBounds: t }) {
  if (e == null || e.isDestroyed()) return t;
  let n = e.getContentBounds();
  return { x: Math.round(n.x + t.x), y: Math.round(n.y + t.y), width: t.width, height: t.height };
}
function dI({ ownerWindow: e, browserBounds: t }) {
  let n = uI({ ownerWindow: e, browserBounds: t });
  if (e == null || e.isDestroyed()) return n;
  let r = e.getContentBounds(),
    i = t.x <= 0 ? qF : 0,
    a = t.y <= 0 ? qF : 0,
    o = t.x + t.width >= r.width ? qF : 0,
    s = t.y + t.height >= r.height ? qF : 0;
  return {
    x: n.x + i,
    y: n.y + a,
    width: Math.max(n.width - i - o, 1),
    height: Math.max(n.height - a - s, 1),
  };
}
function fI(e, t) {
  let n = Math.min(e.x, t.x),
    r = Math.min(e.y, t.y),
    i = Math.max(e.x + e.width, t.x + t.width),
    a = Math.max(e.y + e.height, t.y + t.height);
  return { x: n, y: r, width: Math.max(i - n, 1), height: Math.max(a - r, 1) };
}
function pI(e, t) {
  return { x: e.x - t, y: e.y - t, width: e.width + t * 2, height: e.height + t * 2 };
}
function mI({ browserViewportBounds: e, cardWidth: t, cardHeight: n }) {
  return { x: e.x + e.width - t - GF, y: e.y + e.height - n - GF, width: t, height: n };
}
function hI({
  browserViewportBounds: e,
  viewportScale: t,
  anchorState: n,
  cardWidth: r,
  sideAnchorHeight: i,
  cardHeight: a,
  placementCardWidth: o,
  placementCardHeight: s,
}) {
  let c = n.viewportSize == null || n.viewportSize.width <= 0 ? t : e.width / n.viewportSize.width,
    l = n.viewportSize == null || n.viewportSize.height <= 0 ? t : e.height / n.viewportSize.height,
    u = e.x + SI(n.viewportPoint.x * c, 29, e.width - 29),
    d = e.y + SI(n.viewportPoint.y * l, 29, e.height - 29),
    f = e.x + GF,
    p = e.x + e.width - r - GF,
    m = e.x + e.width - o - GF,
    h = e.y + GF,
    g = e.y + e.height - a - GF,
    _ = e.y + e.height - s - GF,
    v = SI(d - i / 2, h, g),
    y = 26 / 2 + KF,
    b = u + y,
    x = u - y - r;
  if (b >= f && b <= m)
    return { bounds: { x: b, y: v, width: r, height: a }, previewAlignment: `start` };
  let S = u - y - o;
  if (S >= f && S <= m)
    return { bounds: { x, y: v, width: r, height: a }, previewAlignment: `end` };
  let C = SI(u - r / 2, f, p),
    w = d + y;
  if (w <= _)
    return {
      bounds: { x: C, y: w, width: r, height: a },
      previewAlignment: _I({ maxCardX: p, minCardX: f, x: C }),
    };
  let T = d - y - a;
  return d - y - s < h
    ? null
    : {
        bounds: { x: C, y: T, width: r, height: a },
        previewAlignment: _I({ maxCardX: p, minCardX: f, x: C }),
      };
}
function gI({
  browserViewportBounds: e,
  viewportScale: t,
  anchorState: n,
  previewWidth: r,
  previewHeight: i,
}) {
  let a = n.viewportSize == null || n.viewportSize.width <= 0 ? t : e.width / n.viewportSize.width,
    o = n.viewportSize == null || n.viewportSize.height <= 0 ? t : e.height / n.viewportSize.height,
    s = e.x + SI(n.viewportPoint.x * a, 29, e.width - 29),
    c = e.y + SI(n.viewportPoint.y * o, 29, e.height - 29),
    l = e.x + GF,
    u = e.x + e.width - r - GF,
    d = e.y + GF,
    f = e.y + e.height - i - GF,
    p = Math.min(294, Math.max(1, e.width - GF * 2)),
    m = 26 / 2 + KF,
    h = SI(c - i / 2, d, Math.max(d, f)),
    g = s + m;
  if (g + p <= e.x + e.width - GF)
    return { bounds: { x: g, y: h, width: r, height: i }, previewAlignment: `start` };
  if (s - m - p >= l)
    return { bounds: { x: s - m - r, y: h, width: r, height: i }, previewAlignment: `end` };
  let _ = SI(s - r / 2, l, Math.max(l, u)),
    v = c + m;
  return v + 120 <= e.y + e.height - GF
    ? {
        bounds: { x: _, y: v, width: r, height: i },
        previewAlignment: _I({ maxCardX: u, minCardX: l, x: _ }),
      }
    : {
        bounds: { x: _, y: Math.max(d, c - m - i), width: r, height: i },
        previewAlignment: _I({ maxCardX: u, minCardX: l, x: _ }),
      };
}
function _I({ maxCardX: e, minCardX: t, x: n }) {
  return n <= t ? `start` : n >= e ? `end` : `center`;
}
function vI(e, t) {
  return SI(Math.ceil(e), 1, Math.max(1, t));
}
function yI(e, t) {
  return SI(Math.ceil(e), 1, Math.max(1, t));
}
function bI(e, t) {
  return e == null || e.width !== t.width || e.height !== t.height;
}
function xI(e) {
  e.visibilitySyncTimeout != null &&
    (clearTimeout(e.visibilitySyncTimeout), (e.visibilitySyncTimeout = null));
}
function SI(e, t, n) {
  return n < t ? t : Math.min(Math.max(Math.round(e), t), n);
}
var CI = t.Gr(`browser-sidebar-comment-controller`),
  wI = 1e3,
  TI = 32,
  EI = 16,
  DI = 1280,
  OI = 1024,
  kI = 768,
  AI = `data-codex-browser-design-group`,
  jI = t.$s({ root: t.$s({ nodeId: t.Qs() }) }),
  MI = t.$s({ nodeId: t.Qs() }),
  NI = t.$s({ startLine: t.Qs(), startColumn: t.Qs() }),
  PI = t
    .$s({
      name: t.nc(),
      value: t.nc().optional(),
      disabled: t.Ks().optional(),
      implicit: t.Ks().optional(),
      range: NI.optional(),
    })
    .passthrough(),
  FI = t.$s({ text: t.nc().optional(), sourceURL: t.nc().optional() }).passthrough(),
  II = t
    .$s({
      rule: t
        .$s({
          selectorList: t.$s({ text: t.nc().optional() }).optional(),
          media: t.Gs(FI).optional(),
          containerQueries: t.Gs(FI).optional(),
          supports: t.Gs(FI).optional(),
          style: t.$s({ cssProperties: t.Gs(PI).optional() }).optional(),
        })
        .passthrough(),
    })
    .passthrough(),
  LI = t
    .$s({
      inlineStyle: t.$s({ cssProperties: t.Gs(PI).optional() }).optional(),
      matchedCSSRules: t.Gs(II).optional(),
    })
    .passthrough(),
  RI = class {
    overlayManager;
    runtimeCommentScreenshotWaiters = new Map();
    constructor(e, t, n, r = z) {
      ((this.windowManager = e),
        (this.errorReporter = t),
        (this.delegate = n),
        (this.hostId = r),
        (this.overlayManager = new XF(e, t)),
        this.windowManager.addBrowserCommentPopupWindowCreatedHandler((e) => {
          e.hostId === this.hostId && this.overlayManager.claimRendererOpenedWindow(e);
        }));
    }
    prepare(e, n, r, i) {
      !i.visible ||
        i.bounds == null ||
        this.overlayManager
          .prepare({
            owner: e,
            hostId: this.windowManager.getHostIdForWebContents(e) ?? this.hostId,
            browserTabId: r,
            conversationId: n,
            browserBounds: i.bounds,
            viewportScale: i.viewportScale,
            cwd: i.cwd,
          })
          .catch((e) => {
            (CI().warning(`failed to prepare browser comment overlay`, {
              safe: { conversationId: n },
              sensitive: { error: e },
            }),
              this.errorReporter.reportNonFatal(e instanceof Error ? e : t.Oo(e), {
                kind: `browser-sidebar-comment-overlay-prepare`,
              }));
          });
    }
    dismiss(e, t, n, r) {
      (this.overlayManager.dismiss(e, t, n),
        this.closeRuntimeEditor(r?.page?.view.webContents ?? null));
    }
    close(e, t, n) {
      this.overlayManager.close(e, t, n);
    }
    transferConversation(e, t, n, r, i) {
      this.overlayManager.transferConversation(e, t, n, r, i);
    }
    restoreRuntimeEditor(e, t, n, r) {
      if (r == null || r.isDestroyed()) return;
      let i = this.overlayManager.getOverlayStateForOwner(e, t, n);
      i != null &&
        r.send(R, {
          type: `browser-sidebar-runtime-restore-editor`,
          target: i.session.target,
          anchorState: i.session.anchorState,
        });
    }
    handleOverlayMounted(e, t, n, r, i, a) {
      this.overlayManager.handleOverlayMounted({
        owner: e,
        browserTabId: n,
        conversationId: t,
        sessionId: r,
        placementSurfaceSize: a,
        surfaceSize: i,
      });
    }
    handleOverlayPreviewOpenChanged(e, t) {
      this.overlayManager.handleOverlayPreviewOpenChanged(e, t);
    }
    async handleRuntimeOpenEditor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(n.owner, n.page.conversationId, n.page.browserTabId);
      if (r == null || r.bounds == null || r.snapshot.interactionMode !== `comment`) {
        this.closeRuntimeEditor(e, t.target);
        return;
      }
      let i = null;
      if (t.target.mode === `edit`) {
        let n = t.target.commentId;
        if (((i = r.snapshot.comments.find((e) => e.id === n) ?? null), i == null)) {
          this.closeRuntimeEditor(e, t.target);
          return;
        }
      }
      let a = this.delegate.canUseTweaks();
      if (!a && i?.designChange != null && (i.body?.trim() ?? ``).length === 0) {
        this.closeRuntimeEditor(e, t.target);
        return;
      }
      this.overlayManager.getOverlayStateForOwner(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
        { includeHidden: !0 },
      )?.session.surfaceMode === `preview` &&
        this.overlayManager.close(n.owner, n.page.conversationId, n.page.browserTabId);
      let o = a ? i?.designChange : void 0,
        s = a ? (t.designEditorState ?? i?.designChange) : void 0,
        c =
          o != null ||
          (t.target.mode === `create` && s != null && r.snapshot.isTweaksEditorOpen === !0);
      (this.setTweaksEditorOpen(n.owner, n.page.conversationId, n.page.browserTabId, r, c),
        await this.overlayManager.open({
          owner: n.owner,
          hostId: this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
          target: t.target,
          anchorState: t.anchorState,
          body: i?.body ?? ``,
          ...(t.target.mode === `create` && c ? { defaultDesignEditorOpen: !0 } : {}),
          cwd: r.cwd,
          attachedImages: i?.attachedImages,
          ...(o == null ? {} : { designChange: o }),
          ...(s == null ? {} : { designEditorState: s }),
          screenshot: void 0,
          surfaceMode: `editor`,
        }));
    }
    async handleRuntimeOpenDesignEditor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(n.owner, n.page.conversationId, n.page.browserTabId);
      if (r == null || r.bounds == null || r.snapshot.interactionMode !== `comment`) {
        this.closeRuntimeEditor(e, { mode: `design`, groupId: t.designEditorState.id });
        return;
      }
      if (!this.delegate.canUseTweaks()) return;
      let i = t.designEditorState;
      (this.overlayManager.getOverlayStateForOwner(
        n.owner,
        n.page.conversationId,
        n.page.browserTabId,
        { includeHidden: !0 },
      )?.session.surfaceMode === `preview` &&
        this.overlayManager.close(n.owner, n.page.conversationId, n.page.browserTabId),
        this.setTweaksEditorOpen(n.owner, n.page.conversationId, n.page.browserTabId, r, !0),
        this.setAnnotationEditorMode(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
          r,
          `design`,
        ),
        await this.overlayManager.open({
          owner: n.owner,
          hostId: this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
          target: { mode: `design`, groupId: i.id },
          anchorState: t.anchorState,
          body: t.designChange?.comment ?? ``,
          cwd: r.cwd,
          ...(t.designChange == null ? {} : { designChange: t.designChange }),
          designEditorState: i,
          screenshot: void 0,
          surfaceMode: `editor`,
        }));
    }
    async handleRuntimeOpenCommentPreview(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(n.owner, n.page.conversationId, n.page.browserTabId);
      if (r == null || r.bounds == null || r.snapshot.interactionMode !== `comment`) return;
      let i = r.snapshot.comments.find((e) => e.id === t.commentId) ?? null;
      if (i == null) return;
      let a = this.delegate.canUseTweaks();
      if (!a && i.designChange != null && (i.body?.trim() ?? ``).length === 0) return;
      let o = a ? i.designChange : void 0;
      await this.overlayManager.openPreview({
        owner: n.owner,
        hostId: this.windowManager.getHostIdForWebContents(n.owner) ?? this.hostId,
        browserTabId: n.page.browserTabId,
        conversationId: n.page.conversationId,
        browserBounds: r.bounds,
        target: { mode: `edit`, commentId: t.commentId },
        anchorState: t.anchorState,
        body: i.body ?? ``,
        cwd: r.cwd,
        attachedImages: i.attachedImages,
        ...(o == null ? {} : { designChange: o }),
        viewportScale: r.viewportScale,
        screenshot: void 0,
      });
    }
    handleRuntimeCloseCommentPreview(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      !n ||
        n.owner.isDestroyed() ||
        this.overlayManager.closePreview(
          n.owner,
          n.page.conversationId,
          n.page.browserTabId,
          t.commentId,
        );
    }
    handleRuntimeUpdateAnchor(e, t) {
      let n = this.delegate.findPageStateForRuntimeSender(e);
      if (!n || n.owner.isDestroyed()) return;
      let r = this.delegate.getThreadState(n.owner, n.page.conversationId, n.page.browserTabId);
      r == null ||
        r.bounds == null ||
        this.overlayManager.updateAnchor({
          owner: n.owner,
          conversationId: n.page.conversationId,
          browserTabId: n.page.browserTabId,
          target: t.target,
          anchorState: t.anchorState,
          browserBounds: r.bounds,
          viewportScale: r.viewportScale,
        });
    }
    handleRuntimeFocusEditor(e) {
      let t = this.delegate.findPageStateForRuntimeSender(e);
      !t ||
        t.owner.isDestroyed() ||
        this.overlayManager.focus(t.owner, t.page.conversationId, t.page.browserTabId);
    }
    handleRuntimeCommentScreenshotReady(e, t) {
      let n = this.runtimeCommentScreenshotWaiters.get(e.id);
      n == null ||
        n.commentId !== t.commentId ||
        (this.runtimeCommentScreenshotWaiters.delete(e.id), n.resolve(t));
    }
    async handleOverlaySubmit(e, n) {
      let r = this.overlayManager.getOverlayStateForOwner(e, n.conversationId, n.browserTabId);
      if (r == null || r.session.sessionId !== n.sessionId) return;
      let i = n.browserTabId,
        a = this.delegate.getThreadState(r.owner, r.conversationId, i);
      if (a == null) return;
      let o = n.body?.trim() ?? ``,
        s =
          n.designChange === void 0 && this.delegate.canUseTweaks()
            ? r.session.designChange
            : n.designChange,
        c = s !== void 0 || r.session.designChange != null,
        l = s == null ? null : JI(s, o);
      if (o.length === 0 && l == null) {
        (this.setTweaksEditorOpen(r.owner, r.conversationId, i, a, !1),
          this.dismiss(r.owner, r.conversationId, i, a));
        return;
      }
      let d = r.session.target,
        f = a.page?.view.webContents ?? null,
        p = n.captureScreenshot !== !1,
        m =
          d.mode === `create` && n.submitDirectly === !0 && l == null && !t.ps(a.snapshot.comments),
        h = m ? 1 : t.cs(a.snapshot.comments) + 1,
        g = d.mode !== `edit` && p && h >= 10,
        _ = n.cropScreenshotToRegion === !0 && r.session.anchorState.anchor.kind === `region`,
        v = r.session.anchorState.viewportRect,
        y = r.session.anchorState.viewportPoint,
        b = g || _ ? VI(v, y) : null;
      if (m) {
        let e = qI({
            anchorState: r.session.anchorState,
            body: o,
            commentId: (0, u.randomUUID)(),
            attachedImages: n.attachedImages,
            screenshot: r.session.screenshot,
          }),
          s = a.snapshot.comments.length + 1;
        this.dismiss(r.owner, r.conversationId, i, a);
        let c = p
          ? await this.captureDirectCommentScreenshot({
              comment: e,
              conversationId: r.conversationId,
              markerViewportPoint: y,
              annotationViewportRect: v,
              page: f,
              screenshotCropRect: b,
              shouldUseCompactScreenshot: g,
              threadState: a,
            })
          : void 0;
        (this.syncSnapshotRuntimeStateToPage(f, a),
          this.windowManager.sendMessageToWebContents(r.owner, {
            type: `browser-sidebar-direct-comment`,
            browserTabId: i,
            conversationId: r.conversationId,
            sessionId: n.sessionId,
            body: o,
            comment: t.Ja(c == null ? e : { ...e, screenshot: c }, s, i),
          }));
        return;
      }
      if (d.mode === `create` || d.mode === `design`) {
        let e = l?.id ?? (0, u.randomUUID)();
        ((a.snapshot = {
          ...a.snapshot,
          annotationEditorMode: `comment`,
          isOriginalViewEnabled: !1,
          comments: YI(
            a.snapshot.comments,
            qI({
              anchorState: r.session.anchorState,
              body: o,
              commentId: e,
              attachedImages: n.attachedImages,
              designChange: l,
              screenshot: r.session.screenshot,
            }),
          ),
        }),
          this.compactThreadCommentScreenshotsIfNeeded(a),
          this.dismiss(r.owner, r.conversationId, i, a),
          this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i),
          p &&
            (await this.captureSavedCommentScreenshot({
              commentId: e,
              conversationId: r.conversationId,
              markerViewportPoint: y,
              annotationViewportRect: v,
              owner: r.owner,
              page: a.page?.view.webContents ?? null,
              screenshotCropRect: b,
              shouldUseCompactScreenshot: g,
              threadState: a,
            })));
      } else
        d.mode === `edit` &&
          ((a.snapshot = {
            ...a.snapshot,
            comments: a.snapshot.comments.map((e) => {
              if (e.id !== d.commentId) return e;
              let t =
                o.length === 0
                  ? ZI({ ...e, attachedImages: n.attachedImages })
                  : { ...e, body: o, attachedImages: n.attachedImages };
              return c ? (l == null ? XI(t) : { ...t, designChange: l }) : t;
            }),
          }),
          this.dismiss(r.owner, r.conversationId, i, a),
          this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i));
    }
    handleDesignOverlayUpdate(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(n.owner, n.conversationId, n.browserTabId);
      r != null &&
        (n.session.designChange?.id ?? n.session.designEditorState?.id) != null &&
        (this.overlayManager.updateSessionForOwner(
          n.owner,
          n.conversationId,
          n.browserTabId,
          (e) => ({ ...e, designChange: t.group ?? void 0 }),
        ),
        this.syncRuntimeStateToPage(
          n.owner,
          n.conversationId,
          r.page?.view.webContents ?? null,
          r,
        ));
    }
    async handleDesignOverlayAdd(e, n) {
      let r = this.getDesignOverlayState(e, n);
      if (r == null) return;
      let i = this.delegate.getThreadState(r.owner, r.conversationId, r.browserTabId);
      if (i == null) return;
      let a = t.cs(i.snapshot.comments) + 1 >= 10,
        o = r.session.anchorState.viewportRect,
        s = r.session.anchorState.viewportPoint,
        c = a ? VI(o, s) : null,
        l = JI(n.group, n.group.comment?.trim() ?? ``);
      (this.overlayManager.updateSessionForOwner(
        r.owner,
        r.conversationId,
        r.browserTabId,
        (e) => ({ ...e, designChange: l }),
      ),
        (i.snapshot = {
          ...i.snapshot,
          annotationEditorMode: `comment`,
          isOriginalViewEnabled: !1,
          comments: YI(
            i.snapshot.comments,
            qI({
              anchorState: r.session.anchorState,
              body: l.comment ?? ``,
              commentId: l.id,
              designChange: l,
              screenshot: r.session.screenshot,
            }),
          ),
        }),
        this.syncSnapshotRuntimeStateToPage(i.page?.view.webContents ?? null, i),
        this.dismiss(r.owner, r.conversationId, r.browserTabId, i),
        this.delegate.syncCommentSnapshot(r.owner, r.conversationId, r.browserTabId));
      let u = await this.enrichQueuedDesignChangeGroup(
          i.page?.view.webContents ?? null,
          r.session.designEditorState,
          l,
        ),
        d = i.snapshot.comments.find((e) => e.id === l.id)?.designChange;
      (u !== l &&
        tL(d, l) &&
        ((i.snapshot = {
          ...i.snapshot,
          comments: i.snapshot.comments.map((e) =>
            e.id === l.id && e.designChange != null
              ? { ...e, designChange: { ...e.designChange, provenance: u.provenance } }
              : e,
          ),
        }),
        this.delegate.syncCommentSnapshot(r.owner, r.conversationId, r.browserTabId)),
        await this.captureSavedDesignChangeScreenshot({
          annotationViewportRect: o,
          conversationId: r.conversationId,
          groupId: l.id,
          markerViewportPoint: s,
          owner: r.owner,
          page: i.page?.view.webContents ?? null,
          screenshotCropRect: c,
          shouldUseCompactScreenshot: a,
          threadState: i,
        }));
    }
    async enrichQueuedDesignChangeGroup(e, t, n) {
      let r = QI(n.declarations, n.provenance, t?.provenance);
      if ($I(n.declarations, r) || e == null || e.isDestroyed() || t == null || t.id !== n.id)
        return eL(n.provenance, r) ? n : { ...n, provenance: r };
      let i = await this.enrichDesignEditorProvenance(e, {
          ...t,
          declarations: n.declarations,
          ...(n.text == null ? {} : { text: n.text }),
          provenance: r,
        }),
        a = QI(n.declarations, r, i.provenance);
      return eL(n.provenance, a) ? n : { ...n, provenance: a };
    }
    handleDesignOverlayDelete(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(n.owner, n.conversationId, n.browserTabId);
      if (r == null) return;
      let i = r.snapshot.comments.flatMap((e) =>
        e.designChange?.id === t.groupId
          ? (e.body?.trim().length ?? 0) > 0 || (e.attachedImages?.length ?? 0) > 0
            ? [XI(e)]
            : []
          : [e],
      );
      (this.overlayManager.updateSessionForOwner(
        n.owner,
        n.conversationId,
        n.browserTabId,
        (e) => ({ ...e, designChange: void 0 }),
      ),
        (r.snapshot = {
          ...r.snapshot,
          isTweaksEditorOpen: !1,
          annotationEditorMode: i.length > 0 ? r.snapshot.annotationEditorMode : `comment`,
          comments: i,
        }),
        this.syncSnapshotRuntimeStateToPage(r.page?.view.webContents ?? null, r),
        this.dismiss(n.owner, n.conversationId, n.browserTabId, r),
        this.delegate.syncCommentSnapshot(n.owner, n.conversationId, n.browserTabId));
    }
    compactThreadCommentScreenshotsIfNeeded(e) {
      if (e.snapshot.comments.length < 10) return;
      let t = zI(e.snapshot.comments);
      t !== e.snapshot.comments && (e.snapshot = { ...e.snapshot, comments: t });
    }
    handleOverlayDelete(e, n) {
      let r = this.overlayManager.getOverlayStateForOwner(e, n.conversationId, n.browserTabId);
      if (r == null || r.session.sessionId !== n.sessionId) return;
      let i = n.browserTabId,
        a = this.delegate.getThreadState(r.owner, r.conversationId, i);
      if (a == null) return;
      let o = r.session.target;
      if (o.mode !== `edit` || o.commentId !== n.commentId) return;
      let s = a.snapshot.comments.filter((e) => e.id !== o.commentId);
      ((a.snapshot = {
        ...a.snapshot,
        annotationEditorMode: t.ps(s) ? a.snapshot.annotationEditorMode : `comment`,
        comments: s,
      }),
        this.delegate.syncCommentSnapshot(r.owner, r.conversationId, i),
        this.dismiss(r.owner, r.conversationId, i, a));
    }
    handleOverlayClose(e, n, r, i) {
      let a = this.overlayManager.getOverlayStateForOwner(e, n, r);
      if (a == null || a.session.sessionId !== i) return;
      let o = this.delegate.getThreadState(a.owner, n, r);
      if (o != null) {
        let e = t.ps(o.snapshot.comments) ? o.snapshot.annotationEditorMode : `comment`;
        (o.snapshot.isAnnotationAddModifierPressed === !0 ||
          o.snapshot.isDesignModifierPressed === !0 ||
          o.snapshot.isOriginalViewEnabled === !0 ||
          o.snapshot.annotationEditorMode !== e) &&
          ((o.snapshot = {
            ...o.snapshot,
            isAnnotationAddModifierPressed: !1,
            isDesignModifierPressed: !1,
            isOriginalViewEnabled: !1,
            annotationEditorMode: e,
          }),
          this.delegate.syncCommentSnapshot(a.owner, n, r));
      }
      (this.overlayManager.dismiss(a.owner, n, r),
        o != null && this.syncSnapshotRuntimeStateToPage(o.page?.view.webContents ?? null, o),
        this.closeRuntimeEditor(o?.page?.view.webContents ?? null));
    }
    handleOverlayTweaksOpenChanged(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(n.owner, n.conversationId, n.browserTabId);
      r != null &&
        (this.setTweaksEditorOpen(n.owner, n.conversationId, n.browserTabId, r, t.open),
        this.setAnnotationEditorMode(
          n.owner,
          n.conversationId,
          n.browserTabId,
          r,
          t.open ? `design` : `comment`,
        ));
    }
    handleOverlayDesignScrubChanged(e, t) {
      let n = this.getDesignOverlayState(e, t);
      if (n == null) return;
      let r = this.delegate.getThreadState(n.owner, n.conversationId, n.browserTabId);
      r != null && this.setRuntimeDesignScrubProperty(r.page?.view.webContents ?? null, t.property);
    }
    getDesignOverlayState(e, t) {
      let n = this.overlayManager.getOverlayStateForSession(e, t.conversationId, t.sessionId);
      return n == null || (n.session.designChange == null && n.session.designEditorState == null)
        ? null
        : n;
    }
    async captureBrowserScreenshot(e, t) {
      try {
        let n = t.readyMessage.annotationViewportRect ?? t.annotationViewportRect,
          r = t.readyMessage.markerViewportPoint ?? t.markerViewportPoint,
          i = t.screenshotCropRect == null ? null : (VI(n, r) ?? t.screenshotCropRect);
        return UI(
          HI(await e.capturePage(), {
            annotation: t.annotation,
            screenshotCropRect: i,
            shouldUseCompactScreenshot: t.shouldUseCompactScreenshot,
          }),
          t.shouldUseCompactScreenshot,
          i,
          n,
          r,
        );
      } catch (e) {
        (CI().warning(`failed to capture browser comment screenshot`, {
          safe: {},
          sensitive: { error: e },
        }),
          this.errorReporter.reportNonFatal(e instanceof Error ? e : Error(String(e)), {
            kind: `browser-sidebar-comment-screenshot`,
          }));
        return;
      }
    }
    async captureSavedCommentScreenshot({
      annotationViewportRect: e,
      commentId: t,
      conversationId: n,
      markerViewportPoint: r,
      owner: i,
      page: a,
      screenshotCropRect: o,
      shouldUseCompactScreenshot: s,
      threadState: c,
    }) {
      if (!(a == null || a.isDestroyed())) {
        a.send(R, { type: `browser-sidebar-runtime-prepare-comment-screenshot`, commentId: t });
        try {
          let l = await this.waitForRuntimeCommentScreenshotReady(a, t);
          if (l == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser comment screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              CI().warning(`timed out waiting for browser comment screenshot`, {
                safe: { commentId: t, conversationId: n },
              }));
            return;
          }
          if (l.skipScreenshotCapture === !0) return;
          let u = c.snapshot.comments.find((e) => e.id === t);
          if (u == null) return;
          let d = await this.captureBrowserScreenshot(a, {
            annotationViewportRect: e,
            annotation: u,
            markerViewportPoint: r,
            readyMessage: l,
            screenshotCropRect: o,
            shouldUseCompactScreenshot: s,
          });
          if (d == null) return;
          ((c.snapshot = {
            ...c.snapshot,
            comments: c.snapshot.comments.map((e) =>
              e.id === t
                ? {
                    ...e,
                    screenshot: d,
                    ...(e.designChange == null
                      ? {}
                      : { designChange: { ...e.designChange, screenshot: d } }),
                  }
                : e,
            ),
          }),
            this.compactThreadCommentScreenshotsIfNeeded(c),
            this.delegate.syncCommentSnapshot(i, n, c.browserTabId));
        } finally {
          a.isDestroyed() ||
            a.send(R, { type: `browser-sidebar-runtime-clear-comment-screenshot` });
        }
      }
    }
    async captureSavedDesignChangeScreenshot({
      annotationViewportRect: e,
      conversationId: t,
      groupId: n,
      markerViewportPoint: r,
      owner: i,
      page: a,
      screenshotCropRect: o,
      shouldUseCompactScreenshot: s,
      threadState: c,
    }) {
      if (!(a == null || a.isDestroyed())) {
        a.send(R, { type: `browser-sidebar-runtime-prepare-comment-screenshot`, commentId: n });
        try {
          let l = await this.waitForRuntimeCommentScreenshotReady(a, n);
          if (l == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser annotation screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              CI().warning(`timed out waiting for browser annotation screenshot`, {
                safe: { commentId: n, conversationId: t },
              }));
            return;
          }
          let u = c.snapshot.comments.find((e) => e.id === n || e.designChange?.id === n);
          if (u?.designChange == null) return;
          let d = await this.captureBrowserScreenshot(a, {
            annotationViewportRect: e,
            annotation: u.designChange,
            markerViewportPoint: r,
            readyMessage: l,
            screenshotCropRect: o,
            shouldUseCompactScreenshot: s,
          });
          if (d == null) return;
          ((c.snapshot = {
            ...c.snapshot,
            comments: c.snapshot.comments.map((e) =>
              e.id === u.id
                ? {
                    ...e,
                    screenshot: d,
                    designChange:
                      e.designChange == null ? void 0 : { ...e.designChange, screenshot: d },
                  }
                : e,
            ),
          }),
            this.delegate.syncCommentSnapshot(i, t, c.browserTabId));
        } finally {
          a.isDestroyed() ||
            a.send(R, { type: `browser-sidebar-runtime-clear-comment-screenshot` });
        }
      }
    }
    async captureDirectCommentScreenshot({
      annotationViewportRect: e,
      comment: t,
      conversationId: n,
      markerViewportPoint: r,
      page: i,
      screenshotCropRect: a,
      shouldUseCompactScreenshot: o,
      threadState: s,
    }) {
      if (!(i == null || i.isDestroyed())) {
        (this.syncSnapshotRuntimeStateToPage(i, s, [...s.snapshot.comments, t]),
          i.send(R, {
            type: `browser-sidebar-runtime-prepare-comment-screenshot`,
            commentId: t.id,
          }));
        try {
          let s = await this.waitForRuntimeCommentScreenshotReady(i, t.id);
          if (s == null) {
            (this.errorReporter.reportNonFatal(
              Error(`Timed out waiting for browser comment screenshot`),
              { kind: `browser-sidebar-comment-screenshot-ready-timeout` },
            ),
              CI().warning(`timed out waiting for browser comment screenshot`, {
                safe: { commentId: t.id, conversationId: n },
              }));
            return;
          }
          return s.skipScreenshotCapture === !0
            ? void 0
            : await this.captureBrowserScreenshot(i, {
                annotationViewportRect: e,
                annotation: t,
                markerViewportPoint: r,
                readyMessage: s,
                screenshotCropRect: a,
                shouldUseCompactScreenshot: o,
              });
        } finally {
          i.isDestroyed() ||
            i.send(R, { type: `browser-sidebar-runtime-clear-comment-screenshot` });
        }
      }
    }
    async enrichDesignEditorProvenance(e, t) {
      if (e.isDestroyed()) return t;
      let n = e.debugger,
        r = n.isAttached();
      try {
        (r || n.attach(`1.3`),
          await Promise.all([n.sendCommand(`DOM.enable`), n.sendCommand(`CSS.enable`)]));
        let e = jI.parse(await n.sendCommand(`DOM.getDocument`, { pierce: !0 })),
          i = MI.parse(
            await n.sendCommand(`DOM.querySelector`, {
              nodeId: e.root.nodeId,
              selector: `[${AI}~="${uL(t.draftAttribute)}"]`,
            }),
          );
        return i.nodeId === 0
          ? t
          : aL(
              t,
              LI.parse(await n.sendCommand(`CSS.getMatchedStylesForNode`, { nodeId: i.nodeId })),
            );
      } catch (e) {
        return (
          CI().warning(`failed to inspect browser design style provenance`, {
            safe: { groupId: t.id },
            sensitive: { error: e },
          }),
          t
        );
      } finally {
        if (!r && !e.isDestroyed() && n.isAttached())
          try {
            n.detach();
          } catch (e) {
            CI().warning(`failed to detach browser design debugger`, {
              safe: { groupId: t.id },
              sensitive: { error: e },
            });
          }
      }
    }
    syncSnapshotRuntimeStateToPage(e, t, n = t.snapshot.comments) {
      this.sendRuntimeStateToPage(e, t, n);
    }
    syncRuntimeStateToPage(e, t, n, r, i = r.snapshot.comments) {
      this.sendRuntimeStateToPage(
        n,
        r,
        this.getRuntimeCommentsForSync(e, t, r.browserTabId, i),
        this.getRuntimeActiveDesignChangeForSync(e, t, r.browserTabId),
      );
    }
    sendRuntimeStateToPage(e, n, r, i) {
      if (e == null || e.isDestroyed()) return;
      let a = this.delegate.canUseTweaks();
      e.send(R, {
        type: `browser-sidebar-runtime-sync`,
        interactionMode: n.snapshot.interactionMode,
        annotationEditorMode: a ? n.snapshot.annotationEditorMode : `comment`,
        isAgentControllingBrowser: n.isAgentControllingBrowser,
        canUseTweaks: a,
        isDesignModifierPressed: a && n.snapshot.isDesignModifierPressed === !0,
        isOriginalViewEnabled: a && n.snapshot.isOriginalViewEnabled === !0,
        isTweaksEditorOpen: a && n.snapshot.isTweaksEditorOpen === !0,
        ...(a && i != null ? { activeDesignChange: i } : {}),
        intlConfig: n.runtimeIntlConfig,
        comments: t.ds(r, a).map(t.ls),
        viewportScale: n.viewportScale,
        zoomPercent: n.snapshot.zoomPercent,
      });
    }
    getRuntimeCommentsForSync(e, t, n, r) {
      if (t == null) return r;
      let i = this.overlayManager.getOverlayStateForOwner(e, t, n);
      if (i == null) return r;
      let a = i.session.target;
      if (a.mode !== `edit`) return r;
      let o = i.session.designEditorState?.id ?? i.session.designChange?.id;
      return o == null
        ? r
        : r.map((e) =>
            e.id !== a.commentId && e.designChange?.id !== o
              ? e
              : i.session.designChange == null
                ? XI(e)
                : { ...e, designChange: i.session.designChange },
          );
    }
    getRuntimeActiveDesignChangeForSync(e, t, n) {
      if (t == null) return;
      let r = this.overlayManager.getOverlayStateForOwner(e, t, n);
      if (!(r == null || r.session.target.mode === `edit` || r.session.designChange == null))
        return r.session.designChange;
    }
    closeRuntimeEditor(e, t) {
      e == null ||
        e.isDestroyed() ||
        e.send(R, {
          type: `browser-sidebar-runtime-close-editor`,
          ...(t == null ? {} : { target: t }),
        });
    }
    setTweaksEditorOpen(e, t, n, r, i) {
      (r.snapshot.isTweaksEditorOpen ?? !1) !== i &&
        ((r.snapshot = { ...r.snapshot, isTweaksEditorOpen: i }),
        this.delegate.syncCommentSnapshot(e, t, n));
    }
    setRuntimeDesignScrubProperty(e, t) {
      e == null ||
        e.isDestroyed() ||
        e.send(R, { type: `browser-sidebar-runtime-design-scrub-changed`, property: t });
    }
    setAnnotationEditorMode(e, t, n, r, i) {
      r.snapshot.annotationEditorMode !== i &&
        ((r.snapshot = { ...r.snapshot, annotationEditorMode: i }),
        this.delegate.syncCommentSnapshot(e, t, n));
    }
    waitForRuntimeCommentScreenshotReady(e, t) {
      return (
        this.runtimeCommentScreenshotWaiters.get(e.id)?.resolve(null),
        new Promise((n) => {
          let r = setTimeout(() => {
            this.runtimeCommentScreenshotWaiters.get(e.id)?.commentId === t &&
              (this.runtimeCommentScreenshotWaiters.delete(e.id), n(null));
          }, wI);
          this.runtimeCommentScreenshotWaiters.set(e.id, {
            commentId: t,
            resolve: (i) => {
              (clearTimeout(r),
                this.runtimeCommentScreenshotWaiters.get(e.id)?.commentId === t &&
                  this.runtimeCommentScreenshotWaiters.delete(e.id),
                n(i));
            },
          });
        })
      );
    }
  };
function zI(e) {
  let t = !1,
    n = e.map((e) => {
      if (e.screenshot == null || e.screenshot.isCompact === !0) return e;
      t = !0;
      let n = e.screenshot.annotationViewportRect ?? e.screenshot.cropViewportRect ?? e.anchor.rect,
        r = e.screenshot.markerViewportPoint ?? e.markerViewportPoint,
        i = VI(n, r) ?? e.anchor.rect;
      return {
        ...e,
        screenshot: UI(
          BI(a.nativeImage.createFromDataURL(e.screenshot.dataUrl), e, i),
          !0,
          i,
          e.screenshot.annotationViewportRect ?? e.anchor.rect,
          r,
        ),
      };
    });
  return t ? n : e;
}
function BI(e, t, n) {
  let r = t.screenshot;
  return r?.cropViewportRect == null
    ? HI(e, { annotation: t, screenshotCropRect: n, shouldUseCompactScreenshot: !0 })
    : KI(GI(e, r.cropViewportRect, n, t.viewportSize, r.cropPaddingPx ?? TI), kI);
}
function VI(e, t) {
  let n = t == null ? null : { x: t.x - 26 / 2, y: t.y - 26 / 2, width: 26, height: 26 };
  if (e == null) return n;
  if (n == null) return e;
  let r = Math.min(e.x, n.x),
    i = Math.min(e.y, n.y),
    a = Math.max(e.x + e.width, n.x + n.width),
    o = Math.max(e.y + e.height, n.y + n.height);
  return { x: r, y: i, width: a - r, height: o - i };
}
function HI(e, { annotation: t, screenshotCropRect: n, shouldUseCompactScreenshot: r }) {
  let i = t.viewportSize,
    a = n != null && i != null && i.width > 0 && i.height > 0;
  return KI(a ? WI(e, n, i, r ? EI : TI) : e, r ? kI : a ? OI : DI);
}
function UI(e, t, n, r, i) {
  let { width: a, height: o } = e.getSize();
  return {
    ...(r == null ? {} : { annotationViewportRect: r }),
    ...(n == null ? {} : { cropPaddingPx: t ? EI : TI, cropViewportRect: n }),
    dataUrl: e.toDataURL(),
    ...(t ? { isCompact: !0 } : {}),
    ...(i == null ? {} : { markerViewportPoint: i }),
    width: a,
    height: o,
  };
}
function WI(e, t, n, r) {
  let i = e.getSize(),
    a = i.width / n.width,
    o = i.height / n.height,
    s = Math.max(0, Math.floor((t.x - r) * a)),
    c = Math.max(0, Math.floor((t.y - r) * o)),
    l = Math.min(i.width, Math.ceil((t.x + t.width + r) * a)),
    u = Math.min(i.height, Math.ceil((t.y + t.height + r) * o)),
    d = l - s,
    f = u - c;
  return d <= 0 || f <= 0 ? e : e.crop({ x: s, y: c, width: d, height: f });
}
function GI(e, t, n, r, i) {
  if (r == null || r.width <= 0 || r.height <= 0) return e;
  let a = Math.max(0, t.x - i),
    o = Math.max(0, t.y - i),
    s = Math.min(r.width, t.x + t.width + i),
    c = Math.min(r.height, t.y + t.height + i),
    l = Math.max(0, n.x - EI),
    u = Math.max(0, n.y - EI),
    d = Math.min(r.width, n.x + n.width + EI),
    f = Math.min(r.height, n.y + n.height + EI),
    p = s - a,
    m = c - o;
  if (p <= 0 || m <= 0) return e;
  let h = e.getSize(),
    g = h.width / p,
    _ = h.height / m,
    v = Math.max(0, Math.floor((l - a) * g)),
    y = Math.max(0, Math.floor((u - o) * _)),
    b = Math.min(h.width, Math.ceil((d - a) * g)),
    x = Math.min(h.height, Math.ceil((f - o) * _)),
    S = b - v,
    C = x - y;
  return S <= 0 || C <= 0 ? e : e.crop({ x: v, y, width: S, height: C });
}
function KI(e, t) {
  let { width: n, height: r } = e.getSize(),
    i = Math.max(n, r);
  if (i <= t) return e;
  let a = t / i;
  return e.resize({
    width: Math.max(1, Math.round(n * a)),
    height: Math.max(1, Math.round(r * a)),
  });
}
function qI({
  anchorState: e,
  body: t,
  commentId: n,
  attachedImages: r,
  designChange: i,
  screenshot: a,
}) {
  return {
    id: n,
    createdAt: new Date().toISOString(),
    anchor: e.anchor,
    color: `blue`,
    ...(e.anchor.kind === `element` ? { markerViewportPoint: e.viewportPoint } : {}),
    ...(r == null ? {} : { attachedImages: r }),
    ...(t == null || t.length === 0 ? {} : { body: t }),
    ...(i == null ? {} : { designChange: i }),
    ...(a == null ? {} : { screenshot: a }),
    ...(e.themeVariant == null ? {} : { themeVariant: e.themeVariant }),
    ...(e.viewportSize == null ? {} : { viewportSize: e.viewportSize }),
  };
}
function JI(e, t) {
  return {
    ...e,
    ...(t.length === 0 ? {} : { comment: t }),
    status: `queued`,
    updatedAt: new Date().toISOString(),
  };
}
function YI(e, t) {
  return e.some((e) => e.id === t.id) ? e.map((e) => (e.id === t.id ? t : e)) : [...e, t];
}
function XI(e) {
  let { designChange: t, ...n } = e;
  return n;
}
function ZI(e) {
  let { body: t, ...n } = e;
  return n;
}
function QI(e, ...t) {
  let n = new Map();
  for (let e of t)
    for (let t of e ?? []) {
      let e = n.get(t.property);
      (e == null || iL(t) >= iL(e)) && n.set(t.property, t);
    }
  return e.flatMap((e) => {
    let t = n.get(e.property);
    return t == null ? [] : [t];
  });
}
function $I(e, t) {
  return QI(e, t).length === e.length && t.every((e) => iL(e) > 0);
}
function eL(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          e.property === r.property &&
          e.selectorText === r.selectorText &&
          e.sourceUrl === r.sourceUrl &&
          e.sourceLine === r.sourceLine &&
          e.sourceColumn === r.sourceColumn
        );
      })
    : !1;
}
function tL(e, n) {
  return e == null
    ? !1
    : e.id === n.id &&
        e.createdAt === n.createdAt &&
        e.status === n.status &&
        e.draftAttribute === n.draftAttribute &&
        e.selector === n.selector &&
        e.targetLabel === n.targetLabel &&
        t.ss(e.anchor, n.anchor) &&
        e.markerViewportPoint?.x === n.markerViewportPoint?.x &&
        e.markerViewportPoint?.y === n.markerViewportPoint?.y &&
        e.viewportSize?.width === n.viewportSize?.width &&
        e.viewportSize?.height === n.viewportSize?.height &&
        nL(e.declarations, n.declarations) &&
        rL(e.text, n.text);
}
function nL(e, t) {
  return e.length === t.length
    ? e.every((e, n) => {
        let r = t[n];
        return (
          e.property === r.property && e.previousValue === r.previousValue && e.value === r.value
        );
      })
    : !1;
}
function rL(e, t) {
  return e == null || t == null
    ? e == null && t == null
    : e.previousValue === t.previousValue && e.value === t.value;
}
function iL(e) {
  let t = 0;
  return (
    e.selectorText != null && (t += 1),
    e.sourceUrl != null && (t += 1),
    e.sourceLine != null && (t += 1),
    e.sourceColumn != null && (t += 1),
    t
  );
}
function aL(e, t) {
  let n = e.declarations.map((e) => oL(e, t));
  return { ...e, provenance: n };
}
function oL(e, t) {
  let n = sL(e.property, t.inlineStyle?.cssProperties);
  if (n != null)
    return {
      property: e.property,
      selectorText: `element.style`,
      ...(n.range == null
        ? {}
        : { sourceLine: n.range.startLine + 1, sourceColumn: n.range.startColumn + 1 }),
    };
  let r = { property: e.property };
  for (let n of t.matchedCSSRules ?? []) {
    let t = sL(e.property, n.rule.style?.cssProperties);
    t != null &&
      (r = {
        property: e.property,
        selectorText: n.rule.selectorList?.text,
        sourceUrl: lL(n),
        ...(t.range == null
          ? {}
          : { sourceLine: t.range.startLine + 1, sourceColumn: t.range.startColumn + 1 }),
      });
  }
  return r;
}
function sL(e, t) {
  return t?.findLast((t) => cL(e).includes(t.name) && t.disabled !== !0 && t.implicit !== !0);
}
function cL(e) {
  switch (e) {
    case `padding-top`:
    case `padding-right`:
    case `padding-bottom`:
    case `padding-left`:
      return [e, `padding`];
    case `margin-top`:
    case `margin-right`:
    case `margin-bottom`:
    case `margin-left`:
      return [e, `margin`];
    case `row-gap`:
    case `column-gap`:
      return [e, `gap`];
    default:
      return [e];
  }
}
function lL(e) {
  return (
    e.rule.media?.findLast((e) => e.sourceURL != null)?.sourceURL ??
    e.rule.containerQueries?.findLast((e) => e.sourceURL != null)?.sourceURL ??
    e.rule.supports?.findLast((e) => e.sourceURL != null)?.sourceURL
  );
}
function uL(e) {
  return e.replace(/\\/g, `\\\\`).replace(/"/g, `\\"`);
}
function dL(e, t) {
  let n = fL(e),
    r = fL(t);
  if (n == null || r == null) return e === t;
  if (n.type !== r.type) return !1;
  switch (n.type) {
    case `http`:
      return (
        r.type === `http` &&
        n.origin === r.origin &&
        n.pathname === r.pathname &&
        n.search === r.search
      );
    case `file`:
      return r.type === `file` && n.pathname === r.pathname && n.search === r.search;
    case `other`:
      return r.type === `other` && n.value === r.value;
  }
}
function fL(e) {
  try {
    let t = new URL(e);
    return t.protocol === `http:` || t.protocol === `https:`
      ? { type: `http`, origin: t.origin, pathname: t.pathname, search: t.search }
      : t.protocol === `file:`
        ? { type: `file`, pathname: t.pathname, search: t.search }
        : { type: `other`, value: t.toString() };
  } catch {
    return null;
  }
}
var pL = 1e3,
  mL = `CODEX_BROWSER_USE_DEFAULT_VIEWPORT_SIZE`,
  hL = t.Gr(`browser-sidebar-manager`);
function gL() {
  let e = process.env[mL]?.trim(),
    t = /^(?<width>\d+)x(?<height>\d+)$/u.exec(e ?? ``);
  return t?.groups == null
    ? null
    : { width: Number.parseInt(t.groups.width, 10), height: Number.parseInt(t.groups.height, 10) };
}
async function _L(e, t) {
  let n = e.debugger.isAttached(),
    r = null;
  try {
    (n || e.debugger.attach(`1.3`),
      await Promise.race([
        e.debugger.sendCommand(`Emulation.setEmulatedMedia`, {
          features: [{ name: `prefers-color-scheme`, value: t }],
        }),
        new Promise((e, t) => {
          ((r = setTimeout(() => {
            t(Error(`Timed out syncing browser page color scheme`));
          }, pL)),
            r.unref());
        }),
      ]));
  } catch (t) {
    hL().warning(`failed to sync browser page color scheme`, {
      safe: { webContentsId: e.id },
      sensitive: { error: t },
    });
  } finally {
    if ((r != null && clearTimeout(r), !n && e.debugger.isAttached()))
      try {
        e.debugger.detach();
      } catch (t) {
        hL().warning(`failed to detach browser page debugger`, {
          safe: { webContentsId: e.id },
          sensitive: { error: t },
        });
      }
  }
}
function vL(e, t, n) {
  return n === OF(t) ? `${e}:${t}` : `${e}:${t}:${n}`;
}
function yL({ browserTabId: e, conversationId: t }) {
  return e === OF(t) ? t : `${t}\0${e}`;
}
function bL(e) {
  try {
    return r.V().formatMessage(e);
  } catch {
    return IL(e);
  }
}
function xL(e) {
  return e === `about:blank` ? `` : e;
}
function SL(e, t) {
  let n = xL(e),
    r = xL(t);
  if (n.length === 0 || r.length === 0) return !0;
  try {
    let e = new URL(n),
      t = new URL(r);
    return e.protocol === `file:` || t.protocol === `file:`
      ? e.href !== t.href
      : e.origin !== t.origin;
  } catch {
    return n !== r;
  }
}
function CL(e) {
  try {
    let t = new URL(e);
    return t.protocol === `file:`
      ? `file_html`
      : t.protocol !== `http:` && t.protocol !== `https:`
        ? `unknown`
        : t.hostname === `localhost` ||
            t.hostname === `127.0.0.1` ||
            t.hostname === `[::1]` ||
            t.hostname === `::1`
          ? `local_http`
          : `remote_http`;
  } catch {
    return `unknown`;
  }
}
function wL(e) {
  return e.pendingUrl.length > 0 && e.pendingUrl !== e.url ? e.pendingUrl : e.url;
}
function TL({ faviconUrl: e, title: t, url: n }) {
  return {
    runtime: `electron-webview`,
    faviconUrl: e,
    navigation: jL(t, n),
    title: t,
    updatedAt: Date.now(),
    url: n,
  };
}
function EL({ faviconUrl: e, title: t, url: n, webContents: r }) {
  return {
    runtime: `electron-webview`,
    faviconUrl: e,
    navigation: AL(t, n, r),
    title: t,
    updatedAt: Date.now(),
    url: n,
  };
}
function DL(e) {
  let t = e?.navigation ?? null;
  if (t == null || t.entries.length === 0) return null;
  let n = t.currentIndex;
  return n == null || n < 0 || n >= t.entries.length
    ? null
    : t.entries.length > 1 || n !== 0 || t.entries.some((e) => e.pageState != null)
      ? t
      : null;
}
function OL(e, t, n) {
  return Math.min(n, Math.max(t, Math.round(e)));
}
function kL(e) {
  return Math.max(1, Math.ceil(e));
}
function AL(e, t, n) {
  let r = n.navigationHistory;
  if (
    t.length > 0 &&
    !n.isDestroyed() &&
    !n.isLoading() &&
    r != null &&
    typeof r.getAllEntries == `function` &&
    typeof r.getActiveIndex == `function`
  ) {
    let e = r
      .getAllEntries()
      .map((e) => ({
        ...(e.pageState == null ? {} : { pageState: e.pageState }),
        title: e.title,
        url: e.url,
      }));
    if (e.length > 0) return { currentIndex: r.getActiveIndex(), entries: e };
  }
  return jL(e, t);
}
function jL(e, t) {
  return t.length === 0
    ? { currentIndex: null, entries: [] }
    : { currentIndex: 0, entries: [{ title: e, url: t }] };
}
function ML(e) {
  e.navigationVersion += 1;
}
function NL(e) {
  return e.page == null ? e.snapshot.url : wL(e.page);
}
function PL(e, t, n) {
  if (e.isDestroyed() || t == null || n == null || n.isDestroyed()) return;
  let r = a.screen.getCursorScreenPoint(),
    i = e.getContentBounds(),
    o = r.x - i.x - t.x,
    s = r.y - i.y - t.y;
  o < 0 ||
    s < 0 ||
    o > t.width ||
    s > t.height ||
    n.sendInputEvent({ type: `mouseMove`, x: o, y: s, movementX: 0, movementY: 0 });
}
function FL(e, t, n) {
  let r = n.view.webContents;
  return r == null || r.isDestroyed()
    ? null
    : {
        browserTabId: n.browserTabId,
        pageKey: vL(e.owner.id, t, n.browserTabId),
        webContents: r,
        title: n.title || n.fallbackTitle,
        url: wL(n),
        active: e.activeConversationId === t && e.activeBrowserTabId === n.browserTabId,
      };
}
function IL({ defaultMessage: e, values: t }) {
  return e.replace(/\{(\w+)\}/g, (e, n) => t?.[n] ?? e);
}
function LL({
  blocklistLookup: e,
  comments: t,
  sendPendingCommentModeRequestToRuntime: n,
  syncRuntimeStateToPage: r,
  syncThreadState: i,
  threadState: a,
  windowState: o,
}) {
  let s = (e) => {
      (!e.changed && e.request == null) ||
        (e.changed &&
          (i(o, a.conversationId, a), e.shouldSyncRuntime && a.page != null && r(a.page, a)),
        e.shouldPrepareComments && t.prepare(o.owner, a.conversationId, a.browserTabId, a),
        e.request != null && a.page != null && n(a.page, e.request));
    },
    c = (e) => {
      let n = !1,
        r = null,
        i = !1,
        s = !1,
        c = e ? `site-blocked` : null;
      return (
        a.snapshot.commentModeDisabledReason !== c &&
          ((a.snapshot = { ...a.snapshot, commentModeDisabledReason: c }), (n = !0)),
        e
          ? (a.pendingCommentModeActivation != null && (a.pendingCommentModeActivation = null),
            a.snapshot.interactionMode === `comment` &&
              ((a.snapshot = { ...a.snapshot, interactionMode: `browse` }),
              t.dismiss(o.owner, a.conversationId, a.browserTabId, a),
              (n = !0),
              (s = !0)),
            { changed: n, request: r, shouldPrepareComments: i, shouldSyncRuntime: s })
          : (a.pendingCommentModeActivation != null &&
              ((r = a.pendingCommentModeActivation.request),
              (a.pendingCommentModeActivation = null),
              a.snapshot.interactionMode !== `comment` &&
                ((a.snapshot = { ...a.snapshot, interactionMode: `comment` }),
                (n = !0),
                (i = !0),
                (s = !0))),
            { changed: n, request: r, shouldPrepareComments: i, shouldSyncRuntime: s })
      );
    },
    l = (e, t, n) => {
      a.commentModeBlockCheckRequestId === e &&
        ((a.commentModeBlockCheckUrl = null), NL(a) === t && s(c(n)));
    },
    u = NL(a);
  a.pendingCommentModeActivation != null &&
    (a.pendingCommentModeActivation.url !== u ||
      a.pendingCommentModeActivation.navigationVersion !== (a.page?.navigationVersion ?? null)) &&
    (a.pendingCommentModeActivation = null);
  let d = e.cachedCommentModeBlocked(u);
  if (d != null) {
    s(c(d));
    return;
  }
  if (a.commentModeBlockCheckUrl === u) return;
  (a.snapshot.commentModeDisabledReason != null &&
    a.snapshot.url !== u &&
    (a.snapshot = { ...a.snapshot, commentModeDisabledReason: null }),
    (a.commentModeBlockCheckUrl = u));
  let f = ++a.commentModeBlockCheckRequestId;
  e.isCommentModeBlocked(u).then((e) => {
    l(f, u, e);
  });
}
var RL = t.Hs.enum([`agent-history`, `navigation`]),
  zL = t.Hs.object({
    lastOpenedAt: t.Hs.number().finite().nullable(),
    lastSeenAt: t.Hs.number().finite(),
    source: RL.optional(),
    url: t.Hs.string().max(2e3),
  }),
  BL = t.Hs.object({
    hiddenRouteUrls: t.Hs.array(t.Hs.string().max(2e3)).optional().default([]),
    lastOpenedAt: t.Hs.number().finite().nullable(),
    lastRunningAt: t.Hs.number().finite().nullable(),
    lastSeenAt: t.Hs.number().finite(),
    previewImageDataUrl: t.Hs.string().max(5e4).nullable(),
    routes: t.Hs.array(zL).optional().default([]),
    title: t.Hs.string().max(120).nullable(),
    url: t.Hs.string().max(2e3),
  }),
  VL = t.Hs.object({
    hiddenServers: t.Hs.array(BL).optional().default([]),
    version: t.Hs.literal(1),
    servers: t.Hs.array(BL),
  }),
  HL = 800,
  UL = 6e4,
  WL = 128 * 1024,
  GL = 8,
  KL = eB(),
  qL = 16,
  JL = 10,
  YL = 64 * 1024,
  XL = 2,
  ZL = 300,
  QL = [],
  $L = new Set([5e3, 7e3]),
  eR = /\bhttps?:\/\/(?:localhost|127\.0\.0\.1|0\.0\.0\.0|\[::1\]):\d{1,5}(?:[/?#][^\s"'<>`]*)?/gi,
  tR = /\b(?:localhost|127\.0\.0\.1|0\.0\.0\.0):\d{1,5}(?:[/?#][^\s"'<>`]*)?/gi,
  nR = /\bports?\s+[`'"]?(\d{1,5})[`'"]?\b/gi,
  rR = /\.(?:avif|css|eot|gif|ico|jpe?g|js|json|map|mjs|otf|png|svg|ttf|webp|woff2?)$/i,
  iR = /#[0-9a-f]{3,8}\b|rgba?\([^)]*\)|hsla?\([^)]*\)|\b[a-z]+\b/gi,
  aR = /^#[0-9a-f]{3,8}$/i,
  oR = /^rgba?\(\s*\d{1,3}%?\s*,\s*\d{1,3}%?\s*,\s*\d{1,3}%?(?:\s*,\s*(?:0|1|0?\.\d+))?\s*\)$/i,
  sR = /^hsla?\(\s*\d{1,3}(?:deg)?\s*,\s*\d{1,3}%\s*,\s*\d{1,3}%(?:\s*,\s*(?:0|1|0?\.\d+))?\s*\)$/i,
  cR = new Set([
    `black`,
    `blue`,
    `gray`,
    `green`,
    `grey`,
    `orange`,
    `pink`,
    `purple`,
    `red`,
    `white`,
    `yellow`,
  ]),
  lR = new Set([`#root`, `:root`, `body`, `html`]),
  uR = t.Hs.object({
    text: t.Hs.string().optional(),
    type: t.Hs.string().optional(),
  }).passthrough(),
  dR = t.Hs.object({
    content: t.Hs.union([t.Hs.string(), t.Hs.array(uR)]).optional(),
    hidden: t.Hs.boolean().optional(),
    role: t.Hs.enum([`assistant`, `user`]).optional(),
    type: t.Hs.string().optional(),
  }).passthrough(),
  fR = t.Hs.object({
    hidden: t.Hs.boolean().optional(),
    item: t.Hs.unknown().optional(),
    message: t.Hs.unknown().optional(),
    payload: t.Hs.unknown().optional(),
    type: t.Hs.string().optional(),
  }).passthrough(),
  pR = { hiddenServers: [], isLoading: !0, refreshedAt: null, servers: [] },
  mR = class {
    cache = new Map();
    hiddenServers = new Map();
    persistencePath;
    recentServers = new Map();
    loadPersistedServersPromise = null;
    persistServersPromise = Promise.resolve();
    portScanCache = null;
    constructor({ persistencePath: e = null } = {}) {
      this.persistencePath = e;
    }
    getCachedState(e) {
      let t = this.cache.get(nB(e))?.state ?? this.portScanCache?.state ?? null;
      return t == null ? null : Tz(t);
    }
    shouldRefresh(e) {
      return Cz(this.cache.get(nB(e)), { rolloutFileVersion: rB(e) });
    }
    getLocalServerRouteUrl(e) {
      let t = Wz(e);
      return t != null && Yz(t) != null ? t : null;
    }
    refresh(
      e,
      {
        force: t = !1,
        forcePortScan: n = t,
        includePortScan: r = !0,
        navigationRouteUrls: i = [],
        revalidateKnownServers: a = !1,
      } = {},
    ) {
      let o = nB(e),
        s = this.cache.get(o),
        c = rB(e);
      if (s?.inflight != null && !t) return s.inflight;
      if (s != null && !t && !Cz(s, { rolloutFileVersion: c })) return Promise.resolve(Tz(s.state));
      let l = this.discoverLocalServers(e, {
        forcePortScan: n,
        includePortScan: r,
        navigationRouteUrls: i,
        revalidateKnownServers: a,
      }).then(
        (e) => (
          this.cache.get(o)?.inflight === l &&
            this.cache.set(o, { inflight: null, rolloutFileVersion: c, state: e }),
          e
        ),
        () => {
          let e = wz();
          return (
            this.cache.get(o)?.inflight === l &&
              this.cache.set(o, { inflight: null, rolloutFileVersion: c, state: e }),
            e
          );
        },
      );
      return (this.cache.set(o, { inflight: l, rolloutFileVersion: c, state: s?.state ?? pR }), l);
    }
    async recordOpened(e) {
      let t = Wz(e);
      if (t == null) return !1;
      let n = Uz(t);
      if (n == null) return !1;
      let r = Yz(n);
      if (r == null || lz(this.recentServers, n) == null) return !1;
      let i = Kz(t, n) && !qz(t) && (await Pz(t)),
        a = Date.now(),
        o = i ? [{ lastOpenedAt: a, lastSeenAt: a, source: `navigation`, url: t }] : [],
        s = lz(this.recentServers, n);
      if (s == null) return !1;
      let c = i ? hz(o, s.routes) : s.routes.filter((e) => Wz(e.url) !== t);
      return (
        this.recentServers.set(r, {
          hiddenRouteUrls: i ? s.hiddenRouteUrls.filter((e) => e !== t) : s.hiddenRouteUrls,
          lastOpenedAt: a,
          lastRunningAt: s.lastRunningAt,
          lastSeenAt: s.lastSeenAt,
          previewImageDataUrl: s.previewImageDataUrl,
          routes: c,
          source: s.source,
          title: s.title,
          url: s.url,
        }),
        this.trimRecentServers(),
        this.updateCachedServer(r, (e) => ({
          ...e,
          lastOpenedAt: a,
          routes: i ? hz(o, e.routes) : e.routes.filter((e) => Wz(e.url) !== t),
        })),
        this.persistServers(),
        !0
      );
    }
    removeRoute(e, t) {
      let n = Uz(e),
        r = Wz(t);
      if (n == null || r == null || !Kz(r, n)) return !1;
      let i = Yz(n);
      if (i == null) return !1;
      let a = !1,
        o = (e) => (
          (a = !0),
          { ...e, hiddenRouteUrls: _z([r], e.hiddenRouteUrls), routes: gz(e.routes, [r]) }
        ),
        s = lz(this.recentServers, n);
      s != null && this.recentServers.set(i, o(s));
      let c = lz(this.hiddenServers, n);
      return (
        c != null && this.hiddenServers.set(i, o(c)),
        this.updateCachedServer(i, (e) => ({ ...e, routes: gz(e.routes, [r]) })),
        a ? (this.persistServers(), !0) : !1
      );
    }
    hideServer(e) {
      let t = Uz(e.url);
      if (t == null) return !1;
      let n = Yz(t);
      if (n == null) return !1;
      let r = pz(e, lz(this.recentServers, t)?.hiddenRouteUrls ?? []);
      return (
        this.hiddenServers.set(n, r),
        this.recentServers.set(n, r),
        this.trimRecentServers(),
        this.trimHiddenServers(),
        this.updateCachedStates((t) => ({
          ...t,
          hiddenServers: nz([...t.hiddenServers.filter((e) => Yz(e.url) !== n), e]).slice(0, qL),
          servers: t.servers.filter((e) => Yz(e.url) !== n),
        })),
        this.persistServers(),
        !0
      );
    }
    unhideServer(e) {
      let t = Uz(e);
      if (t == null) return !1;
      let n = Yz(t);
      if (n == null) return !1;
      let r = this.hiddenServers.get(n);
      if (r == null) return !1;
      (this.hiddenServers.delete(n), this.recentServers.set(n, r), this.trimRecentServers());
      let i = this.getCachedHiddenServer(n) ?? mz(r);
      return (
        this.updateCachedStates((e) => ({
          ...e,
          hiddenServers: e.hiddenServers.filter((e) => Yz(e.url) !== n),
          servers: nz([...e.servers.filter((e) => Yz(e.url) !== n), i]).slice(0, GL),
        })),
        this.persistServers(),
        !0
      );
    }
    refreshPortScanState({ force: e = !1, includePortScan: t = !0 } = {}) {
      if (!t) return Promise.resolve(wz());
      if (this.portScanCache?.inflight != null) return this.portScanCache.inflight;
      if (this.portScanCache != null && !e && !Cz(this.portScanCache, { rolloutFileVersion: null }))
        return Promise.resolve(this.portScanCache.state);
      let n = Date.now(),
        r = WR(VR(), n).then(
          (e) => {
            let t = { hiddenServers: [], isLoading: !1, refreshedAt: n, servers: e.slice(0, GL) };
            return (
              (this.portScanCache = { inflight: null, rolloutFileVersion: null, state: t }), t
            );
          },
          () => {
            let e = wz();
            return (
              (this.portScanCache = { inflight: null, rolloutFileVersion: null, state: e }), e
            );
          },
        );
      return (
        (this.portScanCache = {
          inflight: r,
          rolloutFileVersion: null,
          state: this.portScanCache?.state ?? pR,
        }),
        r
      );
    }
    async discoverLocalServers(
      e,
      { forcePortScan: t, includePortScan: n, navigationRouteUrls: r, revalidateKnownServers: i },
    ) {
      let a = Date.now(),
        [, o, s] = await Promise.all([
          this.loadPersistedServers(),
          kR(e),
          this.refreshPortScanState({ force: t, includePortScan: n }),
        ]),
        c = zR(o, a, `agent-history`, `agent-history`, !0),
        l = HR(zR(r, a, `recent`, `navigation`, !0), c),
        u = await KR(tz(s.servers, this.recentServers)),
        d = new Set(this.hiddenServers.keys()),
        f = u.filter((e) => {
          let t = Yz(e.url);
          return t == null || !d.has(t);
        }),
        p = uz(l);
      i || dz(p, f);
      for (let e of d) p.add(e);
      let m = await GR(
          [
            ...l.filter((e) => {
              let t = Yz(e.url);
              return t == null || !d.has(t);
            }),
            ...BR(this.recentServers, p),
          ],
          this.recentServers,
        ),
        h = await fz({ hiddenServers: this.hiddenServers, portScanServers: u }),
        g = nz($R([...m, ...f]));
      return (
        this.saveRecentServers(g),
        this.saveHiddenServers(h),
        await this.persistServers(),
        { hiddenServers: h.slice(0, qL), isLoading: !1, refreshedAt: a, servers: g.slice(0, GL) }
      );
    }
    loadPersistedServers() {
      let e = this.persistencePath;
      return e == null
        ? Promise.resolve()
        : ((this.loadPersistedServersPromise ??= FR(e).then(({ hiddenServers: e, servers: t }) => {
            for (let e of t) {
              let t = Yz(e.url);
              t != null && !this.recentServers.has(t) && this.recentServers.set(t, e);
            }
            for (let t of e) {
              let e = Yz(t.url);
              e != null && !this.hiddenServers.has(e) && this.hiddenServers.set(e, t);
            }
            (this.trimRecentServers(), this.trimHiddenServers());
          })),
          this.loadPersistedServersPromise);
    }
    saveRecentServers(e) {
      for (let t of e) {
        let e = Yz(t.url);
        if (e == null) continue;
        let n = lz(this.recentServers, t.url);
        (t.status !== `running` && n == null) ||
          this.recentServers.set(e, {
            hiddenRouteUrls: n?.hiddenRouteUrls ?? [],
            lastOpenedAt: t.lastOpenedAt ?? n?.lastOpenedAt ?? null,
            lastRunningAt:
              t.status === `running` ? t.lastRunningAt : (n?.lastRunningAt ?? t.lastRunningAt),
            lastSeenAt: t.lastSeenAt,
            previewImageDataUrl: t.previewImageDataUrl ?? n?.previewImageDataUrl ?? null,
            routes: t.status === `running` ? t.routes : hz(t.routes, n?.routes ?? []),
            source: t.source,
            title: t.title ?? n?.title ?? null,
            url: t.url,
          });
      }
      this.trimRecentServers();
    }
    saveHiddenServers(e) {
      for (let t of e) {
        let e = Yz(t.url);
        e == null ||
          !this.hiddenServers.has(e) ||
          this.hiddenServers.set(e, pz(t, this.hiddenServers.get(e)?.hiddenRouteUrls ?? []));
      }
      this.trimHiddenServers();
    }
    trimRecentServers() {
      let e = cz(this.recentServers);
      this.recentServers.clear();
      for (let t of e.slice(0, qL)) {
        let e = Yz(t.url);
        e != null && this.recentServers.set(e, t);
      }
    }
    trimHiddenServers() {
      let e = cz(this.hiddenServers);
      this.hiddenServers.clear();
      for (let t of e.slice(0, qL)) {
        let e = Yz(t.url);
        e != null && this.hiddenServers.set(e, t);
      }
    }
    persistServers() {
      let e = this.persistencePath;
      if (e == null) return Promise.resolve();
      let t = LR(this.recentServers),
        n = LR(this.hiddenServers);
      return (
        (this.persistServersPromise = this.persistServersPromise
          .catch(() => void 0)
          .then(() => RR(e, { hiddenServers: n, servers: t }))
          .catch(() => void 0)),
        this.persistServersPromise
      );
    }
    updateCachedServer(e, t) {
      this.updateCachedStates((n) => ({
        ...n,
        hiddenServers: n.hiddenServers.map((n) => (Yz(n.url) === e ? t(n) : n)),
        servers: n.servers.map((n) => (Yz(n.url) === e ? t(n) : n)),
      }));
    }
    getCachedHiddenServer(e) {
      for (let t of this.cache.values()) {
        let n = t.state.hiddenServers.find((t) => Yz(t.url) === e);
        if (n != null) return n;
      }
      return null;
    }
    updateCachedStates(e) {
      this.portScanCache != null &&
        (this.portScanCache = { ...this.portScanCache, state: e(this.portScanCache.state) });
      for (let [t, n] of this.cache) this.cache.set(t, { ...n, state: e(n.state) });
    }
  };
function hR(e) {
  let t = new Set();
  return (Vz(t, e, eR), Vz(t, e, tR), Hz(t, e), Array.from(t));
}
function gR(e) {
  let t = /<title[^>]*>([\s\S]*?)<\/title>/i.exec(e);
  if (t == null) return null;
  let n = Qz(t[1]).replace(/\s+/g, ` `).trim();
  return n.length === 0 ? null : n.slice(0, 120);
}
function _R(e, t = QL) {
  let n = vR(e, t);
  return n.backgroundColor == null && n.textColor == null
    ? null
    : { backgroundColor: n.backgroundColor ?? `#ffffff`, textColor: n.textColor ?? `#1f2937` };
}
function vR(e, t) {
  let n = { backgroundColor: SR(e), textColor: null };
  for (let r of yR(e, t))
    ((n.backgroundColor = TR(r, [`background-color`, `background`]) ?? n.backgroundColor),
      (n.textColor = TR(r, [`color`]) ?? n.textColor));
  return n;
}
function yR(e, t) {
  let n = [];
  for (let t of e.matchAll(/<style\b[^>]*>([\s\S]*?)<\/style>/gi)) bR(n, t[1] ?? ``);
  for (let e of t) bR(n, e);
  for (let t of [`html`, `body`]) {
    let r = CR(e, t, `style`);
    r != null && n.push(r);
  }
  return n;
}
function bR(e, t) {
  for (let n of t.matchAll(/([^{}]+)\{([^{}]*)\}/g)) xR(n[1] ?? ``) && e.push(n[2] ?? ``);
}
function xR(e) {
  return e.split(`,`).some((e) => lR.has(e.trim()));
}
function SR(e) {
  for (let t of e.matchAll(/<meta\b[^>]*>/gi)) {
    let e = t[0];
    if (wR(e, `name`)?.toLowerCase() === `theme-color`) {
      let t = ER(wR(e, `content`));
      if (t != null) return t;
    }
  }
  return null;
}
function CR(e, t, n) {
  let r = RegExp(`<${t}\\b[^>]*>`, `i`).exec(e);
  return r == null ? null : wR(r[0], n);
}
function wR(e, t) {
  return RegExp(`\\b${t}\\s*=\\s*(["'])([\\s\\S]*?)\\1`, `i`).exec(e)?.[2] ?? null;
}
function TR(e, t) {
  let n = null;
  for (let r of e.matchAll(/(?:^|;)\s*([a-z-]+)\s*:\s*([^;]+)/gi)) {
    let e = r[1]?.toLowerCase();
    e != null && t.includes(e) && (n = ER(r[2] ?? ``) ?? n);
  }
  return n;
}
function ER(e) {
  if (e == null) return null;
  iR.lastIndex = 0;
  for (let t of e.matchAll(iR)) {
    let e = DR(t[0]);
    if (e != null) return e;
  }
  return null;
}
function DR(e) {
  let t = e.trim();
  if (aR.test(t)) return t.toLowerCase();
  if (oR.test(t) || sR.test(t)) return t.replace(/\s+/g, ` `);
  let n = t.toLowerCase();
  return cR.has(n) ? n : null;
}
function OR({ previewTheme: e, title: t, url: n }) {
  let r = e?.backgroundColor ?? `#ffffff`,
    i = e?.textColor ?? `#1f2937`,
    a = `<svg xmlns="http://www.w3.org/2000/svg" width="240" height="150" viewBox="0 0 240 150"><rect width="240" height="150" rx="10" fill="#f6f7f7"/><rect x="18" y="18" width="204" height="114" rx="8" fill="${r}" stroke="#d7dadc"/><circle cx="36" cy="36" r="4" fill="#ff6b6b"/><circle cx="50" cy="36" r="4" fill="#f6c85f"/><circle cx="64" cy="36" r="4" fill="#4ade80"/><rect x="32" y="58" width="176" height="12" rx="6" fill="${i}" opacity="0.16"/><rect x="32" y="82" width="124" height="8" rx="4" fill="${i}" opacity="0.24"/><text x="32" y="108" font-family="-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif" font-size="15" font-weight="600" fill="${i}">${$z((t ?? Zz(n)).slice(0, 64))}</text><text x="32" y="126" font-family="-apple-system,BlinkMacSystemFont,Segoe UI,sans-serif" font-size="10" fill="${i}" opacity="0.68">${$z(Zz(n).slice(0, 72))}</text></svg>`;
  return `data:image/svg+xml;base64,${Buffer.from(a).toString(`base64`)}`;
}
async function kR(e) {
  if (e == null || !(0, s.isAbsolute)(e)) return [];
  let t = new Set();
  try {
    let n = (0, y.createInterface)({
      crlfDelay: 1 / 0,
      input: (0, l.createReadStream)(e, { encoding: `utf8` }),
    });
    for await (let e of n) AR(t, e);
    return Array.from(t);
  } catch {
    return [];
  }
}
function AR(e, t) {
  let n = jR(t);
  if (n != null) for (let t of NR(n)) for (let n of hR(t)) e.add(n);
}
function jR(e) {
  try {
    return MR(JSON.parse(e));
  } catch {
    return null;
  }
}
function MR(e) {
  let t = dR.safeParse(e);
  if (t.success && t.data.role != null)
    return t.data.hidden === !0 || (t.data.type != null && t.data.type !== `message`)
      ? null
      : t.data;
  let n = fR.safeParse(e);
  if (!n.success || n.data.hidden === !0) return null;
  for (let e of [n.data.message, n.data.item, n.data.payload]) {
    let t = MR(e);
    if (t != null) return t;
  }
  return null;
}
function NR(e) {
  if (e.content == null) return [];
  if (typeof e.content == `string`) return [e.content];
  let t = [];
  for (let n of e.content) n.text != null && PR(e.role, n.type) && t.push(n.text);
  return t;
}
function PR(e, t) {
  return e === `user`
    ? t == null || t === `text` || t === `input_text`
    : e === `assistant`
      ? t == null || t === `text` || t === `output_text`
      : !1;
}
async function FR(e) {
  try {
    let t = await (0, p.readFile)(e, `utf8`),
      n = VL.safeParse(JSON.parse(t));
    return n.success
      ? { hiddenServers: IR(n.data.hiddenServers), servers: IR(n.data.servers) }
      : { hiddenServers: [], servers: [] };
  } catch {
    return { hiddenServers: [], servers: [] };
  }
}
function IR(e) {
  let n = [];
  for (let r of e) {
    let e = Uz(r.url);
    e != null &&
      !t.qa({ title: r.title, url: e }) &&
      n.push({
        hiddenRouteUrls: _z(r.hiddenRouteUrls),
        lastOpenedAt: r.lastOpenedAt,
        lastRunningAt: r.lastRunningAt,
        lastSeenAt: r.lastSeenAt,
        previewImageDataUrl: r.previewImageDataUrl,
        routes: vz(r.routes),
        source: `recent`,
        title: r.title,
        url: e,
      });
  }
  return n.slice(0, qL);
}
function LR(e) {
  let n = [];
  for (let r of cz(e).slice(0, qL))
    t.qa({ title: r.title, url: r.url }) ||
      n.push({
        hiddenRouteUrls: r.hiddenRouteUrls,
        lastOpenedAt: r.lastOpenedAt,
        lastRunningAt: r.lastRunningAt,
        lastSeenAt: r.lastSeenAt,
        previewImageDataUrl: r.previewImageDataUrl,
        routes: r.routes,
        title: r.title,
        url: r.url,
      });
  return n;
}
async function RR(e, { hiddenServers: t, servers: n }) {
  let r = { hiddenServers: t, version: 1, servers: n };
  await (0, p.mkdir)((0, s.dirname)(e), { recursive: !0 });
  let i = (0, s.join)(
    (0, s.dirname)(e),
    `.${(0, s.basename)(e)}.tmp-${Date.now()}-${(0, u.randomUUID)()}`,
  );
  try {
    (await (0, p.writeFile)(i, `${JSON.stringify(r)}\n`, `utf8`), await (0, p.rename)(i, e));
  } catch (e) {
    throw (await (0, p.rm)(i, { force: !0 }), e);
  }
}
function zR(e, t, n, r, i) {
  let a = new Map();
  for (let o of e) {
    let e = Uz(o);
    e != null &&
      UR(a, {
        refreshLastSeenAt: i,
        routes:
          Kz(o, e) && !qz(o) ? [{ lastOpenedAt: null, lastSeenAt: t, source: r, url: o }] : [],
        source: n,
        url: e,
      });
  }
  return Array.from(a.values());
}
function BR(e, n) {
  let r = [];
  for (let i of cz(e)) {
    let e = Yz(i.url);
    e != null &&
      !n.has(e) &&
      !t.qa({ title: i.title, url: i.url }) &&
      r.push({ refreshLastSeenAt: !1, routes: i.routes, source: `recent`, url: i.url });
  }
  return r;
}
function VR() {
  let e = new Map();
  for (let t of KL)
    UR(e, {
      refreshLastSeenAt: !1,
      routes: [],
      source: `port-scan`,
      url: `http://localhost:${t}/`,
    });
  return Array.from(e.values());
}
function HR(...e) {
  let t = new Map();
  for (let n of e) for (let e of n) UR(t, e);
  return Array.from(t.values());
}
function UR(e, n) {
  let r = Yz(n.url);
  if (r == null || t.qa({ title: null, url: n.url })) return;
  let i = e.get(r);
  if (i != null) {
    e.set(r, {
      ...i,
      refreshLastSeenAt: i.refreshLastSeenAt || n.refreshLastSeenAt,
      routes: hz(i.routes, n.routes),
      source: bz(i.source, n.source),
    });
    return;
  }
  e.set(r, n);
}
async function WR(e, n) {
  return (
    await Promise.all(
      e.map(async (e) => {
        let r = await Ez(e.url);
        return r == null || Bz(r) || t.qa({ title: r.title, url: r.url })
          ? null
          : ZR(e, r, null, n);
      }),
    )
  ).filter(YR);
}
async function GR(e, n) {
  let r = Date.now();
  return (
    await Promise.all(
      e.map(async (e) => {
        let i = await Ez(e.url),
          a = lz(n, e.url);
        return i != null && (Bz(i) || t.qa({ title: i.title, url: i.url }))
          ? null
          : i == null
            ? a == null
              ? null
              : QR(e, a, r)
            : qR(ZR(e, i, a, r));
      }),
    )
  ).filter(YR);
}
function KR(e) {
  return Promise.all(e.map(qR));
}
async function qR(e) {
  return e.status === `running` ? { ...e, routes: await JR(e.routes) } : e;
}
async function JR(e) {
  return (await Promise.all(e.map(async (e) => ((await Pz(e.url)) ? e : null)))).filter(XR);
}
function YR(e) {
  return e != null;
}
function XR(e) {
  return e != null;
}
function ZR(e, t, n, r) {
  let i = t.title ?? null;
  return {
    lastOpenedAt: n?.lastOpenedAt ?? null,
    lastRunningAt: r,
    lastSeenAt: r,
    previewImageDataUrl: OR({ previewTheme: t.previewTheme, title: i, url: t.url }),
    routes: gz(hz(e.routes, n?.routes ?? []), n?.hiddenRouteUrls ?? []),
    source: e.source,
    status: `running`,
    title: i,
    url: t.url,
  };
}
function QR(e, t, n) {
  let r = t?.url ?? e.url,
    i = t?.title ?? null;
  return {
    lastOpenedAt: t?.lastOpenedAt ?? null,
    lastRunningAt: t?.lastRunningAt ?? null,
    lastSeenAt: e.refreshLastSeenAt ? n : (t?.lastSeenAt ?? n),
    previewImageDataUrl: t?.previewImageDataUrl ?? OR({ title: i, url: r }),
    routes: gz(hz(e.routes, t?.routes ?? []), t?.hiddenRouteUrls ?? []),
    source: e.source,
    status: `offline`,
    title: i,
    url: r,
  };
}
function $R(e) {
  let t = new Map();
  for (let n of e) {
    let e = Yz(n.url);
    if (e == null) continue;
    let r = t.get(e);
    if (r == null) {
      t.set(e, n);
      continue;
    }
    if (!(n.lastSeenAt < r.lastSeenAt)) {
      if (n.lastSeenAt > r.lastSeenAt) {
        t.set(e, n);
        continue;
      }
      if (iz(r) && !iz(n)) {
        r.status === `offline` && n.status === `running` && t.set(e, ez(r, n));
        continue;
      }
      if (!iz(r) && iz(n)) {
        t.set(e, n);
        continue;
      }
      r.status === `offline` && n.status === `running` && t.set(e, ez(r, n));
    }
  }
  return Array.from(t.values());
}
function ez(e, t) {
  return {
    ...e,
    lastRunningAt: t.lastRunningAt,
    lastSeenAt: Math.max(e.lastSeenAt, t.lastSeenAt),
    previewImageDataUrl: e.previewImageDataUrl ?? t.previewImageDataUrl,
    routes: hz(e.routes, t.routes),
    status: `running`,
    title: e.title ?? t.title,
  };
}
function tz(e, t) {
  return e.map((e) => {
    let n = lz(t, e.url);
    return n == null
      ? e
      : {
          ...e,
          lastOpenedAt: n.lastOpenedAt,
          previewImageDataUrl: e.previewImageDataUrl ?? n.previewImageDataUrl,
          routes: gz(hz(e.routes, n.routes), n.hiddenRouteUrls),
          source: `recent`,
          title: e.title ?? n.title,
        };
  });
}
function nz(e) {
  return [...e].sort((e, t) => {
    let n = rz(e) - rz(t);
    if (n !== 0) return n;
    let r = az(e) - az(t);
    return r === 0 ? oz(t) - oz(e) : r;
  });
}
function rz(e) {
  return iz(e)
    ? e.status === `running`
      ? 0
      : 1
    : e.source === `recent`
      ? e.status === `running`
        ? 2
        : 3
      : 4;
}
function iz(e) {
  return e.source === `agent-history` || e.routes.some((e) => e.source === `agent-history`);
}
function az(e) {
  return e.lastOpenedAt == null ? 1 : 0;
}
function oz(e) {
  return e.lastOpenedAt ?? e.lastSeenAt;
}
function sz(e) {
  return e.lastOpenedAt ?? e.lastSeenAt;
}
function cz(e) {
  return Array.from(e.values())
    .filter((e) => e.lastRunningAt != null)
    .sort((e, t) => sz(t) - sz(e));
}
function lz(e, t) {
  let n = Yz(t);
  if (n == null) return null;
  let r = e.get(n);
  return r?.lastRunningAt == null ? null : r;
}
function uz(e) {
  let t = new Set();
  for (let n of e) {
    let e = Yz(n.url);
    e != null && t.add(e);
  }
  return t;
}
function dz(e, t) {
  for (let n of t) {
    let t = Yz(n.url);
    t != null && e.add(t);
  }
}
async function fz({ hiddenServers: e, portScanServers: t }) {
  let n = new Map();
  for (let r of t) {
    let t = Yz(r.url);
    t != null && e.has(t) && n.set(t, r);
  }
  let r = await GR(BR(e, new Set()), e);
  for (let e of r) {
    let t = Yz(e.url);
    t != null && n.set(t, e);
  }
  for (let [t, r] of e) n.has(t) || n.set(t, mz(r));
  return nz(Array.from(n.values()));
}
function pz(e, t = []) {
  return {
    hiddenRouteUrls: t,
    lastOpenedAt: e.lastOpenedAt,
    lastRunningAt: e.lastRunningAt,
    lastSeenAt: e.lastSeenAt,
    previewImageDataUrl: e.previewImageDataUrl,
    routes: e.routes,
    source: e.source,
    title: e.title,
    url: e.url,
  };
}
function mz(e) {
  return {
    lastOpenedAt: e.lastOpenedAt,
    lastRunningAt: e.lastRunningAt,
    lastSeenAt: e.lastSeenAt,
    previewImageDataUrl: e.previewImageDataUrl ?? OR({ title: e.title, url: e.url }),
    routes: gz(e.routes, e.hiddenRouteUrls),
    source: e.source,
    status: `offline`,
    title: e.title,
    url: e.url,
  };
}
function hz(...e) {
  let t = new Map();
  for (let n of e)
    for (let e of n) {
      let n = Wz(e.url),
        r = n == null ? null : Uz(n);
      if (n == null || r == null || !Kz(n, r) || qz(n)) continue;
      let i = t.get(n);
      if (i == null) {
        t.set(n, { ...e, url: n });
        continue;
      }
      t.set(n, {
        lastOpenedAt: xz(i.lastOpenedAt, e.lastOpenedAt),
        lastSeenAt: Math.max(i.lastSeenAt, e.lastSeenAt),
        source: yz(i.source, e.source),
        url: n,
      });
    }
  return Array.from(t.values())
    .sort((e, t) => Sz(t) - Sz(e))
    .slice(0, JL);
}
function gz(e, t) {
  let n = new Set(_z(t));
  return n.size === 0
    ? e
    : e.filter((e) => {
        let t = Wz(e.url);
        return t == null || !n.has(t);
      });
}
function _z(...e) {
  let t = new Set();
  for (let n of e)
    for (let e of n) {
      let n = Wz(e),
        r = n == null ? null : Uz(n);
      n != null && r != null && Kz(n, r) && !qz(n) && t.add(n);
    }
  return Array.from(t).slice(0, JL);
}
function vz(e) {
  return hz(
    e.map((e) => ({
      lastOpenedAt: e.lastOpenedAt,
      lastSeenAt: e.lastSeenAt,
      source: `navigation`,
      url: e.url,
    })),
  );
}
function yz(e, t) {
  return e === `agent-history` || t === `agent-history` ? `agent-history` : `navigation`;
}
function bz(e, t) {
  return e === `agent-history` || t === `agent-history`
    ? `agent-history`
    : e === `recent` || t === `recent`
      ? `recent`
      : `port-scan`;
}
function xz(e, t) {
  return e == null ? t : t == null ? e : Math.max(e, t);
}
function Sz(e) {
  return e.lastOpenedAt ?? e.lastSeenAt;
}
function Cz(e, { rolloutFileVersion: t }) {
  if (e == null) return !0;
  if (e.inflight != null) return !1;
  if (e.rolloutFileVersion !== t) return !0;
  let n = e.state.refreshedAt;
  return n == null || Date.now() - n > UL;
}
function wz() {
  return { hiddenServers: [], isLoading: !1, refreshedAt: Date.now(), servers: [] };
}
function Tz(e) {
  let n = e.servers.filter((e) => (e.status === `running` || e.lastRunningAt != null) && !t.qa(e)),
    r = e.hiddenServers.filter(
      (e) => (e.status === `running` || e.lastRunningAt != null) && !t.qa(e),
    );
  return n.length === e.servers.length && r.length === e.hiddenServers.length
    ? e
    : { ...e, hiddenServers: r, servers: n };
}
function Ez(e, t = 0) {
  return new Promise((n) => {
    let r = !1,
      i = (e) => {
        r || ((r = !0), n(e));
      },
      a = new URL(e),
      o = Nz(a.protocol)(
        a,
        {
          headers: {
            Accept: `text/html,application/xhtml+xml,*/*;q=0.1`,
            "User-Agent": `Codex local server discovery`,
          },
        },
        (e) => {
          let n = Fz(e, a);
          if (n != null && t < 2) {
            (e.resume(), Ez(n, t + 1).then(i, () => i(null)));
            return;
          }
          if (!zz(e.headers[`content-type`]).toLowerCase().includes(`html`)) {
            (e.resume(),
              i({
                previewTheme: null,
                serverHeader: zz(e.headers.server),
                title: null,
                url: a.toString(),
              }));
            return;
          }
          Iz(e, a.toString(), zz(e.headers.server), i);
        },
      );
    (o.setTimeout(HL, () => {
      (o.destroy(), i(null));
    }),
      o.on(`error`, () => i(null)),
      o.end());
  });
}
async function Dz(e, t, n) {
  return { previewTheme: _R(e, await Oz(e, n)), serverHeader: t, title: gR(e), url: n };
}
function Oz(e, t) {
  let n = kz(e, t);
  return n.length === 0 ? Promise.resolve(QL) : Promise.all(n.map(jz)).then((e) => e.filter(Mz));
}
function kz(e, t) {
  let n = new URL(t),
    r = new Set();
  for (let t of e.matchAll(/<link\b[^>]*>/gi)) {
    let e = t[0],
      i = wR(e, `rel`),
      a = wR(e, `href`);
    if (i?.toLowerCase().split(/\s+/).includes(`stylesheet`) && a != null) {
      let e = Az(a, n);
      e != null && r.add(e);
    }
  }
  return Array.from(r).slice(0, XL);
}
function Az(e, t) {
  try {
    let n = new URL(e, t);
    return n.protocol !== t.protocol || n.host !== t.host || !Xz(n.hostname) ? null : n.toString();
  } catch {
    return null;
  }
}
function jz(e) {
  return new Promise((t) => {
    let n = !1,
      r = (e) => {
        n || ((n = !0), t(e));
      },
      i = new URL(e),
      a = Nz(i.protocol)(
        i,
        { headers: { Accept: `text/css,*/*;q=0.1`, "User-Agent": `Codex local server discovery` } },
        (e) => {
          Lz(e, YL, r);
        },
      );
    (a.setTimeout(ZL, () => {
      (a.destroy(), r(null));
    }),
      a.on(`error`, () => r(null)),
      a.end());
  });
}
function Mz(e) {
  return e != null;
}
function Nz(e) {
  return e === `https:` ? f.request : d.request;
}
function Pz(e, t = 0) {
  return new Promise((n) => {
    let r = !1,
      i = (e) => {
        r || ((r = !0), n(e));
      },
      a = new URL(e),
      o = Nz(a.protocol)(
        a,
        {
          headers: {
            Accept: `text/html,application/xhtml+xml,*/*;q=0.1`,
            "User-Agent": `Codex local server discovery`,
          },
        },
        (e) => {
          let n = Fz(e, a);
          if ((e.resume(), n != null && t < 2)) {
            Pz(n, t + 1).then(i, () => i(!1));
            return;
          }
          let r = e.statusCode ?? 0;
          i(r >= 200 && r < 300);
        },
      );
    (o.setTimeout(HL, () => {
      (o.destroy(), i(!1));
    }),
      o.on(`error`, () => i(!1)),
      o.end());
  });
}
function Fz(e, t) {
  let n = e.statusCode ?? 0,
    r = zz(e.headers.location);
  if (n < 300 || n >= 400 || r.length === 0) return null;
  try {
    let e = new URL(r, t);
    return Xz(e.hostname) ? e.toString() : null;
  } catch {
    return null;
  }
}
function Iz(e, t, n, r) {
  Lz(e, WL, (e) => {
    if (e == null) {
      r(null);
      return;
    }
    Rz(e, n, t, r);
  });
}
function Lz(e, t, n) {
  let r = [],
    i = 0,
    a = !1,
    o = () => (r.length === 0 ? null : Buffer.concat(r).toString(`utf8`));
  (e.on(`data`, (n) => {
    let a = typeof n == `string` ? Buffer.from(n) : n,
      o = t - i;
    if (o <= 0) {
      e.destroy();
      return;
    }
    let s = a.subarray(0, o);
    (r.push(s), (i += s.length), i >= t && e.destroy());
  }),
    e.on(`end`, () => {
      ((a = !0), n(Buffer.concat(r).toString(`utf8`)));
    }),
    e.on(`error`, () => n(o())),
    e.on(`close`, () => {
      a || n(o());
    }));
}
function Rz(e, t, n, r) {
  Dz(e, t, n).then(r, () => r({ previewTheme: _R(e), serverHeader: t, title: gR(e), url: n }));
}
function zz(e) {
  return e == null ? `` : Array.isArray(e) ? (e[0] ?? ``) : e;
}
function Bz(e) {
  try {
    let t = new URL(e.url),
      n = Jz(t);
    return (
      Xz(t.hostname) &&
      n != null &&
      $L.has(n) &&
      e.serverHeader.trim().toLowerCase().startsWith(`airtunes/`)
    );
  } catch {
    return !1;
  }
}
function Vz(e, t, n) {
  n.lastIndex = 0;
  for (let r of t.matchAll(n)) {
    let t = Wz(r[0]);
    t != null && e.add(t);
  }
}
function Hz(e, t) {
  nR.lastIndex = 0;
  for (let n of t.matchAll(nR)) {
    let t = Number(n[1]);
    if (!Number.isInteger(t) || t < 1 || t > 65535) continue;
    let r = Wz(`localhost:${t}`);
    r != null && e.add(r);
  }
}
function Uz(e) {
  let n = Wz(e);
  if (n == null) return null;
  try {
    let e = new URL(n);
    ((e.pathname = `/`), (e.search = ``), (e.hash = ``));
    let r = e.toString();
    return t.qa({ title: null, url: r }) ? null : r;
  } catch {
    return null;
  }
}
function Wz(e) {
  let n = e.replace(/\\\//g, `/`),
    r = n.indexOf(`](`);
  for (r >= 0 && (n = n.slice(0, r)); /[),.;\]}`]+$/.test(n); ) n = n.slice(0, -1);
  !n.startsWith(`http://`) && !n.startsWith(`https://`) && (n = `http://${n}`);
  try {
    let e = new URL(n);
    if (!Xz(e.hostname) || ((e.hostname = `localhost`), Jz(e) == null)) return null;
    e.pathname = Gz(e.pathname);
    let r = e.toString();
    return t.qa({ title: null, url: r }) ? null : r;
  } catch {
    return null;
  }
}
function Gz(e) {
  let t = e.replace(/\/+/g, `/`);
  return t.length === 0 || t === `/` ? `/` : t.replace(/\/+$/g, ``);
}
function Kz(e, t) {
  let n = Uz(e);
  if (n == null || n !== t) return !1;
  try {
    let n = new URL(e),
      r = new URL(t);
    return n.pathname !== r.pathname || n.search.length > 0 || n.hash.length > 0;
  } catch {
    return !1;
  }
}
function qz(e) {
  try {
    return rR.test(new URL(e).pathname);
  } catch {
    return !1;
  }
}
function Jz(e) {
  let t = e.port.length > 0 ? e.port : e.protocol === `http:` ? `80` : `443`,
    n = Number(t);
  return !Number.isInteger(n) || n < 1 || n > 65535 ? null : n;
}
function Yz(e) {
  try {
    let t = new URL(e),
      n = Jz(t);
    if (n == null) return null;
    let r = Xz(t.hostname) ? `localhost` : t.hostname;
    return `${t.protocol}//${r}:${n}`;
  } catch {
    return null;
  }
}
function Xz(e) {
  return e === `localhost` || e === `127.0.0.1` || e === `0.0.0.0` || e === `[::1]` || e === `::1`;
}
function Zz(e) {
  try {
    let t = new URL(e);
    return `${t.hostname}:${Jz(t) ?? ``}`;
  } catch {
    return e;
  }
}
function Qz(e) {
  return e
    .replace(/&amp;/g, `&`)
    .replace(/&lt;/g, `<`)
    .replace(/&gt;/g, `>`)
    .replace(/&quot;/g, `"`)
    .replace(/&nbsp;/g, ` `)
    .replace(/&#39;/g, `'`);
}
function $z(e) {
  return e
    .replace(/&/g, `&amp;`)
    .replace(/</g, `&lt;`)
    .replace(/>/g, `&gt;`)
    .replace(/"/g, `&quot;`);
}
function eB() {
  return [
    ...tB(3e3, 3020),
    ...tB(4e3, 4010),
    ...tB(5e3, 5010),
    ...tB(5173, 5180),
    ...tB(6006, 6010),
    ...tB(7e3, 7010),
    ...tB(8e3, 8010),
    ...tB(8080, 8090),
    ...tB(9e3, 9010),
    1111,
    1234,
    1313,
    1420,
    24678,
  ];
}
function tB(e, t) {
  return Array.from({ length: t - e + 1 }, (t, n) => e + n);
}
function nB(e) {
  return e ?? ``;
}
function rB(e) {
  if (e == null || !(0, s.isAbsolute)(e)) return null;
  try {
    let t = (0, l.statSync)(e);
    return `${t.mtimeMs}:${t.size}`;
  } catch {
    return null;
  }
}
var iB = 5e3,
  aB = class {
    getWindowStates;
    localServerDiscovery;
    windowManager;
    latestRefreshIdsByRolloutPath = new Map();
    visibleRefreshTimeout = null;
    constructor({
      getWindowStates: e,
      localServerDiscovery: t,
      persistencePath: n = null,
      windowManager: r,
    }) {
      ((this.getWindowStates = e),
        (this.localServerDiscovery = t ?? new mR({ persistencePath: n })),
        (this.windowManager = r));
    }
    recordOpened(e) {
      let t = this.localServerDiscovery.getLocalServerRouteUrl(e),
        n = t == null ? [] : this.getTargets({ includeWeb: !0 });
      this.localServerDiscovery.recordOpened(e).then((e) => {
        if (t != null) {
          this.refreshTargets(n, {
            force: !0,
            forcePortScan: !1,
            includePortScan: !1,
            navigationRouteUrls: [t],
            revalidateKnownServers: !0,
          });
          return;
        }
        e && this.broadcastCachedStatesToVisibleEmptyPages();
      });
    }
    hideServer(e) {
      this.localServerDiscovery.hideServer(e) && this.broadcastCachedStatesToVisibleEmptyPages();
    }
    unhideServer(e) {
      this.localServerDiscovery.unhideServer(e) && this.broadcastCachedStatesToVisibleEmptyPages();
    }
    removeRoute(e, t) {
      this.localServerDiscovery.removeRoute(e, t) &&
        this.broadcastCachedStatesToVisibleEmptyPages();
    }
    sync(e, t, n) {
      if (n.allowLocalServerPortScan) {
        this.refreshTargets([{ conversationId: t, threadState: n, windowState: e }], {
          force: !0,
          forcePortScan: !0,
          includePortScan: !0,
        });
        return;
      }
      if (this.localServerDiscovery.shouldRefresh(n.rolloutPath)) {
        this.refreshVisibleEmptyPages();
        return;
      }
      (this.sendState(e.owner, t, this.localServerDiscovery.getCachedState(n.rolloutPath) ?? pR),
        this.scheduleVisibleRefresh());
    }
    async refreshVisibleEmptyPages({
      force: e = !1,
      forcePortScan: t = !1,
      includePortScan: n = !1,
      navigationRouteUrls: r = [],
      revalidateKnownServers: i = !1,
      showLoading: a = !0,
    } = {}) {
      await this.refreshTargets(this.getTargets(), {
        force: e,
        forcePortScan: t,
        includePortScan: n,
        navigationRouteUrls: r,
        revalidateKnownServers: i,
        showLoading: a,
      });
    }
    async refreshTargets(
      e,
      {
        force: t = !1,
        forcePortScan: n = !1,
        includePortScan: r = !1,
        navigationRouteUrls: i = [],
        revalidateKnownServers: a = !1,
        showLoading: o = !0,
      } = {},
    ) {
      if (o)
        for (let t of e) {
          if (!this.canSendLocalServersState(t)) continue;
          let e = this.localServerDiscovery.getCachedState(t.threadState.rolloutPath);
          this.sendState(
            t.windowState.owner,
            t.conversationId,
            e == null ? pR : { ...e, isLoading: !0 },
          );
        }
      let s = new Map(),
        c = new Map(),
        l = await Promise.all(
          e.map(async (e) => {
            let o = e.threadState.rolloutPath ?? ``,
              l = s.get(o),
              u = c.get(o);
            return (
              (l == null || u == null) &&
                ((l = Symbol()),
                (u = this.localServerDiscovery.refresh(e.threadState.rolloutPath, {
                  force: t,
                  forcePortScan: n,
                  includePortScan: r,
                  navigationRouteUrls: i,
                  revalidateKnownServers: a,
                })),
                s.set(o, l),
                c.set(o, u),
                this.latestRefreshIdsByRolloutPath.set(o, l)),
              { refreshId: l, rolloutPathKey: o, state: await u, target: e }
            );
          }),
        );
      for (let { refreshId: e, rolloutPathKey: t, state: n, target: r } of l)
        this.latestRefreshIdsByRolloutPath.get(t) === e &&
          this.canSendLocalServersState(r) &&
          this.sendState(r.windowState.owner, r.conversationId, n);
      this.scheduleVisibleRefresh();
    }
    getTargets({ includeWeb: e = !1 } = {}) {
      let n = [];
      for (let r of this.getWindowStates())
        if (!r.owner.isDestroyed())
          for (let i of r.threads.values())
            i.visible &&
              (e || i.snapshot.tabType !== t.Jo.WEB) &&
              n.push({ conversationId: i.conversationId, threadState: i, windowState: r });
      return n;
    }
    canSendLocalServersState(e) {
      let n =
        e.windowState.threads.get(`${e.conversationId}\0${e.threadState.browserTabId}`) ??
        [...e.windowState.threads.values()].find((t) => t === e.threadState);
      return n === e.threadState && n.visible && n.snapshot.tabType !== t.Jo.WEB;
    }
    scheduleVisibleRefresh() {
      this.getTargets().length !== 0 &&
        (this.visibleRefreshTimeout ??
          ((this.visibleRefreshTimeout = setTimeout(() => {
            ((this.visibleRefreshTimeout = null),
              this.refreshVisibleEmptyPages({
                force: !0,
                forcePortScan: !1,
                includePortScan: !1,
                revalidateKnownServers: !0,
                showLoading: !1,
              }));
          }, iB)),
          this.visibleRefreshTimeout.unref()));
    }
    broadcastCachedStatesToVisibleEmptyPages() {
      for (let e of this.getTargets()) {
        let t = this.localServerDiscovery.getCachedState(e.threadState.rolloutPath);
        t != null && this.sendState(e.windowState.owner, e.conversationId, t);
      }
    }
    sendState(e, t, n) {
      e.isDestroyed() ||
        this.windowManager.sendMessageToWebContents(e, {
          type: `browser-sidebar-local-servers`,
          conversationId: t,
          state: n,
        });
    }
  },
  oB = e.n((e, n) => {
    n.exports = t.Ht()({ "&": `&amp;`, "<": `&lt;`, ">": `&gt;`, '"': `&quot;`, "'": `&#39;` });
  }),
  sB = e.n((e, n) => {
    var r = oB(),
      i = t.Vt(),
      a = /[&<>"']/g,
      o = RegExp(a.source);
    function s(e) {
      return ((e = i(e)), e && o.test(e) ? e.replace(a, r) : e);
    }
    n.exports = s;
  }),
  cB = e.o(sB()),
  lB = `browserSidebar.contextMenu.inspect`,
  uB = `browserSidebar.contextMenu.back`,
  dB = `browserSidebar.contextMenu.forward`,
  fB = `browserSidebar.contextMenu.reload`,
  pB = `browserSidebar.contextMenu.openInBrowser`,
  mB = `browserSidebar.contextMenu.openExternalBrowser`,
  hB = `browserSidebar.contextMenu.copyLink`,
  gB = `browserSidebar.contextMenu.commentWithCodex`,
  _B = `browserSidebar.contextMenu.attachImage`,
  vB = `browserSidebar.loadError.heading`,
  yB = `This site can't be reached`,
  bB = `browserSidebar.loadError.try`,
  xB = `Try:`,
  SB = `browserSidebar.loadError.checkConnection`,
  CB = `Checking the connection`,
  wB = `browserSidebar.loadError.checkProxyFirewallDns`,
  TB = `Checking the proxy, firewall, and DNS configuration`,
  EB = `browserSidebar.loadError.reload`,
  DB = `Reload`,
  OB = `browserSidebar.crashError.heading`,
  kB = `This page crashed`,
  AB = `browserSidebar.crashError.summary`,
  jB = `{host} crashed unexpectedly`,
  MB = `browserSidebar.crashError.openExternalBrowser`,
  NB = `Open in external browser`,
  PB = `browserSidebar.loadError.dnsSummary`,
  FB = `{host}'s server IP address could not be found`,
  IB = `browserSidebar.loadError.offlineSummary`,
  LB = `{host} could not be loaded because the computer is offline`,
  RB = `browserSidebar.loadError.refusedSummary`,
  zB = `{host} refused to connect`,
  BB = `browserSidebar.loadError.timeoutSummary`,
  VB = `{host} took too long to respond`,
  HB = `browserSidebar.loadError.certificateSummary`,
  UB = `{host}'s certificate could not be verified`,
  WB = `browserSidebar.loadError.genericSummary`,
  GB = `{host} could not be loaded`,
  KB = `browserSidebar.loadError.internetHeader`,
  qB = `Check your Internet connection`,
  JB = `browserSidebar.loadError.internetBody`,
  YB = `Check any cables and restart any routers, modems, or other network devices you may be using`,
  XB = `browserSidebar.loadError.dnsHeader`,
  ZB = `Check your DNS settings`,
  QB = `browserSidebar.loadError.dnsBody`,
  $B = `Contact your network administrator if you are not sure what this means`,
  eV = `browserSidebar.loadError.networkAccessHeader`,
  tV = `Allow Codex to access the network in your firewall or security settings`,
  nV = `browserSidebar.loadError.networkAccessBody`,
  rV = `If Codex is already listed as an allowed app, try removing it from the list and adding it again`,
  iV = `browserSidebar.loadError.proxyHeader`,
  aV = `If you use a proxy server`,
  oV = `browserSidebar.loadError.proxyBody`,
  sV = `Open your system network settings and check whether a proxy has been configured for the active network`,
  cV = `data:text/html;charset=utf-8,`,
  lV = `ERR_PROCESS_CRASHED`,
  uV = `codex-browser-sidebar://open-external`,
  dV = `data:image/png;base64,`,
  fV = `data:image/png;base64,`;
function pV({ content: e, errorCode: t, errorDescription: n, failedUrl: r }) {
  let i = DV(n);
  return {
    errorCode: t,
    errorDescription: i,
    failedUrl: r,
    pageUrl: cV + encodeURIComponent(xV({ errorCodeLabel: EV(t, i), content: e, failedUrl: r })),
    title: e.heading,
  };
}
function mV({ errorDescription: e, failedUrl: t, formatMessage: n, runtimeIntlConfig: r }) {
  let i = DV(e),
    a = SV(t),
    o = wV(r);
  return {
    alignActionsEnd: !1,
    details: [
      {
        header: n({ messageId: KB, defaultMessage: qB }),
        body: n({ messageId: JB, defaultMessage: YB }),
      },
      {
        header: n({ messageId: XB, defaultMessage: ZB }),
        body: n({ messageId: QB, defaultMessage: $B }),
      },
      {
        header: n({ messageId: eV, defaultMessage: tV }),
        body: n({ messageId: nV, defaultMessage: rV }),
      },
      {
        header: n({ messageId: iV, defaultMessage: aV }),
        body: n({ messageId: oV, defaultMessage: sV }),
      },
    ],
    externalBrowserButton: null,
    heading: n({ messageId: vB, defaultMessage: yB }),
    language: o,
    reloadButton: n({ messageId: EB, defaultMessage: DB }),
    showErrorCode: !0,
    suggestions: {
      checkConnection: n({ messageId: SB, defaultMessage: CB }),
      detailsLinkLabel: n({ messageId: wB, defaultMessage: TB }),
      tryLabel: n({ messageId: bB, defaultMessage: xB }),
    },
    summary: n({ ...CV(i), values: { host: a } }),
    textDirection: TV(o),
  };
}
function hV({ failedUrl: e, formatMessage: t, runtimeIntlConfig: n }) {
  let r = SV(e),
    i = wV(n);
  return pV({
    content: {
      alignActionsEnd: !0,
      details: [],
      externalBrowserButton: bb(e)
        ? {
            actionUrl: `${uV}?url=${encodeURIComponent(e)}`,
            label: t({ messageId: MB, defaultMessage: NB }),
          }
        : null,
      heading: t({ messageId: OB, defaultMessage: kB }),
      language: i,
      reloadButton: t({ messageId: EB, defaultMessage: DB }),
      showErrorCode: !1,
      suggestions: null,
      summary: t({ messageId: AB, defaultMessage: jB, values: { host: r } }),
      textDirection: TV(i),
    },
    errorCode: 0,
    errorDescription: lV,
    failedUrl: e,
  });
}
function gV({ errorCode: e, errorDescription: t, isMainFrame: n }) {
  let r = DV(t);
  return !n || e === -3 || r === `ERR_ABORTED` || r === `ERR_BLOCKED_BY_CLIENT`;
}
function _V(e) {
  return e instanceof Error && /ERR_ABORTED|\(-3\) loading '/.test(e.message);
}
function vV(e) {
  return e instanceof Error && e.message.includes(`navigation is already pending`);
}
function yV(e, t) {
  return e != null && t === e.pageUrl;
}
function bV(e) {
  try {
    let t = new URL(e);
    return t.protocol !== `codex-browser-sidebar:` || t.hostname !== `open-external`
      ? null
      : t.searchParams.get(`url`);
  } catch {
    return null;
  }
}
function xV({ content: e, errorCodeLabel: t, failedUrl: n }) {
  let r = e.details.map(
      (e) => `      <div class="suggestions">
        <div class="suggestion-header">${(0, cB.default)(e.header)}</div>
        <div class="suggestion-body">${(0, cB.default)(e.body)}</div>
      </div>`,
    ).join(`
`),
    i =
      e.suggestions?.detailsLinkLabel == null
        ? ``
        : `            <li><a id="details-link" href="#details" aria-expanded="false">${(0, cB.default)(e.suggestions.detailsLinkLabel)}</a></li>`,
    a =
      e.externalBrowserButton == null
        ? ``
        : `      <button id="open-in-browser-button" type="button">${(0, cB.default)(e.externalBrowserButton.label)}</button>`,
    o = e.alignActionsEnd ? `nav-wrapper nav-wrapper-end` : `nav-wrapper`,
    s = e.showErrorCode ? `        <div class="error-code">${(0, cB.default)(t)}</div>` : ``,
    c =
      e.suggestions == null
        ? ``
        : `        <div id="suggestions-list">
          <p>${(0, cB.default)(e.suggestions.tryLabel)}</p>
          <ul>
            <li>${(0, cB.default)(e.suggestions.checkConnection)}</li>
${i}
          </ul>
        </div>`;
  return `<!DOCTYPE html>
<html dir="${(0, cB.default)(e.textDirection)}" lang="${(0, cB.default)(e.language)}">
<head>
  <meta charset="utf-8">
  <meta name="color-scheme" content="light dark">
  <meta name="theme-color" content="#fff">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>${(0, cB.default)(e.heading)}</title>
  <style>
    body {
      --background-color: #fff;
      --error-code-color: rgb(95, 99, 104);
      --google-blue-300: rgb(138, 180, 248);
      --google-gray-300: rgb(218, 220, 224);
      --google-gray-500: rgb(154, 160, 166);
      --google-gray-700: rgb(95, 99, 104);
      --google-gray-900: rgb(32, 33, 36);
      --heading-color: var(--google-gray-900);
      --link-color: rgb(88, 88, 88);
      --button-fill-color: rgb(26 28 31 / 5%);
      --button-fill-color-active: rgb(26 28 31 / 10%);
      --button-text-color: #1a1c1f;
      --text-color: var(--google-gray-700);
      background: var(--background-color);
      color: var(--text-color);
      font-family: system-ui, sans-serif;
      font-size: 70%;
      margin: 0;
      overflow-wrap: break-word;
    }

    @media (prefers-color-scheme: dark) {
      body {
        --background-color: var(--google-gray-900);
        --error-code-color: var(--google-gray-500);
        --heading-color: var(--google-gray-500);
        --link-color: var(--google-blue-300);
        --button-fill-color: rgb(255 255 255 / 5%);
        --button-fill-color-active: rgb(255 255 255 / 10%);
        --button-text-color: #fff;
        --text-color: var(--google-gray-500);
      }
    }

    html {
      -webkit-text-size-adjust: 100%;
      font-size: 125%;
    }

    a {
      color: var(--link-color);
      text-decoration: none;
    }

    button {
      align-items: center;
      background: var(--button-fill-color);
      border: 1px solid transparent;
      border-radius: 8px;
      box-sizing: border-box;
      color: var(--button-text-color);
      cursor: pointer;
      display: inline-flex;
      font-family: inherit;
      font-size: 13px;
      font-weight: 500;
      justify-content: center;
      line-height: 18px;
      margin: 0;
      min-height: 28px;
      padding: 0 8px;
      user-select: none;
      white-space: nowrap;
    }

    button:active {
      background: var(--button-fill-color-active);
      outline: 0;
    }

    button:hover {
      background: var(--button-fill-color-active);
      outline: 0;
    }

    .interstitial-wrapper {
      box-sizing: border-box;
      font-size: 1em;
      line-height: 1.6em;
      margin: 20vh auto 0;
      max-width: 600px;
      width: 100%;
    }

    .icon {
      -webkit-user-select: none;
      animation: icon-spin 3s linear infinite;
      background-image: image-set(url("${dV}") 1x, url("${fV}") 2x);
      background-repeat: no-repeat;
      background-size: 26px 28px;
      display: inline-block;
      height: 28px;
      margin: 0 0 24px;
      transform-origin: 50% 50%;
      width: 26px;
    }

    @keyframes icon-spin {
      100% {
        transform: rotate(360deg);
      }
    }

    h1 {
      color: var(--heading-color);
      font-size: 1.2em;
      font-weight: 500;
      line-height: 1.2em;
      margin: 0 0 12px;
    }

    #main-message > p {
      display: inline;
      font-size: 1em;
    }

    #suggestions-list {
      margin-top: 18px;
    }

    #suggestions-list p {
      margin-block-end: 0;
    }

    #suggestions-list ul {
      margin-top: 0;
    }

    .error-code {
      color: var(--error-code-color);
      font-size: .8em;
      margin-top: 24px;
      text-transform: uppercase;
    }

    .nav-wrapper {
      display: flex;
      gap: 8px;
      justify-content: flex-start;
      margin-top: 51px;
    }

    [dir="rtl"] .nav-wrapper {
      justify-content: flex-end;
    }

    .nav-wrapper-end {
      justify-content: flex-end;
    }

    #details {
      display: none;
      margin: 0 0 50px;
    }

    #main-frame-error.showing-details #details {
      display: block;
    }

    .suggestions {
      margin-top: 18px;
    }

    .suggestion-header {
      font-weight: bold;
      margin-bottom: 4px;
    }

    .suggestion-body {
      color: #777;
    }

    @media (prefers-color-scheme: dark) {
      .icon {
        filter: brightness(1.2);
      }

      .suggestion-body {
        color: var(--text-color);
      }
    }

    @media (max-width: 700px) {
      .interstitial-wrapper {
        padding: 0 10%;
      }
    }

    @media (max-width: 420px) {
      .interstitial-wrapper {
        margin: 7vh auto 12px;
        padding: 0 24px;
      }

      .nav-wrapper {
        margin-top: 30px;
      }

      #details {
        margin: 20px 0;
      }

      h1 {
        font-size: 1.5em;
        margin-bottom: 8px;
      }
    }
  </style>
</head>
<body class="neterror">
  <div id="main-frame-error" class="interstitial-wrapper">
    <div id="main-content">
      <div class="icon" aria-hidden="true"></div>
      <div id="main-message">
        <h1>${(0, cB.default)(e.heading)}</h1>
        <p>${(0, cB.default)(e.summary)}</p>
${c}
${s}
      </div>
    </div>
    <div id="buttons" class="${o}">
${a}
      <button id="reload-button" type="button">${(0, cB.default)(e.reloadButton)}</button>
    </div>
    <div id="details">
${r}
    </div>
  </div>
  <script>
    (() => {
      const failedUrl = ${OV(n)};
      const externalBrowserActionUrl = ${OV(e.externalBrowserButton?.actionUrl ?? ``)};
      const main = document.getElementById("main-frame-error");
      const details = document.getElementById("details");
      const detailsLink = document.getElementById("details-link");
      const openInBrowserButton = document.getElementById("open-in-browser-button");
      const reloadButton = document.getElementById("reload-button");
      const setDetailsVisible = (visible) => {
        main.classList.toggle("showing-details", visible);
        detailsLink?.setAttribute("aria-expanded", String(visible));
        if (visible) {
          details.setAttribute("tabindex", "-1");
          details.focus();
        }
      };
      detailsLink?.addEventListener("click", (event) => {
        event.preventDefault();
        setDetailsVisible(!main.classList.contains("showing-details"));
      });
      reloadButton.addEventListener("click", () => {
        window.location.href = failedUrl;
      });
      openInBrowserButton?.addEventListener("click", () => {
        window.location.href = externalBrowserActionUrl;
      });
    })();
  <\/script>
</body>
</html>`;
}
function SV(e) {
  try {
    let t = new URL(e);
    return t.hostname || t.pathname || e;
  } catch {
    return e;
  }
}
function CV(e) {
  switch (e) {
    case `DNS_PROBE_POSSIBLE`:
    case `ERR_NAME_NOT_RESOLVED`:
      return { messageId: PB, defaultMessage: FB };
    case `ERR_INTERNET_DISCONNECTED`:
      return { messageId: IB, defaultMessage: LB };
    case `ERR_CONNECTION_REFUSED`:
      return { messageId: RB, defaultMessage: zB };
    case `ERR_CONNECTION_TIMED_OUT`:
    case `ERR_TIMED_OUT`:
      return { messageId: BB, defaultMessage: VB };
    default:
      return e.startsWith(`ERR_CERT_`)
        ? { messageId: HB, defaultMessage: UB }
        : { messageId: WB, defaultMessage: GB };
  }
}
function wV(e) {
  return e.locale || e.defaultLocale || `en-US`;
}
function TV(e) {
  return Reflect.get(new Intl.Locale(e.replace(/_/g, `-`)), `textInfo`)?.direction === `rtl`
    ? `rtl`
    : `ltr`;
}
function EV(e, t) {
  return t.length > 0 ? t : e === 0 ? `ERR_FAILED` : `ERR_FAILED (` + e + `)`;
}
function DV(e) {
  return e.trim().toUpperCase();
}
function OV(e) {
  return JSON.stringify(e)
    .replace(/</g, `\\u003C`)
    .replace(/>/g, `\\u003E`)
    .replace(/&/g, `\\u0026`)
    .replace(/\u2028/g, `\\u2028`)
    .replace(/\u2029/g, `\\u2029`);
}
function kV() {
  return { activeMatchOrdinal: 0, matches: 0, query: ``, requestId: null };
}
function AV(e, t, n, r) {
  (e.sendMessageToWebContents(t, {
    type: `browser-sidebar-command`,
    conversationId: n,
    browserTabId: r.browserTabId,
    command: { type: `open-find` },
  }),
    FV(e, t, n, r));
}
function jV(e, t, n, r, i) {
  if (i.length === 0) {
    IV(e, t, n, r);
    return;
  }
  if (r.findState.query === i) return;
  ((r.findState = { ...r.findState, activeMatchOrdinal: 0, matches: 0, query: i, requestId: null }),
    FV(e, t, n, r));
  let a = r.page?.view.webContents;
  a == null || a.isDestroyed() || (r.findState.requestId = a.findInPage(i));
}
function MV(e) {
  LV(e, !0);
}
function NV(e) {
  LV(e, !1);
}
function PV(e, t, n, r, i) {
  i.requestId === r.findState.requestId &&
    ((r.findState = {
      ...r.findState,
      activeMatchOrdinal: i.activeMatchOrdinal,
      matches: i.matches,
      requestId: i.finalUpdate ? null : i.requestId,
    }),
    FV(e, t, n, r));
}
function FV(e, t, n, r) {
  if (t.isDestroyed()) return;
  let i = {
    type: `browser-sidebar-find-state`,
    conversationId: n,
    browserTabId: r.browserTabId,
    state: {
      activeMatchOrdinal: r.findState.activeMatchOrdinal,
      matches: r.findState.matches,
      query: r.findState.query,
    },
  };
  e.sendMessageToWebContents(t, i);
}
function IV(e, t, n, r) {
  if (
    r.findState.query.length === 0 &&
    r.findState.matches === 0 &&
    r.findState.activeMatchOrdinal === 0 &&
    r.findState.requestId == null
  )
    return;
  let i = r.page?.view.webContents;
  (i != null && !i.isDestroyed() && i.stopFindInPage(`clearSelection`),
    (r.findState = kV()),
    FV(e, t, n, r));
}
function LV(e, t) {
  if (e.findState.query.length === 0) return;
  let n = e.page?.view.webContents;
  n == null ||
    n.isDestroyed() ||
    (e.findState.requestId = n.findInPage(e.findState.query, { findNext: !0, forward: t }));
}
var RV = [`nextTab`, `previousTab`],
  zV = [
    `nextRecentThread`,
    `nextThread`,
    `openBrowserTab`,
    `previousRecentThread`,
    `previousThread`,
  ];
function BV({
  closeBrowserTab: e,
  getCanCloseBrowserTab: t,
  getInteractionMode: n,
  getOwnerCommandAccelerators: r = KV,
  getIsTweaksModeActive: i = () => !1,
  isCommentModeAvailable: a,
  owner: o,
  runCommandInOwner: s,
  setInteractionMode: c,
  sendCommandToOwner: l,
  showTweaksExitBlockedToast: u = () => {},
}) {
  let d = UV(r);
  return (f, p) => {
    if (
      tF(p, {
        allowDebugMenu: !1,
        closeTabAccelerators: r(`closeTab`),
        state: {
          bottomPanelBrowserCanZoom: !1,
          bottomPanelBrowserConversationId: null,
          bottomPanelBrowserTabId: null,
          canAcceptAppshotShortcut: !1,
          bottomPanelCanCloseActiveTab: !1,
          focusArea: `right-panel`,
          imagePreviewOpen: !1,
          rightPanelBrowserCanZoom: !1,
          rightPanelBrowserConversationId: null,
          rightPanelBrowserTabId: null,
          rightPanelCanCloseActiveTab: t(),
        },
      })?.type === `close-active-app-shell-tab`
    ) {
      (f.preventDefault(), e());
      return;
    }
    let m = d(p);
    if (m != null) {
      if ((f.preventDefault(), o.focus(), m.allowAppShellTabShortcut)) {
        s(m.commandId, GV(p), { allowAppShellTabShortcut: !0 });
        return;
      }
      s(m.commandId, GV(p));
      return;
    }
    let h = VV(p, { interactionMode: n(), isCommentModeAvailable: a() });
    if (h != null) {
      if (
        (f.preventDefault(),
        h.command.type === `set-interaction-mode` && h.command.interactionMode === `browse` && i())
      ) {
        u();
        return;
      }
      if (
        (h.shouldFocusOwner && o.focus(), h.command.type === `set-interaction-mode` && c != null)
      ) {
        c(h.command.interactionMode);
        return;
      }
      l(h.command);
    }
  };
}
function VV(e, { interactionMode: t, isCommentModeAvailable: n }) {
  if (qV(e, `f`)) return { command: { type: `open-find` }, shouldFocusOwner: !0 };
  if (qV(e, `l`)) return { command: { type: `focus-address` }, shouldFocusOwner: !0 };
  let r = RP(e);
  return r == null
    ? !qV(e, `.`) || !n
      ? null
      : {
          command: {
            type: `set-interaction-mode`,
            interactionMode: t === `comment` ? `browse` : `comment`,
          },
          shouldFocusOwner: !1,
        }
    : { command: r, shouldFocusOwner: !1 };
}
function HV() {
  let e = process.platform === `darwin`,
    n = (n) => t.Ut({ commandId: n, isMacOS: e });
  return {
    closeTab: n(`closeTab`),
    nextTab: n(`nextTab`),
    nextRecentThread: n(`nextRecentThread`),
    nextThread: n(`nextThread`),
    openBrowserTab: n(`openBrowserTab`),
    previousTab: n(`previousTab`),
    previousRecentThread: n(`previousRecentThread`),
    previousThread: n(`previousThread`),
  };
}
function UV(e) {
  return (t) => {
    if (t.type !== `keyDown`) return null;
    let n = GV(t),
      r = WV(t, n, e, RV),
      i = WV(t, n, e, zV);
    return r != null && i != null
      ? { allowAppShellTabShortcut: !0, commandId: i }
      : r == null
        ? i == null
          ? null
          : { commandId: i }
        : { commandId: r };
  };
}
function WV(e, n, r, i) {
  for (let a of i)
    for (let i of r(a)) {
      if (i.includes(` `) || !qP(n, i)) continue;
      let r = t.Yt(a);
      return e.isAutoRepeat && (r == null || r.kind !== `webview` || r.allowsKeyRepeat !== !0)
        ? null
        : a;
    }
  return null;
}
function GV(e) {
  return {
    altKey: e.alt === !0,
    code: e.code,
    ctrlKey: e.control === !0,
    key: e.key,
    metaKey: e.meta === !0,
    repeat: e.isAutoRepeat === !0,
    shiftKey: e.shift === !0,
  };
}
function KV(e) {
  return t.Xt({ commandId: e, isMacOS: process.platform === `darwin` });
}
function qV(e, t) {
  let n = process.platform === `darwin`;
  return (
    e.type === `keyDown` &&
    e.key.toLowerCase() === t &&
    e.alt !== !0 &&
    e.shift !== !0 &&
    (n ? e.meta === !0 && e.control !== !0 : e.control === !0 && e.meta !== !0)
  );
}
var JV = t.Gr(`browser-sidebar-manager`);
function YV(e, { conversationId: t, windowId: n }, r) {
  let i = e.getLiveWindowState(n);
  return i == null
    ? (JV().warning(`IAB_LIFECYCLE browser use route window missing for page lookup`, {
        safe: { conversationId: t, windowId: n },
        sensitive: {},
      }),
      null)
    : e.getPageHandleForConversation(i, t, r ?? OF(t));
}
function XV(e, t) {
  return dH(e, t);
}
function ZV(e, t) {
  return $V(e, t, !0).map(({ active: e, browserTabId: t, title: n, url: r }) => ({
    active: e,
    browserTabId: t,
    title: n,
    url: r,
  }));
}
function QV(e, t) {
  return $V(e, t, !1);
}
function $V(e, { conversationId: n, windowId: r }, i) {
  let a = e.getLiveWindowState(r);
  if (a == null)
    return (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for tab list`, {
        safe: { conversationId: n, windowId: r },
        sensitive: {},
      }),
      []
    );
  let o = [];
  for (let e of a.threads.values()) {
    if (
      e.conversationId !== n ||
      e.pendingTeardown?.reason === `closed` ||
      (i ? e.isBrowserUsePage : !e.isBrowserUsePage)
    )
      continue;
    let r = e.snapshot.title,
      s = e.snapshot.url;
    (i && e.page == null && s.length === 0 && e.snapshot.tabType === t.Jo.NEW_TAB_PAGE) ||
      o.push({
        active: a.activeConversationId === n && a.activeBrowserTabId === e.browserTabId,
        browserTabId: e.browserTabId,
        origin: e.browserUseTabOrigin ?? `agent`,
        title: r.length > 0 ? r : (e.page?.title ?? ``),
        url: s.length > 0 ? s : e.page == null ? `` : wL(e.page),
      });
  }
  return o;
}
async function eH(
  e,
  { browserTabId: t, conversationId: n, origin: r, startingUrl: i, windowId: a },
) {
  let o = e.getLiveWindowState(a);
  if (o == null)
    throw (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for page open`, {
        safe: { conversationId: n, windowId: a },
        sensitive: {},
      }),
      Error(`Codex browser window ${a} is no longer available for browser session ${n}`)
    );
  let s = t ?? OF(n);
  JV().info(`IAB_LIFECYCLE opening route page`, {
    safe: {
      activeConversationId: o.activeConversationId,
      browserTabId: t,
      conversationId: n,
      hasStartingUrl: i != null,
      ownerWebContentsId: o.owner.id,
      threadExists: e.getThreadStateForWindow(o, n, s) != null,
      windowId: a,
    },
    sensitive: {},
  });
  let c = e.ensureThreadStateForWindow(o, n, s, { isBrowserUseTab: !0 });
  ((c.isBrowserUsePage = !0), (c.browserUseTabOrigin = r ?? c.browserUseTabOrigin ?? `agent`));
  let l =
    c.page ?? e.createThreadPageState(o, n, c, { title: c.snapshot.title, url: c.snapshot.url });
  return (
    i != null && wL(l) !== i && e.navigatePageToUrl(l, i, `browser_use`, `browser_use`),
    e.syncThreadState(o, n, c),
    FL(o, n, l) ??
      (JV().info(`IAB_LIFECYCLE waiting for browser sidebar webview attachment`, {
        safe: { conversationId: n, ownerWebContentsId: o.owner.id, windowId: a },
        sensitive: {},
      }),
      e.browserUseOpenRequests.waitForOpen(o.owner, n, s, (t) => {
        tH(e, { conversationId: n, windowId: a }, s, t);
      }))
  );
}
function tH(e, t, n, r) {
  let { conversationId: i, windowId: a } = t,
    o = e.getLiveWindowState(a);
  if (o == null) {
    JV().warning(`IAB_LIFECYCLE browser use route window missing for page close`, {
      safe: { browserTabId: n, conversationId: i, windowId: a },
      sensitive: {},
    });
    return;
  }
  let s = e.getThreadStateForWindow(o, i, n) ?? null;
  s != null &&
    (IV(e.windowManager, o.owner, i, s),
    s.isBrowserUseActive && lH(e, t, !1, n),
    e.browserUseOpenRequests.rejectForRoute({
      browserTabId: n,
      conversationId: i,
      error: r ?? Error(`Codex browser tab was closed by Browser Use`),
      owner: o.owner,
    }),
    e.clearPendingWebviewRoutes(o, s),
    e.requestThreadTeardown(o, i, s, `closed`),
    e.windowManager.sendMessageToWebContents(o.owner, {
      type: `close-browser-tab`,
      browserTabId: n,
      conversationId: i,
    }));
}
function nH(e, t, n) {
  let { conversationId: r, windowId: i } = t,
    a = e.getLiveWindowState(i);
  if (a == null) {
    JV().warning(`IAB_LIFECYCLE browser use route window missing for page release`, {
      safe: { browserTabId: n, conversationId: r, windowId: i },
      sensitive: {},
    });
    return;
  }
  let o = e.getThreadStateForWindow(a, r, n) ?? null;
  if (o == null) return;
  let s = o.isBrowserUseActive;
  (oH(e, t, null, n),
    sH(e, t, null, n),
    (o.isBrowserUsePage = !1),
    (o.isBrowserUseActive = !1),
    (o.browserUseTabOrigin = null),
    (o.pendingBrowserUsePanelOpen = !1),
    (o.hasPendingBrowserUseVisibilityRequest = !1),
    (o.isBrowserUseNavigationRestrictionActive = !1),
    uH(e, t, null, n),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-state`,
      conversationId: r,
      browserTabId: n,
      isActive: !1,
    }),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-page-released`,
      conversationId: r,
      browserTabId: n,
    }),
    e.browserSessionRegistry.recordPageReleased({
      browserTabId: n,
      conversationId: r,
      windowId: i,
    }),
    s && e.enforceDetachedPageBudget(a));
}
function rH(e, { conversationId: t, windowId: n }) {
  let r = e.getLiveWindowState(n);
  if (r == null) {
    JV().warning(`IAB_LIFECYCLE browser use route window missing for route disposal`, {
      safe: { conversationId: t, windowId: n },
      sensitive: {},
    });
    return;
  }
  for (let n of Array.from(r.threads.values()))
    n.conversationId !== t ||
      !n.isBrowserUsePage ||
      (e.browserUseOpenRequests.rejectForRoute({
        browserTabId: n.browserTabId,
        conversationId: t,
        error: Error(`Codex browser route was disposed before the browser pane attached`),
        owner: r.owner,
      }),
      e.comments.close(r.owner, t, n.browserTabId),
      IV(e.windowManager, r.owner, t, n),
      e.clearPendingWebviewRoutes(r, n),
      e.windowManager.sendMessageToWebContents(r.owner, {
        type: `browser-sidebar-clear-pending-panel-open`,
        browserTabId: n.browserTabId,
        conversationId: t,
      }),
      n.page == null ? e.removeThreadState(r, n) : e.requestThreadTeardown(r, t, n, `closed`));
}
function iH(e, t, n, r) {
  let { conversationId: i, windowId: a } = t,
    o = e.getLiveWindowState(a);
  if (o == null)
    throw (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for browser visibility`, {
        safe: { conversationId: i, windowId: a },
        sensitive: {},
      }),
      Error(`Codex browser window ${a} is no longer available for browser session ${i}`)
    );
  let s = r ?? OF(i);
  if (n) {
    (e.ensureThreadStateForWindow(o, i, s, { isBrowserUseTab: !0 }), fH(e, o, i, s));
    return;
  }
  let c = e.getThreadStateForWindow(o, i, s) ?? null;
  (c != null &&
    ((c.pendingBrowserUsePanelOpen = !1),
    (c.hasPendingBrowserUseVisibilityRequest = !1),
    (c.presented = !1),
    (c.visible = !1),
    (c.bounds = null),
    e.updateThreadSessionState(c)),
    e.windowManager.sendMessageToWebContents(o.owner, {
      type: `browser-sidebar-clear-pending-panel-open`,
      browserTabId: s,
      conversationId: i,
    }),
    !(o.activeConversationId !== i || o.activeBrowserTabId !== s) &&
      e.windowManager.sendMessageToWebContents(o.owner, {
        type: `toggle-browser-panel`,
        browserTabId: s,
        open: !1,
        source: `browser_use`,
      }));
}
function aH(e, { conversationId: t, windowId: n }, r) {
  let i = e.getLiveWindowState(n);
  if (i == null)
    throw (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for browser visibility read`, {
        safe: { conversationId: t, windowId: n },
        sensitive: {},
      }),
      Error(`Codex browser window ${n} is no longer available for browser session ${t}`)
    );
  let a = r ?? OF(t),
    o = e.getThreadStateForWindow(i, t, a) ?? null;
  return (
    i.activeConversationId === t &&
    i.activeBrowserTabId === a &&
    (o?.hasPendingBrowserUseVisibilityRequest === !0 || (o?.visible === !0 && o.bounds != null))
  );
}
function oH(e, { conversationId: t, windowId: n }, r, i) {
  let a = e.getLiveWindowState(n);
  if (a == null)
    throw (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for viewport set`, {
        safe: { conversationId: t, windowId: n },
        sensitive: {},
      }),
      Error(`Codex browser window ${n} is no longer available for browser session ${t}`)
    );
  let o =
      r == null
        ? null
        : {
            width: OL(r.width, _F.minWidth, _F.maxWidth),
            height: OL(r.height, _F.minHeight, _F.maxHeight),
          },
    s = i ?? OF(t),
    c =
      o == null
        ? e.getThreadStateForWindow(a, t, s)
        : e.ensureThreadStateForWindow(a, t, s, { isBrowserUseTab: !0 });
  if (c == null) return;
  let l =
    c.emulatedViewportSize?.width !== o?.width || c.emulatedViewportSize?.height !== o?.height;
  ((c.emulatedViewportSize = o),
    e.syncThreadState(a, t, c),
    c.page != null && l && e.queuePageDeviceMetricsSync(c.page, o),
    e.windowManager.sendMessageToWebContents(a.owner, {
      type: `browser-sidebar-browser-use-viewport`,
      conversationId: t,
      browserTabId: c.browserTabId,
      viewportSize: o,
    }));
}
function sH(e, { conversationId: t, windowId: n }, r, i) {
  let a = e.getLiveWindowState(n);
  if (a == null)
    throw (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for capture surface set`, {
        safe: { conversationId: t, windowId: n },
        sensitive: {},
      }),
      Error(`Codex browser window ${n} is no longer available for browser session ${t}`)
    );
  let o = e.getThreadStateForWindow(a, t, i ?? OF(t)) ?? null;
  e.windowManager.sendMessageToWebContents(a.owner, {
    type: `browser-sidebar-browser-use-capture-surface`,
    conversationId: t,
    browserTabId: o?.browserTabId,
    surfaceSize: r == null ? null : { width: kL(r.width), height: kL(r.height) },
  });
}
function cH(e, t, n, r) {
  let i = e.getThreadStateForWindow(t, n, r) ?? null;
  (i?.visible === !0 && i.bounds != null) ||
    (i != null &&
      ((i.pendingBrowserUsePanelOpen = !1),
      (i.hasPendingBrowserUseVisibilityRequest = t.activeConversationId === n)),
    e.windowManager.sendMessageToWebContents(t.owner, {
      type: `browser-sidebar-open-panel-without-animation`,
      conversationId: n,
      browserTabId: i?.browserTabId ?? r,
      source: `browser_use`,
      initiator: `browser_use`,
    }));
}
function lH(e, t, n, r) {
  let i = e.getLiveWindowState(t.windowId);
  if (i == null) {
    JV().warning(`IAB_LIFECYCLE browser use route window missing for active state`, {
      safe: t,
      sensitive: {},
    });
    return;
  }
  let a = r == null ? (n ? [OF(t.conversationId)] : hH(i, t.conversationId)) : [r];
  for (let r of a) {
    n && pH(e, i, t, r);
    let a =
        e.getThreadStateForWindow(i, t.conversationId, r) ??
        (n ? e.ensureThreadStateForWindow(i, t.conversationId, r, { isBrowserUseTab: !0 }) : null),
      o = a?.isBrowserUseActive ?? !1;
    n &&
      !o &&
      a != null &&
      a.emulatedViewportSize == null &&
      e.defaultBrowserUseViewportSize != null &&
      oH(e, t, e.defaultBrowserUseViewportSize, r);
    let s = a != null && !n && a.isBrowserUseNavigationRestrictionActive;
    (a != null && o === n && !s) ||
      (a != null &&
        ((a.isBrowserUseActive = n),
        n &&
          (a.page != null && (a.page.pendingSuspensionId = null),
          (a.isBrowserUsePage = !0),
          (a.browserUseTabOrigin ??= `agent`)),
        n || (a.isBrowserUseNavigationRestrictionActive = !1)),
      JV().info(`IAB_LIFECYCLE syncing browser use active state`, {
        safe: {
          browserTabId: r,
          conversationId: t.conversationId,
          isActive: n,
          ownerWebContentsId: i.owner.id,
          windowId: t.windowId,
        },
        sensitive: {},
      }),
      e.windowManager.sendMessageToWebContents(i.owner, {
        type: `browser-sidebar-browser-use-state`,
        conversationId: t.conversationId,
        browserTabId: r,
        isActive: n,
      }),
      n || uH(e, t, null, r));
  }
}
function uH(e, t, n, r) {
  let i = e.getLiveWindowState(t.windowId);
  if (i == null)
    return (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for cursor state`, {
        safe: t,
        sensitive: {},
      }),
      !1
    );
  let a = r == null ? (n == null ? hH(i, t.conversationId) : [OF(t.conversationId)]) : [r],
    o = !1;
  for (let r of a) {
    let a =
        (n == null
          ? (e.getThreadStateForWindow(i, t.conversationId, r) ?? null)
          : e.ensureThreadStateForWindow(i, t.conversationId, r, { isBrowserUseTab: !0 })
        )?.browserTabId ?? r,
      s = n != null && mH(e, i, t.conversationId, a),
      c =
        n == null
          ? { visible: !1, x: 0, y: 0 }
          : {
              ...(s ? {} : { animateMovement: !1 }),
              ...(n.moveSequence == null ? {} : { moveSequence: n.moveSequence }),
              visible: !0,
              x: n.x,
              y: n.y,
            };
    (e.windowManager.sendMessageToWebContents(i.owner, {
      type: `browser-sidebar-browser-use-cursor-state`,
      conversationId: t.conversationId,
      browserTabId: a,
      ...c,
    }),
      (o = s || o));
  }
  return o;
}
function dH(e, { conversationId: t, windowId: n }) {
  let r = e.getLiveWindowState(n);
  if (r == null)
    return (
      JV().warning(`IAB_LIFECYCLE browser use route window missing for page list`, {
        safe: { conversationId: t, windowId: n },
        sensitive: {},
      }),
      []
    );
  let i = [];
  for (let e of r.threads.values()) {
    if (
      e.conversationId !== t ||
      e.pendingTeardown?.reason === `closed` ||
      !e.isBrowserUsePage ||
      e.page == null
    )
      continue;
    let n = FL(r, t, e.page);
    n != null && i.push(n);
  }
  return i;
}
function fH(e, t, n, r) {
  if (t.activeConversationId !== n) {
    cH(e, t, n, r);
    return;
  }
  let i = e.getThreadStateForWindow(t, n, r) ?? null;
  (i?.visible === !0 && i.bounds != null) ||
    (i != null &&
      ((i.pendingBrowserUsePanelOpen = !1), (i.hasPendingBrowserUseVisibilityRequest = !0)),
    e.windowManager.sendMessageToWebContents(t.owner, {
      type: `toggle-browser-panel`,
      open: !0,
      browserTabId: r,
      conversationId: n,
      source: `browser_use`,
      initiator: `browser_use`,
    }));
}
function pH(e, t, n, r) {
  for (let i of t.threads.values())
    i.conversationId !== n.conversationId ||
      i.browserTabId === r ||
      !i.isBrowserUseActive ||
      ((i.isBrowserUseActive = !1),
      (i.isBrowserUseNavigationRestrictionActive = !1),
      e.windowManager.sendMessageToWebContents(t.owner, {
        type: `browser-sidebar-browser-use-state`,
        conversationId: n.conversationId,
        browserTabId: i.browserTabId,
        isActive: !1,
      }),
      uH(e, n, null, i.browserTabId));
}
function mH(e, n, r, i) {
  let a = e.getThreadStateForWindow(n, r, i) ?? null;
  return (
    n.window.isFocused() &&
    n.activeConversationId === r &&
    n.activeBrowserTabId === i &&
    a?.visible === !0 &&
    a.bounds != null &&
    a.snapshot.tabType === t.Jo.WEB
  );
}
function hH(e, t) {
  let n = [];
  for (let r of e.threads.values())
    r.conversationId === t &&
      (r.isBrowserUsePage || r.isBrowserUseActive) &&
      n.push(r.browserTabId);
  return n;
}
var gH = t.Gr(`browser-use-native-pipe-server`);
async function _H({
  apiImpl: e,
  nativePipeDirectory: t,
  pipePath: n,
  socketPeerAuthorizer: r = cd(),
}) {
  let i;
  try {
    i = await Vu({
      nativePipeDirectory: t,
      pipePath: n,
      socketPeerAuthorizer: r,
      events: {
        onListening: (e) => {
          gH().info(`browser-use native pipe listening`, { safe: {}, sensitive: { pipePath: e } });
        },
        onAuthorizationError: (e) => {
          gH().warning(`browser-use native pipe peer authorization failed`, {
            safe: {},
            sensitive: { error: e },
          });
        },
        onRejectedSocket: (e) => {
          gH().warning(`browser-use native pipe rejected socket peer`, {
            safe: { reason: e.reason ?? `unauthorized` },
            sensitive: {},
          });
        },
        onSocketError: (e) => {
          gH().warning(`browser-use native pipe socket error`, {
            safe: {},
            sensitive: { error: e },
          });
        },
      },
    });
  } catch (t) {
    throw (e.dispose(), t);
  }
  let a = new ut(i, e, {
      onMoveMouseError: (e, t) => {
        gH().warning(`IAB_LIFECYCLE failed to move browser use cursor`, {
          safe: { tabId: t.tabId },
          sensitive: { error: e },
        });
      },
    }),
    o = e.addCdpEventListener((e) => {
      a.sendCdpEvent(e);
    });
  return {
    pipePath: i.pipePath,
    dispose: async () => {
      (o(), e.dispose(), await i.close());
    },
  };
}
function vH(e) {
  switch (e) {
    case `Input.dispatchKeyEvent`:
    case `Input.dispatchMouseEvent`:
    case `Input.insertText`:
    case `Input.synthesizeScrollGesture`:
      return !0;
    default:
      return !1;
  }
}
function yH(e) {
  let t = `__codexIabExpectedInputTargetToken`,
    n = ((e, t) => {
      if (typeof e != `object` || !e || Array.isArray(e)) throw Error(`${t} must be an object`);
      return e;
    })(e.params, `params`),
    r = () => {
      let e = n[t];
      if (e == null) return null;
      if (typeof e != `string` || e.length === 0) throw Error(`${t} must be a string`);
      return e;
    },
    i = (e) => {
      if (typeof e != `object` || !e) return null;
      let t = e.__codexIabInputTargetToken;
      return typeof t == `string` ? t : null;
    },
    a = (e) => {
      let t = r();
      if (t != null && i(e) !== t)
        throw Error(`Focused input target no longer matches the resolved locator`);
    },
    o = (e) => {
      let t = (t) => Math.floor(e / t) % 2 == 1;
      return { altKey: t(1), ctrlKey: t(2), metaKey: t(4), shiftKey: t(8) };
    },
    s = (e, t) => {
      if (typeof e != `number` || !Number.isFinite(e)) throw Error(`${t} must be a finite number`);
      return e;
    },
    c = (e, t) => {
      if (typeof e != `string`) throw Error(`${t} must be a string`);
      return e;
    },
    l = (e) => e?.ownerDocument?.defaultView ?? window,
    u = (e) => (e.defaultView == null ? e.ownerDocument : e)?.defaultView ?? window,
    d = () => {
      throw Error(
        `Input targets inside cross-origin or inaccessible iframes are not currently supported in the in-app browser`,
      );
    },
    f = (e, t, n) => {
      let r = u(e),
        i = e.elementFromPoint(t, n);
      if (i == null || !(i instanceof r.Element)) return null;
      let a = i.shadowRoot;
      if (a != null) {
        let e = a.elementFromPoint(t, n),
          r = u(a);
        if (e != null && e instanceof r.Element) return { target: e, x: t, y: n };
      }
      if (i instanceof r.HTMLIFrameElement)
        try {
          let e = i.contentWindow;
          if (e != null) {
            let r = e.document,
              a = i.getBoundingClientRect();
            return f(r, t - a.left, n - a.top) ?? { target: i, x: t, y: n };
          }
          d();
        } catch {
          d();
        }
      return { target: i, x: t, y: n };
    },
    p = (e, t) => {
      let n = f(document, e, t);
      if (n == null) throw Error(`No element found at point ${e},${t}`);
      return n;
    },
    m = (e) => {
      switch (e) {
        case `left`:
          return 0;
        case `middle`:
          return 1;
        case `right`:
          return 2;
        case `back`:
          return 3;
        case `forward`:
          return 4;
        case `none`:
        case void 0:
          return 0;
        default:
          throw Error(`Unsupported mouse button: ${String(e)}`);
      }
    },
    h = (e, t, n) => {
      let r = l(e),
        i = t.startsWith(`pointer`),
        a = r.PointerEvent,
        o =
          i && typeof a == `function`
            ? new a(t, { ...n, isPrimary: !0, pointerId: 1, pointerType: `mouse` })
            : new r.MouseEvent(t, n);
      return e.dispatchEvent(o);
    },
    g = (e, t, n, r) => {
      let i = l(e);
      return {
        ...o(Number(t.modifiers ?? 0)),
        bubbles: !0,
        button: m(t.button),
        buttons: Number(t.buttons ?? 0),
        cancelable: !0,
        clientX: n,
        clientY: r,
        composed: !0,
        detail: Number(t.clickCount ?? 0),
        screenX: n,
        screenY: r,
        view: i,
      };
    },
    _ = (e) => e instanceof l(e).HTMLElement,
    v = (e) =>
      _(e)
        ? e.isContentEditable || e.tabIndex >= 0
          ? !0
          : e.tagName === `A`
            ? e.hasAttribute(`href`)
            : e.tagName === `BUTTON` ||
              e.tagName === `INPUT` ||
              e.tagName === `SELECT` ||
              e.tagName === `TEXTAREA`
        : !1,
    y = (e) => {
      let t = l(e),
        n = null;
      if (e instanceof t.HTMLLabelElement && e.control != null) n = e.control;
      else
        for (let t = e; t != null; t = t.parentElement)
          if (v(t)) {
            n = t;
            break;
          }
      if (n != null)
        try {
          n.focus({ preventScroll: !0 });
        } catch {
          n.focus();
        }
    },
    b = () => {
      let e = globalThis;
      return ((e.__codexIabInputTranslationState ??= {}), e.__codexIabInputTranslationState);
    },
    x = (e, t) => e != null && Math.abs(e.x - t.x) <= 1 && Math.abs(e.y - t.y) <= 1,
    S = () => {
      let e = c(n.type, `type`),
        t = s(n.x, `x`),
        r = s(n.y, `y`),
        { target: i, x: a, y: o } = p(t, r),
        l = g(i, n, a, o),
        u = b();
      if (e === `mouseMoved`) {
        (Number(n.buttons ?? 0) !== 0 && u.mousePress != null && (u.mousePress.moved = !0),
          h(i, `pointermove`, l),
          h(i, `mousemove`, l));
        return;
      }
      if (e === `mousePressed`) {
        u.mousePress = { button: n.button ?? `left`, moved: !1, x: t, y: r };
        let e = h(i, `pointerdown`, l),
          a = h(i, `mousedown`, l);
        e && a && y(i);
        return;
      }
      if (e === `mouseReleased`) {
        (h(i, `pointerup`, l), h(i, `mouseup`, l));
        let e = u.mousePress;
        if (
          ((u.mousePress = null),
          !(e != null && !e.moved && e.button === (n.button ?? `left`) && x(e, { x: t, y: r })))
        )
          return;
        if (n.button === `right`) {
          h(i, `contextmenu`, l);
          return;
        }
        if (n.button === `middle`) {
          h(i, `auxclick`, l);
          return;
        }
        (h(i, `click`, l), Number(n.clickCount ?? 0) >= 2 && h(i, `dblclick`, l));
        return;
      }
      if (e === `mouseWheel`) {
        T(i, l, Number(n.deltaX ?? 0), Number(n.deltaY ?? 0));
        return;
      }
      throw Error(`Unsupported mouse event type: ${e}`);
    },
    C = (e, t, n) => {
      let r = (e, t, n) => {
        if (n === 0) return !1;
        if (t === `x`) {
          let t = e.scrollWidth - e.clientWidth;
          return n > 0 ? e.scrollLeft < t : e.scrollLeft > 0;
        }
        let r = e.scrollHeight - e.clientHeight;
        return n > 0 ? e.scrollTop < r : e.scrollTop > 0;
      };
      for (let i = e; i != null; i = i.parentElement) {
        let e = getComputedStyle(i),
          a = n !== 0 && /(auto|scroll|overlay)/.test(e.overflowY) && r(i, `y`, n);
        if ((t !== 0 && /(auto|scroll|overlay)/.test(e.overflowX) && r(i, `x`, t)) || a) return i;
      }
      return null;
    },
    w = (e, t, n) => {
      let r = C(e, t, n);
      if (r != null) {
        r.scrollBy({ behavior: `auto`, left: t, top: n });
        return;
      }
      l(e).scrollBy({ behavior: `auto`, left: t, top: n });
    },
    T = (e, t, n, r) => {
      let i = new (l(e).WheelEvent)(`wheel`, { ...t, deltaMode: 0, deltaX: n, deltaY: r });
      e.dispatchEvent(i) && w(e, n, r);
    },
    E = () => {
      let e = s(n.x, `x`),
        t = s(n.y, `y`),
        r = Number(n.xDistance ?? 0),
        i = Number(n.yDistance ?? 0),
        { target: a, x: o, y: c } = p(e, t);
      T(a, g(a, { buttons: 0, clickCount: 0, modifiers: 0 }, o, c), -r, -i);
    },
    D = (e) => {
      let t = e.activeElement;
      if (t == null) return null;
      let n = u(e);
      if (t instanceof n.HTMLIFrameElement)
        try {
          let e = t.contentWindow?.document;
          if (e != null) return D(e) ?? t;
          d();
        } catch {
          d();
        }
      return t instanceof n.HTMLElement && t.shadowRoot != null ? (D(t.shadowRoot) ?? t) : t;
    },
    O = (e) => {
      let t = l(e);
      if (e instanceof t.HTMLTextAreaElement) return !0;
      if (!(e instanceof t.HTMLInputElement)) return !1;
      switch (e.type) {
        case `password`:
        case `search`:
        case `tel`:
        case `text`:
        case `url`:
          return !0;
        default:
          return !1;
      }
    },
    k = (e) => {
      if (!(e instanceof l(e).HTMLInputElement)) return !1;
      switch (e.type) {
        case `email`:
        case `number`:
          return !0;
        default:
          return !1;
      }
    },
    A = (e) => e instanceof l(e).HTMLElement && e.isContentEditable,
    j = (e) => e instanceof l(e).HTMLTextAreaElement,
    M = () => D(document) ?? document.body ?? document.documentElement,
    N = () => {
      let e = D(document);
      return e != null && (O(e) || k(e) || A(e)) ? e : null;
    },
    P = () => {
      let e = N();
      return (e != null && a(e), e);
    },
    F = (e, t, n) => {
      let r = l(e);
      return typeof r.InputEvent == `function`
        ? new r.InputEvent(t, n)
        : new r.Event(t, { bubbles: n.bubbles, cancelable: n.cancelable, composed: n.composed });
    },
    I = (e, t, n = `insertText`) => {
      if (e == null) throw Error(`No editable element is focused`);
      let r = F(e, `beforeinput`, {
        bubbles: !0,
        cancelable: !0,
        composed: !0,
        data: t,
        inputType: n,
      });
      if (e.dispatchEvent(r)) {
        if (O(e)) {
          let n = e.selectionStart ?? e.value.length,
            r = e.selectionEnd ?? e.value.length;
          e.setRangeText(t, n, r, `end`);
        } else if (k(e)) {
          if (e.ownerDocument.execCommand?.(`insertText`, !1, t)) return;
          e.value += t;
        } else if (A(e)) {
          let n = l(e).getSelection(),
            r = n?.rangeCount ? n.getRangeAt(0) : null;
          ((r == null || !e.contains(r.commonAncestorContainer)) &&
            ((r = e.ownerDocument.createRange()), r.selectNodeContents(e), r.collapse(!1)),
            r.deleteContents());
          let i = e.ownerDocument.createTextNode(t);
          (r.insertNode(i),
            r.setStartAfter(i),
            r.collapse(!0),
            n?.removeAllRanges(),
            n?.addRange(r));
        } else throw Error(`Focused element is not editable`);
        e.dispatchEvent(
          F(e, `input`, { bubbles: !0, cancelable: !1, composed: !0, data: t, inputType: n }),
        );
      }
    },
    ee = (e) => {
      if (e == null) return !1;
      if (O(e)) return (e.setSelectionRange(0, e.value.length), !0);
      if (k(e)) return e.ownerDocument.execCommand?.(`selectAll`) === !0;
      if (A(e)) {
        let t = l(e).getSelection(),
          n = e.ownerDocument.createRange();
        return (n.selectNodeContents(e), t?.removeAllRanges(), t?.addRange(n), !0);
      }
      return !1;
    },
    te = (e, t) => {
      if (e == null) return !1;
      let n = t === `forward` ? `deleteContentForward` : `deleteContentBackward`,
        r = F(e, `beforeinput`, {
          bubbles: !0,
          cancelable: !0,
          composed: !0,
          data: null,
          inputType: n,
        });
      if (!e.dispatchEvent(r)) return !0;
      if (O(e)) {
        let n = e.value,
          r = e.selectionStart ?? n.length,
          i = e.selectionEnd ?? n.length;
        (r === i && (t === `forward` ? (i = Math.min(n.length, i + 1)) : (r = Math.max(0, r - 1))),
          e.setRangeText(``, r, i, `end`));
      } else if (k(e)) {
        let n = t === `forward` ? `forwardDelete` : `delete`;
        if (e.ownerDocument.execCommand?.(n)) return !0;
        t === `backward` && (e.value = e.value.slice(0, -1));
      } else if (A(e)) {
        let n = l(e).getSelection();
        if (n == null || n.rangeCount === 0) return !0;
        let r = n.getRangeAt(0);
        if (!e.contains(r.commonAncestorContainer)) return !0;
        (r.collapsed &&
          (t === `forward`
            ? r.setEnd(
                r.startContainer,
                Math.min(r.startContainer.textContent?.length ?? 0, r.startOffset + 1),
              )
            : r.setStart(r.startContainer, Math.max(0, r.startOffset - 1))),
          r.deleteContents());
      } else return !1;
      return (
        e.dispatchEvent(
          F(e, `input`, { bubbles: !0, cancelable: !1, composed: !0, data: null, inputType: n }),
        ),
        !0
      );
    },
    ne = (e) => {
      let t = P(),
        n = t?.ownerDocument ?? document,
        r = !1;
      switch (
        (t != null && O(t)
          ? (r = (t.selectionStart ?? 0) !== (t.selectionEnd ?? 0))
          : t != null && A(t) && (r = l(t).getSelection()?.isCollapsed === !1),
        e)
      ) {
        case `selectAll`:
          ee(t) || n.execCommand?.(`selectAll`);
          return;
        case `deleteBackward`:
          te(t, `backward`);
          return;
        case `deleteForward`:
          te(t, `forward`);
          return;
        case `cut`:
          if (t != null) {
            let e = l(t),
              n =
                typeof e.ClipboardEvent == `function`
                  ? new e.ClipboardEvent(`cut`, { bubbles: !0, cancelable: !0, composed: !0 })
                  : new e.Event(`cut`, { bubbles: !0, cancelable: !0, composed: !0 });
            t.dispatchEvent(n) && r && te(t, `forward`);
          }
          return;
        case `copy`:
        case `paste`:
        case `undo`:
        case `redo`:
          n.execCommand?.(e);
          return;
        default:
          return;
      }
    },
    re = () => {
      let e = Number(n.modifiers ?? 0),
        t = Number(n.windowsVirtualKeyCode ?? 0),
        r = typeof n.code == `string` ? n.code : ``,
        i = ``;
      return (
        typeof n.key == `string` ? (i = n.key) : typeof n.code == `string` && (i = n.code),
        {
          ...o(e),
          bubbles: !0,
          cancelable: !0,
          code: r,
          composed: !0,
          key: i,
          keyCode: t,
          location: Number(n.location ?? 0),
          repeat: !!n.autoRepeat,
          which: t,
        }
      );
    },
    ie = (e, t, n) => {
      let r = new (l(e).KeyboardEvent)(t, n),
        i = [
          [`keyCode`, n.keyCode],
          [`which`, n.which],
          [`charCode`, t === `keypress` ? n.keyCode : 0],
        ];
      for (let [e, t] of i)
        try {
          Object.defineProperty(r, e, { get: () => t });
        } catch {}
      return r;
    },
    ae = () => {
      let e = c(n.type, `type`),
        t = M(),
        r = re();
      if (e === `keyUp`) {
        t.dispatchEvent(ie(t, `keyup`, r));
        return;
      }
      if ((a(t), e !== `keyDown` && e !== `rawKeyDown` && e !== `char`))
        throw Error(`Unsupported key event type: ${e}`);
      if (!(e === `char` || t.dispatchEvent(ie(t, `keydown`, r)))) return;
      let i = Array.isArray(n.commands) ? n.commands : [];
      for (let e of i) typeof e == `string` && ne(e);
      let o = typeof n.text == `string` ? n.text : ``;
      if (o.length > 0) {
        let e = o.codePointAt(0) ?? r.keyCode;
        if (t.dispatchEvent(ie(t, `keypress`, { ...r, key: o, keyCode: e, which: e }))) {
          let e = P();
          e != null && I(e, o);
        }
      } else if (i.length === 0) {
        if (r.key === `Backspace`) te(P(), `backward`);
        else if (r.key === `Delete`) te(P(), `forward`);
        else if (r.key === `Enter`) {
          let e = P();
          e != null &&
            (A(e) || j(e)) &&
            I(
              e,
              `
`,
              `insertLineBreak`,
            );
        }
      }
    };
  try {
    switch (e.method) {
      case `Input.dispatchMouseEvent`:
        S();
        break;
      case `Input.dispatchKeyEvent`:
        ae();
        break;
      case `Input.insertText`:
        I(P(), c(n.text, `text`));
        break;
      case `Input.synthesizeScrollGesture`:
        E();
        break;
    }
    return { ok: !0 };
  } catch (e) {
    return { error: e instanceof Error ? e.message : String(e), ok: !1 };
  }
}
var bH = class {
    type;
    schema;
    payload;
    constructor(e, t, n) {
      ((this.type = e), (this.schema = t), (this.payload = n));
    }
    parse() {
      return this.schema.parse(this.payload);
    }
    toJSON() {
      return { type: this.type, ...this.payload };
    }
  },
  xH;
(function (e) {
  e.assertEqual = (e) => {};
  function t(e) {}
  e.assertIs = t;
  function n(e) {
    throw Error();
  }
  ((e.assertNever = n),
    (e.arrayToEnum = (e) => {
      let t = {};
      for (let n of e) t[n] = n;
      return t;
    }),
    (e.getValidEnumValues = (t) => {
      let n = e.objectKeys(t).filter((e) => typeof t[t[e]] != `number`),
        r = {};
      for (let e of n) r[e] = t[e];
      return e.objectValues(r);
    }),
    (e.objectValues = (t) =>
      e.objectKeys(t).map(function (e) {
        return t[e];
      })),
    (e.objectKeys =
      typeof Object.keys == `function`
        ? (e) => Object.keys(e)
        : (e) => {
            let t = [];
            for (let n in e) Object.prototype.hasOwnProperty.call(e, n) && t.push(n);
            return t;
          }),
    (e.find = (e, t) => {
      for (let n of e) if (t(n)) return n;
    }),
    (e.isInteger =
      typeof Number.isInteger == `function`
        ? (e) => Number.isInteger(e)
        : (e) => typeof e == `number` && Number.isFinite(e) && Math.floor(e) === e));
  function r(e, t = ` | `) {
    return e.map((e) => (typeof e == `string` ? `'${e}'` : e)).join(t);
  }
  ((e.joinValues = r),
    (e.jsonStringifyReplacer = (e, t) => (typeof t == `bigint` ? t.toString() : t)));
})((xH ||= {}));
var SH;
(function (e) {
  e.mergeShapes = (e, t) => ({ ...e, ...t });
})((SH ||= {}));
var V = xH.arrayToEnum([
    `string`,
    `nan`,
    `number`,
    `integer`,
    `float`,
    `boolean`,
    `date`,
    `bigint`,
    `symbol`,
    `function`,
    `undefined`,
    `null`,
    `array`,
    `object`,
    `unknown`,
    `promise`,
    `void`,
    `never`,
    `map`,
    `set`,
  ]),
  CH = (e) => {
    switch (typeof e) {
      case `undefined`:
        return V.undefined;
      case `string`:
        return V.string;
      case `number`:
        return Number.isNaN(e) ? V.nan : V.number;
      case `boolean`:
        return V.boolean;
      case `function`:
        return V.function;
      case `bigint`:
        return V.bigint;
      case `symbol`:
        return V.symbol;
      case `object`:
        return Array.isArray(e)
          ? V.array
          : e === null
            ? V.null
            : e.then && typeof e.then == `function` && e.catch && typeof e.catch == `function`
              ? V.promise
              : typeof Map < `u` && e instanceof Map
                ? V.map
                : typeof Set < `u` && e instanceof Set
                  ? V.set
                  : typeof Date < `u` && e instanceof Date
                    ? V.date
                    : V.object;
      default:
        return V.unknown;
    }
  },
  H = xH.arrayToEnum([
    `invalid_type`,
    `invalid_literal`,
    `custom`,
    `invalid_union`,
    `invalid_union_discriminator`,
    `invalid_enum_value`,
    `unrecognized_keys`,
    `invalid_arguments`,
    `invalid_return_type`,
    `invalid_date`,
    `invalid_string`,
    `too_small`,
    `too_big`,
    `invalid_intersection_types`,
    `not_multiple_of`,
    `not_finite`,
  ]),
  wH = class e extends Error {
    get errors() {
      return this.issues;
    }
    constructor(e) {
      (super(),
        (this.issues = []),
        (this.addIssue = (e) => {
          this.issues = [...this.issues, e];
        }),
        (this.addIssues = (e = []) => {
          this.issues = [...this.issues, ...e];
        }));
      let t = new.target.prototype;
      (Object.setPrototypeOf ? Object.setPrototypeOf(this, t) : (this.__proto__ = t),
        (this.name = `ZodError`),
        (this.issues = e));
    }
    format(e) {
      let t =
          e ||
          function (e) {
            return e.message;
          },
        n = { _errors: [] },
        r = (e) => {
          for (let i of e.issues)
            if (i.code === `invalid_union`) i.unionErrors.map(r);
            else if (i.code === `invalid_return_type`) r(i.returnTypeError);
            else if (i.code === `invalid_arguments`) r(i.argumentsError);
            else if (i.path.length === 0) n._errors.push(t(i));
            else {
              let e = n,
                r = 0;
              for (; r < i.path.length; ) {
                let n = i.path[r];
                (r === i.path.length - 1
                  ? ((e[n] = e[n] || { _errors: [] }), e[n]._errors.push(t(i)))
                  : (e[n] = e[n] || { _errors: [] }),
                  (e = e[n]),
                  r++);
              }
            }
        };
      return (r(this), n);
    }
    static assert(t) {
      if (!(t instanceof e)) throw Error(`Not a ZodError: ${t}`);
    }
    toString() {
      return this.message;
    }
    get message() {
      return JSON.stringify(this.issues, xH.jsonStringifyReplacer, 2);
    }
    get isEmpty() {
      return this.issues.length === 0;
    }
    flatten(e = (e) => e.message) {
      let t = {},
        n = [];
      for (let r of this.issues)
        if (r.path.length > 0) {
          let n = r.path[0];
          ((t[n] = t[n] || []), t[n].push(e(r)));
        } else n.push(e(r));
      return { formErrors: n, fieldErrors: t };
    }
    get formErrors() {
      return this.flatten();
    }
  };
wH.create = (e) => new wH(e);
var TH = (e, t) => {
    let n;
    switch (e.code) {
      case H.invalid_type:
        n =
          e.received === V.undefined
            ? `Required`
            : `Expected ${e.expected}, received ${e.received}`;
        break;
      case H.invalid_literal:
        n = `Invalid literal value, expected ${JSON.stringify(e.expected, xH.jsonStringifyReplacer)}`;
        break;
      case H.unrecognized_keys:
        n = `Unrecognized key(s) in object: ${xH.joinValues(e.keys, `, `)}`;
        break;
      case H.invalid_union:
        n = `Invalid input`;
        break;
      case H.invalid_union_discriminator:
        n = `Invalid discriminator value. Expected ${xH.joinValues(e.options)}`;
        break;
      case H.invalid_enum_value:
        n = `Invalid enum value. Expected ${xH.joinValues(e.options)}, received '${e.received}'`;
        break;
      case H.invalid_arguments:
        n = `Invalid function arguments`;
        break;
      case H.invalid_return_type:
        n = `Invalid function return type`;
        break;
      case H.invalid_date:
        n = `Invalid date`;
        break;
      case H.invalid_string:
        typeof e.validation == `object`
          ? `includes` in e.validation
            ? ((n = `Invalid input: must include "${e.validation.includes}"`),
              typeof e.validation.position == `number` &&
                (n = `${n} at one or more positions greater than or equal to ${e.validation.position}`))
            : `startsWith` in e.validation
              ? (n = `Invalid input: must start with "${e.validation.startsWith}"`)
              : `endsWith` in e.validation
                ? (n = `Invalid input: must end with "${e.validation.endsWith}"`)
                : xH.assertNever(e.validation)
          : (n = e.validation === `regex` ? `Invalid` : `Invalid ${e.validation}`);
        break;
      case H.too_small:
        n =
          e.type === `array`
            ? `Array must contain ${e.exact ? `exactly` : e.inclusive ? `at least` : `more than`} ${e.minimum} element(s)`
            : e.type === `string`
              ? `String must contain ${e.exact ? `exactly` : e.inclusive ? `at least` : `over`} ${e.minimum} character(s)`
              : e.type === `number` || e.type === `bigint`
                ? `Number must be ${e.exact ? `exactly equal to ` : e.inclusive ? `greater than or equal to ` : `greater than `}${e.minimum}`
                : e.type === `date`
                  ? `Date must be ${e.exact ? `exactly equal to ` : e.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(e.minimum))}`
                  : `Invalid input`;
        break;
      case H.too_big:
        n =
          e.type === `array`
            ? `Array must contain ${e.exact ? `exactly` : e.inclusive ? `at most` : `less than`} ${e.maximum} element(s)`
            : e.type === `string`
              ? `String must contain ${e.exact ? `exactly` : e.inclusive ? `at most` : `under`} ${e.maximum} character(s)`
              : e.type === `number`
                ? `Number must be ${e.exact ? `exactly` : e.inclusive ? `less than or equal to` : `less than`} ${e.maximum}`
                : e.type === `bigint`
                  ? `BigInt must be ${e.exact ? `exactly` : e.inclusive ? `less than or equal to` : `less than`} ${e.maximum}`
                  : e.type === `date`
                    ? `Date must be ${e.exact ? `exactly` : e.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(e.maximum))}`
                    : `Invalid input`;
        break;
      case H.custom:
        n = `Invalid input`;
        break;
      case H.invalid_intersection_types:
        n = `Intersection results could not be merged`;
        break;
      case H.not_multiple_of:
        n = `Number must be a multiple of ${e.multipleOf}`;
        break;
      case H.not_finite:
        n = `Number must be finite`;
        break;
      default:
        ((n = t.defaultError), xH.assertNever(e));
    }
    return { message: n };
  },
  EH = TH;
function DH() {
  return EH;
}
var OH = (e) => {
  let { data: t, path: n, errorMaps: r, issueData: i } = e,
    a = [...n, ...(i.path || [])],
    o = { ...i, path: a };
  if (i.message !== void 0) return { ...i, path: a, message: i.message };
  let s = ``,
    c = r
      .filter((e) => !!e)
      .slice()
      .reverse();
  for (let e of c) s = e(o, { data: t, defaultError: s }).message;
  return { ...i, path: a, message: s };
};
function U(e, t) {
  let n = DH(),
    r = OH({
      issueData: t,
      data: e.data,
      path: e.path,
      errorMaps: [e.common.contextualErrorMap, e.schemaErrorMap, n, n === TH ? void 0 : TH].filter(
        (e) => !!e,
      ),
    });
  e.common.issues.push(r);
}
var kH = class e {
    constructor() {
      this.value = `valid`;
    }
    dirty() {
      this.value === `valid` && (this.value = `dirty`);
    }
    abort() {
      this.value !== `aborted` && (this.value = `aborted`);
    }
    static mergeArray(e, t) {
      let n = [];
      for (let r of t) {
        if (r.status === `aborted`) return W;
        (r.status === `dirty` && e.dirty(), n.push(r.value));
      }
      return { status: e.value, value: n };
    }
    static async mergeObjectAsync(t, n) {
      let r = [];
      for (let e of n) {
        let t = await e.key,
          n = await e.value;
        r.push({ key: t, value: n });
      }
      return e.mergeObjectSync(t, r);
    }
    static mergeObjectSync(e, t) {
      let n = {};
      for (let r of t) {
        let { key: t, value: i } = r;
        if (t.status === `aborted` || i.status === `aborted`) return W;
        (t.status === `dirty` && e.dirty(),
          i.status === `dirty` && e.dirty(),
          t.value !== `__proto__` && (i.value !== void 0 || r.alwaysSet) && (n[t.value] = i.value));
      }
      return { status: e.value, value: n };
    }
  },
  W = Object.freeze({ status: `aborted` }),
  AH = (e) => ({ status: `dirty`, value: e }),
  jH = (e) => ({ status: `valid`, value: e }),
  MH = (e) => e.status === `aborted`,
  NH = (e) => e.status === `dirty`,
  PH = (e) => e.status === `valid`,
  FH = (e) => typeof Promise < `u` && e instanceof Promise,
  G;
(function (e) {
  ((e.errToObj = (e) => (typeof e == `string` ? { message: e } : e || {})),
    (e.toString = (e) => (typeof e == `string` ? e : e?.message)));
})((G ||= {}));
var IH = class {
    constructor(e, t, n, r) {
      ((this._cachedPath = []),
        (this.parent = e),
        (this.data = t),
        (this._path = n),
        (this._key = r));
    }
    get path() {
      return (
        this._cachedPath.length ||
          (Array.isArray(this._key)
            ? this._cachedPath.push(...this._path, ...this._key)
            : this._cachedPath.push(...this._path, this._key)),
        this._cachedPath
      );
    }
  },
  LH = (e, t) => {
    if (PH(t)) return { success: !0, data: t.value };
    if (!e.common.issues.length) throw Error(`Validation failed but no issues detected.`);
    return {
      success: !1,
      get error() {
        return ((this._error ||= new wH(e.common.issues)), this._error);
      },
    };
  };
function K(e) {
  if (!e) return {};
  let { errorMap: t, invalid_type_error: n, required_error: r, description: i } = e;
  if (t && (n || r))
    throw Error(
      `Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`,
    );
  return t
    ? { errorMap: t, description: i }
    : {
        errorMap: (t, i) => {
          let { message: a } = e;
          return t.code === `invalid_enum_value`
            ? { message: a ?? i.defaultError }
            : i.data === void 0
              ? { message: a ?? r ?? i.defaultError }
              : t.code === `invalid_type`
                ? { message: a ?? n ?? i.defaultError }
                : { message: i.defaultError };
        },
        description: i,
      };
}
var q = class {
    get description() {
      return this._def.description;
    }
    _getType(e) {
      return CH(e.data);
    }
    _getOrReturnCtx(e, t) {
      return (
        t || {
          common: e.parent.common,
          data: e.data,
          parsedType: CH(e.data),
          schemaErrorMap: this._def.errorMap,
          path: e.path,
          parent: e.parent,
        }
      );
    }
    _processInputParams(e) {
      return {
        status: new kH(),
        ctx: {
          common: e.parent.common,
          data: e.data,
          parsedType: CH(e.data),
          schemaErrorMap: this._def.errorMap,
          path: e.path,
          parent: e.parent,
        },
      };
    }
    _parseSync(e) {
      let t = this._parse(e);
      if (FH(t)) throw Error(`Synchronous parse encountered promise.`);
      return t;
    }
    _parseAsync(e) {
      let t = this._parse(e);
      return Promise.resolve(t);
    }
    parse(e, t) {
      let n = this.safeParse(e, t);
      if (n.success) return n.data;
      throw n.error;
    }
    safeParse(e, t) {
      let n = {
        common: { issues: [], async: t?.async ?? !1, contextualErrorMap: t?.errorMap },
        path: t?.path || [],
        schemaErrorMap: this._def.errorMap,
        parent: null,
        data: e,
        parsedType: CH(e),
      };
      return LH(n, this._parseSync({ data: e, path: n.path, parent: n }));
    }
    "~validate"(e) {
      let t = {
        common: { issues: [], async: !!this[`~standard`].async },
        path: [],
        schemaErrorMap: this._def.errorMap,
        parent: null,
        data: e,
        parsedType: CH(e),
      };
      if (!this[`~standard`].async)
        try {
          let n = this._parseSync({ data: e, path: [], parent: t });
          return PH(n) ? { value: n.value } : { issues: t.common.issues };
        } catch (e) {
          (e?.message?.toLowerCase()?.includes(`encountered`) && (this[`~standard`].async = !0),
            (t.common = { issues: [], async: !0 }));
        }
      return this._parseAsync({ data: e, path: [], parent: t }).then((e) =>
        PH(e) ? { value: e.value } : { issues: t.common.issues },
      );
    }
    async parseAsync(e, t) {
      let n = await this.safeParseAsync(e, t);
      if (n.success) return n.data;
      throw n.error;
    }
    async safeParseAsync(e, t) {
      let n = {
          common: { issues: [], contextualErrorMap: t?.errorMap, async: !0 },
          path: t?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data: e,
          parsedType: CH(e),
        },
        r = this._parse({ data: e, path: n.path, parent: n });
      return LH(n, await (FH(r) ? r : Promise.resolve(r)));
    }
    refine(e, t) {
      let n = (e) =>
        typeof t == `string` || t === void 0 ? { message: t } : typeof t == `function` ? t(e) : t;
      return this._refinement((t, r) => {
        let i = e(t),
          a = () => r.addIssue({ code: H.custom, ...n(t) });
        return typeof Promise < `u` && i instanceof Promise
          ? i.then((e) => (e ? !0 : (a(), !1)))
          : i
            ? !0
            : (a(), !1);
      });
    }
    refinement(e, t) {
      return this._refinement((n, r) =>
        e(n) ? !0 : (r.addIssue(typeof t == `function` ? t(n, r) : t), !1),
      );
    }
    _refinement(e) {
      return new BU({
        schema: this,
        typeName: J.ZodEffects,
        effect: { type: `refinement`, refinement: e },
      });
    }
    superRefine(e) {
      return this._refinement(e);
    }
    constructor(e) {
      ((this.spa = this.safeParseAsync),
        (this._def = e),
        (this.parse = this.parse.bind(this)),
        (this.safeParse = this.safeParse.bind(this)),
        (this.parseAsync = this.parseAsync.bind(this)),
        (this.safeParseAsync = this.safeParseAsync.bind(this)),
        (this.spa = this.spa.bind(this)),
        (this.refine = this.refine.bind(this)),
        (this.refinement = this.refinement.bind(this)),
        (this.superRefine = this.superRefine.bind(this)),
        (this.optional = this.optional.bind(this)),
        (this.nullable = this.nullable.bind(this)),
        (this.nullish = this.nullish.bind(this)),
        (this.array = this.array.bind(this)),
        (this.promise = this.promise.bind(this)),
        (this.or = this.or.bind(this)),
        (this.and = this.and.bind(this)),
        (this.transform = this.transform.bind(this)),
        (this.brand = this.brand.bind(this)),
        (this.default = this.default.bind(this)),
        (this.catch = this.catch.bind(this)),
        (this.describe = this.describe.bind(this)),
        (this.pipe = this.pipe.bind(this)),
        (this.readonly = this.readonly.bind(this)),
        (this.isNullable = this.isNullable.bind(this)),
        (this.isOptional = this.isOptional.bind(this)),
        (this[`~standard`] = { version: 1, vendor: `zod`, validate: (e) => this[`~validate`](e) }));
    }
    optional() {
      return VU.create(this, this._def);
    }
    nullable() {
      return HU.create(this, this._def);
    }
    nullish() {
      return this.nullable().optional();
    }
    array() {
      return xU.create(this);
    }
    promise() {
      return zU.create(this, this._def);
    }
    or(e) {
      return wU.create([this, e], this._def);
    }
    and(e) {
      return OU.create(this, e, this._def);
    }
    transform(e) {
      return new BU({
        ...K(this._def),
        schema: this,
        typeName: J.ZodEffects,
        effect: { type: `transform`, transform: e },
      });
    }
    default(e) {
      let t = typeof e == `function` ? e : () => e;
      return new UU({ ...K(this._def), innerType: this, defaultValue: t, typeName: J.ZodDefault });
    }
    brand() {
      return new KU({ typeName: J.ZodBranded, type: this, ...K(this._def) });
    }
    catch(e) {
      let t = typeof e == `function` ? e : () => e;
      return new WU({ ...K(this._def), innerType: this, catchValue: t, typeName: J.ZodCatch });
    }
    describe(e) {
      let t = this.constructor;
      return new t({ ...this._def, description: e });
    }
    pipe(e) {
      return qU.create(this, e);
    }
    readonly() {
      return JU.create(this);
    }
    isOptional() {
      return this.safeParse(void 0).success;
    }
    isNullable() {
      return this.safeParse(null).success;
    }
  },
  RH = /^c[^\s-]{8,}$/i,
  zH = /^[0-9a-z]+$/,
  BH = /^[0-9A-HJKMNP-TV-Z]{26}$/i,
  VH = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i,
  HH = /^[a-z0-9_-]{21}$/i,
  UH = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/,
  WH =
    /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/,
  GH = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i,
  KH = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`,
  qH,
  JH =
    /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/,
  YH =
    /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/,
  XH =
    /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/,
  ZH =
    /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/,
  QH = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/,
  $H = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/,
  eU = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`,
  tU = RegExp(`^${eU}$`);
function nU(e) {
  let t = `[0-5]\\d`;
  e.precision ? (t = `${t}\\.\\d{${e.precision}}`) : (e.precision ?? (t = `${t}(\\.\\d+)?`));
  let n = e.precision ? `+` : `?`;
  return `([01]\\d|2[0-3]):[0-5]\\d(:${t})${n}`;
}
function rU(e) {
  return RegExp(`^${nU(e)}$`);
}
function iU(e) {
  let t = `${eU}T${nU(e)}`,
    n = [];
  return (
    n.push(e.local ? `Z?` : `Z`),
    e.offset && n.push(`([+-]\\d{2}:?\\d{2})`),
    (t = `${t}(${n.join(`|`)})`),
    RegExp(`^${t}$`)
  );
}
function aU(e, t) {
  return !!(((t === `v4` || !t) && JH.test(e)) || ((t === `v6` || !t) && XH.test(e)));
}
function oU(e, t) {
  if (!UH.test(e)) return !1;
  try {
    let [n] = e.split(`.`);
    if (!n) return !1;
    let r = n
        .replace(/-/g, `+`)
        .replace(/_/g, `/`)
        .padEnd(n.length + ((4 - (n.length % 4)) % 4), `=`),
      i = JSON.parse(atob(r));
    return !(
      typeof i != `object` ||
      !i ||
      (`typ` in i && i?.typ !== `JWT`) ||
      !i.alg ||
      (t && i.alg !== t)
    );
  } catch {
    return !1;
  }
}
function sU(e, t) {
  return !!(((t === `v4` || !t) && YH.test(e)) || ((t === `v6` || !t) && ZH.test(e)));
}
var cU = class e extends q {
  _parse(e) {
    if ((this._def.coerce && (e.data = String(e.data)), this._getType(e) !== V.string)) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.string, received: t.parsedType }), W);
    }
    let t = new kH(),
      n;
    for (let r of this._def.checks)
      if (r.kind === `min`)
        e.data.length < r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          U(n, {
            code: H.too_small,
            minimum: r.value,
            type: `string`,
            inclusive: !0,
            exact: !1,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `max`)
        e.data.length > r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          U(n, {
            code: H.too_big,
            maximum: r.value,
            type: `string`,
            inclusive: !0,
            exact: !1,
            message: r.message,
          }),
          t.dirty());
      else if (r.kind === `length`) {
        let i = e.data.length > r.value,
          a = e.data.length < r.value;
        (i || a) &&
          ((n = this._getOrReturnCtx(e, n)),
          i
            ? U(n, {
                code: H.too_big,
                maximum: r.value,
                type: `string`,
                inclusive: !0,
                exact: !0,
                message: r.message,
              })
            : a &&
              U(n, {
                code: H.too_small,
                minimum: r.value,
                type: `string`,
                inclusive: !0,
                exact: !0,
                message: r.message,
              }),
          t.dirty());
      } else if (r.kind === `email`)
        GH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `email`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `emoji`)
        ((qH ||= new RegExp(KH, `u`)),
          qH.test(e.data) ||
            ((n = this._getOrReturnCtx(e, n)),
            U(n, { validation: `emoji`, code: H.invalid_string, message: r.message }),
            t.dirty()));
      else if (r.kind === `uuid`)
        VH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `uuid`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `nanoid`)
        HH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `nanoid`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `cuid`)
        RH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `cuid`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `cuid2`)
        zH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `cuid2`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `ulid`)
        BH.test(e.data) ||
          ((n = this._getOrReturnCtx(e, n)),
          U(n, { validation: `ulid`, code: H.invalid_string, message: r.message }),
          t.dirty());
      else if (r.kind === `url`)
        try {
          new URL(e.data);
        } catch {
          ((n = this._getOrReturnCtx(e, n)),
            U(n, { validation: `url`, code: H.invalid_string, message: r.message }),
            t.dirty());
        }
      else
        r.kind === `regex`
          ? ((r.regex.lastIndex = 0),
            r.regex.test(e.data) ||
              ((n = this._getOrReturnCtx(e, n)),
              U(n, { validation: `regex`, code: H.invalid_string, message: r.message }),
              t.dirty()))
          : r.kind === `trim`
            ? (e.data = e.data.trim())
            : r.kind === `includes`
              ? e.data.includes(r.value, r.position) ||
                ((n = this._getOrReturnCtx(e, n)),
                U(n, {
                  code: H.invalid_string,
                  validation: { includes: r.value, position: r.position },
                  message: r.message,
                }),
                t.dirty())
              : r.kind === `toLowerCase`
                ? (e.data = e.data.toLowerCase())
                : r.kind === `toUpperCase`
                  ? (e.data = e.data.toUpperCase())
                  : r.kind === `startsWith`
                    ? e.data.startsWith(r.value) ||
                      ((n = this._getOrReturnCtx(e, n)),
                      U(n, {
                        code: H.invalid_string,
                        validation: { startsWith: r.value },
                        message: r.message,
                      }),
                      t.dirty())
                    : r.kind === `endsWith`
                      ? e.data.endsWith(r.value) ||
                        ((n = this._getOrReturnCtx(e, n)),
                        U(n, {
                          code: H.invalid_string,
                          validation: { endsWith: r.value },
                          message: r.message,
                        }),
                        t.dirty())
                      : r.kind === `datetime`
                        ? iU(r).test(e.data) ||
                          ((n = this._getOrReturnCtx(e, n)),
                          U(n, {
                            code: H.invalid_string,
                            validation: `datetime`,
                            message: r.message,
                          }),
                          t.dirty())
                        : r.kind === `date`
                          ? tU.test(e.data) ||
                            ((n = this._getOrReturnCtx(e, n)),
                            U(n, {
                              code: H.invalid_string,
                              validation: `date`,
                              message: r.message,
                            }),
                            t.dirty())
                          : r.kind === `time`
                            ? rU(r).test(e.data) ||
                              ((n = this._getOrReturnCtx(e, n)),
                              U(n, {
                                code: H.invalid_string,
                                validation: `time`,
                                message: r.message,
                              }),
                              t.dirty())
                            : r.kind === `duration`
                              ? WH.test(e.data) ||
                                ((n = this._getOrReturnCtx(e, n)),
                                U(n, {
                                  validation: `duration`,
                                  code: H.invalid_string,
                                  message: r.message,
                                }),
                                t.dirty())
                              : r.kind === `ip`
                                ? aU(e.data, r.version) ||
                                  ((n = this._getOrReturnCtx(e, n)),
                                  U(n, {
                                    validation: `ip`,
                                    code: H.invalid_string,
                                    message: r.message,
                                  }),
                                  t.dirty())
                                : r.kind === `jwt`
                                  ? oU(e.data, r.alg) ||
                                    ((n = this._getOrReturnCtx(e, n)),
                                    U(n, {
                                      validation: `jwt`,
                                      code: H.invalid_string,
                                      message: r.message,
                                    }),
                                    t.dirty())
                                  : r.kind === `cidr`
                                    ? sU(e.data, r.version) ||
                                      ((n = this._getOrReturnCtx(e, n)),
                                      U(n, {
                                        validation: `cidr`,
                                        code: H.invalid_string,
                                        message: r.message,
                                      }),
                                      t.dirty())
                                    : r.kind === `base64`
                                      ? QH.test(e.data) ||
                                        ((n = this._getOrReturnCtx(e, n)),
                                        U(n, {
                                          validation: `base64`,
                                          code: H.invalid_string,
                                          message: r.message,
                                        }),
                                        t.dirty())
                                      : r.kind === `base64url`
                                        ? $H.test(e.data) ||
                                          ((n = this._getOrReturnCtx(e, n)),
                                          U(n, {
                                            validation: `base64url`,
                                            code: H.invalid_string,
                                            message: r.message,
                                          }),
                                          t.dirty())
                                        : xH.assertNever(r);
    return { status: t.value, value: e.data };
  }
  _regex(e, t, n) {
    return this.refinement((t) => e.test(t), {
      validation: t,
      code: H.invalid_string,
      ...G.errToObj(n),
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  email(e) {
    return this._addCheck({ kind: `email`, ...G.errToObj(e) });
  }
  url(e) {
    return this._addCheck({ kind: `url`, ...G.errToObj(e) });
  }
  emoji(e) {
    return this._addCheck({ kind: `emoji`, ...G.errToObj(e) });
  }
  uuid(e) {
    return this._addCheck({ kind: `uuid`, ...G.errToObj(e) });
  }
  nanoid(e) {
    return this._addCheck({ kind: `nanoid`, ...G.errToObj(e) });
  }
  cuid(e) {
    return this._addCheck({ kind: `cuid`, ...G.errToObj(e) });
  }
  cuid2(e) {
    return this._addCheck({ kind: `cuid2`, ...G.errToObj(e) });
  }
  ulid(e) {
    return this._addCheck({ kind: `ulid`, ...G.errToObj(e) });
  }
  base64(e) {
    return this._addCheck({ kind: `base64`, ...G.errToObj(e) });
  }
  base64url(e) {
    return this._addCheck({ kind: `base64url`, ...G.errToObj(e) });
  }
  jwt(e) {
    return this._addCheck({ kind: `jwt`, ...G.errToObj(e) });
  }
  ip(e) {
    return this._addCheck({ kind: `ip`, ...G.errToObj(e) });
  }
  cidr(e) {
    return this._addCheck({ kind: `cidr`, ...G.errToObj(e) });
  }
  datetime(e) {
    return typeof e == `string`
      ? this._addCheck({ kind: `datetime`, precision: null, offset: !1, local: !1, message: e })
      : this._addCheck({
          kind: `datetime`,
          precision: e?.precision === void 0 ? null : e?.precision,
          offset: e?.offset ?? !1,
          local: e?.local ?? !1,
          ...G.errToObj(e?.message),
        });
  }
  date(e) {
    return this._addCheck({ kind: `date`, message: e });
  }
  time(e) {
    return typeof e == `string`
      ? this._addCheck({ kind: `time`, precision: null, message: e })
      : this._addCheck({
          kind: `time`,
          precision: e?.precision === void 0 ? null : e?.precision,
          ...G.errToObj(e?.message),
        });
  }
  duration(e) {
    return this._addCheck({ kind: `duration`, ...G.errToObj(e) });
  }
  regex(e, t) {
    return this._addCheck({ kind: `regex`, regex: e, ...G.errToObj(t) });
  }
  includes(e, t) {
    return this._addCheck({
      kind: `includes`,
      value: e,
      position: t?.position,
      ...G.errToObj(t?.message),
    });
  }
  startsWith(e, t) {
    return this._addCheck({ kind: `startsWith`, value: e, ...G.errToObj(t) });
  }
  endsWith(e, t) {
    return this._addCheck({ kind: `endsWith`, value: e, ...G.errToObj(t) });
  }
  min(e, t) {
    return this._addCheck({ kind: `min`, value: e, ...G.errToObj(t) });
  }
  max(e, t) {
    return this._addCheck({ kind: `max`, value: e, ...G.errToObj(t) });
  }
  length(e, t) {
    return this._addCheck({ kind: `length`, value: e, ...G.errToObj(t) });
  }
  nonempty(e) {
    return this.min(1, G.errToObj(e));
  }
  trim() {
    return new e({ ...this._def, checks: [...this._def.checks, { kind: `trim` }] });
  }
  toLowerCase() {
    return new e({ ...this._def, checks: [...this._def.checks, { kind: `toLowerCase` }] });
  }
  toUpperCase() {
    return new e({ ...this._def, checks: [...this._def.checks, { kind: `toUpperCase` }] });
  }
  get isDatetime() {
    return !!this._def.checks.find((e) => e.kind === `datetime`);
  }
  get isDate() {
    return !!this._def.checks.find((e) => e.kind === `date`);
  }
  get isTime() {
    return !!this._def.checks.find((e) => e.kind === `time`);
  }
  get isDuration() {
    return !!this._def.checks.find((e) => e.kind === `duration`);
  }
  get isEmail() {
    return !!this._def.checks.find((e) => e.kind === `email`);
  }
  get isURL() {
    return !!this._def.checks.find((e) => e.kind === `url`);
  }
  get isEmoji() {
    return !!this._def.checks.find((e) => e.kind === `emoji`);
  }
  get isUUID() {
    return !!this._def.checks.find((e) => e.kind === `uuid`);
  }
  get isNANOID() {
    return !!this._def.checks.find((e) => e.kind === `nanoid`);
  }
  get isCUID() {
    return !!this._def.checks.find((e) => e.kind === `cuid`);
  }
  get isCUID2() {
    return !!this._def.checks.find((e) => e.kind === `cuid2`);
  }
  get isULID() {
    return !!this._def.checks.find((e) => e.kind === `ulid`);
  }
  get isIP() {
    return !!this._def.checks.find((e) => e.kind === `ip`);
  }
  get isCIDR() {
    return !!this._def.checks.find((e) => e.kind === `cidr`);
  }
  get isBase64() {
    return !!this._def.checks.find((e) => e.kind === `base64`);
  }
  get isBase64url() {
    return !!this._def.checks.find((e) => e.kind === `base64url`);
  }
  get minLength() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxLength() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
};
cU.create = (e) => new cU({ checks: [], typeName: J.ZodString, coerce: e?.coerce ?? !1, ...K(e) });
function lU(e, t) {
  let n = (e.toString().split(`.`)[1] || ``).length,
    r = (t.toString().split(`.`)[1] || ``).length,
    i = n > r ? n : r;
  return (
    (Number.parseInt(e.toFixed(i).replace(`.`, ``)) %
      Number.parseInt(t.toFixed(i).replace(`.`, ``))) /
    10 ** i
  );
}
var uU = class e extends q {
  constructor() {
    (super(...arguments),
      (this.min = this.gte),
      (this.max = this.lte),
      (this.step = this.multipleOf));
  }
  _parse(e) {
    if ((this._def.coerce && (e.data = Number(e.data)), this._getType(e) !== V.number)) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.number, received: t.parsedType }), W);
    }
    let t,
      n = new kH();
    for (let r of this._def.checks)
      r.kind === `int`
        ? xH.isInteger(e.data) ||
          ((t = this._getOrReturnCtx(e, t)),
          U(t, {
            code: H.invalid_type,
            expected: `integer`,
            received: `float`,
            message: r.message,
          }),
          n.dirty())
        : r.kind === `min`
          ? (r.inclusive ? e.data < r.value : e.data <= r.value) &&
            ((t = this._getOrReturnCtx(e, t)),
            U(t, {
              code: H.too_small,
              minimum: r.value,
              type: `number`,
              inclusive: r.inclusive,
              exact: !1,
              message: r.message,
            }),
            n.dirty())
          : r.kind === `max`
            ? (r.inclusive ? e.data > r.value : e.data >= r.value) &&
              ((t = this._getOrReturnCtx(e, t)),
              U(t, {
                code: H.too_big,
                maximum: r.value,
                type: `number`,
                inclusive: r.inclusive,
                exact: !1,
                message: r.message,
              }),
              n.dirty())
            : r.kind === `multipleOf`
              ? lU(e.data, r.value) !== 0 &&
                ((t = this._getOrReturnCtx(e, t)),
                U(t, { code: H.not_multiple_of, multipleOf: r.value, message: r.message }),
                n.dirty())
              : r.kind === `finite`
                ? Number.isFinite(e.data) ||
                  ((t = this._getOrReturnCtx(e, t)),
                  U(t, { code: H.not_finite, message: r.message }),
                  n.dirty())
                : xH.assertNever(r);
    return { status: n.value, value: e.data };
  }
  gte(e, t) {
    return this.setLimit(`min`, e, !0, G.toString(t));
  }
  gt(e, t) {
    return this.setLimit(`min`, e, !1, G.toString(t));
  }
  lte(e, t) {
    return this.setLimit(`max`, e, !0, G.toString(t));
  }
  lt(e, t) {
    return this.setLimit(`max`, e, !1, G.toString(t));
  }
  setLimit(t, n, r, i) {
    return new e({
      ...this._def,
      checks: [...this._def.checks, { kind: t, value: n, inclusive: r, message: G.toString(i) }],
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  int(e) {
    return this._addCheck({ kind: `int`, message: G.toString(e) });
  }
  positive(e) {
    return this._addCheck({ kind: `min`, value: 0, inclusive: !1, message: G.toString(e) });
  }
  negative(e) {
    return this._addCheck({ kind: `max`, value: 0, inclusive: !1, message: G.toString(e) });
  }
  nonpositive(e) {
    return this._addCheck({ kind: `max`, value: 0, inclusive: !0, message: G.toString(e) });
  }
  nonnegative(e) {
    return this._addCheck({ kind: `min`, value: 0, inclusive: !0, message: G.toString(e) });
  }
  multipleOf(e, t) {
    return this._addCheck({ kind: `multipleOf`, value: e, message: G.toString(t) });
  }
  finite(e) {
    return this._addCheck({ kind: `finite`, message: G.toString(e) });
  }
  safe(e) {
    return this._addCheck({
      kind: `min`,
      inclusive: !0,
      value: -(2 ** 53 - 1),
      message: G.toString(e),
    })._addCheck({ kind: `max`, inclusive: !0, value: 2 ** 53 - 1, message: G.toString(e) });
  }
  get minValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
  get isInt() {
    return !!this._def.checks.find(
      (e) => e.kind === `int` || (e.kind === `multipleOf` && xH.isInteger(e.value)),
    );
  }
  get isFinite() {
    let e = null,
      t = null;
    for (let n of this._def.checks)
      if (n.kind === `finite` || n.kind === `int` || n.kind === `multipleOf`) return !0;
      else
        n.kind === `min`
          ? (t === null || n.value > t) && (t = n.value)
          : n.kind === `max` && (e === null || n.value < e) && (e = n.value);
    return Number.isFinite(t) && Number.isFinite(e);
  }
};
uU.create = (e) => new uU({ checks: [], typeName: J.ZodNumber, coerce: e?.coerce || !1, ...K(e) });
var dU = class e extends q {
  constructor() {
    (super(...arguments), (this.min = this.gte), (this.max = this.lte));
  }
  _parse(e) {
    if (this._def.coerce)
      try {
        e.data = BigInt(e.data);
      } catch {
        return this._getInvalidInput(e);
      }
    if (this._getType(e) !== V.bigint) return this._getInvalidInput(e);
    let t,
      n = new kH();
    for (let r of this._def.checks)
      r.kind === `min`
        ? (r.inclusive ? e.data < r.value : e.data <= r.value) &&
          ((t = this._getOrReturnCtx(e, t)),
          U(t, {
            code: H.too_small,
            type: `bigint`,
            minimum: r.value,
            inclusive: r.inclusive,
            message: r.message,
          }),
          n.dirty())
        : r.kind === `max`
          ? (r.inclusive ? e.data > r.value : e.data >= r.value) &&
            ((t = this._getOrReturnCtx(e, t)),
            U(t, {
              code: H.too_big,
              type: `bigint`,
              maximum: r.value,
              inclusive: r.inclusive,
              message: r.message,
            }),
            n.dirty())
          : r.kind === `multipleOf`
            ? e.data % r.value !== BigInt(0) &&
              ((t = this._getOrReturnCtx(e, t)),
              U(t, { code: H.not_multiple_of, multipleOf: r.value, message: r.message }),
              n.dirty())
            : xH.assertNever(r);
    return { status: n.value, value: e.data };
  }
  _getInvalidInput(e) {
    let t = this._getOrReturnCtx(e);
    return (U(t, { code: H.invalid_type, expected: V.bigint, received: t.parsedType }), W);
  }
  gte(e, t) {
    return this.setLimit(`min`, e, !0, G.toString(t));
  }
  gt(e, t) {
    return this.setLimit(`min`, e, !1, G.toString(t));
  }
  lte(e, t) {
    return this.setLimit(`max`, e, !0, G.toString(t));
  }
  lt(e, t) {
    return this.setLimit(`max`, e, !1, G.toString(t));
  }
  setLimit(t, n, r, i) {
    return new e({
      ...this._def,
      checks: [...this._def.checks, { kind: t, value: n, inclusive: r, message: G.toString(i) }],
    });
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  positive(e) {
    return this._addCheck({ kind: `min`, value: BigInt(0), inclusive: !1, message: G.toString(e) });
  }
  negative(e) {
    return this._addCheck({ kind: `max`, value: BigInt(0), inclusive: !1, message: G.toString(e) });
  }
  nonpositive(e) {
    return this._addCheck({ kind: `max`, value: BigInt(0), inclusive: !0, message: G.toString(e) });
  }
  nonnegative(e) {
    return this._addCheck({ kind: `min`, value: BigInt(0), inclusive: !0, message: G.toString(e) });
  }
  multipleOf(e, t) {
    return this._addCheck({ kind: `multipleOf`, value: e, message: G.toString(t) });
  }
  get minValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e;
  }
  get maxValue() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e;
  }
};
dU.create = (e) => new dU({ checks: [], typeName: J.ZodBigInt, coerce: e?.coerce ?? !1, ...K(e) });
var fU = class extends q {
  _parse(e) {
    if ((this._def.coerce && (e.data = !!e.data), this._getType(e) !== V.boolean)) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.boolean, received: t.parsedType }), W);
    }
    return jH(e.data);
  }
};
fU.create = (e) => new fU({ typeName: J.ZodBoolean, coerce: e?.coerce || !1, ...K(e) });
var pU = class e extends q {
  _parse(e) {
    if ((this._def.coerce && (e.data = new Date(e.data)), this._getType(e) !== V.date)) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.date, received: t.parsedType }), W);
    }
    if (Number.isNaN(e.data.getTime()))
      return (U(this._getOrReturnCtx(e), { code: H.invalid_date }), W);
    let t = new kH(),
      n;
    for (let r of this._def.checks)
      r.kind === `min`
        ? e.data.getTime() < r.value &&
          ((n = this._getOrReturnCtx(e, n)),
          U(n, {
            code: H.too_small,
            message: r.message,
            inclusive: !0,
            exact: !1,
            minimum: r.value,
            type: `date`,
          }),
          t.dirty())
        : r.kind === `max`
          ? e.data.getTime() > r.value &&
            ((n = this._getOrReturnCtx(e, n)),
            U(n, {
              code: H.too_big,
              message: r.message,
              inclusive: !0,
              exact: !1,
              maximum: r.value,
              type: `date`,
            }),
            t.dirty())
          : xH.assertNever(r);
    return { status: t.value, value: new Date(e.data.getTime()) };
  }
  _addCheck(t) {
    return new e({ ...this._def, checks: [...this._def.checks, t] });
  }
  min(e, t) {
    return this._addCheck({ kind: `min`, value: e.getTime(), message: G.toString(t) });
  }
  max(e, t) {
    return this._addCheck({ kind: `max`, value: e.getTime(), message: G.toString(t) });
  }
  get minDate() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `min` && (e === null || t.value > e) && (e = t.value);
    return e == null ? null : new Date(e);
  }
  get maxDate() {
    let e = null;
    for (let t of this._def.checks)
      t.kind === `max` && (e === null || t.value < e) && (e = t.value);
    return e == null ? null : new Date(e);
  }
};
pU.create = (e) => new pU({ checks: [], coerce: e?.coerce || !1, typeName: J.ZodDate, ...K(e) });
var mU = class extends q {
  _parse(e) {
    if (this._getType(e) !== V.symbol) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.symbol, received: t.parsedType }), W);
    }
    return jH(e.data);
  }
};
mU.create = (e) => new mU({ typeName: J.ZodSymbol, ...K(e) });
var hU = class extends q {
  _parse(e) {
    if (this._getType(e) !== V.undefined) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.undefined, received: t.parsedType }), W);
    }
    return jH(e.data);
  }
};
hU.create = (e) => new hU({ typeName: J.ZodUndefined, ...K(e) });
var gU = class extends q {
  _parse(e) {
    if (this._getType(e) !== V.null) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.null, received: t.parsedType }), W);
    }
    return jH(e.data);
  }
};
gU.create = (e) => new gU({ typeName: J.ZodNull, ...K(e) });
var _U = class extends q {
  constructor() {
    (super(...arguments), (this._any = !0));
  }
  _parse(e) {
    return jH(e.data);
  }
};
_U.create = (e) => new _U({ typeName: J.ZodAny, ...K(e) });
var vU = class extends q {
  constructor() {
    (super(...arguments), (this._unknown = !0));
  }
  _parse(e) {
    return jH(e.data);
  }
};
vU.create = (e) => new vU({ typeName: J.ZodUnknown, ...K(e) });
var yU = class extends q {
  _parse(e) {
    let t = this._getOrReturnCtx(e);
    return (U(t, { code: H.invalid_type, expected: V.never, received: t.parsedType }), W);
  }
};
yU.create = (e) => new yU({ typeName: J.ZodNever, ...K(e) });
var bU = class extends q {
  _parse(e) {
    if (this._getType(e) !== V.undefined) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.void, received: t.parsedType }), W);
    }
    return jH(e.data);
  }
};
bU.create = (e) => new bU({ typeName: J.ZodVoid, ...K(e) });
var xU = class e extends q {
  _parse(e) {
    let { ctx: t, status: n } = this._processInputParams(e),
      r = this._def;
    if (t.parsedType !== V.array)
      return (U(t, { code: H.invalid_type, expected: V.array, received: t.parsedType }), W);
    if (r.exactLength !== null) {
      let e = t.data.length > r.exactLength.value,
        i = t.data.length < r.exactLength.value;
      (e || i) &&
        (U(t, {
          code: e ? H.too_big : H.too_small,
          minimum: i ? r.exactLength.value : void 0,
          maximum: e ? r.exactLength.value : void 0,
          type: `array`,
          inclusive: !0,
          exact: !0,
          message: r.exactLength.message,
        }),
        n.dirty());
    }
    if (
      (r.minLength !== null &&
        t.data.length < r.minLength.value &&
        (U(t, {
          code: H.too_small,
          minimum: r.minLength.value,
          type: `array`,
          inclusive: !0,
          exact: !1,
          message: r.minLength.message,
        }),
        n.dirty()),
      r.maxLength !== null &&
        t.data.length > r.maxLength.value &&
        (U(t, {
          code: H.too_big,
          maximum: r.maxLength.value,
          type: `array`,
          inclusive: !0,
          exact: !1,
          message: r.maxLength.message,
        }),
        n.dirty()),
      t.common.async)
    )
      return Promise.all(
        [...t.data].map((e, n) => r.type._parseAsync(new IH(t, e, t.path, n))),
      ).then((e) => kH.mergeArray(n, e));
    let i = [...t.data].map((e, n) => r.type._parseSync(new IH(t, e, t.path, n)));
    return kH.mergeArray(n, i);
  }
  get element() {
    return this._def.type;
  }
  min(t, n) {
    return new e({ ...this._def, minLength: { value: t, message: G.toString(n) } });
  }
  max(t, n) {
    return new e({ ...this._def, maxLength: { value: t, message: G.toString(n) } });
  }
  length(t, n) {
    return new e({ ...this._def, exactLength: { value: t, message: G.toString(n) } });
  }
  nonempty(e) {
    return this.min(1, e);
  }
};
xU.create = (e, t) =>
  new xU({
    type: e,
    minLength: null,
    maxLength: null,
    exactLength: null,
    typeName: J.ZodArray,
    ...K(t),
  });
function SU(e) {
  if (e instanceof CU) {
    let t = {};
    for (let n in e.shape) {
      let r = e.shape[n];
      t[n] = VU.create(SU(r));
    }
    return new CU({ ...e._def, shape: () => t });
  } else if (e instanceof xU) return new xU({ ...e._def, type: SU(e.element) });
  else if (e instanceof VU) return VU.create(SU(e.unwrap()));
  else if (e instanceof HU) return HU.create(SU(e.unwrap()));
  else if (e instanceof kU) return kU.create(e.items.map((e) => SU(e)));
  else return e;
}
var CU = class e extends q {
  constructor() {
    (super(...arguments),
      (this._cached = null),
      (this.nonstrict = this.passthrough),
      (this.augment = this.extend));
  }
  _getCached() {
    if (this._cached !== null) return this._cached;
    let e = this._def.shape();
    return ((this._cached = { shape: e, keys: xH.objectKeys(e) }), this._cached);
  }
  _parse(e) {
    if (this._getType(e) !== V.object) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.object, received: t.parsedType }), W);
    }
    let { status: t, ctx: n } = this._processInputParams(e),
      { shape: r, keys: i } = this._getCached(),
      a = [];
    if (!(this._def.catchall instanceof yU && this._def.unknownKeys === `strip`))
      for (let e in n.data) i.includes(e) || a.push(e);
    let o = [];
    for (let e of i) {
      let t = r[e],
        i = n.data[e];
      o.push({
        key: { status: `valid`, value: e },
        value: t._parse(new IH(n, i, n.path, e)),
        alwaysSet: e in n.data,
      });
    }
    if (this._def.catchall instanceof yU) {
      let e = this._def.unknownKeys;
      if (e === `passthrough`)
        for (let e of a)
          o.push({
            key: { status: `valid`, value: e },
            value: { status: `valid`, value: n.data[e] },
          });
      else if (e === `strict`)
        a.length > 0 && (U(n, { code: H.unrecognized_keys, keys: a }), t.dirty());
      else if (e !== `strip`) throw Error(`Internal ZodObject error: invalid unknownKeys value.`);
    } else {
      let e = this._def.catchall;
      for (let t of a) {
        let r = n.data[t];
        o.push({
          key: { status: `valid`, value: t },
          value: e._parse(new IH(n, r, n.path, t)),
          alwaysSet: t in n.data,
        });
      }
    }
    return n.common.async
      ? Promise.resolve()
          .then(async () => {
            let e = [];
            for (let t of o) {
              let n = await t.key,
                r = await t.value;
              e.push({ key: n, value: r, alwaysSet: t.alwaysSet });
            }
            return e;
          })
          .then((e) => kH.mergeObjectSync(t, e))
      : kH.mergeObjectSync(t, o);
  }
  get shape() {
    return this._def.shape();
  }
  strict(t) {
    return (
      G.errToObj,
      new e({
        ...this._def,
        unknownKeys: `strict`,
        ...(t === void 0
          ? {}
          : {
              errorMap: (e, n) => {
                let r = this._def.errorMap?.(e, n).message ?? n.defaultError;
                return e.code === `unrecognized_keys`
                  ? { message: G.errToObj(t).message ?? r }
                  : { message: r };
              },
            }),
      })
    );
  }
  strip() {
    return new e({ ...this._def, unknownKeys: `strip` });
  }
  passthrough() {
    return new e({ ...this._def, unknownKeys: `passthrough` });
  }
  extend(t) {
    return new e({ ...this._def, shape: () => ({ ...this._def.shape(), ...t }) });
  }
  merge(t) {
    return new e({
      unknownKeys: t._def.unknownKeys,
      catchall: t._def.catchall,
      shape: () => ({ ...this._def.shape(), ...t._def.shape() }),
      typeName: J.ZodObject,
    });
  }
  setKey(e, t) {
    return this.augment({ [e]: t });
  }
  catchall(t) {
    return new e({ ...this._def, catchall: t });
  }
  pick(t) {
    let n = {};
    for (let e of xH.objectKeys(t)) t[e] && this.shape[e] && (n[e] = this.shape[e]);
    return new e({ ...this._def, shape: () => n });
  }
  omit(t) {
    let n = {};
    for (let e of xH.objectKeys(this.shape)) t[e] || (n[e] = this.shape[e]);
    return new e({ ...this._def, shape: () => n });
  }
  deepPartial() {
    return SU(this);
  }
  partial(t) {
    let n = {};
    for (let e of xH.objectKeys(this.shape)) {
      let r = this.shape[e];
      t && !t[e] ? (n[e] = r) : (n[e] = r.optional());
    }
    return new e({ ...this._def, shape: () => n });
  }
  required(t) {
    let n = {};
    for (let e of xH.objectKeys(this.shape))
      if (t && !t[e]) n[e] = this.shape[e];
      else {
        let t = this.shape[e];
        for (; t instanceof VU; ) t = t._def.innerType;
        n[e] = t;
      }
    return new e({ ...this._def, shape: () => n });
  }
  keyof() {
    return IU(xH.objectKeys(this.shape));
  }
};
((CU.create = (e, t) =>
  new CU({
    shape: () => e,
    unknownKeys: `strip`,
    catchall: yU.create(),
    typeName: J.ZodObject,
    ...K(t),
  })),
  (CU.strictCreate = (e, t) =>
    new CU({
      shape: () => e,
      unknownKeys: `strict`,
      catchall: yU.create(),
      typeName: J.ZodObject,
      ...K(t),
    })),
  (CU.lazycreate = (e, t) =>
    new CU({
      shape: e,
      unknownKeys: `strip`,
      catchall: yU.create(),
      typeName: J.ZodObject,
      ...K(t),
    })));
var wU = class extends q {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = this._def.options;
    function r(e) {
      for (let t of e) if (t.result.status === `valid`) return t.result;
      for (let n of e)
        if (n.result.status === `dirty`)
          return (t.common.issues.push(...n.ctx.common.issues), n.result);
      let n = e.map((e) => new wH(e.ctx.common.issues));
      return (U(t, { code: H.invalid_union, unionErrors: n }), W);
    }
    if (t.common.async)
      return Promise.all(
        n.map(async (e) => {
          let n = { ...t, common: { ...t.common, issues: [] }, parent: null };
          return { result: await e._parseAsync({ data: t.data, path: t.path, parent: n }), ctx: n };
        }),
      ).then(r);
    {
      let e,
        r = [];
      for (let i of n) {
        let n = { ...t, common: { ...t.common, issues: [] }, parent: null },
          a = i._parseSync({ data: t.data, path: t.path, parent: n });
        if (a.status === `valid`) return a;
        (a.status === `dirty` && !e && (e = { result: a, ctx: n }),
          n.common.issues.length && r.push(n.common.issues));
      }
      if (e) return (t.common.issues.push(...e.ctx.common.issues), e.result);
      let i = r.map((e) => new wH(e));
      return (U(t, { code: H.invalid_union, unionErrors: i }), W);
    }
  }
  get options() {
    return this._def.options;
  }
};
wU.create = (e, t) => new wU({ options: e, typeName: J.ZodUnion, ...K(t) });
var TU = (e) =>
    e instanceof PU
      ? TU(e.schema)
      : e instanceof BU
        ? TU(e.innerType())
        : e instanceof FU
          ? [e.value]
          : e instanceof LU
            ? e.options
            : e instanceof RU
              ? xH.objectValues(e.enum)
              : e instanceof UU
                ? TU(e._def.innerType)
                : e instanceof hU
                  ? [void 0]
                  : e instanceof gU
                    ? [null]
                    : e instanceof VU
                      ? [void 0, ...TU(e.unwrap())]
                      : e instanceof HU
                        ? [null, ...TU(e.unwrap())]
                        : e instanceof KU || e instanceof JU
                          ? TU(e.unwrap())
                          : e instanceof WU
                            ? TU(e._def.innerType)
                            : [],
  EU = class e extends q {
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      if (t.parsedType !== V.object)
        return (U(t, { code: H.invalid_type, expected: V.object, received: t.parsedType }), W);
      let n = this.discriminator,
        r = t.data[n],
        i = this.optionsMap.get(r);
      return i
        ? t.common.async
          ? i._parseAsync({ data: t.data, path: t.path, parent: t })
          : i._parseSync({ data: t.data, path: t.path, parent: t })
        : (U(t, {
            code: H.invalid_union_discriminator,
            options: Array.from(this.optionsMap.keys()),
            path: [n],
          }),
          W);
    }
    get discriminator() {
      return this._def.discriminator;
    }
    get options() {
      return this._def.options;
    }
    get optionsMap() {
      return this._def.optionsMap;
    }
    static create(t, n, r) {
      let i = new Map();
      for (let e of n) {
        let n = TU(e.shape[t]);
        if (!n.length)
          throw Error(
            `A discriminator value for key \`${t}\` could not be extracted from all schema options`,
          );
        for (let r of n) {
          if (i.has(r))
            throw Error(`Discriminator property ${String(t)} has duplicate value ${String(r)}`);
          i.set(r, e);
        }
      }
      return new e({
        typeName: J.ZodDiscriminatedUnion,
        discriminator: t,
        options: n,
        optionsMap: i,
        ...K(r),
      });
    }
  };
function DU(e, t) {
  let n = CH(e),
    r = CH(t);
  if (e === t) return { valid: !0, data: e };
  if (n === V.object && r === V.object) {
    let n = xH.objectKeys(t),
      r = xH.objectKeys(e).filter((e) => n.indexOf(e) !== -1),
      i = { ...e, ...t };
    for (let n of r) {
      let r = DU(e[n], t[n]);
      if (!r.valid) return { valid: !1 };
      i[n] = r.data;
    }
    return { valid: !0, data: i };
  } else if (n === V.array && r === V.array) {
    if (e.length !== t.length) return { valid: !1 };
    let n = [];
    for (let r = 0; r < e.length; r++) {
      let i = e[r],
        a = t[r],
        o = DU(i, a);
      if (!o.valid) return { valid: !1 };
      n.push(o.data);
    }
    return { valid: !0, data: n };
  } else if (n === V.date && r === V.date && +e == +t) return { valid: !0, data: e };
  else return { valid: !1 };
}
var OU = class extends q {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e),
      r = (e, r) => {
        if (MH(e) || MH(r)) return W;
        let i = DU(e.value, r.value);
        return i.valid
          ? ((NH(e) || NH(r)) && t.dirty(), { status: t.value, value: i.data })
          : (U(n, { code: H.invalid_intersection_types }), W);
      };
    return n.common.async
      ? Promise.all([
          this._def.left._parseAsync({ data: n.data, path: n.path, parent: n }),
          this._def.right._parseAsync({ data: n.data, path: n.path, parent: n }),
        ]).then(([e, t]) => r(e, t))
      : r(
          this._def.left._parseSync({ data: n.data, path: n.path, parent: n }),
          this._def.right._parseSync({ data: n.data, path: n.path, parent: n }),
        );
  }
};
OU.create = (e, t, n) => new OU({ left: e, right: t, typeName: J.ZodIntersection, ...K(n) });
var kU = class e extends q {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e);
    if (n.parsedType !== V.array)
      return (U(n, { code: H.invalid_type, expected: V.array, received: n.parsedType }), W);
    if (n.data.length < this._def.items.length)
      return (
        U(n, {
          code: H.too_small,
          minimum: this._def.items.length,
          inclusive: !0,
          exact: !1,
          type: `array`,
        }),
        W
      );
    !this._def.rest &&
      n.data.length > this._def.items.length &&
      (U(n, {
        code: H.too_big,
        maximum: this._def.items.length,
        inclusive: !0,
        exact: !1,
        type: `array`,
      }),
      t.dirty());
    let r = [...n.data]
      .map((e, t) => {
        let r = this._def.items[t] || this._def.rest;
        return r ? r._parse(new IH(n, e, n.path, t)) : null;
      })
      .filter((e) => !!e);
    return n.common.async ? Promise.all(r).then((e) => kH.mergeArray(t, e)) : kH.mergeArray(t, r);
  }
  get items() {
    return this._def.items;
  }
  rest(t) {
    return new e({ ...this._def, rest: t });
  }
};
kU.create = (e, t) => {
  if (!Array.isArray(e)) throw Error(`You must pass an array of schemas to z.tuple([ ... ])`);
  return new kU({ items: e, typeName: J.ZodTuple, rest: null, ...K(t) });
};
var AU = class e extends q {
    get keySchema() {
      return this._def.keyType;
    }
    get valueSchema() {
      return this._def.valueType;
    }
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.parsedType !== V.object)
        return (U(n, { code: H.invalid_type, expected: V.object, received: n.parsedType }), W);
      let r = [],
        i = this._def.keyType,
        a = this._def.valueType;
      for (let e in n.data)
        r.push({
          key: i._parse(new IH(n, e, n.path, e)),
          value: a._parse(new IH(n, n.data[e], n.path, e)),
          alwaysSet: e in n.data,
        });
      return n.common.async ? kH.mergeObjectAsync(t, r) : kH.mergeObjectSync(t, r);
    }
    get element() {
      return this._def.valueType;
    }
    static create(t, n, r) {
      return n instanceof q
        ? new e({ keyType: t, valueType: n, typeName: J.ZodRecord, ...K(r) })
        : new e({ keyType: cU.create(), valueType: t, typeName: J.ZodRecord, ...K(n) });
    }
  },
  jU = class extends q {
    get keySchema() {
      return this._def.keyType;
    }
    get valueSchema() {
      return this._def.valueType;
    }
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.parsedType !== V.map)
        return (U(n, { code: H.invalid_type, expected: V.map, received: n.parsedType }), W);
      let r = this._def.keyType,
        i = this._def.valueType,
        a = [...n.data.entries()].map(([e, t], a) => ({
          key: r._parse(new IH(n, e, n.path, [a, `key`])),
          value: i._parse(new IH(n, t, n.path, [a, `value`])),
        }));
      if (n.common.async) {
        let e = new Map();
        return Promise.resolve().then(async () => {
          for (let n of a) {
            let r = await n.key,
              i = await n.value;
            if (r.status === `aborted` || i.status === `aborted`) return W;
            ((r.status === `dirty` || i.status === `dirty`) && t.dirty(), e.set(r.value, i.value));
          }
          return { status: t.value, value: e };
        });
      } else {
        let e = new Map();
        for (let n of a) {
          let r = n.key,
            i = n.value;
          if (r.status === `aborted` || i.status === `aborted`) return W;
          ((r.status === `dirty` || i.status === `dirty`) && t.dirty(), e.set(r.value, i.value));
        }
        return { status: t.value, value: e };
      }
    }
  };
jU.create = (e, t, n) => new jU({ valueType: t, keyType: e, typeName: J.ZodMap, ...K(n) });
var MU = class e extends q {
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e);
    if (n.parsedType !== V.set)
      return (U(n, { code: H.invalid_type, expected: V.set, received: n.parsedType }), W);
    let r = this._def;
    (r.minSize !== null &&
      n.data.size < r.minSize.value &&
      (U(n, {
        code: H.too_small,
        minimum: r.minSize.value,
        type: `set`,
        inclusive: !0,
        exact: !1,
        message: r.minSize.message,
      }),
      t.dirty()),
      r.maxSize !== null &&
        n.data.size > r.maxSize.value &&
        (U(n, {
          code: H.too_big,
          maximum: r.maxSize.value,
          type: `set`,
          inclusive: !0,
          exact: !1,
          message: r.maxSize.message,
        }),
        t.dirty()));
    let i = this._def.valueType;
    function a(e) {
      let n = new Set();
      for (let r of e) {
        if (r.status === `aborted`) return W;
        (r.status === `dirty` && t.dirty(), n.add(r.value));
      }
      return { status: t.value, value: n };
    }
    let o = [...n.data.values()].map((e, t) => i._parse(new IH(n, e, n.path, t)));
    return n.common.async ? Promise.all(o).then((e) => a(e)) : a(o);
  }
  min(t, n) {
    return new e({ ...this._def, minSize: { value: t, message: G.toString(n) } });
  }
  max(t, n) {
    return new e({ ...this._def, maxSize: { value: t, message: G.toString(n) } });
  }
  size(e, t) {
    return this.min(e, t).max(e, t);
  }
  nonempty(e) {
    return this.min(1, e);
  }
};
MU.create = (e, t) =>
  new MU({ valueType: e, minSize: null, maxSize: null, typeName: J.ZodSet, ...K(t) });
var NU = class e extends q {
    constructor() {
      (super(...arguments), (this.validate = this.implement));
    }
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      if (t.parsedType !== V.function)
        return (U(t, { code: H.invalid_type, expected: V.function, received: t.parsedType }), W);
      function n(e, n) {
        return OH({
          data: e,
          path: t.path,
          errorMaps: [t.common.contextualErrorMap, t.schemaErrorMap, DH(), TH].filter((e) => !!e),
          issueData: { code: H.invalid_arguments, argumentsError: n },
        });
      }
      function r(e, n) {
        return OH({
          data: e,
          path: t.path,
          errorMaps: [t.common.contextualErrorMap, t.schemaErrorMap, DH(), TH].filter((e) => !!e),
          issueData: { code: H.invalid_return_type, returnTypeError: n },
        });
      }
      let i = { errorMap: t.common.contextualErrorMap },
        a = t.data;
      if (this._def.returns instanceof zU) {
        let e = this;
        return jH(async function (...t) {
          let o = new wH([]),
            s = await e._def.args.parseAsync(t, i).catch((e) => {
              throw (o.addIssue(n(t, e)), o);
            }),
            c = await Reflect.apply(a, this, s);
          return await e._def.returns._def.type.parseAsync(c, i).catch((e) => {
            throw (o.addIssue(r(c, e)), o);
          });
        });
      } else {
        let e = this;
        return jH(function (...t) {
          let o = e._def.args.safeParse(t, i);
          if (!o.success) throw new wH([n(t, o.error)]);
          let s = Reflect.apply(a, this, o.data),
            c = e._def.returns.safeParse(s, i);
          if (!c.success) throw new wH([r(s, c.error)]);
          return c.data;
        });
      }
    }
    parameters() {
      return this._def.args;
    }
    returnType() {
      return this._def.returns;
    }
    args(...t) {
      return new e({ ...this._def, args: kU.create(t).rest(vU.create()) });
    }
    returns(t) {
      return new e({ ...this._def, returns: t });
    }
    implement(e) {
      return this.parse(e);
    }
    strictImplement(e) {
      return this.parse(e);
    }
    static create(t, n, r) {
      return new e({
        args: t || kU.create([]).rest(vU.create()),
        returns: n || vU.create(),
        typeName: J.ZodFunction,
        ...K(r),
      });
    }
  },
  PU = class extends q {
    get schema() {
      return this._def.getter();
    }
    _parse(e) {
      let { ctx: t } = this._processInputParams(e);
      return this._def.getter()._parse({ data: t.data, path: t.path, parent: t });
    }
  };
PU.create = (e, t) => new PU({ getter: e, typeName: J.ZodLazy, ...K(t) });
var FU = class extends q {
  _parse(e) {
    if (e.data !== this._def.value) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { received: t.data, code: H.invalid_literal, expected: this._def.value }), W);
    }
    return { status: `valid`, value: e.data };
  }
  get value() {
    return this._def.value;
  }
};
FU.create = (e, t) => new FU({ value: e, typeName: J.ZodLiteral, ...K(t) });
function IU(e, t) {
  return new LU({ values: e, typeName: J.ZodEnum, ...K(t) });
}
var LU = class e extends q {
  _parse(e) {
    if (typeof e.data != `string`) {
      let t = this._getOrReturnCtx(e),
        n = this._def.values;
      return (
        U(t, { expected: xH.joinValues(n), received: t.parsedType, code: H.invalid_type }), W
      );
    }
    if (((this._cache ||= new Set(this._def.values)), !this._cache.has(e.data))) {
      let t = this._getOrReturnCtx(e),
        n = this._def.values;
      return (U(t, { received: t.data, code: H.invalid_enum_value, options: n }), W);
    }
    return jH(e.data);
  }
  get options() {
    return this._def.values;
  }
  get enum() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  get Values() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  get Enum() {
    let e = {};
    for (let t of this._def.values) e[t] = t;
    return e;
  }
  extract(t, n = this._def) {
    return e.create(t, { ...this._def, ...n });
  }
  exclude(t, n = this._def) {
    return e.create(
      this.options.filter((e) => !t.includes(e)),
      { ...this._def, ...n },
    );
  }
};
LU.create = IU;
var RU = class extends q {
  _parse(e) {
    let t = xH.getValidEnumValues(this._def.values),
      n = this._getOrReturnCtx(e);
    if (n.parsedType !== V.string && n.parsedType !== V.number) {
      let e = xH.objectValues(t);
      return (
        U(n, { expected: xH.joinValues(e), received: n.parsedType, code: H.invalid_type }), W
      );
    }
    if (
      ((this._cache ||= new Set(xH.getValidEnumValues(this._def.values))), !this._cache.has(e.data))
    ) {
      let e = xH.objectValues(t);
      return (U(n, { received: n.data, code: H.invalid_enum_value, options: e }), W);
    }
    return jH(e.data);
  }
  get enum() {
    return this._def.values;
  }
};
RU.create = (e, t) => new RU({ values: e, typeName: J.ZodNativeEnum, ...K(t) });
var zU = class extends q {
  unwrap() {
    return this._def.type;
  }
  _parse(e) {
    let { ctx: t } = this._processInputParams(e);
    return t.parsedType !== V.promise && t.common.async === !1
      ? (U(t, { code: H.invalid_type, expected: V.promise, received: t.parsedType }), W)
      : jH(
          (t.parsedType === V.promise ? t.data : Promise.resolve(t.data)).then((e) =>
            this._def.type.parseAsync(e, { path: t.path, errorMap: t.common.contextualErrorMap }),
          ),
        );
  }
};
zU.create = (e, t) => new zU({ type: e, typeName: J.ZodPromise, ...K(t) });
var BU = class extends q {
  innerType() {
    return this._def.schema;
  }
  sourceType() {
    return this._def.schema._def.typeName === J.ZodEffects
      ? this._def.schema.sourceType()
      : this._def.schema;
  }
  _parse(e) {
    let { status: t, ctx: n } = this._processInputParams(e),
      r = this._def.effect || null,
      i = {
        addIssue: (e) => {
          (U(n, e), e.fatal ? t.abort() : t.dirty());
        },
        get path() {
          return n.path;
        },
      };
    if (((i.addIssue = i.addIssue.bind(i)), r.type === `preprocess`)) {
      let e = r.transform(n.data, i);
      if (n.common.async)
        return Promise.resolve(e).then(async (e) => {
          if (t.value === `aborted`) return W;
          let r = await this._def.schema._parseAsync({ data: e, path: n.path, parent: n });
          return r.status === `aborted`
            ? W
            : r.status === `dirty` || t.value === `dirty`
              ? AH(r.value)
              : r;
        });
      {
        if (t.value === `aborted`) return W;
        let r = this._def.schema._parseSync({ data: e, path: n.path, parent: n });
        return r.status === `aborted`
          ? W
          : r.status === `dirty` || t.value === `dirty`
            ? AH(r.value)
            : r;
      }
    }
    if (r.type === `refinement`) {
      let e = (e) => {
        let t = r.refinement(e, i);
        if (n.common.async) return Promise.resolve(t);
        if (t instanceof Promise)
          throw Error(
            `Async refinement encountered during synchronous parse operation. Use .parseAsync instead.`,
          );
        return e;
      };
      if (n.common.async === !1) {
        let r = this._def.schema._parseSync({ data: n.data, path: n.path, parent: n });
        return r.status === `aborted`
          ? W
          : (r.status === `dirty` && t.dirty(), e(r.value), { status: t.value, value: r.value });
      } else
        return this._def.schema
          ._parseAsync({ data: n.data, path: n.path, parent: n })
          .then((n) =>
            n.status === `aborted`
              ? W
              : (n.status === `dirty` && t.dirty(),
                e(n.value).then(() => ({ status: t.value, value: n.value }))),
          );
    }
    if (r.type === `transform`)
      if (n.common.async === !1) {
        let e = this._def.schema._parseSync({ data: n.data, path: n.path, parent: n });
        if (!PH(e)) return W;
        let a = r.transform(e.value, i);
        if (a instanceof Promise)
          throw Error(
            `Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`,
          );
        return { status: t.value, value: a };
      } else
        return this._def.schema
          ._parseAsync({ data: n.data, path: n.path, parent: n })
          .then((e) =>
            PH(e)
              ? Promise.resolve(r.transform(e.value, i)).then((e) => ({
                  status: t.value,
                  value: e,
                }))
              : W,
          );
    xH.assertNever(r);
  }
};
((BU.create = (e, t, n) => new BU({ schema: e, typeName: J.ZodEffects, effect: t, ...K(n) })),
  (BU.createWithPreprocess = (e, t, n) =>
    new BU({
      schema: t,
      effect: { type: `preprocess`, transform: e },
      typeName: J.ZodEffects,
      ...K(n),
    })));
var VU = class extends q {
  _parse(e) {
    return this._getType(e) === V.undefined ? jH(void 0) : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
};
VU.create = (e, t) => new VU({ innerType: e, typeName: J.ZodOptional, ...K(t) });
var HU = class extends q {
  _parse(e) {
    return this._getType(e) === V.null ? jH(null) : this._def.innerType._parse(e);
  }
  unwrap() {
    return this._def.innerType;
  }
};
HU.create = (e, t) => new HU({ innerType: e, typeName: J.ZodNullable, ...K(t) });
var UU = class extends q {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = t.data;
    return (
      t.parsedType === V.undefined && (n = this._def.defaultValue()),
      this._def.innerType._parse({ data: n, path: t.path, parent: t })
    );
  }
  removeDefault() {
    return this._def.innerType;
  }
};
UU.create = (e, t) =>
  new UU({
    innerType: e,
    typeName: J.ZodDefault,
    defaultValue: typeof t.default == `function` ? t.default : () => t.default,
    ...K(t),
  });
var WU = class extends q {
  _parse(e) {
    let { ctx: t } = this._processInputParams(e),
      n = { ...t, common: { ...t.common, issues: [] } },
      r = this._def.innerType._parse({ data: n.data, path: n.path, parent: { ...n } });
    return FH(r)
      ? r.then((e) => ({
          status: `valid`,
          value:
            e.status === `valid`
              ? e.value
              : this._def.catchValue({
                  get error() {
                    return new wH(n.common.issues);
                  },
                  input: n.data,
                }),
        }))
      : {
          status: `valid`,
          value:
            r.status === `valid`
              ? r.value
              : this._def.catchValue({
                  get error() {
                    return new wH(n.common.issues);
                  },
                  input: n.data,
                }),
        };
  }
  removeCatch() {
    return this._def.innerType;
  }
};
WU.create = (e, t) =>
  new WU({
    innerType: e,
    typeName: J.ZodCatch,
    catchValue: typeof t.catch == `function` ? t.catch : () => t.catch,
    ...K(t),
  });
var GU = class extends q {
  _parse(e) {
    if (this._getType(e) !== V.nan) {
      let t = this._getOrReturnCtx(e);
      return (U(t, { code: H.invalid_type, expected: V.nan, received: t.parsedType }), W);
    }
    return { status: `valid`, value: e.data };
  }
};
GU.create = (e) => new GU({ typeName: J.ZodNaN, ...K(e) });
var KU = class extends q {
    _parse(e) {
      let { ctx: t } = this._processInputParams(e),
        n = t.data;
      return this._def.type._parse({ data: n, path: t.path, parent: t });
    }
    unwrap() {
      return this._def.type;
    }
  },
  qU = class e extends q {
    _parse(e) {
      let { status: t, ctx: n } = this._processInputParams(e);
      if (n.common.async)
        return (async () => {
          let e = await this._def.in._parseAsync({ data: n.data, path: n.path, parent: n });
          return e.status === `aborted`
            ? W
            : e.status === `dirty`
              ? (t.dirty(), AH(e.value))
              : this._def.out._parseAsync({ data: e.value, path: n.path, parent: n });
        })();
      {
        let e = this._def.in._parseSync({ data: n.data, path: n.path, parent: n });
        return e.status === `aborted`
          ? W
          : e.status === `dirty`
            ? (t.dirty(), { status: `dirty`, value: e.value })
            : this._def.out._parseSync({ data: e.value, path: n.path, parent: n });
      }
    }
    static create(t, n) {
      return new e({ in: t, out: n, typeName: J.ZodPipeline });
    }
  },
  JU = class extends q {
    _parse(e) {
      let t = this._def.innerType._parse(e),
        n = (e) => (PH(e) && (e.value = Object.freeze(e.value)), e);
      return FH(t) ? t.then((e) => n(e)) : n(t);
    }
    unwrap() {
      return this._def.innerType;
    }
  };
((JU.create = (e, t) => new JU({ innerType: e, typeName: J.ZodReadonly, ...K(t) })), CU.lazycreate);
var J;
(function (e) {
  ((e.ZodString = `ZodString`),
    (e.ZodNumber = `ZodNumber`),
    (e.ZodNaN = `ZodNaN`),
    (e.ZodBigInt = `ZodBigInt`),
    (e.ZodBoolean = `ZodBoolean`),
    (e.ZodDate = `ZodDate`),
    (e.ZodSymbol = `ZodSymbol`),
    (e.ZodUndefined = `ZodUndefined`),
    (e.ZodNull = `ZodNull`),
    (e.ZodAny = `ZodAny`),
    (e.ZodUnknown = `ZodUnknown`),
    (e.ZodNever = `ZodNever`),
    (e.ZodVoid = `ZodVoid`),
    (e.ZodArray = `ZodArray`),
    (e.ZodObject = `ZodObject`),
    (e.ZodUnion = `ZodUnion`),
    (e.ZodDiscriminatedUnion = `ZodDiscriminatedUnion`),
    (e.ZodIntersection = `ZodIntersection`),
    (e.ZodTuple = `ZodTuple`),
    (e.ZodRecord = `ZodRecord`),
    (e.ZodMap = `ZodMap`),
    (e.ZodSet = `ZodSet`),
    (e.ZodFunction = `ZodFunction`),
    (e.ZodLazy = `ZodLazy`),
    (e.ZodLiteral = `ZodLiteral`),
    (e.ZodEnum = `ZodEnum`),
    (e.ZodEffects = `ZodEffects`),
    (e.ZodNativeEnum = `ZodNativeEnum`),
    (e.ZodOptional = `ZodOptional`),
    (e.ZodNullable = `ZodNullable`),
    (e.ZodDefault = `ZodDefault`),
    (e.ZodCatch = `ZodCatch`),
    (e.ZodPromise = `ZodPromise`),
    (e.ZodBranded = `ZodBranded`),
    (e.ZodPipeline = `ZodPipeline`),
    (e.ZodReadonly = `ZodReadonly`));
})((J ||= {}));
var Y = cU.create,
  X = uU.create;
(GU.create, dU.create);
var YU = fU.create;
(pU.create, mU.create, hU.create, gU.create, _U.create);
var XU = vU.create;
(yU.create, bU.create);
var Z = xU.create,
  Q = CU.create;
(CU.strictCreate, wU.create, EU.create, OU.create, kU.create);
var ZU = AU.create;
(jU.create, MU.create, NU.create, PU.create, FU.create);
var QU = LU.create;
(RU.create, zU.create, BU.create, VU.create, HU.create, BU.createWithPreprocess, qU.create);
var $U = Q({ id: Y(), description: Y() }),
  eW = Q({ browser: Z($U).optional(), tab: Z($U).optional() }),
  tW = QU([`iab`, `extension`, `cdp`]),
  nW = Q({ capabilities: eW, id: Y(), name: Y(), type: tW, metadata: ZU(Y()).optional() });
(Q({}),
  Z(nW),
  Q({ id: Y() }),
  Q({ browser_id: Y() }),
  Q({ id: Y() }),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({}));
var rW = QU([`handoff`, `deliverable`]);
(Q({ browser_id: Y(), keep: Z(Q({ tab_id: Y(), status: rW })).optional() }),
  Q({}),
  Q({ browser_id: Y() }),
  Q({ display_truncate_max_chars: X().int().positive().optional() }),
  Q({ browser_id: Y(), name: Y() }),
  Q({}),
  Q({ browser_id: Y() }),
  Q({ id: Y().optional() }),
  Q({ browser_id: Y() }),
  Q({ tabs: Z(Q({ id: Y(), url: Y().optional(), title: Y().optional() })) }));
var iW = QU([`html`, `text`, `domSnapshot`]);
(Q({
  browser_id: Y(),
  urls: Z(Y()),
  content_type: iW,
  timeout_ms: X().int().positive().optional(),
}),
  Q({ results: Z(Q({ url: Y(), title: Y().nullable(), content: Y().nullable() })) }));
var aW = Q({ readOnlyHint: YU().optional(), untrustedContentHint: YU().optional() }),
  oW = Q({
    name: Y(),
    title: Y().optional(),
    description: Y().optional(),
    input_schema: XU(),
    annotations: aW.optional(),
    origin: Y().optional(),
    pageUrl: Y().optional(),
  }),
  sW = Q({ browser_id: Y(), tab_id: Y() });
Q({ tools: Z(oW) });
var cW = `webmcp_list_tools`;
function lW(e) {
  return new bH(cW, sW, e);
}
var uW = Q({
  browser_id: Y(),
  tab_id: Y(),
  tool_name: Y(),
  input: XU(),
  timeout_ms: X().int().positive().optional(),
});
Q({ result: XU() });
var dW = `webmcp_invoke_tool`;
function fW(e) {
  return new bH(dW, uW, e);
}
(Q({ browser_id: Y(), tab_id: Y(), url: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({ id: Y(), title: Y().optional(), url: Y().optional() }),
  Q({ browser_id: Y() }),
  Q({
    tabs: Z(
      Q({
        id: Y(),
        title: Y().optional(),
        url: Y().optional(),
        lastOpened: Y().optional(),
        tabGroup: Y().optional(),
      }),
    ),
  }),
  Q({
    browser_id: Y(),
    query: Y().optional(),
    limit: X().int().positive().optional(),
    from: Y().optional(),
    to: Y().optional(),
  }),
  Q({ items: Z(Q({ url: Y(), title: Y().optional(), dateVisited: Y() })) }));
var pW = QU([`debug`, `info`, `log`, `warn`, `error`]);
(Q({
  browser_id: Y(),
  tab_id: Y(),
  filter: Y().optional(),
  levels: Z(pW).optional(),
  limit: X().int().positive().optional(),
}),
  Q({ logs: Z(Q({ level: pW, message: Y(), timestamp: Y(), url: Y().optional() })) }));
var mW = QU([`alert`, `beforeunload`, `confirm`, `prompt`]),
  hW = Q({ id: Y(), type: mW });
(Q({ browser_id: Y(), tab_id: Y() }),
  Q({ dialog: hW.nullable() }),
  Q({
    action: QU([`accept`, `dismiss`]),
    browser_id: Y(),
    dialog_id: Y(),
    prompt_text: Y().optional(),
    tab_id: Y(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({ path: Y() }));
var gW = QU([`pdf`, `md`, `xlsx`, `csv`, `docx`, `pptx`]);
(Q({ browser_id: Y(), tab_id: Y(), format: gW }),
  Q({ path: Y() }),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({ text: Y() }),
  Q({ browser_id: Y(), tab_id: Y(), text: Y() }),
  Q({}));
var _W = Q({
  entries: Z(
    Q({ mime_type: Y(), text: Y().optional(), base64: Y().optional() }).superRefine((e, t) => {
      (e.text !== void 0) == (e.base64 !== void 0) &&
        t.addIssue({
          code: H.custom,
          message: `Clipboard entries must set exactly one of text or base64`,
        });
    }),
  ),
  presentation_style: QU([`unspecified`, `inline`, `attachment`]).optional(),
});
(Q({ browser_id: Y(), tab_id: Y() }), Q({ items: Z(_W) }));
var vW = Q({
  entries: Z(
    Q({ mime_type: Y(), text: Y().optional(), base64: Y().optional() }).superRefine((e, t) => {
      (e.text !== void 0) == (e.base64 !== void 0) &&
        t.addIssue({
          code: H.custom,
          message: `Clipboard entries must set exactly one of text or base64`,
        });
    }),
  ),
  presentation_style: QU([`unspecified`, `inline`, `attachment`]).optional(),
});
(Q({ browser_id: Y(), tab_id: Y(), items: Z(vW) }),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    fullPage: YU().optional(),
    cropX: X().optional(),
    cropY: X().optional(),
    cropWidth: X().optional(),
    cropHeight: X().optional(),
  }),
  Q({ data: Y() }),
  Q({ browser_id: Y(), tab_id: Y(), script: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ value: XU().optional() }),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    x: X(),
    y: X(),
    button: X().optional(),
    keys: Z(Y()).optional(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), x: X(), y: X(), timeout_ms: X().int().positive().optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), x: X(), y: X(), keys: Z(Y()).optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), keys: Z(Y()) }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), path: Z(Q({ x: X(), y: X() })), keys: Z(Y()).optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), x: X(), y: X(), keys: Z(Y()).optional() }),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    x: X(),
    y: X(),
    scroll_x: X(),
    scroll_y: X(),
    keys: Z(Y()).optional(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), text: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), node_id: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), node_id: Y(), timeout_ms: X().int().positive().optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), node_id: Y() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  XU(),
  Q({ browser_id: Y(), tab_id: Y(), keys: Z(Y()) }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), scroll_x: X(), scroll_y: X(), node_id: Y().optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), text: Y() }),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    modifiers: Z(QU([`Alt`, `Control`, `ControlOrMeta`, `Meta`, `Shift`])).optional(),
    button: QU([`left`, `right`, `middle`]).optional(),
    force: YU().optional(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    modifiers: Z(QU([`Alt`, `Control`, `ControlOrMeta`, `Meta`, `Shift`])).optional(),
    button: QU([`left`, `right`, `middle`]).optional(),
    force: YU().optional(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), timeout_ms: X().int().positive().optional() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), value: Y(), replace: YU() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), value: Y() }),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    state: QU([`attached`, `detached`, `visible`, `hidden`]),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y() }),
  Q({ count: X().int() }));
var yW = Q({
  value: Y().optional(),
  label: Y().optional(),
  index: X().int().nonnegative().optional(),
}).superRefine((e, t) => {
  e.value === void 0 &&
    e.label === void 0 &&
    e.index === void 0 &&
    t.addIssue({ code: H.custom, message: `Select option requires value, label, or index` });
});
(Q({
  browser_id: Y(),
  tab_id: Y(),
  selector: Y(),
  selections: Z(yW).min(1),
  timeout_ms: X().int().positive().optional(),
}),
  Q({}),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    checked: YU(),
    force: YU().optional(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y() }),
  Q({ value: YU() }),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y() }),
  Q({ value: YU() }),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ values: Z(Y()) }),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ value: Y().nullable() }),
  Q({ browser_id: Y(), tab_id: Y(), selector: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ value: Y() }),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    name: Y(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({ value: Y().nullable() }),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    selector: Y(),
    relative_selector: Y().optional(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({
    values: Z(Q({ attributes: ZU(Y()), inner_text: Y(), text_content: Y().nullable() }).nullable()),
  }));
var bW = QU([`load`, `domcontentloaded`, `networkidle`, `commit`]);
(Q({
  browser_id: Y(),
  tab_id: Y(),
  url: Y(),
  wait_until: bW.optional(),
  timeout_ms: X().int().positive().optional(),
}),
  Q({ url: Y().optional() }));
var xW = QU([`load`, `domcontentloaded`, `networkidle`]);
(Q({
  browser_id: Y(),
  tab_id: Y(),
  state: xW.optional(),
  timeout_ms: X().int().positive().optional(),
}),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y(), timeout_ms: X().int().nonnegative() }),
  Q({}),
  Q({ browser_id: Y(), tab_id: Y() }),
  Q({ dom_snapshot: Y() }));
var SW = Q({ x: X(), y: X(), width: X(), height: X() }),
  CW = Q({
    primary: Y().nullable().optional(),
    candidates: Z(Y()),
    frameSelectors: Z(Y()).optional(),
  }),
  wW = Q({
    nodeId: X().int().positive().nullable().optional(),
    tagName: Y(),
    role: Y().nullable().optional(),
    visibleText: Y().nullable().optional(),
    ariaName: Y().nullable().optional(),
    testId: Y().nullable().optional(),
    boundingBox: SW.nullable().optional(),
    preview: Y(),
    selector: CW,
  });
(Q({ browser_id: Y(), tab_id: Y(), x: X(), y: X(), include_non_interactable: YU().optional() }),
  Z(wW),
  Q({ browser_id: Y(), tab_id: Y(), x: X(), y: X(), include_non_interactable: YU().optional() }),
  Q({ data: Y() }),
  Q({ browser_id: Y(), tab_id: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ download_id: Y() }),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    download_id: Y(),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({ path: Y().nullable() }),
  Q({ browser_id: Y(), tab_id: Y(), timeout_ms: X().int().positive().optional() }),
  Q({ file_chooser_id: Y(), is_multiple: YU() }),
  Q({
    browser_id: Y(),
    tab_id: Y(),
    file_chooser_id: Y(),
    files: Z(Y()),
    timeout_ms: X().int().positive().optional(),
  }),
  Q({}));
var TW = class {
    constructor(e, t, n, r) {
      ((this.transport = e), (this.browserId = t), (this.documentationApi = n), (this.info = r));
    }
    get id() {
      return this.info.id;
    }
    async documentation() {
      return await this.documentationApi.get(`capabilities/browser/${this.id}`);
    }
  },
  EW = class {
    constructor(e, t, n, r, i) {
      ((this.transport = e),
        (this.browserId = t),
        (this.tabId = n),
        (this.documentationApi = r),
        (this.info = i));
    }
    get id() {
      return this.info.id;
    }
    async documentation() {
      return await this.documentationApi.get(`capabilities/tab/${this.id}`);
    }
  };
function DW({ capability: e, info: t, internalOnly: n }) {
  return {
    capability: e,
    create: (n) => new e({ ...n, info: t }),
    id: t.id,
    info: t,
    ...(n == null ? {} : { internalOnly: n }),
  };
}
function OW({ capability: e, info: t, internalOnly: n }) {
  return {
    capability: e,
    create: (n) => new e({ ...n, info: t }),
    id: t.id,
    info: t,
    ...(n == null ? {} : { internalOnly: n }),
  };
}
var kW = Q({ session_id: Y().min(1).optional(), target_id: Y().min(1).optional() }).refine(
    (e) => (e.session_id == null) != (e.target_id == null),
    `CDP target must provide exactly one of session_id or target_id.`,
  ),
  AW = Q({
    extensionId: Y().optional(),
    sessionId: Y().optional(),
    tabId: X().int().optional(),
    targetId: Y().optional(),
  }),
  jW = Q({
    sequence: X().int().positive(),
    source: AW,
    method: Y().min(1),
    params: ZU(Y(), XU()).optional(),
  }),
  MW = `tab_cdp_call`,
  NW = Q({
    browser_id: Y(),
    tab_id: Y(),
    method: Y().min(1),
    params: ZU(Y(), XU()).optional(),
    target: kW.optional(),
    timeout_ms: X().int().positive().optional(),
  }),
  PW = XU();
function FW(e) {
  return new bH(MW, NW, e);
}
var IW = `tab_cdp_events`,
  LW = Q({
    after_sequence: X().int().nonnegative().optional(),
    browser_id: Y(),
    limit: X().int().positive().max(1e3).optional(),
    methods: Z(Y().min(1)).min(1).optional(),
    tab_id: Y(),
    target: kW.optional(),
    timeout_ms: X().int().nonnegative().optional(),
  }),
  RW = Q({ cursor: X().int().nonnegative(), events: Z(jW), hasMore: YU(), truncated: YU() });
function zW(e) {
  return new bH(IW, LW, e);
}
var BW = {
    id: `cdp`,
    description: `Send raw Chrome DevTools Protocol commands and read debugger events through a supported tab for developer use cases.`,
  },
  VW = 2e3,
  HW = OW({
    capability: class extends EW {
      constructor({ browserId: e, documentation: t, info: n, tabId: r, transport: i }) {
        super(i, e, r, t, n);
      }
      async send(e, t, n) {
        let r = await this.transport.send({
          command: FW({
            browser_id: this.browserId,
            method: e,
            params: t,
            tab_id: this.tabId,
            target: UW(n?.target),
            timeout_ms: n?.timeoutMs,
          }),
          timeoutMs: n?.timeoutMs,
        });
        return PW.parse(r);
      }
      async readEvents(e) {
        let t = await this.transport.send({
          command: zW({
            after_sequence: e?.afterSequence,
            browser_id: this.browserId,
            limit: e?.limit,
            methods: e?.methods,
            tab_id: this.tabId,
            target: UW(e?.target),
            timeout_ms: e?.timeoutMs,
          }),
          timeoutMs: WW(e?.timeoutMs),
        });
        return RW.parse(t);
      }
    },
    info: BW,
  });
function UW(e) {
  if (e != null) return { session_id: e.sessionId, target_id: e.targetId };
}
function WW(e) {
  return e != null && e > 0 ? e + VW : void 0;
}
var GW = 300 * 1e3,
  KW = /^[A-Za-z0-9_-]{1,48}$/,
  qW = new Set([`__proto__`, `constructor`, `prototype`]),
  JW = Q({
    id: Y()
      .regex(KW)
      .refine((e) => !qW.has(e)),
    label: Y(),
    type: Y().min(1),
    autocomplete: Y(),
    required: YU(),
    selector: Y(),
  }),
  YW = Q({
    origin: Y(),
    reason: Y(),
    expires_at: Y()
      .datetime({ offset: !0 })
      .refine(
        (e) => Date.parse(e) <= Date.now() + GW,
        `expires_at must be no more than 5 minutes in the future`,
      ),
    fields: Z(JW).min(1).max(6),
    submit: Q({ selector: Y(), action: QU([`click`, `press_enter`]) }).optional(),
  }),
  XW = `tab_browser_auth_handoff`,
  ZW = YW.extend({ browser_id: Y(), tab_id: Y() }),
  QW = Q({
    status: QU([
      `submitted`,
      `declined`,
      `cancelled`,
      `unavailable`,
      `expired`,
      `origin_changed`,
      `page_changed`,
      `locator_invalid`,
      `submission_failed`,
    ]),
  });
function $W(e) {
  return new bH(XW, ZW, e);
}
var eG = OW({
    capability: class extends EW {
      constructor({ browserId: e, documentation: t, info: n, tabId: r, transport: i }) {
        super(i, e, r, t, n);
      }
      async request(e) {
        let t = await this.transport.send({
          command: $W({ ...e, browser_id: this.browserId, tab_id: this.tabId }),
        });
        return QW.parse(t);
      }
    },
    info: {
      id: `browserAuth`,
      description: `Use when authentication or sign-in blocks the browser task. Pauses for the user to provide credentials to a validated login form without exposing the values to the model. Omit submit for forms that auto-submit during credential entry.`,
    },
  }),
  tG = QU([`font`, `image`, `script`, `stylesheet`, `video`, `other`]),
  nG = QU([`font`, `image`, `stylesheet`, `video`]),
  rG = Q({
    kind: QU([`attribute`, `computedStyle`, `resource`]),
    nodeId: X().int().positive().optional(),
    property: Y().optional(),
  }),
  iG = Q({ id: Y(), kind: tG, name: Y(), sources: Z(rG), url: Y() }),
  aG = Q({ id: Y(), markup: Y(), name: Y() }),
  oG = Q({
    assets: Z(iG),
    id: Y(),
    inlineSvgs: Z(aG),
    pageUrl: Y().nullable(),
    summary: Q({
      byKind: ZU(tG, X().int().nonnegative()),
      inlineSvgCount: X().int().nonnegative(),
      totalCount: X().int().nonnegative(),
    }),
  }),
  sG = Q({ assetIds: Z(Y()).optional(), inventoryId: Y(), kinds: Z(nG).optional() }),
  cG = Q({ contentType: Y().nullable(), id: Y(), kind: nG, name: Y(), path: Y(), url: Y() }),
  lG = Q({ contentType: Y().nullable(), id: Y(), name: Y(), reason: Y(), url: Y() }),
  uG = Q({
    assets: Z(cG),
    directoryPath: Y(),
    failures: Z(lG),
    manifestPath: Y(),
    summary: Q({
      downloadedCount: X().int().nonnegative(),
      elapsedMs: X().nonnegative(),
      failedCount: X().int().nonnegative(),
      requestedCount: X().int().nonnegative(),
    }),
  }),
  dG = `tab_page_assets_bundle`,
  fG = sG.extend({ browser_id: Y(), tab_id: Y() }),
  pG = uG;
function mG(e) {
  return new bH(dG, fG, e);
}
var hG = `tab_page_assets_list`,
  gG = Q({ browser_id: Y(), tab_id: Y() }),
  _G = oG;
function vG(e) {
  return new bH(hG, gG, e);
}
var yG = OW({
  capability: class extends EW {
    constructor({ browserId: e, documentation: t, info: n, tabId: r, transport: i }) {
      super(i, e, r, t, n);
    }
    async list() {
      let e = await this.transport.send({
        command: vG({ browser_id: this.browserId, tab_id: this.tabId }),
      });
      return _G.parse(e);
    }
    async bundle(e) {
      let t = await this.transport.send({
        command: mG({ ...e, browser_id: this.browserId, tab_id: this.tabId }),
      });
      return pG.parse(t);
    }
  },
  info: {
    id: `pageAssets`,
    description: `List assets already observed in the current page state and bundle selected assets into a temporary local artifact.`,
  },
});
bG([
  HW,
  eG,
  yG,
  OW({
    capability: class extends EW {
      constructor({ browserId: e, documentation: t, info: n, tabId: r, transport: i }) {
        super(i, e, r, t, n);
      }
      async listTools() {
        return (
          await this.transport.send({
            command: lW({ browser_id: this.browserId, tab_id: this.tabId }),
          })
        ).tools.map((e) => this.toTool(e));
      }
      async invokeTool({ input: e, timeoutMs: t, toolName: n }) {
        let r = n.trim();
        if (!r) throw Error(`tab.capabilities.webmcp.invokeTool requires a toolName`);
        return (
          await this.transport.send({
            command: fW({
              browser_id: this.browserId,
              tab_id: this.tabId,
              tool_name: r,
              input: e,
              ...(t == null ? {} : { timeout_ms: t }),
            }),
          })
        ).result;
      }
      toTool(e) {
        return {
          name: e.name,
          ...(e.title == null ? {} : { title: e.title }),
          ...(e.description == null ? {} : { description: e.description }),
          inputSchema: e.input_schema,
          ...(e.annotations == null ? {} : { annotations: e.annotations }),
          ...(e.origin == null ? {} : { origin: e.origin }),
          ...(e.pageUrl == null ? {} : { pageUrl: e.pageUrl }),
          invoke: async (t, n) =>
            await this.invokeTool({
              toolName: e.name,
              input: t,
              ...(n?.timeoutMs == null ? {} : { timeoutMs: n.timeoutMs }),
            }),
        };
      }
    },
    info: {
      id: `webmcp`,
      description: `List and invoke page-defined WebMCP tools registered through navigator.modelContext in the active tab.`,
    },
    internalOnly: !0,
  }),
]);
function bG(e) {
  return new Map(e.map((e) => [e.id, e.create]));
}
var xG = e.i({
    PayloadSchema: () => CG,
    ResultSchema: () => wG,
    commandType: () => SG,
    create: () => TG,
  }),
  SG = `browser_visibility_get`,
  CG = Q({ browser_id: Y() }),
  wG = Q({ visible: YU() });
function TG(e) {
  return new bH(SG, CG, e);
}
var EG = e.i({
    PayloadSchema: () => OG,
    ResultSchema: () => kG,
    commandType: () => DG,
    create: () => AG,
  }),
  DG = `browser_visibility_set`,
  OG = Q({ browser_id: Y(), visible: YU() }),
  kG = Q({});
function AG(e) {
  return new bH(DG, OG, e);
}
var jG = e.i({ Get: () => xG, Set: () => EG, commands: () => MG }),
  MG = { [SG]: xG, [DG]: EG },
  NG = DW({
    capability: class extends TW {
      constructor({ browserId: e, documentation: t, info: n, transport: r }) {
        super(r, e, t, n);
      }
      async set(e) {
        let t = await this.transport.send({
          command: AG({ browser_id: this.browserId, visible: e }),
        });
        kG.parse(t);
      }
      async get() {
        let e = await this.transport.send({ command: TG({ browser_id: this.browserId }) });
        return wG.parse(e).visible;
      }
    },
    info: {
      id: `visibility`,
      description: `Use to show or hide the browser to the user, and to determine the browser's current visibility. Keep browser work in the background unless the user asks to see it or live viewing is useful. When the browser should be visible, call set(true).`,
    },
  }),
  PG = e.i({
    PayloadSchema: () => IG,
    ResultSchema: () => LG,
    commandType: () => FG,
    create: () => RG,
  }),
  FG = `browser_viewport_reset`,
  IG = Q({ browser_id: Y() }),
  LG = Q({});
function RG(e) {
  return new bH(FG, IG, e);
}
var zG = e.i({
    PayloadSchema: () => VG,
    ResultSchema: () => HG,
    commandType: () => BG,
    create: () => UG,
  }),
  BG = `browser_viewport_set`,
  VG = Q({ browser_id: Y(), height: X().int().positive(), width: X().int().positive() }),
  HG = Q({});
function UG(e) {
  return new bH(BG, VG, e);
}
var WG = e.i({ Reset: () => PG, Set: () => zG, commands: () => GG }),
  GG = { [FG]: PG, [BG]: zG },
  KG = DW({
    capability: class extends TW {
      constructor({ browserId: e, documentation: t, info: n, transport: r }) {
        super(r, e, t, n);
      }
      async set(e) {
        let t = await this.transport.send({ command: UG({ browser_id: this.browserId, ...e }) });
        HG.parse(t);
      }
      async reset() {
        let e = await this.transport.send({ command: RG({ browser_id: this.browserId }) });
        LG.parse(e);
      }
    },
    info: {
      id: `viewport`,
      description: `Controls an explicit browser viewport override for responsive or device-size testing. Use it when a task calls for specific dimensions or breakpoint validation; otherwise leave it unset so the browser uses its normal 1280x720 viewport. Reset temporary overrides before finishing unless the user asked to keep them.`,
    },
  });
qG([NG, KG]);
function qG(e) {
  return new Map(e.map((e) => [e.id, e.create]));
}
Q({ side_effects: Z(Y()) }).passthrough();
var JG = [NG.info, KG.info],
  YG = [yG.info];
function XG(e) {
  return YG;
}
function ZG({ browserTabId: e, host: t, payload: n }) {
  return dt({
    backendName: `Codex in-app browser`,
    commandSets: [jG, WG],
    handlers: eK({ browserTabId: e, host: t }),
    payload: n,
  });
}
function QG(e) {
  return (
    e.type === `browser_visibility_set` ||
    e.type === `browser_viewport_set` ||
    e.type === `browser_viewport_reset`
  );
}
function $G(e) {
  if (e.type === `browser_visibility_set`)
    return { kind: `visibility`, visible: OG.parse(e).visible };
  if (e.type === `browser_viewport_set`) {
    let t = VG.parse(e);
    return { kind: `viewport`, viewportSize: { width: t.width, height: t.height } };
  }
  return e.type === `browser_viewport_reset`
    ? (IG.parse(e), { kind: `viewport`, viewportSize: null })
    : null;
}
function eK({ browserTabId: e, host: t }) {
  return {
    [DG]: (n) => {
      let r = OG.parse(n);
      return (t.setBrowserVisibleForBrowserUse(r.visible, e), kG.parse({}));
    },
    [SG]: (n) => (CG.parse(n), wG.parse({ visible: t.isBrowserVisibleForBrowserUse(e) })),
    [BG]: (n) => {
      let r = VG.parse(n);
      t.setViewportForBrowserUse({ width: r.width, height: r.height }, e);
      let i = t.waitForPendingDebuggerSync?.(e);
      return i == null ? HG.parse({}) : i.then(() => HG.parse({}));
    },
    [FG]: (n) => {
      (IG.parse(n), t.setViewportForBrowserUse(null, e));
      let r = t.waitForPendingDebuggerSync?.(e);
      return r == null ? LG.parse({}) : r.then(() => LG.parse({}));
    },
  };
}
var tK = `about:blank`,
  nK = 15e3,
  rK = 2e4,
  iK = 1500,
  aK = 1e3,
  oK = 16,
  sK = `tabs_content`,
  cK = t.Gr(`browser-use-iab-api`),
  lK = class {
    tabsById = new Map();
    tabRuntimesById = new Map();
    tabIdsByPageKey = new Map();
    tabIdsByRouteTabKey = new Map();
    userTabsByRouteTabKey = new Map();
    routeTabKeysByTabId = new Map();
    routeKeysByTabId = new Map();
    debuggerListeners = new Map();
    cdpEventListeners = new Set();
    disposeBrowserUseNavigationBlockedListener;
    cdpCommandTimeoutMs;
    cursorArrivalTimeoutMs;
    initialPageUrl;
    pageReadyTimeoutMs;
    cursorArrivalWaitersByMoveSequence = new Map();
    selectedTabIdsByRouteKey = new Map();
    hostsByRouteKey = new Map();
    pendingVisibilityRouteKeys = new Set();
    pendingViewportSizesByRouteKey = new Map();
    nextCdpTabId = 1;
    nextCursorMoveSequence = 1;
    constructor(e, t, n = {}) {
      ((this.getBrowserHost = e),
        (this.options = n),
        (this.disposeBrowserUseNavigationBlockedListener = t((e) => {
          this.emitBrowserUseNavigationBlockedEvent(e);
        })),
        (this.cdpCommandTimeoutMs = n.cdpCommandTimeoutMs ?? rK),
        (this.cursorArrivalTimeoutMs = n.cursorArrivalTimeoutMs ?? iK),
        (this.initialPageUrl = n.initialPageUrl ?? tK),
        (this.pageReadyTimeoutMs = n.pageReadyTimeoutMs ?? nK));
    }
    ping() {
      return `pong`;
    }
    async getTabs(e) {
      return this.runLoggedRequest(e, `getTabs`, (e) => this.getTabsForBrowserUse(e));
    }
    async createTab(e) {
      return this.runLoggedRequest(e, `createTab`, (e) => this.createTabForBrowserUse(e));
    }
    async nameSession(e) {
      await this.nameSessionForBrowserUse(this.requireBrowserUseSession(e), e);
    }
    async attach(e) {
      await this.attachForBrowserUse(this.requireBrowserUseSession(e), e);
    }
    async attachTarget(e) {
      let t = this.requireBrowserUseSession(e),
        n = await this.getTab(t, e.tabId);
      (this.assertCurrentTarget(n, e.targetId, `attachTarget`),
        await this.attachForBrowserUse(t, e));
    }
    async detach(e) {
      await this.detachForBrowserUse(this.requireBrowserUseSession(e), e);
    }
    async detachTarget(e) {
      let t = this.requireBrowserUseSession(e),
        n = await this.getTab(t, e.tabId);
      (this.assertCurrentTarget(n, e.targetId, `detachTarget`),
        await this.detachForBrowserUse(t, e));
    }
    async executeCdp(e) {
      return this.runLoggedRequest(e, `executeCdp:${e.method}`, (t) =>
        this.executeCdpForBrowserUse(t, e),
      );
    }
    async executeUnhandledCommand(e) {
      return this.runLoggedRequest(e, `executeUnhandledCommand:${e.type}`, (t) => {
        if (e.type === sK)
          throw Error(
            `browser.tabs.content is not supported in Codex in-app browser. Open tabs with browser.tabs.new(), navigate them, then read each tab directly.`,
          );
        let n = this.getRequiredBrowserHost(t),
          r = this.getActiveBrowserUseTab(t, {
            assertCurrentPageAllowed: !1,
            markBrowserUseCommand: !1,
          });
        if (r == null && this.getTabMode(t) === `multiTab` && QG(e)) {
          let t = $G(e);
          if (t != null) return (this.setPendingCapabilityIntent(n.routeKey, t), {});
        }
        return ZG({ browserTabId: r?.browserTabId, host: n, payload: e });
      });
    }
    addCdpEventListener(e) {
      return (
        this.cdpEventListeners.add(e),
        () => {
          this.cdpEventListeners.delete(e);
        }
      );
    }
    async getInfo(e) {
      let n = this.getBrowserUseSession(e);
      cK().info(`IAB_LIFECYCLE iab backend info request received`, {
        safe: this.getRequestSafeFields(n, e, `getInfo`),
        sensitive: {},
      });
      try {
        this.ensureBrowserUseSessionRoute(n);
        let r = this.getMetadata(n),
          i = {
            name: `Codex In-app Browser`,
            version: t.hs(t.ys(a.app.getVersion())),
            type: `iab`,
            capabilities: { browser: JG, tab: XG(this.options.buildFlavor) },
            metadata: r,
          };
        return (
          cK().info(`IAB_LIFECYCLE iab backend info request completed`, {
            safe: {
              ...this.getRequestSafeFields(n, e, `getInfo`),
              metadataCodexSessionId: r.codexSessionId,
            },
            sensitive: {},
          }),
          i
        );
      } catch (t) {
        throw (
          cK().warning(`IAB_LIFECYCLE iab backend info request failed`, {
            safe: this.getRequestSafeFields(n, e, `getInfo`),
            sensitive: { error: t },
          }),
          t
        );
      }
    }
    getUserTabs(e) {
      let t = this.requireBrowserUseSession(e);
      return this.getTabMode(t) === `multiTab` ? this.getUserTabInfosForSession(t) : [];
    }
    getUserHistory(e) {
      throw (
        this.requireBrowserUseSession(e),
        Error(`browser.user.history is only available with the Chrome backend.`)
      );
    }
    async claimUserTab(e) {
      let t = this.requireBrowserUseSession(e);
      if (this.getTabMode(t) !== `multiTab`)
        throw Error(
          `browser.user.claimTab is only available with the Chrome or multi-tab IAB backend.`,
        );
      let n = this.getRequiredBrowserHost(t),
        r = this.tabsById.get(e.tabId) ?? null;
      if (r?.routeKey === n.routeKey) {
        let e = await this.requireLiveTab(t, r);
        return (
          this.selectedTabIdsByRouteKey.set(e.routeKey, e.cdpTabId),
          this.markBrowserUseCommandForTab(t, e),
          this.serializeTab(e)
        );
      }
      let i = this.getUserTabForTabId(t, e.tabId);
      if (i == null) throw Error(`Unknown tab: ${e.tabId}`);
      let a = await n.openPageForBrowserUse({ browserTabId: i.browserTabId, origin: `user` }),
        o = this.updateTabForPage(a, n.routeKey, `user`, e.tabId);
      return (
        this.selectedTabIdsByRouteKey.set(o.routeKey, o.cdpTabId),
        this.markBrowserUseCommandForTab(t, o),
        this.serializeTab(o)
      );
    }
    async finalizeTabs(e) {
      let t = this.requireBrowserUseSession(e);
      if (this.getTabMode(t) !== `multiTab`)
        throw Error(`browser.tabs.finalize is only available with Chrome or multi-tab IAB.`);
      if (!Array.isArray(e.keep)) throw Error(`finalizeTabs requires a keep array`);
      let n = this.updateTabsForSession(t),
        r = dK(e.keep, new Set(n.map((e) => e.cdpTabId)));
      await Promise.allSettled(
        n.map(async (e) => {
          await this.cleanupDebuggerForTabBestEffort(e, `finalize-tabs`);
          let t = r.get(e.cdpTabId);
          if (!(t === `handoff` && e.origin === `agent`)) {
            if (t === `handoff` || t === `deliverable` || e.origin !== `agent`) {
              this.releaseTab(e);
              return;
            }
            this.closeTab(e);
          }
        }),
      );
      for (let e of this.getBrowserHostsForSession(t))
        (e.setBrowserUseActive(!1), this.clearPendingCapabilityIntents(e.routeKey));
    }
    async moveMouse(e) {
      jK(`moveMouse`, e);
      let t = this.requireBrowserUseSession(e),
        n = await this.getTab(t, e.tabId);
      this.markBrowserUseCommandForTab(t, n);
      let r = this.nextCursorMoveSequence;
      this.nextCursorMoveSequence += 1;
      let i = !1;
      for (let a of this.getBrowserHostsForSession(t))
        i = a.setBrowserUseCursor({ moveSequence: r, x: e.x, y: e.y }, n.browserTabId) || i;
      e.waitForArrival === !1 || !i || (await this.waitForCursorArrival(r));
    }
    notifyCursorArrived(e) {
      Number.isInteger(e.moveSequence) &&
        this.options.ensureSessionRoute?.(e) === !0 &&
        this.resolveCursorArrivalWaiter(e.moveSequence);
    }
    async releaseSessionControl(e) {
      let t = this.getBrowserHostsForSession(e);
      await this.detachBrowserUseTabsForHostsBestEffort(t);
      for (let e of t)
        (e.setBrowserUseCursor(null),
          e.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(e.routeKey));
    }
    async turnEnded(e) {}
    disposeRoute(e) {
      let t = this.hostsByRouteKey.get(e) ?? null;
      (this.hostsByRouteKey.delete(e),
        this.selectedTabIdsByRouteKey.delete(e),
        this.forgetTabIdsForRoute(e),
        this.forgetUserTabsForRoute(e),
        t != null &&
          (t.setBrowserUseCursor(null),
          t.setBrowserUseActive(!1),
          this.clearPendingCapabilityIntents(t.routeKey)));
      for (let t of Array.from(this.tabsById.values()))
        t.routeKey === e &&
          (this.cleanupDebuggerForTabBestEffort(t, `route-disposed`), this.removeTab(t));
    }
    reassignRoute(e, t, n, r) {
      let i = this.selectedTabIdsByRouteKey.get(e) ?? null,
        a = i == null ? null : (this.tabsById.get(i) ?? null);
      i != null &&
        (n == null || a?.browserTabId === n) &&
        (this.selectedTabIdsByRouteKey.delete(e), this.selectedTabIdsByRouteKey.set(t, i));
      let o = this.hostsByRouteKey.get(e) ?? null;
      o != null && (this.hostsByRouteKey.delete(e), this.hostsByRouteKey.set(t, o));
      for (let [i, a] of this.routeKeysByTabId) {
        if (a !== e) continue;
        let o = this.tabsById.get(i);
        (n != null && o?.browserTabId !== n) ||
          (this.routeKeysByTabId.set(i, t),
          o != null &&
            this.tabsById.set(i, { ...o, browserTabId: r ?? o.browserTabId, routeKey: t }));
      }
      for (let [i, a] of Array.from(this.userTabsByRouteTabKey.entries())) {
        if (a.routeKey !== e || (n != null && a.browserTabId !== n)) continue;
        let o = r ?? a.browserTabId,
          s = DK(t, o),
          c = this.tabIdsByRouteTabKey.get(i) ?? null;
        (this.userTabsByRouteTabKey.delete(i),
          this.userTabsByRouteTabKey.set(s, { ...a, browserTabId: o, routeKey: t }),
          c != null && this.moveRouteTabId(i, s, c, t));
      }
      for (let [i, a] of Array.from(this.tabIdsByRouteTabKey.entries())) {
        let o = OK(e, i);
        o == null || (n != null && o !== n) || this.moveRouteTabId(i, DK(t, r ?? o), a, t);
      }
    }
    dispose() {
      (this.resolveAllCursorArrivalWaiters(), this.disposeBrowserUseNavigationBlockedListener());
      for (let e of this.hostsByRouteKey.values()) e.setBrowserUseActive(!1);
      for (let e of this.tabsById.values()) (this.unregisterDebuggerListener(e), this.detachTab(e));
      (this.tabsById.clear(),
        this.tabRuntimesById.clear(),
        this.tabIdsByPageKey.clear(),
        this.tabIdsByRouteTabKey.clear(),
        this.userTabsByRouteTabKey.clear(),
        this.routeTabKeysByTabId.clear(),
        this.routeKeysByTabId.clear(),
        this.selectedTabIdsByRouteKey.clear(),
        this.hostsByRouteKey.clear(),
        this.pendingVisibilityRouteKeys.clear(),
        this.pendingViewportSizesByRouteKey.clear());
    }
    requireBrowserUseSession(e) {
      let t = this.getBrowserUseSession(e);
      return (this.ensureBrowserUseSessionRoute(t), t);
    }
    getBrowserUseSession(e) {
      let t = e?.session_id;
      if (typeof t != `string`) throw Error(`Missing required browser session_id`);
      return { conversationId: t };
    }
    ensureBrowserUseSessionRoute(e) {
      if (this.options.ensureSessionRoute?.(e) !== !0)
        throw Error(`No Codex browser route is available for browser session ${e.conversationId}`);
    }
    async runLoggedRequest(e, t, n) {
      let r = this.getBrowserUseSession(e);
      cK().info(`IAB_LIFECYCLE iab backend request received`, {
        safe: this.getRequestSafeFields(r, e, t),
        sensitive: {},
      });
      try {
        this.ensureBrowserUseSessionRoute(r);
        let i = await n(r);
        return (
          cK().info(`IAB_LIFECYCLE iab backend request completed`, {
            safe: this.getRequestSafeFields(r, e, t),
            sensitive: {},
          }),
          i
        );
      } catch (n) {
        throw (
          cK().warning(`IAB_LIFECYCLE iab backend request failed`, {
            safe: this.getRequestSafeFields(r, e, t),
            sensitive: { error: n },
          }),
          n
        );
      }
    }
    getRequestSafeFields(e, t, n) {
      return { conversationId: e.conversationId, request: n, turnId: t.turn_id ?? null };
    }
    async waitForCursorArrival(e) {
      this.cursorArrivalTimeoutMs <= 0 ||
        (await new Promise((t) => {
          let n = null,
            r = () => {
              (n != null && clearTimeout(n),
                this.cursorArrivalWaitersByMoveSequence.delete(e),
                t());
            };
          ((n = setTimeout(r, this.cursorArrivalTimeoutMs)),
            MK(n),
            this.cursorArrivalWaitersByMoveSequence.set(e, r));
        }));
    }
    resolveCursorArrivalWaiter(e) {
      this.cursorArrivalWaitersByMoveSequence.get(e)?.();
    }
    resolveAllCursorArrivalWaiters() {
      for (let e of Array.from(this.cursorArrivalWaitersByMoveSequence.values())) e();
    }
    getTabMode(e) {
      return this.options.getTabMode?.(e) ?? `singleTab`;
    }
    getMetadata(e) {
      let t = {};
      return (
        this.options.buildFlavor != null && (t.codexAppBuildFlavor = this.options.buildFlavor),
        this.options.appSessionId != null && (t.codexAppSessionId = this.options.appSessionId),
        (t.codexSessionId = e.conversationId),
        (t.codexIabTabMode = this.getTabMode(e)),
        t
      );
    }
    async getTabsForBrowserUse(e) {
      if (this.getTabMode(e) === `multiTab`) {
        let t = this.updateTabsForSession(e),
          n = this.getRequiredBrowserHost(e),
          r = this.selectedTabIdsByRouteKey.get(n.routeKey) ?? null;
        if (r == null || t.every((e) => e.cdpTabId !== r)) {
          let e = t.find((e) => e.active) ?? t[0];
          e != null && this.selectedTabIdsByRouteKey.set(n.routeKey, e.cdpTabId);
        }
        return t.map((e) => this.serializeTab(e));
      }
      let t = this.getActiveBrowserUseTab(e, {
        assertCurrentPageAllowed: !1,
        markBrowserUseCommand: !1,
      });
      return t == null ? [] : [this.serializeTab(t)];
    }
    async createTabForBrowserUse(e) {
      if (this.getTabMode(e) === `multiTab`) {
        let n = this.getRequiredBrowserHost(e),
          r = t.rs(`browser-use:${(0, u.randomUUID)()}`),
          i = !1,
          a = !1;
        try {
          (n.setBrowserUseActive(!0, r), this.applyPendingCapabilityIntents(n, r));
          let t = n.openPageForBrowserUse({
            browserTabId: r,
            origin: `agent`,
            startingUrl: this.initialPageUrl,
          });
          i = !0;
          let o = await t;
          a = !0;
          let s = this.updateTabForPage(o, n.routeKey, `agent`);
          return (
            cK().info(`IAB_LIFECYCLE iab createTab mapped page to tab`, {
              safe: {
                ...e,
                browserTabId: r,
                pageKey: o.pageKey,
                routeKey: n.routeKey,
                tabId: s.cdpTabId,
                webContentsId: o.webContents.id,
              },
              sensitive: {},
            }),
            this.markBrowserUseCommandForTab(e, s),
            this.selectedTabIdsByRouteKey.set(n.routeKey, s.cdpTabId),
            this.serializeTab(s)
          );
        } catch (e) {
          throw ((!i || a) && (n.setBrowserUseActive(!1, r), n.closePageForBrowserUse(r)), e);
        }
      }
      let n = this.getActiveBrowserUseTab(e, { assertCurrentPageAllowed: !1 });
      if (n != null) {
        let t = await this.requireLiveTab(e, n);
        return (await this.navigateTabToInitialPage(t), this.serializeTab(t));
      }
      let r = this.getRequiredBrowserHost(e);
      r.setBrowserUseActive(!0);
      let i = await r.openPageForBrowserUse({ origin: `agent`, startingUrl: this.initialPageUrl }),
        a = this.updateTabForPage(i, r.routeKey, `agent`);
      return (
        cK().info(`IAB_LIFECYCLE iab createTab mapped page to tab`, {
          safe: {
            ...e,
            pageKey: i.pageKey,
            routeKey: r.routeKey,
            tabId: a.cdpTabId,
            webContentsId: i.webContents.id,
          },
          sensitive: {},
        }),
        this.markBrowserUseCommandForTab(e, a),
        this.selectedTabIdsByRouteKey.set(r.routeKey, a.cdpTabId),
        this.serializeTab(a)
      );
    }
    async nameSessionForBrowserUse(e, t) {
      if ((this.getRequiredBrowserHost(e), typeof t.name != `string`))
        throw Error(`nameSession requires a name`);
    }
    async attachForBrowserUse(e, t) {
      let n = await this.getTab(e, t.tabId);
      (this.markBrowserUseCommandForTab(e, n),
        this.assertCurrentPageAllowed(e, n),
        await this.attachTab(n),
        this.registerDebuggerListener(n));
    }
    async detachForBrowserUse(e, t) {
      this.requireBrowserUseTab(e, t.tabId);
      let n = this.tabsById.get(t.tabId);
      n != null &&
        (this.markBrowserUseCommandForTab(e, n),
        this.unregisterDebuggerListener(n),
        await this.detachTab(n));
    }
    async executeCdpForBrowserUse(e, t) {
      let n = await this.getTabForCommand(e, t),
        r = this.getBrowserHost(e),
        i = await this.getCdpNavigationUrl(n, t),
        a = !1;
      if ((this.markBrowserUseCommandForTab(e, n), t.method === `Target.closeTarget`))
        return this.executeEmulatedTargetCommand(n, t);
      this.assertCurrentPageAllowed(e, n);
      try {
        r != null &&
          i != null &&
          (r.assertBrowserUseNavigationAllowed(n.pageKey, i),
          r.trackPendingBrowserUseNavigation(n.pageKey, i),
          (a = !0));
        let o = fK(t.method);
        if (
          (o || gK(t.method),
          await this.attachForBrowserUse(e, { tabId: n.cdpTabId }),
          await this.ensureTabReadyForCdp(n),
          o)
        )
          return await this.executeEmulatedTargetCommand(n, t);
        if (t.method === `Page.reload`)
          return (
            CK(t.commandParams) && t.commandParams.ignoreCache === !0
              ? n.webContents.reloadIgnoringCache()
              : n.webContents.reload(),
            {}
          );
        let s = await this.executeTranslatedInputCommand(e, n, t);
        if (s != null) return s;
        try {
          return (
            await r?.waitForPendingDebuggerSync?.(n.browserTabId),
            await this.executeCdpCommandWithCaptureSurface(n, r, t)
          );
        } finally {
          t.method === `Page.close` &&
            (await this.cleanupDebuggerForTabBestEffort(n, `page-close`));
        }
      } catch (e) {
        throw (
          r != null && a && r.clearPendingBrowserUseNavigation(n.pageKey),
          PK(e) && (await this.cleanupDebuggerForTabBestEffort(n, `cdp-timeout`)),
          e
        );
      }
    }
    getRequiredBrowserHost(e) {
      let t = this.getBrowserHost(e);
      if (t == null)
        throw (
          cK().warning(`IAB_LIFECYCLE iab host lookup returned null`, { safe: e, sensitive: {} }),
          Error(`No in-app Browser host is available for this browser-use session`)
        );
      return this.rememberBrowserHost(t);
    }
    rememberBrowserHost(e) {
      return (this.hostsByRouteKey.set(e.routeKey, e), e);
    }
    setPendingCapabilityIntent(e, t) {
      switch (t.kind) {
        case `visibility`:
          t.visible
            ? this.pendingVisibilityRouteKeys.add(e)
            : this.pendingVisibilityRouteKeys.delete(e);
          return;
        case `viewport`:
          t.viewportSize == null
            ? this.pendingViewportSizesByRouteKey.delete(e)
            : this.pendingViewportSizesByRouteKey.set(e, t.viewportSize);
          return;
      }
    }
    applyPendingCapabilityIntents(e, t) {
      let n = this.pendingViewportSizesByRouteKey.get(e.routeKey) ?? null;
      (n != null &&
        (this.pendingViewportSizesByRouteKey.delete(e.routeKey), e.setViewportForBrowserUse(n, t)),
        this.pendingVisibilityRouteKeys.has(e.routeKey) &&
          (this.pendingVisibilityRouteKeys.delete(e.routeKey),
          e.setBrowserVisibleForBrowserUse(!0, t)));
    }
    clearPendingCapabilityIntents(e) {
      (this.pendingVisibilityRouteKeys.delete(e), this.pendingViewportSizesByRouteKey.delete(e));
    }
    getBrowserHostsForSession(e) {
      let t = new Set(),
        n = this.getBrowserHost(e);
      n != null && t.add(this.rememberBrowserHost(n));
      for (let [n, r] of this.hostsByRouteKey.entries()) EK(n) === e.conversationId && t.add(r);
      return [...t];
    }
    updateActivePageTab(e) {
      let t = this.getBrowserHost(e);
      if (t == null) return null;
      this.rememberBrowserHost(t);
      let n = t.getPageForBrowserUse();
      return n == null ? null : this.updateTabForPage(n, t.routeKey);
    }
    updateTabsForSession(e) {
      this.clearDestroyedTabRuntimes();
      let t = this.getRequiredBrowserHost(e);
      if (this.getTabMode(e) !== `multiTab`)
        return t
          .getPagesForBrowserUse()
          .filter((e) => !e.webContents.isDestroyed())
          .map((e) => this.updateTabForPage(e, t.routeKey));
      let n = t.getTabsForBrowserUse(),
        r = new Set(),
        i = n.map((e) => {
          let n = DK(t.routeKey, e.browserTabId);
          r.add(n);
          let i = this.updateTabForInfo(e, t.routeKey),
            a = t.getPageForBrowserUse(e.browserTabId);
          return a == null || a.webContents.isDestroyed()
            ? i
            : this.updateTabForPage(a, t.routeKey, e.origin);
        });
      return (this.pruneMissingTabs(t.routeKey, r), this.clearDestroyedTabRuntimes(), i);
    }
    getUserTabInfosForSession(e) {
      return this.getUserTabsForSession(e).map((e) => ({
        id: this.getUserTabIdForState(e),
        title: e.title,
        url: e.url,
      }));
    }
    getUserTabForTabId(e, t) {
      this.getUserTabInfosForSession(e);
      let n = this.getRequiredBrowserHost(e),
        r = this.routeTabKeysByTabId.get(t) ?? null;
      return r == null || this.routeKeysByTabId.get(t) !== n.routeKey
        ? null
        : (this.userTabsByRouteTabKey.get(r) ?? null);
    }
    getUserTabsForSession(e) {
      this.clearDestroyedTabRuntimes();
      let t = this.getRequiredBrowserHost(e),
        n = t.getUserTabsForBrowserUse(),
        r = new Set(),
        i = [];
      for (let e of n) {
        let n = DK(t.routeKey, e.browserTabId);
        r.add(n);
        let a = { browserTabId: e.browserTabId, routeKey: t.routeKey, title: e.title, url: e.url };
        (this.userTabsByRouteTabKey.set(n, a), i.push(a));
      }
      return (this.pruneMissingUserTabs(t.routeKey, r), i);
    }
    getUserTabIdForState(e) {
      let t = DK(e.routeKey, e.browserTabId),
        n = this.tabIdsByRouteTabKey.get(t) ?? null;
      if (n != null)
        return (this.routeTabKeysByTabId.set(n, t), this.routeKeysByTabId.set(n, e.routeKey), n);
      let r = this.nextCdpTabId;
      return (
        (this.nextCdpTabId += 1),
        this.tabIdsByRouteTabKey.set(t, r),
        this.routeTabKeysByTabId.set(r, t),
        this.routeKeysByTabId.set(r, e.routeKey),
        r
      );
    }
    pruneMissingUserTabs(e, t) {
      let n = `${e}\0`;
      for (let e of Array.from(this.userTabsByRouteTabKey.keys()))
        !e.startsWith(n) || t.has(e) || this.userTabsByRouteTabKey.delete(e);
    }
    forgetUserTabsForRoute(e) {
      let t = `${e}\0`;
      for (let e of Array.from(this.userTabsByRouteTabKey.keys()))
        e.startsWith(t) && this.userTabsByRouteTabKey.delete(e);
    }
    pruneMissingTabs(e, t) {
      for (let n of Array.from(this.tabsById.values())) {
        if (n.routeKey !== e) continue;
        let r = DK(n.routeKey, n.browserTabId);
        t.has(r) ||
          (this.forgetRouteTabId(n),
          this.cleanupDebuggerForTabBestEffort(n, `tab-inventory-pruned`),
          this.removeTab(n));
      }
    }
    forgetTabIdsForRoute(e) {
      let t = `${e}\0`;
      for (let [e, n] of Array.from(this.tabIdsByRouteTabKey.entries()))
        e.startsWith(t) &&
          (this.tabIdsByRouteTabKey.delete(e),
          this.routeTabKeysByTabId.delete(n),
          this.routeKeysByTabId.delete(n));
    }
    moveRouteTabId(e, t, n, r) {
      e !== t && this.tabIdsByRouteTabKey.delete(e);
      let i = this.tabIdsByRouteTabKey.get(t) ?? null;
      if (i != null && i !== n) {
        let e = this.tabsById.get(i) ?? null;
        (e != null && this.removeTab(e),
          this.routeTabKeysByTabId.delete(i),
          this.routeKeysByTabId.delete(i));
      }
      (this.tabIdsByRouteTabKey.set(t, n),
        this.routeTabKeysByTabId.set(n, t),
        this.routeKeysByTabId.set(n, r));
    }
    getActiveBrowserUseTab(e, t = {}) {
      this.clearDestroyedTabRuntimes();
      let n =
        this.getTabMode(e) === `multiTab`
          ? this.getSelectedOrFirstTabForSession(e)
          : this.updateActivePageTab(e);
      return n == null
        ? (this.clearDestroyedTabRuntimes(), null)
        : (t.markBrowserUseCommand !== !1 && this.markBrowserUseCommandForTab(e, n),
          t.assertCurrentPageAllowed !== !1 && this.assertCurrentPageAllowed(e, n),
          this.selectedTabIdsByRouteKey.set(n.routeKey, n.cdpTabId),
          this.clearDestroyedTabRuntimes(),
          n);
    }
    getSelectedOrFirstTabForSession(e) {
      let t = this.getRequiredBrowserHost(e),
        n = this.updateTabsForSession(e),
        r = this.selectedTabIdsByRouteKey.get(t.routeKey) ?? null;
      return (
        (r == null ? null : (n.find((e) => e.cdpTabId === r) ?? null)) ??
        n.find((e) => e.active) ??
        n[0] ??
        null
      );
    }
    updateTabForPage(e, t, n = `agent`, r) {
      let i = this.isMultiTabRoute(t)
          ? r
          : (this.tabIdsByPageKey.get(e.pageKey) ??
            this.getTabIdForWebContents(e.webContents) ??
            r),
        a = this.updateTabForInfo(
          {
            active: e.active,
            browserTabId: e.browserTabId ?? OF(EK(t)),
            origin: n,
            title: e.title,
            url: e.url,
          },
          t,
          i,
        ),
        o = this.tabRuntimesById.get(a.cdpTabId) ?? null,
        s = o?.pageKey ?? null,
        c = o?.webContents ?? null;
      (s != null && s !== e.pageKey && this.tabIdsByPageKey.delete(s),
        c != null && c !== e.webContents && this.unregisterDebuggerListener(a));
      let l = { ...a, pageKey: e.pageKey, webContents: e.webContents };
      return (
        this.tabRuntimesById.set(l.cdpTabId, { pageKey: l.pageKey, webContents: l.webContents }),
        this.tabIdsByPageKey.set(e.pageKey, l.cdpTabId),
        l
      );
    }
    updateTabForInfo(e, t, n) {
      let r = DK(t, e.browserTabId),
        i = this.isMultiTabRoute(t) ? (this.tabIdsByRouteTabKey.get(r) ?? null) : null,
        a = i ?? n ?? this.nextCdpTabId;
      i == null && a >= this.nextCdpTabId && (this.nextCdpTabId = a + 1);
      let o = this.tabsById.get(a) ?? null,
        s = this.routeKeysByTabId.get(a) ?? null;
      (s != null &&
        s !== t &&
        this.selectedTabIdsByRouteKey.get(s) === a &&
        this.selectedTabIdsByRouteKey.delete(s),
        this.isMultiTabRoute(t) &&
          (this.tabIdsByRouteTabKey.set(r, a), this.routeTabKeysByTabId.set(a, r)),
        this.routeKeysByTabId.set(a, t));
      let c = {
        browserTabId: e.browserTabId,
        cdpTabId: a,
        origin: o?.origin ?? e.origin,
        routeKey: t,
        title: e.title,
        url: e.url,
        active: e.active,
      };
      return (this.tabsById.set(a, c), c);
    }
    getTabIdForWebContents(e) {
      for (let [t, n] of this.tabRuntimesById.entries()) if (n.webContents === e) return t;
      return null;
    }
    serializeTab(e) {
      let t = this.tabRuntimesById.get(e.cdpTabId) ?? null,
        n = t == null || t.webContents.isDestroyed() ? `` : t.webContents.getTitle(),
        r = t == null || t.webContents.isDestroyed() ? `` : t.webContents.getURL();
      return {
        id: e.cdpTabId,
        title: AK(n, r, e.title, e.url),
        active: e.cdpTabId === this.selectedTabIdsByRouteKey.get(e.routeKey),
        url: kK(r, e.url),
      };
    }
    markBrowserUseCommandForTab(e, t) {
      let n = this.getBrowserHost(e);
      if (n == null) return;
      this.rememberBrowserHost(n);
      let r = this.tabRuntimesById.get(t.cdpTabId) ?? null;
      (r != null && n.activateBrowserUseNavigationRestriction(r.pageKey),
        n.setBrowserUseActive(!0, t.browserTabId));
    }
    emitBrowserUseNavigationBlockedEvent(e) {
      let t = this.tabIdsByPageKey.get(e.pageKey);
      t != null &&
        this.emitCdpEvent({
          source: { tabId: t },
          method: `Page.navigationBlocked`,
          params: { errorText: e.error.message, url: e.url },
        });
    }
    assertCurrentPageAllowed(e, t) {
      let n = this.tabRuntimesById.get(t.cdpTabId) ?? null;
      n != null &&
        this.getBrowserHost(e)?.assertBrowserUseNavigationAllowed(
          n.pageKey,
          n.webContents.getURL() || t.url,
        );
    }
    assertCurrentTarget(e, t, n) {
      if (t !== pK(e)) throw Error(`${n} can only target the current in-app browser tab`);
    }
    assertTabMatchesCurrentBrowserUsePage(e, t) {
      let n = this.getRequiredBrowserHost(e).getPageForBrowserUse(t.browserTabId);
      if (
        !(
          n != null &&
          n.pageKey === t.pageKey &&
          n.webContents === t.webContents &&
          !t.webContents.isDestroyed()
        )
      )
        throw (
          cK().warning(`IAB_LIFECYCLE iab rejected stale input tab`, {
            safe: {
              ...e,
              expectedPageKey: n?.pageKey ?? null,
              pageWebContentsDestroyed: n?.webContents.isDestroyed() ?? null,
              tabId: t.cdpTabId,
              tabPageKey: t.pageKey,
              tabWebContentsDestroyed: t.webContents.isDestroyed(),
            },
            sensitive: {},
          }),
          Error(
            `Tab ${t.cdpTabId} is no longer attached to the browser page for session ${e.conversationId}`,
          )
        );
    }
    async getCdpNavigationUrl(e, t) {
      let n = _K(t);
      if (n != null) return n;
      let r = vK(t);
      return r == null ? null : this.getHistoryNavigationUrl(e, r);
    }
    async getHistoryNavigationUrl(e, t) {
      let n = e.webContents.debugger.isAttached();
      (await this.attachTab(e), await this.ensureTabReadyForCdp(e));
      try {
        let n = await IK(
            e.webContents.debugger.sendCommand(`Page.getNavigationHistory`, {}),
            this.cdpCommandTimeoutMs,
            `Timed out reading navigation history for tab ${e.cdpTabId}`,
          ),
          r = (Array.isArray(n.entries) ? n.entries : []).find((e) => e.id === t)?.url;
        if (typeof r != `string`)
          throw Error(`Unable to resolve navigation history entry ${t} for tab ${e.cdpTabId}`);
        return r;
      } finally {
        n || (await this.detachTab(e));
      }
    }
    async getTab(e, t) {
      this.clearDestroyedTabRuntimes();
      let n =
        this.getTabMode(e) === `multiTab`
          ? this.getSelectedOrFirstTabForSession(e)
          : this.updateActivePageTab(e);
      if ((this.clearDestroyedTabRuntimes(), n?.cdpTabId === t))
        return (
          this.getAttachedLiveTab(this.getRequiredBrowserHost(e), n) ?? this.requireLiveTab(e, n)
        );
      this.requireBrowserUseTab(e, t);
      let r = this.tabsById.get(t);
      if (r == null) throw Error(`Unknown tab: ${t}`);
      return (
        this.getAttachedLiveTab(this.getRequiredBrowserHost(e), r) ?? this.requireLiveTab(e, r)
      );
    }
    getAttachedLiveTab(e, t) {
      let n = e.getPageForBrowserUse(t.browserTabId);
      return n == null || n.webContents.isDestroyed()
        ? null
        : this.updateTabForPage(n, t.routeKey, t.origin, t.cdpTabId);
    }
    async requireLiveTab(e, t) {
      let n = this.getRequiredBrowserHost(e),
        r = this.getAttachedLiveTab(n, t);
      if (r != null) return r;
      if (
        !this.isMultiTabRoute(t.routeKey) &&
        (this.tabRuntimesById.get(t.cdpTabId) ?? null) != null
      )
        throw Error(`Unknown tab: ${t.cdpTabId}`);
      let i = await n.openPageForBrowserUse({ browserTabId: t.browserTabId });
      return this.updateTabForPage(i, t.routeKey, t.origin, t.cdpTabId);
    }
    async getTabForCommand(e, t) {
      if (t.target.tabId != null) return this.getTab(e, t.target.tabId);
      if (!this.shouldAutoBootstrapNavigateCommand(t) || this.getActiveBrowserUseTab(e) != null)
        throw Error(`executeCdp requires a tabId target`);
      let n = await this.createTabForBrowserUse(e);
      return this.getTab(e, n.id);
    }
    shouldAutoBootstrapNavigateCommand(e) {
      return _K(e) != null;
    }
    clearDestroyedTabRuntimes() {
      for (let e of this.tabsById.values()) {
        let t = this.tabRuntimesById.get(e.cdpTabId) ?? null;
        t == null ||
          !t.webContents.isDestroyed() ||
          (this.unregisterDebuggerListener(e),
          this.tabIdsByPageKey.get(t.pageKey) === e.cdpTabId &&
            this.tabIdsByPageKey.delete(t.pageKey),
          this.tabRuntimesById.delete(e.cdpTabId));
      }
    }
    closeTab(e) {
      let t = this.hostsByRouteKey.get(e.routeKey) ?? null,
        n = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      (t == null && n != null ? n.webContents.close() : t?.closePageForBrowserUse(e.browserTabId),
        this.forgetRouteTabId(e),
        this.removeTab(e),
        this.forgetUserTab(e));
    }
    releaseTab(e) {
      this.hostsByRouteKey.get(e.routeKey)?.releasePageForBrowserUse(e.browserTabId);
      let t = DK(e.routeKey, e.browserTabId);
      (this.userTabsByRouteTabKey.set(t, {
        browserTabId: e.browserTabId,
        routeKey: e.routeKey,
        title: e.title,
        url: e.url,
      }),
        this.removeTab(e));
    }
    removeTab(e) {
      this.tabsById.delete(e.cdpTabId);
      let t = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      (t != null &&
        this.tabIdsByPageKey.get(t.pageKey) === e.cdpTabId &&
        this.tabIdsByPageKey.delete(t.pageKey),
        this.tabRuntimesById.delete(e.cdpTabId),
        this.selectedTabIdsByRouteKey.get(e.routeKey) === e.cdpTabId &&
          this.selectedTabIdsByRouteKey.delete(e.routeKey));
    }
    forgetUserTab(e) {
      let t = DK(e.routeKey, e.browserTabId);
      this.userTabsByRouteTabKey.delete(t);
    }
    forgetRouteTabId(e) {
      if (!this.isMultiTabRoute(e.routeKey)) return;
      let t = DK(e.routeKey, e.browserTabId);
      (this.tabIdsByRouteTabKey.get(t) === e.cdpTabId && this.tabIdsByRouteTabKey.delete(t),
        this.routeTabKeysByTabId.get(e.cdpTabId) === t &&
          this.routeTabKeysByTabId.delete(e.cdpTabId),
        this.routeKeysByTabId.get(e.cdpTabId) === e.routeKey &&
          this.routeKeysByTabId.delete(e.cdpTabId));
    }
    isMultiTabRoute(e) {
      return this.options.getTabMode?.({ conversationId: EK(e) }) === `multiTab`;
    }
    requireBrowserUseTab(e, t) {
      let n = this.getRequiredBrowserHost(e);
      if (
        this.tabsById.get(t)?.routeKey !== n.routeKey &&
        !(
          this.getTabMode(e) === `multiTab` &&
          (this.updateTabsForSession(e), this.tabsById.get(t)?.routeKey === n.routeKey)
        )
      )
        throw (
          cK().warning(`IAB_LIFECYCLE iab rejected tab for route`, {
            safe: {
              ...e,
              expectedRouteKey: n.routeKey,
              tabId: t,
              tabRouteKey: this.routeKeysByTabId.get(t) ?? null,
            },
            sensitive: {},
          }),
          Error(`Tab ${t} is not part of browser session ${e.conversationId}`)
        );
    }
    async detachBrowserUseTabsForHostsBestEffort(e) {
      let t = new Set([...e].map((e) => e.routeKey));
      await Promise.allSettled(
        [...this.tabsById.values()]
          .filter((e) => t.has(e.routeKey))
          .map((e) => this.cleanupDebuggerForTabBestEffort(e, `session-activity-ended`)),
      );
    }
    async cleanupDebuggerForTabBestEffort(e, t) {
      let n = this.tabRuntimesById.get(e.cdpTabId) ?? null;
      this.unregisterDebuggerListener(e, t);
      try {
        await this.detachTab(e, n);
      } catch (r) {
        cK().warning(`IAB_LIFECYCLE failed to detach debugger`, {
          safe: {
            pageKey: n?.pageKey ?? null,
            reason: t,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsDestroyed: n?.webContents.isDestroyed() ?? null,
            webContentsId: n?.webContents.id ?? null,
          },
          sensitive: { error: r },
        });
      }
    }
    registerDebuggerListener(e) {
      if (this.debuggerListeners.has(e.cdpTabId)) return;
      let t = (n, r, i) => {
          if (this.debuggerListeners.get(e.cdpTabId)?.listener !== t) return;
          let a = { source: { tabId: e.cdpTabId }, method: r };
          (uK(i) && (a.params = i), this.emitCdpEvent(a));
        },
        n = () => {
          this.unregisterDebuggerListener(e, `webcontents-destroyed`);
        },
        r = (t, n) => {
          (cK().info(`IAB_LIFECYCLE debugger render process gone`, {
            safe: {
              exitCode: TK(n),
              pageKey: e.pageKey,
              reason: wK(n),
              routeKey: e.routeKey,
              tabId: e.cdpTabId,
              webContentsId: e.webContents.id,
            },
            sensitive: {},
          }),
            this.cleanupDebuggerForTabBestEffort(e, `render-process-gone`));
        };
      (e.webContents.debugger.on(`message`, t),
        e.webContents.once(`destroyed`, n),
        e.webContents.once(`render-process-gone`, r),
        this.debuggerListeners.set(e.cdpTabId, {
          destroyedListener: n,
          listener: t,
          renderProcessGoneListener: r,
          webContents: e.webContents,
          webContentsId: e.webContents.id,
        }),
        cK().info(`IAB_LIFECYCLE registered debugger listener`, {
          safe: {
            pageKey: e.pageKey,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsId: e.webContents.id,
          },
          sensitive: {},
        }));
    }
    unregisterDebuggerListener(e, t = `manual`) {
      let n = this.debuggerListeners.get(e.cdpTabId);
      if (n == null) return;
      this.debuggerListeners.delete(e.cdpTabId);
      let r = n.webContents.isDestroyed();
      (r || n.webContents.debugger.removeListener(`message`, n.listener),
        n.webContents.removeListener(`destroyed`, n.destroyedListener),
        n.webContents.removeListener(`render-process-gone`, n.renderProcessGoneListener),
        cK().info(`IAB_LIFECYCLE unregistered debugger listener`, {
          safe: {
            pageKey: this.tabRuntimesById.get(e.cdpTabId)?.pageKey ?? null,
            reason: t,
            routeKey: e.routeKey,
            tabId: e.cdpTabId,
            webContentsDestroyed: r,
            webContentsId: n.webContentsId,
          },
          sensitive: {},
        }));
    }
    emitCdpEvent(e) {
      for (let t of this.cdpEventListeners) t(e);
    }
    async attachTab(e) {
      if (e.webContents.isDestroyed()) throw Error(`Unknown tab: ${e.cdpTabId}`);
      if (!e.webContents.debugger.isAttached())
        try {
          e.webContents.debugger.attach(`1.3`);
        } catch (e) {
          if ((e instanceof Error ? e.message : String(e)).includes(`already attached`)) return;
          throw e;
        }
    }
    async detachTab(e, t = this.tabRuntimesById.get(e.cdpTabId) ?? null) {
      t == null ||
        t.webContents.isDestroyed() ||
        (t.webContents.debugger.isAttached() && t.webContents.debugger.detach());
    }
    async ensureTabReadyForCdp(e) {
      if (e.webContents.isDestroyed()) throw Error(`Unknown tab: ${e.cdpTabId}`);
      e.webContents.getURL().length > 0 ||
        (await IK(
          e.webContents.loadURL(this.initialPageUrl),
          this.pageReadyTimeoutMs,
          `Timed out loading initial page "${this.initialPageUrl}" for tab ${e.cdpTabId}`,
        ));
    }
    async navigateTabToInitialPage(e) {
      if (e.webContents.isDestroyed()) throw Error(`Unknown tab: ${e.cdpTabId}`);
      e.webContents.getURL() !== this.initialPageUrl &&
        (await IK(
          e.webContents.loadURL(this.initialPageUrl),
          this.pageReadyTimeoutMs,
          `Timed out loading initial page "${this.initialPageUrl}" for tab ${e.cdpTabId}`,
        ),
        (e.url = this.initialPageUrl));
    }
    async executeTranslatedInputCommand(e, t, n) {
      if (!n.method.startsWith(`Input.`)) return null;
      if (!vH(n.method))
        throw Error(
          `${n.method} is not currently supported in the in-app browser because input CDP commands are translated to JavaScript to preserve focus`,
        );
      this.assertTabMatchesCurrentBrowserUsePage(e, t);
      let r = await IK(
        t.webContents.executeJavaScript(FK({ method: n.method, params: n.commandParams ?? {} })),
        this.cdpCommandTimeoutMs,
        `Timed out translating CDP input command "${n.method}" for tab ${t.cdpTabId}`,
      );
      if (CK(r) && r.ok === !0) return {};
      let i = CK(r) && typeof r.error == `string` ? r.error : `unknown translation failure`;
      throw Error(`Unable to translate ${n.method} in the in-app browser: ${i}`);
    }
    async executeEmulatedTargetCommand(e, t) {
      if (t.method === `Target.getTargets`)
        return {
          targetInfos: [...this.tabsById.values()]
            .filter((t) => t.routeKey === e.routeKey)
            .map((e) => mK(e)),
        };
      if (t.method === `Target.closeTarget`) {
        if (hK(t) !== pK(e))
          throw Error(`Target.closeTarget can only close the current in-app browser tab`);
        return (
          await this.cleanupDebuggerForTabBestEffort(e, `target-close`),
          this.closeTab(e),
          { success: !0 }
        );
      }
      throw Error(`Browser Use CDP method is not supported in the in-app browser: ${t.method}`);
    }
    async executeCdpCommandWithCaptureSurface(e, t, n) {
      let r = yK(n);
      if (t == null || r == null) return this.executeCdpCommand(e, n);
      t.setCaptureSurfaceForBrowserUse(r, e.browserTabId);
      try {
        return (await this.waitForCaptureSurface(e, r), await this.executeCdpCommand(e, n));
      } finally {
        t.setCaptureSurfaceForBrowserUse(null, e.browserTabId);
      }
    }
    async executeCdpCommand(e, t) {
      return (
        gK(t.method),
        await IK(
          e.webContents.debugger.sendCommand(t.method, t.commandParams ?? {}),
          this.cdpCommandTimeoutMs,
          `Timed out running CDP command "${t.method}" for tab ${e.cdpTabId}`,
        )
      );
    }
    async waitForCaptureSurface(e, t) {
      let n = Date.now() + aK;
      do {
        let r;
        try {
          r = await IK(
            e.webContents.debugger.sendCommand(`Page.getLayoutMetrics`, {}),
            Math.max(1, n - Date.now()),
            `Timed out reading layout metrics for tab ${e.cdpTabId}`,
          );
        } catch {
          return;
        }
        if (bK(r, t)) return;
        await NK(oK);
      } while (Date.now() < n);
    }
  };
function uK(e) {
  return typeof e == `object` && !!e;
}
function dK(e, t) {
  let n = new Map();
  for (let r of e) {
    if (r == null) throw Error(`finalizeTabs received invalid tab entry`);
    let { tabId: e, status: i } = r;
    if (!Number.isInteger(e)) throw Error(`finalizeTabs requires an integer tabId`);
    if (!t.has(e)) throw Error(`finalizeTabs cannot keep unknown tab ${e}`);
    if (i !== `handoff` && i !== `deliverable`)
      throw Error(`finalizeTabs received invalid status ${String(i)}`);
    if (n.has(e)) throw Error(`finalizeTabs received duplicate tab ${e}`);
    n.set(e, i);
  }
  return n;
}
function fK(e) {
  return e === `Target.getTargets` || e === `Target.closeTarget`;
}
function pK(e) {
  return `browser-use-iab-tab:${e.cdpTabId}`;
}
function mK(e) {
  let t = pK(e);
  return {
    attached: !0,
    canAccessOpener: !1,
    id: t,
    tabId: e.cdpTabId,
    targetId: t,
    title: e.title,
    type: `page`,
    url: e.url,
  };
}
function hK(e) {
  if (!CK(e.commandParams)) return null;
  let t = e.commandParams.targetId;
  return typeof t == `string` ? t : null;
}
function gK(e) {
  if (e.startsWith(`Target.`))
    throw Error(`Browser Use CDP method is not supported in the in-app browser: ${e}`);
}
function _K(e) {
  if (e.method !== `Page.navigate` || !CK(e.commandParams)) return null;
  let t = e.commandParams.url;
  return typeof t == `string` ? t : null;
}
function vK(e) {
  if (e.method !== `Page.navigateToHistoryEntry` || !CK(e.commandParams)) return null;
  let t = e.commandParams.entryId;
  return typeof t == `number` ? t : null;
}
function yK(e) {
  if (
    e.method !== `Page.captureScreenshot` ||
    !CK(e.commandParams) ||
    e.commandParams.captureBeyondViewport !== !0
  )
    return null;
  let t = e.commandParams.clip;
  if (!CK(t)) return null;
  let n = t.width,
    r = t.height;
  return typeof n != `number` ||
    typeof r != `number` ||
    !Number.isFinite(n) ||
    !Number.isFinite(r) ||
    n <= 0 ||
    r <= 0
    ? null
    : { width: Math.ceil(n), height: Math.ceil(r) };
}
function bK(e, t) {
  let n = xK(e);
  return n == null
    ? !1
    : SK(n, `clientWidth`, `width`) >= t.width && SK(n, `clientHeight`, `height`) >= t.height;
}
function xK(e) {
  return CK(e)
    ? CK(e.cssVisualViewport)
      ? e.cssVisualViewport
      : CK(e.visualViewport)
        ? e.visualViewport
        : null
    : null;
}
function SK(e, t, n) {
  let r = e[t];
  if (typeof r == `number`) return r;
  let i = e[n];
  return typeof i == `number` ? i : 0;
}
function CK(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function wK(e) {
  if (!CK(e)) return null;
  let t = e.reason;
  return typeof t == `string` ? t : null;
}
function TK(e) {
  if (!CK(e)) return null;
  let t = e.exitCode;
  return typeof t == `number` ? t : null;
}
function EK(e) {
  return e.slice(e.indexOf(`:`) + 1);
}
function DK(e, t) {
  return `${e}\0${t}`;
}
function OK(e, n) {
  let r = `${e}\0`;
  return n.startsWith(r) ? t.rs(n.slice(r.length)) : null;
}
function kK(e, t) {
  return t.length > 0 && t !== `about:blank` && (e.length === 0 || e === `about:blank` || AF(e))
    ? t
    : e || t;
}
function AK(e, t, n, r) {
  return n.length > 0 &&
    n !== `about:blank` &&
    r.length > 0 &&
    r !== `about:blank` &&
    (e.length === 0 ||
      e === `about:blank` ||
      AF(e) ||
      t.length === 0 ||
      t === `about:blank` ||
      AF(t))
    ? n
    : e || n;
}
function jK(e, t) {
  if (!Number.isFinite(t.x) || !Number.isFinite(t.y))
    throw Error(`${e} requires finite x and y coordinates`);
}
function MK(e) {
  typeof e == `object` && `unref` in e && e.unref();
}
async function NK(e) {
  await new Promise((t) => {
    MK(setTimeout(t, e));
  });
}
function PK(e) {
  return e instanceof Error ? e.message.startsWith(`Timed out`) : !1;
}
function FK(e) {
  return `(${yH.toString()})(${JSON.stringify(e)});`;
}
function IK(e, t, n) {
  return new Promise((r, i) => {
    let a = !1,
      o = setTimeout(() => {
        a || ((a = !0), i(Error(n)));
      }, t);
    (o.unref(),
      e.then(
        (e) => {
          a || ((a = !0), clearTimeout(o), r(e));
        },
        (e) => {
          a || ((a = !0), clearTimeout(o), i(e));
        },
      ));
  });
}
var LK = t.Gr(`browser-session-registry`);
function RK(e) {
  return e;
}
var zK = 200,
  BK = class {
    delegate = null;
    browserUseNativePipeEnabled = !1;
    backendState = { apiImpl: null, server: null, starting: null };
    hostsByRouteKey = new Map();
    debugEvents = [];
    nextDebugEventSequence = 1;
    sessionRoutes = new Map();
    windows = new Map();
    tabModesByRouteKey = new Map();
    constructor(
      e = {
        appSessionId: r.N,
        buildFlavor: n.i.Dev,
        errorReporter: { reportNonFatal: () => void 0 },
      },
    ) {
      this.options = e;
    }
    setDelegate(e) {
      this.delegate = e;
    }
    recordDebugEvent({ details: e = [], ...t }) {
      (this.debugEvents.push({
        ...t,
        details: [...e],
        sequence: this.nextDebugEventSequence,
        timestampMs: Date.now(),
      }),
        (this.nextDebugEventSequence += 1),
        this.debugEvents.length > zK && this.debugEvents.shift());
    }
    getDebugSnapshot() {
      return {
        events: this.debugEvents.map((e) => ({ ...e, details: [...e.details] })),
        sessionRoutes: Array.from(this.sessionRoutes.values()).map((e) => {
          let t = Array.from(this.windows.get(e.windowId)?.pages.values() ?? []),
            n =
              t.find(
                (t) =>
                  t.conversationId === e.conversationId && t.browserTabId === OF(e.conversationId),
              ) ??
              t.find((t) => t.conversationId === e.conversationId) ??
              null;
          return {
            conversationId: e.conversationId,
            disposeAfterSessionActivity: e.disposeAfterSessionActivity,
            ownerWebContentsId: e.ownerWebContentsId,
            page: n == null ? null : { ...n },
            windowId: e.windowId,
          };
        }),
      };
    }
    setBrowserUseNativePipeEnabled(e) {
      if (this.browserUseNativePipeEnabled !== e) {
        if (((this.browserUseNativePipeEnabled = e), !e)) {
          this.disposeBackend();
          return;
        }
        this.ensureBackend();
      }
    }
    setRouteTabMode(e, t) {
      let n = HK(e),
        r = t === `multiTab` ? `multiTab` : `singleTab`;
      this.getRouteTabMode(e) !== r &&
        (this.tabModesByRouteKey.set(n, r),
        LK().info(`IAB_LIFECYCLE set browser route tab mode`, {
          safe: {
            conversationId: e.conversationId,
            requestedTabMode: t,
            tabMode: r,
            windowId: e.windowId,
          },
          sensitive: {},
        }));
    }
    getRouteTabMode(e) {
      return this.tabModesByRouteKey.get(HK(e)) ?? `singleTab`;
    }
    captureSessionRoute({
      conversationId: e,
      disposeAfterSessionActivity: t = !1,
      multiTabBrowserUseEnabled: n = !1,
      owner: r,
    }) {
      let i = a.BrowserWindow.fromWebContents(r);
      if (i == null || i.isDestroyed()) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          guestWebContentsId: null,
          kind: `session-route-capture-failed`,
          message: `Failed to capture Browser Use session route`,
          ownerWebContentsId: r.id,
          pageKey: null,
          windowId: null,
        }),
          LK().warning(`IAB_LIFECYCLE failed to capture session route`, {
            safe: { conversationId: e, ownerWebContentsId: r.id },
            sensitive: {},
          }));
        return;
      }
      let o = RK(i.id),
        s = { conversationId: e, windowId: o },
        c = this.tabModesByRouteKey.get(HK(s));
      this.setRouteTabMode(s, n ? `multiTab` : (c ?? `singleTab`));
      let l = this.sessionRoutes.get(e);
      if (l != null) {
        if (l.windowId === o) {
          if (l.ownerWebContentsId !== r.id) {
            let t = l.ownerWebContentsId;
            ((l.ownerWebContentsId = r.id),
              this.recordDebugEvent({
                browserTabId: null,
                conversationId: e,
                details: [{ label: `previousOwnerWebContentsId`, value: String(t) }],
                guestWebContentsId: null,
                kind: `session-route-owner-rebound`,
                message: `Rebound Browser Use session route owner`,
                ownerWebContentsId: r.id,
                pageKey: null,
                windowId: i.id,
              }),
              LK().info(`IAB_LIFECYCLE rebound session route owner`, {
                safe: {
                  conversationId: e,
                  ownerWebContentsId: r.id,
                  previousOwnerWebContentsId: t,
                  windowId: i.id,
                },
                sensitive: {},
              }));
          }
          t &&
            !l.disposeAfterSessionActivity &&
            ((l.disposeAfterSessionActivity = !0),
            this.recordDebugEvent({
              browserTabId: null,
              conversationId: e,
              guestWebContentsId: null,
              kind: `session-route-disposal-updated`,
              message: `Marked Browser Use session route disposable`,
              ownerWebContentsId: r.id,
              pageKey: null,
              windowId: i.id,
            }),
            LK().info(`IAB_LIFECYCLE updated session route disposal`, {
              safe: {
                conversationId: e,
                disposeAfterSessionActivity: t,
                ownerWebContentsId: r.id,
                windowId: i.id,
              },
              sensitive: {},
            }));
          return;
        }
        let a = { conversationId: e, windowId: l.windowId },
          c = this.windows.get(a.windowId) ?? null,
          u = this.getPageRecordForConversation(a.windowId, e);
        if (c == null || !this.hasPageRecordForConversation(c, e)) {
          let o = l.ownerWebContentsId,
            c = a.windowId;
          (this.reassignRoute(a, s),
            (l.ownerWebContentsId = r.id),
            t && !l.disposeAfterSessionActivity && (l.disposeAfterSessionActivity = !0),
            n && this.setRouteTabMode(s, `multiTab`),
            this.recordDebugEvent({
              browserTabId: null,
              conversationId: e,
              details: [
                { label: `previousOwnerWebContentsId`, value: String(o) },
                { label: `previousWindowId`, value: String(c) },
              ],
              guestWebContentsId: null,
              kind: `session-route-capture-reassigned`,
              message: `Reassigned duplicate Browser Use session route capture`,
              ownerWebContentsId: r.id,
              pageKey: null,
              windowId: i.id,
            }),
            LK().info(`IAB_LIFECYCLE reassigned duplicate session route`, {
              safe: {
                conversationId: e,
                ownerWebContentsId: r.id,
                previousOwnerWebContentsId: o,
                previousWindowId: c,
                windowId: i.id,
              },
              sensitive: {},
            }));
          return;
        }
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          details: [
            { label: `existingOwnerWebContentsId`, value: String(l.ownerWebContentsId) },
            { label: `existingWindowId`, value: String(l.windowId) },
          ],
          guestWebContentsId: u?.guestWebContentsId ?? null,
          kind: `session-route-capture-ignored`,
          message: `Ignored duplicate Browser Use session route capture`,
          ownerWebContentsId: r.id,
          pageKey: u?.pageKey ?? null,
          windowId: i.id,
        }),
          LK().warning(`IAB_LIFECYCLE ignored duplicate session route capture`, {
            safe: {
              conversationId: e,
              existingOwnerWebContentsId: l.ownerWebContentsId,
              existingWindowId: l.windowId,
              ownerWebContentsId: r.id,
              windowId: i.id,
            },
            sensitive: {},
          }));
        return;
      }
      this.captureSessionRouteForWindow({
        conversationId: e,
        disposeAfterSessionActivity: t,
        ownerWebContentsId: r.id,
        windowId: o,
      });
    }
    bindSessionRouteOwner(e, t) {
      let n = this.sessionRoutes.get(e);
      if (n == null) {
        this.captureSessionRoute({ conversationId: e, owner: t });
        return;
      }
      let r = a.BrowserWindow.fromWebContents(t);
      if (r == null || r.isDestroyed()) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          guestWebContentsId: null,
          kind: `session-route-bind-failed`,
          message: `Failed to bind Browser Use session route owner`,
          ownerWebContentsId: t.id,
          pageKey: null,
          windowId: null,
        }),
          LK().warning(`IAB_LIFECYCLE failed to rebind session route`, {
            safe: { conversationId: e, ownerWebContentsId: t.id },
            sensitive: {},
          }));
        return;
      }
      let i = { conversationId: e, windowId: n.windowId },
        o = { conversationId: e, windowId: RK(r.id) };
      if (i.windowId === o.windowId && n.ownerWebContentsId === t.id) return;
      let s = this.windows.get(i.windowId) ?? null,
        c = this.getPageRecordForConversation(i.windowId, e);
      if (i.windowId !== o.windowId && s != null && this.hasPageRecordForConversation(s, e)) {
        (this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          details: [
            { label: `previousOwnerWebContentsId`, value: String(n.ownerWebContentsId) },
            { label: `previousWindowId`, value: String(i.windowId) },
          ],
          guestWebContentsId: c?.guestWebContentsId ?? null,
          kind: `session-route-live-rebind-ignored`,
          message: `Ignored Browser Use session route rebind with existing page`,
          ownerWebContentsId: t.id,
          pageKey: c?.pageKey ?? null,
          windowId: o.windowId,
        }),
          LK().warning(`IAB_LIFECYCLE ignored session route rebind`, {
            safe: {
              conversationId: e,
              ownerWebContentsId: t.id,
              previousOwnerWebContentsId: n.ownerWebContentsId,
              previousWindowId: i.windowId,
              windowId: o.windowId,
            },
            sensitive: {},
          }));
        return;
      }
      let l = n.ownerWebContentsId,
        u = i.windowId;
      (this.reassignRoute(i, o),
        (n.ownerWebContentsId = t.id),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          details: [
            { label: `previousOwnerWebContentsId`, value: String(l) },
            { label: `previousWindowId`, value: String(u) },
          ],
          guestWebContentsId: null,
          kind: `session-route-owner-bound`,
          message: `Bound Browser Use session route to exact owner`,
          ownerWebContentsId: t.id,
          pageKey: null,
          windowId: o.windowId,
        }),
        LK().info(`IAB_LIFECYCLE rebound captured session route`, {
          safe: {
            conversationId: e,
            ownerWebContentsId: t.id,
            previousOwnerWebContentsId: l,
            previousWindowId: u,
            windowId: o.windowId,
          },
          sensitive: {},
        }));
    }
    ensureSessionRoute(e) {
      let t = this.sessionRoutes.get(e.conversationId);
      if (t != null) {
        if (this.delegate?.isWindowAlive(t.windowId) === !0) return !0;
        (this.disposeBrowserUseRoute({ conversationId: t.conversationId, windowId: t.windowId }),
          this.recordDebugEvent({
            browserTabId: null,
            conversationId: e.conversationId,
            guestWebContentsId: null,
            kind: `session-route-stale-discarded`,
            message: `Discarded stale Browser Use session route`,
            ownerWebContentsId: t.ownerWebContentsId,
            pageKey: null,
            windowId: t.windowId,
          }),
          LK().warning(`IAB_LIFECYCLE discarded stale session route`, {
            safe: {
              conversationId: e.conversationId,
              ownerWebContentsId: t.ownerWebContentsId,
              windowId: t.windowId,
            },
            sensitive: {},
          }));
      }
      return (
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `session-route-missing`,
          message: `Missing exact Browser Use session route owner`,
          ownerWebContentsId: null,
          pageKey: null,
          windowId: null,
        }),
        LK().warning(`IAB_LIFECYCLE missing exact session route owner`, {
          safe: { conversationId: e.conversationId },
          sensitive: {},
        }),
        !1
      );
    }
    captureSessionRouteForWindow({
      conversationId: e,
      disposeAfterSessionActivity: t,
      ownerWebContentsId: n,
      windowId: r,
    }) {
      (this.sessionRoutes.set(e, {
        conversationId: e,
        disposeAfterSessionActivity: t,
        ownerWebContentsId: n,
        windowId: r,
      }),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e,
          details: [{ label: `disposeAfterSessionActivity`, value: String(t) }],
          guestWebContentsId: null,
          kind: `session-route-captured`,
          message: `Captured Browser Use session route`,
          ownerWebContentsId: n,
          pageKey: null,
          windowId: r,
        }),
        LK().info(`IAB_LIFECYCLE captured session route`, {
          safe: {
            conversationId: e,
            disposeAfterSessionActivity: t,
            ownerWebContentsId: n,
            windowId: r,
          },
          sensitive: {},
        }));
    }
    getBrowserUseHost(e) {
      let t = this.resolveBrowserRoute(e);
      return this.getOrCreateBrowserUseHost(t);
    }
    endSessionActivity({ conversationId: e }) {
      let t = this.sessionRoutes.get(e);
      if (t == null) return;
      let n = { conversationId: t.conversationId, windowId: t.windowId },
        r = HK(n);
      this.backendState.apiImpl?.releaseSessionControl({ conversationId: e }).catch(() => void 0);
      let i = this.hostsByRouteKey.get(r);
      (i == null ? this.delegate?.setBrowserUseActive(n, !1) : i.setBrowserUseActive(!1),
        t.disposeAfterSessionActivity && this.disposeBrowserUseRoute(n),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t.conversationId,
          guestWebContentsId: null,
          kind: `session-activity-ended`,
          message: `Ended Browser Use session activity`,
          ownerWebContentsId: t.ownerWebContentsId,
          pageKey: null,
          windowId: t.windowId,
        }),
        LK().info(`IAB_LIFECYCLE ended browser use session activity`, {
          safe: {
            conversationId: t.conversationId,
            disposeAfterSessionActivity: t.disposeAfterSessionActivity,
            windowId: t.windowId,
          },
          sensitive: {},
        }));
    }
    notifyBrowserUseCursorArrived({ owner: e, payload: t }) {
      let n = this.sessionRoutes.get(t.conversationId);
      if (n != null) {
        if (n.ownerWebContentsId !== e.id) {
          LK().warning(`IAB_LIFECYCLE ignored cursor arrival from stale owner`, {
            safe: {
              conversationId: t.conversationId,
              expectedOwnerWebContentsId: n.ownerWebContentsId,
              moveSequence: t.moveSequence,
              ownerWebContentsId: e.id,
            },
            sensitive: {},
          });
          return;
        }
        this.backendState.apiImpl?.notifyCursorArrived(t);
      }
    }
    getOrCreateBrowserUseHost(e) {
      this.assertWindowAlive(e);
      let t = HK(e),
        n = this.hostsByRouteKey.get(t);
      if (n != null) return n;
      let r = new VK(this, e, t);
      return (
        this.hostsByRouteKey.set(t, r),
        LK().info(`IAB_LIFECYCLE created browser use host`, {
          safe: { conversationId: e.conversationId, routeKey: t, windowId: e.windowId },
          sensitive: {},
        }),
        r
      );
    }
    handleWindowClosed(e) {
      let t = RK(e);
      this.windows.delete(t);
      for (let e of this.tabModesByRouteKey.keys())
        e.startsWith(`${t}:`) && this.tabModesByRouteKey.delete(e);
      for (let [e, n] of this.hostsByRouteKey)
        n.windowId === t &&
          (this.backendState.apiImpl?.disposeRoute(e), this.hostsByRouteKey.delete(e));
      for (let [e, n] of this.sessionRoutes) n.windowId === t && this.sessionRoutes.delete(e);
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: null,
        guestWebContentsId: null,
        kind: `browser-window-removed`,
        message: `Removed Browser session window`,
        ownerWebContentsId: null,
        pageKey: null,
        windowId: t,
      }),
        LK().info(`IAB_LIFECYCLE removed browser session window`, {
          safe: { windowId: t },
          sensitive: {},
        }));
    }
    recordGuestAttached({
      browserTabId: e,
      conversationId: t,
      guestWebContentsId: n,
      pageKey: r,
      windowId: i,
    }) {
      let a = this.ensurePageRecord(
        { browserTabId: e, conversationId: t, windowId: i },
        { pageKey: r },
      );
      ((a.guestWebContentsId = n),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t,
          guestWebContentsId: n,
          kind: `browser-guest-attached`,
          message: `Attached Browser sidebar guest`,
          ownerWebContentsId: this.windows.get(i)?.ownerWebContentsId ?? null,
          pageKey: r,
          windowId: i,
        }),
        LK().info(`IAB_LIFECYCLE attached browser sidebar guest`, {
          safe: {
            conversationId: t,
            browserTabId: e,
            guestWebContentsId: n,
            pageKey: r,
            windowId: i,
          },
          sensitive: {},
        }));
    }
    recordPageClosed(e) {
      (this.forgetPageRecord(e),
        LK().info(`IAB_LIFECYCLE closed browser sidebar page`, { safe: e, sensitive: {} }));
    }
    recordPageReleased(e) {
      (this.forgetPageRecord(e),
        LK().info(`IAB_LIFECYCLE released browser sidebar page`, { safe: e, sensitive: {} }));
    }
    forgetPageRecord(e) {
      let t = this.windows.get(e.windowId) ?? null;
      (t?.pages.delete(UK(e)),
        t != null &&
          !this.hasPageRecordForConversation(t, e.conversationId) &&
          t.conversations.delete(e.conversationId));
      let n = HK(e),
        r = t != null && this.hasPageRecordForConversation(t, e.conversationId);
      (t == null || !r) &&
        (this.hostsByRouteKey.delete(n), this.backendState.apiImpl?.disposeRoute(n));
    }
    recordPageCreated(e) {
      (this.ensurePageRecord(e),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `browser-page-created`,
          message: `Created Browser sidebar page state`,
          ownerWebContentsId: this.windows.get(e.windowId)?.ownerWebContentsId ?? null,
          pageKey: null,
          windowId: e.windowId,
        }),
        LK().info(`IAB_LIFECYCLE created browser sidebar page state`, { safe: e, sensitive: {} }));
    }
    reassignRoute(e, t, n, r) {
      if (
        e.conversationId === t.conversationId &&
        e.windowId === t.windowId &&
        (r == null || n === r)
      )
        return;
      let i = HK(e),
        a = HK(t),
        o = this.tabModesByRouteKey.get(i) ?? null,
        s = this.windows.get(e.windowId) ?? null,
        c = this.ensureWindowRecord(t.windowId);
      for (let [i, a] of s?.pages ?? []) {
        if (a.conversationId !== e.conversationId || (n != null && a.browserTabId !== n)) continue;
        s?.pages.delete(i);
        let o = r ?? a.browserTabId;
        c.pages.set(
          UK({ browserTabId: o, conversationId: t.conversationId, windowId: t.windowId }),
          { ...a, browserTabId: o, conversationId: t.conversationId, windowId: t.windowId },
        );
      }
      (s != null &&
        !this.hasPageRecordForConversation(s, e.conversationId) &&
        s.conversations.delete(e.conversationId),
        c.conversations.add(t.conversationId));
      let l = this.hostsByRouteKey.get(i) ?? null;
      (l != null &&
        (this.hostsByRouteKey.delete(i), l.reassignRoute(t, a), this.hostsByRouteKey.set(a, l)),
        o != null && (this.tabModesByRouteKey.delete(i), this.tabModesByRouteKey.set(a, o)));
      let u = this.sessionRoutes.get(e.conversationId);
      (u?.windowId === e.windowId &&
        (this.sessionRoutes.delete(e.conversationId),
        (u.conversationId = t.conversationId),
        (u.windowId = t.windowId),
        this.sessionRoutes.set(t.conversationId, u)),
        i === a
          ? this.backendState.apiImpl?.reassignRoute(i, a, n, r)
          : (this.backendState.apiImpl?.reassignRoute(i, a),
            n != null &&
              r != null &&
              n !== r &&
              this.backendState.apiImpl?.reassignRoute(a, a, n, r)),
        LK().info(`IAB_LIFECYCLE reassigned browser use route`, {
          safe: {
            sourceConversationId: e.conversationId,
            sourceWindowId: e.windowId,
            targetConversationId: t.conversationId,
            targetWindowId: t.windowId,
          },
          sensitive: {},
        }));
    }
    disposeConversation(e) {
      let t = new Map();
      for (let n of this.windows.values()) {
        if (!n.conversations.has(e)) continue;
        let r = { conversationId: e, windowId: n.windowId };
        t.set(HK(r), r);
      }
      for (let n of this.hostsByRouteKey.values()) {
        if (n.conversationId !== e) continue;
        let r = { conversationId: e, windowId: n.windowId };
        t.set(HK(r), r);
      }
      let n = this.sessionRoutes.get(e);
      if (n != null) {
        this.sessionRoutes.delete(e);
        let r = { conversationId: e, windowId: n.windowId };
        t.set(HK(r), r);
      }
      for (let e of t.values()) this.disposeBrowserUseRoute(e);
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: e,
        details: [{ label: `routeCount`, value: String(t.size) }],
        guestWebContentsId: null,
        kind: `browser-conversation-disposed`,
        message: `Disposed Browser Use conversation`,
        ownerWebContentsId: null,
        pageKey: null,
        windowId: null,
      }),
        LK().info(`IAB_LIFECYCLE disposed browser use conversation`, {
          safe: { conversationId: e, routeCount: t.size },
          sensitive: {},
        }));
    }
    recordPageTornDown({ browserTabId: e, conversationId: t, guestWebContentsId: n, windowId: r }) {
      let i =
        this.windows.get(r)?.pages.get(UK({ browserTabId: e, conversationId: t, windowId: r })) ??
        null;
      (i?.guestWebContentsId === n && (i.guestWebContentsId = null),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t,
          guestWebContentsId: n,
          kind: `browser-guest-torn-down`,
          message: `Tore down Browser sidebar guest`,
          ownerWebContentsId: this.windows.get(r)?.ownerWebContentsId ?? null,
          pageKey: i?.pageKey ?? null,
          windowId: r,
        }),
        LK().warning(`IAB_LIFECYCLE browser sidebar guest torn down`, {
          safe: { conversationId: t, browserTabId: e, guestWebContentsId: n, windowId: r },
          sensitive: {},
        }));
    }
    registerThread(e) {
      (this.ensureWindowRecord(e.windowId).conversations.add(e.conversationId),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: e.conversationId,
          guestWebContentsId: null,
          kind: `browser-thread-registered`,
          message: `Registered Browser sidebar thread`,
          ownerWebContentsId: this.windows.get(e.windowId)?.ownerWebContentsId ?? null,
          pageKey: null,
          windowId: e.windowId,
        }),
        LK().info(`IAB_LIFECYCLE registered browser sidebar thread`, { safe: e, sensitive: {} }));
    }
    registerWindow({ ownerWebContentsId: e, windowId: t }) {
      let n = RK(t),
        r = (this.windows.get(n) ?? null)?.ownerWebContentsId,
        i = this.ensureWindowRecord(n);
      if (((i.ownerWebContentsId = e), r != null && r !== e))
        for (let t of this.sessionRoutes.values()) t.windowId === n && (t.ownerWebContentsId = e);
      (this.recordDebugEvent({
        browserTabId: null,
        conversationId: null,
        details: r == null ? [] : [{ label: `previousOwnerWebContentsId`, value: String(r) }],
        guestWebContentsId: null,
        kind: r == null ? `browser-window-registered` : `browser-window-owner-rebound`,
        message:
          r == null ? `Registered Browser sidebar window` : `Rebound Browser sidebar window owner`,
        ownerWebContentsId: e,
        pageKey: null,
        windowId: n,
      }),
        LK().info(
          r == null
            ? `IAB_LIFECYCLE registered browser sidebar window`
            : `IAB_LIFECYCLE rebound browser sidebar window`,
          {
            safe: { ownerWebContentsId: e, previousOwnerWebContentsId: r, windowId: n },
            sensitive: {},
          },
        ));
    }
    resolveBrowserRoute(e) {
      let t = this.sessionRoutes.get(e.conversationId);
      if (t == null)
        throw (
          LK().warning(`IAB_LIFECYCLE missing browser use session route`, {
            safe: e,
            sensitive: {},
          }),
          Error(`No Codex browser route captured for browser session ${e.conversationId}`)
        );
      let n = { conversationId: t.conversationId, windowId: t.windowId };
      return (
        this.assertWindowAlive(n),
        this.recordDebugEvent({
          browserTabId: null,
          conversationId: t.conversationId,
          guestWebContentsId:
            this.windows.get(t.windowId)?.pages.get(t.conversationId)?.guestWebContentsId ?? null,
          kind: `session-route-resolved`,
          message: `Resolved Browser Use route`,
          ownerWebContentsId: t.ownerWebContentsId,
          pageKey: this.windows.get(t.windowId)?.pages.get(t.conversationId)?.pageKey ?? null,
          windowId: t.windowId,
        }),
        LK().info(`IAB_LIFECYCLE resolved browser use route`, {
          safe: {
            conversationId: t.conversationId,
            ownerWebContentsId: t.ownerWebContentsId,
            windowId: t.windowId,
          },
          sensitive: {},
        }),
        n
      );
    }
    isSubagentBrowserUseRoute(e) {
      let t = this.sessionRoutes.get(e.conversationId);
      return t?.disposeAfterSessionActivity === !0 && t.windowId === e.windowId;
    }
    recordPageSnapshot({
      browserTabId: e,
      conversationId: t,
      guestWebContentsId: n,
      isLoading: r,
      pageKey: i,
      url: a,
      windowId: o,
    }) {
      let s = this.ensurePageRecord(
          { browserTabId: e, conversationId: t, windowId: o },
          { pageKey: i },
        ),
        c = s.guestWebContentsId,
        l = s.isLoading,
        u = s.pageKey;
      ((s.guestWebContentsId = n),
        (s.isLoading = r),
        (s.pageKey = i),
        (s.url = a),
        (c !== n || l !== r || u !== i) &&
          this.recordDebugEvent({
            browserTabId: null,
            conversationId: t,
            details: [{ label: `isLoading`, value: String(r) }],
            guestWebContentsId: n,
            kind: `browser-page-snapshot-updated`,
            message: `Updated Browser sidebar page snapshot`,
            ownerWebContentsId: this.windows.get(o)?.ownerWebContentsId ?? null,
            pageKey: i,
            windowId: o,
          }));
    }
    getPageSnapshot(e) {
      let t = this.windows.get(e.windowId)?.pages.get(UK(e)) ?? null;
      return t == null ? null : { ...t };
    }
    getWindowSnapshot(e) {
      let t = RK(e),
        n = this.windows.get(t) ?? null;
      return n == null
        ? null
        : {
            conversations: Array.from(n.conversations),
            ownerWebContentsId: n.ownerWebContentsId,
            pages: Array.from(n.pages.values()).map((e) => ({ ...e })),
            windowId: t,
          };
    }
    getSnapshotFields() {
      let e = 0,
        t = 0,
        n = 0,
        r = 0;
      for (let i of this.windows.values()) {
        ((e += i.conversations.size), (r += i.pages.size));
        for (let e of i.pages.values())
          (e.guestWebContentsId != null && (t += 1), e.isLoading && (n += 1));
      }
      return {
        browser_use_window_count: this.windows.size,
        browser_use_conversation_count: e,
        browser_use_page_count: r,
        browser_use_guest_webcontents_count: t,
        browser_use_loading_page_count: n,
        browser_use_session_route_count: this.sessionRoutes.size,
      };
    }
    assertWindowAlive(e) {
      if (this.delegate?.isWindowAlive(e.windowId) !== !0)
        throw (
          LK().warning(`IAB_LIFECYCLE browser use bound window unavailable`, {
            safe: e,
            sensitive: {},
          }),
          Error(
            `Codex browser window ${e.windowId} is no longer available for browser session ${e.conversationId}`,
          )
        );
    }
    ensureBackend() {
      this.browserUseNativePipeEnabled &&
        (this.backendState.server != null ||
          this.backendState.starting != null ||
          (this.backendState.starting = (async () => {
            let e = null;
            try {
              (LK().info(`browser_use_iab_backend_startup_started`, {
                safe: { backend: `iab`, platform: process.platform, release: a.app.getVersion() },
                sensitive: { appSessionId: this.options.appSessionId },
              }),
                (e = new lK(
                  (e) => (this.ensureSessionRoute(e) ? this.getBrowserUseHost(e) : null),
                  (e) => this.getDelegate().addBrowserUseNavigationBlockedListener(e),
                  {
                    appSessionId: this.options.appSessionId,
                    buildFlavor: this.options.buildFlavor,
                    ensureSessionRoute: (e) => this.ensureSessionRoute(e),
                    getTabMode: (e) => this.getRouteTabMode(this.resolveBrowserRoute(e)),
                  },
                )),
                (this.backendState.apiImpl = e),
                (this.backendState.server = await _H({ apiImpl: e })),
                LK().info(`browser_use_iab_backend_startup_ready`, {
                  safe: { backend: `iab`, platform: process.platform, release: a.app.getVersion() },
                  sensitive: { pipePath: this.backendState.server.pipePath },
                }));
            } catch (t) {
              (LK().warning(`browser_use_iab_backend_startup_failed`, {
                safe: {
                  backend: `iab`,
                  phase: `native-pipe-startup`,
                  platform: process.platform,
                  reason: t instanceof Error ? t.name || `Error` : typeof t,
                  release: a.app.getVersion(),
                },
                sensitive: { error: t },
              }),
                e?.dispose(),
                (this.backendState.apiImpl = null),
                this.options.errorReporter.reportNonFatal(
                  t instanceof Error ? t : Error(String(t)),
                  { kind: `browser-use-native-pipe-startup` },
                ));
            } finally {
              this.backendState.starting = null;
            }
          })()));
    }
    async disposeBackend() {
      (this.backendState.starting != null && (await this.backendState.starting),
        await this.backendState.server?.dispose(),
        (this.backendState.server = null),
        (this.backendState.apiImpl = null));
    }
    disposeBrowserUseRoute(e) {
      let t = HK(e);
      (this.backendState.apiImpl?.disposeRoute(t),
        this.hostsByRouteKey.delete(t),
        this.sessionRoutes.get(e.conversationId)?.windowId === e.windowId &&
          this.sessionRoutes.delete(e.conversationId),
        this.tabModesByRouteKey.delete(t),
        this.delegate?.disposeBrowserUseRoute(e));
      let n = this.windows.get(e.windowId) ?? null;
      if ((n?.conversations.delete(e.conversationId), n != null))
        for (let [t, r] of n.pages) r.conversationId === e.conversationId && n.pages.delete(t);
      LK().info(`IAB_LIFECYCLE disposed browser use route`, { safe: e, sensitive: {} });
    }
    getDelegate() {
      if (this.delegate == null) throw Error(`Browser session registry is not attached`);
      return this.delegate;
    }
    ensureWindowRecord(e) {
      let t = this.windows.get(e);
      if (t != null) return t;
      let n = { conversations: new Set(), ownerWebContentsId: -1, pages: new Map(), windowId: e };
      return (this.windows.set(e, n), n);
    }
    ensurePageRecord(e, t = {}) {
      let n = this.ensureWindowRecord(e.windowId);
      n.conversations.add(e.conversationId);
      let r = UK(e),
        i = n.pages.get(r);
      if (i != null) return (t.pageKey !== void 0 && (i.pageKey = t.pageKey), i);
      let a = {
        conversationId: e.conversationId,
        browserTabId: e.browserTabId,
        guestWebContentsId: null,
        isLoading: !1,
        pageKey: t.pageKey ?? null,
        url: ``,
        windowId: e.windowId,
      };
      return (n.pages.set(r, a), a);
    }
    hasPageRecordForConversation(e, t) {
      for (let n of e.pages.values()) if (n.conversationId === t) return !0;
      return !1;
    }
    getPageRecordForConversation(e, t) {
      return (
        this.windows
          .get(e)
          ?.pages.values()
          .find((e) => e.conversationId === t) ?? null
      );
    }
  },
  VK = class {
    conversationId;
    windowId;
    openingPagePromisesByBrowserTabId = new Map();
    routeKey;
    constructor(e, t, n) {
      ((this.registry = e),
        (this.conversationId = t.conversationId),
        (this.windowId = t.windowId),
        (this.routeKey = n));
    }
    reassignRoute(e, t) {
      ((this.conversationId = e.conversationId), (this.windowId = e.windowId), (this.routeKey = t));
    }
    getPageForBrowserUse(e) {
      return this.registry.getDelegate().getPageForBrowserUse(this.route, e);
    }
    getPagesForBrowserUse() {
      return this.registry.getDelegate().getPagesForBrowserUse(this.route);
    }
    getTabsForBrowserUse() {
      return this.registry.getDelegate().getTabsForBrowserUse(this.route);
    }
    getUserTabsForBrowserUse() {
      return this.registry.getDelegate().getUserTabsForBrowserUse(this.route);
    }
    async openPageForBrowserUse(e) {
      let t = e?.browserTabId ?? OF(this.conversationId);
      this.setBrowserUseActive(!0, t);
      let n = this.openingPagePromisesByBrowserTabId.get(t) ?? null;
      if (n != null) return n;
      let r = this.registry
        .getDelegate()
        .openPageForBrowserUse(this.route, {
          browserTabId: t,
          origin: e?.origin,
          startingUrl: e?.startingUrl,
        })
        .catch((e) => {
          throw (this.setBrowserUseActive(!1, t), this.closePageForBrowserUse(t), e);
        })
        .finally(() => {
          this.openingPagePromisesByBrowserTabId.delete(t);
        });
      this.openingPagePromisesByBrowserTabId.set(t, r);
      let i = await r;
      return (
        LK().info(`IAB_LIFECYCLE host opened page`, {
          safe: {
            conversationId: this.conversationId,
            pageKey: i.pageKey,
            routeKey: this.routeKey,
            webContentsId: i.webContents.id,
            windowId: this.windowId,
          },
          sensitive: {},
        }),
        i
      );
    }
    setBrowserVisibleForBrowserUse(e, t) {
      if (this.registry.isSubagentBrowserUseRoute(this.route))
        throw Error(`IAB visibility is not supported in a subagent thread`);
      (e && this.setBrowserUseActive(!0, t),
        this.registry.getDelegate().setBrowserVisibleForBrowserUse(this.route, e, t));
    }
    isBrowserVisibleForBrowserUse(e) {
      return this.registry.getDelegate().isBrowserVisibleForBrowserUse(this.route, e);
    }
    setViewportForBrowserUse(e, t) {
      (this.setBrowserUseActive(!0, t),
        this.registry.getDelegate().setViewportForBrowserUse(this.route, e, t));
    }
    setCaptureSurfaceForBrowserUse(e, t) {
      this.registry.getDelegate().setCaptureSurfaceForBrowserUse(this.route, e, t);
    }
    waitForPendingDebuggerSync(e) {
      return this.registry.getDelegate().waitForPendingDebuggerSync(this.route, e);
    }
    activateBrowserUseNavigationRestriction(e) {
      this.registry.getDelegate().activateBrowserUseNavigationRestriction(e);
    }
    assertBrowserUseNavigationAllowed(e, t) {
      this.registry.getDelegate().assertBrowserUseNavigationAllowed(e, t);
    }
    trackPendingBrowserUseNavigation(e, t) {
      this.registry.getDelegate().trackPendingBrowserUseNavigation(e, t);
    }
    clearPendingBrowserUseNavigation(e) {
      this.registry.getDelegate().clearPendingBrowserUseNavigation(e);
    }
    setBrowserUseActive(e, t) {
      this.registry.getDelegate().setBrowserUseActive(this.route, e, t);
    }
    setBrowserUseCursor(e, t) {
      return this.registry.getDelegate().setBrowserUseCursor(this.route, e, t);
    }
    closePageForBrowserUse(e) {
      this.registry.getDelegate().closePageForBrowserUse(this.route, e);
    }
    releasePageForBrowserUse(e) {
      this.registry.getDelegate().releasePageForBrowserUse(this.route, e);
    }
    get route() {
      return { conversationId: this.conversationId, windowId: this.windowId };
    }
  };
function HK({ conversationId: e, windowId: t }) {
  return `${t}:${e}`;
}
function UK({ browserTabId: e, conversationId: t }) {
  return yL({ browserTabId: e, conversationId: t });
}
function WK({
  browserSessionRegistry: e,
  comments: n,
  disposeThreadState: r,
  getBrowserUseRouteBrowserTabIds: i,
  isBrowserUseManagedBrowserTab: a,
  getThreadStateForWindow: o,
  setBrowserUseActiveForRoute: s,
  sourceBrowserTabId: c,
  sourceConversationId: l,
  syncRuntimeStateToPage: u,
  syncThreadState: d,
  targetBrowserTabId: f,
  targetConversationId: p,
  transferPendingWebviewRoutes: m,
  windowState: h,
}) {
  if (l === p && (f == null || c === f)) return;
  let g = o(h, l, c) ?? null;
  if (g == null) return;
  let _ = g.browserTabId,
    v = _ === OF(l),
    y = f ?? (v ? OF(p) : _),
    b = yL(g),
    x = yL({ browserTabId: y, conversationId: p }),
    S = t.fs(b, x);
  if (h.transferredConversationStateKeys.has(S)) {
    (e.recordDebugEvent({
      browserTabId: g.browserTabId,
      conversationId: p,
      details: [
        { label: `sourceConversationId`, value: l },
        { label: `transferKey`, value: S },
      ],
      guestWebContentsId: g.page?.view.webContents?.id ?? null,
      kind: `browser-route-transfer-replayed`,
      message: `Ignored replayed Browser route transfer`,
      ownerWebContentsId: h.owner.id,
      pageKey: null,
      windowId: h.window.id,
    }),
      h.ignoredConversationIds.add(b));
    return;
  }
  let C = h.threads.get(x) ?? null;
  if (C?.page != null) {
    (e.recordDebugEvent({
      browserTabId: y,
      conversationId: p,
      details: [{ label: `sourceConversationId`, value: l }],
      guestWebContentsId: C.page.view.webContents?.id ?? null,
      kind: `browser-route-transfer-target-exists`,
      message: `Ignored Browser route transfer because target page exists`,
      ownerWebContentsId: h.owner.id,
      pageKey: null,
      windowId: h.window.id,
    }),
      h.ignoredConversationIds.add(b),
      h.transferredConversationStateKeys.add(S));
    return;
  }
  C != null && r(h, p, C);
  let w = g.isBrowserUseActive,
    T = a(l, _),
    E = T && i(h, l).length <= 1,
    D = y === OF(p) || T,
    O = RK(h.window.id),
    k = E && D;
  (k &&
    e.reassignRoute({ conversationId: l, windowId: O }, { conversationId: p, windowId: O }, _, y),
    m(h, { browserTabId: _, conversationId: l }, { browserTabId: y, conversationId: p }),
    h.threads.set(x, g),
    h.threads.delete(b),
    h.activeConversationId === l &&
      h.activeBrowserTabId === g.browserTabId &&
      ((h.activeConversationId = p), (h.activeBrowserTabId = y)),
    (g.conversationId = p),
    (g.browserTabId = y),
    l !== p && (g.ownerRoutePath = null),
    g.page != null &&
      (!k &&
        T &&
        (e.recordPageClosed({ browserTabId: _, conversationId: l, windowId: O }),
        s({ conversationId: l, windowId: O }, !1, _)),
      (g.page.conversationId = p),
      (g.page.browserTabId = y),
      !k && D && e.recordPageCreated({ browserTabId: y, conversationId: p, windowId: O }),
      w && ((g.isBrowserUseActive = !1), s({ conversationId: p, windowId: O }, !0, y))),
    e.registerThread({ conversationId: p, windowId: RK(h.window.id) }),
    n.transferConversation(h.owner, l, _, p, y),
    h.ignoredConversationIds.add(b),
    h.transferredConversationStateKeys.add(S),
    e.recordDebugEvent({
      browserTabId: y,
      conversationId: p,
      details: [
        { label: `sourceBrowserTabId`, value: _ },
        { label: `sourceConversationId`, value: l },
      ],
      guestWebContentsId: g.page?.view.webContents?.id ?? null,
      kind: `browser-route-transferred`,
      message: `Transferred Browser route state`,
      ownerWebContentsId: h.owner.id,
      pageKey: g.page == null ? null : vL(h.owner.id, p, y),
      windowId: h.window.id,
    }),
    d(h, p, g),
    g.page != null && u(g.page, g));
}
function GK({
  browserSessionRegistry: e,
  browserUseOpenRequests: t,
  getThreadStateForWindow: n,
  windows: r,
}) {
  let i = Array.from(r),
    a = e.getDebugSnapshot();
  return {
    capturedAtMs: Date.now(),
    events: a.events,
    openRequests: t
      .getDebugSnapshot()
      .map((e) => qK({ getThreadStateForWindow: n, request: e, windowStates: i })),
    pendingAttaches: i.flatMap((e) => XK(e)),
    sessionRoutes: a.sessionRoutes,
    tabs: i.flatMap((e) => Array.from(e.threads.values()).map((t) => JK(e, t))),
    windows: i.map((e) => YK(e)),
  };
}
function KK({
  browserSessionRegistry: e,
  getLiveWindowState: t,
  getThreadStateForWindow: n,
  options: { browserTabId: r, conversationId: i, windowId: a },
  windowManager: o,
}) {
  let s = t(a);
  if (s == null) return { opened: !1, reason: `missing-window` };
  let c = n(s, i, r);
  return c == null
    ? { opened: !1, reason: `missing-tab` }
    : c.ownerRoutePath == null
      ? { opened: !1, reason: `missing-owner-route` }
      : (o.sendMessageToWebContents(s.owner, { type: `navigate-to-route`, path: c.ownerRoutePath }),
        s.window.isMinimized() && s.window.restore(),
        s.window.show(),
        s.window.focus(),
        e.recordDebugEvent({
          browserTabId: r,
          conversationId: i,
          details: [{ label: `ownerRoutePath`, value: c.ownerRoutePath }],
          guestWebContentsId: c.page?.view.webContents?.id ?? null,
          kind: `browser-owner-opened`,
          message: `Opened exact Browser owner route`,
          ownerWebContentsId: s.owner.id,
          pageKey: c.page == null ? null : vL(s.owner.id, i, r),
          windowId: s.window.id,
        }),
        { opened: !0 });
}
function qK({ getThreadStateForWindow: e, request: t, windowStates: n }) {
  let r = n.find((e) => e.owner.id === t.ownerWebContentsId) ?? null,
    i =
      r == null || t.conversationId == null || t.browserTabId == null
        ? null
        : e(r, t.conversationId, t.browserTabId);
  return {
    ...t,
    activeBrowserTabId: r?.activeBrowserTabId ?? null,
    activeConversationId: r?.activeConversationId ?? null,
    guestWebContentsId: i?.page?.view.webContents?.id ?? null,
    hasPage: i == null ? null : i.page != null,
    ownerRoutePath: i?.ownerRoutePath ?? null,
    pendingAttachCount: r == null || i == null ? null : ZK(r, i),
    pendingBrowserUsePanelOpen: i?.pendingBrowserUsePanelOpen ?? null,
    routeActive: r == null || i == null ? null : r.activeConversationId === i.conversationId,
    windowId: r?.window.id ?? null,
  };
}
function JK(e, t) {
  let n = t.page;
  return {
    active: e.activeConversationId === t.conversationId && e.activeBrowserTabId === t.browserTabId,
    bounds: t.bounds,
    browserTabId: t.browserTabId,
    canOpenOwner: t.ownerRoutePath != null,
    conversationId: t.conversationId,
    guestWebContentsId: n?.view.webContents?.id ?? null,
    hasPage: n != null,
    hasPendingBrowserUseVisibilityRequest: t.hasPendingBrowserUseVisibilityRequest,
    isAgentControllingBrowser: t.isAgentControllingBrowser,
    isBrowserUseActive: t.isBrowserUseActive,
    isBrowserUsePage: t.isBrowserUsePage,
    isLoading: n?.isLoading ?? t.snapshot.isLoading,
    isRestoring: n?.isRestoring ?? !1,
    isSuspended: n?.isSuspended ?? t.snapshot.isSuspended,
    lastLifecycleSequence: t.lastLifecycleSequence,
    lastPresentedSequence: t.lastPresentedSequence,
    mountGeneration: t.mountGeneration,
    ownerRoutePath: t.ownerRoutePath,
    ownerWebContentsId: e.owner.id,
    pageKey: n == null ? null : vL(e.owner.id, t.conversationId, t.browserTabId),
    pendingAttachCount: ZK(e, t),
    pendingBrowserUsePanelOpen: t.pendingBrowserUsePanelOpen,
    pendingTeardown: t.pendingTeardown,
    pendingUrl: n?.pendingUrl ?? ``,
    presented: t.presented,
    sessionState: t.sessionState,
    title: n?.title || t.snapshot.title,
    url: n == null ? t.snapshot.url : wL(n),
    visible: t.visible,
    windowId: e.window.id,
  };
}
function YK(e) {
  return {
    activeBrowserTabId: e.activeBrowserTabId,
    activeConversationId: e.activeConversationId,
    ignoredConversationCount: e.ignoredConversationIds.size,
    ownerWebContentsId: e.owner.id,
    pendingAttachCount: e.pendingWebviewAttachTokens.length,
    pendingAdoptedWebviewCount: e.pendingAdoptedWebviewRoutesByWebContentsId.size,
    pendingLegacyWebviewCount: e.pendingRetainedWebviewRoutesByInstanceId.size,
    transferredConversationStateCount: e.transferredConversationStateKeys.size,
    windowId: e.window.id,
  };
}
function XK(e) {
  return [
    ...Array.from(e.pendingRetainedWebviewRoutesByInstanceId.values()).map((t) => ({
      browserTabId: t.route.browserTabId,
      conversationId: t.route.conversationId,
      kind: `legacy`,
      mountGeneration: t.mountGeneration,
      webContentsId: null,
      windowId: e.window.id,
    })),
    ...Array.from(e.pendingAdoptedWebviewRoutesByWebContentsId).map(([t, n]) => ({
      browserTabId: n.route.browserTabId,
      conversationId: n.route.conversationId,
      kind: `adopted`,
      mountGeneration: n.mountGeneration,
      webContentsId: t,
      windowId: e.window.id,
    })),
    ...Array.from(e.pendingWebviewRoutesByAttachToken.values()).map((t) => ({
      browserTabId: t.route.browserTabId,
      conversationId: t.route.conversationId,
      kind: `token`,
      mountGeneration: t.mountGeneration,
      webContentsId: null,
      windowId: e.window.id,
    })),
  ];
}
function ZK(e, t) {
  return XK(e).filter(
    (e) => e.browserTabId === t.browserTabId && e.conversationId === t.conversationId,
  ).length;
}
function QK(e) {
  return e.tabType === t.Jo.WEB && e.commentModeDisabledReason == null;
}
function $K({
  canReload: e,
  isAnnotationModeEnabled: t = !0,
  snapshot: n,
  params: i,
  webContents: o,
  window: s,
  onBack: c,
  onAttachImage: l,
  onComment: u,
  onForward: d,
  onInspect: f,
  onOpenInBrowser: p,
  onOpenExternal: m,
  onReload: h,
}) {
  let g = r.V(),
    _ = [];
  (t &&
    n != null &&
    QK(n) &&
    _.push(
      {
        label: g.formatMessage({ messageId: gB, defaultMessage: `Annotate` }),
        click: () => {
          o.isDestroyed() || u({ x: i.x, y: i.y });
        },
      },
      { type: `separator` },
    ),
    pF(i) &&
      _.push(
        {
          label: g.formatMessage({ messageId: _B, defaultMessage: `Attach image to thread` }),
          click: () => {
            o.isDestroyed() || l(i.srcURL);
          },
        },
        { type: `separator` },
      ));
  let v = eq(i, o);
  v.length > 0 && _.push(...v, { type: `separator` });
  let y = rq(i),
    b = iq(i);
  ((y || b) &&
    (y && _.push({ role: `cut`, enabled: i.editFlags.canCut }),
    _.push({ role: `copy`, enabled: i.editFlags.canCopy }),
    y && _.push({ role: `paste`, enabled: i.editFlags.canPaste }),
    _.push({ type: `separator` })),
    aq(i) &&
      _.push(
        {
          label: g.formatMessage({ messageId: uB, defaultMessage: `Back` }),
          enabled: o.canGoBack(),
          click: () => {
            o.isDestroyed() || c();
          },
        },
        {
          label: g.formatMessage({ messageId: dB, defaultMessage: `Forward` }),
          enabled: o.canGoForward(),
          click: () => {
            o.isDestroyed() || d();
          },
        },
        {
          label: g.formatMessage({ messageId: fB, defaultMessage: `Reload` }),
          enabled: e,
          click: () => {
            o.isDestroyed() || h();
          },
        },
        { type: `separator` },
      ),
    i.linkURL.length > 0 &&
      (gb(i.linkURL) &&
        _.push({
          label: g.formatMessage({ messageId: pB, defaultMessage: `Open in browser` }),
          click: () => {
            p(i.linkURL);
          },
        }),
      yb(i.linkURL) &&
        _.push({
          label: g.formatMessage({ messageId: mB, defaultMessage: `Open in external browser` }),
          click: () => {
            m(i.linkURL);
          },
        }),
      _.push(
        {
          label: g.formatMessage({ messageId: hB, defaultMessage: `Copy link` }),
          click: () => {
            a.clipboard.writeText(i.linkURL);
          },
        },
        { type: `separator` },
      )),
    _.push({
      label: g.formatMessage({ messageId: lB, defaultMessage: `Inspect` }),
      click: () => {
        o.isDestroyed() || f({ x: i.x, y: i.y });
      },
    }),
    a.Menu.buildFromTemplate(_).popup({ window: s }));
}
function eq(e, t) {
  let n = [],
    r = e.passwordManager;
  return r == null
    ? n
    : (r.selectPassword != null &&
        n.push({
          label: r.selectPassword.label,
          click: () => {
            tq(t, `select-password`);
          },
        }),
      r.suggestPassword != null &&
        n.push({
          label: r.suggestPassword.label,
          click: () => {
            tq(t, `suggest-password`);
          },
        }),
      n);
}
function tq(e, t) {
  e.isDestroyed() || !nq(e) || e._executePasswordManagerContextMenuCommand(t);
}
function nq(e) {
  return (
    `_executePasswordManagerContextMenuCommand` in e &&
    typeof e._executePasswordManagerContextMenuCommand == `function`
  );
}
function rq(e) {
  return e.isEditable;
}
function iq(e) {
  return e.selectionText.length > 0;
}
function aq(e) {
  return (
    !e.isEditable &&
    e.formControlType === `none` &&
    e.mediaType === `none` &&
    e.linkURL.length === 0 &&
    e.srcURL.length === 0 &&
    e.selectionText.length === 0
  );
}
var oq = t.Gr(`browser-sidebar-manager`);
function sq({ errorReporter: e, pageState: t, webContents: n }) {
  let r = DL(t.serializedPageState),
    i = r?.currentIndex ?? null;
  if (r == null || i == null || i < 0 || i >= r.entries.length) return null;
  try {
    return n.navigationHistory
      .restore({ entries: r.entries, index: i })
      .then(() => !0)
      .catch(
        (n) => (
          oq().warning(`failed to restore browser sidebar navigation history`, {
            safe: { conversationId: t.conversationId },
            sensitive: { error: n },
          }),
          e.reportNonFatal(n instanceof Error ? n : Error(String(n)), {
            kind: `browser-sidebar-restore-navigation-history`,
          }),
          !1
        ),
      );
  } catch (n) {
    return (
      oq().warning(`failed to start browser sidebar navigation restore`, {
        safe: { conversationId: t.conversationId },
        sensitive: { error: n },
      }),
      e.reportNonFatal(n instanceof Error ? n : Error(String(n)), {
        kind: `browser-sidebar-restore-navigation-history`,
      }),
      null
    );
  }
}
function cq({ navigatePageToUrl: e, pageState: n, webContents: r }) {
  if (n.pendingUrl.length === 0) return !1;
  let i = xL(r.getURL());
  return (AF(i) && t.uo(n.pendingUrl).length === 0) ||
    (i.length > 0 && t.uo(i) === t.uo(n.pendingUrl))
    ? !1
    : (e(n.pendingUrl, n.pendingNavigationUsage?.source, n.pendingNavigationUsage?.initiator), !0);
}
function lq() {
  return {
    tabType: t.Jo.NEW_TAB_PAGE,
    isSuspended: !1,
    title: t.Xo,
    url: ``,
    faviconUrl: null,
    isAudible: !1,
    isCapturingUserMedia: !1,
    isLoading: !1,
    canGoBack: !1,
    canGoForward: !1,
    zoomPercent: 100,
    commentModeDisabledReason: null,
    interactionMode: `browse`,
    comments: [],
  };
}
var uq = t.Gr(`browser-sidebar-manager`),
  dq = `-owl-native-contents-close`,
  fq = `password-manager-open-settings`,
  pq = 50,
  mq = 100;
function hq(e, t, n) {
  return {
    browserTabId: t,
    conversationId: e,
    view: { webContents: null },
    isSuspended: !1,
    isRestoring: !1,
    pendingSuspensionId: null,
    fallbackTitle: n.title,
    pendingUrl: n.url,
    url: ``,
    faviconUrl: null,
    navigationVersion: 0,
    loadError: null,
    pendingNavigationUsage: null,
    title: n.title,
    isAudible: !1,
    isCapturingUserMedia: !1,
    isLoading: !1,
    canGoBack: !1,
    canGoForward: !1,
    serializedPageState: TL({ faviconUrl: null, title: n.title, url: n.url }),
    debuggerSync: Promise.resolve(),
    deviceMetricsSyncVersion: 0,
    dispose: () => void 0,
  };
}
function gq(e, t, n, r, i) {
  let a = hq(n, r.browserTabId, i);
  return (
    (r.page = a),
    e.updateThreadSessionState(r),
    e.isBrowserUseManagedBrowserTab(n, r.browserTabId) &&
      e.browserSessionRegistry.recordPageCreated({
        browserTabId: r.browserTabId,
        conversationId: n,
        windowId: RK(t.window.id),
      }),
    e.syncThreadState(t, n, r),
    e.syncRuntimeStateToPage(a, r),
    r.isAnnotationModeEnabled && e.comments.prepare(t.owner, n, r.browserTabId, r),
    a
  );
}
function _q(e, n, r, i, a) {
  if (r.view.webContents?.id === i.id) return;
  let o = i,
    s = r.isSuspended;
  (r.dispose(),
    (r.isRestoring = s),
    (r.isSuspended = !1),
    (r.pendingSuspensionId = null),
    (r.view.webContents = i),
    (r.isAudible = i.isCurrentlyAudible()),
    (r.isCapturingUserMedia =
      `isCapturingUserMedia` in i &&
      typeof i.isCapturingUserMedia == `function` &&
      i.isCapturingUserMedia()),
    e.onBrowserSidebarStateChanged(),
    e.isBrowserUseManagedBrowserTab(r.conversationId, r.browserTabId) &&
      e.browserSessionRegistry.recordGuestAttached({
        browserTabId: r.browserTabId,
        conversationId: r.conversationId,
        guestWebContentsId: i.id,
        pageKey: vL(n.owner.id, r.conversationId, r.browserTabId),
        windowId: RK(n.window.id),
      }));
  let c = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
  c != null &&
    (e.updateThreadSessionState(c),
    e.syncPageZoomPercent(r, c.snapshot.zoomPercent),
    e.queuePageDeviceMetricsSync(r, c.emulatedViewportSize),
    e.windowManager.sendMessageToWebContents(n.owner, {
      type: `browser-sidebar-webview-attached`,
      browserTabId: r.browserTabId,
      conversationId: r.conversationId,
      mountGeneration: c.mountGeneration,
    }));
  let l = () => {
      if (!r.isRestoring) return;
      r.isRestoring = !1;
      let t = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      t != null && (e.updateThreadSessionState(t), e.enforceDetachedPageBudget(n));
    },
    u = (a) => {
      let o = vF(a ?? i.getURL());
      !AF(o) &&
        !yV(r.loadError, o) &&
        (a != null || o.length > 0 || r.url.length === 0) &&
        (a != null || o !== r.url) &&
        (SL(r.url, o) && (r.faviconUrl = null),
        (r.url = o),
        (r.pendingUrl = o),
        a != null && (r.loadError = null));
      let s = AF(o) ? `` : i.getTitle(),
        c = i.isLoading();
      ((r.title = s || r.loadError?.title || (c && t.uo(o).length > 0 ? r.title : r.fallbackTitle)),
        (r.isLoading = c),
        (r.canGoBack = i.canGoBack()),
        (r.canGoForward = i.canGoForward()),
        (r.serializedPageState = TL({ faviconUrl: r.faviconUrl, title: r.title, url: wL(r) })),
        i.setBackgroundThrottling(!t.po(wL(r))));
      let l = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      if (l != null) {
        let t = l.snapshot.interactionMode;
        (e.syncThreadState(n, r.conversationId, l),
          l.page != null && l.snapshot.interactionMode !== t && e.syncRuntimeStateToPage(r, l));
      }
    },
    d = () => {
      u();
    },
    f = (t, i) => {
      ((r.faviconUrl = i.find((e) => e.length > 0) ?? null),
        (r.serializedPageState = TL({ faviconUrl: r.faviconUrl, title: r.title, url: wL(r) })));
      let a = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      a != null && e.syncThreadState(n, r.conversationId, a);
    },
    p = () => {
      u();
    },
    m = () => {
      (l(), u());
      let n = r.pendingNavigationUsage;
      n?.source === `browser_use` &&
        t.uo(i.getURL()) === n.expectedUrl &&
        (e.notifyBrowserSidebarNavigationUsage(
          r.conversationId,
          r.browserTabId,
          `browser_use`,
          n.initiator,
          n.urlKind,
        ),
        (r.pendingNavigationUsage = null));
    },
    h = () => {
      let t = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      t != null && e.syncThreadState(n, r.conversationId, t);
    },
    g = (e) => {
      r.isAudible !== e.audible && ((r.isAudible = e.audible), h());
    },
    _ = (e) => {
      r.isCapturingUserMedia !== e.capturing && ((r.isCapturingUserMedia = e.capturing), h());
    },
    v = (t) => {
      let i = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      if (i != null) {
        if (i.pendingTeardown != null) {
          e.finalizeThreadTeardown(n, i.conversationId, i, i.pendingTeardown.teardownId);
          return;
        }
        IV(e.windowManager, n.owner, r.conversationId, i);
      }
      (e.dismissCommentEditorForCommittedNavigation(n, r, t), u(t));
    },
    y = (e, t) => {
      v(t);
    },
    b = (e, t, n) => {
      n && v(t);
    },
    x = (e) => {
      e.isMainFrame &&
        (r.pendingNavigationUsage?.source === `browser_use` && (r.pendingNavigationUsage = null),
        v(e.url));
    },
    S = (a, o, s, c, u) => {
      let d = vF(c);
      if (
        (u && c === `about:blank`) ||
        AF(d) ||
        yV(r.loadError, d) ||
        gV({ errorCode: o, errorDescription: s, isMainFrame: u })
      )
        return;
      let f = d || r.pendingUrl || r.url || vF(i.getURL());
      if (f.length === 0) return;
      let p = mV({
        errorDescription: s,
        failedUrl: f,
        formatMessage: bL,
        runtimeIntlConfig:
          e.getThreadStateForWindow(n, r.conversationId, r.browserTabId)?.runtimeIntlConfig ?? t.as,
      });
      (e.loadBrowserSidebarErrorPage(n, r, i, {
        failedUrl: f,
        kind: `load-error`,
        loadError: pV({ content: p, errorCode: o, errorDescription: s, failedUrl: f }),
      }),
        l());
    },
    C = (n) => {
      if (
        (n.isMainFrame && ML(r),
        e.openBrowserSidebarExternalUrl({
          initiatorUrl: n.initiator?.url ?? null,
          pageState: r,
          url: n.url,
          webContents: i,
        }))
      ) {
        n.preventDefault();
        return;
      }
      if (e.isRestrictedBrowserSidebarNavigation(i.id, n.url)) {
        n.preventDefault();
        return;
      }
      n.isMainFrame && i.setBackgroundThrottling(!t.po(n.url));
    },
    w = (a, o) => {
      ML(r);
      let s = a.url ?? o;
      if (
        e.openBrowserSidebarExternalUrl({
          initiatorUrl: a.initiator?.url ?? null,
          pageState: r,
          url: s,
          webContents: i,
        })
      ) {
        a.preventDefault();
        return;
      }
      if (e.isRestrictedBrowserSidebarNavigation(i.id, s)) {
        a.preventDefault();
        return;
      }
      i.setBackgroundThrottling(!t.po(s));
      let c = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      if (c != null) {
        if ((IV(e.windowManager, n.owner, r.conversationId, c), c.pendingTeardown != null)) {
          e.finalizeThreadTeardown(n, r.conversationId, c, c.pendingTeardown.teardownId);
          return;
        }
        (e.updateThreadSessionState(c), e.syncThreadState(n, r.conversationId, c));
      }
    },
    T = (a, o) => {
      a.isMainFrame && ML(r);
      let s = a.url ?? o;
      if (e.isRestrictedBrowserSidebarNavigation(i.id, s)) {
        a.preventDefault();
        return;
      }
      if (a.isMainFrame) {
        i.setBackgroundThrottling(!t.po(s));
        let a = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
        a != null && IV(e.windowManager, n.owner, r.conversationId, a);
      }
    },
    E = () => {
      r.view.webContents?.id === i.id &&
        ((r.pendingSuspensionId = null),
        (r.isAudible = !1),
        (r.isCapturingUserMedia = !1),
        (r.view.webContents = null),
        (r.dispose = () => void 0),
        e.onBrowserSidebarStateChanged(),
        e.browserSessionRegistry.recordPageTornDown({
          browserTabId: r.browserTabId,
          conversationId: r.conversationId,
          guestWebContentsId: i.id,
          windowId: RK(n.window.id),
        }));
      let t = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      t != null &&
        (e.syncThreadState(n, r.conversationId, t),
        IV(e.windowManager, n.owner, r.conversationId, t));
    },
    D = () => {
      e.windowManager.sendMessageToWebContents(n.owner, {
        type: `close-browser-tab`,
        browserTabId: r.browserTabId,
        conversationId: r.conversationId,
      });
    },
    O = (a) => {
      let o = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      e.loadBrowserSidebarErrorPage(n, r, i, {
        failedUrl: a,
        kind: `crash`,
        loadError: hV({
          failedUrl: a,
          formatMessage: bL,
          runtimeIntlConfig: o?.runtimeIntlConfig ?? t.as,
        }),
      });
    },
    k = (e, t) => {
      if (t.reason === `clean-exit`) return;
      uq().warning(`browser sidebar render process gone`, {
        safe: { conversationId: r.conversationId, exitCode: t.exitCode, reason: t.reason },
        sensitive: { url: i.getURL() },
      });
      let n = r.pendingUrl || r.url || vF(i.getURL());
      n.length !== 0 && O(n);
    },
    A = () => {
      uq().warning(`IAB browser page unresponsive`, {
        safe: { conversationId: r.conversationId },
        sensitive: { url: i.getURL() },
      });
      let e = r.pendingUrl || r.url || vF(i.getURL());
      e.length !== 0 && O(e);
    },
    j = () => {
      let t = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      t &&
        (uq().info(`browser sidebar dom-ready`, {
          safe: { conversationId: r.conversationId, interactionMode: t.snapshot.interactionMode },
          sensitive: { url: i.getURL() },
        }),
        e.syncRuntimeStateToPage(r, t),
        e.comments.restoreRuntimeEditor(
          n.owner,
          r.conversationId,
          r.browserTabId,
          r.view.webContents,
        ));
    },
    M = (t, n, i) => {
      (uq().error(`browser sidebar preload failed`, {
        safe: { conversationId: r.conversationId, preloadPath: n },
        sensitive: { error: i },
      }),
        e.errorReporter.reportNonFatal(i, { kind: `browser-sidebar-preload-error` }));
    },
    N = (e, t, n, i, a) => {
      uq().info(`browser sidebar console message`, {
        safe: { conversationId: r.conversationId, level: t, line: i },
        sensitive: { message: n, sourceId: a },
      });
    },
    P = (t, i) => {
      let a = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId);
      a != null && PV(e.windowManager, n.owner, r.conversationId, a, i);
    },
    F = () => e.getThreadStateForWindow(n, r.conversationId, r.browserTabId),
    I = () => {
      let t = F();
      return iF(
        e.windowManager.getAppShellShortcutState(n.owner.id),
        r.conversationId,
        r.browserTabId,
        oF(t?.hostKind ?? null),
      );
    },
    ee = BV({
      closeBrowserTab: () => {
        let t = I();
        t != null && e.windowManager.sendMessageToWebContents(n.owner, t);
      },
      getCanCloseBrowserTab: () => I() != null,
      getInteractionMode: () => F()?.snapshot.interactionMode ?? `browse`,
      getOwnerCommandAccelerators: (t) => e.getOwnerCommandAccelerators(t),
      getIsTweaksModeActive: () => {
        let e = F();
        return e != null && e.isAnnotationModeEnabled && yF(e.snapshot);
      },
      isCommentModeAvailable: () => {
        let e = F();
        return e != null && e.isAnnotationModeEnabled && QK(e.snapshot);
      },
      owner: n.owner,
      runCommandInOwner: (t, i, a) => {
        let o = F(),
          s = aF(
            e.windowManager.getAppShellShortcutState(n.owner.id),
            r.conversationId,
            r.browserTabId,
            oF(o?.hostKind ?? null),
          );
        e.windowManager.sendMessageToWebContents(n.owner, {
          type: `run-command`,
          id: t,
          ...(a?.allowAppShellTabShortcut === !0 ? { allowAppShellTabShortcut: !0 } : {}),
          keyboardEvent: i,
          ...(s == null ? {} : { originPanel: s }),
        });
      },
      setInteractionMode: (t) => {
        e.setInteractionMode(n.owner, r.conversationId, t, r.browserTabId);
      },
      sendCommandToOwner: (t) => {
        e.windowManager.sendMessageToWebContents(n.owner, {
          type: `browser-sidebar-command`,
          conversationId: r.conversationId,
          browserTabId: r.browserTabId,
          command: t,
        });
      },
      showTweaksExitBlockedToast: () => {
        F() != null && e.showTweaksExitBlockedToast(n.owner, r.conversationId, r.browserTabId);
      },
    }),
    te = (t, i) => {
      (e.setDesignModifierPressed(n.owner, r.conversationId, i.alt === !0), ee(t, i));
    },
    ne = (t, a) => {
      if (i.isDestroyed() || n.window.isDestroyed()) return;
      let o = r.loadError != null || r.url.length > 0 || r.pendingUrl.length > 0,
        s = F();
      $K({
        canReload: o,
        isAnnotationModeEnabled: s?.isAnnotationModeEnabled ?? !0,
        snapshot: s?.snapshot ?? null,
        params: a,
        webContents: i,
        window: n.window,
        onBack: () => {
          e.goBack(n.owner, r.conversationId, r.browserTabId);
        },
        onAttachImage: (t) => {
          fF({
            browserTabId: r.browserTabId,
            browserPageUrl: r.url,
            conversationId: r.conversationId,
            owner: n.owner,
            sourceUrl: t,
            webContents: i,
            windowManager: e.windowManager,
          });
        },
        onComment: (t) => {
          e.startCommentAtPoint(n.owner, r, t);
        },
        onForward: () => {
          e.goForward(n.owner, r.conversationId, r.browserTabId);
        },
        onInspect: (e) => {
          i.inspectElement(e.x, e.y);
        },
        onOpenInBrowser: (t) => {
          e.windowManager.sendMessageToWebContents(n.owner, {
            type: `open-browser-tab`,
            conversationId: r.conversationId,
            initialUrl: t,
            insertToRightOfTabId: r.browserTabId,
            source: `manual`,
            initiator: `side_panel_menu`,
          });
        },
        onOpenExternal: (t) => {
          e.openBrowserSidebarUrlExternally(r.conversationId, t);
        },
        onReload: () => {
          e.reload(n.owner, r.conversationId, void 0, r.browserTabId);
        },
      });
    },
    re = () => {
      n.owner.isDestroyed() ||
        e.windowManager.sendMessageToWebContents(n.owner, {
          type: `navigate-to-route`,
          path: t.to,
        });
    };
  (i.on(`page-title-updated`, d),
    i.on(`page-favicon-updated`, f),
    i.on(`audio-state-changed`, g),
    i.on(`user-media-capture-state-changed`, _),
    i.on(`did-start-loading`, p),
    i.on(`did-stop-loading`, m),
    i.on(`did-navigate`, y),
    i.on(`did-navigate-in-page`, b),
    i.on(`did-redirect-navigation`, x),
    i.on(`did-fail-load`, S),
    i.on(`will-frame-navigate`, C),
    i.on(`will-navigate`, w),
    i.on(`will-redirect`, T),
    i.on(`destroyed`, E),
    o.on(dq, D),
    i.on(`render-process-gone`, k),
    i.on(`unresponsive`, A),
    i.on(`dom-ready`, j),
    i.on(`preload-error`, M),
    i.on(`console-message`, N),
    i.on(`found-in-page`, P),
    i.on(`before-input-event`, te),
    i.on(`context-menu`, ne),
    o.on(fq, re),
    i.setWindowOpenHandler(({ disposition: t, referrer: a, url: o }) =>
      e.openBrowserSidebarExternalUrl({
        initiatorUrl: a?.url ?? null,
        pageState: r,
        url: o,
        webContents: i,
      }) || e.isRestrictedBrowserSidebarNavigation(i.id, o)
        ? { action: `deny` }
        : Sq(i) && (t === `foreground-tab` || t === `background-tab`)
          ? {
              action: `allow`,
              createWindow: (i) => {
                let a = Cq(i, n.owner);
                return (
                  e.windowManager.sendMessageToWebContents(
                    n.owner,
                    xq({
                      ...(t === `background-tab` ? { active: !1 } : {}),
                      adoptionLease: a.adoptionLease,
                      adoptedWebContentsId: a.webContents.id,
                      conversationId: r.conversationId,
                      initialUrl: o,
                      insertToRightOfTabId: r.browserTabId,
                    }),
                  ),
                  a.webContents
                );
              },
            }
          : t === `background-tab`
            ? (e.windowManager.sendMessageToWebContents(
                n.owner,
                xq({
                  active: !1,
                  conversationId: r.conversationId,
                  initialUrl: o,
                  insertToRightOfTabId: r.browserTabId,
                }),
              ),
              { action: `deny` })
            : (e.windowManager.sendMessageToWebContents(n.owner, {
                ...xq({
                  conversationId: r.conversationId,
                  initialUrl: o,
                  insertToRightOfTabId: r.browserTabId,
                }),
              }),
              { action: `deny` }),
    ),
    (r.dispose = () => {
      (i.removeListener(`page-title-updated`, d),
        i.removeListener(`page-favicon-updated`, f),
        i.removeListener(`audio-state-changed`, g),
        i.removeListener(`user-media-capture-state-changed`, _),
        i.removeListener(`did-start-loading`, p),
        i.removeListener(`did-stop-loading`, m),
        i.removeListener(`did-navigate`, y),
        i.removeListener(`did-navigate-in-page`, b),
        i.removeListener(`did-redirect-navigation`, x),
        i.removeListener(`did-fail-load`, S),
        i.removeListener(`will-frame-navigate`, C),
        i.removeListener(`will-navigate`, w),
        i.removeListener(`will-redirect`, T),
        i.removeListener(`destroyed`, E),
        o.removeListener(dq, D),
        i.removeListener(`render-process-gone`, k),
        i.removeListener(`unresponsive`, A),
        i.removeListener(`dom-ready`, j),
        i.removeListener(`preload-error`, M),
        i.removeListener(`console-message`, N),
        i.removeListener(`found-in-page`, P),
        i.removeListener(`before-input-event`, te),
        i.removeListener(`context-menu`, ne),
        o.removeListener(fq, re),
        r.view.webContents?.id === i.id &&
          ((r.isRestoring = !1),
          (r.isAudible = !1),
          (r.isCapturingUserMedia = !1),
          (r.view.webContents = null),
          e.onBrowserSidebarStateChanged()));
    }));
  let ie = sq({ errorReporter: e.errorReporter, pageState: r, webContents: i });
  (ie == null
    ? (u(),
      cq({
        navigatePageToUrl: (t, n, i) => {
          e.navigatePageToUrl(r, t, n, i);
        },
        pageState: r,
        webContents: i,
      }) || l())
    : ie.then(() => {
        r.view.webContents?.id === i.id &&
          !i.isDestroyed() &&
          (u(),
          cq({
            navigatePageToUrl: (t, n, i) => {
              e.navigatePageToUrl(r, t, n, i);
            },
            pageState: r,
            webContents: i,
          }) || l());
      }),
    e.finishPageAttach(n, r, i, a),
    e.enforceDetachedPageBudget(n));
}
function vq(e, n, r, i, a, o) {
  let s = r.length === 0 ? `about:blank` : r,
    c = t.uo(s),
    l = n.view.webContents;
  if (
    ((n.pendingNavigationUsage =
      i == null || a == null ? null : { expectedUrl: c, initiator: a, source: i, urlKind: CL(s) }),
    l == null)
  ) {
    ((n.loadError = null),
      (n.pendingUrl = r),
      SL(n.url, s) && (n.faviconUrl = null),
      (n.url = s),
      (n.serializedPageState = TL({
        faviconUrl: n.faviconUrl,
        title: n.title || n.fallbackTitle,
        url: wL(n),
      })));
    return;
  }
  if (e.isRestrictedBrowserSidebarNavigation(l.id, s)) {
    n.pendingNavigationUsage = null;
    return;
  }
  (i != null && e.emptyPageLocalServers.recordOpened(o ?? s),
    (n.loadError = null),
    (n.pendingUrl = r),
    SL(n.url, s) && (n.faviconUrl = null),
    (n.serializedPageState = TL({
      faviconUrl: n.faviconUrl,
      title: n.title || n.fallbackTitle,
      url: wL(n),
    })),
    ML(n),
    l.setBackgroundThrottling(
      !s.startsWith(`http://localhost`) &&
        !s.startsWith(`http://127.0.0.1`) &&
        !s.startsWith(`http://[::1]`) &&
        !s.startsWith(`https://localhost`) &&
        !s.startsWith(`https://127.0.0.1`) &&
        !s.startsWith(`https://[::1]`),
    ));
  let u = n.navigationVersion,
    d = (i = 0) => {
      if (
        l.isDestroyed() ||
        n.view.webContents?.id !== l.id ||
        n.navigationVersion !== u ||
        (n.pendingUrl.length > 0 && t.uo(n.pendingUrl) !== c)
      )
        return;
      let a = (a) => {
        if (!(n.pendingUrl.length > 0 && t.uo(n.pendingUrl) !== c) && !_V(a)) {
          if (vV(a) && i < mq) {
            setTimeout(() => {
              d(i + 1);
            }, pq);
            return;
          }
          (n.pendingNavigationUsage?.expectedUrl === c && (n.pendingNavigationUsage = null),
            !(n.loadError != null && t.uo(n.loadError.failedUrl) === c) &&
              (uq().warning(`failed to load browser sidebar url`, {
                safe: { conversationId: n.conversationId },
                sensitive: { error: a, url: r },
              }),
              e.errorReporter.reportNonFatal(a instanceof Error ? a : Error(String(a)), {
                kind: `browser-sidebar-load-url`,
              })));
        }
      };
      try {
        l.loadURL(s)
          .then(() => {
            let r = n.pendingNavigationUsage,
              i = t.uo(l.getURL());
            (r?.expectedUrl === c &&
              (r.source !== `browser_use` || i === c) &&
              e.notifyBrowserSidebarNavigationUsage(
                n.conversationId,
                n.browserTabId,
                r.source,
                r.initiator,
                r.urlKind,
              ),
              r?.expectedUrl === c && (n.pendingNavigationUsage = null));
          })
          .catch(a);
      } catch (e) {
        a(e);
      }
    };
  d();
}
function yq(e, n, r, i) {
  let a = n.owner;
  if (a.isDestroyed() || i.pendingTeardown?.reason === `closed`) return;
  let o = i.page,
    s = o == null ? `` : wL(o),
    c = o == null ? `` : o.title || o.fallbackTitle,
    l = t.uo(s),
    u = t.uo(i.snapshot.url),
    d = o != null && o.isRestoring && u.length > 0 && (l.length === 0 || AF(s) || AF(c)),
    f =
      o != null &&
      o.isRestoring &&
      o.isLoading &&
      o.loadError == null &&
      l.length > 0 &&
      u === l &&
      i.snapshot.tabType === t.Jo.WEB &&
      i.snapshot.title.length > 0 &&
      i.snapshot.title !== o.fallbackTitle,
    p = o != null && !i.isBrowserUsePage && !d && l.length === 0,
    m =
      o == null
        ? i.snapshot
        : p
          ? lq()
          : {
              ...i.snapshot,
              tabType: t.Jo.WEB,
              isSuspended: o.isSuspended,
              title: d || f ? i.snapshot.title : o.title || o.fallbackTitle,
              url: d ? i.snapshot.url : s,
              faviconUrl: d ? i.snapshot.faviconUrl : o.faviconUrl,
              isAudible: o.isAudible,
              isCapturingUserMedia: o.isCapturingUserMedia,
              isLoading: o.isLoading,
              canGoBack: o.canGoBack,
              canGoForward: o.canGoForward,
            };
  ((i.snapshot = m),
    o != null &&
      e.isBrowserUseManagedBrowserTab(r, i.browserTabId) &&
      e.browserSessionRegistry.recordPageSnapshot({
        browserTabId: i.browserTabId,
        conversationId: r,
        guestWebContentsId: o.view.webContents?.id ?? null,
        isLoading: o.isLoading,
        pageKey: vL(a.id, r, i.browserTabId),
        url: m.url,
        windowId: RK(n.window.id),
      }),
    FV(e.windowManager, n.owner, r, i),
    i.visible && m.tabType !== t.Jo.WEB && e.emptyPageLocalServers.sync(n, r, i),
    e.windowManager.sendMessageToWebContents(a, {
      type: `browser-sidebar-state`,
      conversationId: r,
      browserTabId: i.browserTabId,
      snapshot: m,
    }));
}
function bq(e, n, r, i, { failedUrl: a, kind: o, loadError: s }) {
  let c = e.getThreadStateForWindow(n, r.conversationId, r.browserTabId) ?? null;
  (c != null && IV(e.windowManager, n.owner, r.conversationId, c),
    (r.loadError = s),
    r.pendingNavigationUsage?.expectedUrl === t.uo(a) && (r.pendingNavigationUsage = null),
    (r.pendingUrl = a),
    (r.faviconUrl = null),
    (r.isAudible = !1),
    (r.isCapturingUserMedia = !1),
    (r.title = s.title),
    (r.isLoading = !1),
    (r.canGoBack = i.canGoBack()),
    (r.canGoForward = i.canGoForward()),
    (r.serializedPageState = TL({ faviconUrl: r.faviconUrl, title: r.title, url: wL(r) })),
    c != null && e.syncThreadState(n, r.conversationId, c));
  let l = r.navigationVersion,
    u = (t = 0) => {
      if (
        i.isDestroyed() ||
        r.view.webContents?.id !== i.id ||
        r.loadError !== s ||
        r.navigationVersion !== l
      )
        return;
      if (i.isLoading() && t < mq) {
        setTimeout(() => {
          u(t + 1);
        }, pq);
        return;
      }
      let n = (n) => {
        if (!(r.loadError !== s || _V(n))) {
          if (vV(n) && t < mq) {
            setTimeout(() => {
              u(t + 1);
            }, pq);
            return;
          }
          (uq().warning(`failed to load browser sidebar error page`, {
            safe: { conversationId: r.conversationId, kind: o },
            sensitive: { error: n, url: a },
          }),
            e.errorReporter.reportNonFatal(n instanceof Error ? n : Error(String(n)), {
              kind: `browser-sidebar-load-error-page`,
            }));
        }
      };
      try {
        i.loadURL(s.pageUrl).catch(n);
      } catch (e) {
        n(e);
      }
    };
  u();
}
function xq({
  active: e = !0,
  adoptionLease: t,
  adoptedWebContentsId: n,
  conversationId: r,
  initialUrl: i,
  insertToRightOfTabId: a,
}) {
  return {
    type: `open-browser-tab`,
    conversationId: r,
    initialUrl: i,
    ...(a == null ? {} : { insertToRightOfTabId: a }),
    ...(t == null ? {} : { adoptionLease: t }),
    ...(n == null ? {} : { adoptedWebContentsId: n }),
    ...(e ? {} : { active: e }),
    source: `manual`,
    initiator: `window_open`,
  };
}
function Sq(e) {
  return (
    typeof e == `object` &&
    !!e &&
    `_createWebViewAdoptionLease` in e &&
    typeof e._createWebViewAdoptionLease == `function`
  );
}
function Cq(e, t) {
  let n = `webContents` in e && Sq(e.webContents) ? e.webContents : null;
  if (n == null) throw Error(`Owl browser sidebar window.open child WebContents missing`);
  let r = n._createWebViewAdoptionLease(t);
  if (typeof r != `string` || r.length === 0)
    throw Error(`Owl browser sidebar webview adoption lease is empty`);
  return { adoptionLease: r, webContents: n };
}
var wq = 12,
  Tq = t.Gr(`browser-sidebar-manager`),
  Eq = class {
    tabActivitySequence = 0;
    protectedOverflowWindowIds = new Set();
    constructor(e) {
      this.options = e;
    }
    markTabActivity(e) {
      ((this.tabActivitySequence += 1), (e.lastTabActivitySequence = this.tabActivitySequence));
    }
    isThreadProtectedFromSuspension(e) {
      return e.isBrowserUseActive;
    }
    enforceDetachedPageBudgets() {
      for (let e of this.options.windows.values()) this.enforceDetachedPageBudget(e);
    }
    enforceDetachedPageBudget(e) {
      let t = Dq(e);
      if (t.length <= wq) {
        this.protectedOverflowWindowIds.delete(e.window.id);
        return;
      }
      let n = t.filter((e) => this.isThreadEligibleForSuspension(e));
      if (n.length === 0) {
        t.every((e) => this.isThreadProtectedFromSuspension(e)) &&
          !this.protectedOverflowWindowIds.has(e.window.id) &&
          (this.protectedOverflowWindowIds.add(e.window.id), this.logProtectedTabBudgetOverflow(e));
        return;
      }
      for (let t of e.threads.values())
        if (t.page?.isRestoring === !0 || t.pendingTeardown?.reason === `suspended`) return;
      let r = n.sort((e, t) => {
        let n = e.lastTabActivitySequence - t.lastTabActivitySequence;
        return n === 0 ? e.browserTabId.localeCompare(t.browserTabId) : n;
      })[0];
      this.options.suspendDetachedThread(e, r);
    }
    handleWindowClosed(e) {
      this.protectedOverflowWindowIds.delete(e);
    }
    getTabBudgetWindowStats(e) {
      let t = Dq(e),
        n = 0;
      for (let e of t) e.isBrowserUseActive && (n += 1);
      return {
        browserUseProtectedBudgetCandidateCount: n,
        eligibleBudgetCandidateCount: t.filter((e) => this.isThreadEligibleForSuspension(e)).length,
        liveDetachedRendererCount: t.length,
        protectedBudgetCandidateCount: n,
      };
    }
    logProtectedTabBudgetOverflow(e) {
      let t = this.getTabBudgetWindowStats(e);
      (this.options.browserSessionRegistry.recordDebugEvent({
        browserTabId: null,
        conversationId: null,
        details: [
          { label: `budget`, value: String(wq) },
          { label: `liveDetachedRendererCount`, value: String(t.liveDetachedRendererCount) },
          {
            label: `protectedBudgetCandidateCount`,
            value: String(t.protectedBudgetCandidateCount),
          },
        ],
        guestWebContentsId: null,
        kind: `browser-tab-budget-protected-overflow`,
        message: `All over-budget Browser tabs are protected`,
        ownerWebContentsId: e.owner.id,
        pageKey: null,
        windowId: e.window.id,
      }),
        Tq().info(`IAB_TAB_BUDGET protected-overflow`, {
          safe: {
            budget: wq,
            browserUseProtectedBudgetCandidateCount: t.browserUseProtectedBudgetCandidateCount,
            eligibleBudgetCandidateCount: t.eligibleBudgetCandidateCount,
            liveDetachedRendererCount: t.liveDetachedRendererCount,
            liveDetachedRendererExcess: Math.max(t.liveDetachedRendererCount - wq, 0),
            protectedBudgetCandidateCount: t.protectedBudgetCandidateCount,
          },
          sensitive: { windowId: e.window.id },
        }));
    }
    isThreadEligibleForSuspension(e) {
      return (
        e.pendingTeardown == null &&
        !e.page?.isRestoring &&
        !this.isThreadProtectedFromSuspension(e)
      );
    }
  };
function Dq(e) {
  return Array.from(e.threads.values()).filter(
    (e) =>
      e.sessionState === `live-detached` &&
      e.page?.view.webContents != null &&
      !e.page.view.webContents.isDestroyed(),
  );
}
var Oq = t.Gr(`browser-sidebar-manager`),
  kq = (0, s.join)((0, o.tmpdir)(), `codex-browser-tab-lifecycle.jsonl`);
function Aq({ action: e, isBrowserUseTab: t, threadState: n, windows: r }) {
  let i = t ?? n.isBrowserUsePage,
    a = 0,
    o = 0,
    s = 0,
    c = 0;
  for (let e of r.values())
    for (let t of e.threads.values())
      Iq(t) &&
        ((a += 1),
        t.conversationId === n.conversationId && (s += 1),
        t.isBrowserUsePage && ((o += 1), t.conversationId === n.conversationId && (c += 1)));
  return {
    type: `browser-sidebar-tab-lifecycle`,
    action: e,
    conversationId: n.conversationId,
    threadBrowserTabCount: s,
    appBrowserTabCount: a,
    threadBrowserUseTabCount: c,
    appBrowserUseTabCount: o,
    isBrowserUseTab: i,
  };
}
function jq({ windowManager: e, windowState: t, ...n }) {
  if (!Iq(n.threadState)) return;
  let r = Aq(n);
  (Lq(r, n, t),
    Oq().info(`IAB_TAB_ANALYTICS browser tab lifecycle`, {
      safe: {
        action: r.action,
        appBrowserTabCount: r.appBrowserTabCount,
        appBrowserUseTabCount: r.appBrowserUseTabCount,
        browserTabId: n.threadState.browserTabId,
        conversationId: r.conversationId,
        isBrowserUseTab: r.isBrowserUseTab,
        ownerWebContentsId: t.owner.id,
        threadBrowserTabCount: r.threadBrowserTabCount,
        threadBrowserUseTabCount: r.threadBrowserUseTabCount,
      },
      sensitive: {},
    }),
    e.sendMessageToWebContents(t.owner, r));
}
function Mq(e, t, n, r) {
  jq({ action: `opened`, threadState: r, windowManager: e, windows: t, windowState: n });
}
function Nq(e, t, n, r, i) {
  (e.registerThread({ conversationId: i.conversationId, windowId: RK(r.window.id) }),
    Mq(t, n, r, i));
}
function Pq(e, t, n, r) {
  jq({
    action: `closed`,
    isBrowserUseTab: r.isBrowserUsePage,
    threadState: r,
    windowManager: e,
    windows: t,
    windowState: n,
  });
}
function Fq(e, t, n, r) {
  let i = yL(r);
  (n.threads.delete(i),
    n.ignoredConversationIds.delete(i),
    n.activeConversationId === r.conversationId &&
      n.activeBrowserTabId === r.browserTabId &&
      ((n.activeConversationId = null), (n.activeBrowserTabId = null)),
    Pq(e, t, n, r));
}
function Iq(e) {
  return e.isBrowserUsePage || e.browserTabId !== OF(e.conversationId);
}
function Lq(e, t, n) {
  try {
    (0, l.appendFileSync)(
      kq,
      `${JSON.stringify({ timestamp: new Date().toISOString(), action: e.action, appBrowserTabCount: e.appBrowserTabCount, appBrowserUseTabCount: e.appBrowserUseTabCount, browserTabId: t.threadState.browserTabId, conversationId: e.conversationId, isBrowserUseTab: e.isBrowserUseTab, ownerWebContentsId: n.owner.id, threadBrowserTabCount: e.threadBrowserTabCount, threadBrowserUseTabCount: e.threadBrowserUseTabCount })}\n`,
      `utf8`,
    );
  } catch (e) {
    Oq().warning(`IAB_TAB_ANALYTICS failed to write debug file`, {
      safe: { error: e instanceof Error ? e.message : String(e) },
      sensitive: {},
    });
  }
}
var Rq = new WeakSet();
function zq(e) {
  Rq.add(e);
}
function Bq(e) {
  return Rq.has(e);
}
var Vq = t.Gr(`browser-sidebar-manager`),
  Hq = `data-browser-sidebar-retained-webview`,
  Uq = `owl-webcontents-adoption-lease`,
  Wq = `owl-webcontents-adopted-web-contents-id`,
  Gq = `data-browser-sidebar-adopted-initial-url`,
  Kq = new WeakMap(),
  qq = [
    { allowedNavigationUrls: t.Ya, isEnabled: () => r.C(t.Ei), partition: t.Za, src: t.Xa },
    { allowedNavigationUrls: [t.Qa], isEnabled: () => r.C(t.Di), partition: t.$a, src: t.Qa },
    { allowedNavigationUrls: [t.eo], isEnabled: () => r.C(t.Ei), partition: t.ro, src: t.no },
    { allowedNavigationUrls: t.io, isEnabled: () => r.C(t.ki), partition: t.oo, src: t.ao },
  ];
function Jq({
  attachRetainedWebview: e,
  attachWebview: n,
  configureBrowserSession: r,
  createThreadPageState: i,
  ensureThreadStateForWindow: a,
  openBrowserSettingsUrlInTab: o,
  owner: s,
  preloadPath: c,
  recordDebugEvent: l,
  state: u,
  windowId: d,
}) {
  let f = (e, n, o) => {
      if (t.aa(o.partition) != null) return;
      let f = Qq(o);
      if (f != null) {
        if (u.browserSettingsWebviewMountIds.size === 0) {
          (Vq().warning(`IAB_LIFECYCLE rejected browser settings webview outside settings route`, {
            safe: { ownerWebContentsId: s.id, windowId: d },
            sensitive: {},
          }),
            e.preventDefault());
          return;
        }
        Xq({ configureBrowserSession: r, params: o, webPreferences: n });
        let t = fJ(o);
        if (t == null) {
          (Vq().warning(
            `IAB_LIFECYCLE rejected browser settings webview attachment without instance id`,
            { safe: { ownerWebContentsId: s.id, windowId: d }, sensitive: {} },
          ),
            e.preventDefault());
          return;
        }
        u.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.set(
          t,
          f.allowedNavigationUrls,
        );
        return;
      }
      let p = DF(o),
        m = p == null ? null : (u.threads.get(yL(p)) ?? a(p.conversationId, p.browserTabId));
      if (p == null || m == null) {
        (l({
          browserTabId: p?.browserTabId ?? null,
          conversationId: p?.conversationId ?? null,
          guestWebContentsId: null,
          kind: `browser-webview-attach-rejected`,
          message: `Rejected Browser sidebar webview attachment`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }),
          Vq().warning(`IAB_LIFECYCLE rejected browser sidebar webview attachment`, {
            safe: {
              browserTabId: p?.browserTabId ?? null,
              conversationId: p?.conversationId ?? null,
              ownerWebContentsId: s.id,
              threadExists: p == null ? !1 : m != null,
              windowId: d,
            },
            sensitive: {},
          }),
          e.preventDefault());
        return;
      }
      Vq().info(`IAB_LIFECYCLE browser sidebar will attach webview`, {
        safe: {
          browserTabId: p.browserTabId,
          conversationId: p.conversationId,
          ownerWebContentsId: s.id,
          pendingAdoptedAttachCount: u.pendingAdoptedWebviewRoutesByWebContentsId.size,
          pendingAttachCount: u.pendingWebviewAttachTokens.length,
          pendingRetainedAttachCount: u.pendingRetainedWebviewRoutesByInstanceId.size,
          windowId: d,
        },
        sensitive: {},
      });
      let h = m.page ?? i(p.conversationId, m, { title: m.snapshot.title, url: m.snapshot.url });
      if (h.view.webContents != null && !h.view.webContents.isDestroyed()) {
        (l({
          browserTabId: p.browserTabId,
          conversationId: p.conversationId,
          guestWebContentsId: h.view.webContents.id,
          kind: `browser-webview-reattach-existing`,
          message: `Reattached existing Browser sidebar webview`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }),
          Vq().info(`IAB_LIFECYCLE reattaching existing browser sidebar webview`, {
            safe: {
              browserTabId: p.browserTabId,
              conversationId: p.conversationId,
              guestWebContentsId: h.view.webContents.id,
              ownerWebContentsId: s.id,
              windowId: d,
            },
            sensitive: {},
          }));
        return;
      }
      if (
        (l({
          browserTabId: p.browserTabId,
          conversationId: p.conversationId,
          details: [
            { label: `hasPendingUrl`, value: String(h.pendingUrl.length > 0) },
            { label: `hasUrl`, value: String(h.url.length > 0) },
          ],
          guestWebContentsId: null,
          kind: `browser-webview-attach-requested`,
          message: `Requested Browser sidebar webview attachment`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }),
        Vq().info(`IAB_LIFECYCLE requested browser sidebar webview attachment`, {
          safe: {
            browserTabId: p.browserTabId,
            conversationId: p.conversationId,
            ownerWebContentsId: s.id,
            hasPendingUrl: h.pendingUrl.length > 0,
            hasUrl: h.url.length > 0,
            windowId: d,
          },
          sensitive: {},
        }),
        Yq({ configureBrowserSession: r, params: o, preloadPath: c, webPreferences: n }),
        gJ(o))
      ) {
        let e = _J(o);
        if (e != null) {
          let t = vJ(o);
          (u.pendingAdoptedWebviewRoutesByWebContentsId.set(e, {
            adoptedInitialUrl: t ?? void 0,
            mountGeneration: m.mountGeneration,
            route: p,
          }),
            Vq().info(`IAB_ADOPTION queued adopted webview attach`, {
              safe: {
                adoptedWebContentsId: e,
                browserTabId: p.browserTabId,
                conversationId: p.conversationId,
                hasInitialUrl: t != null,
                windowId: d,
              },
              sensitive: {},
            }));
        }
        return;
      }
      if (hJ(o)) {
        let r = fJ(o);
        if (r == null) {
          (Vq().warning(
            `IAB_LIFECYCLE rejected retained browser sidebar webview attachment without instance id`,
            {
              safe: {
                browserTabId: p.browserTabId,
                conversationId: p.conversationId,
                ownerWebContentsId: s.id,
                windowId: d,
              },
              sensitive: {},
            },
          ),
            e.preventDefault());
          return;
        }
        let i = wL(h);
        ((o.src = i.length === 0 ? `about:blank` : i),
          t.po(i) && (n.backgroundThrottling = !1),
          u.pendingRetainedWebviewRoutesByInstanceId.set(r, {
            mountGeneration: m.mountGeneration,
            route: p,
          }),
          Vq().info(`IAB_LIFECYCLE queued retained browser sidebar webview route`, {
            safe: {
              browserTabId: p.browserTabId,
              conversationId: p.conversationId,
              instanceId: r,
              ownerWebContentsId: s.id,
              pendingRetainedAttachCount: u.pendingRetainedWebviewRoutesByInstanceId.size,
              windowId: d,
            },
            sensitive: {},
          }));
        return;
      }
      let g = crypto.randomUUID();
      ((o.src = kF(g)),
        u.pendingWebviewAttachTokens.push(g),
        u.pendingWebviewRoutesByAttachToken.set(g, {
          mountGeneration: m.mountGeneration,
          route: p,
        }),
        Vq().info(`IAB_LIFECYCLE created browser sidebar webview attach token`, {
          safe: {
            browserTabId: p.browserTabId,
            conversationId: p.conversationId,
            mountGeneration: m.mountGeneration,
            ownerWebContentsId: s.id,
            pendingAttachCount: u.pendingWebviewAttachTokens.length,
            windowId: d,
          },
          sensitive: {},
        }),
        l({
          browserTabId: p.browserTabId,
          conversationId: p.conversationId,
          details: [
            { label: `attachToken`, value: g },
            { label: `mountGeneration`, value: String(m.mountGeneration) },
          ],
          guestWebContentsId: null,
          kind: `browser-webview-attach-token-created`,
          message: `Created Browser sidebar webview attach token`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }));
    },
    p = (t, r) => {
      if (Bq(r.session)) return;
      let i = $q(r, u);
      if (i != null) {
        tJ({ allowedNavigationUrls: i, openBrowserSettingsUrlInTab: o, state: u, webContents: r });
        return;
      }
      if (
        (l({
          browserTabId: null,
          conversationId: null,
          guestWebContentsId: r.id,
          kind: `browser-webview-did-attach`,
          message: `Did attach Browser sidebar webview`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }),
        Vq().info(`IAB_LIFECYCLE did attach browser sidebar webview`, {
          safe: {
            guestWebContentsId: r.id,
            ownerWebContentsId: s.id,
            pendingAdoptedAttachCount: u.pendingAdoptedWebviewRoutesByWebContentsId.size,
            pendingAttachCount: u.pendingWebviewAttachTokens.length,
            pendingRetainedAttachCount: u.pendingRetainedWebviewRoutesByInstanceId.size,
            windowId: d,
          },
          sensitive: {},
        }),
        yJ(r, n) || bJ(r, u, n))
      )
        return;
      let a = eJ(r, u);
      if (a != null) {
        e(r, a);
        return;
      }
      u.pendingWebviewAttachTokens.length > 0 &&
        (l({
          browserTabId: null,
          conversationId: null,
          details: [
            { label: `pendingAttachCount`, value: String(u.pendingWebviewAttachTokens.length) },
          ],
          guestWebContentsId: r.id,
          kind: `browser-webview-attach-deferred`,
          message: `Deferred Browser sidebar webview attach until bootstrap URL`,
          ownerWebContentsId: s.id,
          pageKey: null,
          windowId: d,
        }),
        Vq().info(`IAB_LIFECYCLE deferred browser sidebar webview attachment until bootstrap url`, {
          safe: {
            guestWebContentsId: r.id,
            ownerWebContentsId: s.id,
            pendingAttachCount: u.pendingWebviewAttachTokens.length,
            windowId: d,
          },
          sensitive: {},
        }),
        xJ(r, n));
    };
  return (
    s.on(`will-attach-webview`, f),
    s.on(`did-attach-webview`, p),
    () => {
      (s.removeListener(`will-attach-webview`, f),
        s.removeListener(`did-attach-webview`, p),
        u.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.clear());
      for (let e of u.browserSettingsWebviewNavigationDisposersByWebContentsId.values()) e();
    }
  );
}
function Yq({ configureBrowserSession: e, params: t, preloadPath: n, webPreferences: r }) {
  ((t.partition = cy(`app`)), (r.session = e(`app`)), (r.preload = n), Zq(t, r));
}
function Xq({ configureBrowserSession: e, params: t, webPreferences: n }) {
  ((t.partition = cy(`app`)),
    (n.session = e(`app`)),
    delete n.preload,
    Zq(t, n),
    (t.allowpopups = ``),
    Object.assign(n, { disablePopups: !1 }));
}
function Zq(e, t) {
  (delete e.allowpopups,
    delete e.disablewebsecurity,
    delete e.webpreferences,
    (t.sandbox = !0),
    (t.devTools = !0),
    (t.nodeIntegration = !1),
    (t.nodeIntegrationInSubFrames = !1),
    (t.nodeIntegrationInWorker = !1),
    (t.contextIsolation = !0),
    (t.webSecurity = !0),
    (t.allowRunningInsecureContent = !1),
    (t.webviewTag = !1),
    (t.plugins = !1),
    Object.assign(t, { disablePopups: !0 }));
}
function Qq(e) {
  let t =
    qq.find(
      (t) =>
        e.partition === t.partition &&
        sJ({ allowedNavigationUrls: t.allowedNavigationUrls, navigationUrl: e.src }),
    ) ?? null;
  return t?.isEnabled() === !0 ? t : null;
}
function $q(e, t) {
  let n = pJ(e);
  if (n == null) return null;
  let r = t.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.get(n) ?? null;
  return r == null
    ? null
    : (t.pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId.delete(n), r);
}
function eJ(e, t) {
  let n = pJ(e);
  if (n == null) return null;
  let r = t.pendingRetainedWebviewRoutesByInstanceId.get(n) ?? null;
  return r == null ? null : (t.pendingRetainedWebviewRoutesByInstanceId.delete(n), r.route);
}
function tJ({
  allowedNavigationUrls: e,
  openBrowserSettingsUrlInTab: t,
  state: n,
  webContents: r,
}) {
  (n.browserSettingsWebviewNavigationDisposersByWebContentsId.get(r.id)?.(),
    n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.set(r.id, e),
    n.browserSettingsWebContentsByWebContentsId.set(r.id, r));
  let i = () => {
      nJ(r, n.browserSettingsWebviewThemeVariant);
    },
    a = oJ({
      allowedNavigationUrls: e,
      onDispose: () => {
        (n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.delete(r.id),
          n.browserSettingsWebContentsByWebContentsId.delete(r.id),
          n.browserSettingsWebviewNavigationDisposersByWebContentsId.delete(r.id));
      },
      webContents: r,
    });
  (n.browserSettingsWebviewNavigationDisposersByWebContentsId.set(r.id, () => {
    (r.removeListener(`did-finish-load`, i), a());
  }),
    r.setWindowOpenHandler(({ url: e }) => (gb(e) && t(e), { action: `deny` })),
    r.on(`did-finish-load`, i),
    i());
}
function nJ(e, t) {
  let n = (Kq.get(e) ?? Promise.resolve())
    .catch(() => void 0)
    .then(async () => {
      await _L(e, t);
    });
  Kq.set(e, n);
}
function rJ({ webContentsId: e, windows: t, url: n }) {
  let r = e == null ? null : aJ(t, e);
  return r != null && !sJ({ allowedNavigationUrls: r, navigationUrl: n });
}
function iJ(e, t) {
  if (e.browserSettingsWebviewThemeVariant !== t) {
    e.browserSettingsWebviewThemeVariant = t;
    for (let n of e.browserSettingsWebContentsByWebContentsId.values()) nJ(n, t);
  }
}
function aJ(e, t) {
  for (let n of e) {
    let e = n.browserSettingsWebviewAllowedNavigationUrlsByWebContentsId.get(t) ?? null;
    if (e != null) return e;
  }
  return null;
}
function oJ({ allowedNavigationUrls: e, onDispose: t, webContents: n }) {
  let r = (t) => sJ({ allowedNavigationUrls: e, navigationUrl: t }),
    i = (e, t) => {
      !e.isMainFrame || r(t) || e.preventDefault();
    },
    a = (e) => {
      i(e, e.url);
    },
    o = (e, t) => {
      i(e, e.url ?? t);
    },
    s = (e, t) => {
      i(e, e.url ?? t);
    },
    c = (t, i) => {
      !i ||
        r(t) ||
        n.loadURL(e[0] ?? `about:blank`).catch(() => {
          n.close();
        });
    },
    l = (e, t) => {
      c(t, !0);
    },
    u = (e, t, n) => {
      c(t, n);
    },
    d = () => {
      (n.removeListener(`will-frame-navigate`, a),
        n.removeListener(`will-navigate`, o),
        n.removeListener(`will-redirect`, s),
        n.removeListener(`did-navigate`, l),
        n.removeListener(`did-navigate-in-page`, u),
        n.removeListener(`destroyed`, d),
        t());
    };
  return (
    n.on(`will-frame-navigate`, a),
    n.on(`will-navigate`, o),
    n.on(`will-redirect`, s),
    n.on(`did-navigate`, l),
    n.on(`did-navigate-in-page`, u),
    n.on(`destroyed`, d),
    d
  );
}
function sJ({ allowedNavigationUrls: e, navigationUrl: t }) {
  return e.some((e) => cJ({ allowedNavigationUrl: e, navigationUrl: t }));
}
function cJ({ allowedNavigationUrl: e, navigationUrl: t }) {
  let n = lJ(e),
    r = lJ(t);
  return n == null || r == null || r.protocol !== n.protocol || r.host !== n.host
    ? !1
    : uJ(n.pathname, r.pathname);
}
function lJ(e) {
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
function uJ(e, t) {
  let n = dJ(e),
    r = dJ(t);
  return n === `/` || r === n || r.startsWith(`${n}/`);
}
function dJ(e) {
  let t = e.replace(/\/+$/, ``);
  return t.length === 0 ? `/` : t;
}
function fJ(e) {
  return mJ(e.instanceId);
}
function pJ(e) {
  return mJ(e.viewInstanceId);
}
function mJ(e) {
  let t = Number(e);
  return Number.isInteger(t) && t > 0 ? t : null;
}
function hJ(e) {
  return Hq in e;
}
function gJ(e) {
  let t = e[Uq];
  return typeof t == `string` && t.length > 0;
}
function _J(e) {
  let t = Number(e[Wq]);
  return Number.isInteger(t) && t > 0 ? t : null;
}
function vJ(e) {
  let t = e[Gq];
  return typeof t == `string` && t.length > 0 ? t : null;
}
function yJ(e, t) {
  if (e.isDestroyed()) return !1;
  let n = jF(e.getURL());
  return n == null ? !1 : (t(e, n), !0);
}
function bJ(e, t, n) {
  if (e.isDestroyed()) return !1;
  let r = t.pendingAdoptedWebviewRoutesByWebContentsId.get(e.id) ?? null;
  if (r == null) return t.canceledAdoptedWebContentsIds.delete(e.id) ? (e.close(), !0) : !1;
  t.pendingAdoptedWebviewRoutesByWebContentsId.delete(e.id);
  let i = crypto.randomUUID();
  return (
    t.pendingWebviewAttachTokens.push(i), t.pendingWebviewRoutesByAttachToken.set(i, r), n(e, i), !0
  );
}
function xJ(e, t) {
  let n = () => {
      (e.removeListener(`did-finish-load`, r),
        e.removeListener(`did-navigate`, r),
        e.removeListener(`did-navigate-in-page`, r),
        e.removeListener(`destroyed`, n));
    },
    r = () => {
      yJ(e, t) && n();
    };
  (e.on(`did-finish-load`, r),
    e.on(`did-navigate`, r),
    e.on(`did-navigate-in-page`, r),
    e.on(`destroyed`, n));
}
var SJ = t.Gr(`browser-sidebar-manager`);
function CJ({ ensureWindowState: e, windowId: t, windows: n }) {
  let r = n.get(t) ?? null;
  if (r == null) {
    let n = a.BrowserWindow.fromId(t);
    n != null && !n.isDestroyed() && !n.webContents.isDestroyed() && (r = e(n, n.webContents));
  }
  return r == null || r.window.isDestroyed() || r.owner.isDestroyed()
    ? (SJ().warning(`IAB_LIFECYCLE route window is not live`, {
        safe: {
          hasWindowState: r != null,
          ownerDestroyed: r?.owner.isDestroyed() ?? null,
          windowDestroyed: r?.window.isDestroyed() ?? null,
          windowId: t,
        },
        sensitive: {},
      }),
      null)
    : r;
}
function wJ({ getLiveWindowState: e, lastSyncedWindowId: t, windows: n }) {
  let r = a.BrowserWindow.getFocusedWindow();
  if (r != null && !r.isDestroyed()) return r.webContents;
  let i = a.BrowserWindow.getAllWindows().filter((e) => !e.isDestroyed());
  if (i.length === 1) return i[0]?.webContents ?? null;
  if (t != null) {
    let n = e(t);
    if (n != null) return n.owner;
  }
  for (let t of n) {
    let n = e(t.window.id);
    if (n != null) return n.owner;
  }
  return null;
}
function TJ({ getLiveWindowState: e, windows: t }) {
  let n = a.BrowserWindow.getFocusedWindow();
  return n == null || n.isDestroyed() || !t.has(n.id) ? null : e(n.id);
}
function EJ({
  getFocusedWindowState: e,
  getLiveWindowState: t,
  lastSyncedWindowId: n,
  windows: r,
}) {
  let i = e();
  if (i != null) return i;
  if (n != null) {
    let e = t(n);
    if (e != null) return e;
  }
  for (let e of r) {
    let n = t(e.window.id);
    if (n != null) return n;
  }
  return null;
}
function DJ({
  conversationId: e,
  getFocusedWindowState: t,
  hasThreadForConversation: n,
  lastSyncedWindowId: r,
  windows: i,
}) {
  let a = t();
  if (a != null && !a.owner.isDestroyed() && n(a, e)) return a;
  if (r != null) {
    let t = i.get(r) ?? null;
    if (t != null && !t.owner.isDestroyed() && n(t, e)) return t;
  }
  for (let t of i.values()) if (!t.owner.isDestroyed() && n(t, e)) return t;
  return null;
}
var OJ = t.Gr(`browser-sidebar-screenshot`);
async function kJ(e) {
  let t = await e.capturePage();
  a.clipboard.writeImage(t);
}
async function AJ({
  browserTabId: e,
  conversationId: n,
  errorReporter: r,
  origin: i,
  pageState: a,
  windowManager: o,
}) {
  let s = a?.browserTabId ?? e ?? t.rs(crypto.randomUUID()),
    c = a?.view.webContents;
  if (c == null || c.isDestroyed()) {
    o.sendMessageToWebContents(i, {
      type: `browser-sidebar-screenshot-copy-failed`,
      conversationId: n,
      browserTabId: s,
    });
    return;
  }
  try {
    (await kJ(c),
      o.sendMessageToWebContents(i, {
        type: `browser-sidebar-screenshot-copied`,
        conversationId: n,
        browserTabId: s,
      }));
  } catch (e) {
    (OJ().warning(`failed to capture browser sidebar screenshot`, {
      safe: { conversationId: n },
      sensitive: { error: e },
    }),
      r.reportNonFatal(e instanceof Error ? e : Error(String(e)), {
        kind: `browser-sidebar-screenshot`,
      }),
      o.sendMessageToWebContents(i, {
        type: `browser-sidebar-screenshot-copy-failed`,
        conversationId: n,
        browserTabId: s,
      }));
  }
}
async function jJ(e, t) {
  await e.executeJavaScript(MJ(t));
}
function MJ(e) {
  return `(() => {
    const scroll = ${JSON.stringify(e)};
    const root = document.scrollingElement ?? document.documentElement;
    switch (scroll.type) {
      case "pixels":
        window.scrollBy({ top: scroll.y, behavior: "auto" });
        return;
      case "pages":
        window.scrollBy({
          top: window.innerHeight * scroll.count,
          behavior: "auto",
        });
        return;
      case "edge":
        root.scrollTo({
          top: scroll.edge === "top" ? 0 : root.scrollHeight,
          behavior: "auto",
        });
        return;
    }
  })();`;
}
var NJ = t.Gr(`browser-sidebar-manager`),
  PJ = (0, s.join)(__dirname, `comment-preload.js`),
  FJ = class {
    windows = new Map();
    configuredPartitions = new Set();
    tabBudget;
    browserUseOpenRequests;
    browserUseNavigationBlockedListeners = new Set();
    activeDraggedImagesByOwnerWebContentsId = new Map();
    onBrowserSidebarStateChanged = () => {};
    lastSyncedWindowId = null;
    ownerCommandAccelerators = HV();
    comments;
    emptyPageLocalServers;
    allowNonLocalBrowserUseNavigation = !1;
    isTweaksFeatureEnabled = !1;
    lifecycleSequence = 0;
    browserUseNavigationRestriction = new MF({
      findPageStateForPageKey: (e) => this.findPageStateForPageKey(e),
      findPageStateForWebContentsId: (e) => this.findPageStateForWebContentsId(e),
      isEnabled: () => !this.allowNonLocalBrowserUseNavigation,
      onNavigationBlocked: (e, t, n) => {
        (NJ().warning(`blocked browser-use navigation`, {
          safe: {},
          sensitive: { error: t, url: n },
        }),
          this.notifyBrowserUseNavigationBlocked({
            error: t,
            pageKey: vL(e.windowState.owner.id, e.conversationId, e.threadState.browserTabId),
            url: n,
          }));
      },
    });
    constructor(e, t, n = z, r = FP(), i = new BK(), a = null, o = gL(), s = {}) {
      ((this.windowManager = e),
        (this.errorReporter = t),
        (this.commentModeBlocklistLookup = r),
        (this.browserSessionRegistry = i),
        (this.defaultBrowserUseViewportSize = o),
        (this.browserUseOpenRequests = new BF((e) => {
          this.browserSessionRegistry.recordDebugEvent(e);
        })),
        (this.tabBudget = new Eq({
          browserSessionRegistry: this.browserSessionRegistry,
          suspendDetachedThread: (e, t) => {
            this.suspendDetachedThread(e, t);
          },
          windows: this.windows,
        })),
        (this.emptyPageLocalServers = new aB({
          getWindowStates: () => this.windows.values(),
          persistencePath: a,
          windowManager: e,
        })),
        this.browserSessionRegistry.setDelegate({
          addBrowserUseNavigationBlockedListener: (e) =>
            this.addBrowserUseNavigationBlockedListener(e),
          activateBrowserUseNavigationRestriction: (e) =>
            this.activateBrowserUseNavigationRestriction(e),
          assertBrowserUseNavigationAllowed: (e, t) => this.assertBrowserUseNavigationAllowed(e, t),
          clearPendingBrowserUseNavigation: (e) => this.clearPendingBrowserUseNavigation(e),
          disposeBrowserUseRoute: (e) => this.disposeBrowserUseRouteForRoute(e),
          getPageForBrowserUse: (e, t) => this.getPageForBrowserUseForRoute(e, t),
          getPagesForBrowserUse: (e) => this.getPagesForBrowserUseForRoute(e),
          getTabsForBrowserUse: (e) => this.getTabsForBrowserUseForRoute(e),
          getUserTabsForBrowserUse: (e) => this.getUserTabsForBrowserUseForRoute(e),
          isWindowAlive: (e) => this.getLiveWindowState(e) != null,
          closePageForBrowserUse: (e, t) => this.closePageForBrowserUseForRoute(e, t),
          releasePageForBrowserUse: (e, t) => this.releasePageForBrowserUseForRoute(e, t),
          openPageForBrowserUse: (e, t) =>
            this.openPageForBrowserUseForRoute({
              ...e,
              browserTabId: t?.browserTabId,
              origin: t?.origin,
              startingUrl: t?.startingUrl,
            }),
          isBrowserVisibleForBrowserUse: (e, t) => this.isBrowserVisibleForBrowserUseForRoute(e, t),
          setBrowserVisibleForBrowserUse: (e, t, n) =>
            this.setBrowserVisibleForBrowserUseForRoute(e, t, n),
          setViewportForBrowserUse: (e, t, n) => this.setViewportForBrowserUseForRoute(e, t, n),
          setCaptureSurfaceForBrowserUse: (e, t, n) =>
            this.setCaptureSurfaceForBrowserUseForRoute(e, t, n),
          waitForPendingDebuggerSync: (e, t) => this.waitForPendingDebuggerSyncForRoute(e, t),
          setBrowserUseActive: (e, t, n) => this.setBrowserUseActiveForRoute(e, t, n),
          setBrowserUseCursor: (e, t, n) => this.setBrowserUseCursorForRoute(e, t, n),
          trackPendingBrowserUseNavigation: (e, t) => this.trackPendingBrowserUseNavigation(e, t),
        }),
        (this.comments = new RI(
          e,
          t,
          {
            canUseTweaks: () => this.isTweaksFeatureEnabled,
            getThreadState: (e, t, n) => this.getThreadState(e, t, n),
            findPageStateForRuntimeSender: (e) => this.findPageStateForRuntimeSender(e),
            syncCommentSnapshot: (e, t, n) => {
              let r = this.getThreadState(e, t, n);
              r != null && this.syncCommentSnapshot(e, t, r);
            },
          },
          n,
        )),
        this.observeThreadLifecycle(s.appServerNotificationSource));
    }
    observeThreadLifecycle(e) {
      e?.registerInternalNotificationHandler((e) => {
        if (IJ(e)) {
          this.browserSessionRegistry.disposeConversation(e.params.threadId);
          return;
        }
        RJ(e) &&
          this.browserSessionRegistry.endSessionActivity({ conversationId: e.params.threadId });
      });
    }
    setOnBrowserSidebarStateChanged(e) {
      this.onBrowserSidebarStateChanged = e;
    }
    refreshOwnerCommandAccelerators() {
      this.ownerCommandAccelerators = HV();
    }
    browserSettingsWebviewMounted(e, t, n = `light`) {
      let r = this.ensureCurrentWindowState(e);
      r != null && (r.browserSettingsWebviewMountIds.add(t), iJ(r, n));
    }
    browserSettingsWebviewThemeChanged(e, t) {
      let n = this.ensureCurrentWindowState(e);
      n != null && iJ(n, t);
    }
    browserSettingsWebviewUnmounted(e, t) {
      this.getCurrentWindowState(e)?.browserSettingsWebviewMountIds.delete(t);
    }
    sync(e, t) {
      let n = this.ensureCurrentWindowState(e);
      if (n == null) return;
      this.lastSyncedWindowId = n.window.id;
      let r = n.activeConversationId,
        i = n.activeBrowserTabId,
        a = t.mountGeneration ?? 0,
        o = t.presented ?? (t.visible && t.bounds != null),
        s = this.getBrowserTabIdForCommand(n, t.conversationId, t.browserTabId);
      if (
        (this.browserSessionRegistry.recordDebugEvent({
          browserTabId: s,
          conversationId: t.conversationId,
          details: [
            { label: `mountGeneration`, value: String(a) },
            { label: `presented`, value: String(o) },
            { label: `visible`, value: String(t.visible) },
            ...(t.transferSourceConversationId == null
              ? []
              : [{ label: `transferSourceConversationId`, value: t.transferSourceConversationId }]),
          ],
          guestWebContentsId: null,
          kind: `browser-sidebar-sync`,
          message: `Synced Browser sidebar route`,
          ownerWebContentsId: e.id,
          pageKey: null,
          windowId: n.window.id,
        }),
        t.transferSourceConversationId != null &&
          this.transferConversationState(
            n,
            t.transferSourceConversationId,
            t.transferSourceBrowserTabId,
            t.conversationId,
            s,
          ),
        n.ignoredConversationIds.delete(yL({ browserTabId: s, conversationId: t.conversationId })))
      )
        return;
      (o || n.activeConversationId == null) &&
        ((n.activeConversationId = t.conversationId), (n.activeBrowserTabId = s));
      let c = this.ensureThreadState(n, t, s);
      this.setThreadAnnotationModeEnabled(e, n, c, t.annotationModeEnabled ?? !0);
      let l = c.presented;
      ((c.allowLocalServerPortScan = t.allowLocalServerPortScan === !0),
        (c.cwd = t.cwd ?? null),
        (c.hostKind = t.hostKind ?? null),
        (c.rolloutPath = t.rolloutPath ?? null),
        (c.presented = o),
        o && !l && this.tabBudget.markTabActivity(c),
        (c.visible = t.visible),
        (c.mountGeneration = a),
        o &&
          c.pendingTeardown?.reason === `suspended` &&
          ((c.pendingTeardown = null), c.page != null && (c.page.pendingSuspensionId = null)),
        (c.hasPendingBrowserUseVisibilityRequest = !1),
        (c.isAgentControllingBrowser = t.isAgentControllingBrowser),
        t.isAgentControllingBrowser || (c.isBrowserUseNavigationRestrictionActive = !1),
        (c.runtimeIntlConfig = t.runtimeIntlConfig));
      let u = c.themeVariant;
      ((c.themeVariant = t.themeVariant ?? c.themeVariant),
        (c.bounds = t.bounds
          ? {
              x: Math.round(t.bounds.x),
              y: Math.round(t.bounds.y),
              width: Math.round(t.bounds.width),
              height: Math.round(t.bounds.height),
            }
          : null));
      let d = t.emulatedViewportSize ?? null,
        f =
          c.emulatedViewportSize?.width !== d?.width ||
          c.emulatedViewportSize?.height !== d?.height;
      if (
        ((c.emulatedViewportSize = d),
        (c.viewportScale = t.viewportScale ?? 1),
        this.updateThreadSessionState(c),
        this.syncThreadState(n, t.conversationId, c),
        c.page != null)
      ) {
        if (
          (o && (c.page.pendingSuspensionId = null),
          f && this.queuePageDeviceMetricsSync(c.page, c.emulatedViewportSize),
          u !== c.themeVariant)
        ) {
          let e = c.page.view.webContents;
          e != null && !e.isDestroyed() && this.queuePageColorSchemeSync(c.page, e, c.themeVariant);
        }
        this.syncRuntimeStateToPage(c.page, c);
      }
      if (
        (this.comments[c.isAnnotationModeEnabled ? `prepare` : `dismiss`](
          e,
          t.conversationId,
          s,
          c,
        ),
        o && r != null && i != null && (r !== t.conversationId || i !== s))
      ) {
        let t = this.getThreadStateForWindow(n, r, i) ?? null;
        (t != null && (t.hasPendingBrowserUseVisibilityRequest = !1),
          this.comments.dismiss(e, r, i, t),
          this.comments.close(e, r, i));
      }
      ((!c.presented || c.bounds == null) && this.comments.dismiss(e, t.conversationId, s, c),
        this.tabBudget.enforceDetachedPageBudget(n),
        this.resolveBrowserUseOpenRequests(n));
    }
    updateThreadSessionState(e) {
      e.sessionState;
      let t = this.lifecycleSequence + 1;
      if (
        ((this.lifecycleSequence = t),
        (e.lastLifecycleSequence = t),
        e.presented && (e.lastPresentedSequence = t),
        e.page == null)
      )
        e.sessionState = e.snapshot.isSuspended ? `suspended` : `cold`;
      else if (e.pendingTeardown != null) return;
      else
        e.page.isSuspended
          ? (e.sessionState = `suspended`)
          : e.page.view.webContents == null || e.page.view.webContents.isDestroyed()
            ? (e.sessionState = `cold`)
            : (e.sessionState = e.presented ? `live-attached` : `live-detached`);
      e.sessionState;
    }
    setThreadAnnotationModeEnabled(e, t, n, r) {
      if (SF(n, r)) {
        let { browserTabId: r, conversationId: i, page: a } = n;
        (this.comments.dismiss(e, i, r, n),
          this.syncThreadState(t, i, n),
          a != null && this.syncRuntimeStateToPage(a, n));
      }
    }
    suspendDetachedThread(e, t) {
      if (
        !(
          t.presented ||
          this.tabBudget.isThreadProtectedFromSuspension(t) ||
          t.page == null ||
          t.page.isSuspended
        ) &&
        this.suspend(e.owner, t.conversationId, crypto.randomUUID(), t.browserTabId) &&
        t.page.pendingSuspensionId != null
      ) {
        if (this.tabBudget.isThreadProtectedFromSuspension(t)) {
          ((t.page.pendingSuspensionId = null), this.tabBudget.enforceDetachedPageBudget(e));
          return;
        }
        this.requestThreadTeardown(e, t.conversationId, t, `suspended`);
      }
    }
    requestThreadTeardown(e, t, n, r) {
      let i = n.page;
      if (i == null) {
        if (r === `closed`) {
          let i = crypto.randomUUID();
          (this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
            this.browserSessionRegistry.recordPageClosed({
              browserTabId: n.browserTabId,
              conversationId: t,
              windowId: RK(e.window.id),
            }),
            this.windowManager.sendMessageToWebContents(e.owner, {
              type: `browser-sidebar-destroy-webview`,
              browserTabId: n.browserTabId,
              conversationId: t,
              mountGeneration: n.mountGeneration,
              reason: r,
              teardownId: i,
            }),
            this.removeThreadState(e, n));
        }
        return;
      }
      if (n.pendingTeardown?.reason === r) return;
      let a = crypto.randomUUID();
      n.pendingTeardown = { mountGeneration: n.mountGeneration, reason: r, teardownId: a };
      let o = i.view.webContents;
      if (o == null || o.isDestroyed()) {
        this.finalizeThreadTeardown(e, t, n, a);
        return;
      }
      this.windowManager.sendMessageToWebContents(e.owner, {
        type: `browser-sidebar-destroy-webview`,
        browserTabId: n.browserTabId,
        conversationId: t,
        mountGeneration: n.mountGeneration,
        reason: r,
        teardownId: a,
      });
    }
    finalizeThreadTeardown(e, t, n, r) {
      let i = n.pendingTeardown;
      if (i?.teardownId !== r) return;
      let a = i.reason;
      if (
        a === `suspended` &&
        (n.presented ||
          this.tabBudget.isThreadProtectedFromSuspension(n) ||
          i.mountGeneration !== n.mountGeneration)
      ) {
        ((n.pendingTeardown = null),
          n.page != null && (n.page.pendingSuspensionId = null),
          this.updateThreadSessionState(n),
          this.syncThreadState(e, t, n),
          this.tabBudget.enforceDetachedPageBudget(e));
        return;
      }
      let o = n.page,
        s = o?.view.webContents ?? null;
      if (
        (s != null && !s.isDestroyed() && s.close(),
        o != null &&
          ((o.pendingSuspensionId = null),
          (o.isRestoring = !1),
          (o.isSuspended = a === `suspended`),
          o.dispose(),
          (o.dispose = () => void 0),
          this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
            (a === `closed`
              ? this.browserSessionRegistry.recordPageClosed({
                  browserTabId: n.browserTabId,
                  conversationId: t,
                  windowId: RK(e.window.id),
                })
              : s != null &&
                this.browserSessionRegistry.recordPageTornDown({
                  browserTabId: n.browserTabId,
                  conversationId: t,
                  guestWebContentsId: s.id,
                  windowId: RK(e.window.id),
                }))),
        (n.pendingTeardown = null),
        a === `closed`)
      ) {
        this.removeThreadState(e, n);
        return;
      }
      (this.updateThreadSessionState(n),
        this.syncThreadState(e, t, n),
        this.tabBudget.enforceDetachedPageBudget(e));
    }
    findThreadStateForPendingTeardown(e, t) {
      for (let n of e.threads.values()) if (n.pendingTeardown?.teardownId === t) return n;
      return null;
    }
    syncOwner(e, t, n, r = !1, i, a = !0) {
      let o = this.ensureCurrentWindowState(e);
      if (o == null) return;
      this.lastSyncedWindowId = o.window.id;
      let s = n ?? (o.activeConversationId === t ? o.activeBrowserTabId : null) ?? OF(t);
      ((o.activeConversationId = t),
        this.browserSessionRegistry.setRouteTabMode(
          { conversationId: t, windowId: RK(o.window.id) },
          r ? `multiTab` : `singleTab`,
        ),
        o.ignoredConversationIds.delete(yL({ browserTabId: s, conversationId: t })),
        (o.activeBrowserTabId = s),
        this.browserSessionRegistry.bindSessionRouteOwner(t, e));
      let c = this.ensureThreadStateForWindow(o, t, s);
      for (let n of o.threads.values())
        n.conversationId === t &&
          ((n.ownerRoutePath = i ?? null), this.setThreadAnnotationModeEnabled(e, o, n, a));
      (this.browserSessionRegistry.recordDebugEvent({
        browserTabId: s,
        conversationId: t,
        details: [{ label: `ownerRoutePath`, value: i ?? `missing` }],
        guestWebContentsId: c?.page?.view.webContents?.id ?? null,
        kind: `browser-owner-synced`,
        message: `Synced exact Browser owner`,
        ownerWebContentsId: e.id,
        pageKey: c?.page == null ? null : vL(e.id, t, s),
        windowId: o.window.id,
      }),
        c?.pendingBrowserUsePanelOpen === !0 &&
          ((c.pendingBrowserUsePanelOpen = !1), this.openBrowserPanelWithoutAnimation(o, t, s)));
    }
    getActivePageForBrowserUse() {
      return HF({
        getPreferredWindowState: () => this.getPreferredWindowStateForBrowserUse(),
        getWindowStates: () => this.windows.values(),
        getWindowId: (e) => e.window.id,
        getActivePage: (e) => this.getActivePageHandle(e),
      });
    }
    getDebugSnapshot() {
      return GK({
        browserSessionRegistry: this.browserSessionRegistry,
        browserUseOpenRequests: this.browserUseOpenRequests,
        getThreadStateForWindow: this.getThreadStateForWindow.bind(this),
        windows: this.windows.values(),
      });
    }
    openDebugTabOwner(e) {
      return KK({
        browserSessionRegistry: this.browserSessionRegistry,
        getLiveWindowState: this.getLiveWindowState.bind(this),
        getThreadStateForWindow: this.getThreadStateForWindow.bind(this),
        options: e,
        windowManager: this.windowManager,
      });
    }
    async openActivePageForBrowserUse({ startingUrl: e } = {}) {
      return UF({
        startingUrl: e,
        getPreferredWindowState: () => this.getPreferredWindowStateForBrowserUse(),
        getOrCreateActiveVisiblePage: (e) =>
          this.getOrCreatePageForBrowserUse(e, e.activeConversationId, { requireVisible: !0 }),
        getFocusedOwner: () => this.getFocusedOwnerWebContents(),
        isOwnerDestroyed: (e) => e.isDestroyed(),
        sendOpenBrowserPanelMessage: (e, t) => {
          let n = a.BrowserWindow.fromWebContents(e),
            r = n == null || n.isDestroyed() ? null : (this.windows.get(n.id) ?? null);
          this.windowManager.sendMessageToWebContents(e, {
            type: `open-browser-tab`,
            ...(r?.activeConversationId == null ? {} : { conversationId: r.activeConversationId }),
            ...(t.startingUrl == null ? {} : { initialUrl: t.startingUrl }),
            source: `browser_use`,
            initiator: `browser_use`,
          });
        },
        getWindowStateForOwner: (e) => {
          let t = a.BrowserWindow.fromWebContents(e);
          return t == null || t.isDestroyed() ? null : (this.windows.get(t.id) ?? null);
        },
        waitForOpenRequest: (e) => this.browserUseOpenRequests.waitForOpen(e, null),
      });
    }
    getPageForBrowserUse(e) {
      let t = OF(e),
        n = this.getPreferredWindowStateForConversation(e);
      if (n != null) {
        let r = this.getPageHandleForConversation(n, e, t);
        if (r != null) return r;
      }
      for (let r of this.windows.values()) {
        if (r === n || r.owner.isDestroyed() || !this.hasThreadForConversation(r, e)) continue;
        let i = this.getPageHandleForConversation(r, e, t);
        if (i != null) return i;
      }
      return null;
    }
    async openPageForBrowserUse({ conversationId: e, startingUrl: t }) {
      let n = this.getPreferredWindowStateForConversation(e);
      if (n == null) {
        let t = this.getFocusedOwnerWebContents();
        if (t != null && !t.isDestroyed()) {
          let r = a.BrowserWindow.fromWebContents(t);
          r != null &&
            !r.isDestroyed() &&
            ((n = this.ensureWindowState(r, t)),
            (this.lastSyncedWindowId = r.id),
            this.ensureThreadStateForCommand(t, e, OF(e)));
        }
      }
      if (n == null) throw Error(`No Codex browser pane available for browser session ${e}`);
      let r = OF(e),
        i =
          this.getThreadStateForWindow(n, e, r) ?? this.ensureThreadStateForCommand(n.owner, e, r);
      i != null && ((i.isBrowserUsePage = !0), (i.browserUseTabOrigin ??= `agent`));
      let o =
        i?.page ??
        this.ensureThreadPageForCommand(n.owner, e, r, {
          title: i?.snapshot.title ?? `New tab`,
          url: i?.snapshot.url ?? ``,
        });
      n.activeConversationId === e && n.activeBrowserTabId === r
        ? i?.visible !== !0 || i?.bounds == null
          ? (i != null && (i.pendingBrowserUsePanelOpen = !1),
            this.windowManager.sendMessageToWebContents(n.owner, {
              type: `toggle-browser-panel`,
              open: !0,
              browserTabId: r,
              ...(t == null ? {} : { url: t }),
              source: `browser_use`,
              initiator: `browser_use`,
            }))
          : i != null &&
            o != null &&
            o.view.webContents == null &&
            (t != null &&
              o.pendingUrl.length === 0 &&
              o.url.length === 0 &&
              this.navigatePageToUrl(o, t, `browser_use`, `browser_use`),
            this.syncThreadState(n, e, i))
        : i != null && this.openBrowserPanelWithoutAnimation(n, e, r);
      let s = o == null ? null : FL(n, e, o);
      if (s == null) {
        if (n.activeConversationId === e) {
          let t = { conversationId: e, windowId: RK(n.window.id) };
          return this.browserUseOpenRequests.waitForOpen(n.owner, e, r, (e) => {
            this.closePageForBrowserUseForRoute(t, r, e);
          });
        }
        throw Error(`No Codex browser pane available for browser session ${e}`);
      }
      return s;
    }
    getBrowserUseRouteActionHelpers() {
      return {
        browserSessionRegistry: this.browserSessionRegistry,
        browserUseOpenRequests: this.browserUseOpenRequests,
        clearPendingWebviewRoutes: this.clearPendingWebviewRoutes.bind(this),
        comments: this.comments,
        createThreadPageState: this.createThreadPageState.bind(this),
        defaultBrowserUseViewportSize: this.defaultBrowserUseViewportSize,
        ensureThreadStateForWindow: this.ensureThreadStateForWindow.bind(this),
        enforceDetachedPageBudget: this.tabBudget.enforceDetachedPageBudget.bind(this.tabBudget),
        getLiveWindowState: this.getLiveWindowState.bind(this),
        getPageHandleForConversation: this.getPageHandleForConversation.bind(this),
        getThreadStateForWindow: this.getThreadStateForWindow.bind(this),
        navigatePageToUrl: this.navigatePageToUrl.bind(this),
        queuePageDeviceMetricsSync: this.queuePageDeviceMetricsSync.bind(this),
        removeThreadState: this.removeThreadState.bind(this),
        requestThreadTeardown: this.requestThreadTeardown.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        updateThreadSessionState: this.updateThreadSessionState.bind(this),
        windowManager: this.windowManager,
      };
    }
    getPageForBrowserUseForRoute(e, t) {
      return YV(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    getPagesForBrowserUseForRoute(e) {
      return XV(this.getBrowserUseRouteActionHelpers(), e);
    }
    getTabsForBrowserUseForRoute(e) {
      return QV(this.getBrowserUseRouteActionHelpers(), e);
    }
    getUserTabsForBrowserUseForRoute(e) {
      return ZV(this.getBrowserUseRouteActionHelpers(), e);
    }
    async openPageForBrowserUseForRoute({
      browserTabId: e,
      conversationId: t,
      origin: n,
      startingUrl: r,
      windowId: i,
    }) {
      return eH(this.getBrowserUseRouteActionHelpers(), {
        browserTabId: e,
        conversationId: t,
        origin: n,
        startingUrl: r,
        windowId: i,
      });
    }
    closePageForBrowserUseForRoute(e, t, n) {
      tH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    releasePageForBrowserUseForRoute(e, t) {
      nH(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    disposeBrowserUseRouteForRoute(e) {
      rH(this.getBrowserUseRouteActionHelpers(), e);
    }
    setBrowserVisibleForBrowserUseForRoute(e, t, n) {
      iH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    isBrowserVisibleForBrowserUseForRoute(e, t) {
      return aH(this.getBrowserUseRouteActionHelpers(), e, t);
    }
    setViewportForBrowserUseForRoute(e, t, n) {
      oH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setCaptureSurfaceForBrowserUseForRoute(e, t, n) {
      sH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    async waitForPendingDebuggerSyncForRoute(e, t) {
      let n = this.getLiveWindowState(e.windowId);
      if (n == null) return;
      let r =
        (this.getThreadStateForWindow(n, e.conversationId, t ?? OF(e.conversationId)) ?? null)
          ?.page ?? null;
      r != null && (await r.debuggerSync.catch(() => void 0));
    }
    openBrowserPanelWithoutAnimation(e, t, n) {
      cH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setBrowserUseActiveForRoute(e, t, n) {
      let r = this.getLiveWindowState(e.windowId),
        i = new Set(
          r == null
            ? []
            : Array.from(r.threads.values()).filter(
                (t) => t.conversationId === e.conversationId && t.isBrowserUseActive,
              ),
        );
      if ((lH(this.getBrowserUseRouteActionHelpers(), e, t, n), r == null)) return;
      let a = !1;
      for (let t of r.threads.values())
        t.conversationId === e.conversationId &&
          i.has(t) !== t.isBrowserUseActive &&
          ((a = !0), t.isBrowserUseActive && this.tabBudget.markTabActivity(t));
      a && this.tabBudget.enforceDetachedPageBudget(r);
    }
    setBrowserUseCursorForRoute(e, t, n) {
      return uH(this.getBrowserUseRouteActionHelpers(), e, t, n);
    }
    setBrowserUseActiveForConversation(e, t) {
      let n = this.getPreferredWindowStateForConversation(e);
      n != null &&
        this.setBrowserUseActiveForRoute({ conversationId: e, windowId: RK(n.window.id) }, t);
    }
    activateBrowserUseNavigationRestriction(e) {
      this.browserUseNavigationRestriction.activate(e);
    }
    addBrowserUseNavigationBlockedListener(e) {
      return (
        this.browserUseNavigationBlockedListeners.add(e),
        () => {
          this.browserUseNavigationBlockedListeners.delete(e);
        }
      );
    }
    assertBrowserUseNavigationAllowed(e, t) {
      this.browserUseNavigationRestriction.assertAllowed(e, t);
    }
    setAllowNonLocalBrowserUseNavigation(e) {
      this.allowNonLocalBrowserUseNavigation = e;
    }
    setTweaksFeatureEnabled(e) {
      if (this.isTweaksFeatureEnabled !== e) {
        this.isTweaksFeatureEnabled = e;
        for (let n of this.windows.values())
          for (let [r, i] of n.threads) {
            if (!e) {
              let e = t.ds(i.snapshot.comments, !1),
                a = {
                  ...i.snapshot,
                  annotationEditorMode: `comment`,
                  isDesignModifierPressed: !1,
                  isOriginalViewEnabled: !1,
                  isTweaksEditorOpen: !1,
                  comments: e,
                };
              (i.snapshot.annotationEditorMode !== a.annotationEditorMode ||
                i.snapshot.isDesignModifierPressed !== a.isDesignModifierPressed ||
                i.snapshot.isOriginalViewEnabled !== a.isOriginalViewEnabled ||
                i.snapshot.isTweaksEditorOpen !== a.isTweaksEditorOpen ||
                i.snapshot.comments.some((e) => e.designChange != null)) &&
                ((i.snapshot = a), this.syncThreadState(n, r, i));
            }
            i.page != null && this.syncRuntimeStateToPage(i.page, i);
          }
      }
    }
    notifyBrowserUseNavigationBlocked(e) {
      for (let t of this.browserUseNavigationBlockedListeners) t(e);
    }
    handleCommentOverlayMounted(e, t, n, r, i, a) {
      this.comments.handleOverlayMounted(e, t, n, r, i, a);
    }
    handleCommentOverlayPreviewOpenChanged(e, t) {
      this.comments.handleOverlayPreviewOpenChanged(e, t);
    }
    handleRuntimeCommentScreenshotReady(e, t) {
      this.comments.handleRuntimeCommentScreenshotReady(e, t);
    }
    goBack(e, t, n) {
      this.navigateCurrentPageHistory(e, t, `back`, n);
    }
    goForward(e, t, n) {
      this.navigateCurrentPageHistory(e, t, `forward`, n);
    }
    getActiveVisiblePageForWindow(e) {
      if (e.isDestroyed()) return null;
      let t = this.windows.get(e.id) ?? null,
        n = t?.activeConversationId ?? null,
        r = t?.activeBrowserTabId ?? null;
      if (t == null || n == null || r == null) return null;
      let i = this.getThreadStateForWindow(t, n, r),
        a = i?.page ?? null;
      return i == null || !i.visible || i.bounds == null || a?.view.webContents == null
        ? null
        : { conversationId: n, page: a, threadState: i, windowState: t };
    }
    getVisiblePageForWebContents(e, t) {
      let n = this.findPageStateForWebContentsId(t.id);
      return n == null ||
        n.windowState.window !== e ||
        !n.threadState.visible ||
        n.threadState.bounds == null
        ? null
        : n;
    }
    getReloadTargetForWindow(e, t) {
      return (
        (t == null ? null : this.getVisiblePageForWebContents(e, t)) ??
        this.getActiveVisiblePageForWindow(e)
      );
    }
    canReloadActiveVisiblePage(e, t = null) {
      return this.getReloadTargetForWindow(e, t) != null;
    }
    getFocusedVisibleBrowserTabPanel(e, t) {
      if (t == null) return null;
      let n = this.getVisiblePageForWebContents(e, t);
      return n == null
        ? null
        : aF(
            this.windowManager.getAppShellShortcutState(n.windowState.owner.id),
            n.conversationId,
            n.threadState.browserTabId,
            oF(n.threadState.hostKind),
          );
    }
    closeFocusedVisibleBrowserTab(e, t) {
      if (t == null) return !1;
      let n = this.getVisiblePageForWebContents(e, t);
      if (n == null) return !1;
      let r = iF(
        this.windowManager.getAppShellShortcutState(n.windowState.owner.id),
        n.conversationId,
        n.threadState.browserTabId,
        oF(n.threadState.hostKind),
      );
      return r == null
        ? !1
        : (this.windowManager.sendMessageToWebContents(n.windowState.owner, r), !0);
    }
    reloadActiveVisiblePage(e, t = null) {
      this.reloadActiveVisiblePageWithOptions(e, {}, t);
    }
    reloadActiveVisiblePageWithOptions(e, t = {}, n = null) {
      let r = this.getReloadTargetForWindow(e, n);
      r != null &&
        this.reload(r.windowState.owner, r.conversationId, t, r.threadState.browserTabId);
    }
    reloadOpenPages() {
      for (let e of this.windows.values())
        for (let t of e.threads.values())
          t.page?.view.webContents != null &&
            this.reload(e.owner, t.conversationId, void 0, t.browserTabId);
    }
    scroll(e, n, r, i) {
      let a = this.getPageState(e, n, i)?.view.webContents ?? null;
      a != null &&
        jJ(a, r).catch((e) => {
          (NJ().warning(`failed to scroll browser sidebar page`, {
            safe: { conversationId: n },
            sensitive: { error: e },
          }),
            this.errorReporter.reportNonFatal(e instanceof Error ? e : t.Oo(e), {
              kind: `browser-sidebar-scroll`,
            }));
        });
    }
    openFind(e, t, n) {
      let r = this.ensureThreadCommandContext(e, t, n);
      r != null && AV(this.windowManager, r.windowState.owner, t, r.threadState);
    }
    setFindQuery(e, t, n, r) {
      let i = this.ensureThreadCommandContext(e, t, r);
      i != null && jV(this.windowManager, i.windowState.owner, t, i.threadState, n);
    }
    findNext(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && MV(r);
    }
    findPrevious(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && NV(r);
    }
    closeFind(e, t, n) {
      let r = this.getCurrentWindowState(e);
      if (r == null) return;
      let i = this.getThreadStateForWindow(r, t, n);
      i != null && IV(this.windowManager, r.owner, t, i);
    }
    reload(e, t, { ignoreCache: n = !1 } = {}, r) {
      let i = this.getCurrentPageCommandContext(e, t, r);
      if (i == null) return;
      let { page: a, threadState: o, windowState: s } = i;
      if ((IV(this.windowManager, s.owner, t, o), a.loadError != null)) {
        this.navigatePageToUrl(a, a.loadError.failedUrl);
        return;
      }
      if (a.url.length === 0 && a.pendingUrl.length > 0) {
        if ((this.navigatePageToUrl(a, a.pendingUrl), a.view.webContents == null)) {
          let n = this.getThreadState(e, t, r);
          n != null && this.syncThreadStateFromOrigin(e, t, n);
        }
        return;
      }
      if ((ML(a), n)) {
        a.view.webContents?.reloadIgnoringCache();
        return;
      }
      a.view.webContents?.reload();
    }
    reset(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      if (r == null) return;
      let i = this.ensureCurrentWindowState(e);
      if (i == null) return;
      (this.comments.close(e, t, r.browserTabId),
        (r.pendingBrowserUsePanelOpen = !1),
        (r.pendingCommentModeActivation = null),
        (r.commentModeBlockCheckUrl = null),
        (r.commentModeBlockCheckRequestId += 1),
        IV(this.windowManager, i.owner, t, r));
      let a = this.isBrowserUseManagedBrowserTab(t, r.browserTabId);
      (r.page?.dispose(),
        this.clearPendingWebviewRoutes(i, r),
        (r.page = null),
        (r.isBrowserUsePage = !1),
        (r.browserUseTabOrigin = null),
        (r.isBrowserUseNavigationRestrictionActive = !1),
        (r.snapshot = lq()),
        a &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: RK(i.window.id) },
            !1,
            r.browserTabId,
          ),
        this.syncThreadState(i, t, r));
    }
    stop(e, t, n) {
      let r = this.getPageState(e, t, n);
      r && r.view.webContents?.stop();
    }
    suspend(e, n, r, i) {
      let a = this.getCurrentPageCommandContext(e, n, i);
      if (a == null) return !1;
      let { page: o, threadState: s } = a,
        c = o.view.webContents;
      if (c == null || c.isDestroyed() || s.isBrowserUseActive) return !1;
      let l = wL(o);
      return (
        (o.serializedPageState =
          (o.pendingUrl.length > 0 && t.uo(o.pendingUrl) !== t.uo(xL(c.getURL()))
            ? `pending-url`
            : `navigation-history`) == `pending-url`
            ? TL({ faviconUrl: o.faviconUrl, title: o.title || o.fallbackTitle, url: l })
            : EL({
                faviconUrl: o.faviconUrl,
                title: o.title || o.fallbackTitle,
                url: l,
                webContents: c,
              })),
        (o.pendingSuspensionId = r),
        !0
      );
    }
    browserSidebarWebviewDestroyed(e, t, n, r) {
      let i = this.getCurrentWindowState(e);
      if (i == null) return;
      let a = this.getThreadStateForWindow(i, t, n) ?? this.findThreadStateForPendingTeardown(i, r);
      a != null && this.finalizeThreadTeardown(i, a.conversationId, a, r);
    }
    attachWebview(e, t) {
      for (let n of this.windows.values()) {
        let r = n.pendingWebviewRoutesByAttachToken.get(t) ?? null;
        if (r == null) continue;
        let { mountGeneration: i, route: a } = r;
        ((n.pendingWebviewAttachTokens = n.pendingWebviewAttachTokens.filter((e) => e !== t)),
          n.pendingWebviewRoutesByAttachToken.delete(t));
        let o = n.threads.get(yL(a)) ?? null;
        if (o?.page == null) {
          (this.browserSessionRegistry.recordDebugEvent({
            browserTabId: a.browserTabId,
            conversationId: a.conversationId,
            details: [{ label: `attachToken`, value: t }],
            guestWebContentsId: e.id,
            kind: `browser-runtime-attach-missing-page`,
            message: `Runtime attach had no Browser page`,
            ownerWebContentsId: n.owner.id,
            pageKey: null,
            windowId: n.window.id,
          }),
            NJ().warning(`IAB_LIFECYCLE runtime attach had no page`, {
              safe: {
                browserTabId: a.browserTabId,
                conversationId: a.conversationId,
                guestWebContentsId: e.id,
                ownerWebContentsId: n.owner.id,
                windowId: n.window.id,
              },
              sensitive: {},
            }),
            e.isDestroyed() || e.close());
          return;
        }
        if (o.mountGeneration !== i) {
          (this.browserSessionRegistry.recordDebugEvent({
            browserTabId: a.browserTabId,
            conversationId: a.conversationId,
            details: [
              { label: `attachToken`, value: t },
              { label: `mountGeneration`, value: String(i) },
              { label: `threadMountGeneration`, value: String(o.mountGeneration) },
            ],
            guestWebContentsId: e.id,
            kind: `browser-runtime-attach-stale`,
            message: `Rejected stale Browser sidebar attach`,
            ownerWebContentsId: n.owner.id,
            pageKey: null,
            windowId: n.window.id,
          }),
            NJ().warning(`IAB_LIFECYCLE rejected stale browser sidebar attach`, {
              safe: {
                browserTabId: a.browserTabId,
                conversationId: a.conversationId,
                guestWebContentsId: e.id,
                mountGeneration: i,
                ownerWebContentsId: n.owner.id,
                threadMountGeneration: o.mountGeneration,
                windowId: n.window.id,
              },
              sensitive: {},
            }),
            e.isDestroyed() || e.close());
          return;
        }
        let s = this.findPageStateForWebContentsId(e.id);
        if (
          s != null &&
          (s.conversationId !== a.conversationId || s.threadState.browserTabId !== a.browserTabId)
        ) {
          (this.browserSessionRegistry.recordDebugEvent({
            browserTabId: a.browserTabId,
            conversationId: a.conversationId,
            details: [
              { label: `attachToken`, value: t },
              { label: `previousBrowserTabId`, value: s.threadState.browserTabId },
              { label: `previousConversationId`, value: s.conversationId },
            ],
            guestWebContentsId: e.id,
            kind: `browser-runtime-attach-route-rebind-rejected`,
            message: `Rejected Browser sidebar route rebind`,
            ownerWebContentsId: n.owner.id,
            pageKey: null,
            windowId: n.window.id,
          }),
            NJ().warning(`IAB_LIFECYCLE rejected browser sidebar route rebind`, {
              safe: {
                browserTabId: a.browserTabId,
                conversationId: a.conversationId,
                guestWebContentsId: e.id,
                ownerWebContentsId: n.owner.id,
                previousBrowserTabId: s.threadState.browserTabId,
                previousConversationId: s.conversationId,
                windowId: n.window.id,
              },
              sensitive: {},
            }),
            e.isDestroyed() || e.close());
          return;
        }
        (this.browserSessionRegistry.recordDebugEvent({
          browserTabId: a.browserTabId,
          conversationId: a.conversationId,
          details: [{ label: `attachToken`, value: t }],
          guestWebContentsId: e.id,
          kind: `browser-runtime-attached`,
          message: `Runtime attached Browser sidebar webview`,
          ownerWebContentsId: n.owner.id,
          pageKey: vL(n.owner.id, a.conversationId, a.browserTabId),
          windowId: n.window.id,
        }),
          NJ().info(`IAB_LIFECYCLE runtime attached browser sidebar webview`, {
            safe: {
              browserTabId: a.browserTabId,
              conversationId: a.conversationId,
              guestWebContentsId: e.id,
              ownerWebContentsId: n.owner.id,
              windowId: n.window.id,
            },
            sensitive: {},
          }),
          this.attachPageWebContents(n, o.page, e, o.themeVariant),
          r.adoptedInitialUrl != null &&
            this.navigateBlankAdoptedWindowOpenChildAfterAttach(o.page, e, r.adoptedInitialUrl));
        return;
      }
      this.findPageStateForWebContentsId(e.id) ??
        (this.browserSessionRegistry.recordDebugEvent({
          browserTabId: null,
          conversationId: null,
          guestWebContentsId: e.id,
          kind: `browser-runtime-attach-no-route`,
          message: `Runtime attach had no pending route`,
          ownerWebContentsId: null,
          pageKey: null,
          windowId: null,
        }),
        NJ().warning(`IAB_LIFECYCLE runtime attach had no pending route`, {
          safe: { guestWebContentsId: e.id },
          sensitive: {},
        }),
        e.isDestroyed() || e.close());
    }
    setZoomPercent(e, n, r, i) {
      let a = this.ensureThreadStateForCommand(e, n, i);
      a != null && this.setThreadZoomPercent(e, n, a, t.Wa(r));
    }
    stepZoom(e, n, r, i) {
      let a = this.ensureThreadStateForCommand(e, n, i);
      a != null && this.setThreadZoomPercent(e, n, a, t.Ka(a.snapshot.zoomPercent, r));
    }
    resetZoom(e, t, n) {
      let r = this.ensureThreadStateForCommand(e, t, n);
      r != null && this.setThreadZoomPercent(e, t, r, 100);
    }
    navigate(e, t, n, r, i, a, o) {
      let s = this.ensureThreadCommandContext(e, t, a);
      if (s == null) return;
      let { threadState: c, windowState: l } = s;
      r === `browser_use` && ((c.isBrowserUsePage = !0), (c.browserUseTabOrigin ??= `agent`));
      let u =
        c.page ??
        this.ensureThreadPageForCommand(e, t, a, { title: c.snapshot.title, url: c.snapshot.url });
      u &&
        (IV(this.windowManager, l.owner, t, c),
        dL(wL(u), n) || this.comments.dismiss(e, t, c.browserTabId, c),
        this.navigatePageToUrl(u, n, r, i, o),
        u.view.webContents ?? this.syncThreadStateFromOrigin(e, t, c));
    }
    hideLocalServer(e) {
      this.emptyPageLocalServers.hideServer(e);
    }
    unhideLocalServer(e) {
      this.emptyPageLocalServers.unhideServer(e);
    }
    removeLocalServerRoute(e, t) {
      this.emptyPageLocalServers.removeRoute(e, t);
    }
    notifyBrowserSidebarNavigationUsage(e, t, n, r, i) {
      let a = this.getPreferredWindowStateForConversation(e);
      a == null ||
        a.owner.isDestroyed() ||
        this.windowManager.sendMessageToWebContents(a.owner, {
          type: `browser-sidebar-usage`,
          conversationId: e,
          browserTabId: t,
          kind: `navigated`,
          source: n,
          initiator: r,
          urlKind: i,
        });
    }
    trackPendingBrowserUseNavigation(e, n) {
      let r = this.findPageStateForPageKey(e);
      r != null &&
        (r.page.pendingNavigationUsage = {
          expectedUrl: t.uo(n),
          initiator: `browser_use`,
          source: `browser_use`,
          urlKind: CL(n),
        });
    }
    clearPendingBrowserUseNavigation(e) {
      let t = this.findPageStateForPageKey(e);
      t != null &&
        t.page.pendingNavigationUsage?.source === `browser_use` &&
        (t.page.pendingNavigationUsage = null);
    }
    async captureScreenshotToClipboard(e, t, n) {
      this.getThreadState(e, t, n)?.isAnnotationModeEnabled !== !1 &&
        (await AJ({
          browserTabId: n,
          conversationId: t,
          errorReporter: this.errorReporter,
          origin: e,
          pageState: this.getPageState(e, t, n),
          windowManager: this.windowManager,
        }));
    }
    refreshCursor(e, t, n) {
      let r = this.getCurrentPageCommandContext(e, t, n);
      r != null && PL(r.windowState.window, r.threadState.bounds, r.page.view.webContents);
    }
    setThreadZoomPercent(e, t, n, r) {
      let i = n.snapshot.zoomPercent !== r;
      if (
        (i && (n.snapshot = { ...n.snapshot, zoomPercent: r }),
        n.page != null &&
          (this.syncPageZoomPercent(n.page, r), this.syncRuntimeStateToPage(n.page, n)),
        !i)
      )
        return;
      let o = a.BrowserWindow.fromWebContents(e);
      if (o == null || o.isDestroyed()) return;
      let s = this.ensureWindowState(o, e);
      this.syncThreadState(s, t, n);
    }
    syncPageZoomPercent(e, n) {
      let r = e.view.webContents;
      r == null || r.isDestroyed() || r.setZoomFactor(t.Ga(n));
    }
    queuePageDeviceMetricsSync(e, t) {
      e.deviceMetricsSyncVersion += 1;
      let n = e.deviceMetricsSyncVersion;
      this.queuePageDebuggerSync(e, async () => {
        n === e.deviceMetricsSyncVersion && (await this.syncPageDeviceMetrics(e, t));
      });
    }
    queuePageDebuggerSync(e, t) {
      return ((e.debuggerSync = e.debuggerSync.catch(() => void 0).then(t)), e.debuggerSync);
    }
    queuePageColorSchemeSync(e, t, n) {
      return this.queuePageDebuggerSync(e, async () => {
        await _L(t, n);
      });
    }
    async syncPageDeviceMetrics(e, t) {
      let n = e.view.webContents;
      if (n == null || n.isDestroyed()) return;
      let r = n.debugger,
        i = !1;
      try {
        if ((r.isAttached() || (r.attach(`1.3`), (i = !0)), t == null)) {
          await r.sendCommand(`Emulation.clearDeviceMetricsOverride`);
          return;
        }
        await r.sendCommand(`Emulation.setDeviceMetricsOverride`, {
          deviceScaleFactor: 1,
          height: Math.round(t.height),
          mobile: !1,
          width: Math.round(t.width),
        });
      } catch (t) {
        NJ().warning(`failed to sync browser sidebar device metrics`, {
          safe: { conversationId: e.conversationId },
          sensitive: { error: t },
        });
      } finally {
        i && !n.isDestroyed() && r.isAttached() && r.detach();
      }
    }
    setInteractionMode(e, t, n, r) {
      let i = this.ensureThreadCommandContext(e, t, r);
      if (i == null) return;
      let { threadState: a, windowState: o } = i;
      if (!(n === `comment` && !a.isAnnotationModeEnabled)) {
        if (n !== `comment`) {
          if (
            a.snapshot.interactionMode === `comment` &&
            this.isTweaksFeatureEnabled &&
            yF(a.snapshot)
          ) {
            this.showTweaksExitBlockedToast(o.owner, t, a.browserTabId);
            return;
          }
          if (a.snapshot.interactionMode === n && a.pendingCommentModeActivation == null) return;
          ((a.pendingCommentModeActivation = null),
            (a.snapshot = xF(a.snapshot, n)),
            this.comments.dismiss(e, t, a.browserTabId, a),
            this.syncThreadState(o, t, a),
            a.page != null && this.syncRuntimeStateToPage(a.page, a),
            this.comments.prepare(e, t, a.browserTabId, a));
          return;
        }
        this.requestCommentMode(o, t, a);
      }
    }
    startCommentAtPoint(e, n, r) {
      let i = this.getThreadState(e, n.conversationId, n.browserTabId);
      if (i == null) return;
      let a = this.ensureCurrentWindowState(e);
      if (a == null) return;
      let o =
        this.isTweaksFeatureEnabled &&
        i.snapshot.annotationEditorMode === `design` &&
        t.ps(i.snapshot.comments);
      this.requestCommentMode(a, n.conversationId, i, {
        kind: o ? `design` : `comment`,
        viewportPoint: r,
      });
    }
    startDesignEditAtPoint(e, t, n) {
      if (!this.isTweaksFeatureEnabled) return;
      let r = this.getThreadState(e, t.conversationId, t.browserTabId);
      if (r == null) return;
      let i = this.ensureCurrentWindowState(e);
      i != null &&
        this.requestCommentMode(i, t.conversationId, r, { kind: `design`, viewportPoint: n });
    }
    requestCommentMode(e, t, n, r) {
      if (!n.isAnnotationModeEnabled) return;
      let i = NL(n);
      if (i.length === 0) return;
      let a = this.commentModeBlocklistLookup.cachedCommentModeBlocked(i);
      if (a === !0) {
        this.refreshCommentModeBlockStatus(e, n);
        return;
      }
      if (a === !1) {
        ((n.pendingCommentModeActivation = null),
          n.snapshot.interactionMode !== `comment` &&
            ((n.snapshot = {
              ...xF(n.snapshot, `comment`),
              isTweaksEditorOpen: bF(n.snapshot, this.isTweaksFeatureEnabled),
            }),
            this.syncThreadState(e, t, n),
            n.page != null && this.syncRuntimeStateToPage(n.page, n),
            this.comments.prepare(e.owner, t, n.browserTabId, n)),
          r != null && n.page != null && this.sendPendingCommentModeRequestToRuntime(n.page, r));
        return;
      }
      ((n.pendingCommentModeActivation = {
        url: i,
        navigationVersion: n.page?.navigationVersion ?? null,
        request: r ?? null,
      }),
        this.refreshCommentModeBlockStatus(e, n));
    }
    clearComments(e, t, n) {
      this.clearAnnotations(e, t, n, !0);
    }
    discardPendingAnnotations(e, t, n) {
      this.clearAnnotations(e, t, n, !1);
    }
    clearAnnotations(e, t, n, r) {
      let i = this.ensureThreadStateForCommand(e, t, n);
      i != null &&
        ((i.snapshot = xF(i.snapshot, r ? `browse` : void 0, [])),
        r && this.comments.dismiss(e, t, i.browserTabId, i),
        this.syncThreadStateFromOrigin(e, t, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i));
    }
    addAnnotationsToComposer(e, t, n) {
      let r = this.ensureThreadCommandContext(e, t, n);
      if (r == null) return;
      let { threadState: i, windowState: a } = r;
      ((i.pendingCommentModeActivation = null),
        (i.snapshot = xF(i.snapshot, `browse`)),
        this.comments.dismiss(e, t, i.browserTabId, i),
        this.syncThreadState(a, t, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i),
        this.comments.prepare(e, t, i.browserTabId, i));
    }
    closePage(e, t, n) {
      let r = this.getCurrentWindowState(e);
      if (r == null) return;
      let i = this.getThreadStateForWindow(r, t, n);
      i != null &&
        (IV(this.windowManager, r.owner, t, i),
        r.activeConversationId === t &&
          r.activeBrowserTabId === i.browserTabId &&
          (r.activeBrowserTabId = null),
        i.isBrowserUseActive &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: RK(r.window.id) },
            !1,
            i.browserTabId,
          ),
        this.requestThreadTeardown(r, t, i, `closed`));
    }
    transferConversation(e, t, n, r, i) {
      let a = this.ensureCurrentWindowState(e);
      a != null && this.transferConversationState(a, t, r, n, i);
    }
    transferConversationState(e, t, n, r, i) {
      WK({
        browserSessionRegistry: this.browserSessionRegistry,
        comments: this.comments,
        disposeThreadState: this.disposeThreadState.bind(this),
        getBrowserUseRouteBrowserTabIds: this.getBrowserUseRouteBrowserTabIds.bind(this),
        isBrowserUseManagedBrowserTab: this.isBrowserUseManagedBrowserTab.bind(this),
        getThreadStateForWindow: this.getThreadStateForWindow.bind(this),
        setBrowserUseActiveForRoute: this.setBrowserUseActiveForRoute.bind(this),
        sourceBrowserTabId: n,
        sourceConversationId: t,
        syncRuntimeStateToPage: this.syncRuntimeStateToPage.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        targetBrowserTabId: i,
        targetConversationId: r,
        transferPendingWebviewRoutes: this.transferPendingWebviewRoutes.bind(this),
        windowState: e,
      });
    }
    selectComment(e, t, n, r) {
      let i = this.getThreadState(e, t, r),
        a = this.getPageState(e, t, r),
        o = a?.view.webContents ?? null;
      i == null ||
        a == null ||
        o == null ||
        o.isDestroyed() ||
        (this.syncRuntimeStateToPage(a, i),
        o.send(R, { type: `browser-sidebar-runtime-select-comment`, commentId: n }));
    }
    async handleRuntimeOpenEditor(e, t) {
      await this.comments.handleRuntimeOpenEditor(e, t);
    }
    async handleRuntimeOpenDesignEditor(e, t) {
      this.isTweaksFeatureEnabled && (await this.comments.handleRuntimeOpenDesignEditor(e, t));
    }
    async handleRuntimeOpenCommentPreview(e, t) {
      await this.comments.handleRuntimeOpenCommentPreview(e, t);
    }
    handleRuntimeCloseCommentPreview(e, t) {
      this.comments.handleRuntimeCloseCommentPreview(e, t);
    }
    handleRuntimeUpdateAnchor(e, t) {
      this.comments.handleRuntimeUpdateAnchor(e, t);
    }
    handleRuntimeFocusEditor(e) {
      this.comments.handleRuntimeFocusEditor(e);
    }
    handleRuntimeExitCommentMode(e) {
      let t = this.findPageStateForRuntimeSender(e);
      !t ||
        t.owner.isDestroyed() ||
        this.addAnnotationsToComposer(t.owner, t.page.conversationId, t.page.browserTabId);
    }
    handleRuntimeDesignModifierState(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      !n ||
        n.owner.isDestroyed() ||
        this.setDesignModifierPressed(n.owner, n.page.conversationId, t.pressed);
    }
    setDesignModifierPressed(e, t, n) {
      let r = this.getThreadState(e, t),
        i = this.getCurrentWindowState(e);
      if (r == null || i == null) return;
      let a = this.isTweaksFeatureEnabled && r.snapshot.interactionMode === `comment` && n;
      (r.snapshot.isDesignModifierPressed ?? !1) !== a &&
        ((r.snapshot = { ...r.snapshot, isDesignModifierPressed: a }),
        this.syncThreadState(i, t, r),
        r.page != null && this.syncRuntimeStateToPage(r.page, r));
    }
    setAnnotationAddModifierPressed(e, t, n) {
      let r = this.getThreadState(e, t),
        i = this.getCurrentWindowState(e);
      if (r == null || i == null) return;
      let a = r.snapshot.interactionMode === `comment` && n;
      (r.snapshot.isAnnotationAddModifierPressed ?? !1) !== a &&
        ((r.snapshot = { ...r.snapshot, isAnnotationAddModifierPressed: a }),
        this.syncThreadState(i, t, r));
    }
    setOriginalViewEnabled(e, n, r) {
      let i = this.getThreadState(e, n),
        a = this.getCurrentWindowState(e);
      if (i == null || a == null) return;
      let o = i.snapshot.interactionMode === `comment` && t.ms(i.snapshot.comments) && r;
      (i.snapshot.isOriginalViewEnabled ?? !1) !== o &&
        ((i.snapshot = { ...i.snapshot, isOriginalViewEnabled: o }),
        this.syncThreadState(a, n, i),
        i.page != null && this.syncRuntimeStateToPage(i.page, i));
    }
    handleRuntimeImageDragStarted(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      n == null ||
        n.owner.isDestroyed() ||
        (this.activeDraggedImagesByOwnerWebContentsId.set(n.owner.id, {
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          sourceUrl: t.sourceUrl,
        }),
        this.windowManager.sendMessageToWebContents(n.owner, {
          type: `browser-sidebar-image-drag-state`,
          browserTabId: n.page.browserTabId,
          conversationId: n.page.conversationId,
          isActive: !0,
        }));
    }
    handleRuntimeImageDragEnded(e) {
      let t = this.findPageStateForRuntimeSender(e);
      t == null ||
        t.owner.isDestroyed() ||
        mF(
          this.activeDraggedImagesByOwnerWebContentsId,
          t.page.browserTabId,
          t.page.conversationId,
          t.owner,
          this.windowManager,
        );
    }
    async attachDraggedImageToThread(e, t, n) {
      let r = this.activeDraggedImagesByOwnerWebContentsId.get(e.id) ?? null;
      if (r?.conversationId !== t || r.browserTabId !== n) return;
      let i = this.getPageState(e, t, n),
        a = i?.view.webContents ?? null;
      i == null ||
        a == null ||
        a.isDestroyed() ||
        (mF(this.activeDraggedImagesByOwnerWebContentsId, n, t, e, this.windowManager),
        await fF({
          browserTabId: n,
          browserPageUrl: i.url,
          conversationId: i.conversationId,
          owner: e,
          sourceUrl: r.sourceUrl,
          webContents: a,
          windowManager: this.windowManager,
        }));
    }
    handleCommentOverlaySubmit(e, t) {
      return this.comments.handleOverlaySubmit(e, t);
    }
    handleDesignOverlayUpdate(e, t) {
      this.comments.handleDesignOverlayUpdate(e, t);
    }
    async handleDesignOverlayAdd(e, t) {
      await this.comments.handleDesignOverlayAdd(e, t);
    }
    handleDesignOverlayDelete(e, t) {
      this.comments.handleDesignOverlayDelete(e, t);
    }
    handleCommentOverlayDelete(e, t) {
      this.comments.handleOverlayDelete(e, t);
    }
    handleCommentOverlayClose(e, t, n, r) {
      this.comments.handleOverlayClose(e, t, n, r);
    }
    handleCommentOverlayTweaksOpenChanged(e, t) {
      this.comments.handleOverlayTweaksOpenChanged(e, t);
    }
    handleCommentOverlayDesignScrubChanged(e, t) {
      this.isTweaksFeatureEnabled && this.comments.handleOverlayDesignScrubChanged(e, t);
    }
    ensureWindowState(e, t) {
      let n = e.id,
        r = this.windows.get(n);
      if (r)
        return (
          (r.owner = t),
          this.browserSessionRegistry.registerWindow({ ownerWebContentsId: t.id, windowId: n }),
          r
        );
      let i = {
        owner: t,
        window: e,
        threads: new Map(),
        activeConversationId: null,
        activeBrowserTabId: null,
        ignoredConversationIds: new Set(),
        pendingRetainedWebviewRoutesByInstanceId: new Map(),
        browserSettingsWebviewMountIds: new Set(),
        browserSettingsWebviewThemeVariant: `light`,
        pendingBrowserSettingsWebviewAllowedNavigationUrlsByInstanceId: new Map(),
        browserSettingsWebviewAllowedNavigationUrlsByWebContentsId: new Map(),
        browserSettingsWebContentsByWebContentsId: new Map(),
        browserSettingsWebviewNavigationDisposersByWebContentsId: new Map(),
        pendingAdoptedWebviewRoutesByWebContentsId: new Map(),
        canceledAdoptedWebContentsIds: new Set(),
        pendingWebviewAttachTokens: [],
        pendingWebviewRoutesByAttachToken: new Map(),
        transferredConversationStateKeys: new Set(),
        disposeWebviewListeners: () => void 0,
      };
      return (
        (i.disposeWebviewListeners = Jq({
          attachRetainedWebview: (e, r) => {
            let a = i.threads.get(yL(r)) ?? null;
            if (a?.page == null) {
              (NJ().warning(`IAB_LIFECYCLE retained browser sidebar attach had no page`, {
                safe: {
                  browserTabId: r.browserTabId,
                  conversationId: r.conversationId,
                  guestWebContentsId: e.id,
                  ownerWebContentsId: t.id,
                  windowId: n,
                },
                sensitive: {},
              }),
                e.isDestroyed() || e.close());
              return;
            }
            this.attachPageWebContents(i, a.page, e, a.themeVariant);
          },
          attachWebview: (e, t) => {
            this.attachWebview(e, t);
          },
          configureBrowserSession: (e) => this.configureBrowserSession(e),
          createThreadPageState: (e, t, n) => this.createThreadPageState(i, e, t, n),
          ensureThreadStateForWindow: (e, t) => this.ensureThreadStateForWindow(i, e, t),
          openBrowserSettingsUrlInTab: (e) =>
            i.activeConversationId == null
              ? !1
              : (this.windowManager.sendMessageToWebContents(t, {
                  type: `open-browser-tab`,
                  conversationId: i.activeConversationId,
                  initialUrl: e,
                  source: `manual`,
                  initiator: `window_open`,
                }),
                !0),
          owner: t,
          preloadPath: PJ,
          recordDebugEvent: (e) => {
            this.browserSessionRegistry.recordDebugEvent(e);
          },
          state: i,
          windowId: n,
        })),
        this.windows.set(n, i),
        this.browserSessionRegistry.registerWindow({ ownerWebContentsId: t.id, windowId: n }),
        e.on(`closed`, () => {
          this.disposeWindowState(n);
        }),
        i
      );
    }
    ensureThreadState(e, t, n) {
      let r = yL({ browserTabId: n, conversationId: t.conversationId }),
        i = e.threads.get(r);
      if (i) return i;
      let a = {
        allowLocalServerPortScan: !1,
        browserTabId: n,
        conversationId: t.conversationId,
        findState: kV(),
        page: null,
        hostKind: t.hostKind ?? null,
        pendingBrowserUsePanelOpen: !1,
        hasPendingBrowserUseVisibilityRequest: !1,
        pendingCommentModeActivation: null,
        visible: !1,
        bounds: null,
        presented: !1,
        mountGeneration: 0,
        pendingTeardown: null,
        sessionState: `cold`,
        lastTabActivitySequence: 0,
        lastPresentedSequence: 0,
        lastLifecycleSequence: 0,
        emulatedViewportSize: t.emulatedViewportSize ?? null,
        viewportScale: 1,
        cwd: t.cwd ?? null,
        rolloutPath: t.rolloutPath ?? null,
        isAgentControllingBrowser: !1,
        isAnnotationModeEnabled: t.annotationModeEnabled ?? !0,
        isBrowserUseActive: !1,
        isBrowserUsePage: !1,
        browserUseTabOrigin: null,
        isBrowserUseNavigationRestrictionActive: !1,
        ownerRoutePath: null,
        commentModeBlockCheckRequestId: 0,
        commentModeBlockCheckUrl: null,
        themeVariant: `light`,
        runtimeIntlConfig: t.runtimeIntlConfig,
        snapshot: lq(),
      };
      return (
        this.tabBudget.markTabActivity(a),
        e.threads.set(r, a),
        Nq(this.browserSessionRegistry, this.windowManager, this.windows, e, a),
        a
      );
    }
    getPageLifecycleHelpers() {
      return {
        browserSessionRegistry: this.browserSessionRegistry,
        comments: this.comments,
        dismissCommentEditorForCommittedNavigation:
          this.dismissCommentEditorForCommittedNavigation.bind(this),
        emptyPageLocalServers: this.emptyPageLocalServers,
        enforceDetachedPageBudget: this.tabBudget.enforceDetachedPageBudget.bind(this.tabBudget),
        errorReporter: this.errorReporter,
        finalizeThreadTeardown: this.finalizeThreadTeardown.bind(this),
        finishPageAttach: this.finishPageAttach.bind(this),
        getThreadStateForWindow: this.getThreadStateForWindow.bind(this),
        goBack: this.goBack.bind(this),
        goForward: this.goForward.bind(this),
        isBrowserUseManagedBrowserTab: this.isBrowserUseManagedBrowserTab.bind(this),
        isRestrictedBrowserSidebarNavigation: this.isRestrictedBrowserSidebarNavigation.bind(this),
        loadBrowserSidebarErrorPage: this.loadBrowserSidebarErrorPage.bind(this),
        navigatePageToUrl: this.navigatePageToUrl.bind(this),
        notifyBrowserSidebarNavigationUsage: this.notifyBrowserSidebarNavigationUsage.bind(this),
        onBrowserSidebarStateChanged: this.onBrowserSidebarStateChanged,
        openBrowserSidebarExternalUrl: this.openBrowserSidebarExternalUrl.bind(this),
        openBrowserSidebarUrlExternally: this.openBrowserSidebarUrlExternally.bind(this),
        getOwnerCommandAccelerators: (e) => this.ownerCommandAccelerators[e] ?? [],
        queuePageDeviceMetricsSync: this.queuePageDeviceMetricsSync.bind(this),
        reload: (e, t, n, r) => {
          this.reload(e, t, n == null ? void 0 : { ignoreCache: n }, r);
        },
        setDesignModifierPressed: this.setDesignModifierPressed.bind(this),
        setInteractionMode: this.setInteractionMode.bind(this),
        showTweaksExitBlockedToast: this.showTweaksExitBlockedToast.bind(this),
        startCommentAtPoint: this.startCommentAtPoint.bind(this),
        startDesignEditAtPoint: this.startDesignEditAtPoint.bind(this),
        syncPageZoomPercent: this.syncPageZoomPercent.bind(this),
        syncRuntimeStateToPage: this.syncRuntimeStateToPage.bind(this),
        syncThreadState: this.syncThreadState.bind(this),
        updateThreadSessionState: this.updateThreadSessionState.bind(this),
        windowManager: this.windowManager,
      };
    }
    createPageState(e, t, n) {
      return hq(e, t, n);
    }
    isBrowserUseManagedBrowserTab(e, t) {
      if (t === OF(e)) return !0;
      for (let n of this.windows.values())
        if ((this.getThreadStateForWindow(n, e, t) ?? null)?.isBrowserUsePage === !0) return !0;
      return !1;
    }
    getBrowserUseRouteBrowserTabIds(e, t) {
      let n = [];
      for (let r of e.threads.values())
        r.conversationId === t &&
          (r.isBrowserUsePage || r.isBrowserUseActive) &&
          n.push(r.browserTabId);
      return n;
    }
    createThreadPageState(e, t, n, r) {
      return gq(this.getPageLifecycleHelpers(), e, t, n, r);
    }
    attachPageWebContents(e, t, n, r) {
      _q(this.getPageLifecycleHelpers(), e, t, n, r);
    }
    async finishPageAttach(e, t, n, r) {
      (await this.queuePageColorSchemeSync(t, n, r),
        !(t.view.webContents?.id !== n.id || n.isDestroyed()) &&
          this.resolveBrowserUseOpenRequests(e));
    }
    configureBrowserSession(e) {
      let t = cy(e),
        n = a.session.fromPartition(t);
      return this.configuredPartitions.has(t)
        ? n
        : (n.setPermissionRequestHandler((e, t, n) => {
            n(!1);
          }),
          n.setPermissionCheckHandler(() => !1),
          n.on(`will-download`, (e, t) => {
            (e.preventDefault(), t.cancel());
          }),
          n.webRequest.onBeforeRequest((e, t) => {
            let n =
                e.webContentsId == null
                  ? null
                  : this.findPageStateForWebContentsId(e.webContentsId),
              r =
                this.allowNonLocalBrowserUseNavigation &&
                n?.threadState.isAgentControllingBrowser === !0;
            t({
              cancel:
                (e.resourceType === `mainFrame` &&
                  rJ({
                    webContentsId: e.webContentsId,
                    windows: this.windows.values(),
                    url: e.url,
                  })) ||
                (!r &&
                  WF({
                    currentUrl: n == null ? null : n.page.url,
                    resourceType: e.resourceType,
                    requestUrl: e.url,
                  })) ||
                (PF(e.resourceType) &&
                  this.isRestrictedBrowserSidebarNavigation(e.webContentsId, e.url)),
            });
          }),
          this.configuredPartitions.add(t),
          n);
    }
    isRestrictedBrowserSidebarNavigation(e, t) {
      let n = e == null ? null : this.findPageStateForWebContentsId(e);
      return (
        !(n != null && yV(n.page.loadError, vF(t))) &&
        this.browserUseNavigationRestriction.isRestrictedForWebContentsId(e, t)
      );
    }
    navigatePageToUrl(e, t, n, r, i) {
      vq(this.getPageLifecycleHelpers(), e, t, n, r, i);
    }
    navigateBlankAdoptedWindowOpenChildAfterAttach(e, t, n) {
      if (LJ(n)) return;
      let r = !1,
        i = () => {
          setTimeout(o, 0);
        },
        a = () => {
          if (r) return;
          r = !0;
          function e() {
            ((r = !1),
              t.removeListener(`did-stop-loading`, n),
              t.removeListener(`did-fail-load`, n),
              t.removeListener(`destroyed`, e));
          }
          function n() {
            (e(), i());
          }
          (t.on(`did-stop-loading`, n), t.on(`did-fail-load`, n), t.on(`destroyed`, e));
        },
        o = () => {
          if (t.isDestroyed() || e.view.webContents?.id !== t.id || !LJ(t.getURL())) {
            NJ().info(`IAB_ADOPTION skipped adopted fallback load`, {
              safe: {
                browserTabId: e.browserTabId,
                conversationId: e.conversationId,
                guestWebContentsId: t.id,
                isDestroyed: t.isDestroyed(),
                isStillAttached: e.view.webContents?.id === t.id,
                wasBlank: !t.isDestroyed() && LJ(t.getURL()),
                hasInitialUrl: n.length > 0,
              },
              sensitive: {},
            });
            return;
          }
          if (t.isLoading()) {
            (NJ().info(`IAB_ADOPTION waiting for adopted child load`, {
              safe: {
                browserTabId: e.browserTabId,
                conversationId: e.conversationId,
                guestWebContentsId: t.id,
                hasInitialUrl: n.length > 0,
              },
              sensitive: {},
            }),
              a());
            return;
          }
          (NJ().info(`IAB_ADOPTION loading blank adopted child target`, {
            safe: {
              browserTabId: e.browserTabId,
              conversationId: e.conversationId,
              guestWebContentsId: t.id,
              hasInitialUrl: n.length > 0,
            },
            sensitive: {},
          }),
            this.navigatePageToUrl(e, n, `manual`, `window_open`));
        };
      if (t.isLoading()) {
        (NJ().info(`IAB_ADOPTION waiting for adopted child initial load`, {
          safe: {
            browserTabId: e.browserTabId,
            conversationId: e.conversationId,
            guestWebContentsId: t.id,
            hasInitialUrl: n.length > 0,
          },
          sensitive: {},
        }),
          a());
        return;
      }
      i();
    }
    syncThreadState(e, t, n) {
      (this.refreshCommentModeBlockStatus(e, n), yq(this.getPageLifecycleHelpers(), e, t, n));
    }
    syncThreadStateFromOrigin(e, t, n) {
      if (n.page == null) return;
      let r = this.getCurrentWindowState(e);
      r != null && this.syncThreadState(r, t, n);
    }
    dismissCommentEditor(e, t, n) {
      let r = this.getThreadState(e, t, n);
      r != null && this.comments.dismiss(e, t, n, r);
    }
    dismissCommentEditorForCommittedNavigation(e, t, n) {
      let r = this.getThreadStateForWindow(e, t.conversationId, t.browserTabId);
      r == null ||
        r.snapshot.interactionMode !== `comment` ||
        dL(t.url, n) ||
        this.comments.dismiss(e.owner, t.conversationId, t.browserTabId, r);
    }
    syncRuntimeStateToPage(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        this.comments.syncRuntimeStateToPage(
          this.findPageStateForWebContentsId(n.id)?.windowState.owner ?? n,
          e.conversationId,
          n,
          t,
        );
    }
    showTweaksExitBlockedToast(e, t, n) {
      e.isDestroyed() ||
        this.windowManager.sendMessageToWebContents(e, {
          type: `browser-sidebar-runtime-show-toast`,
          browserTabId: n,
          conversationId: t,
          toast: `tweaks-exit-blocked`,
        });
    }
    sendRuntimeCreateCommentAtPoint(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        n.send(R, { type: `browser-sidebar-runtime-create-comment-at-point`, viewportPoint: t });
    }
    sendRuntimeOpenDesignEditorAtPoint(e, t) {
      let n = e.view.webContents;
      n == null ||
        n.isDestroyed() ||
        n.send(R, {
          type: `browser-sidebar-runtime-open-design-editor-at-point`,
          viewportPoint: t,
        });
    }
    sendPendingCommentModeRequestToRuntime(e, t) {
      switch (t.kind) {
        case `comment`:
          this.sendRuntimeCreateCommentAtPoint(e, t.viewportPoint);
          return;
        case `design`:
          this.sendRuntimeOpenDesignEditorAtPoint(e, t.viewportPoint);
          return;
      }
    }
    syncCommentSnapshot(e, t, n) {
      (this.syncThreadStateFromOrigin(e, t, n),
        n.page != null && this.syncRuntimeStateToPage(n.page, n));
    }
    loadBrowserSidebarErrorPage(e, t, n, { failedUrl: r, kind: i, loadError: a }) {
      bq(this.getPageLifecycleHelpers(), e, t, n, { failedUrl: r, kind: i, loadError: a });
    }
    openBrowserSidebarExternalUrl({ initiatorUrl: e, pageState: t, url: n, webContents: r }) {
      if (!yV(t.loadError, r.getURL()) || e !== t.loadError?.pageUrl) return !1;
      let i = bV(n);
      return i == null || !bb(i)
        ? !1
        : (this.openBrowserSidebarUrlExternally(t.conversationId, i), !0);
    }
    openBrowserSidebarUrlExternally(e, t) {
      Promise.resolve(a.shell.openExternal(t)).catch((n) => {
        NJ().warning(`failed to open browser sidebar url externally`, {
          safe: { conversationId: e },
          sensitive: { error: n, url: t },
        });
      });
    }
    handleRuntimeMouseNavigation(e, t) {
      let n = this.findPageStateForRuntimeSender(e);
      if (!(n == null || n.owner.isDestroyed()))
        switch (t.direction) {
          case `back`:
            this.goBack(n.owner, n.page.conversationId, n.page.browserTabId);
            break;
          case `forward`:
            this.goForward(n.owner, n.page.conversationId, n.page.browserTabId);
            break;
        }
    }
    findPageStateForRuntimeSender(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (r?.view.webContents?.id === e.id) return { owner: t.owner, page: r };
        }
      return null;
    }
    findPageStateForPageKey(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (r != null && vL(t.owner.id, n.conversationId, n.browserTabId) === e)
            return { conversationId: n.conversationId, page: r, threadState: n, windowState: t };
        }
      return null;
    }
    findPageStateForWebContentsId(e) {
      for (let t of this.windows.values())
        for (let n of t.threads.values()) {
          let r = n.page;
          if (r?.view.webContents?.id === e)
            return { conversationId: n.conversationId, page: r, threadState: n, windowState: t };
        }
      return null;
    }
    navigateCurrentPageHistory(e, t, n, r) {
      let i = this.getCurrentPageCommandContext(e, t, r);
      if (i == null) return;
      let { page: a, threadState: o, windowState: s } = i,
        c = a.view.webContents;
      if (
        c != null &&
        !(n === `back` && !c.canGoBack()) &&
        !(n === `forward` && !c.canGoForward())
      ) {
        if ((IV(this.windowManager, s.owner, t, o), ML(a), n === `back`)) {
          c.goBack();
          return;
        }
        c.goForward();
      }
    }
    getCurrentPageCommandContext(e, t, n) {
      let r = this.getCurrentWindowState(e);
      if (r == null) return null;
      let i = this.getThreadStateForWindow(r, t, n);
      return i?.page == null ? null : { page: i.page, threadState: i, windowState: r };
    }
    ensureThreadCommandContext(e, t, n) {
      let r = this.ensureCurrentWindowState(e);
      if (r == null) return null;
      let i = this.getBrowserTabIdForCommand(r, t, n);
      return (
        (r.activeConversationId = t),
        (r.activeBrowserTabId = i),
        { threadState: this.ensureThreadStateForWindow(r, t, i), windowState: r }
      );
    }
    ensureCurrentWindowState(e) {
      let t = this.getCurrentWindow(e);
      return t == null ? null : this.ensureWindowState(t, e);
    }
    getCurrentWindowState(e) {
      let t = this.getCurrentWindow(e);
      return t == null ? null : (this.windows.get(t.id) ?? null);
    }
    getCurrentWindow(e) {
      let t = a.BrowserWindow.fromWebContents(e);
      return t == null || t.isDestroyed() ? null : t;
    }
    getBrowserTabIdForCommand(e, t, n) {
      return n ?? (e.activeConversationId === t ? e.activeBrowserTabId : null) ?? OF(t);
    }
    getExistingBrowserTabIdForConversation(e, t) {
      for (let n of e.threads.values()) if (n.conversationId === t) return n.browserTabId;
      return null;
    }
    hasThreadForConversation(e, t) {
      return this.getExistingBrowserTabIdForConversation(e, t) != null;
    }
    getThreadStateForWindow(e, t, n) {
      let r = n ?? (e.activeConversationId === t ? e.activeBrowserTabId : null) ?? OF(t);
      return e.threads.get(yL({ browserTabId: r, conversationId: t })) ?? null;
    }
    getPageState(e, t, n) {
      return this.getThreadState(e, t, n)?.page ?? null;
    }
    getThreadState(e, t, n) {
      let r = this.getCurrentWindowState(e);
      return r == null ? null : this.getThreadStateForWindow(r, t, n);
    }
    getThreadPageState(e, t, n) {
      let r = this.getCurrentWindowState(e);
      return r == null ? null : (this.getThreadStateForWindow(r, t, n)?.page ?? null);
    }
    ensureThreadStateForCommand(e, t, n) {
      return this.ensureThreadCommandContext(e, t, n)?.threadState ?? null;
    }
    ensureThreadStateForWindow(e, n, r, { isBrowserUseTab: i = !1 } = {}) {
      let a = r ?? (e.activeConversationId === n ? e.activeBrowserTabId : null) ?? OF(n),
        o = yL({ browserTabId: a, conversationId: n }),
        s = e.threads.get(o);
      if (s != null) return s;
      let c = {
        browserTabId: a,
        conversationId: n,
        findState: kV(),
        page: null,
        hostKind: null,
        pendingBrowserUsePanelOpen: !1,
        hasPendingBrowserUseVisibilityRequest: !1,
        pendingCommentModeActivation: null,
        visible: !1,
        bounds: null,
        presented: !1,
        mountGeneration: 0,
        pendingTeardown: null,
        sessionState: `cold`,
        lastTabActivitySequence: 0,
        lastPresentedSequence: 0,
        lastLifecycleSequence: 0,
        emulatedViewportSize: null,
        viewportScale: 1,
        cwd: null,
        rolloutPath: null,
        isAgentControllingBrowser: !1,
        isAnnotationModeEnabled: !0,
        isBrowserUseActive: !1,
        isBrowserUsePage: i,
        browserUseTabOrigin: null,
        isBrowserUseNavigationRestrictionActive: !1,
        ownerRoutePath: null,
        commentModeBlockCheckRequestId: 0,
        commentModeBlockCheckUrl: null,
        themeVariant: `light`,
        runtimeIntlConfig: t.as,
        snapshot: lq(),
      };
      return (
        this.tabBudget.markTabActivity(c),
        e.threads.set(o, c),
        Nq(this.browserSessionRegistry, this.windowManager, this.windows, e, c),
        c
      );
    }
    refreshCommentModeBlockStatus(e, t) {
      LL({
        blocklistLookup: this.commentModeBlocklistLookup,
        comments: this.comments,
        sendPendingCommentModeRequestToRuntime: (e, t) => {
          this.sendPendingCommentModeRequestToRuntime(e, t);
        },
        syncRuntimeStateToPage: (e, t) => {
          this.syncRuntimeStateToPage(e, t);
        },
        syncThreadState: (e, t, n) => {
          this.syncThreadState(e, t, n);
        },
        windowState: e,
        threadState: t,
      });
    }
    ensureThreadPageForCommand(e, t, n, r) {
      let i = this.getThreadPageState(e, t, n);
      if (i != null) return i;
      let a = this.ensureThreadStateForCommand(e, t, n);
      if (a == null) return null;
      let o = this.ensureCurrentWindowState(e);
      return o == null ? null : (a.page ?? this.createThreadPageState(o, t, a, r), a.page);
    }
    disposeThreadState(e, t, n) {
      this.comments.close(e.owner, t, n.browserTabId);
      let r = n.page;
      (this.clearPendingWebviewRoutes(e, n),
        r != null &&
          (r.dispose(),
          this.isBrowserUseManagedBrowserTab(t, n.browserTabId) &&
            this.browserSessionRegistry.recordPageClosed({
              browserTabId: n.browserTabId,
              conversationId: t,
              windowId: RK(e.window.id),
            })),
        n.isBrowserUseActive &&
          this.setBrowserUseActiveForRoute(
            { conversationId: t, windowId: RK(e.window.id) },
            !1,
            n.browserTabId,
          ));
    }
    removeThreadState(e, t) {
      Fq(this.windowManager, this.windows, e, t);
    }
    clearPendingWebviewRoutes(e, t) {
      for (let [n, r] of e.pendingRetainedWebviewRoutesByInstanceId) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          e.pendingRetainedWebviewRoutesByInstanceId.delete(n);
      }
      for (let [n, r] of e.pendingAdoptedWebviewRoutesByWebContentsId) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          (e.canceledAdoptedWebContentsIds.add(n),
          e.pendingAdoptedWebviewRoutesByWebContentsId.delete(n));
      }
      for (let [n, r] of e.pendingWebviewRoutesByAttachToken) {
        let i = r.route;
        i.browserTabId === t.browserTabId &&
          i.conversationId === t.conversationId &&
          ((e.pendingWebviewAttachTokens = e.pendingWebviewAttachTokens.filter((e) => e !== n)),
          e.pendingWebviewRoutesByAttachToken.delete(n));
      }
    }
    transferPendingWebviewRoutes(e, t, n) {
      for (let [r, i] of e.pendingRetainedWebviewRoutesByInstanceId) {
        let a = i.route;
        a.browserTabId === t.browserTabId &&
          a.conversationId === t.conversationId &&
          e.pendingRetainedWebviewRoutesByInstanceId.set(r, { ...i, route: n });
      }
      for (let [r, i] of e.pendingAdoptedWebviewRoutesByWebContentsId) {
        let a = i.route;
        a.browserTabId === t.browserTabId &&
          a.conversationId === t.conversationId &&
          e.pendingAdoptedWebviewRoutesByWebContentsId.set(r, { ...i, route: n });
      }
      for (let [r, i] of e.pendingWebviewRoutesByAttachToken) {
        let a = i.route;
        a.browserTabId === t.browserTabId &&
          a.conversationId === t.conversationId &&
          e.pendingWebviewRoutesByAttachToken.set(r, { ...i, route: n });
      }
    }
    disposeWindowState(e) {
      let t = this.windows.get(e);
      if (t) {
        this.browserUseOpenRequests.rejectForOwner(
          t.owner,
          Error(`The Codex window closed while waiting for a Browser webview to attach`),
        );
        for (let e of t.threads.values()) this.disposeThreadState(t, e.conversationId, e);
        (t.disposeWebviewListeners(),
          t.threads.clear(),
          this.windows.delete(e),
          this.tabBudget.handleWindowClosed(e),
          this.lastSyncedWindowId === e && (this.lastSyncedWindowId = null),
          this.browserSessionRegistry.handleWindowClosed(e));
      }
    }
    getLiveWindowState(e) {
      return CJ({
        ensureWindowState: this.ensureWindowState.bind(this),
        windowId: e,
        windows: this.windows,
      });
    }
    getFocusedOwnerWebContents() {
      return wJ({
        getLiveWindowState: this.getLiveWindowState.bind(this),
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows.values(),
      });
    }
    getFocusedWindowState() {
      return TJ({ getLiveWindowState: this.getLiveWindowState.bind(this), windows: this.windows });
    }
    getPreferredWindowStateForBrowserUse() {
      return EJ({
        getFocusedWindowState: this.getFocusedWindowState.bind(this),
        getLiveWindowState: this.getLiveWindowState.bind(this),
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows.values(),
      });
    }
    getPreferredWindowStateForConversation(e) {
      return DJ({
        conversationId: e,
        getFocusedWindowState: this.getFocusedWindowState.bind(this),
        hasThreadForConversation: this.hasThreadForConversation.bind(this),
        lastSyncedWindowId: this.lastSyncedWindowId,
        windows: this.windows,
      });
    }
    getActivePageHandle(e) {
      let t = e.activeConversationId;
      return t == null
        ? null
        : this.getPageHandleForConversation(e, t, e.activeBrowserTabId ?? OF(t));
    }
    getPageHandleForConversation(e, t, n) {
      if (t == null) return null;
      let r = this.getThreadStateForWindow(e, t, n ?? void 0) ?? null;
      return r?.pendingTeardown != null ||
        r?.page == null ||
        r.page.view.webContents == null ||
        r.page.view.webContents.isDestroyed()
        ? null
        : FL(e, t, r.page);
    }
    getOrCreatePageForBrowserUse(e, t, { requireVisible: n }) {
      if (t == null) return null;
      let r = this.getThreadStateForWindow(e, t, OF(t)) ?? null;
      if (n && (r == null || !r.visible || r.bounds == null)) return null;
      let i = r?.snapshot ?? lq(),
        a = OF(t),
        o =
          r?.page ?? this.ensureThreadPageForCommand(e.owner, t, a, { title: i.title, url: i.url });
      return o == null ? null : FL(e, t, o);
    }
    getOrCreateActivePageForBrowserUse(e) {
      return this.getOrCreatePageForBrowserUse(e, e.activeConversationId, { requireVisible: !0 });
    }
    resolveBrowserUseOpenRequests(e) {
      this.browserUseOpenRequests.resolveForWindow(e, (t) =>
        t.conversationId == null
          ? this.getOrCreateActivePageForBrowserUse(e)
          : this.getPageHandleForConversation(e, t.conversationId, t.browserTabId),
      );
    }
  };
function IJ(e) {
  return e.method === `thread/archived` && typeof e.params?.threadId == `string`;
}
function LJ(e) {
  let n = t.uo(e);
  return n.length === 0 || n.startsWith(`about:blank#`);
}
function RJ(e) {
  let t = e.params?.status;
  return (
    e.method === `thread/status/changed` &&
    typeof e.params?.threadId == `string` &&
    typeof t == `object` &&
    !!t &&
    `type` in t &&
    t.type === `idle`
  );
}
var zJ = t.Gr(`remote-localhost-navigation`);
async function BJ({
  appServerConnectionRegistry: e,
  getHostConfigForHostId: n,
  hostId: r,
  url: i,
}) {
  if (r == null || r === `local`) return i;
  let a = n(r),
    o = t.do(i);
  if (!t.ya(a) || o == null) return i;
  try {
    let t = await e.getConnection(r).startRemoteLocalhostPortForward(o),
      n = new URL(i);
    return ((n.hostname = `localhost`), (n.port = String(t)), n.toString());
  } catch (e) {
    return (
      zJ().warning(`browser_sidebar.remote_localhost_port_forward_failed`, {
        safe: { remotePort: o },
        sensitive: { error: e, hostId: r, url: i },
      }),
      null
    );
  }
}
var VJ = class {
    id;
    constructor(e) {
      ((this.webContents = e), (this.id = e.id));
    }
    send(e, t) {
      this.webContents.send(e, t);
    }
    isDestroyed() {
      return this.webContents.isDestroyed();
    }
    onDestroyed(e) {
      let t = () => {
        e();
      };
      return (
        this.webContents.once(`destroyed`, t),
        {
          dispose: () => {
            this.webContents.removeListener(`destroyed`, t);
          },
        }
      );
    }
  },
  HJ = t.$s({
    legacyRoot: t.nc().min(1).nullable(),
    projectId: t.nc().trim().min(1),
    root: t.nc().optional(),
  }),
  UJ = t.$s({
    legacyRoot: t.nc().min(1).nullable(),
    projectId: t.nc().trim().min(1),
    root: t.nc(),
  }),
  WJ = t.$s({ legacyRoot: t.nc().min(1).nullable(), projectId: t.nc().trim().min(1) });
function GJ({
  getProjectWritableRootsValue: e,
  getRootCompareKeys: n = KJ,
  getRootForStorage: r = (e) => e,
  notifyChanged: i,
  runSerializedMutation: a,
  selectWritableRoot: o,
  setProjectWritableRootsValue: s,
}) {
  let c = () => t.Qi(e()),
    l = Promise.resolve(),
    u =
      a ??
      ((e) => {
        let t = l.then(e, e);
        return (
          (l = t.then(
            () => void 0,
            () => void 0,
          )),
          t
        );
      }),
    d = (e, t, n) => (s({ ...e, [t]: n }), i(), { changed: !0, projectId: t, roots: n });
  return {
    addRoot: async (e) => {
      let { legacyRoot: t, projectId: i, root: a } = qJ(e),
        s = await o(a);
      return s == null
        ? { changed: !1, projectId: i, roots: XJ(c(), i, t) }
        : u(async () => {
            let e = c(),
              a = XJ(e, i, t),
              o = await n(s);
            return (await Promise.all(a.map((e) => n(e.path)))).some((e) => ZJ(e, o))
              ? { changed: !1, projectId: i, roots: a }
              : d(e, i, [...a, { kind: `local`, path: r(s) }]);
          });
    },
    removeRoot: async (e) => {
      let { legacyRoot: t, projectId: r, root: i } = JJ(e);
      return u(async () => {
        let e = c(),
          a = XJ(e, r, t);
        if (a.length === 0) return { changed: !1, projectId: r, roots: [] };
        let o = await n(i),
          s = (
            await Promise.all(
              a.map(async (e) => ({ compareKeys: await n(e.path), writableRoot: e })),
            )
          )
            .filter(({ compareKeys: e }) => !ZJ(e, o))
            .map(({ writableRoot: e }) => e);
        return s.length === a.length ? { changed: !1, projectId: r, roots: a } : d(e, r, s);
      });
    },
    clearRoots: async (e) => {
      let { legacyRoot: t, projectId: n } = YJ(e);
      return u(async () => {
        let e = c(),
          r = Object.hasOwn(e, n),
          i = XJ(e, n, t);
        return r && i.length === 0 ? { changed: !1, projectId: n, roots: [] } : d(e, n, []);
      });
    },
  };
}
async function KJ(e) {
  let n = s.resolve(e);
  try {
    return (await (0, p.stat)(n)).isDirectory()
      ? [t.ko(n), t.ko(await (0, p.realpath)(n))]
      : [t.ko(n)];
  } catch {
    return [t.ko(n)];
  }
}
function qJ(e) {
  let t = HJ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots add request`);
  return t.data;
}
function JJ(e) {
  let t = UJ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots remove request`);
  return t.data;
}
function YJ(e) {
  let t = WJ.safeParse(e);
  if (!t.success) throw Error(`Invalid project writable roots clear request`);
  return t.data;
}
function XJ(e, t, n) {
  return Object.hasOwn(e, t) ? (e[t] ?? []) : n == null ? [] : [{ kind: `local`, path: n }];
}
function ZJ(e, t) {
  return e.some((e) => t.includes(e));
}
var QJ = 3e4,
  $J = 5e3,
  eY = 6e4,
  tY = class {
    logger = t.Gr(`sampler-manager`);
    appStateHeartbeat;
    appStateSnapshotRequestById = new t.Pa({ ttlMs: eY });
    appStateTriggerMinIntervalGate = new t.ia({ minIntervalMs: $J });
    constructor(e, t, n, r, i) {
      ((this.hostId = e),
        (this.windowManager = t),
        (this.desktopSentry = n),
        (this.electronSampler = r),
        (this.options = i),
        (this.appStateHeartbeat = setInterval(() => {
          this.requestAppStateSnapshot(`heartbeat`);
        }, QJ)),
        this.appStateHeartbeat.unref());
    }
    dispose() {
      clearInterval(this.appStateHeartbeat);
    }
    handleRendererReady(e) {
      this.requestAppStateSnapshot(`heartbeat`, e);
    }
    async handleMessage(e, t) {
      switch (t.type) {
        case `electron-app-state-snapshot-trigger`:
          this.requestAppStateSnapshot(t.reason, e);
          break;
        case `electron-app-state-snapshot-response`:
          await this.handleAppStateSnapshotResponse(e, t);
          break;
      }
    }
    async enrichWithHostSnapshotFields(e, t) {
      let n = await this.electronSampler.collectSnapshotFields(e),
        r = { ...t, ...n, ...this.options.getBrowserUseSnapshotFields() },
        i = this.options.getStdioIoStatsSnapshot();
      return (
        i != null &&
          ((r.app_server_stdio_bytes_read_total = i.bytesReadTotal),
          (r.app_server_stdio_bytes_written_total = i.bytesWrittenTotal),
          (r.app_server_stdio_bytes_read_last_30s = i.bytesReadLast30s),
          (r.app_server_stdio_bytes_written_last_30s = i.bytesWrittenLast30s)),
        r
      );
    }
    async handleAppStateSnapshotResponse(e, t) {
      let n = w.performance.now(),
        r = this.appStateSnapshotRequestById.get(t.requestId, n);
      if (!r || r.webContentsId !== e.id) return;
      this.appStateSnapshotRequestById.delete(t.requestId);
      let i = await this.enrichWithHostSnapshotFields(e, t.fields);
      (this.logger().debug(`app_state_snapshot`, { safe: i, sensitive: {} }),
        this.desktopSentry.addBreadcrumb({
          category: `app_state`,
          level: `info`,
          message: `app_state_snapshot`,
          data: i,
        }));
    }
    requestAppStateSnapshot(e, t) {
      let n = w.performance.now();
      if (e !== `heartbeat` && !this.appStateTriggerMinIntervalGate.canPass(n)) return;
      let r = t == null ? this.windowManager.getPrimaryWindow() : null;
      if (e === `heartbeat` && t == null && r != null && !r.isFocused()) return;
      let i = t ?? r?.webContents;
      if (!i || i.isDestroyed() || !this.windowManager.isWebContentsReady(i.id)) return;
      this.appStateTriggerMinIntervalGate.mark(n);
      let a = (0, u.randomUUID)();
      (this.appStateSnapshotRequestById.set(a, { webContentsId: i.id }, n),
        this.windowManager.sendMessageToWebContents(i, {
          type: `electron-app-state-snapshot-request`,
          hostId: this.hostId,
          requestId: a,
          reason: e,
        }));
    }
  },
  nY = t.Gr(`content-tracing`);
async function rY(e) {
  let t = a.app
      .getAppMetrics()
      .map((e) => ({
        pid: e.pid,
        type: e.type,
        memory:
          e.memory == null
            ? null
            : {
                workingSetSize: e.memory.workingSetSize,
                peakWorkingSetSize: e.memory.peakWorkingSetSize,
                privateBytes: e.memory.privateBytes ?? 0,
                sharedBytes: 0,
              },
      })),
    n =
      e == null || e.isDestroyed()
        ? null
        : await (async () => {
            let n = e.webContents;
            if (typeof n.getProcessMemoryInfo == `function`)
              return n
                .getProcessMemoryInfo()
                .then((e) => ({ private: e.private, shared: e.shared, residentSet: e.residentSet }))
                .catch(() => null);
            let r = n.getOSProcessId();
            if (r <= 0) return null;
            let i = t.find((e) => e.pid === r);
            return i?.memory == null
              ? null
              : {
                  private: i.memory.privateBytes,
                  shared: i.memory.sharedBytes,
                  residentSet: i.memory.workingSetSize,
                };
          })(),
    r = Object.fromEntries(Object.entries(a.app.getGPUFeatureStatus())),
    i = null,
    o = null;
  try {
    let e = iY(await a.app.getGPUInfo(`basic`)),
      t = e?.gpuDevice ?? [],
      n = t.find((e) => e.active) ?? t[0];
    i = n?.vendorString ?? (n?.vendorId == null ? null : String(n.vendorId));
    let r = e?.auxAttributes ?? {},
      s = r.glRenderer ?? r.gl_renderer;
    o = typeof s == `string` ? s : null;
  } catch (e) {
    nY().warning(`Failed to collect GPU info for trace upload.`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  return {
    appMetrics: t,
    rendererMemory: n,
    gpuFeatureStatus: r,
    gpuVendor: i,
    gpuRenderer: o,
    hardwareAccelerationEnabled: !a.app.commandLine.hasSwitch(`disable-gpu`),
  };
}
function iY(e) {
  return typeof e != `object` || !e ? null : e;
}
var aY = t.Gr(`content-tracing`),
  oY = `Start Performance Trace`,
  sY = `Stop Performance Trace`,
  cY = `Waiting to Start Trace…`,
  lY = `Saving Trace…`,
  uY = `Waiting for Trace Details…`,
  dY = `Uploading Trace…`,
  fY = `toggle-trace-recording`,
  pY = process.env.CODEX_TRACE_SHORTCUT != null,
  mY = [`*`, `disabled-by-default-v8.cpu_profiler`, `disabled-by-default-v8.cpu_profiler.hires`],
  hY = `idle`,
  gY = null,
  _Y = null;
function vY({
  allowDevtools: e,
  accelerator: t,
  windowManager: n,
  traceRecordingUploadOptions: r,
}) {
  let i = async (e) => {
    if (
      !(
        hY === `awaiting-start-confirmation` ||
        hY === `saving` ||
        hY === `awaiting-upload-details` ||
        hY === `uploading`
      )
    )
      try {
        let t = null;
        if (hY === `idle`) {
          if ((xY(n, e, `awaiting-start-confirmation`), !(await eN()))) {
            xY(n, e, `idle`);
            return;
          }
          (await a.contentTracing.startRecording({
            included_categories: mY,
            recording_mode: `record-continuously`,
          }),
            (gY = Date.now()),
            xY(n, e, `recording`),
            aY().info(`Started content trace recording.`));
          return;
        }
        xY(n, e, `saving`);
        let i = await a.contentTracing.stopRecording(
            (0, s.join)((0, o.tmpdir)(), `codex-trace-${Date.now()}.json`),
          ),
          c = gY == null ? null : Date.now() - gY;
        if (
          (aY().info(`Stopped content trace recording.`, {
            safe: { recordingDurationMs: c },
            sensitive: { tracePath: i },
          }),
          r != null)
        )
          try {
            let o = a.BrowserWindow.getFocusedWindow(),
              s = o != null && !o.isDestroyed() ? o : null,
              l = s?.id ?? null,
              u = s == null || !n.hasRegisteredWebContents(s.webContents) ? null : z,
              d = rY(s);
            xY(n, e, `awaiting-upload-details`);
            let [f, p] = await Promise.all([rN(), d]);
            xY(n, e, `uploading`);
            let { uploadTraceRecording: m } = await Promise.resolve().then(() =>
              require(`./trace-recording-upload-BroD-tA7.js`),
            );
            ((t = await m({
              ...r,
              tracePath: i,
              recordingDurationMs: c,
              correlation: { conversationId: f.conversationId, hostId: u, windowId: l },
              runtimeHealth: p,
              traceRecordingNote: f.note.length > 0 ? f.note : null,
            })),
              aY().info(`Uploaded content trace recording.`, {
                safe: { sourceWindowId: l, sourceHostId: u },
                sensitive: { tracePath: i, traceConversationId: f.conversationId },
              }));
          } finally {
            await CY(i);
          }
        ((gY = null),
          xY(n, e, `idle`),
          t != null &&
            n.sendMessageToAllRegisteredWindows({ type: `trace-recording-uploaded`, reportId: t }));
      } catch (t) {
        let r = wY();
        (r === `saving`
          ? xY(n, e, `recording`)
          : (r === `awaiting-start-confirmation` ||
              r === `awaiting-upload-details` ||
              r === `uploading`) &&
            ((gY = null), xY(n, e, `idle`)),
          aY().warning(`Failed to toggle content trace recording.`, {
            safe: {},
            sensitive: { error: t },
          }));
      }
  };
  return (
    (_Y = i),
    {
      id: fY,
      label: SY(hY),
      accelerator: e && pY ? t : void 0,
      click: async (e) => {
        await i(e);
      },
    }
  );
}
function yY(e) {
  e.sendMessageToAllRegisteredWindows({ type: `trace-recording-state-changed`, state: hY });
}
async function bY() {
  let e = a.Menu.getApplicationMenu()?.getMenuItemById(fY);
  e == null || _Y == null || (await _Y(e));
}
function xY(e, t, n) {
  ((hY = n), (t.label = SY(n)), yY(e));
}
function SY(e) {
  return e === `recording`
    ? sY
    : e === `awaiting-start-confirmation`
      ? cY
      : e === `saving`
        ? lY
        : e === `awaiting-upload-details`
          ? uY
          : e === `uploading`
            ? dY
            : oY;
}
async function CY(e) {
  try {
    await (0, p.rm)(e, { force: !0 });
  } catch (t) {
    aY().warning(`Failed to remove temporary trace recording file.`, {
      safe: {},
      sensitive: { error: t, tracePath: e },
    });
  }
}
function wY() {
  return hY;
}
var TY = e.o(r.T()),
  EY = t.Wr(`worktree-cleanup-service`),
  DY = 7200 * 60 * 1e3;
async function OY(e, n) {
  let r = Array.from(new Set(n)),
    i = await Promise.all(
      r.map(async (n) => {
        try {
          let r = await e.readThread(n, { includeTurns: !1 });
          if (r == null) return [n, null];
          let i = Number(r.updatedAt) * 1e3,
            a = null;
          if (r.path != null)
            try {
              let n = await t.jr.stat(r.path, e);
              a = Number.isFinite(n.mtimeMs) ? n.mtimeMs : null;
            } catch {}
          let o = [Number.isFinite(i) ? i : null, a].filter((e) => e != null);
          return [
            n,
            {
              updatedAtMs: o.length === 0 ? null : Math.max(...o),
              isInProgress: r.status.type === `active`,
              cwd: r.cwd ?? null,
              readFailed: !1,
            },
          ];
        } catch (e) {
          return (
            EY.error(`Failed to get thread metadata`, {
              safe: { threadId: n },
              sensitive: { error: e },
            }),
            [n, { updatedAtMs: Date.now(), isInProgress: !0, cwd: null, readFailed: !0 }]
          );
        }
      }),
    );
  return Object.fromEntries(i);
}
function kY(e) {
  let n = e.get(t.Fo.PINNED_THREAD_IDS);
  return Array.isArray(n) ? n.filter((e) => typeof e == `string`) : [];
}
function AY({ pinnedThreadIds: e, threadMetadataById: n, worktrees: r }) {
  let i = e
    .map((e) => n[e]?.cwd)
    .filter((e) => e != null)
    .map((e) => t.gt(e).replace(/\/+$/, ``));
  return r
    .map((e) => e.dir)
    .filter((e) => {
      let n = t.gt(e).replace(/\/+$/, ``);
      return i.some((e) => e === n || e.startsWith(`${n}/`));
    });
}
function jY({ pinnedThreadIds: e, threadMetadataById: t }) {
  return e.some((e) => {
    let n = t[e];
    return n == null || n.readFailed;
  });
}
function MY(e) {
  return e.getEffective(t.fi.keepCount.key) ?? t.fi.keepCount.default;
}
function NY(e, n, r = !0) {
  return !r && n.get(t.Fo.WORKTREE_AUTO_CLEANUP_UNPACKAGED_OVERRIDE_ENABLED) !== !0
    ? !1
    : e.getEffective(t.fi.autoCleanupEnabled.key) !== !1;
}
function PY(e, t = Date.now()) {
  let n = e.get(Hr),
    r;
  return (
    typeof n == `number` && Number.isFinite(n) && n > 0 ? (r = n) : ((r = t), e.set(Hr, r)),
    t < r + DY
  );
}
function FY({ globalState: e, worktrees: t }) {
  return t.map((e) => e.dir).filter((t) => IY({ globalState: e, worktreeDir: t }));
}
function IY({ globalState: e, worktreeDir: n }) {
  let i = t.gt(n).replace(/\/+$/, ``);
  return r.s(e).some((e) => {
    let n = t.gt(e).replace(/\/+$/, ``);
    return n === i || n.startsWith(`${i}/`);
  });
}
var LY = 32e3,
  RY = t.Gr(`worktree-service`),
  zY = class {
    statesById = new Map();
    newbornWorktreeRoots = new Set();
    newbornWorktreeRootsByStreamId = new Map();
    cleanupRunning = !1;
    cleanupPending = !1;
    constructor(e) {
      ((this.options = e),
        this.options.subscribeGitWorkerEvent(`create-worktree-path`, (e) => {
          this.handleCreateWorktreePath(e);
        }),
        this.options.subscribeGitWorkerEvent(`create-worktree-setup-started`, (e) => {
          this.handleCreateWorktreeSetupStarted(e);
        }),
        this.options.subscribeGitWorkerEvent(`create-worktree-stream`, (e) => {
          this.handleCreateWorktreeStream(e);
        }),
        this.publish());
    }
    create(e) {
      if (this.statesById.has(e.id)) return;
      let t = {
        ...e,
        createdAt: Date.now(),
        phase: `queued`,
        labelEdited: !1,
        outputText: ``,
        errorMessage: null,
        worktreeWorkspaceRoot: null,
        worktreeGitRoot: null,
        needsAttention: !1,
        isPinned: !1,
        pinnedBeforeThreadId: null,
      };
      (this.statesById.set(t.id, { entry: t, runtime: null }), this.publish(), this.start(t.id));
    }
    cancel(e) {
      let t = this.statesById.get(e);
      (t?.runtime?.abortController.abort(),
        this.statesById.delete(e) &&
          (this.publish(),
          t?.entry.worktreeGitRoot != null &&
            this.deleteWorktree(t.entry.worktreeGitRoot, `new-branch-cleanup`)));
    }
    dismiss(e) {
      let t = this.statesById.get(e);
      (t?.runtime?.abortController.abort(),
        this.statesById.delete(e) &&
          (this.publish(),
          t?.entry.phase === `failed` &&
            t.entry.worktreeGitRoot != null &&
            this.deleteWorktree(t.entry.worktreeGitRoot, `new-branch-cleanup`)));
    }
    retry(e) {
      let t = this.statesById.get(e);
      if (!t) return;
      let n = t.entry.phase === `failed` ? t.entry.worktreeGitRoot : null;
      (t.runtime?.abortController.abort(),
        (t.entry = {
          ...t.entry,
          phase: `queued`,
          outputText: ``,
          errorMessage: null,
          worktreeWorkspaceRoot: null,
          worktreeGitRoot: null,
          needsAttention: !1,
        }),
        (t.runtime = null),
        this.publish(),
        n != null && this.deleteWorktree(n, `new-branch-cleanup`),
        this.start(e));
    }
    continueWithoutSetup(e) {
      this.updateEntry(e, (e) =>
        e.phase !== `failed` || e.worktreeGitRoot == null || e.worktreeWorkspaceRoot == null
          ? e
          : {
              ...e,
              phase: `worktree-ready`,
              needsAttention: !1,
              outputText: BY(
                e.outputText,
                `[info] Continuing without local environment setup
`,
              ),
            },
      );
    }
    updateMetadata(e, t) {
      this.updateEntry(e, (e) => {
        switch (t.type) {
          case `isPinned`:
            return { ...e, isPinned: t.isPinned };
          case `pinnedBeforeThreadId`:
            return { ...e, pinnedBeforeThreadId: t.beforeThreadId };
          case `label`:
            return { ...e, label: t.label };
          case `labelEdited`:
            return { ...e, labelEdited: t.labelEdited };
          case `needsAttention`:
            return { ...e, needsAttention: t.needsAttention };
        }
      });
    }
    getPendingWorktrees() {
      return Array.from(this.statesById.values(), (e) => e.entry);
    }
    async start(e) {
      let n = this.statesById.get(e);
      if (!n) return;
      let { entry: r } = n,
        i = {
          abortController: new AbortController(),
          outputDecoder: new TextDecoder(),
          streamId: (0, u.randomUUID)(),
        };
      ((n.runtime = i),
        this.updateEntry(e, (e) => ({
          ...e,
          phase: `creating`,
          outputText: BY(
            ``,
            `[info] Starting worktree creation
`,
          ),
        })));
      try {
        let n = await this.requestGitWorker({
          method: `create-worktree`,
          params: {
            hostConfig: this.options.hostConfig,
            operationSource: `worktree_pending_create`,
            cwd: t.xa(r.sourceWorkspaceRoot),
            startingState: r.startingState,
            localEnvironmentConfigPath: r.localEnvironmentConfigPath,
            streamId: i.streamId,
            allowSetupFailure: !0,
            setUpSyncedBranch: r.launchMode === `create-stable-worktree` ? !1 : void 0,
          },
          signal: i.abortController.signal,
        });
        if (n.setupError != null) {
          (RY().info(`[worktree-create] setup-failed`, {
            safe: {
              worktreeId: t.ut(n.worktreeGitRoot),
              flow: `pending`,
              launchMode: r.launchMode,
              hasLocalEnvironment: r.localEnvironmentConfigPath != null,
            },
            sensitive: {},
          }),
            this.updateEntry(e, (e) => ({
              ...e,
              phase: `failed`,
              errorMessage: n.setupError,
              worktreeGitRoot: n.worktreeGitRoot,
              worktreeWorkspaceRoot: n.worktreeWorkspaceRoot,
              needsAttention: !0,
            })));
          let i = this.statesById.get(e);
          (i && (i.runtime = null), this.notifyFailure(e, n.setupError));
          return;
        }
        if (
          (this.updateEntry(e, (e) => ({
            ...e,
            phase: `worktree-ready`,
            worktreeGitRoot: n.worktreeGitRoot,
            worktreeWorkspaceRoot: n.worktreeWorkspaceRoot,
          })),
          RY().info(`[worktree-create] ready`, {
            safe: {
              worktreeId: t.ut(n.worktreeGitRoot),
              flow: `pending`,
              launchMode: r.launchMode,
              hasLocalEnvironment: r.localEnvironmentConfigPath != null,
              wasNewbornProtected: this.newbornWorktreeRoots.has(n.worktreeGitRoot),
              protectedNewbornCount: this.newbornWorktreeRoots.size,
            },
            sensitive: {},
          }),
          r.launchMode === `create-stable-worktree`)
        ) {
          (this.forgetNewbornWorktreeStream(i.streamId),
            this.addWorkspaceRoot(n.worktreeWorkspaceRoot, r.label),
            this.statesById.delete(e),
            this.publish());
          return;
        }
        let a = this.statesById.get(e);
        (a && (a.runtime = null), this.runCleanup());
      } catch (t) {
        this.forgetNewbornWorktreeStream(i.streamId);
        let n = this.statesById.get(e);
        if ((n && (n.runtime = null), VY(t))) return;
        let r = t instanceof Error ? t.message : String(t);
        (this.updateEntry(e, (e) => ({
          ...e,
          phase: `failed`,
          errorMessage: r,
          needsAttention: !0,
          outputText: BY(e.outputText, `[stderr] ${r}\n`),
        })),
          this.notifyFailure(e, r));
      }
    }
    async handleArchiveCleanup({
      conversationId: e,
      cwd: n,
      replacementOwnerThreadId: r,
      replacementOwnerCwd: i,
    }) {
      let a = t.xa(n);
      if (a == null) return;
      let o = await this.requestGitWorker({
        method: `resolve-worktree-for-thread`,
        params: {
          cwd: a,
          conversationId: t.Qo(e),
          hostConfig: this.options.hostConfig,
          operationSource: `worktree_archive_cleanup`,
        },
      });
      if (o.worktreeGitRoot == null) return;
      let s = o.worktreeGitRoot;
      if (!IY({ globalState: this.options.globalState, worktreeDir: String(s) })) {
        if (r != null && i != null && HY(i, String(s)))
          try {
            (await this.setOwnerThread({ worktree: s, conversationId: t.Qo(r) }),
              RY().info(`[worktree-archive-cleanup] owner-transferred`, {
                safe: { worktreeId: t.ut(s), archivedThreadId: e, replacementOwnerThreadId: r },
                sensitive: {},
              }));
            return;
          } catch (n) {
            RY().warning(`[worktree-archive-cleanup] owner-transfer-failed`, {
              safe: { worktreeId: t.ut(s), archivedThreadId: e, replacementOwnerThreadId: r },
              sensitive: { error: n },
            });
            return;
          }
        await this.deleteWorktree(s, `archive-cleanup`);
      }
    }
    async createManagedWorktree({
      hostId: e,
      cwd: n,
      startingState: r,
      localEnvironmentConfigPath: i,
      streamId: a,
    }) {
      try {
        let o = await this.requestGitWorker({
            method: `create-worktree`,
            params: {
              hostConfig: this.options.getHostConfigForHostId(e),
              operationSource: `worktree_managed_create`,
              cwd: t.xa(n),
              startingState: r,
              localEnvironmentConfigPath: i,
              streamId: a,
            },
          }),
          s = this.newbornWorktreeRoots.has(o.worktreeGitRoot);
        return (
          this.newbornWorktreeRoots.add(o.worktreeGitRoot),
          RY().info(`[worktree-create] ready`, {
            safe: {
              worktreeId: t.ut(o.worktreeGitRoot),
              flow: `managed`,
              hasLocalEnvironment: i != null,
              wasNewbornProtected: s,
              protectedNewbornCount: this.newbornWorktreeRoots.size,
            },
            sensitive: {},
          }),
          this.runCleanup(),
          o
        );
      } catch (e) {
        throw (this.forgetNewbornWorktreeStream(a), e);
      }
    }
    async deleteWorktree(e, n) {
      (this.forgetNewbornWorktreeRoot(e),
        await this.requestGitWorker({
          method: `delete-worktree`,
          params: {
            worktree: t.Sa(e),
            force: !0,
            reason: n,
            hostConfig: this.options.hostConfig,
            operationSource: WY(n),
          },
        }),
        this.options.windowManager.sendMessageToAllWindows({
          type: `worktrees-reload-requested`,
          hostId: this.options.hostId,
        }),
        this.runCleanup());
    }
    async setOwnerThread({ worktree: e, conversationId: n }) {
      let r = t.Sa(e);
      (await this.requestGitWorker({
        method: `set-worktree-owner-thread`,
        params: {
          worktree: r,
          conversationId: n,
          hostConfig: this.options.hostConfig,
          operationSource: `worktree_set_owner_thread`,
        },
      }),
        RY().info(`[worktree-create] owner-thread-set`, {
          safe: { worktreeId: t.ut(r), ownerThreadId: n },
          sensitive: {},
        }),
        this.forgetNewbornWorktreeRoot(e));
    }
    requestCleanup() {
      this.runCleanup();
    }
    async runCleanup() {
      if (((this.cleanupPending = !0), !this.cleanupRunning)) {
        this.cleanupRunning = !0;
        try {
          for (; this.cleanupPending; ) {
            this.cleanupPending = !1;
            try {
              await this.runCleanupOnce();
            } catch (e) {
              RY().warning(`[worktree-cleanup] run-failed`, { safe: {}, sensitive: { error: e } });
            }
          }
        } finally {
          this.cleanupRunning = !1;
        }
      }
    }
    async runCleanupOnce() {
      if (!NY(this.options.settingsStore, this.options.globalState, a.app.isPackaged)) return;
      let e = t.ht(this.options.appServerConnection);
      await t.it(e);
      let n = MY(this.options.settingsStore),
        { worktrees: r } = await this.requestGitWorker({
          method: `codex-worktrees`,
          params: { hostConfig: this.options.hostConfig, operationSource: `worktree_auto_cleanup` },
        });
      if (r.length <= n) return;
      let i = FY({ globalState: this.options.globalState, worktrees: r }),
        o = kY(this.options.globalState),
        s = new Map(),
        c = await Promise.all(
          r.map(async (n) => {
            let r = null,
              i = !1;
            try {
              let i = await this.options.gitManager.getWorktreeRepository(t.xa(n.dir), e);
              r = i == null ? null : ((await t.H(i))?.ownerThreadId ?? null);
            } catch {
              ((r = null), (i = !0));
            }
            s.set(String(n.dir), i);
            let a = null;
            try {
              let e = await t.jr.stat(n.dir, this.options.appServerConnection);
              a = e.birthtimeMs > 0 ? e.birthtimeMs : null;
            } catch {
              a = null;
            }
            return { worktree: n, ownerThreadId: r, createdAtMs: a };
          }),
        ),
        l = Array.from(new Set([...c.map((e) => e.ownerThreadId).filter((e) => e != null), ...o])),
        u = await OY(this.options.appServerConnection, l);
      if (jY({ pinnedThreadIds: o, threadMetadataById: u })) {
        RY().info(`[worktree-cleanup] skip-pinned-thread-metadata-unavailable`, {
          safe: { pinnedThreadCount: o.length },
          sensitive: {},
        });
        return;
      }
      let d = AY({ pinnedThreadIds: o, threadMetadataById: u, worktrees: r }),
        f = Array.from(this.statesById.values(), (e) => e.entry.worktreeGitRoot).filter(
          (e) => e != null,
        ),
        p = Array.from(this.newbornWorktreeRoots),
        m = t.V({
          records: c,
          keepCount: n,
          threadMetadataById: u,
          pinnedThreadIds: o,
          protectPreMigrationOwnerlessWorktrees: PY(this.options.globalState),
          protectedWorktreeDirs: Array.from(new Set([...i, ...d, ...f, ...p])),
        }),
        h = new Set(i),
        g = m.candidates.filter((e) => !h.has(String(e.worktree.dir)));
      RY().info(`[worktree-cleanup] plan-summary`, {
        safe: {
          totalWorktrees: r.length,
          keepCount: n,
          protectedWorkspaceRootCount: i.length,
          protectedPinnedThreadCount: d.length,
          protectedPendingCount: f.length,
          protectedNewbornCount: p.length,
          ownerlessCount: c.filter((e) => e.ownerThreadId == null).length,
          candidatesCount: m.candidates.length,
          deletesPlanned: g.length,
        },
        sensitive: {},
      });
      let _ = new Set([...i, ...d, ...f, ...p]);
      (g.slice(0, 20).forEach((e) => {
        let n = String(e.worktree.dir),
          r = s.get(n) === !0,
          i = e.ownerThreadId == null ? null : (u[e.ownerThreadId] ?? null);
        RY().info(`[worktree-cleanup] delete-candidate`, {
          safe: {
            worktreeId: t.ut(e.worktree.dir),
            reason: e.ownerThreadId == null ? `ownerless` : `owned`,
            ownerThreadId: e.ownerThreadId,
            ownerThreadUpdatedAtMs: i?.updatedAtMs ?? null,
            ownerThreadInProgress: i?.isInProgress ?? !1,
            createdAtMs: e.createdAtMs,
            ownerReadFailed: r,
            isProtected: _.has(n),
          },
          sensitive: {},
        });
      }),
        await Promise.all(
          g.map(async (e) => {
            await this.requestGitWorker({
              method: `delete-worktree`,
              params: {
                worktree: e.worktree.dir,
                force: !0,
                reason: `startup-cleanup`,
                hostConfig: this.options.hostConfig,
                operationSource: `worktree_auto_cleanup`,
              },
            });
          }),
        ),
        g.length > 0 &&
          this.options.windowManager.sendMessageToAllWindows({
            type: `worktrees-reload-requested`,
            hostId: this.options.hostId,
          }));
    }
    handleCreateWorktreePath(e) {
      e.hostId === this.options.hostId &&
        (this.newbornWorktreeRoots.add(e.worktreeGitRoot),
        this.newbornWorktreeRootsByStreamId.set(e.streamId, e.worktreeGitRoot),
        RY().info(`[worktree-create] path-allocated`, {
          safe: {
            worktreeId: t.ut(e.worktreeGitRoot),
            protectedNewbornCount: this.newbornWorktreeRoots.size,
          },
          sensitive: {},
        }));
    }
    forgetNewbornWorktreeStream(e) {
      let t = this.newbornWorktreeRootsByStreamId.get(e);
      t != null &&
        (this.newbornWorktreeRootsByStreamId.delete(e), this.newbornWorktreeRoots.delete(t));
    }
    forgetNewbornWorktreeRoot(e) {
      this.newbornWorktreeRoots.delete(e);
      for (let [t, n] of this.newbornWorktreeRootsByStreamId)
        n === e && this.newbornWorktreeRootsByStreamId.delete(t);
    }
    handleCreateWorktreeStream(e) {
      let t = Array.from(this.statesById.entries()).find(
        ([, t]) => t.runtime?.streamId === e.streamId,
      );
      if (!t) return;
      let [n, r] = t;
      if (r.runtime == null) return;
      let i = r.runtime.outputDecoder.decode(e.data, { stream: !0 });
      this.updateEntry(n, (e) => ({ ...e, outputText: BY(e.outputText, i) }));
    }
    handleCreateWorktreeSetupStarted(e) {
      let t = Array.from(this.statesById.entries()).find(
        ([, t]) => t.runtime?.streamId === e.streamId,
      );
      t &&
        this.updateEntry(t[0], (e) => (e.phase === `creating` ? { ...e, phase: `setting-up` } : e));
    }
    requestGitWorker(e) {
      return this.options.requestGitWorker(e);
    }
    addWorkspaceRoot(e, t) {
      let n = (0, TY.default)([...r.s(this.options.globalState), e]);
      (r.d(this.options.globalState, n),
        r.f(this.options.globalState, (n) => ({ ...n, [e]: t })),
        r.i(this.options.globalState).length === 0 &&
          (r.u(this.options.globalState, [e]),
          this.options.windowManager.sendMessageToAllWindows({
            type: `active-workspace-roots-updated`,
          })),
        this.options.windowManager.sendMessageToAllWindows({
          type: `workspace-root-options-updated`,
        }));
    }
    notifyFailure(e, n) {
      let r = this.options.settingsStore.getEffective(t.hi.turnMode.key);
      if (r === `off`) return;
      let i = this.options.windowManager.getPrimaryWindow();
      if (!i || (r === `unfocused` && i.isFocused())) return;
      let a = {
        id: `worktree-init-failed-${e}`,
        kind: `turn-complete`,
        title: `Worktree setup failed`,
        body: n,
      };
      this.options.desktopNotificationManager.showNotification(a, i.webContents, () => {});
    }
    publish() {
      let e = Array.from(this.statesById.values(), (e) => e.entry).sort(
        (e, t) => e.createdAt - t.createdAt,
      );
      this.options.setSharedPendingWorktrees(e);
    }
    updateEntry(e, t) {
      let n = this.statesById.get(e);
      n && ((n.entry = t(n.entry)), this.publish());
    }
  };
function BY(e, t) {
  if (!t) return e;
  let n = `${e}${t}`;
  return n.length <= LY ? n : n.slice(-LY);
}
function VY(e) {
  return e instanceof Error && e.name === `AbortError`;
}
function HY(e, t) {
  let n = UY(e),
    r = UY(t);
  return r === `/` ? n === `/` : n === r || n.startsWith(`${r}/`);
}
function UY(e) {
  let n = t.gt(e).replace(/\/+$/, ``);
  return n || (e.startsWith(`/`) ? `/` : n);
}
function WY(e) {
  switch (e) {
    case `settings-delete-targeted`:
      return `worktree_settings_delete`;
    case `archive-cleanup`:
      return `worktree_archive_cleanup`;
    case `startup-cleanup`:
      return `worktree_auto_cleanup`;
    case `new-branch-cleanup`:
      return `worktree_new_branch_cleanup`;
    case `remove-workspace-root-cleanup`:
      return `worktree_remove_workspace_root_cleanup`;
  }
}
var $ = t.Gr(`electron-message-handler`),
  GY = 560,
  KY = 1024,
  qY = 680,
  JY = 100,
  YY = `New project`,
  XY = 5e3,
  ZY = 5 * 6e4,
  QY = 5e3;
function $Y(e) {
  return e.mode === `onboarding`
    ? e.onboardingVariant === `v2`
      ? { width: KY, height: qY }
      : { width: GY, height: GY }
    : null;
}
var eX = {
    "thread-follower-start-turn": `thread-follower-start-turn-request`,
    "thread-follower-load-complete-history": `thread-follower-load-complete-history-request`,
    "thread-follower-compact-thread": `thread-follower-compact-thread-request`,
    "thread-follower-steer-turn": `thread-follower-steer-turn-request`,
    "thread-follower-interrupt-turn": `thread-follower-interrupt-turn-request`,
    "thread-follower-update-thread-settings": `thread-follower-update-thread-settings-request`,
    "thread-follower-edit-last-user-turn": `thread-follower-edit-last-user-turn-request`,
    "thread-follower-command-approval-decision": `thread-follower-command-approval-decision-request`,
    "thread-follower-file-approval-decision": `thread-follower-file-approval-decision-request`,
    "thread-follower-permissions-request-approval-response": `thread-follower-permissions-request-approval-response-request`,
    "thread-follower-submit-user-input": `thread-follower-submit-user-input-request`,
    "thread-follower-submit-mcp-server-elicitation-response": `thread-follower-submit-mcp-server-elicitation-response-request`,
    "thread-follower-set-queued-follow-ups-state": `thread-follower-set-queued-follow-ups-state-request`,
  },
  tX = class {
    sharedObjectSubscribers = new Map();
    sharedObjectSubscriberOrigins = new WeakSet();
    host;
    primaryWindowRestoreBounds = null;
    primaryWindowMode = null;
    datadogLogger;
    maxLogLevel;
    powerSaveBlockerId = null;
    powerSaveBlockingWebContentsIds = new Set();
    pluggedInRemoteControlPowerSaveWebContentsIds = new Set();
    powerSaveTrackedWebContentsIds = new Set();
    pendingThreadFollowerStartTurnRequests = new Map();
    pendingThreadFollowerLoadCompleteHistoryRequests = new Map();
    pendingThreadFollowerCompactThreadRequests = new Map();
    pendingThreadFollowerSteerTurnRequests = new Map();
    pendingThreadFollowerInterruptTurnRequests = new Map();
    pendingThreadFollowerUpdateThreadSettingsRequests = new Map();
    pendingThreadFollowerEditLastUserTurnRequests = new Map();
    pendingThreadFollowerCommandApprovalDecisionRequests = new Map();
    pendingThreadFollowerFileApprovalDecisionRequests = new Map();
    pendingThreadFollowerPermissionsRequestApprovalResponseRequests = new Map();
    pendingThreadFollowerSubmitUserInputRequests = new Map();
    pendingThreadFollowerSubmitMcpServerElicitationRequests = new Map();
    pendingThreadFollowerSetQueuedFollowUpsStateRequests = new Map();
    pendingThreadRoleRequests = new Map();
    pendingDynamicToolsForThreadStartRequests = new Map();
    samplerManager;
    worktreeServicesByHostId = new Map();
    projectWritableRootsMutationQueue = Promise.resolve();
    requestGitWorker;
    subscribeGitWorkerEvent;
    browserSessionRegistry;
    browserSidebarManager;
    automationSchedulerController = null;
    onBrowserSidebarStateChanged = () => {};
    constructor(
      e,
      t,
      n,
      i,
      o,
      c,
      l,
      u,
      d,
      f,
      p,
      m,
      h,
      g,
      _,
      v,
      y,
      b,
      x,
      S,
      C,
      w,
      T,
      E = (e) => {
        if (e === this.hostId) return this.host;
        throw Error(`Host config for host ID ${e} not found`);
      },
      D,
      O,
    ) {
      ((this.hostId = e),
        (this.appServerClient = n),
        (this.windowManager = i),
        (this.avatarOverlayManager = o),
        (this.appServerConnectionRegistry = c),
        (this.fetchWrapper = l),
        (this.globalState = u),
        (this.sharedObjectRepository = d),
        (this.terminalManager = f),
        (this.gitManager = p),
        (this.desktopNotificationManager = h),
        (this.errorReporter = g),
        (this.desktopSentry = _),
        (this.updateWorkerSentryUser = v),
        (this.getIpcClientForWebContents = y),
        (this.inboxManager = b),
        (this.getHostConfigForHostId = E),
        (this.hasActiveComputerUseTurn = D),
        (this.settingsStore = O),
        (this.host = t),
        this.sharedObjectRepository.addSubscriber((e, t) => {
          this.sharedObjectSubscribers.get(e)?.forEach((n, r) => {
            this.sendSharedObjectUpdate(r, e, t);
          });
        }),
        (this.datadogLogger = x),
        (this.maxLogLevel = S),
        (this.requestGitWorker = m),
        (this.subscribeGitWorkerEvent = w),
        a.powerMonitor.on(`on-ac`, () => {
          this.syncPowerSaveBlocker();
        }),
        a.powerMonitor.on(`on-battery`, () => {
          this.syncPowerSaveBlocker();
        }),
        (this.browserSessionRegistry = new BK({
          appSessionId: r.N,
          buildFlavor: C,
          errorReporter: this.errorReporter,
        })),
        (this.browserSidebarManager = new FJ(
          this.windowManager,
          this.errorReporter,
          this.hostId,
          PP({ appServerClient: this.getAppServerConnection(this.hostId) }),
          this.browserSessionRegistry,
          s.join(a.app.getPath(`userData`), `browser-sidebar-local-servers.json`),
          void 0,
          { appServerNotificationSource: this.getAppServerConnection(this.hostId) },
        )),
        (this.samplerManager = new tY(this.hostId, this.windowManager, this.desktopSentry, T, {
          getBrowserUseSnapshotFields: () => this.browserSessionRegistry.getSnapshotFields(),
          getStdioIoStatsSnapshot: () =>
            this.getAppServerConnection(this.hostId).getStdioIoStatsSnapshot(),
        })),
        this.ensureWorktreeService(this.hostId));
    }
    getWorktreeService(e = this.hostId) {
      return this.ensureWorktreeService(e);
    }
    setAutomationSchedulerController(e) {
      this.automationSchedulerController = e;
    }
    getBrowserSidebarManager() {
      return this.browserSidebarManager;
    }
    getBrowserSessionRegistry() {
      return this.browserSessionRegistry;
    }
    createProjectWritableRootsService(e) {
      return GJ({
        getProjectWritableRootsValue: () => this.globalState.get(t.Fo.PROJECT_WRITABLE_ROOTS),
        getRootCompareKeys: (e) => KJ(le(e, this.host)),
        getRootForStorage: (e) => I(e, this.shouldUseWslPaths()),
        notifyChanged: () => {
          this.windowManager.sendMessageToAllWindows({
            type: `global-state-updated`,
            keys: [t.Fo.PROJECT_WRITABLE_ROOTS],
          });
        },
        runSerializedMutation: (e) => this.runProjectWritableRootsMutation(e),
        selectWritableRoot: async (t) =>
          t == null ? this.pickLocalWorkspaceRoot(e) : this.resolveWorkspaceRoot(t),
        setProjectWritableRootsValue: (e) => {
          this.globalState.set(t.Fo.PROJECT_WRITABLE_ROOTS, e);
        },
      });
    }
    runProjectWritableRootsMutation(e) {
      let t = this.projectWritableRootsMutationQueue.then(e, e);
      return (
        (this.projectWritableRootsMutationQueue = t.then(
          () => void 0,
          () => void 0,
        )),
        t
      );
    }
    setOnBrowserSidebarStateChanged(e) {
      ((this.onBrowserSidebarStateChanged = e),
        this.browserSidebarManager.setOnBrowserSidebarStateChanged(e));
    }
    getAppServerConnection(e) {
      return this.appServerConnectionRegistry.getConnection(e);
    }
    ensureWorktreeService(e) {
      let t = this.worktreeServicesByHostId.get(e);
      return (
        t ||
        ((t = new zY({
          appServerConnection: this.getAppServerConnection(e),
          desktopNotificationManager: this.desktopNotificationManager,
          globalState: this.globalState,
          settingsStore: this.getSettingsStore(),
          getHostConfigForHostId: this.getHostConfigForHostId,
          gitManager: this.gitManager,
          hostConfig: this.getHostConfigForHostId(e),
          hostId: e,
          requestGitWorker: this.requestGitWorker,
          setSharedPendingWorktrees: () => {
            this.publishPendingWorktrees();
          },
          subscribeGitWorkerEvent: this.subscribeGitWorkerEvent,
          windowManager: this.windowManager,
        })),
        this.worktreeServicesByHostId.set(e, t),
        this.publishPendingWorktrees(),
        t)
      );
    }
    publishPendingWorktrees() {
      this.sharedObjectRepository.set(
        `pending_worktrees`,
        Array.from(this.worktreeServicesByHostId.values()).flatMap((e) => e.getPendingWorktrees()),
      );
    }
    async handleMessage(e, n) {
      if (EP(n)) {
        await OP(this.avatarOverlayManager, e, n, (t) => {
          this.windowManager.sendMessageToWebContents(e, t);
        });
        return;
      }
      switch (n.type) {
        case `open-codex-bundle`:
          break;
        case `reload-bundled-plugins`:
          break;
        case `debug-window-origin-conversation-changed`:
          this.windowManager.debugWindowManager?.setOriginConversation(e, n.conversationId);
          break;
        case `open-mcp-app-sandbox-devtools`:
          this.windowManager.openMcpAppSandboxDevtools(e, n.sandboxId);
          break;
        case `set-telemetry-user`: {
          this.updateWorkerSentryUser(n.authMethod, n.userId, n.email, n.accountId);
          let e =
            n.authMethod == null ? null : { id: n.userId ?? void 0, authMethod: n.authMethod };
          (this.datadogLogger.setUserInfo(e),
            this.desktopSentry.setUser(
              e == null ? null : { ...e, account_id: n.accountId ?? void 0 },
            ));
          break;
        }
        case `ready`:
          (this.windowManager.markWebContentsReady(e),
            this.avatarOverlayManager.handleRendererReady(e.id),
            this.terminalManager.track(e),
            this.sendCustomPrompts(e).catch((e) => {
              this.errorReporter.reportNonFatal(e instanceof Error ? e : t.Oo(e), {
                kind: `send-custom-prompts`,
              });
            }),
            this.sendPersistedAtomState(e),
            this.samplerManager.handleRendererReady(e),
            $().info(`Handled 'ready' message, sent ide-context-updated`));
          break;
        case `open-in-main-window`:
        case `open-browser-in-main-window`:
        case `open-current-main-window`:
        case `open-in-new-window`:
        case `mac-menu-bar-enabled-changed`:
        case `electron-avatar-overlay-restore-ready`:
        case `electron-sparkle-autodownload-changed`:
        case `electron-desktop-features-changed`:
        case `global-dictation-enabled-changed`:
        case `global-dictation-force-lock-changed`:
        case `global-dictation-pet-renderer-ready`:
        case `global-dictation-renderer-ready`:
        case `global-dictation-stop-requested`:
        case `global-dictation-pointer-interaction-changed`:
        case `global-dictation-recording-stopped`:
        case `global-dictation-dismiss`:
        case `global-dictation-close`:
        case `global-dictation-completed`:
        case `global-dictation-record-history-item`:
        case `global-dictation-in-app-started`:
        case `global-dictation-failed`:
          break;
        case `power-save-blocker-set`:
          this.updatePowerSaveBlocker(e, n.shouldBlock, n.keepRemoteControlAwakeWhilePluggedIn);
          break;
        case `local-thread-activity-changed`:
          Jv(n.hasInProgressLocalConversation);
          break;
        case `electron-set-window-mode`:
          this.setPrimaryWindowMode(e, { mode: n.mode, onboardingVariant: n.onboardingVariant });
          break;
        case `inbox-item-set-read-state`:
          if (n.isRead) {
            let e = Date.now();
            this.inboxManager?.markRead(n.id, e);
            break;
          }
          this.inboxManager?.markUnread(n.id);
          break;
        case `inbox-items-create`: {
          if (!this.inboxManager) break;
          let e = Date.now(),
            t = n.conversationId || n.turnId || (0, u.randomUUID)(),
            r = n.items.map((r, i) => ({
              id: n.items.length > 1 && !r.id ? `${t}-${i + 1}` : r.id || t,
              automationId: null,
              automationName: null,
              title: r.title ?? null,
              description: r.description ?? null,
              archivedAssistantMessage: null,
              archivedUserMessage: null,
              archivedReason: null,
              sourceCwd: null,
              threadId: n.conversationId ?? null,
              readAt: null,
              createdAt: e,
              status: null,
            }));
          this.inboxManager.persistAndNotify(r);
          break;
        }
        case `archive-thread`:
          this.getAppServerConnection(n.hostId).registerArchiveThread(n.conversationId, {
            cwd: n.cwd,
            cleanupWorktree: n.cleanupWorktree,
            replacementOwnerThreadId: n.replacementOwnerThreadId,
            replacementOwnerCwd: n.replacementOwnerCwd,
          });
          break;
        case `unarchive-thread`:
          this.getAppServerConnection(n.hostId).registerUnarchiveThread(n.conversationId);
          break;
        case `thread-archived`: {
          let t = this.getIpcClientForWebContents(e);
          t &&
            (await t.sendBroadcast(`thread-archived`, {
              hostId: n.hostId,
              conversationId: n.conversationId,
              cwd: n.cwd,
            }));
          break;
        }
        case `thread-unarchived`: {
          let t = this.getIpcClientForWebContents(e);
          t &&
            (await t.sendBroadcast(`thread-unarchived`, {
              hostId: n.hostId,
              conversationId: n.conversationId,
            }));
          break;
        }
        case `thread-queued-followups-changed`: {
          let t = this.getIpcClientForWebContents(e);
          t &&
            (await t.sendBroadcast(`thread-queued-followups-changed`, {
              conversationId: n.conversationId,
              messages: n.messages,
            }));
          break;
        }
        case `thread-stream-state-changed`: {
          let t = this.getIpcClientForWebContents(e);
          t && (await t.sendBroadcast(`thread-stream-state-changed`, n));
          break;
        }
        case `thread-read-state-changed`: {
          let t = this.getIpcClientForWebContents(e);
          t &&
            (await t.sendBroadcast(`thread-read-state-changed`, {
              conversationId: n.conversationId,
              hasUnreadTurn: n.hasUnreadTurn,
            }));
          break;
        }
        case `query-cache-invalidate`: {
          n.queryKey[0] === `plugins` && rr(this.getAppServerConnection(this.hostId));
          let t = this.getIpcClientForWebContents(e);
          t && (await t.sendBroadcast(`query-cache-invalidate`, { queryKey: n.queryKey }));
          break;
        }
        case `thread-follower-start-turn-response`:
          this.handleThreadFollowerStartTurnResponse(e, n);
          break;
        case `thread-follower-load-complete-history-response`:
          this.handleThreadFollowerLoadCompleteHistoryResponse(e, n);
          break;
        case `thread-follower-compact-thread-response`:
          this.handleThreadFollowerCompactThreadResponse(e, n);
          break;
        case `thread-follower-steer-turn-response`:
          this.handleThreadFollowerSteerTurnResponse(e, n);
          break;
        case `thread-follower-interrupt-turn-response`:
          this.handleThreadFollowerInterruptTurnResponse(e, n);
          break;
        case `thread-follower-update-thread-settings-response`:
          this.handleThreadFollowerUpdateThreadSettingsResponse(e, n);
          break;
        case `thread-follower-edit-last-user-turn-response`:
          this.handleThreadFollowerEditLastUserTurnResponse(e, n);
          break;
        case `thread-follower-command-approval-decision-response`:
          this.handleThreadFollowerCommandApprovalDecisionResponse(e, n);
          break;
        case `thread-follower-file-approval-decision-response`:
          this.handleThreadFollowerFileApprovalDecisionResponse(e, n);
          break;
        case `thread-follower-permissions-request-approval-response-response`:
          this.handleThreadFollowerPermissionsRequestApprovalResponse(e, n);
          break;
        case `thread-follower-submit-user-input-response`:
          this.handleThreadFollowerSubmitUserInputResponse(e, n);
          break;
        case `thread-follower-submit-mcp-server-elicitation-response-response`:
          this.handleThreadFollowerSubmitMcpServerElicitationResponse(e, n);
          break;
        case `thread-follower-set-queued-follow-ups-state-response`:
          this.handleThreadFollowerSetQueuedFollowUpsStateResponse(e, n);
          break;
        case `thread-role-response`:
          this.handleThreadRoleResponse(e, n);
          break;
        case `dynamic-tools-for-thread-start-response`:
          this.handleDynamicToolsForThreadStartResponse(e, n);
          break;
        case `persisted-atom-sync-request`:
          this.sendPersistedAtomState(e);
          break;
        case `persisted-atom-update`:
          this.updatePersistedAtomState(n.key, n.deleted ? void 0 : n.value);
          break;
        case `browser-sidebar-owner-sync`:
          if (
            ($().info(`IAB_LIFECYCLE received browser sidebar owner sync`, {
              safe: {
                browserTabId: n.browserTabId ?? null,
                conversationId: n.conversationId,
                originWebContentsId: e.id,
                windowId: a.BrowserWindow.fromWebContents(e)?.id ?? null,
              },
              sensitive: { ownerRoutePath: n.ownerRoutePath },
            }),
            this.getBrowserSidebarOwner(e) !== e)
          ) {
            $().warning(`IAB_LIFECYCLE ignored browser sidebar owner sync from auxiliary window`, {
              safe: {
                conversationId: n.conversationId,
                originWebContentsId: e.id,
                windowId: a.BrowserWindow.fromWebContents(e)?.id ?? null,
              },
              sensitive: {},
            });
            break;
          }
          (this.browserSidebarManager.syncOwner(
            e,
            n.conversationId,
            n.browserTabId,
            n.multiTabBrowserUseEnabled === !0,
            n.ownerRoutePath,
            n.annotationModeEnabled,
          ),
            this.onBrowserSidebarStateChanged());
          break;
        case `browser-sidebar-sync`:
          (this.browserSidebarManager.sync(e, n.payload), this.onBrowserSidebarStateChanged());
          break;
        case `browser-sidebar-webview-host-created`:
          $().info(`IAB_LIFECYCLE received browser sidebar webview host created`, {
            safe: {
              browserTabId: n.browserTabId,
              conversationId: n.conversationId,
              hostKind: n.hostKind,
              originWebContentsId: e.id,
              windowId: a.BrowserWindow.fromWebContents(e)?.id ?? null,
            },
            sensitive: {},
          });
          break;
        case `browser-settings-webview-mounted`:
          this.browserSidebarManager.browserSettingsWebviewMounted(e, n.mountId, n.themeVariant);
          break;
        case `browser-settings-webview-theme-changed`:
          this.browserSidebarManager.browserSettingsWebviewThemeChanged(e, n.themeVariant);
          break;
        case `browser-settings-webview-unmounted`:
          this.browserSidebarManager.browserSettingsWebviewUnmounted(e, n.mountId);
          break;
        case `browser-sidebar-webview-destroyed`:
          (this.browserSidebarManager.browserSidebarWebviewDestroyed(
            e,
            n.conversationId,
            n.browserTabId,
            n.teardownId,
          ),
            this.onBrowserSidebarStateChanged());
          break;
        case `browser-use-session-route-capture`: {
          let t = this.getBrowserSidebarOwner(e);
          if (
            ($().info(`IAB_LIFECYCLE received browser use session route capture`, {
              safe: {
                conversationId: n.conversationId,
                disposeAfterSessionActivity: n.disposeAfterSessionActivity ?? !1,
                originWebContentsId: e.id,
                ownerWebContentsId: t?.id ?? null,
                windowId: a.BrowserWindow.fromWebContents(e)?.id ?? null,
              },
              sensitive: {},
            }),
            t == null)
          ) {
            $().warning(
              `IAB_LIFECYCLE ignored browser use session route capture without primary window`,
              {
                safe: { conversationId: n.conversationId, originWebContentsId: e.id },
                sensitive: {},
              },
            );
            break;
          }
          this.browserSessionRegistry.captureSessionRoute({
            conversationId: n.conversationId,
            disposeAfterSessionActivity: n.disposeAfterSessionActivity,
            multiTabBrowserUseEnabled: n.multiTabBrowserUseEnabled === !0,
            owner: t,
          });
          break;
        }
        case `browser-use-session-activity-ended`:
          this.browserSessionRegistry.endSessionActivity({ conversationId: n.conversationId });
          break;
        case `browser-use-cursor-arrived`:
          this.browserSessionRegistry.notifyBrowserUseCursorArrived({
            owner: e,
            payload: { conversationId: n.conversationId, moveSequence: n.moveSequence },
          });
          break;
        case `browser-use-non-local-sites-allowed-changed`:
          this.browserSidebarManager.setAllowNonLocalBrowserUseNavigation(n.allowed);
          break;
        case `open-browser-tab`:
          this.sendMessageToView(e, n);
          break;
        case `browser-sidebar-tweaks-enabled-changed`:
          this.browserSidebarManager.setTweaksFeatureEnabled(n.enabled);
          break;
        case `browser-sidebar-command`:
          switch (n.command.type) {
            case `navigate`: {
              let t = await BJ({
                appServerConnectionRegistry: this.appServerConnectionRegistry,
                getHostConfigForHostId: this.getHostConfigForHostId,
                hostId: n.command.hostId,
                url: n.command.url,
              });
              if (t == null) break;
              this.browserSidebarManager.navigate(
                e,
                n.conversationId,
                t,
                n.command.source,
                n.command.initiator,
                n.browserTabId,
                n.command.url,
              );
              break;
            }
            case `hide-local-server`:
              this.browserSidebarManager.hideLocalServer(n.command.server);
              break;
            case `unhide-local-server`:
              this.browserSidebarManager.unhideLocalServer(n.command.url);
              break;
            case `remove-local-server-route`:
              this.browserSidebarManager.removeLocalServerRoute(
                n.command.serverUrl,
                n.command.routeUrl,
              );
              break;
            case `capture-screenshot`:
              await this.browserSidebarManager.captureScreenshotToClipboard(
                e,
                n.conversationId,
                n.browserTabId,
              );
              break;
            case `set-interaction-mode`:
              this.browserSidebarManager.setInteractionMode(
                e,
                n.conversationId,
                n.command.interactionMode,
                n.browserTabId,
              );
              break;
            case `set-design-modifier-pressed`:
              this.browserSidebarManager.setDesignModifierPressed(
                e,
                n.conversationId,
                n.command.pressed,
              );
              break;
            case `set-original-view-enabled`:
              this.browserSidebarManager.setOriginalViewEnabled(
                e,
                n.conversationId,
                n.command.enabled,
              );
              break;
            case `clear-comments`:
              this.browserSidebarManager.clearComments(e, n.conversationId, n.browserTabId);
              break;
            case `discard-pending-annotations`:
              this.browserSidebarManager.discardPendingAnnotations(
                e,
                n.conversationId,
                n.browserTabId,
              );
              break;
            case `add-annotations-to-composer`:
              this.browserSidebarManager.addAnnotationsToComposer(
                e,
                n.conversationId,
                n.browserTabId,
              );
              break;
            case `close-tab`:
              this.browserSidebarManager.closePage(e, n.conversationId, n.browserTabId);
              break;
            case `go-back`:
              this.browserSidebarManager.goBack(e, n.conversationId, n.browserTabId);
              break;
            case `go-forward`:
              this.browserSidebarManager.goForward(e, n.conversationId, n.browserTabId);
              break;
            case `scroll`:
              this.browserSidebarManager.scroll(
                e,
                n.conversationId,
                n.command.scroll,
                n.browserTabId,
              );
              break;
            case `open-find`:
              this.browserSidebarManager.openFind(e, n.conversationId, n.browserTabId);
              break;
            case `set-find-query`:
              this.browserSidebarManager.setFindQuery(
                e,
                n.conversationId,
                n.command.query,
                n.browserTabId,
              );
              break;
            case `find-next`:
              this.browserSidebarManager.findNext(e, n.conversationId, n.browserTabId);
              break;
            case `find-previous`:
              this.browserSidebarManager.findPrevious(e, n.conversationId, n.browserTabId);
              break;
            case `close-find`:
              this.browserSidebarManager.closeFind(e, n.conversationId, n.browserTabId);
              break;
            case `focus-address`:
              break;
            case `refresh-cursor`:
              this.browserSidebarManager.refreshCursor(e, n.conversationId, n.browserTabId);
              break;
            case `step-zoom`:
              this.browserSidebarManager.stepZoom(
                e,
                n.conversationId,
                n.command.delta,
                n.browserTabId,
              );
              break;
            case `set-zoom-percent`:
              this.browserSidebarManager.setZoomPercent(
                e,
                n.conversationId,
                n.command.zoomPercent,
                n.browserTabId,
              );
              break;
            case `reset-zoom`:
              this.browserSidebarManager.resetZoom(e, n.conversationId, n.browserTabId);
              break;
            case `reload`:
              this.browserSidebarManager.reload(
                e,
                n.conversationId,
                { ignoreCache: n.command.ignoreCache },
                n.browserTabId,
              );
              break;
            case `reset`:
              this.browserSidebarManager.reset(e, n.conversationId, n.browserTabId);
              break;
            case `stop`:
              this.browserSidebarManager.stop(e, n.conversationId, n.browserTabId);
              break;
            case `select-comment`:
              this.browserSidebarManager.selectComment(
                e,
                n.conversationId,
                n.command.commentId,
                n.browserTabId,
              );
              break;
            case `transfer-conversation`:
              this.browserSidebarManager.transferConversation(
                e,
                n.conversationId,
                n.command.targetConversationId,
                n.browserTabId,
                n.command.targetBrowserTabId,
              );
              break;
          }
          break;
        case `browser-sidebar-runtime-open-comment-preview`:
          this.browserSidebarManager.handleRuntimeOpenCommentPreview(e, n);
          break;
        case `browser-sidebar-runtime-close-comment-preview`:
          this.browserSidebarManager.handleRuntimeCloseCommentPreview(e, n);
          break;
        case `browser-sidebar-runtime-open-editor`:
          await this.browserSidebarManager.handleRuntimeOpenEditor(e, n);
          break;
        case `browser-sidebar-runtime-open-design-editor`:
          await this.browserSidebarManager.handleRuntimeOpenDesignEditor(e, n);
          break;
        case `browser-sidebar-runtime-comment-screenshot-ready`:
          this.browserSidebarManager.handleRuntimeCommentScreenshotReady(e, n);
          break;
        case `browser-sidebar-runtime-update-anchor`:
          this.browserSidebarManager.handleRuntimeUpdateAnchor(e, n);
          break;
        case `browser-sidebar-runtime-focus-editor`:
          this.browserSidebarManager.handleRuntimeFocusEditor(e);
          break;
        case `browser-sidebar-runtime-exit-comment-mode`:
          this.browserSidebarManager.handleRuntimeExitCommentMode(e);
          break;
        case `browser-sidebar-runtime-design-modifier-state`:
          this.browserSidebarManager.handleRuntimeDesignModifierState(e, n);
          break;
        case `browser-sidebar-runtime-image-drag-started`:
          this.browserSidebarManager.handleRuntimeImageDragStarted(e, n);
          break;
        case `browser-sidebar-runtime-image-drag-ended`:
          this.browserSidebarManager.handleRuntimeImageDragEnded(e);
          break;
        case `browser-sidebar-runtime-mouse-navigation`:
          this.browserSidebarManager.handleRuntimeMouseNavigation(e, n);
          break;
        case `browser-sidebar-attach-dragged-image`:
          await this.browserSidebarManager.attachDraggedImageToThread(
            e,
            n.conversationId,
            n.browserTabId,
          );
          break;
        case `browser-sidebar-comment-overlay-submit`:
          await this.browserSidebarManager.handleCommentOverlaySubmit(e, n);
          break;
        case `browser-sidebar-design-overlay-update`:
          this.browserSidebarManager.handleDesignOverlayUpdate(e, n);
          break;
        case `browser-sidebar-design-overlay-add`:
          await this.browserSidebarManager.handleDesignOverlayAdd(e, n);
          break;
        case `browser-sidebar-design-overlay-delete`:
          this.browserSidebarManager.handleDesignOverlayDelete(e, n);
          break;
        case `browser-sidebar-comment-overlay-delete`:
          this.browserSidebarManager.handleCommentOverlayDelete(e, n);
          break;
        case `browser-sidebar-comment-overlay-mounted`:
          this.browserSidebarManager.handleCommentOverlayMounted(
            e,
            n.conversationId,
            n.browserTabId,
            n.sessionId,
            n.surfaceSize,
            n.placementSurfaceSize,
          );
          break;
        case `browser-sidebar-comment-overlay-preview-open-changed`:
          this.browserSidebarManager.handleCommentOverlayPreviewOpenChanged(e, n);
          break;
        case `browser-sidebar-comment-overlay-tweaks-open-changed`:
          this.browserSidebarManager.handleCommentOverlayTweaksOpenChanged(e, n);
          break;
        case `browser-sidebar-comment-overlay-design-scrub-changed`:
          this.browserSidebarManager.handleCommentOverlayDesignScrubChanged(e, n);
          break;
        case `browser-sidebar-comment-overlay-close`:
          this.browserSidebarManager.handleCommentOverlayClose(
            e,
            n.conversationId,
            n.browserTabId,
            n.sessionId,
          );
          break;
        case `persisted-atom-reset`:
          this.resetPersistedAtomState();
          break;
        case `heartbeat-automations-enabled-changed`:
          this.automationSchedulerController?.setHeartbeatAutomationsEnabled(n.enabled);
          break;
        case `codex-runtimes-config-changed`:
          this.sharedObjectRepository.set(`codex_runtimes_config`, n.config);
          break;
        case `heartbeat-automation-thread-state-changed`:
          this.automationSchedulerController?.setHeartbeatThreadRendererState({
            threadId: n.threadId,
            isEligible: n.isEligible,
            collaborationMode: n.collaborationMode,
            permissions: n.permissions,
            reason: n.reason,
          });
          break;
        case `codex-app-server-restart`:
          (await this.getAppServerConnection(n.hostId).restart({
            killCodexProcess: n.killCodexProcess ?? !1,
          }),
            $().info(`Codex app-server restart requested`));
          break;
        case `debug-run-eval-control-request`: {
          let t = this.windowManager.debugWindowManager?.getOwnerWebContentsId(e.id) ?? null;
          if (t != null) {
            this.windowManager.sendMessageToWebContentsId(t, {
              type: `debug-run-eval-control-request`,
              requestId: n.requestId,
              sourceWebContentsId: e.id,
              action: n.action,
            });
            break;
          }
          let r = a.BrowserWindow.fromWebContents(e),
            i =
              r != null && this.windowManager.getPrimaryWindows().includes(r)
                ? r
                : this.windowManager.getPrimaryWindow();
          if (i == null) {
            this.windowManager.sendMessageToWebContents(e, {
              type: `debug-run-eval-control-response`,
              requestId: n.requestId,
              ok: !1,
              errorMessage: `No primary app window is available`,
            });
            break;
          }
          this.windowManager.sendMessageToWindow(i, {
            type: `debug-run-eval-control-request`,
            requestId: n.requestId,
            sourceWebContentsId: e.id,
            action: n.action,
          });
          break;
        }
        case `debug-run-eval-control-response`:
          this.windowManager.sendMessageToWebContentsId(n.sourceWebContentsId, {
            type: `debug-run-eval-control-response`,
            requestId: n.requestId,
            ok: n.ok,
            result: n.result,
            errorMessage: n.errorMessage,
          });
          break;
        case `set-primary-runtime-install-release`:
          this.windowManager.sendMessageToAllWindows(n);
          break;
        case `toggle-trace-recording`:
          await bY();
          break;
        case `log-message`: {
          let { level: e } = n;
          if (!t.x(e, this.maxLogLevel)) break;
          $().log(e, n.message, n.tags);
          break;
        }
        case `electron-app-state-snapshot-trigger`:
        case `electron-app-state-snapshot-response`:
          await this.samplerManager.handleMessage(e, n);
          break;
        case `shared-object-subscribe`: {
          this.sharedObjectSubscriberOrigins.has(e) ||
            (this.sharedObjectSubscriberOrigins.add(e),
            e.once(`destroyed`, () => {
              for (let [t, n] of this.sharedObjectSubscribers)
                (n.delete(e), n.size === 0 && this.sharedObjectSubscribers.delete(t));
            }));
          let t = this.sharedObjectSubscribers.get(n.key) ?? new Map();
          (t.set(e, (t.get(e) ?? 0) + 1), this.sharedObjectSubscribers.set(n.key, t));
          let r = this.sharedObjectRepository.get(n.key);
          this.sendSharedObjectUpdate(e, n.key, r);
          break;
        }
        case `shared-object-unsubscribe`: {
          let t = this.sharedObjectSubscribers.get(n.key);
          if (t == null) break;
          let r = (t.get(e) ?? 0) - 1;
          if (r > 0) {
            t.set(e, r);
            break;
          }
          (t.delete(e), t.size === 0 && this.sharedObjectSubscribers.delete(n.key));
          break;
        }
        case `shared-object-set`:
          this.sharedObjectRepository.set(n.key, n.value);
          break;
        case `mcp-request`:
          $().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-request`,
              requestId: String(n.request.id),
              method: n.request.method,
              originWebcontentsId: e.id,
              originHostId: n.hostId,
            },
            sensitive: {},
          });
          try {
            await this.getAppServerConnection(n.hostId).handleClientRequest(new VJ(e), n.request);
          } catch (t) {
            this.sendMessageToView(e, {
              type: `mcp-response`,
              hostId: n.hostId,
              message: {
                id: n.request.id,
                error: {
                  code: -32e3,
                  message: t instanceof Error ? t.message : `Desktop app server request failed`,
                },
              },
            });
          }
          break;
        case `thread-prewarm-start`:
          ($().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `thread-prewarm-start`,
              requestId: String(n.request.id),
              method: n.request.method,
              originWebcontentsId: e.id,
              originHostId: n.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(n.hostId).handlePrewarmThreadStart(
              new VJ(e),
              n.request,
            ));
          break;
        case `mcp-notification`:
          ($().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-notification`,
              method: n.request.method,
              originWebcontentsId: e.id,
              originHostId: n.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(n.hostId).handleClientNotification(n.request));
          break;
        case `mcp-response`:
          ($().debug(`app_server.bridge_received`, {
            safe: {
              messageType: `mcp-response`,
              requestId: String(n.response.id),
              originWebcontentsId: e.id,
              originHostId: n.hostId,
            },
            sensitive: {},
          }),
            await this.getAppServerConnection(n.hostId).handleClientResponse(n.response));
          break;
        case `fetch`:
          await this.fetchWrapper.handleRequest(e, n);
          break;
        case `cancel-fetch`:
          this.fetchWrapper.cancelRequest(e, n);
          break;
        case `fetch-stream`:
          this.fetchWrapper.handleStreamRequest(e, n);
          break;
        case `cancel-fetch-stream`:
          this.fetchWrapper.cancelStreamRequest(e, n);
          break;
        case `pending-worktree-cancel`:
          this.getWorktreeService(n.hostId).cancel(n.id);
          break;
        case `pending-worktree-create`:
          this.getWorktreeService(n.hostId).create(n.request);
          break;
        case `pending-worktree-dismiss`:
          this.getWorktreeService(n.hostId).dismiss(n.id);
          break;
        case `pending-worktree-retry`:
          this.getWorktreeService(n.hostId).retry(n.id);
          break;
        case `pending-worktree-continue`:
          this.getWorktreeService(n.hostId).continueWithoutSetup(n.id);
          break;
        case `pending-worktree-update-metadata`:
          this.getWorktreeService(n.hostId).updateMetadata(n.id, n.update);
          break;
        case `show-diff`:
          this.windowManager.sendMessageToWebContents(e, { type: `toggle-diff-panel`, open: !0 });
          break;
        case `show-plan-summary`:
          break;
        case `update-diff-if-open`:
          break;
        case `electron-add-new-workspace-root-option`:
          await this.addWorkspaceRootOption(e, !0, n.root);
          break;
        case `electron-create-new-workspace-root-option`: {
          let t = await this.createDefaultWorkspaceRoot(n.projectName ?? YY);
          await this.addWorkspaceRootOption(e, !0, t);
          break;
        }
        case `electron-pick-workspace-root-option`:
          await this.addWorkspaceRootOption(e, !1);
          break;
        case `electron-onboarding-skip-workspace`:
          await this.skipOnboardingWorkspace(e, n.projectName);
          break;
        case `electron-onboarding-pick-workspace-or-create-default`:
          await this.pickOnboardingWorkspaceOrCreateDefault(e, n.defaultProjectName);
          break;
        case `electron-update-workspace-root-options`: {
          let t = ue(n.roots, this.host);
          (r.d(this.globalState, t),
            r.f(this.globalState, (e) => {
              let n = de(e, this.host);
              if (Object.keys(n).length === 0) return;
              let r = {};
              return (
                t.forEach((e) => {
                  n[e] !== void 0 && (r[e] = n[e]);
                }),
                r
              );
            }));
          let i = r.i(this.globalState),
            a = i.filter((e) => t.includes(e));
          (a.length === 0 && t.length > 0 && (a = [t[0]]),
            (i.length !== a.length || i.some((e, t) => e !== a[t])) &&
              (r.u(this.globalState, a), e.send(R, { type: `active-workspace-roots-updated` })),
            e.send(R, { type: `workspace-root-options-updated` }));
          break;
        }
        case `electron-rename-workspace-root-option`: {
          let t = le(n.root, this.host),
            i = n.label.trim();
          if (!ue(r.s(this.globalState), this.host).includes(t)) break;
          (r.f(this.globalState, (e) => {
            let n = de(e, this.host);
            return (i ? (n[t] = i) : delete n[t], n);
          }),
            e.send(R, { type: `workspace-root-options-updated` }));
          break;
        }
        case `electron-set-active-workspace-root`:
          try {
            let t = le(n.root, this.host),
              i = r.i(this.globalState),
              a = r.s(this.globalState),
              o = r.o(this.globalState),
              s = ue(a, this.host),
              c = s.includes(t) ? s : [t, ...s],
              l = [t],
              u = de(o, this.host),
              d = !(0, TP.default)(i, l),
              f = !(0, TP.default)(a, c),
              p = !(0, TP.default)(o, u);
            if (!d && !f && !p) break;
            (d && r.u(this.globalState, l),
              f && r.d(this.globalState, c),
              p && r.f(this.globalState, () => u),
              e.send(R, { type: `active-workspace-roots-updated` }));
          } catch (e) {
            $().error(`Failed to set active workspace root`, { safe: {}, sensitive: { error: e } });
          }
          break;
        case `electron-clear-active-workspace-root`:
          if (r.i(this.globalState).length === 0) break;
          (r.u(this.globalState, []), e.send(R, { type: `active-workspace-roots-updated` }));
          break;
        case `open-in-browser`: {
          let { url: r } = n;
          if (typeof r == `string` && this.windowManager.queueCodexDeepLinkUrl(r, n.originHostId))
            break;
          if (n.useExternalBrowser === !0 || n.openTarget === `external-browser`) {
            if (typeof r == `string` && yb(r))
              try {
                await a.shell.openExternal(r);
              } catch (e) {
                $().error(`Open-in-browser failed`, { safe: {}, sensitive: { error: e } });
              }
            else $().warning(`Open-in-browser received invalid url`);
            break;
          }
          if (typeof r == `string` && gb(r))
            try {
              let i = Re(),
                o =
                  this.settingsStore?.getEffective(t.vi.openLinkInTargetPreference.key) ??
                  t.vi.openLinkInTargetPreference.default,
                s =
                  this.settingsStore?.getEffective(t.vi.openLocalUrlInTargetPreference.key) ??
                  t.vi.openLocalUrlInTargetPreference.default,
                c = nX(n.initiator),
                l = n.openTarget == null && n.disposition === `new-tab` && c,
                d = n.openTarget ?? (l ? rX(o) : o),
                f = n.openTarget ?? (l ? rX(s) : s);
              if (
                _b({
                  browserPaneEnabled: i.browserPane,
                  link: { type: `url`, url: r },
                  openLinkInTargetPreference: d,
                  openLocalUrlInTargetPreference: f,
                  webLinksDefaultInAppBrowser:
                    n.openTarget === `in-app-browser` || (i.linksDefaultInAppBrowser && c),
                })
              ) {
                e.send(R, {
                  type: `toggle-browser-panel`,
                  ...(n.disposition === `new-tab`
                    ? { browserTabId: t.rs(`manual:${(0, u.randomUUID)()}`) }
                    : {}),
                  open: !0,
                  url: r,
                  hostId: n.hostId,
                  source: n.source ?? `manual`,
                  initiator: n.initiator ?? `open_in_browser_bridge`,
                });
                break;
              }
              await a.shell.openExternal(r);
            } catch (e) {
              $().error(`Open-in-browser failed`, { safe: {}, sensitive: { error: e } });
            }
          else $().warning(`Open-in-browser received invalid url`);
          break;
        }
        case `electron-set-badge-count`:
          a.app.setBadgeCount(n.count);
          break;
        case `view-focused`:
        case `quit-app`:
        case `tray-menu-threads-changed`:
          break;
        case `keyboard-layout-map-changed`:
          IO(n.keyboardLayoutMap);
          break;
        case `app-shell-shortcut-state-changed`:
          this.windowManager.setAppShellShortcutState(e.id, {
            bottomPanelBrowserCanZoom: n.bottomPanelBrowserCanZoom,
            bottomPanelBrowserConversationId: n.bottomPanelBrowserConversationId,
            bottomPanelBrowserTabId: n.bottomPanelBrowserTabId,
            canAcceptAppshotShortcut: n.canAcceptAppshotShortcut,
            bottomPanelCanCloseActiveTab: n.bottomPanelCanCloseActiveTab,
            focusArea: n.focusArea,
            imagePreviewOpen: n.imagePreviewOpen,
            terminalFocused: n.terminalFocused,
            rightPanelBrowserCanZoom: n.rightPanelBrowserCanZoom,
            rightPanelBrowserConversationId: n.rightPanelBrowserConversationId,
            rightPanelBrowserTabId: n.rightPanelBrowserTabId,
            rightPanelCanCloseActiveTab: n.rightPanelCanCloseActiveTab,
          });
          break;
        case `subagent-thread-opened`:
          this.getAppServerConnection(n.hostId).markSubagentThreadOpened(n.conversationId);
          break;
        case `electron-window-focus-request`: {
          let t = a.BrowserWindow.fromWebContents(e);
          this.windowManager.sendMessageToWebContents(e, {
            type: `electron-window-focus-changed`,
            isFocused: t?.isFocused() ?? !1,
          });
          break;
        }
        case `electron-window-zoom-changed`:
          this.windowManager.setWindowZoom(e, n.windowZoom);
          break;
        case `worker-request`:
        case `worker-request-cancel`:
          break;
        case `navigate-in-new-editor-tab`:
        case `open-vscode-command`:
        case `open-extension-settings`:
        case `open-keyboard-shortcuts`:
        case `open-config-toml`:
        case `show-settings`:
        case `install-wsl`:
          throw Error(`"${n.type}" is not implemented in Electron.`);
        case `terminal-create`:
        case `terminal-attach`:
          await this.terminalManager.createOrAttach(e, n);
          break;
        case `terminal-write`:
          this.terminalManager.write(e, n.sessionId, n.data);
          break;
        case `terminal-run-action`:
          this.terminalManager.runAction(e, n.sessionId, { cwd: n.cwd, command: n.command });
          break;
        case `terminal-resize`:
          this.terminalManager.resize(e, n.sessionId, n.cols, n.rows);
          break;
        case `terminal-close`:
          this.terminalManager.close(e, n.sessionId);
          break;
      }
    }
    setPrimaryWindowMode(e, t) {
      let n = a.BrowserWindow.fromWebContents(e);
      if (!n || n.isDestroyed()) return;
      let r = this.windowManager.getPrimaryWindow();
      if (!r || r.isDestroyed() || r.id !== n.id) return;
      let i = $Y(t);
      if ((0, TP.default)(this.primaryWindowMode, t)) {
        i != null && this.showPrimaryWindow(n);
        return;
      }
      if (((this.primaryWindowMode = t), i != null)) {
        ((this.primaryWindowRestoreBounds ||= {
          bounds: n.getNormalBounds(),
          wasMaximized: n.isMaximized(),
          wasFullScreen: n.isFullScreen(),
        }),
          n.isFullScreen() && n.setFullScreen(!1),
          n.isMaximized() && n.unmaximize(),
          n.setResizable(!1),
          n.setMaximizable(!1),
          n.setFullScreenable(!1),
          n.setMinimumSize(i.width, i.height),
          n.setSize(i.width, i.height),
          n.center(),
          this.showPrimaryWindow(n));
        return;
      }
      if (
        (n.setResizable(!0),
        n.setMaximizable(!0),
        n.setFullScreenable(!0),
        this.primaryWindowRestoreBounds)
      ) {
        let { bounds: e, wasMaximized: t, wasFullScreen: r } = this.primaryWindowRestoreBounds;
        this.primaryWindowRestoreBounds = null;
        let i = this.windowManager.getPrimaryMinimumSize();
        n.setMinimumSize(i.width, i.height);
        let a = { ...e, width: Math.max(e.width, i.width), height: Math.max(e.height, i.height) };
        (n.setBounds(a), t && n.maximize(), r && n.setFullScreen(!0));
      }
      (this.windowManager.syncPrimaryMinimumSize(), this.showPrimaryWindow(n));
    }
    showPrimaryWindow(e) {
      e.isVisible() || (e.show(), e.focus());
    }
    flushTelemetry() {
      this.datadogLogger.flushNow();
    }
    dispose() {
      this.samplerManager.dispose();
    }
    sendMessageToOrigin(e, t) {
      this.windowManager.sendMessageToWebContents(e, t);
    }
    async getThreadRole(e, n) {
      return new Promise((r, i) => {
        let a = (0, u.randomUUID)(),
          o = setTimeout(() => {
            (this.pendingThreadRoleRequests.delete(a), i(Error(`thread-role-timeout`)));
          }, XY);
        this.pendingThreadRoleRequests.set(a, {
          originId: e.id,
          resolve: r,
          reject: i,
          timeout: o,
        });
        let s = {
          type: `thread-role-request`,
          hostId: this.hostId,
          requestId: t.$o(a),
          conversationId: n,
        };
        this.sendMessageToOrigin(e, s);
      });
    }
    requestDynamicToolsForThreadStart(e, t = !1) {
      let n = this.windowManager.getPrimaryWindow();
      return n == null
        ? Promise.resolve([])
        : new Promise((r) => {
            let i = (0, u.randomUUID)(),
              a = setTimeout(() => {
                (this.pendingDynamicToolsForThreadStartRequests.delete(i), r([]));
              }, QY);
            (this.pendingDynamicToolsForThreadStartRequests.set(i, {
              originId: n.webContents.id,
              resolve: r,
              timeout: a,
            }),
              this.sendMessageToOrigin(n.webContents, {
                type: `dynamic-tools-for-thread-start-requested`,
                hostId: this.hostId,
                requestId: i,
                featureOverrides: e,
                responseTarget: `electron`,
                threadStartKind: `default`,
                threadToolsEnabled: t,
                useDefaultFeatureOverrides: !0,
              }));
          });
    }
    forwardThreadFollowerRequest(e, n, r, i, a = XY) {
      return new Promise((o, s) => {
        let c = setTimeout(() => {
          (r.delete(n.requestId), s(Error(i)));
        }, a);
        r.set(n.requestId, { originId: e.id, resolve: o, reject: s, timeout: c });
        let l = {
          type: eX[n.method],
          hostId: this.hostId,
          requestId: t.$o(n.requestId),
          params: n.params,
        };
        this.sendMessageToOrigin(e, l);
      });
    }
    async handleThreadFollowerStartTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerStartTurnRequests,
        `thread-follower-start-turn-timeout`,
      );
    }
    async handleThreadFollowerLoadCompleteHistoryRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerLoadCompleteHistoryRequests,
        `thread-follower-load-complete-history-timeout`,
        ZY,
      );
    }
    async handleThreadFollowerCompactThreadRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerCompactThreadRequests,
        `thread-follower-compact-thread-timeout`,
      );
    }
    async handleThreadFollowerSteerTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSteerTurnRequests,
        `thread-follower-steer-turn-timeout`,
      );
    }
    async handleThreadFollowerInterruptTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerInterruptTurnRequests,
        `thread-follower-interrupt-turn-timeout`,
      );
    }
    async handleThreadFollowerUpdateThreadSettingsRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerUpdateThreadSettingsRequests,
        `thread-follower-update-thread-settings-timeout`,
      );
    }
    async handleThreadFollowerEditLastUserTurnRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerEditLastUserTurnRequests,
        `thread-follower-edit-last-user-turn-timeout`,
      );
    }
    async handleThreadFollowerCommandApprovalDecisionRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerCommandApprovalDecisionRequests,
        `thread-follower-command-approval-decision-timeout`,
      );
    }
    async handleThreadFollowerFileApprovalDecisionRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerFileApprovalDecisionRequests,
        `thread-follower-file-approval-decision-timeout`,
      );
    }
    async handleThreadFollowerPermissionsRequestApprovalResponseRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests,
        `thread-follower-permissions-request-approval-response-timeout`,
      );
    }
    async handleThreadFollowerSubmitUserInputRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSubmitUserInputRequests,
        `thread-follower-submit-user-input-timeout`,
      );
    }
    async handleThreadFollowerSubmitMcpServerElicitationRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSubmitMcpServerElicitationRequests,
        `thread-follower-submit-mcp-server-elicitation-response-timeout`,
      );
    }
    async handleThreadFollowerSetQueuedFollowUpsStateRequest(e, t) {
      return this.forwardThreadFollowerRequest(
        e,
        t,
        this.pendingThreadFollowerSetQueuedFollowUpsStateRequests,
        `thread-follower-set-queued-follow-ups-state-timeout`,
      );
    }
    rejectPendingThreadFollowerActionRequestsForOrigin(e) {
      for (let [t, n] of this.pendingThreadFollowerStartTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerStartTurnRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerLoadCompleteHistoryRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerLoadCompleteHistoryRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerCompactThreadRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerCompactThreadRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerSteerTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSteerTurnRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerInterruptTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerInterruptTurnRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerUpdateThreadSettingsRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerUpdateThreadSettingsRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerEditLastUserTurnRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerEditLastUserTurnRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerCommandApprovalDecisionRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerCommandApprovalDecisionRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerFileApprovalDecisionRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerFileApprovalDecisionRequests.delete(t));
      for (let [
        t,
        n,
      ] of this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerSubmitUserInputRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSubmitUserInputRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerSubmitMcpServerElicitationRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSubmitMcpServerElicitationRequests.delete(t));
      for (let [t, n] of this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.delete(t));
      for (let [t, n] of this.pendingThreadRoleRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.reject(Error(`webcontents-destroyed`)),
          this.pendingThreadRoleRequests.delete(t));
      for (let [t, n] of this.pendingDynamicToolsForThreadStartRequests.entries())
        n.originId === e.id &&
          (clearTimeout(n.timeout),
          n.resolve([]),
          this.pendingDynamicToolsForThreadStartRequests.delete(t));
    }
    handleThreadFollowerStartTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerStartTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower start-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerStartTurnRequests.delete(n), clearTimeout(r.timeout), t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower start-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerLoadCompleteHistoryResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerLoadCompleteHistoryRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower load-complete-history response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerLoadCompleteHistoryRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower load-complete-history response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerCompactThreadResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerCompactThreadRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower compact-thread response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerCompactThreadRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower compact-thread response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSteerTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSteerTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower steer-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerSteerTurnRequests.delete(n), clearTimeout(r.timeout), t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower steer-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerInterruptTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerInterruptTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower interrupt-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerInterruptTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower interrupt-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerUpdateThreadSettingsResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerUpdateThreadSettingsRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower update-thread-settings response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerUpdateThreadSettingsRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower update-thread-settings response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerEditLastUserTurnResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerEditLastUserTurnRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower edit-last-user-turn response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerEditLastUserTurnRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower edit-last-user-turn response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerCommandApprovalDecisionResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerCommandApprovalDecisionRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower command-approval response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerCommandApprovalDecisionRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower command-approval response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerFileApprovalDecisionResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerFileApprovalDecisionRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower file-approval response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerFileApprovalDecisionRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower file-approval response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerPermissionsRequestApprovalResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower permissions-request response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerPermissionsRequestApprovalResponseRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower permissions-request response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSubmitUserInputResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSubmitUserInputRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower submit-user-input response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerSubmitUserInputRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower submit-user-input response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSubmitMcpServerElicitationResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSubmitMcpServerElicitationRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower submit-mcp-server-elicitation response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerSubmitMcpServerElicitationRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower submit-mcp-server-elicitation response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadFollowerSetQueuedFollowUpsStateResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread follower queued-follow-ups response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if (
        (this.pendingThreadFollowerSetQueuedFollowUpsStateRequests.delete(n),
        clearTimeout(r.timeout),
        t.error)
      ) {
        r.reject(Error(t.error));
        return;
      }
      if (!t.result) {
        r.reject(Error(`Missing thread follower queued-follow-ups response`));
        return;
      }
      r.resolve(t.result);
    }
    handleThreadRoleResponse(e, t) {
      let n = String(t.requestId),
        r = this.pendingThreadRoleRequests.get(n);
      if (!r || r.originId !== e.id) {
        $().warning(`Unknown thread role response requestId`, {
          safe: { requestId: n },
          sensitive: {},
        });
        return;
      }
      if ((this.pendingThreadRoleRequests.delete(n), clearTimeout(r.timeout), t.error)) {
        r.reject(Error(t.error));
        return;
      }
      r.resolve(t.role);
    }
    handleDynamicToolsForThreadStartResponse(e, t) {
      let n = this.pendingDynamicToolsForThreadStartRequests.get(t.requestId);
      if (!n || n.originId !== e.id) {
        $().warning(`Unknown dynamic tools response requestId`, {
          safe: { requestId: t.requestId },
          sensitive: {},
        });
        return;
      }
      (this.pendingDynamicToolsForThreadStartRequests.delete(t.requestId),
        clearTimeout(n.timeout),
        n.resolve(t.dynamicTools));
    }
    sendPersistedAtomState(e) {
      let t = this.globalState.get(`electron-persisted-atom-state`) ?? {};
      this.sendMessageToView(e, { type: `persisted-atom-sync`, state: t });
    }
    updatePersistedAtomState(e, t) {
      let n = { ...(this.globalState.get(`electron-persisted-atom-state`) ?? {}) };
      (t === void 0 ? delete n[e] : (n[e] = t),
        this.globalState.set(`electron-persisted-atom-state`, n),
        this.broadcastPersistedAtomUpdate(e, t));
    }
    broadcastPersistedAtomUpdate(e, t) {
      let n = t === void 0;
      this.windowManager.sendMessageToAllWindows({
        type: `persisted-atom-updated`,
        key: e,
        value: n ? null : t,
        deleted: n,
      });
    }
    resetPersistedAtomState() {
      (this.globalState.set(`electron-persisted-atom-state`, {}),
        this.windowManager.sendMessageToAllWindows({ type: `persisted-atom-sync`, state: {} }));
    }
    async skipOnboardingWorkspace(e, t) {
      let n = null;
      try {
        ((n = await this.createDefaultWorkspaceRoot(t ?? YY)),
          this.setOnboardingWorkspace(e, n),
          e.send(R, { type: `electron-onboarding-skip-workspace-result`, success: !0, root: n }),
          this.navigateToOnboardingWorkspace(e));
      } catch (t) {
        $().error(`Failed to skip onboarding workspace selection`, {
          safe: {},
          sensitive: { error: t },
        });
        let r = t instanceof Error ? t.message : String(t);
        e.send(R, {
          type: `electron-onboarding-skip-workspace-result`,
          success: !1,
          root: n ?? void 0,
          error: r,
        });
      }
    }
    async pickOnboardingWorkspaceOrCreateDefault(e, t) {
      let n = null,
        r = `picked`;
      try {
        ((n = await this.pickLocalWorkspaceRoot(e)),
          (n ??= ((r = `created_default`), await this.createDefaultWorkspaceRoot(t))),
          this.setOnboardingWorkspace(e, n),
          e.send(R, {
            type: `electron-onboarding-pick-workspace-or-create-default-result`,
            success: !0,
            source: r,
            root: n,
          }),
          this.navigateToOnboardingWorkspace(e));
      } catch (t) {
        $().error(`Failed onboarding workspace pick/create`, { safe: {}, sensitive: { error: t } });
        let i = t instanceof Error ? t.message : String(t);
        e.send(R, {
          type: `electron-onboarding-pick-workspace-or-create-default-result`,
          success: !1,
          source: r,
          root: n ?? void 0,
          error: i,
        });
      }
    }
    setOnboardingWorkspace(e, t) {
      let n = ue([t, ...r.s(this.globalState)], this.host);
      (r.d(this.globalState, n),
        r.c(this.globalState, t),
        r.f(this.globalState, (e) => de(e, this.host)),
        e.send(R, { type: `workspace-root-options-updated` }),
        r.u(this.globalState, [le(t, this.host)]),
        e.send(R, { type: `active-workspace-roots-updated` }));
    }
    navigateToOnboardingWorkspace(e) {
      e.send(R, {
        type: `navigate-to-route`,
        path: `/`,
        state: { focusComposerNonce: Date.now() },
      });
    }
    async pickLocalWorkspaceRoot(e) {
      if (this.host.id !== `local`)
        throw Error(`Workspace picker is only supported for local hosts`);
      let t = [`openDirectory`, `createDirectory`];
      (await this.shouldShowHiddenFilesInPicker()) && t.push(`showHiddenFiles`);
      let n = { properties: t, title: `Select Project Root` },
        r = a.BrowserWindow.fromWebContents(e),
        i = r == null ? await a.dialog.showOpenDialog(n) : await a.dialog.showOpenDialog(r, n),
        o = i.filePaths?.[0];
      return i.canceled || !o ? null : this.resolveWorkspaceRoot(o);
    }
    sanitizeDefaultProjectName(e) {
      let t = s.basename(e).trim();
      return t.length === 0 || t === `.` || t === `..` ? YY : t;
    }
    async findAvailableDefaultWorkspaceRoot(e, t, n) {
      let r = n == null ? t : `${t} ${n}`,
        i = s.join(e, r);
      if (!(await this.folderExists(i))) return i;
      let a = n == null ? 2 : n + 1;
      return this.findAvailableDefaultWorkspaceRoot(e, t, a);
    }
    async createDefaultWorkspaceRoot(e) {
      if (t.va(this.host))
        throw Error(`Creating a default workspace is only supported for local hosts`);
      let n = this.sanitizeDefaultProjectName(e),
        i = await this.findAvailableDefaultWorkspaceRoot(a.app.getPath(`documents`), n, void 0);
      (await t.jr.mkdir(i, { recursive: !0 }, this.appServerClient),
        this.getSettingsStore().getEffective(t.pi.conversationDetailMode.key) !== `STEPS_PROSE` &&
          (await this.initializeDefaultWorkspace(i)));
      let o = de(r.o(this.globalState), this.host),
        c = ue(r.s(this.globalState), this.host).some(
          (e) => (o[e]?.trim() || s.basename(e).trim()) === n,
        );
      return (
        s.basename(i) !== n &&
          !c &&
          r.f(this.globalState, (e) => ({ ...de(e, this.host), [i]: n })),
        i
      );
    }
    async folderExists(e) {
      try {
        return (await t.jr.stat(e, this.appServerClient), !0);
      } catch {
        return !1;
      }
    }
    async initializeDefaultWorkspace(e) {
      try {
        let n = t.ht(this.appServerClient);
        if (!(await t.ft(e, [`version`], n)).success) return;
        let r = await t.ft(e, [`init`], n);
        if (r.success) {
          this.gitManager.invalidateStableMetadata();
          return;
        }
        let i = r.stderr || r.stdout || `Failed to run git init`;
        $().warning(`Failed to initialize default workspace git repository`, {
          safe: {},
          sensitive: { message: i },
        });
      } catch (e) {
        $().warning(`Failed to initialize default workspace git repository`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async addWorkspaceRootOption(e, t = !0, n) {
      if (this.host.id !== `local`) {
        this.windowManager.sendMessageToWebContents(e, {
          type: `remote-workspace-root-requested`,
          mode: t ? `add` : `pick`,
          host: this.host.display_name,
          hostId: this.host.id,
          setActive: t ? !0 : void 0,
        });
        return;
      }
      let i = null;
      if (
        ((i = n ? await this.resolveWorkspaceRoot(n) : await this.pickLocalWorkspaceRoot(e)),
        i == null)
      )
        return;
      if (!t) {
        e.send(R, { type: `workspace-root-option-picked`, root: I(i, this.shouldUseWslPaths()) });
        return;
      }
      let a = ue(r.s(this.globalState), this.host),
        o = a.includes(i) ? a : [i, ...a];
      (o.length !== a.length &&
        (r.d(this.globalState, o),
        r.c(this.globalState, i),
        e.send(R, { type: `workspace-root-options-updated` })),
        r.u(this.globalState, [i]),
        e.send(R, { type: `active-workspace-roots-updated` }),
        e.send(R, { type: `workspace-root-option-added`, root: I(i, this.shouldUseWslPaths()) }),
        e.send(R, {
          type: `navigate-to-route`,
          path: `/`,
          state: { focusComposerNonce: Date.now() },
        }));
    }
    async resolveWorkspaceRoot(e) {
      try {
        let t = le(e, this.host);
        return (await (0, p.stat)(t)).isDirectory() ? s.resolve(t) : null;
      } catch (e) {
        return (
          $().warning(`Failed to stat workspace root`, { safe: {}, sensitive: { error: e } }), null
        );
      }
    }
    async shouldShowHiddenFilesInPicker() {
      return (await this.readHiddenFilesPreference()) ?? !1;
    }
    async readHiddenFilesPreference() {
      return process.platform === `darwin` ? this.readMacHiddenFilesPreference() : null;
    }
    async readMacHiddenFilesPreference() {
      let e = await this.readCommandOutput([
        `defaults`,
        `read`,
        `com.apple.finder`,
        `AppleShowAllFiles`,
      ]);
      if (e == null) return null;
      let t = e.trim().toLowerCase();
      return t === `1` || t === `true` || t === `yes`
        ? !0
        : t === `0` || t === `false` || t === `no`
          ? !1
          : null;
    }
    async readCommandOutput(e) {
      let n = new AbortController(),
        r = !1,
        i = setTimeout(() => {
          ((r = !0), n.abort());
        }, JY);
      try {
        let { stdout: i, code: a } = await t.An({ args: e, signal: n.signal }).wait();
        return r || a !== 0 ? null : i;
      } catch (e) {
        return (
          r ||
            $().warning(`Failed to read hidden files preference`, {
              safe: {},
              sensitive: { error: e },
            }),
          null
        );
      } finally {
        clearTimeout(i);
      }
    }
    revealWindowsReviewRequest(e, n) {
      if (!iX(n, this.hasActiveComputerUseTurn)) return;
      let r = this.getPrimaryWindow();
      r != null &&
        ($().info(`[windows-review-request] reveal requested`, {
          safe: {
            conversationId: n.params.threadId,
            hostId: e,
            method: n.method,
            requestId: n.id,
            wasMinimized: r.isMinimized(),
          },
          sensitive: {},
        }),
        r.webContents.isDestroyed() ||
          this.windowManager.sendMessageToWebContents(r.webContents, {
            type: `navigate-to-route`,
            path: t.Bi(t.Qo(n.params.threadId)),
          }),
        this.restoreShowAndFocusWindow(r, { stealFocus: !0 }));
    }
    getPrimaryWindow() {
      let e = this.windowManager.getPrimaryWindow();
      return e == null || e.isDestroyed() ? null : e;
    }
    getBrowserSidebarOwner(e) {
      let t = a.BrowserWindow.fromWebContents(e);
      return t != null && this.windowManager.getPrimaryWindows().includes(t)
        ? e
        : (this.getPrimaryWindow()?.webContents ?? null);
    }
    restoreShowAndFocusWindow(e, { stealFocus: t }) {
      (e.isMinimized() && e.restore(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!0),
        e.show(),
        e.focus(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!1));
    }
    updatePowerSaveBlocker(e, t, n) {
      let r = e.id;
      (this.powerSaveTrackedWebContentsIds.has(r) ||
        (this.powerSaveTrackedWebContentsIds.add(r),
        e.once(`destroyed`, () => {
          (this.powerSaveTrackedWebContentsIds.delete(r),
            this.powerSaveBlockingWebContentsIds.delete(r),
            this.pluggedInRemoteControlPowerSaveWebContentsIds.delete(r),
            this.syncPowerSaveBlocker());
        })),
        t
          ? this.powerSaveBlockingWebContentsIds.add(r)
          : this.powerSaveBlockingWebContentsIds.delete(r),
        n
          ? this.pluggedInRemoteControlPowerSaveWebContentsIds.add(r)
          : this.pluggedInRemoteControlPowerSaveWebContentsIds.delete(r),
        this.syncPowerSaveBlocker());
    }
    syncPowerSaveBlocker() {
      let e =
        this.powerSaveBlockingWebContentsIds.size > 0 ||
        (!a.powerMonitor.isOnBatteryPower() &&
          this.pluggedInRemoteControlPowerSaveWebContentsIds.size > 0);
      if (e && this.powerSaveBlockerId == null) {
        this.powerSaveBlockerId = a.powerSaveBlocker.start(`prevent-app-suspension`);
        return;
      }
      !e &&
        this.powerSaveBlockerId != null &&
        (a.powerSaveBlocker.stop(this.powerSaveBlockerId), (this.powerSaveBlockerId = null));
    }
    async sendCustomPrompts(e) {
      let n = await t.u();
      this.sendMessageToView(e, { type: `custom-prompts-updated`, prompts: n });
    }
    sendMessageToView(e, t) {
      e.isDestroyed() || e.send(R, t);
    }
    sendSharedObjectUpdate(e, t, n) {
      e.isDestroyed() ||
        this.sendMessageToView(e, { type: `shared-object-updated`, key: t, value: n });
    }
    shouldUseWslPaths() {
      return t.Br(this.host);
    }
    getSettingsStore() {
      if (this.settingsStore == null) throw Error(`Settings store unavailable`);
      return this.settingsStore;
    }
  };
function nX(e) {
  return e === `markdown_link_click` || e === `mcp_app_resource`;
}
function rX(e) {
  switch (e) {
    case `external-browser`:
      return `in-app-browser`;
    case `in-app-browser`:
      return `external-browser`;
  }
}
function iX(e, t) {
  return process.platform !== `win32` ||
    (e.method !== `item/commandExecution/requestApproval` &&
      e.method !== `mcpServer/elicitation/request`) ||
    e.params.turnId == null
    ? !1
    : t({ sessionId: e.params.threadId, turnId: e.params.turnId });
}
var aX = t.Gr(`electron-sampler`),
  oX = 5,
  sX = 5,
  cX = 5,
  lX = 5,
  uX = 5,
  dX = class {
    previousDescendantProcessSnapshot = null;
    previousRendererWorkingSetKbTotal = null;
    constructor(e) {
      this.options = e;
    }
    async collectSnapshotFields(e) {
      let t = process.memoryUsage(),
        n = {
          main_process_rss_bytes: t.rss,
          main_process_heap_total_bytes: t.heapTotal,
          main_process_heap_used_bytes: t.heapUsed,
          main_process_external_bytes: t.external,
          main_process_array_buffers_bytes: t.arrayBuffers,
          system_free_memory_bytes: (0, o.freemem)(),
          renderer_webcontents_id: e.id,
        },
        r = e.getOSProcessId();
      try {
        let t = a.app.getAppMetrics(),
          i = t.find((e) => e.pid === process.pid),
          o = t.find((e) => e.pid === r);
        (i && this.addMainProcessFields(n, i),
          o && this.addRendererMemoryFields(n, o),
          this.addRendererProcessAggregateFields(n, e, t));
      } catch (e) {
        aX().warning(`Failed to collect renderer process memory snapshot`, {
          safe: {},
          sensitive: { error: e },
        });
      }
      let i = this.options.gitWorkerInvocationSampler.getSnapshot();
      return (
        (n.git_worker_invocations_total = i.totalInvocations),
        (n.git_worker_invocations_last_30s = i.invocationsLast30s),
        await this.addChildProcessFields(n),
        n
      );
    }
    addMainProcessFields(e, t) {
      let n = t.cpu;
      n && `percentCPUUsage` in n && (e.main_process_cpu_percent = n.percentCPUUsage);
    }
    addRendererMemoryFields(e, t) {
      let n = t.cpu;
      n && `percentCPUUsage` in n && (e.renderer_process_cpu_percent = n.percentCPUUsage);
      let r = t.memory;
      r &&
        (`workingSetSize` in r && (e.renderer_process_working_set_kb = r.workingSetSize),
        `peakWorkingSetSize` in r &&
          (e.renderer_process_peak_working_set_kb = r.peakWorkingSetSize),
        `privateBytes` in r && (e.renderer_process_private_bytes_kb = r.privateBytes),
        `sharedBytes` in r && (e.renderer_process_shared_bytes_kb = r.sharedBytes));
    }
    addRendererProcessAggregateFields(e, t, n) {
      let r = a.webContents.getAllWebContents().filter((e) => !e.isDestroyed()),
        i = new Map();
      for (let e of r) {
        let t = e.getOSProcessId();
        if (t <= 0) continue;
        let n = i.get(t) ?? [];
        (n.push(e), i.set(t, n));
      }
      let o = new Map(n.map((e) => [e.pid, e])),
        s = [];
      for (let [e, n] of i) {
        let r = o.get(e)?.memory;
        r == null ||
          !(`workingSetSize` in r) ||
          s.push({
            hasCurrentWebContents: n.some((e) => e.id === t.id),
            peakWorkingSetKb: `peakWorkingSetSize` in r ? r.peakWorkingSetSize : null,
            webContentsCount: n.length,
            workingSetKb: r.workingSetSize,
          });
      }
      let c = s.reduce((e, t) => e + t.workingSetKb, 0);
      ((e.renderer_process_count_total = s.length),
        (e.renderer_webcontents_count_total = r.length),
        (e.renderer_process_working_set_kb_total = c),
        this.previousRendererWorkingSetKbTotal != null &&
          (e.renderer_process_working_set_kb_delta_since_last_snapshot =
            c - this.previousRendererWorkingSetKbTotal),
        (this.previousRendererWorkingSetKbTotal = c),
        s
          .sort((e, t) => t.workingSetKb - e.workingSetKb)
          .slice(0, uX)
          .forEach((t, n) => {
            let r = n + 1;
            ((e[`renderer_process_rank_${r}_working_set_kb`] = t.workingSetKb),
              (e[`renderer_process_rank_${r}_webcontents_count`] = t.webContentsCount),
              (e[`renderer_process_rank_${r}_has_current_webcontents`] = t.hasCurrentWebContents),
              t.peakWorkingSetKb != null &&
                (e[`renderer_process_rank_${r}_peak_working_set_kb`] = t.peakWorkingSetKb));
          }));
    }
    async addChildProcessFields(e) {
      try {
        let r = await t.Cn(process.pid),
          i = t.Tn(r),
          a = i.byKind.find((e) => e.kind === `app_server`) ?? null,
          o = i.byKind.find((e) => e.kind === `git`) ?? null;
        ((e.host_child_process_count_total = i.totalCount),
          (e.host_child_process_rss_kb_total = i.totalRssKb),
          (e.host_child_process_count_direct = i.directCount),
          (e.host_child_process_rss_kb_direct = i.directRssKb),
          (e.host_descendant_process_count_total = i.totalCount),
          (e.host_descendant_process_rss_kb_total = i.totalRssKb),
          (e.host_descendant_process_count_direct = i.directCount),
          (e.host_descendant_process_rss_kb_direct = i.directRssKb),
          (e.host_descendant_process_max_depth = r.reduce((e, t) => Math.max(e, t.depth), 0)),
          (e.host_child_git_process_count = o?.count ?? 0),
          (e.host_child_git_process_rss_kb = o?.rssKb ?? 0),
          (e.host_child_app_server_process_count = a?.count ?? 0),
          (e.host_child_app_server_rss_kb = a?.rssKb ?? 0),
          i.oldestAgeSeconds != null &&
            ((e.host_child_process_oldest_age_seconds = i.oldestAgeSeconds),
            (e.host_descendant_process_oldest_age_seconds = i.oldestAgeSeconds)),
          this.previousDescendantProcessSnapshot != null &&
            ((e.host_descendant_process_count_delta_since_last_snapshot =
              i.totalCount - this.previousDescendantProcessSnapshot.count),
            (e.host_descendant_process_rss_kb_delta_since_last_snapshot =
              i.totalRssKb - this.previousDescendantProcessSnapshot.rssKb)),
          (this.previousDescendantProcessSnapshot = { count: i.totalCount, rssKb: i.totalRssKb }),
          i.byKind.slice(0, oX).forEach((t, n) => {
            let r = n + 1;
            ((e[`host_descendant_process_kind_${r}`] = t.kind),
              (e[`host_descendant_process_kind_${r}_count`] = t.count),
              (e[`host_descendant_process_kind_${r}_rss_kb`] = t.rssKb),
              (e[`host_descendant_process_kind_${r}_direct_count`] = t.directCount),
              (e[`host_descendant_process_kind_${r}_direct_rss_kb`] = t.directRssKb));
          }));
        for (let t of i.byKind)
          ((e[`host_descendant_${t.kind}_process_count`] = t.count),
            (e[`host_descendant_${t.kind}_rss_kb`] = t.rssKb),
            (e[`host_descendant_${t.kind}_direct_process_count`] = t.directCount),
            (e[`host_descendant_${t.kind}_direct_rss_kb`] = t.directRssKb));
        (n.i.isInternal(this.options.buildFlavor) && fX(e, r),
          pX(e, r, n.i.isInternal(this.options.buildFlavor)));
        let s = new Map(r.map((e) => [e.pid, e.kind]));
        r.filter((e) => e.rssKb != null)
          .sort((e, t) => (t.rssKb ?? 0) - (e.rssKb ?? 0))
          .slice(0, cX)
          .forEach((t, r) => {
            let i = r + 1;
            ((e[`host_descendant_process_rank_${i}_kind`] = t.kind),
              (e[`host_descendant_process_rank_${i}_rss_kb`] = t.rssKb ?? 0),
              (e[`host_descendant_process_rank_${i}_depth`] = t.depth),
              (e[`host_descendant_process_rank_${i}_root_kind`] = s.get(t.rootChildPid) ?? t.kind),
              n.i.isInternal(this.options.buildFlavor) &&
                (e[`host_descendant_process_rank_${i}_command_name`] = mX(t.command)),
              t.ageSeconds != null &&
                (e[`host_descendant_process_rank_${i}_age_seconds`] = t.ageSeconds));
          });
      } catch (e) {
        aX().warning(`Failed to collect child process snapshot`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
  };
function fX(e, t) {
  let n = new Map();
  for (let e of t) {
    let t = mX(e.command),
      r = n.get(t) ?? { commandName: t, count: 0, rssKb: 0 };
    ((r.count += 1), (r.rssKb += e.rssKb ?? 0), n.set(t, r));
  }
  Array.from(n.values())
    .sort((e, t) =>
      t.rssKb === e.rssKb
        ? t.count === e.count
          ? e.commandName.localeCompare(t.commandName)
          : t.count - e.count
        : t.rssKb - e.rssKb,
    )
    .slice(0, sX)
    .forEach((t, n) => {
      let r = n + 1;
      ((e[`host_descendant_process_command_${r}`] = t.commandName),
        (e[`host_descendant_process_command_${r}_count`] = t.count),
        (e[`host_descendant_process_command_${r}_rss_kb`] = t.rssKb));
    });
}
function pX(e, t, n) {
  let r = new Map(t.filter((e) => e.depth === 1).map((e) => [e.pid, e])),
    i = new Map();
  for (let e of t) {
    let t = r.get(e.rootChildPid);
    if (t == null) continue;
    let n = i.get(e.rootChildPid) ?? {
      commandName: mX(t.command),
      count: 0,
      kind: t.kind,
      rssKb: 0,
    };
    ((n.count += 1), (n.rssKb += e.rssKb ?? 0), i.set(e.rootChildPid, n));
  }
  Array.from(i.values())
    .sort((e, t) =>
      t.rssKb === e.rssKb
        ? t.count === e.count
          ? e.kind.localeCompare(t.kind)
          : t.count - e.count
        : t.rssKb - e.rssKb,
    )
    .slice(0, lX)
    .forEach((t, r) => {
      let i = r + 1;
      ((e[`host_descendant_root_${i}_kind`] = t.kind),
        (e[`host_descendant_root_${i}_process_count`] = t.count),
        (e[`host_descendant_root_${i}_rss_kb`] = t.rssKb),
        n && (e[`host_descendant_root_${i}_command_name`] = t.commandName));
    });
}
function mX(e) {
  let t = /^(?:"([^"]+)"|'([^']+)'|(\S+))/.exec(e.trim()),
    n = t?.[1] ?? t?.[2] ?? t?.[3];
  return n == null
    ? `unknown`
    : (0, s.basename)(n.replaceAll(`\\`, `/`)).replace(/^\((.+)\)$/, `$1`);
}
function hX() {
  return {
    watchMetaRelease: () =>
      new Promise((e) => {
        _O([`command`], e) ?? e();
      }),
  };
}
var gX = `input-monitoring-permission.node`,
  _X = `input_monitoring_permission.node`,
  vX = (0, C.createRequire)(__filename);
function yX({ addon: e, platform: n = process.platform } = {}) {
  if (n !== `darwin`) return `unavailable`;
  try {
    return (e ?? bX()).hasInputMonitoringPermission() ? `granted` : `denied`;
  } catch (e) {
    return (
      t
        .Ur()
        .warning(`Failed to check input monitoring access`, { safe: {}, sensitive: { error: e } }),
      `unavailable`
    );
  }
}
function bX() {
  let e = xX().find(l.existsSync);
  if (e == null) throw Error(`Input Monitoring permission addon not found`);
  return vX(e);
}
function xX() {
  let e = s.default.join(
      a.app.getAppPath(),
      `native`,
      `input-monitoring-permission-addon`,
      `build`,
      `Release-${process.arch}`,
      _X,
    ),
    t = Ze({ env: process.env, resourcesPath: process.resourcesPath });
  return t == null ? [e] : [s.default.join(t, `native`, gX), e];
}
var SX = new Map([
  [`image/gif`, `gif`],
  [`image/jpeg`, `jpg`],
  [`image/png`, `png`],
  [`image/webp`, `webp`],
]);
async function CX({ bytes: e, mimeType: t }) {
  let n = SX.get(t);
  if (n == null) return null;
  let r = (0, s.join)((0, o.tmpdir)(), `codex-clipboard-${(0, u.randomUUID)()}.${n}`);
  return (await (0, p.writeFile)(r, e, { flag: `wx`, mode: 384 }), r);
}
var wX = t.Gr(`install-primary-runtime`),
  TX = 3600 * 1e3,
  EX = 3e4,
  DX = new Map(),
  OX = Promise.resolve();
function kX() {
  let e = !1;
  for (let { controller: t } of DX.values()) (t.abort(), (e = !0));
  return e;
}
function AX(e) {
  return typeof e != `object` || !e
    ? !1
    : Object.entries(e).some(([e, t]) => e === `workspace_dependencies` && t === !0);
}
async function jX(e) {
  let t = async (n) => {
    let r = await e.sendAppServerRequest(`experimentalFeature/list`, { cursor: n, limit: 100 });
    return r.data.some((e) => e.name === `workspace_dependencies` && e.enabled === !0)
      ? !0
      : r.nextCursor == null
        ? !1
        : t(r.nextCursor);
  };
  return t(null);
}
var MX = class {
  startupChecked = !1;
  nextUpdateAtMs = null;
  running = !1;
  constructor(e) {
    this.options = e;
  }
  shouldPoll() {
    return this.getStaticDisabledReason() == null;
  }
  shouldTickForSharedObjectUpdate(e, t) {
    return (e === `statsig_default_enable_features` && AX(t)) || e === `codex_runtimes_config`;
  }
  async getStatus() {
    let e = await this.getDisabledReason();
    return {
      disabledReason: e,
      enabled: e == null,
      isRunning: this.running,
      nextRunAt: this.nextUpdateAtMs,
      startupChecked: this.startupChecked,
    };
  }
  async pollIfDue() {
    if (this.getStaticDisabledReason() != null) return;
    let e = Date.now(),
      t = this.nextUpdateAtMs != null && e >= this.nextUpdateAtMs;
    if (!(this.startupChecked && !t) && !this.running) {
      this.running = !0;
      try {
        if ((await this.getDisabledReason()) != null) {
          ((this.startupChecked = !0), (this.nextUpdateAtMs = this.getNextUpdateAtMs(e)));
          return;
        }
        let n = `latest`,
          r = await this.options.getCacheStatus({
            release: n,
            runtimeInstallConfig: this.getInstallConfig(),
            target: this.options.getTarget(),
          });
        if (!this.startupChecked) {
          if (
            ((this.startupChecked = !0),
            (this.nextUpdateAtMs = this.getNextUpdateAtMs(e)),
            r.status !== `missing`)
          )
            return;
        } else if (!t) return;
        if (
          ((this.nextUpdateAtMs = this.getNextUpdateAtMs(e)),
          r.status === `current` || r.status === `unsupported`)
        )
          return;
        await this.installUpdate(n);
      } catch (e) {
        wX().warning(`primary_runtime_update_poll_failed`, { safe: {}, sensitive: { error: e } });
      } finally {
        this.running = !1;
      }
    }
  }
  async runNow(e) {
    let t = await this.getDisabledReason();
    if (t != null)
      return { bundleVersion: null, nextRunAt: this.nextUpdateAtMs, reason: t, status: `skipped` };
    if (this.running)
      return {
        bundleVersion: null,
        nextRunAt: this.nextUpdateAtMs,
        reason: `already-running`,
        status: `skipped`,
      };
    this.running = !0;
    try {
      let t = await this.options.getCacheStatus({
        release: e,
        runtimeInstallConfig: this.getInstallConfig(),
        target: this.options.getTarget(),
      });
      if (t.status === `current`)
        return {
          bundleVersion: t.installedBundleVersion,
          nextRunAt: this.nextUpdateAtMs,
          reason: `current`,
          status: `already-current`,
        };
      if (t.status === `unsupported`)
        return {
          bundleVersion: null,
          nextRunAt: this.nextUpdateAtMs,
          reason: `unsupported-windows-version`,
          status: `skipped`,
        };
      let n = await this.installUpdate(e);
      return {
        bundleVersion: n.bundleVersion,
        nextRunAt: this.nextUpdateAtMs,
        reason: n.status === `already-current` ? `current` : null,
        status: n.status,
      };
    } finally {
      this.running = !1;
    }
  }
  getStaticDisabledReason() {
    return this.options.hostId === `local`
      ? this.options.sharedObjectRepository?.get(`codex_runtimes_config`) == null
        ? `runtime-config-missing`
        : AX(this.options.sharedObjectRepository?.get(`statsig_default_enable_features`))
          ? null
          : `feature-gate-disabled`
      : `not-local-host`;
  }
  async getDisabledReason() {
    let e = this.getStaticDisabledReason();
    if (e != null) return e;
    try {
      return (await this.options.isWorkspaceDependenciesFeatureEnabled())
        ? null
        : `feature-gate-disabled`;
    } catch (e) {
      return (
        wX().warning(`workspace_dependencies_feature_check_failed`, {
          safe: {},
          sensitive: { error: e },
        }),
        `feature-gate-disabled`
      );
    }
  }
  getInstallConfig() {
    return this.options.sharedObjectRepository?.get(`codex_runtimes_config`)?.runtimes[t.f] ?? t.wi;
  }
  getNextUpdateAtMs(e) {
    let t = this.getJitterMs(),
      n = Math.floor(e / TX) * TX + t;
    return n > e ? n : n + TX;
  }
  getJitterMs() {
    let e = this.options.globalState.get(t.Fo.PRIMARY_RUNTIME_UPDATE_JITTER_MS);
    if (typeof e == `number` && Number.isInteger(e) && e >= 0 && e < TX) return e;
    let n = Math.floor(Math.random() * TX);
    return (this.options.globalState.set(t.Fo.PRIMARY_RUNTIME_UPDATE_JITTER_MS, n), n);
  }
  async installUpdate(e) {
    return this.options.installForConnection({
      connection: this.options.appServerConnectionRegistry.getConnection(this.options.hostId),
      errorReporter: this.options.errorReporter,
      onProgress: this.options.onProgress,
      release: e,
      runtimeConfig: this.options.sharedObjectRepository?.get(`codex_runtimes_config`),
    });
  }
};
async function NX({
  connection: e,
  errorReporter: n,
  installRuntime: r = t.v,
  onProgress: i,
  runtimeConfig: a,
  release: o,
  target: s = t.m(),
}) {
  let c = a?.runtimes[t.f] ?? t.wi,
    l = `${s.platform}-${s.arch}`,
    u = a?.runtimes[t.f] != null;
  wX().info(`primary_runtime_install_started`, {
    safe: { hasConfiguredRuntime: u, release: o, target: l },
  });
  let d;
  try {
    d = await UX({ installRuntime: r, onProgress: i, release: o, runtimeOptions: c, target: s });
  } catch (e) {
    throw (
      VX(e) ||
        HX({ error: e, errorReporter: n, hasConfiguredRuntime: u, release: o, targetKey: l }),
      e
    );
  }
  try {
    (i?.({
      bundleVersion: d.bundleVersion,
      downloadedBytes: null,
      errorMessage: null,
      phase: `configuring`,
      release: o,
      totalBytes: null,
    }),
      await FX(e, d.paths),
      await IX(e, d.paths),
      PX({ connection: e, release: o, targetKey: l }));
  } catch (e) {
    throw (
      VX(e) ||
        HX({ error: e, errorReporter: n, hasConfiguredRuntime: u, release: o, targetKey: l }),
      i?.({
        bundleVersion: d.bundleVersion,
        downloadedBytes: null,
        errorMessage: e instanceof Error ? e.message : String(e),
        phase: `error`,
        release: o,
        totalBytes: null,
      }),
      e
    );
  }
  return (
    wX().info(`primary_runtime_install_finished`, {
      safe: { bundleVersion: d.bundleVersion, release: o, status: d.status, target: l },
    }),
    i?.({
      bundleVersion: d.bundleVersion,
      downloadedBytes: null,
      errorMessage: null,
      phase: `ready`,
      release: o,
      totalBytes: null,
    }),
    { bundleVersion: d.bundleVersion, status: d.status }
  );
}
function PX({ connection: e, release: t, targetKey: n }) {
  e.sendAppServerRequest(`skills/list`, { forceReload: !0 }).catch((e) => {
    wX().warning(`primary_runtime_skills_reload_failed`, {
      safe: { release: t, target: n },
      sensitive: { error: e },
    });
  });
}
async function FX(e, t) {
  if (t.bundledPluginMarketplacePaths.length === 0) return;
  let n = (
    await Promise.all(
      t.bundledPluginMarketplacePaths.map(async (t) => ({
        marketplaceRoot: t,
        synced: await vs({ appServerConnection: e, marketplaceRoot: t }),
      })),
    )
  ).find((e) => !e.synced);
  if (n != null)
    throw Error(`Failed to sync primary runtime bundled plugin marketplace: ${n.marketplaceRoot}`);
}
async function IX(e, n) {
  if (n.skillsToRemove.length === 0 && n.bundledSkillPaths.length === 0) return;
  let r = await e.codexHome();
  if (
    (await LX({ codexHome: r, skillsToRemove: n.skillsToRemove }), n.bundledSkillPaths.length === 0)
  )
    return;
  let i = s.default.join(r, `skills`, t.f);
  (await p.default.rm(i, { force: !0, recursive: !0 }),
    await p.default.mkdir(i, { recursive: !0 }),
    await Promise.all(
      n.bundledSkillPaths.map((e) =>
        p.default.cp(
          s.default.dirname(e),
          s.default.join(i, s.default.basename(s.default.dirname(e))),
          { recursive: !0 },
        ),
      ),
    ));
}
async function LX({ codexHome: e, skillsToRemove: t }) {
  if (t.length === 0) return;
  let n = s.default.join(e, `skills`);
  await Promise.all(
    t.map(async (t) => {
      let r = RX({ skillDir: t, skillsRoot: n });
      if (
        !(
          await p.default.stat(r).catch((e) => {
            if (BX(e, `ENOENT`)) return null;
            throw e;
          })
        )?.isDirectory()
      )
        return;
      let i = s.default.join(
        e,
        `.tmp`,
        `legacy-primary-runtime-skills`,
        `${s.default.basename(r)}-${Date.now()}-${(0, u.randomUUID)()}`,
      );
      (await p.default.mkdir(s.default.dirname(i), { recursive: !0 }),
        (await p.default
          .rename(r, i)
          .then(() => !0)
          .catch((e) => {
            if (BX(e, `ENOENT`)) return !1;
            throw e;
          })) &&
          wX().info(`primary_runtime_legacy_skill_migrated`, {
            safe: { skillDir: t },
            sensitive: { backupPath: i, skillRoot: r },
          }));
    }),
  );
}
function RX({ skillDir: e, skillsRoot: t }) {
  let n = s.default.resolve(t),
    r = s.default.resolve(t, e);
  if (zX(r, n)) return r;
  throw Error(
    `Primary runtime removed skill path must stay within the Codex skills directory: ${e}`,
  );
}
function zX(e, t) {
  let n = s.default.relative(t, e);
  return n !== `` && !n.startsWith(`..`) && !s.default.isAbsolute(n);
}
function BX(e, t) {
  return typeof e == `object` && !!e && `code` in e && e.code === t;
}
function VX(e) {
  return e instanceof Error || e instanceof DOMException
    ? e.name === `AbortError` || e.message.toLowerCase().includes(`aborted`)
    : !1;
}
function HX({ error: e, errorReporter: t, hasConfiguredRuntime: n, release: r, targetKey: i }) {
  if (
    e instanceof Error &&
    e.message === `Codex Workspace dependencies are not supported on Windows 10.`
  ) {
    wX().info(`primary_runtime_install_unsupported_host`, {
      safe: { errorName: e.name, hasConfiguredRuntime: n, release: r, target: i },
      sensitive: { error: e },
    });
    return;
  }
  (wX().error(`primary_runtime_install_failed`, {
    safe: {
      errorName: e instanceof Error ? e.name : typeof e,
      hasConfiguredRuntime: n,
      release: r,
      target: i,
    },
    sensitive: { error: e },
  }),
    !(e instanceof TypeError && e.message === `fetch failed`) &&
      t.reportNonFatal(e instanceof Error ? e : Error(String(e)), {
        kind: `install-primary-runtime`,
        tags: { release: r, target: i },
      }));
}
async function UX({ installRuntime: e, onProgress: t, release: n, runtimeOptions: r, target: i }) {
  let a = JSON.stringify({ release: n, runtimeOptions: r, target: i }),
    o = DX.get(a);
  if (o != null) return o.promise;
  let s = new AbortController(),
    c = OX.then(
      () => (
        s.signal.throwIfAborted(),
        e(i, r, n, {
          onProgress: (e) => {
            t?.({ ...e, errorMessage: null, release: n });
          },
          signal: s.signal,
        })
      ),
    ).catch((e) => {
      throw (
        t?.({
          bundleVersion: null,
          downloadedBytes: null,
          errorMessage: e instanceof Error ? e.message : String(e),
          phase: `error`,
          release: n,
          totalBytes: null,
        }),
        e
      );
    });
  (DX.set(a, { controller: s, promise: c }),
    (OX = c.then(
      () => void 0,
      () => void 0,
    )));
  try {
    return await c;
  } finally {
    DX.delete(a);
  }
}
async function WX({ hostId: e }) {
  let t = await GX({ hostId: e });
  return {
    bundleVersion: t.bundleVersion,
    installed: t.installed,
    instructions: t.installed ? t.instructions : null,
  };
}
async function GX({ hostId: e }) {
  wX().info(`primary_runtime_dependencies_diagnose_started`, {
    safe: { hostId: e },
    sensitive: {},
  });
  let n = await t.g();
  return (
    wX().info(`primary_runtime_dependencies_diagnose_finished`, {
      safe: {
        bundleVersion: n.bundleVersion,
        installed: n.installed,
        problemCount: n.problems.length,
      },
      sensitive: {},
    }),
    n
  );
}
async function KX({
  connection: e,
  errorReporter: n,
  installRuntime: r = t.v,
  onProgress: i,
  release: a,
  runtimeConfig: o,
  target: s = t.m(),
  confirmDeleteRuntimeRoot: c,
}) {
  let l = await t._();
  if (l != null && (await c?.(l)) === !1) throw Error(`Codex dependency reset was canceled.`);
  return (
    await t.p(),
    NX({
      connection: e,
      errorReporter: n,
      installRuntime: r,
      onProgress: i,
      release: a,
      runtimeConfig: o,
      target: s,
    })
  );
}
function qX(e) {
  if (e !== `local`) throw Error(`Codex runtime installation only supports the local host.`);
}
function JX({
  appServerConnectionRegistry: e,
  errorReporter: n,
  globalState: r,
  hostId: i,
  sharedObjectRepository: a,
  windowManager: s,
}) {
  let c = new Map(),
    l = (e, t) => {
      s.sendMessageToAllWindows({
        type: `primary-runtime-install-progress`,
        hostId: e,
        progress: t,
      });
    },
    u = (t) => {
      let n = c.get(t);
      if (n != null) return n;
      let r = jX(e.getConnection(t)).catch((e) => {
        throw (c.delete(t), e);
      });
      return (c.set(t, r), r);
    },
    d = async (e) => {
      if (!t.Ti({ osRelease: o.default.release(), platform: process.platform })) throw Error(t.d);
      if (!(await u(e))) throw Error(`Codex dependencies are disabled in settings.`);
    },
    f = new MX({
      appServerConnectionRegistry: e,
      errorReporter: n,
      getCacheStatus: t.h,
      getTarget: t.m,
      globalState: r,
      hostId: i,
      installForConnection: NX,
      isWorkspaceDependenciesFeatureEnabled: () => u(i),
      onProgress: (e) => {
        l(i, e);
      },
      sharedObjectRepository: a,
    });
  return {
    isWorkspaceDependenciesFeatureEnabled: u,
    rpc: {
      install: async ({ hostId: t, release: r }) => (
        qX(t),
        await d(t),
        NX({
          connection: e.getConnection(t),
          errorReporter: n,
          onProgress: (e) => {
            l(t, e);
          },
          release: r,
          runtimeConfig: a?.get(`codex_runtimes_config`),
        })
      ),
      cancelInstall: ({ hostId: e }) => (qX(e), { canceled: kX() }),
      diagnoseDependencies: async ({ hostId: e }) => {
        qX(e);
        let t = await GX({ hostId: e });
        return { bundleVersion: t.bundleVersion, installed: t.installed, problems: t.problems };
      },
      loadDependencies: async ({ hostId: e }) => (
        qX(e),
        (await u(e))
          ? WX({ hostId: e })
          : { bundleVersion: null, installed: !1, instructions: null }
      ),
      finishInstall: async ({ hostId: t, release: r }) => (
        qX(t),
        await d(t),
        NX({
          connection: e.getConnection(t),
          errorReporter: n,
          onProgress: (e) => {
            l(t, e);
          },
          release: r,
          runtimeConfig: a?.get(`codex_runtimes_config`),
        })
      ),
      getUpdateStatus: () => f.getStatus(),
      runUpdateNow: ({ release: e }) => f.runNow(e),
      resetDependencies: async ({ hostId: t, release: r }) => (
        qX(t),
        await d(t),
        KX({
          confirmDeleteRuntimeRoot: void 0,
          connection: e.getConnection(t),
          errorReporter: n,
          onProgress: (e) => {
            l(t, e);
          },
          release: r,
          runtimeConfig: a?.get(`codex_runtimes_config`),
        })
      ),
    },
    startUpdatePolling: () => {
      if (i !== `local`) return () => void 0;
      let e = () => {
          f.shouldPoll() && f.pollIfDue();
        },
        t = a?.addSubscriber((t, n) => {
          (t === `statsig_default_enable_features` && c.clear(),
            f.shouldTickForSharedObjectUpdate(t, n) && e());
        });
      e();
      let n = setInterval(e, EX);
      return (
        n.unref(),
        () => {
          (clearInterval(n), t?.());
        }
      );
    },
  };
}
function YX(e, t) {
  for (let n of t) e = e.replace(RegExp(`\\` + n, `g`), `\\` + n);
  return e;
}
function XX(e, t) {
  return (
    (e = YX(e, `\\()[]{}.+^$|`)),
    (e = e.replace(/\*/g, `.*`).replace(/\?/g, `.?`)),
    RegExp(`^(?:` + e + `)$`).test(t)
  );
}
function ZX(e, t) {
  let n = Array.isArray(e) ? e : e.split(/,/),
    r = !1;
  for (let e of n)
    if (e[0] == `!` && XX(e.slice(1), t)) return !1;
    else XX(e, t) && (r = !0);
  return r;
}
var QX = /\s/,
  $X = /\r|\n/,
  eZ = /^(Host|Match)$/i,
  tZ =
    /^(GlobalKnownHostsFile|Host|IPQoS|SendEnv|UserKnownHostsFile|ProxyCommand|Match|CanonicalDomains)$/i,
  nZ = /^(?:CertificateFile|IdentityFile|IdentityAgent|User)$/i,
  rZ = /^(Include|IdentityFile)$/i,
  iZ;
(function (e) {
  ((e[(e.DIRECTIVE = 1)] = `DIRECTIVE`),
    (e[(e.COMMENT = 2)] = `COMMENT`),
    (e[(e.EMPTY = 3)] = `EMPTY`));
})((iZ ||= {}));
var aZ = [`IdentityFile`, `LocalForward`, `RemoteForward`, `DynamicForward`, `CertificateFile`];
function oZ(e, t) {
  return t.hasOwnProperty(e.param) && t[e.param] === e.value;
}
function sZ(e) {
  for (let t of e)
    if (t.type === iZ.DIRECTIVE && `config` in t) {
      for (let e of t.config) if (e.before) return e.before;
    }
  return `  `;
}
function cZ(e, t) {
  let n = (e, n) => {
    switch (e.toLowerCase()) {
      case `all`:
        return !0;
      case `final`:
        return t.inFinalPass ? !0 : ((t.doFinalPass = !0), !1);
      case `exec`:
        return (
          (0, m.spawnSync)(
            `function main {
          ${n}
        }
        main`,
            { shell: !0 },
          ).status === 0
        );
      case `host`:
        return ZX(n, t.params.HostName);
      case `originalhost`:
        return ZX(n, t.params.OriginalHost);
      case `user`:
        return ZX(n, t.params.User);
      case `localuser`:
        return ZX(n, t.params.LocalUser);
    }
  };
  for (let t in e) {
    let r = e[t];
    if (!n(t, Array.isArray(r) ? r.map(({ val: e }) => e) : r)) return !1;
  }
  return !0;
}
var lZ = class e extends Array {
  static DIRECTIVE = iZ.DIRECTIVE;
  static COMMENT = iZ.COMMENT;
  static EMPTY = iZ.EMPTY;
  static parse(e) {
    return uZ(e);
  }
  static stringify(e) {
    return dZ(e);
  }
  compute(e, t) {
    typeof e == `string` && (e = { Host: e });
    let n;
    try {
      n = o.default.userInfo();
    } catch {
      n = { username: process.env.USER || process.env.USERNAME || `` };
    }
    let r = {
        params: {
          Host: e.Host,
          HostName: e.Host,
          OriginalHost: e.Host,
          User: n.username,
          LocalUser: n.username,
        },
        inFinalPass: !1,
        doFinalPass: !1,
      },
      i = {},
      a = (e, n) => {
        let a = t?.ignoreCase ? e.toLowerCase() : e,
          o;
        o = Array.isArray(n)
          ? /ProxyCommand/i.test(a)
            ? n
                .map(
                  ({ val: e, separator: t, quoted: n }) =>
                    `${t}${n ? `"${e.replace(/"/g, `\\"`)}"` : e}`,
                )
                .join(``)
                .trim()
            : n.map(({ val: e }) => e)
          : n;
        let s = Array.isArray(o) ? o[0] : o;
        aZ.some((t) => t.toLowerCase() === e.toLowerCase())
          ? (i[a] || (i[a] = [])).push(...[].concat(o))
          : (i[a] ??
            (e === `HostName` ? (r.params.HostName = s) : e === `User` && (r.params.User = s),
            (i[a] = o)));
      };
    e.User !== void 0 && a(`User`, e.User);
    let s = () => {
      for (let e of this)
        if (e.type === iZ.DIRECTIVE)
          if (
            /^host$/i.test(e.param) &&
            ZX(Array.isArray(e.value) ? e.value.map(({ val: e }) => e) : e.value, r.params.Host)
          ) {
            let t = !1,
              n = [];
            a(e.param, e.value);
            for (let r of e.config)
              r.type === iZ.DIRECTIVE &&
                (a(r.param, r.value),
                /^CanonicalizeHostName$/i.test(r.param) && r.value === `yes` && (t = !0),
                /^CanonicalDomains$/i.test(r.param) &&
                  Array.isArray(r.value) &&
                  (n = r.value.map(({ val: e }) => e)));
            if (n.length > 0 && t && r.params.Host === r.params.OriginalHost)
              for (let e of n) {
                let t = `${r.params.OriginalHost}.${e}`,
                  { status: n, stderr: i } = (0, m.spawnSync)(`nslookup`, [t]);
                if (n === 0 && !/can't find/.test(i.toString())) {
                  ((r.params.Host = t), a(`Host`, t), s());
                  break;
                }
              }
          } else if (/^match$/i.test(e.param) && `criteria` in e && cZ(e.criteria, r))
            for (let t of e.config) t.type === iZ.DIRECTIVE && a(t.param, t.value);
          else /^(host|match)$/i.test(e.param) || a(e.param, e.value);
    };
    return (
      s(), r.doFinalPass && ((r.inFinalPass = !0), (r.params.Host = r.params.HostName), s()), i
    );
  }
  find(e) {
    if (typeof e == `function`) return super.find(e);
    if (!(e && (`Host` in e || `Match` in e))) throw Error(`Can only find by Host or Match`);
    return super.find((t) => `param` in t && oZ(t, e));
  }
  remove(e) {
    let t;
    if (typeof e == `function`) t = super.findIndex(e);
    else if (e && (`Host` in e || `Match` in e))
      t = super.findIndex((t) => `param` in t && oZ(t, e));
    else throw Error(`Can only remove by Host or Match`);
    if (t >= 0) return this.splice(t, 1);
  }
  toString() {
    return dZ(this);
  }
  append(t) {
    let n = sZ(this),
      r = this.length > 0 ? this[this.length - 1] : null,
      i = (r && r.config) || this,
      a = this,
      o = i.length > 0 ? i[i.length - 1] : r;
    o &&
      !o.after &&
      (o.after = `
`);
    let s = i !== a;
    for (let r in t) {
      let c = t[r],
        l = {
          type: iZ.DIRECTIVE,
          param: r,
          separator: ` `,
          value: Array.isArray(c)
            ? c.map((e, t) => ({ val: e, separator: t === 0 ? `` : ` ` }))
            : c,
          before: s ? n : n.replace(/  |\t/, ``),
          after: `
`,
        };
      (eZ.test(r)
        ? ((s = !0),
          (l.before = n.replace(/  |\t/, ``)),
          (i = a),
          o &&
            o.after ===
              `
` &&
            (o.after += `
`),
          i.push(l),
          (i = l.config = new e()))
        : i.push(l),
        (o = l));
    }
    return a;
  }
  prepend(t, n = !1) {
    let r = sZ(this),
      i = this,
      a = 0;
    if (n) {
      for (; a < this.length && !(`config` in this[a]); ) a += 1;
      if (a >= this.length) return this.append(t);
    }
    let o = !1,
      s = 0;
    for (let n in t) {
      s += 1;
      let c = t[n],
        l = {
          type: iZ.DIRECTIVE,
          param: n,
          separator: ` `,
          value: Array.isArray(c)
            ? c.map((e, t) => ({ val: e, separator: t === 0 ? `` : ` ` }))
            : c,
          before: ``,
          after: `
`,
        };
      if (eZ.test(n)) {
        ((l.before = r.replace(/  |\t/, ``)),
          i.splice(a, 0, l),
          (i = l.config = new e()),
          (o = !0));
        continue;
      }
      if (
        (s === Object.keys(t).length &&
          (l.after += `
`),
        !o)
      ) {
        (i.splice(a, 0, l),
          (a += 1),
          rZ.test(n) &&
            (l.after += `
`));
        continue;
      }
      ((l.before = r), i.push(l));
    }
    return i;
  }
};
function uZ(e) {
  let t = typeof e == `string` ? e : e.toString(`utf-8`),
    n = 0,
    r = o(),
    i = new lZ(),
    a = i;
  function o() {
    return t[n++];
  }
  function s() {
    let e = ``;
    for (; QX.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function c() {
    let e = ``;
    for (; $X.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function l() {
    let e = ``;
    for (; r && /[^ \t=]/.test(r); ) ((e += r), (r = o()));
    return e;
  }
  function u() {
    let e = s();
    return (r === `=` && ((e += r), (r = o())), e + s());
  }
  function d() {
    let e = ``,
      t = !1,
      n = !1;
    for (; r && !$X.test(r); ) {
      if (n) ((e += r === `"` ? r : `\\${r}`), (n = !1));
      else if (r === `"` && (!e || t)) t = !t;
      else if (r === `\\`) n = !0;
      else if (r === `#` && !t) break;
      else e += r;
      r = o();
    }
    if (t || n) throw Error(`Unexpected line break at ${e}`);
    return e.trim();
  }
  function f() {
    let e = iZ.COMMENT,
      t = ``;
    for (; r && !$X.test(r); ) ((t += r), (r = o()));
    return { type: e, content: t, before: ``, after: `` };
  }
  function p() {
    let e = [],
      t = ``,
      n = !1,
      i = ` `,
      a = !1,
      s = !1;
    for (; r && !$X.test(r); ) {
      if (s) ((t += r === `"` ? r : `\\${r}`), (s = !1));
      else if (r === `"`) a = !a;
      else if (r === `\\`) s = !0;
      else if (a) ((t += r), (n = !0));
      else if (/[ \t=]/.test(r))
        t && (e.push({ val: t, separator: i, quoted: n }), (t = ``), (n = !1), (i = r));
      else if (r === `#` && e.length > 0) break;
      else t += r;
      r = o();
    }
    if (a || s)
      throw Error(
        `Unexpected line break at ${e
          .map(({ val: e }) => e)
          .concat(t)
          .join(` `)}`,
      );
    return (t && e.push({ val: t, separator: i, quoted: n }), e.length > 1 ? e : e[0].val);
  }
  function m() {
    let e = iZ.DIRECTIVE,
      t = l(),
      n = tZ.test(t),
      i = {
        type: e,
        param: t,
        separator: u(),
        quoted: !n && r === `"`,
        value: n ? p() : d(),
        before: ``,
        after: ``,
      };
    if ((i.quoted || delete i.quoted, /^Match$/i.test(t))) {
      let e = {};
      typeof i.value == `string` && (i.value = [{ val: i.value, separator: ``, quoted: i.quoted }]);
      let t = 0;
      for (; t < i.value.length; ) {
        let { val: n } = i.value[t];
        switch (n.toLowerCase()) {
          case `all`:
          case `canonical`:
          case `final`:
            ((e[n] = []), (t += 1));
            break;
          default:
            if (t + 1 >= i.value.length) throw Error(`Missing value for match criteria ${n}`);
            ((e[n] = i.value[t + 1].val), (t += 2));
            break;
        }
      }
      i.criteria = e;
    }
    return i;
  }
  function h() {
    let e = s(),
      t = r === `#` ? f() : m(),
      n = c();
    return ((t.before = e), (t.after = n), t);
  }
  for (; r; ) {
    let e = h();
    e.type === iZ.DIRECTIVE && eZ.test(e.param)
      ? ((i = a), i.push(e), (i = e.config = new lZ()))
      : e.type === iZ.DIRECTIVE && !e.param
        ? i.length === 0
          ? a.length === 0
            ? a.push({ type: iZ.EMPTY, before: ``, after: e.before })
            : (a[a.length - 1].after += e.before)
          : (i[i.length - 1].after += e.before)
        : i.push(e);
  }
  return a;
}
function dZ(e) {
  let t = ``;
  function n(e, t) {
    if (Array.isArray(e)) {
      let t = ``;
      for (let { val: r, separator: i, quoted: a } of e) t += (t ? i : ``) + n(r, a || QX.test(r));
      return t;
    }
    return t ? `"${e}"` : e;
  }
  function r(e) {
    let t = e.quoted || (nZ.test(e.param) && typeof e.value == `string` && QX.test(e.value)),
      r = n(e.value, t);
    return `${e.param}${e.separator}${r}`;
  }
  let i = (e) => {
    ((t += e.before),
      e.type === iZ.COMMENT
        ? (t += e.content)
        : e.type === iZ.DIRECTIVE && aZ.includes(e.param)
          ? (Array.isArray(e.value) ? e.value : [e.value]).forEach((n, i, a) => {
              ((t += r({ ...e, value: typeof n == `string` ? n : n.val })),
                i < a.length - 1 && (t += `\n${e.before}`));
            })
          : e.type === iZ.DIRECTIVE && (t += r(e)),
      (t += e.after),
      `config` in e && e.config.forEach(i));
  };
  return (e.forEach(i), t);
}
var fZ = t.Gr(`remote-connections/discovery`),
  pZ = /[!*?[\]]/,
  mZ = /[*?[\]{}()]/,
  hZ = new Set([`colima`]),
  gZ = new Set([`github.com`]);
async function _Z(e) {
  try {
    if (!l.default.existsSync(e))
      return (
        fZ().info(`ssh_config_entrypoint_missing`, { safe: {}, sensitive: { entrypointPath: e } }),
        []
      );
    fZ().info(`discovery_started`, { safe: {}, sensitive: { entrypointPath: e } });
    let t = vZ(e),
      n = t.filter((e) => !hZ.has(e.trim().toLowerCase())),
      r = (await Promise.all(n.map((t) => yZ({ entrypointPath: e, sshAlias: t })))).filter(
        (e) => e != null,
      );
    return (
      fZ().info(`discovery_completed`, {
        safe: {
          collectedAliasCount: t.length,
          filteredAliasCount: t.length - n.length,
          resolvedConnectionCount: r.length,
          droppedAliasCount: n.length - r.length,
        },
        sensitive: { entrypointPath: e, collectedAliases: t, filteredAliases: n },
      }),
      r.sort((e, t) => e.displayName.localeCompare(t.displayName))
    );
  } catch (e) {
    throw (
      fZ().warning(`failed to discover remote SSH connections`, {
        safe: {},
        sensitive: { error: e },
      }),
      Error(`Failed to discover remote SSH connections.`)
    );
  }
}
function vZ(e) {
  let t = new Set(),
    n = new Set(),
    r = s.default.dirname(s.default.resolve(e));
  return (
    bZ({
      configPath: s.default.resolve(e),
      sshConfigRootDirectory: r,
      discoveredAliases: t,
      visitedConfigPaths: n,
    }),
    Array.from(t)
  );
}
async function yZ({ entrypointPath: e, sshAlias: n }) {
  let {
    stdout: r,
    stderr: i,
    code: a,
  } = await t.An({ args: [`ssh`, `-G`, `-F`, e, n], spawnInsideWsl: !1 }).wait();
  if (a !== 0)
    return (
      fZ().warning(`failed to resolve discovered SSH alias`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: n, stderr: i },
      }),
      null
    );
  let o = xZ(r);
  if (!o.hostname)
    return (
      fZ().info(`resolved_ssh_alias_missing_hostname`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: n },
      }),
      null
    );
  let s = o.hostname.trim().toLowerCase();
  return gZ.has(s)
    ? (fZ().info(`resolved_ssh_alias_excluded`, {
        safe: {},
        sensitive: { entrypointPath: e, sshAlias: n, hostname: o.hostname },
      }),
      null)
    : {
        hostId: t.pa(n),
        displayName: n,
        source: `discovered`,
        autoConnect: !1,
        sshAlias: n,
        sshHost: o.hostname,
        sshPort: o.port,
        identity: o.identity,
      };
}
function bZ({
  configPath: e,
  sshConfigRootDirectory: t,
  discoveredAliases: n,
  visitedConfigPaths: r,
}) {
  if (r.has(e)) return;
  r.add(e);
  let i = lZ.parse(l.default.readFileSync(e, `utf8`));
  for (let e of i) {
    if (e.type !== iZ.DIRECTIVE) continue;
    let i = e.param.toLowerCase();
    if (i === `host` && `config` in e) {
      let t = wZ(e)[0];
      t != null && n.add(t);
      continue;
    }
    if (i === `include`)
      for (let i of CZ({ sshConfigRootDirectory: t, includeValue: TZ(e) }))
        bZ({
          configPath: i,
          sshConfigRootDirectory: t,
          discoveredAliases: n,
          visitedConfigPaths: r,
        });
  }
}
function xZ(e) {
  let t = lZ.parse(e),
    n = null,
    r = null,
    i = null;
  for (let e of SZ(t)) {
    if (e.type !== iZ.DIRECTIVE) continue;
    let t = e.param.toLowerCase(),
      a = EZ(e);
    if (a != null) {
      if (t === `hostname` && n == null) {
        n = a;
        continue;
      }
      if (t === `port` && r == null) {
        let e = Number(a);
        Number.isSafeInteger(e) && (r = e);
        continue;
      }
      t === `identityfile` && i == null && a !== `none` && (i = a);
    }
  }
  return { hostname: n, port: r, identity: i };
}
function SZ(e) {
  let t = [];
  for (let n of e) n.type === iZ.DIRECTIVE && (t.push(n), `config` in n && t.push(...SZ(n.config)));
  return t;
}
function CZ({ sshConfigRootDirectory: e, includeValue: t }) {
  let n = [];
  for (let r of t) {
    let t = DZ(r),
      i = s.default.isAbsolute(t) ? t : s.default.resolve(e, t),
      a = l.default.globSync(i);
    if (a.length === 0 && !mZ.test(i)) {
      l.default.existsSync(i) && n.push(i);
      continue;
    }
    n.push(...a.filter((e) => l.default.statSync(e).isFile()));
  }
  return n;
}
function wZ(e) {
  return TZ(e)
    .filter((e) => e.length > 0)
    .filter((e) => !pZ.test(e));
}
function TZ(e) {
  return Array.isArray(e.value)
    ? e.value.map((e) => e.val).filter(Boolean)
    : e.value.split(/\s+/u).filter(Boolean);
}
function EZ(e) {
  if (Array.isArray(e.value))
    return e.value
      .map((e) => e.val)
      .join(``)
      .trim();
  let t = e.value.trim();
  return t.length === 0 ? null : t;
}
function DZ(e) {
  return e === `~`
    ? o.default.homedir()
    : e.startsWith(`~/`) || e.startsWith(`~\\`)
      ? s.default.join(o.default.homedir(), e.slice(2))
      : e;
}
var OZ = `app_EMoamEEZ73f0CkXaXp7hrann`,
  kZ = `https://auth.openai.com`,
  AZ = `CODEX_APP_SERVER_LOGIN_ISSUER`,
  jZ = [1455, 1457],
  MZ = `/auth/callback`,
  NZ = 10 * 6e4,
  PZ = `codex.remote_control.enroll`,
  FZ = `remote_control`,
  IZ = `codex://settings/connections`,
  LZ = t.$s({ access_token: t.nc().min(1) }).passthrough();
async function RZ({
  accountId: e,
  desktopApiOptions: t,
  fetchToken: n = (e, t) => a.net.fetch(e, t),
  openExternalUrl: r = (e) => a.shell.openExternal(e),
  timeoutMs: i = NZ,
}) {
  let o = BZ(),
    s = HZ(),
    c = UZ(32),
    l = await GZ({ state: c, timeoutMs: i });
  try {
    return (
      await r(
        zZ({
          issuer: o,
          clientId: OZ,
          redirectUri: l.redirectUri,
          codeChallenge: s.codeChallenge,
          state: c,
          originator: t.desktopOriginator,
          accountId: e,
        }),
      ),
      (
        await WZ({
          code: await l.authorizationCode,
          codeVerifier: s.codeVerifier,
          clientId: OZ,
          issuer: o,
          redirectUri: l.redirectUri,
          fetchToken: n,
        })
      ).access_token
    );
  } finally {
    l.close();
  }
}
function zZ({
  issuer: e,
  clientId: t,
  redirectUri: n,
  codeChallenge: r,
  state: i,
  originator: a,
  accountId: o,
}) {
  let s = new URL(`/oauth/authorize`, VZ(e)),
    c = new URLSearchParams({
      response_type: `code`,
      client_id: t,
      redirect_uri: n,
      scope: PZ,
      code_challenge: r,
      code_challenge_method: `S256`,
      state: i,
      originator: a,
      reauth: FZ,
      max_age: `0`,
      codex_cli_simplified_flow: `true`,
    });
  return (
    o != null && (c.set(`allowed_workspace_id`, o), c.set(`current_workspace_id`, o)),
    (s.search = c.toString()),
    s.toString()
  );
}
function BZ() {
  return process.env[AZ]?.trim() || kZ;
}
function VZ(e) {
  return e.replace(/\/+$/, ``);
}
function HZ() {
  let e = UZ(32);
  return {
    codeVerifier: e,
    codeChallenge: (0, u.createHash)(`sha256`).update(e).digest(`base64url`),
  };
}
function UZ(e) {
  return (0, u.randomBytes)(e).toString(`base64url`);
}
async function WZ({
  code: e,
  codeVerifier: t,
  clientId: n,
  issuer: r,
  redirectUri: i,
  fetchToken: a,
}) {
  let o = await a(new URL(`/oauth/token`, VZ(r)).toString(), {
    method: `POST`,
    headers: { "Content-Type": `application/x-www-form-urlencoded` },
    body: new URLSearchParams({
      grant_type: `authorization_code`,
      code: e,
      redirect_uri: i,
      client_id: n,
      code_verifier: t,
    }).toString(),
  });
  if (!o.ok) throw Error(`Remote control step-up token exchange failed with status ${o.status}.`);
  return LZ.parse(await o.json());
}
async function GZ({ state: e, timeoutMs: t }) {
  let n = null,
    r = null,
    i = null,
    a = null,
    o = null,
    s = !1,
    c = (e) => {
      s || ((s = !0), i != null && (clearTimeout(i), (i = null)), e());
    },
    l = new Promise((e, t) => {
      ((a = e), (o = t));
    });
  for (let t of jZ) {
    let i = (0, d.createServer)((n, r) => {
      try {
        if (n.url == null) {
          KZ(r, 400, `Bad Request`);
          return;
        }
        let i = new URL(n.url, `http://localhost:${t}`);
        if (i.pathname !== MZ) {
          KZ(r, 404, `Not Found`);
          return;
        }
        if (i.searchParams.get(`state`) !== e) {
          KZ(r, 400, `State mismatch`);
          return;
        }
        let s = i.searchParams.get(`error`);
        if (s != null) {
          let e = i.searchParams.get(`error_description`);
          (KZ(r, 400, `Remote control login failed`),
            c(() => {
              o?.(Error(e == null || e.length === 0 ? s : `${s}: ${e}`));
            }));
          return;
        }
        let l = i.searchParams.get(`code`);
        if (l == null || l.length === 0) {
          KZ(r, 400, `Missing authorization code`);
          return;
        }
        (qZ(r),
          c(() => {
            a?.(l);
          }));
      } catch (e) {
        (KZ(r, 400, `Bad Request`),
          c(() => {
            o?.(e);
          }));
      }
    });
    try {
      (await new Promise((e, n) => {
        (i.once(`error`, n), i.listen(t, `localhost`, e));
      }),
        (n = i),
        (r = t));
      break;
    } catch (e) {
      if ((i.close(), !(e instanceof Error) || !(`code` in e) || e.code !== `EADDRINUSE`)) throw e;
    }
  }
  if (n == null || r == null)
    throw Error(`Remote control login callback ports 1455 and 1457 are already in use.`);
  return (
    (i = setTimeout(() => {
      c(() => {
        o?.(Error(`Timed out waiting for remote control step-up login.`));
      });
    }, t)),
    {
      redirectUri: `http://localhost:${r}${MZ}`,
      authorizationCode: l,
      close: () => {
        (n?.close(),
          c(() => {
            o?.(Error(`Remote control step-up login was cancelled.`));
          }));
      },
    }
  );
}
function KZ(e, t, n) {
  (e.writeHead(t, { "Content-Type": `text/plain; charset=utf-8`, Connection: `close` }), e.end(n));
}
function qZ(e) {
  (e.writeHead(200, { "Content-Type": `text/html; charset=utf-8`, Connection: `close` }),
    e.end(`<!doctype html>
<html>
  <body>
    <p>Remote control authorization completed.</p>
    <p><a href="${IZ}">Return to Codex</a></p>
    <script>window.location.href = ${JSON.stringify(IZ)};<\/script>
  </body>
</html>`));
}
var JZ = t.Gr(`remote-connections/window-context`),
  YZ = 250,
  XZ = class {
    remoteConnectionsByHostId = new Map();
    shouldLoadRemoteControlConnections = !1;
    constructor(e, t, n, r, i, a, o, s, c, l, u) {
      ((this.appState = e),
        (this.sharedObjectRepository = t),
        (this.owningHostId = n),
        (this.appServerConnectionRegistry = r),
        (this.desktopApiOptions = i),
        (this.desktopAuthAppServerClient = a),
        (this.remoteControlDeviceKeyClient = o),
        (this.getHostConfigForHostId = s),
        (this.getAppServerClientForHostId = c),
        (this.createAndRegisterRemoteConnection = l),
        (this.disposeRemoteConnection = u));
    }
    reconcileRemoteConnections(e) {
      let t = this.remoteConnectionsByHostId.size,
        n = 0,
        r = new Map();
      for (let t of e) t.hostId !== this.owningHostId && r.set(t.hostId, t);
      for (let e of r.values()) {
        let t = e.hostId,
          r = this.remoteConnectionsByHostId.get(t),
          i = this.appServerConnectionRegistry.getMaybeConnection(t);
        if (!e.autoConnect) {
          i != null && this.disposeRemoteConnection(t);
          continue;
        }
        if (!(i != null && $Z(r, e))) {
          i != null && this.disposeRemoteConnection(t);
          try {
            this.appServerConnectionRegistry.addConnection(
              t,
              this.createAndRegisterRemoteConnection(e),
            );
          } catch (e) {
            ((n += 1),
              JZ().warning(`reconcile_create_connection_failed`, {
                safe: {},
                sensitive: { error: e, hostId: t },
              }));
          }
        }
      }
      for (let e of this.remoteConnectionsByHostId.keys())
        r.has(e) || this.disposeRemoteConnection(e);
      this.remoteConnectionsByHostId.clear();
      for (let [e, t] of r) this.remoteConnectionsByHostId.set(e, t);
      JZ().info(`reconcile_completed`, {
        safe: { nextConnectionCount: r.size, previousConnectionCount: t, creationFailureCount: n },
        sensitive: {},
      });
    }
    handleLocalRemoteControlIdentityChanged() {
      (this.removeLocalRemoteControlConnection(),
        this.shouldLoadRemoteControlConnections &&
          this.refreshRemoteControlConnections().catch((e) => {
            JZ().warning(`refresh_remote_control_for_local_identity_failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }));
    }
    async refreshRemoteConnections() {
      let e = this.remoteConnectionsByHostId.size;
      JZ().info(`refresh_started`, { safe: { previousConnectionCount: e }, sensitive: {} });
      let t = await this.loadRemoteConnections();
      this.sharedObjectRepository.set(`remote_ssh_connections`, t);
      let n = ZZ(t);
      return (
        JZ().info(`refresh_completed`, {
          safe: {
            previousConnectionCount: e,
            nextConnectionCount: t.length,
            discoveredConnectionCount: n.discovered,
            codexManagedConnectionCount: n.codexManaged,
            autoConnectConnectionCount: t.filter((e) => e.autoConnect).length,
          },
          sensitive: {},
        }),
        { remoteConnections: t }
      );
    }
    async discoverRemoteConnections() {
      return { discoveredRemoteConnections: await _Z(tQ()) };
    }
    async refreshRemoteControlConnections() {
      if (!this.shouldLoadRemoteControlConnections)
        return (
          this.sharedObjectRepository.set(`remote_control_connections`, []),
          this.sharedObjectRepository.set(`remote_control_connections_state`, {
            available: !1,
            accessRequired: !1,
            authRequired: !1,
            clientAuthorized: !1,
          }),
          { remoteControlConnections: [] }
        );
      let e = this.sharedObjectRepository.get(`remote_control_connections`),
        t =
          this.sharedObjectRepository.get(`remote_control_connections_state`)?.clientAuthorized ===
          !0,
        n = e?.length ?? 0;
      JZ().info(`refresh_remote_control_started`, {
        safe: { previousConnectionCount: n },
        sensitive: {},
      });
      let r = this.filterLocalRemoteControlConnection(
        await this.loadRemoteControlConnections(),
      ).map((e) => ({ ...e, autoConnect: this.getAutoConnectForRemoteConnection(e) }));
      return (
        this.sharedObjectRepository.set(`remote_control_connections`, r),
        this.sharedObjectRepository.get(`remote_control_connections_state`)?.clientAuthorized === !0
          ? this.connectRemoteConnectionsAndLogFailures(
              r.filter((e) => e.autoConnect).map((e) => e.hostId),
            )
          : t &&
            (await Promise.all((e ?? []).map((e) => this.disconnectRemoteConnection(e.hostId)))),
        JZ().info(`refresh_remote_control_completed`, {
          safe: {
            previousConnectionCount: n,
            nextConnectionCount: r.length,
            autoConnectConnectionCount: r.filter((e) => e.autoConnect).length,
          },
          sensitive: {},
        }),
        { remoteControlConnections: r }
      );
    }
    async setRemoteControlConnectionsEnabled(e) {
      if (this.shouldLoadRemoteControlConnections === e) {
        let e = this.sharedObjectRepository.get(`remote_control_connections`);
        if (e != null) return { remoteControlConnections: e };
      }
      return (
        (this.shouldLoadRemoteControlConnections = e), this.refreshRemoteControlConnections()
      );
    }
    async authorizeRemoteControlConnections() {
      return (
        await Ug({
          appServerClient: this.desktopAuthAppServerClient,
          desktopApiOptions: this.desktopApiOptions,
          deviceKeyClient: this.remoteControlDeviceKeyClient,
          globalState: this.appState,
          requestRemoteControlEnrollmentStepUpToken: ({ accountId: e }) =>
            RZ({ accountId: e, desktopApiOptions: this.desktopApiOptions }),
        }),
        this.refreshRemoteControlConnections()
      );
    }
    async renameRemoteControlEnvironment({ envId: e, name: t }) {
      let n = t.trim();
      if (n.length === 0) throw Error(`Remote control display name cannot be empty.`);
      let r = this.sharedObjectRepository.get(`remote_control_connections`),
        i = r?.find((t) => t.envId === e);
      if (i == null) throw Error(`Remote control environment not found.`);
      let a = await Cg({
          appServerClient: this.desktopAuthAppServerClient,
          desktopApiOptions: this.desktopApiOptions,
          envId: e,
          name: n,
        }),
        o = this.sharedObjectRepository.get(`remote_control_connections`) ?? r ?? [],
        s = o.find((t) => t.envId === e) ?? i,
        c = { ...a, autoConnect: this.getAutoConnectForRemoteConnection(s) },
        l = o.map((t) => (t.envId === e ? c : t));
      return (
        this.sharedObjectRepository.set(`remote_control_connections`, l),
        { remoteControlConnection: c, remoteControlConnections: l }
      );
    }
    async deleteRemoteControlEnvironment({ envId: e }) {
      let t = this.sharedObjectRepository.get(`remote_control_connections`),
        n = t?.find((t) => t.envId === e);
      if (n == null) throw Error(`Remote control environment not found.`);
      if (n.online && n.autoConnect)
        throw Error(`Disable this remote control environment before deleting it.`);
      (await wg({
        appServerClient: this.desktopAuthAppServerClient,
        desktopApiOptions: this.desktopApiOptions,
        envId: e,
      }),
        this.clearRemoteConnectionAutoConnect(n.hostId));
      let r = (this.sharedObjectRepository.get(`remote_control_connections`) ?? t ?? []).filter(
        (t) => t.envId !== e,
      );
      return (
        this.sharedObjectRepository.set(`remote_control_connections`, r),
        { remoteControlConnections: r }
      );
    }
    async saveCodexManagedRemoteSshConnections(e) {
      let n = this.addConnectionAnalyticsIds(e);
      return (
        this.appState.set(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS, n),
        await this.refreshRemoteConnections(),
        { remoteConnections: n }
      );
    }
    async setRemoteConnectionAutoConnect(e, n) {
      let r = this.getRemoteConnectionAutoConnectByHostId();
      this.appState.set(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, { ...r, [e]: n });
      let i = await this.refreshRemoteConnectionsForHostId(e);
      return n
        ? { remoteConnections: i, ...(await this.ensureRemoteConnectionConnected(e)) }
        : { remoteConnections: i, ...(await this.disconnectRemoteConnection(e)) };
    }
    async installRemoteCodex(e) {
      let t = yv(this.getHostConfigForHostId(e));
      if (t == null) throw Error(`Codex install requires an SSH connection.`);
      await new _v(t).installCodex();
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      return n?.getConnectionError()?.code === `update-required`
        ? (await n.restart({ killCodexProcess: !0 }),
          { state: n.getConnectionState(), error: n.getConnectionError() })
        : this.ensureRemoteConnectionConnected(e);
    }
    async startRemoteChatgptLoginPortForward(e, t) {
      if (this.getHostConfigForHostId(e).kind === `remote-control`)
        throw Error(`Remote control does not support ChatGPT login forwarding yet.`);
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      if (n == null) throw Error(`Connection for host ID ${e} not found`);
      return { forwarded: await n.startRemoteChatgptLoginPortForward({ loginId: t }) };
    }
    async stopRemoteChatgptLoginPortForward(e, t) {
      if (this.getHostConfigForHostId(e).kind === `remote-control`)
        throw Error(`Remote control does not support ChatGPT login forwarding yet.`);
      let n = this.appServerConnectionRegistry.getMaybeConnection(e);
      return n == null ? { stopped: !1 } : { stopped: n.stopRemoteChatgptLoginPortForward(t) };
    }
    async appServerConnectionState(e) {
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      return {
        state: t?.getConnectionState() ?? `disconnected`,
        error: t?.getConnectionError() ?? null,
      };
    }
    async remoteWorkspaceDirectoryEntries(e, n, r = !1) {
      let i = this.getHostConfigForHostId(e),
        a = this.getAppServerClientForHostId(e),
        o = t.Br(i),
        s = await this.getWorkspaceDirectoryPath(a, n),
        c = await this.getParentDirectoryPath(a, s),
        l = await t.jr.readdir(s, { withFileTypes: !0 }, a),
        u = await a.platformPath(),
        d = l
          .map((e) => {
            let t = u.join(s, e.name);
            return { name: e.name, path: I(t, o), type: e.isDirectory() ? `directory` : `file` };
          })
          .filter((e) => !r || e.type === `directory`)
          .sort((e, t) => {
            if (e.type !== t.type) return e.type === `directory` ? -1 : 1;
            let n = e.name.startsWith(`.`);
            return n === t.name.startsWith(`.`) ? e.name.localeCompare(t.name) : n ? 1 : -1;
          });
      return { directoryPath: I(s, o), parentPath: c == null ? null : I(c, o), entries: d };
    }
    async getWorkspaceDirectoryPath(e, n) {
      let r = n?.trim() || (await this.getDefaultWorkspaceDirectoryPath(e));
      if (!(await t.jr.stat(r, e)).isDirectory()) throw Error(`Directory not found: ${r}`);
      return r;
    }
    async getDefaultWorkspaceDirectoryPath(e) {
      let n = e.hostConfig;
      if (!t.va(n)) return (0, o.homedir)();
      let r = await e.platformPath(),
        i = await e.codexHome();
      return r.dirname(i);
    }
    async getParentDirectoryPath(e, t) {
      let n = (await e.platformPath()).dirname(t);
      return n === t ? null : n;
    }
    async connectRemoteConnectionsAndLogFailures(e) {
      let t = 0;
      await Promise.all(
        Array.from(new Set(e), (e) => {
          let n = this.isAutoConnectSshRemoteConnection(e) ? t++ * YZ : 0;
          return this.connectRemoteConnectionAndLogFailure(e, n);
        }),
      );
    }
    isAutoConnectSshRemoteConnection(e) {
      let n = this.remoteConnectionsByHostId.get(e);
      return n != null && n.autoConnect && !t.ga(n);
    }
    async connectRemoteConnectionAndLogFailure(e, t) {
      if (!(t > 0 && (await eQ(t), !this.isAutoConnectSshRemoteConnection(e))))
        try {
          await this.ensureRemoteConnectionConnected(e);
        } catch (t) {
          (JZ().warning(`connect_from_config_apply_failed`, {
            safe: {},
            sensitive: { error: t, hostId: e },
          }),
            this.sharedObjectRepository
              .get(`remote_control_connections`)
              ?.some((t) => t.hostId === e) === !0 &&
              (await this.refreshRemoteControlClientAuthorizationState()));
        }
    }
    async ensureRemoteConnectionConnected(e) {
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      return t == null
        ? this.connectRemoteConnection(e)
        : (t.getConnectionState() !== `connected` && (await t.connect()),
          { state: t.getConnectionState(), error: t.getConnectionError() });
    }
    async connectRemoteConnection(e) {
      let t = this.remoteConnectionsByHostId.get(e);
      if (t == null) throw Error(`Remote connection for host ID ${e} not found`);
      (this.appServerConnectionRegistry.getMaybeConnection(e) != null &&
        this.disposeRemoteConnection(e),
        JZ().info(`connect_requested`, { safe: {}, sensitive: { hostId: e } }));
      let n = null;
      try {
        ((n = this.createAndRegisterRemoteConnection(t)),
          this.appServerConnectionRegistry.addConnection(e, n),
          await n.connect());
      } catch (t) {
        throw (
          JZ().warning(`connect_failed`, { safe: {}, sensitive: { error: t, hostId: e } }),
          Error(`Failed to connect remote connection.`)
        );
      }
      let r = n?.getConnectionState() ?? `disconnected`,
        i = n?.getConnectionError() ?? null;
      return (
        JZ().info(`connect_completed`, {
          safe: { state: r, hasError: i != null },
          sensitive: { hostId: e, connectionError: i },
        }),
        { state: r, error: i }
      );
    }
    async disconnectRemoteConnection(e) {
      JZ().info(`disconnect_requested`, { safe: {}, sensitive: { hostId: e } });
      let t = this.appServerConnectionRegistry.getMaybeConnection(e);
      if (t == null) return { state: `disconnected`, error: null };
      this.disposeRemoteConnection(e);
      let n = t.getConnectionError();
      return (
        JZ().info(`disconnect_completed`, {
          safe: { hasError: n != null },
          sensitive: { hostId: e, connectionError: n },
        }),
        { state: `disconnected`, error: n }
      );
    }
    async loadRemoteConnections() {
      let e = await this.restoreSavedAliasConnectionsForExistingRemoteProjects(
          this.loadSavedRemoteConnections(),
        ),
        t = await Promise.all(
          e.flatMap((e) => {
            let t = e.alias?.trim();
            return t
              ? [
                  yZ({ entrypointPath: tQ(), sshAlias: t }).then((t) =>
                    t == null
                      ? null
                      : {
                          ...t,
                          hostId: e.hostId,
                          connectionAnalyticsId: e.connectionAnalyticsId,
                          displayName: e.displayName,
                          autoConnect: !1,
                        },
                  ),
                ]
              : [];
          }),
        ),
        n = [
          ...e.flatMap((e) =>
            e.hostname == null
              ? []
              : [
                  {
                    hostId: e.hostId,
                    connectionAnalyticsId: e.connectionAnalyticsId,
                    displayName: e.displayName,
                    source: `codex-managed`,
                    autoConnect: !1,
                    sshAlias: null,
                    sshHost: e.hostname,
                    sshPort: e.sshPort,
                    identity: e.identity,
                  },
                ],
          ),
          ...t.filter((e) => e != null),
        ].map((e) => ({ ...e, autoConnect: this.getAutoConnectForRemoteConnection(e) })),
        r = this.addConnectionAnalyticsIds(n);
      return (
        JZ().info(`load_completed`, {
          safe: { savedRemoteConnectionCount: e.length, totalConnectionCount: r.length },
          sensitive: {},
        }),
        r
      );
    }
    async restoreSavedAliasConnectionsForExistingRemoteProjects(e) {
      if (this.appState.get(t.Fo.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED) === !0) return e;
      let n = this.appState.get(t.Fo.REMOTE_PROJECTS);
      if (n == null || n.length === 0)
        return (
          JZ().info(`remote_project_connection_restore_skipped`, {
            safe: { reason: `no_remote_projects` },
            sensitive: {},
          }),
          this.appState.set(t.Fo.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED, !0),
          e
        );
      try {
        let r = await _Z(tQ()),
          i = QZ({ discoveredRemoteConnections: r, remoteProjects: n, savedRemoteConnections: e }),
          a = i.length - e.length;
        return (
          (0, TP.default)(e, i) || this.appState.set(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS, i),
          JZ().info(`remote_project_connection_restore_completed`, {
            safe: {
              discoveredRemoteConnectionCount: r.length,
              remoteProjectCount: n.length,
              restoredConnectionCount: a,
              savedRemoteConnectionCountAfter: i.length,
              savedRemoteConnectionCountBefore: e.length,
            },
            sensitive: {},
          }),
          this.appState.set(t.Fo.REMOTE_PROJECT_CONNECTION_BACKFILL_COMPLETED, !0),
          i
        );
      } catch (t) {
        JZ().warning(`remote_project_connection_restore_failed`, {
          safe: { remoteProjectCount: n.length, savedRemoteConnectionCount: e.length },
          sensitive: { error: t },
        });
      }
      return e;
    }
    loadSavedRemoteConnections() {
      let e = this.appState.get(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS) ?? [],
        n = e.map((e) => {
          if (`alias` in e && `hostname` in e)
            return e.alias == null
              ? {
                  hostId: e.hostId,
                  connectionAnalyticsId: e.connectionAnalyticsId,
                  displayName: e.displayName,
                  source: `codex-managed`,
                  alias: null,
                  hostname: e.hostname,
                  sshPort: e.sshPort,
                  identity: e.identity,
                }
              : {
                  hostId: e.hostId,
                  connectionAnalyticsId: e.connectionAnalyticsId,
                  displayName: e.displayName,
                  source: `discovered`,
                  alias: e.alias,
                  hostname: null,
                  sshPort: null,
                  identity: null,
                };
          let t = e,
            n = t.sshAlias?.trim() || null;
          return n == null
            ? {
                hostId: t.hostId,
                connectionAnalyticsId: t.connectionAnalyticsId,
                displayName: t.displayName,
                source: `codex-managed`,
                alias: null,
                hostname: t.sshHost,
                sshPort: t.sshPort,
                identity: t.identity,
              }
            : {
                hostId: t.hostId,
                connectionAnalyticsId: t.connectionAnalyticsId,
                displayName: t.displayName,
                source: `discovered`,
                alias: n,
                hostname: null,
                sshPort: null,
                identity: null,
              };
        });
      return (
        (0, TP.default)(e, n) || this.appState.set(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS, n), n
      );
    }
    async refreshRemoteConnectionsForHostId(e) {
      return (
        this.sharedObjectRepository
          .get(`remote_control_connections`)
          ?.some((t) => t.hostId === e) === !0
          ? await this.refreshRemoteControlConnections()
          : await this.refreshRemoteConnections(),
        this.getSharedRemoteConnections()
      );
    }
    getSharedRemoteConnections() {
      return [
        ...(this.sharedObjectRepository.get(`remote_ssh_connections`) ?? []),
        ...(this.sharedObjectRepository.get(`remote_control_connections`) ?? []),
      ];
    }
    getAutoConnectForRemoteConnection(e) {
      return this.getRemoteConnectionAutoConnectByHostId()[e.hostId] ?? !1;
    }
    getRemoteConnectionAutoConnectByHostId() {
      return this.appState.get(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID) ?? {};
    }
    addConnectionAnalyticsIds(e) {
      let n = this.appState.get(t.Fo.REMOTE_CONNECTION_ANALYTICS_ID_BY_HOST_ID) ?? {},
        r = null,
        i = e.map((e) => {
          let t = n[e.hostId] ?? e.connectionAnalyticsId ?? (0, u.randomUUID)();
          return (
            n[e.hostId] !== t && ((r ??= { ...n }), (r[e.hostId] = t)),
            { ...e, connectionAnalyticsId: t }
          );
        });
      return (r != null && this.appState.set(t.Fo.REMOTE_CONNECTION_ANALYTICS_ID_BY_HOST_ID, r), i);
    }
    clearRemoteConnectionAutoConnect(e) {
      let n = this.getRemoteConnectionAutoConnectByHostId();
      if (!(e in n)) return;
      let r = { ...n };
      (delete r[e], this.appState.set(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID, r));
    }
    async loadRemoteControlConnections() {
      try {
        let e = this.getRemoteControlClientEnrollmentStatus(),
          t = Re().remoteControlClientScopedEnvironments
            ? e.then(({ clientId: e }) =>
                e == null
                  ? []
                  : Sg({
                      appServerClient: this.desktopAuthAppServerClient,
                      desktopApiOptions: this.desktopApiOptions,
                      clientId: e,
                    }),
              )
            : Sg({
                appServerClient: this.desktopAuthAppServerClient,
                clientId: void 0,
                desktopApiOptions: this.desktopApiOptions,
              }),
          [n, { clientAuthorized: r, clientId: i }] = await Promise.all([t, e]);
        return (
          this.sharedObjectRepository.set(`local_remote_control_client_id`, i),
          this.sharedObjectRepository.set(`remote_control_connections_state`, {
            available: !0,
            accessRequired: !1,
            authRequired: !1,
            clientAuthorized: r,
          }),
          n
        );
      } catch (e) {
        if (e instanceof yg)
          return (
            this.sharedObjectRepository.set(`local_remote_control_client_id`, null),
            this.sharedObjectRepository.set(`remote_control_connections_state`, {
              available: !1,
              accessRequired: !1,
              authRequired: !1,
              clientAuthorized: !1,
            }),
            []
          );
        if (e instanceof xg)
          return (
            this.sharedObjectRepository.set(`remote_control_connections_state`, {
              available: !1,
              accessRequired: !0,
              authRequired: !1,
              clientAuthorized: !1,
            }),
            []
          );
        if (e instanceof bg)
          return (
            this.sharedObjectRepository.set(`local_remote_control_client_id`, null),
            this.sharedObjectRepository.set(`remote_control_connections_state`, {
              available: !0,
              accessRequired: !1,
              authRequired: !0,
              clientAuthorized: !1,
            }),
            JZ().warning(`load_remote_control_unauthed`, { safe: {}, sensitive: { error: e } }),
            []
          );
        throw (
          this.sharedObjectRepository.set(`remote_control_connections_state`, {
            available: !0,
            accessRequired: !1,
            authRequired: !1,
            clientAuthorized: !1,
          }),
          this.sharedObjectRepository.set(`local_remote_control_client_id`, null),
          e
        );
      }
    }
    removeLocalRemoteControlConnection() {
      let e = this.sharedObjectRepository.get(`remote_control_connections`);
      if (e == null) return;
      let t = this.filterLocalRemoteControlConnection(e);
      t.length !== e.length && this.sharedObjectRepository.set(`remote_control_connections`, t);
    }
    filterLocalRemoteControlConnection(e) {
      let t = this.sharedObjectRepository.get(`local_remote_control_environment_id`),
        n = this.sharedObjectRepository.get(`local_remote_control_installation_id`);
      return t == null && n == null
        ? e
        : e.filter((e) => e.envId !== t && (n == null || e.installationId !== n));
    }
    async refreshLocalRemoteControlClientId() {
      try {
        let { clientId: e } = await this.getRemoteControlClientEnrollmentStatus();
        this.sharedObjectRepository.set(`local_remote_control_client_id`, e);
      } catch (e) {
        JZ().warning(`refresh_local_remote_control_client_id_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    async refreshRemoteControlClientAuthorizationState() {
      try {
        let { clientAuthorized: e, clientId: t } =
          await this.getRemoteControlClientEnrollmentStatus();
        this.sharedObjectRepository.set(`local_remote_control_client_id`, t);
        let n = this.sharedObjectRepository.get(`remote_control_connections_state`);
        if (n == null) return;
        this.sharedObjectRepository.set(`remote_control_connections_state`, {
          ...n,
          clientAuthorized: e,
        });
      } catch (e) {
        JZ().warning(`refresh_remote_control_client_authorization_state_failed`, {
          safe: {},
          sensitive: { error: e },
        });
      }
    }
    getRemoteControlClientEnrollmentStatus() {
      return Wg({
        appServerClient: this.desktopAuthAppServerClient,
        desktopApiOptions: this.desktopApiOptions,
        deviceKeyClient: this.remoteControlDeviceKeyClient,
        globalState: this.appState,
      });
    }
  };
function ZZ(e) {
  return e.reduce(
    (e, t) => (t.source === `codex-managed` ? (e.codexManaged += 1) : (e.discovered += 1), e),
    { codexManaged: 0, discovered: 0 },
  );
}
function QZ({ discoveredRemoteConnections: e, remoteProjects: t, savedRemoteConnections: n }) {
  let r = new Set(
      n.flatMap((e) => {
        let t = e.alias?.trim();
        return t ? [t] : [];
      }),
    ),
    i = new Set(t.map((e) => e.hostId)),
    a = e.flatMap((e) => {
      let t = e.sshAlias?.trim();
      return !t || r.has(t) || !i.has(e.hostId)
        ? []
        : (r.add(t),
          [
            {
              hostId: e.hostId,
              displayName: e.displayName,
              source: `discovered`,
              alias: t,
              hostname: null,
              sshPort: null,
              identity: null,
            },
          ]);
    });
  return a.length === 0 ? n : [...n, ...a];
}
function $Z(e, n) {
  return e == null
    ? !1
    : (0, TP.default)(e, n) ||
        (!e.autoConnect &&
          n.autoConnect &&
          (0, TP.default)({ ...e, autoConnect: n.autoConnect }, n))
      ? !0
      : t.ga(e) && t.ga(n) && e.hostId === n.hostId && e.envId === n.envId;
}
async function eQ(e) {
  await new Promise((t) => {
    setTimeout(t, e);
  });
}
function tQ() {
  return (0, s.join)((0, o.homedir)(), `.ssh`, `config`);
}
var nQ = `config.toml`;
async function rQ({ codexHome: e, hostConfig: n, logger: r = t.Ur() }) {
  if (n.kind === `local`)
    try {
      (await iQ(s.default.join(e ?? t.Mr({ preferWsl: t.Br(n) }), nQ))) &&
        r.info(`Removed remote_control from config before app-server start`);
    } catch (e) {
      r.warning(`Failed to remove remote_control before app-server start`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
async function iQ(e) {
  let n;
  try {
    n = await (0, p.readFile)(e, `utf8`);
  } catch (e) {
    if (cQ(e)) return !1;
    throw e;
  }
  let r = t.Or(n, { integersAsBigInt: !1 });
  if (!oQ(r)) return !1;
  let i = F(aQ(r));
  return (
    await (0, p.writeFile)(
      e,
      i.trim() === ``
        ? ``
        : i.endsWith(`
`)
          ? i
          : `${i}\n`,
      `utf8`,
    ),
    !0
  );
}
function aQ(e) {
  let t = { ...e };
  if ((Object.hasOwn(t, `remote_control`) && delete t.remote_control, !sQ(t.features))) return t;
  let n = { ...t.features };
  return (
    Object.hasOwn(n, `remote_control`) && delete n.remote_control,
    Object.keys(n).length === 0 ? (delete t.features, t) : ((t.features = n), t)
  );
}
function oQ(e) {
  return (
    Object.hasOwn(e, `remote_control`) ||
    (sQ(e.features) && Object.hasOwn(e.features, `remote_control`))
  );
}
function sQ(e) {
  return typeof e == `object` && !!e && !Array.isArray(e);
}
function cQ(e) {
  return e instanceof Error && `code` in e && e.code === `ENOENT`;
}
var lQ = (0, C.createRequire)(__filename),
  uQ = `remote-control-device-key.node`,
  dQ = `codex-device-key-sign-payload/v1`,
  fQ = /^[A-Za-z0-9_-]+$/u;
function pQ({ resourcesPath: e }) {
  let t = null,
    n = () => {
      if (process.platform !== `darwin`)
        throw Error(`Remote control device keys are only available on macOS`);
      if (e == null) throw Error(`Remote control device keys require resourcesPath`);
      return ((t ??= lQ((0, s.join)(e, `native`, uQ))), t);
    };
  return {
    createDeviceKey: (e) => n().createDeviceKey(e ?? `hardware_only`),
    deleteDeviceKey: (e) => n().deleteDeviceKey(e),
    getDeviceKeyPublic: (e) => n().getDeviceKeyPublic(e),
    signDeviceKey: async (e, t) => {
      let r = mQ(t);
      return { ...(await n().signDeviceKey(e, r)), signedPayloadBase64: r.toString(`base64`) };
    },
  };
}
function mQ(e) {
  return Buffer.from(JSON.stringify({ domain: dQ, payload: hQ(e) }), `utf8`);
}
function hQ(e) {
  switch (e.type) {
    case `remoteControlClientConnection`:
      if ((gQ(e.nonce), e.audience !== `remote_control_client_websocket`))
        throw Error(`Invalid remote-control device-key connection audience`);
      if (e.scopes.length !== 1 || e.scopes[0] !== `remote_control_controller_websocket`)
        throw Error(`Invalid remote-control device-key connection scopes`);
      return (
        _Q(e.tokenSha256Base64url),
        {
          accountUserId: e.accountUserId,
          audience: e.audience,
          clientId: e.clientId,
          nonce: e.nonce,
          scopes: e.scopes,
          sessionId: e.sessionId,
          targetOrigin: e.targetOrigin,
          targetPath: e.targetPath,
          tokenExpiresAt: e.tokenExpiresAt,
          tokenSha256Base64url: e.tokenSha256Base64url,
          type: e.type,
        }
      );
    case `remoteControlClientEnrollment`:
      if ((gQ(e.nonce), e.audience !== `remote_control_client_enrollment`))
        throw Error(`Invalid remote-control device-key enrollment audience`);
      return (
        _Q(e.deviceIdentitySha256Base64url),
        {
          accountUserId: e.accountUserId,
          audience: e.audience,
          challengeExpiresAt: e.challengeExpiresAt,
          challengeId: e.challengeId,
          clientId: e.clientId,
          deviceIdentitySha256Base64url: e.deviceIdentitySha256Base64url,
          nonce: e.nonce,
          targetOrigin: e.targetOrigin,
          targetPath: e.targetPath,
          type: e.type,
        }
      );
  }
}
function gQ(e) {
  if (!fQ.test(e) || Buffer.from(e, `base64url`).length < 32)
    throw Error(`Invalid remote-control device-key nonce`);
}
function _Q(e) {
  if (!fQ.test(e) || Buffer.from(e, `base64url`).length !== 32)
    throw Error(`Invalid remote-control device-key SHA-256 digest`);
}
var vQ = `(remote) `;
function yQ(e) {
  let t = {
    id: e.id,
    display_name: e.displayName,
    kind: `ssh`,
    codex_cli_command: e.codexCliCommand,
    terminal_command: e.terminalCommand,
    default_workspaces: e.defaultWorkspaces ?? [],
    [H_]: {
      sshAlias: e.sshAlias ?? null,
      sshHost: e.sshHost,
      sshPort: e.sshPort,
      identity: e.identity,
    },
  };
  return (e.homeDir && (t.home_dir = e.homeDir), t);
}
function bQ(e) {
  let n = t.ha({
    alias: e.sshAlias,
    hostname: e.sshHost,
    sshPort: e.sshPort,
    identity: e.identity,
  });
  return yQ({
    id: e.hostId,
    displayName: `${vQ}${e.displayName}`,
    sshAlias: e.sshAlias ?? null,
    sshHost: e.sshHost,
    sshPort: e.sshPort,
    identity: e.identity,
    codexCliCommand: [],
    terminalCommand: [`ssh`, ...n],
    defaultWorkspaces: [],
  });
}
function xQ(e) {
  return {
    id: e.hostId,
    display_name: e.displayName,
    kind: t.fa,
    codex_cli_command: [],
    terminal_command: [],
    default_workspaces: [],
    env_id: e.envId,
    host_name: e.hostName,
    environment_kind: e.environmentKind,
    online: e.online,
    busy: e.busy,
    os: e.os,
    arch: e.arch,
    app_server_version: e.appServerVersion,
    last_seen_at: e.lastSeenAt,
  };
}
function SQ(e) {
  return t.ga(e) ? xQ(e) : bQ(e);
}
function CQ() {
  var e =
      typeof SuppressedError == `function`
        ? SuppressedError
        : function (e, t) {
            var n = Error();
            return ((n.name = `SuppressedError`), (n.error = e), (n.suppressed = t), n);
          },
    t = {},
    n = [];
  function r(e, t) {
    if (t != null) {
      if (Object(t) !== t)
        throw TypeError(
          `using declarations can only be used with objects, functions, null, or undefined.`,
        );
      if (e) var r = t[Symbol.asyncDispose || Symbol.for(`Symbol.asyncDispose`)];
      if (r === void 0 && ((r = t[Symbol.dispose || Symbol.for(`Symbol.dispose`)]), e)) var i = r;
      if (typeof r != `function`) throw TypeError(`Object is not disposable.`);
      (i &&
        (r = function () {
          try {
            i.call(t);
          } catch (e) {
            return Promise.reject(e);
          }
        }),
        n.push({ v: t, d: r, a: e }));
    } else e && n.push({ d: t, a: e });
    return t;
  }
  return {
    e: t,
    u: r.bind(null, !1),
    a: r.bind(null, !0),
    d: function () {
      var r,
        i = this.e,
        a = 0;
      function o() {
        for (; (r = n.pop()); )
          try {
            if (!r.a && a === 1) return ((a = 0), n.push(r), Promise.resolve().then(o));
            if (r.d) {
              var e = r.d.call(r.v);
              if (r.a) return ((a |= 2), Promise.resolve(e).then(o, s));
            } else a |= 1;
          } catch (e) {
            return s(e);
          }
        if (a === 1) return i === t ? Promise.resolve() : Promise.reject(i);
        if (i !== t) throw i;
      }
      function s(n) {
        return ((i = i === t ? n : new e(n, i)), o());
      }
      return o();
    },
  };
}
var wQ = t.Gr(`notifications-service`),
  TQ = class extends ig {
    constructor(e, t, n) {
      (super(), (this.windowManager = e), (this.origin = t), (this.desktopNotificationManager = n));
    }
    #e(e) {
      let t = this.#n(e);
      t != null && this.#t(t, { stealFocus: process.platform === `win32` });
    }
    #t(e, { stealFocus: t }) {
      (e.isMinimized() && e.restore(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!0),
        e.show(),
        e.focus(),
        t && process.platform === `win32` && e.setAlwaysOnTop(!1));
    }
    #n(e) {
      let t = this.windowManager.getPrimaryWindow() ?? a.BrowserWindow.fromWebContents(e);
      return !t || t.isDestroyed() ? null : t;
    }
    show(e, t) {
      let n = t?.dup();
      (wQ().info(`[desktop-notifications] forward show`, {
        safe: { notificationId: e.id, kind: e.kind },
        sensitive: {},
      }),
        this.desktopNotificationManager.showNotification(
          e,
          this.origin,
          (e) => {
            try {
              var t = CQ();
              let r = t.u(n?.dup());
              (e.actionType === `open` && this.#e(this.origin),
                wQ().info(`[desktop-notifications] emit action`, {
                  safe: {
                    notificationId: e.notificationId,
                    actionType: e.actionType,
                    actionId: e.actionId ?? `none`,
                  },
                  sensitive: {},
                }),
                t.u(r?.({ id: e.actionId ?? null, type: e.actionType, reply: e.reply ?? null })));
            } catch (e) {
              t.e = e;
            } finally {
              t.d();
            }
          },
          () => n?.[Symbol.dispose](),
        ));
    }
    hide(e) {
      e.notificationId == null
        ? (this.desktopNotificationManager.dismissByConversationId(e.conversationId ?? null),
          e.navigationPath != null &&
            this.desktopNotificationManager.dismissByNavigationPath(e.navigationPath))
        : this.desktopNotificationManager.dismissByNotificationId(e.notificationId);
    }
  },
  EQ = class extends ig {
    activeSessionId = null;
    focusOrder = 0;
    lifetime = null;
    owner = null;
    sourceLifetimes = new Map();
    sources = new Map();
    beginSession(e, t) {
      return this.activeSessionId == null
        ? ((this.activeSessionId = e),
          (this.lifetime = t.dup()),
          this.lifetime.onRpcBroken(() => {
            this.endSession(e);
          }),
          !0)
        : !1;
    }
    endSession(e) {
      this.activeSessionId === e &&
        ((this.activeSessionId = null),
        this.lifetime?.[Symbol.dispose](),
        (this.lifetime = null),
        this.owner?.[Symbol.dispose](),
        (this.owner = null));
    }
    setOwner(e, t) {
      this.activeSessionId === e &&
        (this.owner?.[Symbol.dispose](),
        (this.owner = t.dup()),
        (this.sources.size > 0 || this.sourceLifetimes.size === 0) &&
          this.sendSelectedSnapshot(`content_window_changed`));
    }
    clearOwner(e) {
      this.activeSessionId === e && (this.owner?.[Symbol.dispose](), (this.owner = null));
    }
    registerSource(e, t) {
      let n = this.sourceLifetimes.get(e),
        r = t.dup();
      (this.sourceLifetimes.set(e, r),
        n?.[Symbol.dispose](),
        r.onRpcBroken(() => {
          this.sourceLifetimes.get(e) === r && this.removeSource(e);
        }));
    }
    publish(e, t) {
      if (this.sourceLifetimes.has(e))
        switch (t.kind) {
          case `content_window_snapshot`: {
            if (t.snapshot == null) {
              this.removeSource(e);
              break;
            }
            let n = this.getSelectedSource(),
              r = this.sources.get(e);
            this.sources.set(e, {
              focusOrder: t.snapshot.isFocused === !0 ? ++this.focusOrder : (r?.focusOrder ?? 0),
              snapshot: t.snapshot,
            });
            let i = this.getSelectedSource();
            (i?.[0] === e || i?.[0] !== n?.[0] || (n == null && r == null)) &&
              this.sendSelectedSnapshot(t.reason);
            break;
          }
        }
    }
    removeSource(e) {
      let t = this.sourceLifetimes.get(e);
      (this.sourceLifetimes.delete(e), t?.[Symbol.dispose]());
      let n = this.getSelectedSource();
      !this.sources.delete(e) ||
        n?.[0] !== e ||
        this.sendSelectedSnapshot(`content_source_removed`);
    }
    sendSelectedSnapshot(e) {
      try {
        var t = CQ();
        let n = this.getSelectedSource();
        t.u(
          this.owner?.({
            kind: `content_window_snapshot`,
            reason: e,
            snapshot: n?.[1].snapshot ?? null,
          }),
        );
      } catch (e) {
        t.e = e;
      } finally {
        t.d();
      }
    }
    getSelectedSource() {
      let e = null;
      for (let t of this.sources.entries())
        t[1].focusOrder !== 0 && (e == null || t[1].focusOrder > e[1].focusOrder) && (e = t);
      return e;
    }
  },
  DQ = t.Gr(`system-permissions-service`),
  OQ = `x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture`,
  kQ = `x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility`;
function AQ(e) {
  return process.platform === `darwin`
    ? {
        requestMicrophoneAccess: async () => {
          try {
            await a.systemPreferences.askForMediaAccess(`microphone`);
          } catch (e) {
            DQ().error(`Microphone permission request failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }
        },
        openScreenRecordingSettings: async () => {
          try {
            await a.shell.openExternal(OQ);
          } catch (e) {
            DQ().error(`Open screen recording settings failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }
        },
        openAccessibilitySettings: async () => {
          try {
            await a.shell.openExternal(kQ);
          } catch (e) {
            DQ().error(`Open accessibility settings failed`, { safe: {}, sensitive: { error: e } });
          }
        },
        startPermissionSettingsAppDrag: async () => {
          try {
            let t = r.E(),
              n = await a.app.getFileIcon(t, { size: `normal` });
            e.startDrag({ file: t, icon: n });
          } catch (e) {
            DQ().error(`Start permission settings app drag failed`, {
              safe: {},
              sensitive: { error: e },
            });
          }
        },
      }
    : null;
}
var jQ = (0, C.createRequire)(__filename),
  MQ = class e {
    static create(t) {
      let { spawn: n } = jQ(`node-pty`),
        [r, ...i] = t.command,
        a = n(r, i, { cols: t.cols, rows: t.rows, cwd: t.cwd, env: t.env });
      return (
        a.onData((e) => {
          t.callbacks.onData(e);
        }),
        a.onExit(({ exitCode: e, signal: n }) => {
          t.callbacks.onExit({ code: e, signal: NQ(n) });
        }),
        new e(a)
      );
    }
    constructor(e) {
      this.pty = e;
    }
    async write(e) {
      this.pty.write(e);
    }
    async resize(e, t) {
      this.pty.resize(e, t);
    }
    async terminate() {
      this.pty.kill();
    }
    async dispose() {
      this.pty.kill();
    }
  };
function NQ(e) {
  return e == null ? null : String(e);
}
var PQ = class {
  decoder = new v.StringDecoder(`utf8`);
  session = null;
  settled = !1;
  disposed = !1;
  pendingInput = ``;
  scheduledWrite = null;
  constructor(e, t) {
    ((this.sessionPromise = e), (this.callbacks = t), this.run());
  }
  async run() {
    let e;
    try {
      e = await this.sessionPromise;
    } catch {
      if (((this.settled = !0), this.disposed)) return;
      this.callbacks.onExit({ code: null, signal: null });
      return;
    }
    ((this.session = e),
      e.response.then(
        (e) => {
          this.settled = !0;
          let t = this.flushDecoder();
          (!this.disposed && t.length > 0 && this.callbacks.onData(t),
            !this.disposed && this.callbacks.onExit({ code: e.exitCode, signal: null }));
        },
        () => {
          this.settled = !0;
          let e = this.flushDecoder();
          (!this.disposed && e.length > 0 && this.callbacks.onData(e),
            !this.disposed && this.callbacks.onExit({ code: null, signal: null }));
        },
      ));
  }
  handleOutputDelta(e) {
    if (this.disposed || this.settled) return;
    let t = this.decoder.write(e.chunk);
    t.length > 0 && this.callbacks.onData(t);
  }
  write(e) {
    if (this.disposed || this.settled) return Promise.resolve();
    if (((this.pendingInput += e), this.scheduledWrite != null)) return this.scheduledWrite;
    let t = Promise.resolve().then(async () => {
      let e = this.pendingInput;
      ((this.pendingInput = ``), (this.scheduledWrite = null), await this.writeInput(e));
    });
    return ((this.scheduledWrite = t), t);
  }
  async writeInput(e) {
    let t = await this.getActiveSession();
    t == null || this.disposed || this.settled || (await t.write(Buffer.from(e, `utf8`)));
  }
  async resize(e, t) {
    if (this.disposed || this.settled) return;
    let n = await this.getActiveSession();
    n == null || this.disposed || this.settled || (await n.resize({ cols: e, rows: t }));
  }
  async terminate() {
    if (this.disposed || this.settled) return;
    let e = await this.getActiveSession();
    e == null || this.disposed || this.settled || (await e.terminate());
  }
  async dispose() {
    if (
      this.disposed ||
      ((this.disposed = !0), (this.pendingInput = ``), this.flushDecoder(), this.settled)
    )
      return;
    let e = await this.getActiveSession();
    if (!(e == null || this.settled))
      try {
        await e.terminate();
      } catch {}
  }
  flushDecoder() {
    return this.decoder.end();
  }
  async getActiveSession() {
    if (this.session != null) return this.session;
    try {
      return ((this.session = await this.sessionPromise), this.session);
    } catch {
      return null;
    }
  }
};
function FQ(e) {
  let t = ``;
  for (let n = 0; n < e.length; ) {
    if (e[n] !== `\x1B`) {
      ((t += e[n]), (n += 1));
      continue;
    }
    let r = e[n + 1];
    if (r === `[`) {
      let r = n + 2;
      for (; r < e.length; ) {
        let t = e[r];
        if (t >= `@` && t <= `~`) break;
        r += 1;
      }
      if (r >= e.length) {
        t += e.slice(n);
        break;
      }
      if (e[r] === `n`) {
        n = r + 1;
        continue;
      }
      ((t += e.slice(n, r + 1)), (n = r + 1));
      continue;
    }
    if (r === `]`) {
      let r = n + 2,
        i = 0,
        a = -1;
      for (; r < e.length; ) {
        if (e[r] === `\x07`) {
          ((a = r), (i = 1));
          break;
        }
        if (e.startsWith(`\x1B\\`, r)) {
          ((a = r), (i = 2));
          break;
        }
        r += 1;
      }
      if (a === -1) {
        t += e.slice(n);
        break;
      }
      let o = e.slice(n + 2, a);
      if (/^\d+;\?$/.test(o)) {
        n = a + i;
        continue;
      }
      ((t += e.slice(n, a + i)), (n = a + i));
      continue;
    }
    ((t += e[n]), (n += 1));
  }
  return t;
}
var IQ = 16e3,
  LQ = `xterm-256color`,
  RQ = {
    write: () => Promise.resolve(),
    resize: () => Promise.resolve(),
    terminate: () => Promise.resolve(),
    dispose: () => Promise.resolve(),
  };
function zQ(e, t) {
  return `${e}:${t}`;
}
var BQ = class {
  sessions = new Map();
  sessionsByWindowConversation = new Map();
  actionRestartPromises = new Map();
  trackedOrigins = new Set();
  terminalCommand;
  constructor(e, t, n, r, i, a, o) {
    ((this.windowManager = e),
      (this.globalState = t),
      (this.getIntegratedTerminalShellPreference = n),
      (this.getWorktreeShellEnvironmentForCwd = i),
      (this.getHostConfigForHostId = a),
      (this.getProcessConnectionForHostId = o),
      (this.terminalCommand = r ?? null));
  }
  track(e) {
    this.trackedOrigins.has(e.id) ||
      (this.trackedOrigins.add(e.id),
      e.once(`destroyed`, () => {
        (this.trackedOrigins.delete(e.id), this.cleanupForOrigin(e.id));
      }));
  }
  async createOrAttach(e, t) {
    let n = this.getExistingSessionId({
      ownerId: e.id,
      allowConversationSessionFallback: t.type === `terminal-attach`,
      ...t,
    });
    return n
      ? this.attach(e, n, {
          nextSessionId: t.sessionId,
          conversationId: t.conversationId,
          conversationTitle: t.conversationTitle,
          hostId: t.hostId,
          cwd: t.cwd,
          forceCwdSync: t.forceCwdSync,
          cols: t.cols,
          rows: t.rows,
          preserveOnOwnerDestroy: t.preserveOnOwnerDestroy,
        })
      : this.create(e, t);
  }
  write(e, t, n) {
    let r = this.sessions.get(t);
    if (!r) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (r.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    this.performSessionAction({
      origin: e,
      session: r,
      sessionId: t,
      action: async () => {
        await r.backend.write(n);
      },
    });
  }
  runAction(e, t, n) {
    let r = this.sessions.get(t);
    if (!r) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (r.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    let i = (this.actionRestartPromises.get(t) ?? Promise.resolve())
      .catch(() => void 0)
      .then(async () => {
        this.sessions.get(t) === r &&
          (await this.performSessionAction({
            origin: e,
            session: r,
            sessionId: t,
            action: () => this.restartSessionForAction(r, t, n),
          }));
      });
    (this.actionRestartPromises.set(t, i),
      i.finally(() => {
        this.actionRestartPromises.get(t) === i && this.actionRestartPromises.delete(t);
      }));
  }
  resize(e, t, n, r) {
    let i = this.sessions.get(t);
    if (!i) {
      this.sendError(e, t, `Session missing`);
      return;
    }
    if (i.ownerId !== e.id) {
      this.sendError(e, t, `Session owned by another window`);
      return;
    }
    this.resizeSession(e, i, t, n, r);
  }
  close(e, t) {
    let n = this.sessions.get(t);
    if (n) {
      if (n.ownerId !== e.id) {
        this.sendError(e, t, `Session owned by another window`);
        return;
      }
      this.destroySession(n, { code: null, signal: null });
    }
  }
  appendOutput(e, t) {
    let n = this.sessions.get(e);
    n &&
      ((n.buffer = `${n.buffer}${t}`.slice(-IQ)),
      n.attached && this.send(n.owner, { type: `terminal-data`, sessionId: n.id, data: t }));
  }
  closeSession(e) {
    let t = this.sessions.get(e);
    t && this.destroySession(t, { code: null, signal: null });
  }
  getSnapshotForConversationId(e, t) {
    let n = this.sessionsByWindowConversation.get(zQ(t, e));
    if (!n) return null;
    let r = this.sessions.get(n);
    return r
      ? { cwd: r.cwd, shell: r.shell, buffer: r.buffer, truncated: r.buffer.length >= IQ }
      : null;
  }
  async create(e, n) {
    let r = n.sessionId ?? (0, u.randomUUID)(),
      i = this.resolveRequestedCwd(n.cwd, n.hostId);
    try {
      let t = await this.createTerminalBackend({
          hostId: n.hostId,
          conversationTitle: n.conversationTitle,
          sessionId: r,
          requestedCwd: i,
          cols: n.cols ?? 80,
          rows: n.rows ?? 24,
        }),
        a = {
          id: r,
          backend: t.backend,
          owner: e,
          ownerId: e.id,
          buffer: ``,
          cwd: i,
          shell: t.shell,
          shellKind: t.shellKind,
          size: { cols: n.cols ?? 80, rows: n.rows ?? 24 },
          attached: !1,
          conversationId: n.conversationId,
          hostId: n.hostId,
          conversationTitle: n.conversationTitle,
          preserveOnOwnerDestroy: n.preserveOnOwnerDestroy ?? !1,
        };
      (this.sessions.set(r, a),
        n.conversationId &&
          this.sessionsByWindowConversation.set(zQ(e.id, String(n.conversationId)), r),
        t.pendingState.buffer.length > 0 && (a.buffer = t.pendingState.buffer.slice(-IQ)));
      let o = t.initialShellCwd;
      return o != null &&
        !(await this.performSessionAction({
          origin: e,
          session: a,
          sessionId: r,
          action: async () => {
            await this.seedShellCwd(a, o);
          },
        }))
        ? null
        : (this.flushInit(a),
          this.sendAttached(a),
          t.pendingState.exit != null && this.destroySession(a, t.pendingState.exit),
          r);
    } catch (n) {
      return (this.sendError(e, r, t.Oo(n)), null);
    }
  }
  async attach(e, n, r) {
    let i = this.sessions.get(n);
    if (!i) return (this.sendError(e, n, `Session missing`), null);
    if (i.owner.isDestroyed() || i.ownerId === e.id) ((i.owner = e), (i.ownerId = e.id));
    else if (i.ownerId !== e.id)
      return (this.sendError(e, n, `Session owned by another window`), null);
    (r.conversationId &&
      ((i.conversationId = r.conversationId),
      this.sessionsByWindowConversation.set(zQ(e.id, String(r.conversationId)), i.id)),
      r.hostId !== void 0 && (i.hostId = r.hostId),
      r.conversationTitle !== void 0 && (i.conversationTitle = r.conversationTitle));
    let { cols: a, rows: o } = r;
    if (
      a != null &&
      o != null &&
      ((i.size = { cols: a, rows: o }),
      !(await this.performSessionAction({
        origin: e,
        session: i,
        sessionId: n,
        action: async () => {
          await i.backend.resize(a, o);
        },
      })))
    )
      return null;
    if (r.forceCwdSync && r.cwd) {
      let a = this.resolveRequestedCwd(r.cwd, r.hostId),
        o = r.hostId == null ? null : (this.getHostConfigForHostId?.(r.hostId) ?? null);
      if (
        ((o != null && t.va(o)) ||
          i.shellKind !== `posix` ||
          this.resolveLocalCwd(a) !== process.cwd()) &&
        ((i.cwd = a),
        !(await this.performSessionAction({
          origin: e,
          session: i,
          sessionId: n,
          action: async () => {
            await this.seedShellCwd(i, a);
          },
        })))
      )
        return null;
    }
    return (
      r.nextSessionId && r.nextSessionId !== i.id && this.rekeySession(i, r.nextSessionId),
      r.preserveOnOwnerDestroy != null && (i.preserveOnOwnerDestroy = r.preserveOnOwnerDestroy),
      this.flushInit(i),
      this.sendAttached(i),
      i.id
    );
  }
  destroySession(e, n) {
    if (this.sessions.get(e.id) !== e) return;
    (this.sessions.delete(e.id),
      this.actionRestartPromises.delete(e.id),
      e.conversationId && this.deleteConversationMapping(e.ownerId, String(e.conversationId), e.id),
      e.backend.dispose().catch((n) => {
        e.owner.isDestroyed() || this.sendError(e.owner, e.id, t.Oo(n));
      }));
    let r = {
      type: `terminal-exit`,
      sessionId: e.id,
      code: n?.code ?? null,
      signal: n?.signal ?? null,
    };
    e.owner.isDestroyed() || this.send(e.owner, r);
  }
  async restartSessionForAction(e, t, n) {
    let r = e.backend;
    if (((e.backend = RQ), (e.attached = !1), await r.dispose(), this.sessions.get(t) !== e))
      return;
    let i = await this.createTerminalBackend({
      hostId: e.hostId,
      conversationTitle: e.conversationTitle,
      sessionId: t,
      requestedCwd: n.cwd,
      cols: e.size.cols,
      rows: e.size.rows,
    });
    if (this.sessions.get(t) !== e) {
      await i.backend.dispose();
      return;
    }
    if (
      ((e.backend = i.backend),
      (e.cwd = n.cwd),
      (e.shell = i.shell),
      (e.shellKind = i.shellKind),
      (e.buffer = i.pendingState.buffer.slice(-IQ)),
      this.flushInit(e, !0),
      this.sendAttached(e),
      i.pendingState.exit != null)
    ) {
      this.destroySession(e, i.pendingState.exit);
      return;
    }
    await e.backend.write(HQ(e.shellKind, this.resolveShellCwd(e.shellKind, n.cwd), n.command));
  }
  async performSessionAction(e) {
    try {
      return (await e.action(), !0);
    } catch (n) {
      return (
        this.sendError(e.origin, e.sessionId, t.Oo(n)),
        this.destroySession(e.session, { code: null, signal: null }),
        !1
      );
    }
  }
  resizeSession(e, t, n, r, i) {
    return t.size.cols === r && t.size.rows === i
      ? Promise.resolve(!0)
      : ((t.size = { cols: r, rows: i }),
        this.performSessionAction({
          origin: e,
          session: t,
          sessionId: n,
          action: async () => {
            await t.backend.resize(r, i);
          },
        }));
  }
  flushInit(e, t = !1) {
    if (t || e.buffer.length > 0) {
      let t = e.buffer.length > 0 ? FQ(e.buffer) : ``;
      this.send(e.owner, { type: `terminal-init-log`, sessionId: e.id, log: t });
    }
    e.attached = !0;
  }
  sendAttached(e) {
    this.send(e.owner, { type: `terminal-attached`, sessionId: e.id, cwd: e.cwd, shell: e.shell });
  }
  send(e, t) {
    this.windowManager.sendMessageToWebContents(e, t);
  }
  sendError(e, t, n) {
    this.windowManager.sendMessageToWebContents(e, {
      type: `terminal-error`,
      sessionId: t,
      message: n,
    });
  }
  resolveRequestedCwd(e, t) {
    if (e) return e;
    let n = r.i(this.globalState);
    if (n[0]) return n[0];
    if (t != null) {
      let e = this.getHostConfigForHostId?.(t) ?? null;
      if (e?.home_dir != null && e.home_dir.length > 0) return e.home_dir;
    }
    return process.cwd();
  }
  resolveLocalCwd(e) {
    let t = te(e);
    return (0, l.existsSync)(t) ? t : (0, l.existsSync)(e) ? e : process.cwd();
  }
  resolveShellCwd(e, t) {
    if (e === `wsl`) return I(t, !0);
    if (e === `posix`) return t;
    let n = te(t);
    return (0, l.existsSync)(n) ? n : t;
  }
  async seedShellCwd(e, n) {
    await e.backend.write(`cd ${t.Nn(n)}\n`);
  }
  getExistingSessionId(e) {
    if (e.sessionId && this.sessions.has(e.sessionId)) return e.sessionId;
    if (e.allowConversationSessionFallback && e.conversationId) {
      let t = this.sessionsByWindowConversation.get(zQ(e.ownerId, String(e.conversationId)));
      if (t) return t;
    }
  }
  cleanupForOrigin(e) {
    for (let t of Array.from(this.sessions.values()))
      if (t.ownerId === e) {
        if (t.preserveOnOwnerDestroy) {
          t.attached = !1;
          continue;
        }
        this.destroySession(t, { code: null, signal: null });
      }
  }
  rekeySession(e, t) {
    this.sessions.get(e.id) === e &&
      (this.sessions.delete(e.id),
      (e.id = t),
      this.sessions.set(t, e),
      e.conversationId &&
        this.sessionsByWindowConversation.set(zQ(e.ownerId, String(e.conversationId)), t));
  }
  deleteConversationMapping(e, t, n) {
    let r = zQ(e, t);
    this.sessionsByWindowConversation.get(r) === n && this.sessionsByWindowConversation.delete(r);
  }
  resolveTerminalCommand(e) {
    let t = null;
    return (
      e != null && (t = this.getHostConfigForHostId?.(e)?.terminal_command ?? null),
      t && t.length > 0
        ? t
        : this.terminalCommand && this.terminalCommand.length > 0
          ? this.terminalCommand
          : zM(this.getIntegratedTerminalShellPreference() ?? null)
    );
  }
  async buildTerminalEnv(e, n, r) {
    let i = { ...process.env };
    if ((n != null && (i.CODEX_APP_TITLE = n), this.isLocalTerminalSession(r))) {
      let t = await this.getWorktreeShellEnvironmentForCwd(e);
      if (t != null) {
        for (let e of t.exclude) delete i[e];
        Object.assign(i, t.set);
      }
    }
    return (
      process.platform !== `win32` && ((i.TERM = LQ), delete i.TERMINFO, delete i.TERMINFO_DIRS),
      t.Vr(i)
    );
  }
  isLocalTerminalSession(e) {
    return e == null ? !0 : this.getHostConfigForHostId?.(e)?.kind === `local`;
  }
  buildRemoteProcessEnv() {
    return { TERM: LQ, TERMINFO: null, TERMINFO_DIRS: null };
  }
  async createTerminalBackend(e) {
    let { hostId: n } = e,
      r = n == null ? null : (this.getHostConfigForHostId?.(n) ?? null),
      i = null,
      a = { buffer: ``, exit: null },
      o = () => (i == null ? null : this.getSessionForBackend(i)),
      s = {
        onData: (e) => {
          let t = o();
          if (t == null) {
            a.buffer = `${a.buffer}${e}`.slice(-IQ);
            return;
          }
          this.appendOutput(t.id, e);
        },
        onExit: (e) => {
          let t = o();
          if (t == null) {
            a.exit = e;
            return;
          }
          this.destroySession(t, e);
        },
      },
      c = await (n != null && r && t.va(r)
        ? this.createRemoteTerminalBackend({
            hostId: n,
            sessionId: e.sessionId,
            requestedCwd: e.requestedCwd,
            cols: e.cols,
            rows: e.rows,
            callbacks: s,
          })
        : this.createLocalTerminalBackend({
            hostId: e.hostId,
            conversationTitle: e.conversationTitle,
            requestedCwd: e.requestedCwd,
            cols: e.cols,
            rows: e.rows,
            callbacks: s,
          }));
    return ((i = c.backend), { ...c, pendingState: a });
  }
  getSessionForBackend(e) {
    for (let t of this.sessions.values()) if (t.backend === e) return t;
    return null;
  }
  async createLocalTerminalBackend(e) {
    let t = this.resolveLocalCwd(e.requestedCwd),
      n = this.resolveTerminalCommand(e.hostId);
    return {
      backend: MQ.create({
        command: n,
        cwd: t,
        env: await this.buildTerminalEnv(t, e.conversationTitle, e.hostId),
        cols: e.cols,
        rows: e.rows,
        callbacks: e.callbacks,
      }),
      shell: BM(n),
      shellKind: VQ(n),
      initialShellCwd: e.requestedCwd !== t && t === process.cwd() ? e.requestedCwd : void 0,
      pendingState: { buffer: ``, exit: null },
    };
  }
  createRemoteTerminalBackend(e) {
    let t = this.getProcessConnectionForHostId?.(e.hostId) ?? null,
      n = I_(),
      r = null;
    return (
      (r = new PQ(
        t?.startProcess({
          processHandle: e.sessionId,
          command: n,
          tty: !0,
          size: { cols: e.cols, rows: e.rows },
          streamStdoutStderr: !0,
          outputBytesCap: null,
          timeoutMs: null,
          cwd: e.requestedCwd,
          env: this.buildRemoteProcessEnv(),
          onStdoutDelta: (e) => {
            r?.handleOutputDelta(e);
          },
          onStderrDelta: (e) => {
            r?.handleOutputDelta(e);
          },
        }) ?? Promise.reject(Error(`Remote process connection is unavailable`)),
        e.callbacks,
      )),
      { backend: r, shell: BM(n), shellKind: `posix`, pendingState: { buffer: ``, exit: null } }
    );
  }
};
function VQ(e) {
  if (process.platform !== `win32`) return `posix`;
  let t = JQ(e);
  return t === `wsl` || t === `wsl.exe`
    ? `wsl`
    : t === `pwsh` || t === `pwsh.exe` || t === `powershell` || t === `powershell.exe`
      ? `powershell`
      : t === `cmd` || t === `cmd.exe`
        ? `commandPrompt`
        : `posix`;
}
function HQ(e, t, n) {
  if (e === `powershell`) {
    let r = n.replace(
      /\r\n|\r|\n/g,
      `\r
`,
    );
    return `${GQ(e, t)}; ${r}\r
`;
  }
  if (e === `commandPrompt`) {
    let r = n.replace(
      /\r\n|\r|\n/g,
      `\r
`,
    );
    return `${GQ(e, t)} && ${r}\r
`;
  }
  return e === `wsl`
    ? WQ(
        t,
        n,
        `
`,
      )
    : UQ(t, n);
}
function UQ(e, t) {
  return WQ(
    e,
    t,
    process.platform === `win32`
      ? `\r
`
      : `
`,
  );
}
function WQ(e, t, n) {
  let r = t.replace(/\r\n|\r|\n/g, n);
  return `cd ${JSON.stringify(e)} && ${r}${n}`;
}
function GQ(e, t) {
  return e === `powershell`
    ? `Set-Location -LiteralPath ${KQ(t)}`
    : e === `commandPrompt`
      ? `cd /d ${qQ(t)}`
      : `cd ${JSON.stringify(t)}`;
}
function KQ(e) {
  return `'${e.replace(/'/g, `''`)}'`;
}
function qQ(e) {
  return `"${e.replace(/"/g, `""`)}"`;
}
function JQ(e) {
  let t = (e[0] ?? ``).split(/[/\\]/);
  return t[t.length - 1]?.toLowerCase() ?? ``;
}
var YQ = 1e6,
  XQ = t.$s({
    file: t.nc().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*\.html$/),
    hostId: t.nc().trim().min(1).optional(),
    threadId: t.nc().regex(/^[a-zA-Z0-9_-]+$/),
  });
function ZQ({ getAppServerClient: e }) {
  return {
    read: async (n) => {
      let r = XQ.safeParse(n);
      if (!r.success) throw Error(`Invalid visualization read request`);
      let { file: i, hostId: a, threadId: o } = r.data,
        s = e(a),
        [c, l] = await Promise.all([s.codexHome(), s.platformPath()]),
        u = t.Xr(o);
      if (u == null) throw Error(`Invalid visualization read request`);
      let d = l.join(c, `visualizations`, ...u.split(`/`), o, i);
      if ((await s.getMetadata({ path: d }, { bypassCache: !0 })).isSymlink)
        throw Error(`Invalid visualization read request`);
      let { dataBase64: f } = await s.readFile({ path: d }),
        p = Buffer.from(f, `base64`);
      return p.byteLength <= YQ ? { contents: p.toString(`utf8`) } : null;
    },
  };
}
var QQ = class extends ig {
  constructor(e, t) {
    (super(), (this.getAppServerClient = e), (this.globalState = t));
  }
  async read({ hostId: e, path: n, representation: r }) {
    let i = this.getAppServerClient(e),
      a = await t$(n, i, this.globalState);
    switch (r) {
      case `auto`:
        return $Q(await t.jr.readFileBase64(a, i));
      case `blob`:
        return { blob: e$(await t.jr.readFileBase64(a, i)) };
      case `text`:
        return { text: await t.jr.readFile(a, i) };
    }
  }
};
async function $Q(e) {
  let n = typeof e == `string` ? Buffer.from(e, `base64`) : e;
  return (await t.Ea(n.subarray(0, 4096))) === `text`
    ? { text: n.toString(`utf8`) }
    : { blob: e$(e) };
}
function e$(e) {
  return typeof e == `string` ? e : e.toString(`base64`);
}
async function t$(e, n, i) {
  return t.va(n.hostConfig)
    ? n$(e, null, await n.platformPath())
    : n$(ne(e, t.Br(n.hostConfig)), r.i(i)[0] ?? r.s(i)[0] ?? null);
}
function n$(e, n, r = s) {
  let i = r.normalize(r.sep === `\\` ? t.Po(e) : e);
  if (t.S(i, r) || n == null) return i;
  let a = i.split(r.sep).filter(Boolean);
  if (a.length === 0) return n;
  let o = r.join(n, ...a);
  if ((0, l.existsSync)(o)) return o;
  let c = a.indexOf(r.basename(n));
  return c === -1 ? o : r.join(n, ...a.slice(c + 1));
}
var r$ = t.Wr(`WindowContext`),
  i$ = `Codex rebuilt its local database.`,
  a$ = `x-apple.systempreferences:com.apple.preference.security?Privacy_ListenEvent`;
function o$(e) {
  if (e.method !== `configWarning`) return null;
  let { params: n } = e;
  return n == null || n.summary !== i$
    ? null
    : t.n(
        [i$, typeof n.details == `string` ? n.details : ``].join(`
`),
      );
}
var s$ = class {
  appServerConnectionRegistry;
  remoteControlDeviceKeyClient;
  messageHandler;
  remoteConnectionsHandler;
  sharedObjectRepository;
  fetchHandler;
  appState;
  errorReporter;
  workspaceFiles;
  options;
  scopedState;
  primaryRuntimeService;
  keyboardModifierService = hX();
  realtimeContextService = new EQ();
  initialRemoteConnectionsRefresh;
  sshConnectTimeoutSeconds;
  codexMicroService = null;
  codexMicroServicePromise = null;
  disposables = new t.Rn();
  registeredWindows = new Map();
  appServerClients = new Map();
  appViewsByWebContentsId = new Map();
  ipcClientsByWebContentsId = new Map();
  constructor(e) {
    ((this.options = e),
      (this.appState = e.appGlobalState),
      (this.errorReporter = e.errorReporter),
      (this.scopedState = e.globalState),
      (this.sharedObjectRepository = new t.o()),
      this.sharedObjectRepository.set(`host_config`, Iu),
      this.sharedObjectRepository.set(`remote_ssh_connections`, []),
      this.sharedObjectRepository.set(`remote_control_connections_state`, {
        available: !1,
        accessRequired: !1,
        authRequired: !1,
        clientAuthorized: !1,
      }),
      this.sharedObjectRepository.set(`local_remote_control_client_id`, null),
      this.sharedObjectRepository.set(
        `local_remote_control_environment_id`,
        e.appGlobalState.get(Kr),
      ),
      this.sharedObjectRepository.set(
        `local_remote_control_installation_id`,
        e.appGlobalState.get(qr),
      ));
    let n = t.Et();
    n != null && this.sharedObjectRepository.set(`local_remote_control_enabled`, n);
    let r = this.createAppServerConnection(z);
    ((this.remoteControlDeviceKeyClient = pQ({
      resourcesPath: Ze({ env: process.env, resourcesPath: process.resourcesPath }),
    })),
      this.appServerClients.set(z, r),
      (this.appServerConnectionRegistry = new t.Dn()),
      this.appServerConnectionRegistry.addConnection(z, r),
      (this.workspaceFiles = new QQ((e) => this.getAppServerClient(e), this.scopedState)));
    let i = new BQ(
      e.windowManager,
      this.scopedState,
      () => e.settingsStore.getEffective(t._i.integratedTerminalShell.key),
      Iu.terminal_command ?? null,
      async (n) => {
        let i = await e.gitManager.getWorktreeRepository(t.xa(n), t.ht(r));
        return i == null ? null : t.ot(i);
      },
      (e) => this.getHostConfigForHostId(e),
      (e) => this.appServerConnectionRegistry.getMaybeConnection(e),
    );
    this.remoteConnectionsHandler = new XZ(
      this.appState,
      this.sharedObjectRepository,
      z,
      this.appServerConnectionRegistry,
      {
        desktopOriginator: e.desktopOriginator,
        devApiBaseUrl: e.devApiBaseUrl,
        prodApiBaseUrl: e.prodApiBaseUrl,
      },
      r,
      this.remoteControlDeviceKeyClient,
      this.getHostConfigForHostId.bind(this),
      this.getAppServerClient.bind(this),
      this.createAndRegisterRemoteConnection.bind(this),
      this.disposeRemoteConnection.bind(this),
    );
    let a = JX({
      appServerConnectionRegistry: this.appServerConnectionRegistry,
      errorReporter: e.errorReporter,
      globalState: this.scopedState,
      hostId: z,
      sharedObjectRepository: this.sharedObjectRepository,
      windowManager: e.windowManager,
    });
    ((this.primaryRuntimeService = a.rpc),
      (this.fetchHandler = new kN(
        this.scopedState,
        (e) => this.getRegisteredWindowIpcClient(e),
        this.appServerConnectionRegistry,
        e.gitManager,
        r,
        this.remoteConnectionsHandler,
        e.hotkeyWindowLifecycleManager,
        e.windowManager,
        i,
        a.isWorkspaceDependenciesFeatureEnabled,
        (e) => this.getAppServerClient(e),
        (e) => this.messageHandler.getWorktreeService(e),
        e.requestGitWorker,
        this.sharedObjectRepository,
        e.errorReporter,
        e.repoRoot,
        e.globalDictationHotkeyController,
        () => {
          this.messageHandler.getBrowserSidebarManager().reloadOpenPages();
        },
        e.refreshApplicationMenu,
        e.requestComputerUseCaptureWorker,
        e.subscribeComputerUseCaptureWorkerEvent,
        () => {
          (e.windowManager.refreshAppShellCommandAccelerators(),
            this.messageHandler.getBrowserSidebarManager().refreshOwnerCommandAccelerators());
        },
        e.settingsStore,
        e.updateDockIcon,
        e.realtimeSessionRecorder,
      )),
      this.disposables.add(() => this.fetchHandler.dispose()),
      this.disposables.add(a.startUpdatePolling()));
    let o = cP(this.scopedState);
    this.disposables.add(
      r.registerInternalNotificationHandler((e) => {
        e.method === `account/updated` && e.params?.authMode == null && o.clear();
      }),
    );
    let s = new bP(this.fetchHandler, {
        desktopOriginator: e.desktopOriginator,
        devApiBaseUrl: e.devApiBaseUrl,
        integrityStateStore: o,
        messageChannel: e.messageChannel,
        appServerConnectionRegistry: this.appServerConnectionRegistry,
        prodApiBaseUrl: e.prodApiBaseUrl,
      }),
      c = new dX({
        buildFlavor: e.buildFlavor,
        gitWorkerInvocationSampler: e.gitWorkerInvocationSampler,
      });
    ((this.messageHandler = new tX(
      z,
      Iu,
      r,
      e.windowManager,
      e.avatarOverlayManager,
      this.appServerConnectionRegistry,
      s,
      this.scopedState,
      this.sharedObjectRepository,
      i,
      e.gitManager,
      e.requestGitWorker,
      e.desktopNotificationManager,
      e.errorReporter,
      e.desktopSentry,
      e.updateWorkerSentryUser,
      (e) => this.getRegisteredWindowIpcClient(e),
      e.inboxManager,
      e.datadogLogger,
      e.maxLogLevel,
      e.buildFlavor,
      e.subscribeGitWorkerEvent,
      c,
      (e) => this.getHostConfigForHostId(e),
      e.hasActiveComputerUseTurn,
      e.settingsStore,
    )),
      this.disposables.add(
        this.sharedObjectRepository.addSubscriber((e) => {
          if (e === `remote_ssh_connections` || e === `remote_control_connections`) {
            this.remoteConnectionsHandler.reconcileRemoteConnections(
              this.getSharedRemoteConnections(),
            );
            return;
          }
          if (
            e === `local_remote_control_environment_id` ||
            e === `local_remote_control_installation_id`
          ) {
            let e = this.sharedObjectRepository.get(`local_remote_control_environment_id`),
              t = this.sharedObjectRepository.get(`local_remote_control_installation_id`);
            (e != null && this.appState.set(Kr, e),
              t != null && this.appState.set(qr, t),
              this.remoteConnectionsHandler.handleLocalRemoteControlIdentityChanged());
          }
        }),
      ),
      (this.initialRemoteConnectionsRefresh = Promise.all([
        this.remoteConnectionsHandler.refreshRemoteConnections(),
        this.remoteConnectionsHandler.refreshLocalRemoteControlClientId(),
      ])
        .then(() => void 0)
        .catch((e) => {
          r$.warning(`Failed to refresh remote connections`, { safe: {}, sensitive: { error: e } });
        })));
  }
  createAppHost(e) {
    let i = {
      debugWindowManager: this.options.windowManager.debugWindowManager,
      getBrowserSnapshot: () => this.messageHandler.getBrowserSidebarManager().getDebugSnapshot(),
      openBrowserTabOwner: (e) =>
        this.messageHandler.getBrowserSidebarManager().openDebugTabOwner(e),
      origin: e,
    };
    return new ag({
      appActions: {
        runInPrimaryWindow: async (e) => {
          let t = this.options.windowManager.getPrimaryWindow();
          if (t == null) throw Error(`No primary app window is available`);
          let n = this.appViewsByWebContentsId.get(t.webContents.id);
          if (n == null) throw Error(`The primary app window is not ready`);
          return (await n.services).appActions.run(e);
        },
      },
      appUpdates: Yv({
        getPrimaryWindow: () => this.options.windowManager.getPrimaryWindow(),
        origin: e,
        sparkleManager: this.options.sparkleManager,
      }),
      avatarOverlay: this.options.avatarOverlayManager.rpcFor(e),
      browserProfileImport: vy(),
      debug: new hb(i),
      hotkeyWindowHotkeys: this.options.hotkeyWindowLifecycleManager.rpcFor(e),
      keyboardModifiers: this.keyboardModifierService,
      appshotHotkeys: this.options.appshotHotkeyController.rpc,
      browserUseOriginState: { updateOriginRules: My },
      customAvatars: this.createCustomAvatarsService(),
      fileAttachments: {
        countFolderFiles: ({ folderPath: e, hostId: n }) => t.kt(e, this.getAppServerClient(n)),
        persistImageFileToTemp: CX,
      },
      workspaceFiles: this.workspaceFiles,
      owlFeatures: n.a() ? r.S() : void 0,
      primaryRuntime: this.primaryRuntimeService,
      chromeNativeHost: this.createChromeNativeHostService(),
      projectWritableRoots: this.messageHandler.createProjectWritableRootsService(e),
      realtimeContext: this.realtimeContextService,
      systemPermissions: AQ(e),
      threadArchive: this.createThreadArchiveService(),
      visualizations: ZQ({ getAppServerClient: (e) => this.getAppServerClient(e) }),
      codexMicro: this.createCodexMicroHostService(e),
      notifications: new TQ(this.options.windowManager, e, this.options.desktopNotificationManager),
    });
  }
  async registerAppView(e, t) {
    let { appUpdates: n } = await t.services;
    e.isDestroyed() ||
      (this.appViewsByWebContentsId.set(e.id, t),
      e.once(`destroyed`, () => {
        this.appViewsByWebContentsId.get(e.id) === t && this.appViewsByWebContentsId.delete(e.id);
      }),
      await n.stateChanged(this.getAppUpdateViewState()));
  }
  broadcastAppUpdateState() {
    let e = this.getAppUpdateViewState();
    for (let t of this.appViewsByWebContentsId.values())
      t.services
        .then(({ appUpdates: t }) => t.stateChanged(e))
        .catch((e) => {
          r$.warning(`Failed to publish app update state to renderer`, {
            safe: {},
            sensitive: { error: e },
          });
        });
  }
  async applyCodexAppConfigFromDisk(e) {
    let t = await Lc(e);
    return t.status === `loaded`
      ? {
          status: `applied`,
          configPath: t.configPath,
          result: await this.applyCodexAppConfig(t.config),
        }
      : (t.status === `missing` && (this.sshConnectTimeoutSeconds = void 0), t);
  }
  getCodexMicroService() {
    return this.codexMicroService == null
      ? ((this.codexMicroServicePromise ??= Promise.resolve()
          .then(() => require(`./codex-micro-service.js`))
          .then(({ CodexMicroService: e }) => {
            let t = new e({
              onDeviceStateChanged: (e) => {
                this.options.windowManager.sendMessageToAllWindows({
                  type: `codex-micro-device-state-changed`,
                  state: e,
                });
              },
              onHidEvent: (e) => {
                let t = this.options.windowManager.getPrimaryWindow();
                t != null &&
                  this.options.windowManager.sendMessageToWindow(t, {
                    type: `codex-micro-hid-event`,
                    event: e,
                  });
              },
              onJoystickEvent: (e) => {
                let t = this.options.windowManager.getPrimaryWindow();
                t != null &&
                  this.options.windowManager.sendMessageToWindow(t, {
                    type: `codex-micro-joystick-event`,
                    event: e,
                  });
              },
            });
            return (
              (this.codexMicroService = t),
              this.disposables.add(() => {
                t.dispose();
              }),
              t
            );
          })
          .finally(() => {
            this.codexMicroServicePromise = null;
          })),
        this.codexMicroServicePromise)
      : Promise.resolve(this.codexMicroService);
  }
  createCustomAvatarsService() {
    return {
      load: () =>
        t.zt({
          appServerClient: this.getAppServerClient(),
          preferWsl: !!process.env.WSL_DISTRO_NAME,
        }),
    };
  }
  createChromeNativeHostService() {
    return Zy({
      devRuntimeRepoRoot: process.env.NODE_ENV === `development` ? this.options.repoRoot : void 0,
      getCodexHome: (e) => this.getAppServerClient(e).codexHome(),
      getHostConfig: (e) => this.getHostConfigForHostId(e),
    });
  }
  createThreadArchiveService() {
    return {
      archiveInactiveThread: async ({ hostId: e, threadId: n }) => {
        let r = this.getAppServerClient(e);
        if (t.va(r.hostConfig)) throw Error(`Archiving inactive chats is only supported locally`);
        let i = await t.sr({ codexHome: await r.codexHome(), threadId: n });
        return (
          i && this.appServerConnectionRegistry.getConnection(e).handleArchivedThreadSideEffects(n),
          { success: i }
        );
      },
    };
  }
  createCodexMicroHostService(e) {
    return {
      focusWindow: async () =>
        this.options.windowManager.getPrimaryWindow()?.webContents === e
          ? this.options.windowManager.showPrimaryWindow({ stealFocus: !0 })
          : !1,
      getState: async () => {
        let e = await this.getCodexMicroService();
        return (
          e?.start(), e?.getState() ?? { status: `not-detected`, error: null, battery: null }
        );
      },
      getInputMonitoringPermissionStatus: async () => yX(),
      openInputMonitoringSettings: async () => {
        try {
          await a.shell.openExternal(a$);
        } catch (e) {
          r$.error(`Open input monitoring settings failed`, { safe: {}, sensitive: { error: e } });
        }
      },
      updateLighting: async (t) =>
        this.options.windowManager.getPrimaryWindow()?.webContents === e
          ? ((await (await this.getCodexMicroService())?.updateLighting(t)) ?? !1)
          : !1,
    };
  }
  async applyCodexAppConfig(e) {
    ((this.sshConnectTimeoutSeconds = e.sshConnectTimeoutSeconds),
      await this.initialRemoteConnectionsRefresh);
    let n = Zv({ config: e, globalState: this.appState }),
      { remoteConnections: r } = await this.remoteConnectionsHandler.refreshRemoteConnections(),
      i = Qv({ config: e, globalState: this.appState, remoteConnections: r });
    return (
      n && i.changedGlobalStateKeys.unshift(t.Fo.CODEX_MANAGED_REMOTE_CONNECTIONS),
      i.changedGlobalStateKeys.includes(t.Fo.REMOTE_CONNECTION_AUTO_CONNECT_BY_HOST_ID) &&
        (await this.remoteConnectionsHandler.refreshRemoteConnections()),
      i.changedGlobalStateKeys.length > 0 &&
        this.options.windowManager.sendMessageToAllWindows({
          type: `global-state-updated`,
          keys: i.changedGlobalStateKeys,
        }),
      i.remoteProjectsChanged &&
        this.options.windowManager.sendMessageToAllWindows({
          type: `workspace-root-options-updated`,
        }),
      this.remoteConnectionsHandler.connectRemoteConnectionsAndLogFailures(
        i.remoteConnectionHostIdsToConnect,
      ),
      i
    );
  }
  registerWindow(e) {
    let { webContents: t } = e,
      n = new VJ(t);
    (this.registeredWindows.set(t.id, n),
      t.once(`destroyed`, () => {
        this.registeredWindows.delete(t.id);
      }));
    for (let e of this.appServerConnectionRegistry.getAllHostIds())
      this.appServerConnectionRegistry.getConnection(e).registerWebviewWindow(n);
    this.registerIpcClientForWebContents(e.webContents);
  }
  handleMessage(e, t) {
    return this.messageHandler.handleMessage(e, t);
  }
  getBrowserSidebarManager() {
    return this.messageHandler.getBrowserSidebarManager();
  }
  getBrowserSessionRegistry() {
    return this.messageHandler.getBrowserSessionRegistry();
  }
  flushTelemetry() {
    this.messageHandler.flushTelemetry();
  }
  dispose() {
    let e = this.appServerConnectionRegistry.getAllHostIds();
    (this.messageHandler.dispose(), this.disposables.dispose());
    for (let t of e)
      (this.appServerConnectionRegistry.getMaybeConnection(t)?.dispose(),
        this.appServerConnectionRegistry.removeConnection(t),
        this.appServerClients.delete(t));
    (this.registeredWindows.clear(), this.appViewsByWebContentsId.clear());
    for (let { dispose: e } of this.ipcClientsByWebContentsId.values()) e();
    this.ipcClientsByWebContentsId.clear();
  }
  getMessageHandler() {
    return this.messageHandler;
  }
  getSharedObject(e) {
    return this.sharedObjectRepository.get(e);
  }
  getAppUpdateViewState() {
    return {
      downloadProgressPercent: this.options.sparkleManager.getDownloadProgressPercent(),
      installProgressPercent: this.options.sparkleManager.getInstallProgressPercent(),
      isUpdateReady: this.options.sparkleManager.getIsUpdateReady(),
      lifecycleState: this.options.sparkleManager.getUpdateLifecycleState(),
      relaunchNotice: this.options.sparkleManager.getRelaunchNotice(),
    };
  }
  setChronicleConfig(e) {
    this.sharedObjectRepository.set(`codex_chronicle_config`, e);
  }
  getSharedObjectSnapshot() {
    return this.sharedObjectRepository.getSnapshot();
  }
  getHostConfigForHostId(e) {
    if (e === `local`) return Iu;
    let t = this.getSharedRemoteConnections().find((t) => t.hostId === e);
    if (!t) throw Error(`Host config for host ID ${e} not found`);
    return SQ(t);
  }
  getSharedRemoteConnections() {
    return [
      ...(this.sharedObjectRepository.get(`remote_ssh_connections`) ?? []),
      ...(this.sharedObjectRepository.get(`remote_control_connections`) ?? []),
    ];
  }
  createAppServerConnection(e, r = Iu, i = !1, o, s) {
    let c = {
        desktopOriginator: this.options.desktopOriginator,
        devApiBaseUrl: this.options.devApiBaseUrl,
        prodApiBaseUrl: this.options.prodApiBaseUrl,
      },
      l = Ze({ env: process.env, resourcesPath: process.resourcesPath }),
      u;
    if (t._a(r)) {
      if (o == null)
        throw Error(
          `Remote control transport requires a desktop app-server client for authentication.`,
        );
      if (s == null)
        throw Error(`Remote control transport requires the remote-control device-key client.`);
      u = c$({
        transportKind: `remote-control`,
        hostConfig: r,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        desktopApiOptions: c,
        desktopAuthAppServerClient: o,
        remoteControlDeviceKeyClient: s,
        globalState: this.appState,
        getSshConnectTimeoutSeconds: () => this.sshConnectTimeoutSeconds,
      });
    } else
      u = c$({
        transportKind: `standard`,
        hostConfig: r,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        desktopApiOptions: c,
        globalState: this.appState,
        getSshConnectTimeoutSeconds: () => this.sshConnectTimeoutSeconds,
      });
    let d = this.options.hostProcess.getVersion(),
      f = new t.En(this.options.messageChannel, {
        buildFlavor: this.options.buildFlavor,
        clientInfo: { name: Ql, title: `Codex Desktop`, version: d },
        defaultServiceName: $l,
        repoRoot: this.options.repoRoot,
        resourcesPath: l,
        errorReporter: this.options.errorReporter,
        sharedObjectRepository: this.sharedObjectRepository,
        hostId: e,
        hostConfig: r,
        onArchiveCleanup: async ({
          conversationId: t,
          cwd: n,
          replacementOwnerThreadId: r,
          replacementOwnerCwd: i,
        }) => {
          await this.messageHandler
            .getWorktreeService(e)
            .handleArchiveCleanup({
              conversationId: t,
              cwd: n,
              replacementOwnerThreadId: r,
              replacementOwnerCwd: i,
            });
        },
        transport: u,
        hostProcess: this.options.hostProcess,
        isOnline: () => a.net.isOnline(),
        supportsAttestationRequests: ye,
        isMcpAppsDilEnabled: () => Re().dil,
        beforeConnect: r.id === `local` ? () => rQ({ hostConfig: r }) : void 0,
        prepareRemoteChatgptLoginPortForward: async () => {
          await Q_({ caller: `ssh_connect` });
        },
        ensureAuth: i,
        getRemoteConnectionMaxRetryAttempts: () =>
          this.appState.get(t.Fo.REMOTE_CONNECTION_MAX_RETRY_ATTEMPTS) ?? null,
      });
    if (r.id === `local` && this.options.onAppServerSqliteRecovery != null) {
      let e = this.options.onAppServerSqliteRecovery;
      f.registerInternalNotificationHandler((t) => {
        let n = o$(t);
        n != null &&
          (r$.info(`App-server SQLite recovery config warning received`, {
            safe: {},
            sensitive: { backupFolder: n.backupFolder, databasePath: n.databasePath },
          }),
          e(n));
      });
    }
    return (
      f.registerInternalServerRequestHandler({
        methods: [`item/commandExecution/requestApproval`, `mcpServer/elicitation/request`],
        handler: (t) => (this.messageHandler.revealWindowsReviewRequest(e, t), null),
      }),
      f.registerInternalServerRequestHandler({
        methods: [`attestation/generate`],
        handler: xe({ bundleIdentifier: n.r(this.options.buildFlavor), resourcesPath: l }),
      }),
      f
    );
  }
  createAndRegisterRemoteConnection(e) {
    let t = SQ(e),
      n = this.createAppServerConnection(
        e.hostId,
        t,
        !0,
        this.getLocalAppServerClient(),
        this.remoteControlDeviceKeyClient,
      );
    this.appServerClients.set(e.hostId, n);
    for (let e of this.registeredWindows.values()) n.registerWebviewWindow(e);
    return n;
  }
  disposeRemoteConnection(e) {
    let t = this.appServerConnectionRegistry.getMaybeConnection(e);
    t &&
      (t.dispose(),
      this.appServerConnectionRegistry.removeConnection(e),
      this.appServerClients.delete(e));
  }
  getLocalAppServerClient() {
    return this.appServerConnectionRegistry.getConnection(z);
  }
  getAppServerClient(e = z) {
    let t = this.appServerClients.get(e);
    if (!t) throw Error(`App server client for host ID ${e} not found`);
    return t;
  }
  registerIpcClientForWebContents(e) {
    if (
      this.appServerConnectionRegistry.getConnection(`local`).getTransportKind() !== `stdio` ||
      this.ipcClientsByWebContentsId.has(e.id)
    )
      return;
    let n = new t.C(`desktop`, this.errorReporter),
      r = new t.Rn();
    r.add(
      n.addAnyBroadcastHandler(async (t) => {
        let n = { ...t, type: `ipc-broadcast` };
        this.messageHandler.sendMessageToOrigin(e, n);
      }),
    );
    let i = async (t) => (await this.messageHandler.getThreadRole(e, t.conversationId)) === `owner`;
    (r.add(
      n.addRequestHandler(`thread-follower-start-turn`, i, async (t) =>
        this.messageHandler.handleThreadFollowerStartTurnRequest(e, t),
      ),
    ),
      r.add(
        n.addRequestHandler(`thread-follower-load-complete-history`, i, async (t) =>
          this.messageHandler.handleThreadFollowerLoadCompleteHistoryRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-compact-thread`, i, async (t) =>
          this.messageHandler.handleThreadFollowerCompactThreadRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-steer-turn`, i, async (t) =>
          this.messageHandler.handleThreadFollowerSteerTurnRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-interrupt-turn`, i, async (t) =>
          this.messageHandler.handleThreadFollowerInterruptTurnRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-update-thread-settings`, i, async (t) =>
          this.messageHandler.handleThreadFollowerUpdateThreadSettingsRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-edit-last-user-turn`, i, async (t) =>
          this.messageHandler.handleThreadFollowerEditLastUserTurnRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-command-approval-decision`, i, async (t) =>
          this.messageHandler.handleThreadFollowerCommandApprovalDecisionRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-file-approval-decision`, i, async (t) =>
          this.messageHandler.handleThreadFollowerFileApprovalDecisionRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-permissions-request-approval-response`, i, async (t) =>
          this.messageHandler.handleThreadFollowerPermissionsRequestApprovalResponseRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-submit-user-input`, i, async (t) =>
          this.messageHandler.handleThreadFollowerSubmitUserInputRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(
          `thread-follower-submit-mcp-server-elicitation-response`,
          i,
          async (t) =>
            this.messageHandler.handleThreadFollowerSubmitMcpServerElicitationRequest(e, t),
        ),
      ),
      r.add(
        n.addRequestHandler(`thread-follower-set-queued-follow-ups-state`, i, async (t) =>
          this.messageHandler.handleThreadFollowerSetQueuedFollowUpsStateRequest(e, t),
        ),
      ),
      r.add(() => n.dispose()),
      this.ipcClientsByWebContentsId.set(e.id, {
        client: n,
        dispose: () => {
          (r.dispose(), this.messageHandler.rejectPendingThreadFollowerActionRequestsForOrigin(e));
        },
      }),
      e.once(`destroyed`, () => {
        this.disposeIpcClientForWebContents(e);
      }));
  }
  disposeIpcClientForWebContents(e) {
    let t = this.ipcClientsByWebContentsId.get(e.id);
    t && (this.ipcClientsByWebContentsId.delete(e.id), t.dispose());
  }
  getRegisteredWindowIpcClient(e) {
    return this.ipcClientsByWebContentsId.get(e.id)?.client ?? null;
  }
};
function c$(e) {
  let n = yv(e.hostConfig);
  if (n)
    return (
      r$.info(`[ssh-websocket-v0] selected app-server transport`, {
        safe: { hostId: e.hostConfig.id },
      }),
      new _v({ ...n, getConnectTimeoutSeconds: e.getSshConnectTimeoutSeconds })
    );
  if (e.transportKind === `remote-control`)
    return new Hg({
      hostConfig: e.hostConfig,
      appServerClient: e.desktopAuthAppServerClient,
      deviceKeyClient: e.remoteControlDeviceKeyClient,
      desktopApiOptions: e.desktopApiOptions,
      globalState: e.globalState,
    });
  let r = t.sn(e.hostConfig);
  return r
    ? new t.on({ hostConfig: e.hostConfig, websocketUrl: r })
    : new t.On({
        hostConfig: e.hostConfig,
        repoRoot: e.repoRoot,
        resourcesPath: e.resourcesPath,
        defaultOriginator: e.defaultOriginator,
      });
}
function l$({
  app: e,
  globalState: n,
  settingsStore: i,
  windowServices: a,
  repoRoot: o,
  errorReporter: s,
  desktopSentry: c,
  datadogLogger: l,
  maxLogLevel: u,
  buildFlavor: d,
  sparkleManager: f,
  avatarOverlayManager: p,
  hotkeyWindowLifecycleManager: m,
  hasActiveComputerUseTurn: h,
  globalDictationHotkeyController: g,
  appshotHotkeyController: _,
  gitManager: v,
  requestComputerUseCaptureWorker: y,
  requestGitWorker: b,
  refreshApplicationMenu: x,
  subscribeComputerUseCaptureWorkerEvent: S,
  subscribeGitWorkerEvent: C,
  gitWorkerInvocationSampler: w,
  updateWorkerSentryUser: T,
  appEvent: E,
  onCompletedThreadsChanged: D,
  onAppServerSqliteRecovery: O,
}) {
  let k = new Lm(e),
    A = new Im({ platform: process.platform, onCompletedThreadsChanged: D }),
    j = new Wm(),
    M,
    N = new Gm({
      getRecordingGeneration: () => j.getGeneration(),
      isRecordingEnabled: () => j.isEnabled(),
      getRolloutPath: async (e) =>
        (await M.appServerConnectionRegistry.getConnection(`local`).readThread(e))?.path ?? null,
      hostId: z,
      rootDirectory: Gm.getRootDirectory({ codexHome: t.Mr() }),
    }),
    P = new Rm({
      inboxItemsChanged: (e) => {
        a.sendMessageToAllWindows({ type: e });
      },
    }),
    F = () => {};
  return (
    r.l(n, Iu.default_workspaces),
    (M = new s$({
      hostProcess: k,
      appGlobalState: n,
      settingsStore: i,
      windowManager: a.windowManager,
      globalState: n,
      repoRoot: o,
      errorReporter: s,
      desktopSentry: c,
      desktopNotificationManager: A,
      updateWorkerSentryUser: T,
      gitManager: v,
      inboxManager: P,
      datadogLogger: l,
      maxLogLevel: u,
      buildFlavor: d,
      sparkleManager: f,
      gitWorkerInvocationSampler: w,
      desktopOriginator: Ql,
      devApiBaseUrl: Zl,
      prodApiBaseUrl: Xl,
      messageChannel: R,
      avatarOverlayManager: p,
      hotkeyWindowLifecycleManager: m,
      hasActiveComputerUseTurn: h,
      globalDictationHotkeyController: g,
      realtimeSessionRecorder: N,
      refreshApplicationMenu: x,
      updateDockIcon: a.updateDockIcon,
      appshotHotkeyController: _,
      requestComputerUseCaptureWorker: y,
      requestGitWorker: b,
      subscribeComputerUseCaptureWorkerEvent: S,
      subscribeGitWorkerEvent: C,
      onAppServerSqliteRecovery: O,
    })),
    M.getMessageHandler().setOnBrowserSidebarStateChanged(F),
    M.appServerConnectionRegistry.getConnection(z).registerInternalNotificationHandler((e) => {
      if ((N.record(e), e.method === `account/login/completed`)) {
        a.windowManager.showPrimaryWindow({ stealFocus: !0 });
        return;
      }
      e.method === `turn/completed` && E.emit(`turnComplete`);
    }),
    a.setWindowContext(M),
    {
      desktopNotificationManager: A,
      getWindowContext: () => M,
      getElectronMessageHandlerForWindow: (e) => {
        let t = a.getContextForWebContents(e.webContents);
        return t ? t.getMessageHandler() : null;
      },
      getAppServerClientForHost: ({ hostId: e }) => {
        try {
          return M.getAppServerClient(e);
        } catch {
          return null;
        }
      },
      getWorktreeCleanupInputsForHost: async ({ hostKey: t, threadIds: r }) => {
        let a = M.appServerConnectionRegistry.getMaybeConnection(t);
        return {
          threadMetadataById: a
            ? await OY(a, r)
            : Object.fromEntries(
                r.map((e) => [
                  e,
                  { updatedAtMs: Date.now(), isInProgress: !0, cwd: null, readFailed: !0 },
                ]),
              ),
          pinnedThreadIds: kY(n),
          protectPreMigrationOwnerlessWorktrees: PY(n),
          autoCleanupEnabled: NY(i, n, e.isPackaged),
          keepCount: MY(i),
        };
      },
      setOnBrowserSidebarStateChanged: (e) => {
        ((F = e), M.getMessageHandler().setOnBrowserSidebarStateChanged(F));
      },
      setRealtimeSessionRecordingEnabled: (e) => {
        j.setEnabled(e);
      },
      flushAndDisposeContext: () => {
        (M.dispose(), M.flushTelemetry());
      },
    }
  );
}
function u$(e) {
  return (
    a.ipcMain.handle(zl, async (t, n) => {
      if (!e(t)) return;
      let r = a.BrowserWindow.fromWebContents(t.sender),
        i = a.Menu.getApplicationMenu()?.getMenuItemById(n.menuId)?.submenu;
      if (i)
        return new Promise((e) => {
          i.popup({ window: r ?? void 0, x: Math.round(n.x), y: Math.round(n.y), callback: e });
        });
    }),
    () => a.ipcMain.removeHandler(zl)
  );
}
var d$ = 16;
function f$(e) {
  let { width: t, height: n } = e.getSize();
  return !t || !n || (t <= d$ && n <= d$)
    ? e
    : e.resize({ width: d$, height: d$, quality: `best` });
}
function p$(e, t) {
  if (e)
    try {
      if (e.startsWith(`data:`)) return f$(a.nativeImage.createFromDataURL(e));
      if (e.startsWith(`file://`)) return f$(a.nativeImage.createFromPath((0, x.fileURLToPath)(e)));
      if (e.startsWith(`/`)) return f$(a.nativeImage.createFromPath(e));
      for (let n of t) {
        let t = new URL(e, `file://${n.endsWith(`/`) ? n : `${n}/`}`).pathname;
        if ((0, l.existsSync)(t)) {
          let e = a.nativeImage.createFromPath(t);
          if (!e.isEmpty()) return f$(e);
        }
      }
    } catch {}
}
function m$(e, t) {
  return (
    a.ipcMain.handle(Rl, async (n, r) => {
      if (!t(n)) return { id: null };
      let i = a.BrowserWindow.fromWebContents(n.sender);
      return new Promise((t) => {
        let n = !1,
          o = (e) => {
            n || ((n = !0), t({ id: e }));
          },
          s = (t) =>
            t.map((t) =>
              t.type === `separator`
                ? { type: `separator` }
                : {
                    id: t.id,
                    label: t.label,
                    role: t.role,
                    enabled: t.enabled ?? !0,
                    icon: p$(t.icon, e),
                    toolTip: t.toolTip,
                    click: () => o(t.id),
                    submenu: t.submenu ? s(t.submenu) : void 0,
                  },
            );
        a.Menu.buildFromTemplate(s(r)).popup({ window: i ?? void 0, callback: () => o(null) });
      });
    }),
    () => a.ipcMain.removeHandler(Rl)
  );
}
function h$(e, t, n) {
  return new rg(new g$(e), t, n).getRemoteMain();
}
var g$ = class {
    constructor(e) {
      ((this.#e = e),
        e.start(),
        e.on(`message`, (e) => {
          this.#i ||
            (e.data == null
              ? this.#a(Error(`Peer closed MessagePort connection.`))
              : typeof e.data == `string`
                ? this.#t
                  ? (this.#t(e.data), (this.#t = void 0), (this.#n = void 0))
                  : this.#r.push(e.data)
                : this.#a(TypeError(`Received non-string message from MessagePort.`)));
        }),
        e.on(`close`, () => {
          this.#a(Error(`MessagePort message error.`));
        }));
    }
    #e;
    #t;
    #n;
    #r = [];
    #i;
    async send(e) {
      if (this.#i) throw this.#i;
      this.#e.postMessage(e);
    }
    async receive() {
      if (this.#r.length > 0) {
        let e = this.#r.shift();
        if (e != null) return e;
      }
      if (this.#i) throw this.#i;
      return new Promise((e, t) => {
        ((this.#t = e), (this.#n = t));
      });
    }
    abort(e) {
      try {
        this.#e.postMessage(null);
      } catch {}
      (this.#e.close(), (this.#i ||= e));
    }
    #a(e) {
      this.#i || ((this.#i = e), (this.#n &&= (this.#n(e), (this.#t = void 0), void 0)));
    }
  },
  _$ = 3,
  v$ = 35;
function y$(e) {
  return e.type === `tray-menu-threads-changed`;
}
var b$ = class {
  nativeTrayClickSuppressionReason = null;
  clearNativeTrayClickSuppressionTimeout = null;
  chronicleTrayIconRefreshInterval = null;
  chronicleTrayIconState = `default`;
  isNativeTrayMenuOpen = !1;
  trayMenuThreads = {
    runningThreads: [],
    unreadThreads: [],
    pinnedThreads: [],
    recentThreads: [],
    usageLimits: [],
  };
  constructor(e, t, n, r, i, a, o, s, c, l) {
    if (
      ((this.tray = e),
      (this.onTrayButtonClick = t),
      (this.onTrayMenuOpenMainWindow = n),
      (this.onTrayMenuOpenNewThread = r),
      (this.onTrayMenuOpenRecentThread = i),
      (this.getChronicleSidecarControlState = a),
      (this.toggleChronicleSidecar = o),
      (this.trayIcons = s),
      (this.nativeIntl = c),
      (this.appName = l),
      process.platform === `darwin`)
    ) {
      (this.tray.on(`mouse-down`, () => {
        if ((this.clearPendingNativeTrayClickSuppression(), this.isNativeTrayMenuOpen)) {
          this.nativeTrayClickSuppressionReason = `close`;
          return;
        }
        ((this.nativeTrayClickSuppressionReason = `open`), this.openNativeTrayMenu());
      }),
        this.tray.on(`click`, () => {
          this.consumeIgnoredNativeTrayClick() || this.openNativeTrayMenu();
        }),
        this.tray.on(`right-click`, () => {
          this.consumeIgnoredNativeTrayClick() || this.openNativeTrayMenu();
        }),
        this.getChronicleSidecarControlState != null &&
          this.trayIcons.chronicleRunningIcon != null &&
          (this.updateChronicleTrayIcon(),
          (this.chronicleTrayIconRefreshInterval = setInterval(() => {
            this.updateChronicleTrayIcon();
          }, 1e3)),
          this.chronicleTrayIconRefreshInterval.unref?.()));
      return;
    }
    (this.tray.on(`click`, () => {
      this.onTrayButtonClick();
    }),
      this.tray.on(`right-click`, () => {
        this.openNativeTrayMenu();
      }));
  }
  destroy() {
    (this.chronicleTrayIconRefreshInterval != null &&
      (clearInterval(this.chronicleTrayIconRefreshInterval),
      (this.chronicleTrayIconRefreshInterval = null)),
      this.tray.destroy());
  }
  async handleMessage(e) {
    switch (e.type) {
      case `tray-menu-threads-changed`:
        this.trayMenuThreads = e.trayMenuThreads;
        return;
    }
  }
  consumeIgnoredNativeTrayClick() {
    return this.nativeTrayClickSuppressionReason == null
      ? !1
      : (this.clearNativeTrayClickSuppression(), !0);
  }
  openNativeTrayMenu() {
    this.updateChronicleTrayIcon();
    let e = a.Menu.buildFromTemplate(this.getNativeTrayMenuItems());
    (e.once(`menu-will-show`, () => {
      this.isNativeTrayMenuOpen = !0;
    }),
      e.once(`menu-will-close`, () => {
        ((this.isNativeTrayMenuOpen = !1), this.handleNativeTrayMenuClosed());
      }),
      this.tray.popUpContextMenu(e));
  }
  handleNativeTrayMenuClosed() {
    if (this.nativeTrayClickSuppressionReason !== `close`) {
      this.clearNativeTrayClickSuppression();
      return;
    }
    (this.clearPendingNativeTrayClickSuppression(),
      (this.clearNativeTrayClickSuppressionTimeout = setTimeout(() => {
        this.nativeTrayClickSuppressionReason === `close` && this.clearNativeTrayClickSuppression();
      }, 0)),
      this.clearNativeTrayClickSuppressionTimeout.unref?.());
  }
  clearPendingNativeTrayClickSuppression() {
    this.clearNativeTrayClickSuppressionTimeout != null &&
      (clearTimeout(this.clearNativeTrayClickSuppressionTimeout),
      (this.clearNativeTrayClickSuppressionTimeout = null));
  }
  clearNativeTrayClickSuppression() {
    (this.clearPendingNativeTrayClickSuppression(), (this.nativeTrayClickSuppressionReason = null));
  }
  getNativeTrayMenuItems() {
    let {
        pinnedThreads: e,
        recentThreads: t,
        runningThreads: n,
        unreadThreads: r,
        usageLimits: i,
      } = this.trayMenuThreads,
      o = this.nativeIntl.formatMessage({ messageId: Ou, defaultMessage: ku }),
      s = this.nativeIntl.formatMessage({ messageId: Eu, defaultMessage: Du }),
      c = C$({
        label: this.nativeIntl.formatMessage({ messageId: vu, defaultMessage: yu }),
        moreLabel: s,
        threads: n,
        projectlessLabel: o,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      l = C$({
        label: this.nativeIntl.formatMessage({ messageId: Su, defaultMessage: Cu }),
        moreLabel: s,
        threads: r,
        projectlessLabel: o,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      u = C$({
        label: this.nativeIntl.formatMessage({ messageId: gu, defaultMessage: _u }),
        moreLabel: s,
        threads: e,
        projectlessLabel: o,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      d = C$({
        label: this.nativeIntl.formatMessage({ messageId: bu, defaultMessage: xu }),
        moreLabel: s,
        threads: t,
        projectlessLabel: o,
        onOpenThread: this.onTrayMenuOpenRecentThread,
      }),
      f =
        process.platform !== `darwin` || i.length === 0
          ? []
          : [
              {
                label: this.nativeIntl.formatMessage({ messageId: wu, defaultMessage: Tu }),
                enabled: !1,
              },
              ...i.map(({ label: e }) => ({ label: e, enabled: !1 })),
            ],
      p = this.getChronicleSidecarControlState?.(),
      m =
        p?.enabled === !0
          ? {
              label: x$(p),
              enabled: S$(p),
              click: async (e) => {
                if (this.toggleChronicleSidecar == null) return;
                e.enabled = !1;
                let t = null;
                try {
                  ((t = await this.toggleChronicleSidecar()),
                    this.updateChronicleTrayIcon(t),
                    (e.label = x$(t)));
                } finally {
                  e.enabled = t == null ? !0 : S$(t);
                }
              },
            }
          : null,
      h = [c, l, u, d, f]
        .filter((e) => e.length > 0)
        .flatMap((e, t) => (t === 0 ? e : [{ type: `separator` }, ...e]));
    return [
      ...h,
      ...(h.length > 0 ? [{ type: `separator` }] : []),
      {
        label: this.nativeIntl.formatMessage({ messageId: mu, defaultMessage: hu }),
        click: () => {
          this.onTrayMenuOpenNewThread();
        },
      },
      ...(m == null ? [] : [m]),
      { type: `separator` },
      {
        label: this.nativeIntl.formatMessage({
          messageId: fu,
          defaultMessage: pu,
          values: { appName: this.appName },
        }),
        click: () => {
          this.onTrayMenuOpenMainWindow();
        },
      },
      { type: `separator` },
      {
        label: T$(this.appName),
        click: () => {
          a.app.quit();
        },
      },
    ];
  }
  updateChronicleTrayIcon(e = this.getChronicleSidecarControlState?.()) {
    if (this.trayIcons.chronicleRunningIcon == null) return;
    let t = e?.running === !0 ? `chronicle-running` : `default`;
    t !== this.chronicleTrayIconState &&
      ((this.chronicleTrayIconState = t),
      this.tray.setImage(
        t === `chronicle-running`
          ? this.trayIcons.chronicleRunningIcon
          : this.trayIcons.defaultIcon,
      ));
  }
};
function x$(e) {
  switch (e.state) {
    case `disabled`:
    case `stopped`:
      return `Resume Chronicle`;
    case `running`:
    case `restarting`:
      return `Pause Chronicle`;
    case `starting`:
      return `Starting Chronicle...`;
    case `stopping`:
      return `Stopping Chronicle...`;
  }
}
function S$(e) {
  return e.state !== `starting` && e.state !== `stopping`;
}
function C$({ label: e, moreLabel: t, threads: n, projectlessLabel: r, onOpenThread: i }) {
  if (n.length === 0) return [];
  let a = n.slice(0, _$),
    o = n.slice(_$);
  return [
    { label: e, enabled: !1 },
    ...a.map((e) => w$({ thread: e, projectlessLabel: r, onOpenThread: i })),
    ...(o.length > 0
      ? [
          {
            label: t,
            submenu: o.map((e) => w$({ thread: e, projectlessLabel: r, onOpenThread: i })),
          },
        ]
      : []),
  ];
}
function w$({ thread: e, projectlessLabel: t, onOpenThread: n }) {
  let r = Array.from(e.title);
  return {
    label:
      r.length <= v$
        ? e.title
        : `${r
            .slice(0, v$ - 1)
            .join(``)
            .trimEnd()}…`,
    sublabel: e.isProjectless ? t : e.projectLabel,
    click: () => {
      n(e.path);
    },
  };
}
function T$(e) {
  let t = a.Menu.buildFromTemplate([{ role: `quit` }]);
  return (Array.isArray(t) ? t : t.items)[0]?.label ?? `Quit ${e}`;
}
var E$ = 6e4,
  D$ = new Map(),
  O$ = t.Js(`mode`, [
    t.$s({ mode: t.Xs(`create`) }),
    t.$s({ mode: t.Xs(`edit`), commentId: t.nc() }),
    t.$s({ mode: t.Xs(`design`), groupId: t.nc() }),
  ]),
  k$ = t.$s({ x: t.Qs(), y: t.Qs(), width: t.Qs(), height: t.Qs() }),
  A$ = t.$s({ x: t.Qs(), y: t.Qs() }),
  j$ = t.$s({
    provider: t.Xs(`google-docs`),
    kind: t.Xs(`google-docs`),
    documentId: t.nc(),
    tabId: t.nc().optional(),
    documentTitle: t.nc().optional(),
    selectedText: t.nc().optional(),
    visibleText: t.nc().optional(),
  }),
  M$ = t.$s({
    pageUrl: t.nc(),
    frameUrl: t.nc().nullable(),
    title: t.nc(),
    elementPath: t.nc(),
    point: t.$s({ xPercent: t.Qs(), y: t.Qs() }),
    rect: k$,
    isFixed: t.Ks(),
    framePath: t.Gs(t.nc()),
    documentContext: j$.optional(),
    scrollContainers: t
      .Gs(t.$s({ selector: t.nc(), scrollLeft: t.Qs(), scrollTop: t.Qs() }))
      .optional(),
  }),
  N$ = t.Js(`kind`, [
    M$.extend({
      kind: t.Xs(`element`),
      role: t.nc().nullable(),
      name: t.nc().nullable(),
      selector: t.nc().nullable(),
      nearbyText: t.nc().nullable(),
    }),
    M$.extend({
      kind: t.Xs(`region`),
      role: t.Ws(),
      name: t.Ws(),
      selector: t.Ws(),
      nearbyText: t.Ws(),
    }),
  ]),
  P$ = t.$s({
    anchor: N$,
    cardViewportRect: k$.optional(),
    viewportRect: k$.nullable(),
    viewportPoint: A$,
    viewportSize: t.$s({ width: t.Qs(), height: t.Qs() }).optional(),
  }),
  F$ = t.$s({
    property: t.nc(),
    value: t.nc(),
    previousValue: t.nc(),
    placeholderValue: t.nc().optional(),
  }),
  I$ = t.$s({ value: t.nc(), previousValue: t.nc() }),
  L$ = t.$s({
    id: t.nc(),
    anchor: N$,
    declarations: t.Gs(F$),
    draftAttribute: t.nc(),
    markerViewportPoint: A$.optional(),
    provenance: t.Gs(
      t.$s({
        property: t.nc(),
        selectorText: t.nc().optional(),
        sourceUrl: t.nc().optional(),
        sourceLine: t.Qs().optional(),
        sourceColumn: t.Qs().optional(),
      }),
    ),
    selector: t.nc().nullable(),
    targetLabel: t.nc(),
    text: I$.optional(),
    viewportSize: t.$s({ width: t.Qs(), height: t.Qs() }).optional(),
  }),
  R$ = L$.extend({
    createdAt: t.nc(),
    updatedAt: t.nc(),
    status: t.rc([t.Xs(`current`), t.Xs(`queued`)]),
  }),
  z$ = t.Js(`type`, [
    t.$s({ type: t.Xs(`browser-sidebar-runtime-close-comment-preview`), commentId: t.nc() }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-comment-screenshot-ready`),
      commentId: t.nc(),
      annotationViewportRect: k$.nullable().optional(),
      markerViewportPoint: A$.optional(),
      skipScreenshotCapture: t.Ks().optional(),
    }),
    t.$s({ type: t.Xs(`browser-sidebar-runtime-exit-comment-mode`) }),
    t.$s({ type: t.Xs(`browser-sidebar-runtime-design-modifier-state`), pressed: t.Ks() }),
    t.$s({ type: t.Xs(`browser-sidebar-runtime-focus-editor`) }),
    t.$s({ type: t.Xs(`browser-sidebar-runtime-image-drag-ended`) }),
    t.$s({ type: t.Xs(`browser-sidebar-runtime-image-drag-started`), sourceUrl: t.nc() }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-mouse-navigation`),
      direction: t.rc([t.Xs(`back`), t.Xs(`forward`)]),
    }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-open-comment-preview`),
      commentId: t.nc(),
      anchorState: P$,
    }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-open-editor`),
      target: O$,
      anchorState: P$,
      designEditorState: L$.optional(),
      wantsScreenshot: t.Ks().optional(),
    }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-open-design-editor`),
      anchorState: P$,
      designEditorState: L$,
      designChange: R$.optional(),
    }),
    t.$s({
      type: t.Xs(`browser-sidebar-runtime-update-anchor`),
      target: O$,
      anchorState: P$.nullable(),
    }),
  ]);
function B$() {
  let e = !1,
    t = null,
    n = !1;
  return {
    allowQuitTemporarilyForUpdateInstall: (r) => {
      ((e = r?.allowWithoutPrompt ?? !0),
        (n = r?.skipDrainBeforeQuit ?? !1),
        t && clearTimeout(t),
        (t = setTimeout(() => {
          ((e = !1), (n = !1), (t = null));
        }, 6e4)),
        t.unref());
    },
    canQuitWithoutPrompt: () => e,
    markQuitApproved: () => {
      e = !0;
    },
    shouldSkipDrainBeforeQuit: () => n,
  };
}
function V$({
  hotkeyWindowLifecycleManager: e,
  globalDictationLifecycleManager: t,
  flushAndDisposeContext: n,
  disposables: r,
}) {
  (e.dispose(), t.dispose(), n(), r.dispose());
}
function H$({
  desktopSentry: e,
  hotkeyWindowLifecycleManager: n,
  globalDictationLifecycleManager: r,
  codexHome: i,
  nativeContextMenuIconSearchRoots: o,
  getContextForWebContents: s,
  getPrimaryWindow: c,
  showPrimaryWindow: l,
  sendMessageToWindow: u,
  ensureWindow: d,
  createFreshWindow: f,
  setDesktopFeatureAvailability: p,
  setAutomaticBackgroundDownloadsEnabled: m,
  setRealtimeSessionRecordingEnabled: h = () => {},
  restoreAvatarOverlayOpenState: g,
  getAvatarOverlayCompositionSurfaceHost: _,
  reloadBundledPlugins: v,
  setMacMenuBarEnabled: y,
  navigateToRoute: b,
  getDockMenuController: x,
  getTrayController: S,
  isTrustedIpcEvent: C,
}) {
  (m$(o, C), u$(C));
  let w = !1;
  (a.ipcMain.handle(Pl, async (e, i) => {
    if (!C(e)) return;
    if (i.type === `electron-avatar-overlay-restore-ready`) {
      w || ((w = !0), await g(e.sender));
      return;
    }
    if (i.type === `electron-sparkle-autodownload-changed`) {
      m(!i.disableSparkleAutodownload);
      return;
    }
    if (i.type === `electron-desktop-features-changed`) {
      (s(e.sender)?.setChronicleConfig(i.codexChronicleConfig),
        h(i.realtimeSessionRecording),
        p({
          ambientSuggestions: i.ambientSuggestions,
          appshotsEnabled: i.appshotsEnabled,
          inAppBrowserUse: i.inAppBrowserUse,
          inAppBrowserUseAllowed: i.inAppBrowserUseAllowed,
          linksDefaultInAppBrowser: i.linksDefaultInAppBrowser,
          browserPane: i.browserPane,
          externalBrowserUse: i.externalBrowserUse,
          externalBrowserUseAllowed: i.externalBrowserUseAllowed,
          computerUse: i.computerUse,
          computerUseNodeRepl: i.computerUseNodeRepl,
          sites: i.sites,
          control: i.control,
          dil: i.dil,
          multiBrowserTabs: i.multiBrowserTabs,
          multiWindow: i.multiWindow,
          processManager: i.processManager,
          remoteControlClientScopedEnvironments: i.remoteControlClientScopedEnvironments,
          visualize: i.visualize,
        }));
      return;
    }
    if (i.type === `mac-menu-bar-enabled-changed`) {
      await y(i.enabled);
      return;
    }
    if (i.type === `reload-bundled-plugins`) {
      await v?.();
      return;
    }
    if (i.type === `open-browser-in-main-window`) {
      if (!Re().browserPane || !t.Ki(i.path)) return;
      n.hide();
      let r =
          s(e.sender)
            ?.getBrowserSidebarManager()
            .getPreferredWindowStateForConversation(i.browserPanel.conversationId)?.window ?? c(),
        a = r != null;
      if (((r ??= await f(i.path)), r == null)) return;
      (r.isMinimized() && r.restore(),
        r.show(),
        r.focus(),
        u(r, i.browserPanel),
        a && b(r, i.path));
      return;
    }
    if (i.type === `open-current-main-window` || i.type === `open-in-main-window`) {
      let e = i.type === `open-current-main-window` ? null : i.path;
      if (e != null && !t.Ki(e)) return;
      n.hide();
      let r = c(),
        a = r != null || e == null ? await d() : await f(e);
      a &&
        (i.type === `open-current-main-window` && i.stealFocus === !0
          ? l({ stealFocus: !0 })
          : (a.isMinimized() && a.restore(), a.show(), a.focus()),
        r != null && e != null && b(a, e));
      return;
    }
    if (i.type === `open-in-new-window`) {
      if (!t.Ki(i.path)) return;
      n.hide();
      let e = await f(i.path);
      e && (e.isMinimized() && e.restore(), e.show(), e.focus());
      return;
    }
    if (i.type === `show-settings`) {
      n.hide();
      let e = await d();
      if (!e) return;
      (e.isMinimized() && e.restore(), e.show(), e.focus(), b(e, `/settings/${i.section}`));
      return;
    }
    if (i.type === `quit-app`) {
      a.app.quit();
      return;
    }
    if (y$(i)) {
      await Promise.all([S()?.handleMessage(i), x?.()?.handleMessage(i)]);
      return;
    }
    if (await r.handleMessage(e, i)) return;
    let o = s(e.sender);
    if (!o) {
      t.Ur().warning(`Message received for unknown window context`);
      return;
    }
    await o.handleMessage(e.sender, i);
  }),
    a.ipcMain.handle(Fl, async (e, n) => {
      let r = z$.safeParse(n);
      if (!r.success) {
        t.Ur().warning(`Invalid browser sidebar runtime message received`);
        return;
      }
      let i = a.BrowserWindow.fromWebContents(e.sender);
      if (i == null) {
        t.Ur().warning(`Browser sidebar runtime message received without owner window`);
        return;
      }
      let o = s(i.webContents);
      if (o == null) {
        t.Ur().warning(`Browser sidebar runtime message received for unknown window context`);
        return;
      }
      await o.handleMessage(e.sender, r.data);
    }),
    a.ipcMain.handle(Gl, async (e, n) => (C(e) ? t.At({ codexHome: i, params: n }) : null)),
    a.ipcMain.handle(ql, (t) => {
      if (!C(t)) return;
      let n = Error(`Desktop Sentry test error (debug button)`);
      e.captureException(n, { tags: { trigger: `debug-button` } });
    }),
    a.ipcMain.on(Jl, (e) => {
      if (!C(e)) return;
      let [n] = e.ports,
        r = s(e.sender),
        i = r?.createAppHost(e.sender),
        a = h$(n, i);
      r?.registerAppView(e.sender, a).catch((e) => {
        t.Ur().warning(`Failed to register AppView RPC services`, {
          safe: {},
          sensitive: { error: e },
        });
      });
    }),
    a.ipcMain.on(t.zo, (e) => {
      if (!C(e)) return;
      let [t] = e.ports,
        n = _?.(e.sender);
      t != null && n != null && h$(t, n);
    }));
}
function U$({
  buildFlavor: e,
  getContextForWebContents: t,
  isTrustedIpcEvent: n,
  usesOwlAppShell: i,
}) {
  (a.ipcMain.on(Bl, (e) => {
    if (!n(e)) {
      e.returnValue = null;
      return;
    }
    e.returnValue = r.j;
  }),
    a.ipcMain.on(Vl, (t) => {
      if (!n(t)) {
        t.returnValue = null;
        return;
      }
      t.returnValue = e;
    }),
    a.ipcMain.on(Hl, (e) => {
      if (!n(e)) {
        e.returnValue = !1;
        return;
      }
      e.returnValue = i;
    }),
    a.ipcMain.on(Wl, (e) => {
      if (!n(e)) {
        e.returnValue = {};
        return;
      }
      e.returnValue = t(e.sender)?.getSharedObjectSnapshot() ?? {};
    }));
}
function W$({
  isWindows: e,
  disableQuitConfirmationPrompt: n,
  quitState: r,
  windows: i,
  applicationMenuManager: o,
  ensureWindow: s,
  hotkeyWindowLifecycleManager: c,
  globalDictationLifecycleManager: l,
  globalState: u,
  settingsStore: d,
  flushAndDisposeContext: f,
  disposables: p,
  appEvent: m,
  errorReporter: h,
}) {
  let g = !1,
    _ = !1;
  (a.app.on(`window-all-closed`, () => {
    ((process.platform === `darwin` && !a.app.isPackaged) ||
      (process.platform !== `darwin` && !e)) &&
      a.app.quit();
  }),
    a.app.on(`before-quit`, (o) => {
      if (Dl()) {
        o.preventDefault();
        return;
      }
      let s = qv(),
        c = t.er().some((e) => e.status === `ACTIVE`);
      if (e || r.canQuitWithoutPrompt() || n || (!s && !c)) {
        ((_ = !0), i.markAppQuitting());
        return;
      }
      let l = a.app.getName();
      if (
        a.dialog.showMessageBoxSync({
          type: `warning`,
          buttons: [`Quit`, `Cancel`],
          defaultId: 0,
          cancelId: 1,
          noLink: !0,
          title: `Quit ${l}?`,
          message: `Quit ${l}?`,
          detail: G$({ hasInProgressLocalConversation: s, hasEnabledAutomations: c }),
        }) !== 0
      ) {
        o.preventDefault();
        return;
      }
      (r.markQuitApproved(), (_ = !0), i.markAppQuitting());
    }),
    a.app.on(`child-process-gone`, (e, t) => {
      if (t.reason !== `clean-exit`) {
        if (K$(t)) {
          q$(t);
          return;
        }
        h.reportFatal(Error(`Child process gone (${t.type})`), {
          tags: { errorType: `child-process-gone`, processType: t.type, reason: t.reason },
          extra: { exitCode: t.exitCode, name: t.name, serviceName: t.serviceName },
        });
      }
    }),
    a.app.on(`activate`, () => {
      _ || (i.showLastActivePrimaryWindow() || s(), o.refresh());
    }),
    a.app.on(`browser-window-blur`, () => {
      a.BrowserWindow.getFocusedWindow() ?? m.emit(`background`);
    }),
    a.app.on(`browser-window-focus`, () => {
      (m.emit(`foreground`), o.refresh());
    }),
    a.app.on(`will-quit`, (e) => {
      if (((_ = !0), !g)) {
        if (r.shouldSkipDrainBeforeQuit()) {
          V$({
            hotkeyWindowLifecycleManager: c,
            globalDictationLifecycleManager: l,
            flushAndDisposeContext: f,
            disposables: p,
          });
          return;
        }
        (e.preventDefault(),
          (g = !0),
          c.dispose(),
          l.dispose(),
          Promise.all([u.flush(), d.flush()]).finally(() => {
            (f(), p.dispose(), a.app.quit());
          }));
      }
    }));
}
function G$({ hasInProgressLocalConversation: e, hasEnabledAutomations: t }) {
  return e && t
    ? `Active local threads on this machine will be interrupted and enabled automations won't run while Codex is closed`
    : e
      ? `Active local threads on this machine will be interrupted`
      : `Enabled automations won't run while Codex is closed`;
}
function K$(e) {
  return e.type === `GPU` || (e.type === `Utility` && e.serviceName === `audio.mojom.AudioService`);
}
function q$(e) {
  let n = `${e.type}:${e.serviceName ?? e.name ?? ``}:${e.reason}`,
    r = Date.now(),
    i = D$.get(n);
  (i != null && r - i < E$) ||
    (D$.set(n, r),
    t
      .Ur()
      .warning(`Recoverable Chromium child process gone`, {
        safe: {
          exitCode: e.exitCode,
          name: e.name ?? null,
          processType: e.type,
          reason: e.reason,
          serviceName: e.serviceName ?? null,
        },
      }));
}
var J$ = `codex.aboutDialog.title`,
  Y$ = `About {appName}`,
  X$ = `codex.aboutDialog.ok`,
  Z$ = `codex.aboutDialog.versionLine`,
  Q$ = `Version {version}`,
  $$ = `codex.aboutDialog.versionLineWithDate`,
  e1 = `Version {version} • Released {releaseDate}`,
  t1 = `codex.aboutDialog.buildInfoLabel`,
  n1 = `Build information`,
  r1 = 380,
  i1 = 360,
  a1 = 72,
  o1 = `      --background: #202020;
      --border: #3a3a3a;
      --button-background: #2d2d2d;
      --button-background-hover: #383838;
      --button-border: #6b6b6b;
      --button-text: #f5f5f5;
      --footer-background: #252525;
      --muted-text: #c8c8c8;
      --selection: #005a9e;
      --text: #f5f5f5;`,
  s1 = `      --background: #ffffff;
      --border: #d8d8d8;
      --button-background: #f3f3f3;
      --button-background-hover: #e9e9e9;
      --button-border: #8a8a8a;
      --button-text: #1f1f1f;
      --footer-background: #f5f5f5;
      --muted-text: #4f4f4f;
      --selection: #0078d4;
      --text: #1f1f1f;`,
  c1 = `    .footer {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      min-height: 56px;
      padding: 12px 14px 14px;
      border-top: 1px solid var(--border);
      background: var(--footer-background);
    }

    button {
      min-width: 76px;
      height: 28px;
      padding: 0 18px;
      border: 1px solid var(--button-border);
      border-radius: 2px;
      background: var(--button-background);
      color: var(--button-text);
      font: inherit;
      user-select: none;
      -webkit-user-select: none;
    }

    button:hover {
      background: var(--button-background-hover);
    }

    button:focus-visible {
      outline: 2px solid var(--selection);
      outline-offset: 1px;
    }`,
  l1 = null;
async function u1({ buildFlavor: e, parent: n }) {
  if (l1 != null && !l1.isDestroyed()) {
    (l1.isMinimized() && l1.restore(), l1.show(), l1.focus());
    return;
  }
  let i = a.app.getName(),
    o = a.app.getVersion(),
    s = d1(o),
    c = t.$i(e),
    l = c == null ? o : `${o} • ${c}`,
    u = process.platform === `darwin`,
    d = r.V(),
    f = await g1(),
    p = d.formatMessage({ messageId: J$, defaultMessage: Y$, values: { appName: i } }),
    m = u ? null : d.formatMessage({ messageId: X$, defaultMessage: `OK` }),
    h =
      s == null
        ? d.formatMessage({ messageId: Z$, defaultMessage: Q$, values: { version: l } })
        : d.formatMessage({
            messageId: $$,
            defaultMessage: e1,
            values: { version: l, releaseDate: s },
          }),
    g = d.formatMessage({ messageId: t1, defaultMessage: n1 }),
    _ = f1(o),
    v =
      _.length === 0
        ? h
        : [h, ``, ..._].join(`
`),
    y = n != null && !n.isDestroyed() ? n : null,
    b = a.nativeTheme.shouldUseDarkColors,
    x = new a.BrowserWindow({
      width: r1,
      height: i1,
      useContentSize: !0,
      title: p,
      show: !1,
      resizable: !1,
      minimizable: !1,
      maximizable: !1,
      fullscreenable: !1,
      autoHideMenuBar: !0,
      backgroundColor: b ? `#202020` : `#ffffff`,
      ...(f.windowIcon.isEmpty() ? {} : { icon: f.windowIcon }),
      ...(y == null ? {} : { parent: y, ...(u ? {} : { modal: !0 }) }),
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        sandbox: !0,
        spellcheck: !1,
        devTools: !1,
      },
    });
  ((l1 = x),
    x.setMenuBarVisibility(!1),
    process.platform !== `darwin` && x.removeMenu(),
    m1(x, y),
    x.once(`closed`, () => {
      l1 === x && (l1 = null);
    }),
    x.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    x.webContents.on(`will-navigate`, (e) => {
      e.preventDefault();
    }));
  try {
    if (
      (await x.loadURL(
        `data:text/html;charset=utf-8,${encodeURIComponent(_1({ appDisplayName: i, buildInfoLabel: g, buildInfoText: v, iconDataUrl: f.htmlIconDataUrl, isDark: b, okLabel: m, title: p }))}`,
      ),
      x.isDestroyed())
    )
      return;
    (await p1(x), m1(x, y), x.show(), x.focus());
  } catch {
    (l1 === x && (l1 = null), x.isDestroyed() || x.destroy());
  }
}
function d1(e) {
  let n = t.gs(e);
  return n == null ? null : new Intl.DateTimeFormat(void 0, { dateStyle: `medium` }).format(n);
}
function f1(e) {
  return [];
}
async function p1(e) {
  let t = await e.webContents.executeJavaScript(
      `Math.ceil(document.querySelector(".dialog")?.getBoundingClientRect().height ?? document.documentElement.scrollHeight)`,
      !0,
    ),
    n = typeof t == `number` && Number.isFinite(t) ? t : i1;
  e.setContentSize(r1, n, !1);
}
function m1(e, t) {
  if (t == null || t.isDestroyed()) {
    e.center();
    return;
  }
  let n = t.getBounds(),
    r = e.getBounds(),
    i = a.screen.getDisplayMatching(n).workArea,
    o = h1(Math.round(n.x + (n.width - r.width) / 2), i.x, i.x + Math.max(0, i.width - r.width)),
    s = h1(
      Math.round(n.y + (n.height - r.height) / 2),
      i.y,
      i.y + Math.max(0, i.height - r.height),
    );
  e.setPosition(o, s, !1);
}
function h1(e, t, n) {
  return Math.min(Math.max(e, t), n);
}
async function g1() {
  let e = process.platform === `darwin`,
    t = e ? r.E() : process.execPath,
    [n, i] = await Promise.all([
      e ? OE(t) : null,
      a.app.getFileIcon(t, { size: process.platform === `win32` ? `large` : `normal` }),
    ]);
  return {
    htmlIconDataUrl:
      n ?? (i.isEmpty() ? null : i.resize({ width: a1, height: a1, quality: `best` }).toDataURL()),
    windowIcon: i,
  };
}
function _1({
  appDisplayName: e,
  buildInfoLabel: t,
  buildInfoText: n,
  iconDataUrl: r,
  isDark: i,
  okLabel: a,
  title: o,
}) {
  let s = r == null ? `` : `<img class="app-icon" src="${(0, cB.default)(r)}" alt="">`,
    c =
      a == null
        ? ``
        : `    <footer class="footer">
      <button id="ok" type="button" autofocus>${(0, cB.default)(a)}</button>
    </footer>`,
    l =
      a == null
        ? ``
        : `
    document.getElementById("ok").addEventListener("click", closeDialog);`;
  return `<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; style-src 'unsafe-inline'; script-src 'unsafe-inline'">
  <title>${(0, cB.default)(o)}</title>
  <style>
    :root {
      color-scheme: ${i ? `dark` : `light`};
${i ? o1 : s1}
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      width: 100vw;
      overflow: hidden;
      background: var(--background);
      color: var(--text);
      font-family: "Segoe UI", system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      font-size: 12px;
    }

    ::selection {
      background: var(--selection);
      color: #ffffff;
    }

    .dialog {
      display: flex;
      flex-direction: column;
    }

    .content {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: ${a == null ? `32px` : `32px 32px 22px`};
      text-align: center;
    }

    .app-icon {
      width: ${a1}px;
      height: ${a1}px;
      object-fit: contain;
      margin-bottom: 14px;
      user-select: none;
      -webkit-user-drag: none;
    }

    .app-name {
      margin-bottom: 18px;
      font-size: 17px;
      line-height: 1.25;
      font-weight: 600;
    }

    .build-info {
      width: 100%;
      margin: 0;
      line-height: 1.45;
      color: var(--muted-text);
      white-space: pre-wrap;
      overflow-wrap: anywhere;
      border: 0;
      background: transparent;
      font: inherit;
    }

    .app-name,
    .build-info,
    .copyright {
      cursor: text;
      user-select: text;
      -webkit-user-select: text;
    }

    .copyright {
      margin-top: 14px;
      color: var(--muted-text);
    }

${a == null ? `` : c1}
  </style>
</head>
<body>
  <main class="dialog">
    <section class="content" aria-labelledby="app-name">
      ${s}
      <div class="app-name" id="app-name">${(0, cB.default)(e)}</div>
      <pre class="build-info" aria-label="${(0, cB.default)(t)}">${(0, cB.default)(n)}</pre>
      <div class="copyright">© OpenAI</div>
    </section>
${c}
  </main>
  <script>
    const closeDialog = () => window.close();
${l}
    window.addEventListener("keydown", (event) => {
      if (event.key === "Escape") {
        event.preventDefault();
        closeDialog();
      }
    });
  <\/script>
</body>
</html>`;
}
var v1 = t.Gr(`sparkle`),
  y1 = `/`,
  b1 = `/settings/general-settings`;
function x1({
  buildFlavor: e,
  isMacOS: n,
  allowDevtools: i,
  allowDebugMenu: o,
  allowWindowReload: s,
  enableSparkle: c,
  desktopFeatureAvailability: l,
  sparkleManager: u,
  ensurePrimaryWindowVisible: d,
  createFreshWindow: f,
  queueCodexDeepLinkUrl: p,
  navigateToRoute: m,
  collapseHotkeyWindowToHome: h,
  getElectronMessageHandlerForWindow: g,
  windowManager: _,
  traceRecordingUploadOptions: v,
}) {
  let y = async () => {
      let e = a.BrowserWindow.getFocusedWindow();
      return e && !e.isDestroyed() ? e : d();
    },
    b = (e, i) => {
      let a = t.Zt({ commandId: e });
      if (a == null) throw Error(`Electron menu command '${e}' is not defined`);
      return {
        label: r.V().formatMessage({ messageId: a.menuTitleIntlId, defaultMessage: a.menuTitle }),
        accelerator: t.Ut({ commandId: e, isMacOS: n }).filter((e) => !e.includes(` `) && !t.Qt(e))[
          i ?? 0
        ],
      };
    },
    x = {
      ...b(`settings`),
      click: async () => {
        let e = await d();
        e && m(e, b1);
      },
    },
    S = {
      ...b(`newThread`),
      click: async () => {
        let e = await y();
        if (e) {
          if (_.isHotkeyWindowThread(e)) {
            await h?.();
            return;
          }
          _.sendMessageToWindow(e, { type: `run-command`, id: `newThread` });
        }
      },
    },
    C = { ...b(`newThread`, 1), acceleratorWorksWhenHidden: !0, visible: !1, click: S.click },
    w = {
      ...b(`quickChat`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `new-quick-chat` });
      },
    },
    T = {
      ...b(`newWindow`),
      click: async () => {
        let e = await f(y1);
        e && (e.isMinimized() && e.restore(), e.show(), e.focus());
      },
    },
    E = b(`closeWindow`),
    D = (e) => {
      if (e != null && t.Ut({ commandId: `closeTab`, isMacOS: n }).includes(e)) {
        let e = a.webContents.getFocusedWebContents(),
          t = a.BrowserWindow.getFocusedWindow();
        if (t != null && !t.isDestroyed()) {
          let e = ke(t);
          if (e != null) {
            _.sendMessageToWindow(t, e);
            return;
          }
        }
        if (e != null) {
          let n = t != null && !t.isDestroyed() ? [t] : a.BrowserWindow.getAllWindows();
          for (let t of n)
            if (!t.isDestroyed() && Oe(t)?.closeFocusedVisibleBrowserTab(t, e)) return;
        }
        if (t != null && !t.isDestroyed()) {
          let e = nF(_.getAppShellShortcutState(t.webContents.id));
          if (e != null) {
            _.sendMessageToWindow(t, e);
            return;
          }
        }
      }
      let r = a.BrowserWindow.getFocusedWindow();
      r == null || r.isDestroyed() || r.close();
    },
    O = {
      ...E,
      click: () => {
        D(E.accelerator);
      },
    },
    k = b(`closeWindow`, 1),
    A = {
      ...k,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: () => {
        D(k.accelerator);
      },
    },
    j = {
      ...b(`openFolder`),
      click: async () => {
        let e = await y();
        if (!e) return;
        let t = g(e);
        t && (await t.addWorkspaceRootOption(e.webContents));
      },
    },
    M = {
      label: `Log Out`,
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `log-out` });
      },
    },
    N = async (e) => {
      let t = await y();
      t &&
        _.sendMessageToWindow(t, { type: `command-menu`, ...(e === void 0 ? {} : { query: e }) });
    },
    P = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `file-search-command-menu` });
    },
    F = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `chat-search-command-menu` });
    },
    I = async (e, { allowAppShellTabShortcut: t = !1 } = {}) => {
      let n = await y();
      if (!n) return;
      let r = t && Me(e) ? Ae(n) : null;
      _.sendMessageToWindow(n, {
        type: `run-command`,
        id: e,
        ...(t ? { allowAppShellTabShortcut: t } : {}),
        ...(r == null ? {} : { originPanel: r }),
      });
    },
    ee = async () => {
      await I(`feedback`);
    },
    te = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `copy-conversation-path` });
    },
    ne = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `toggle-thread-pin` });
    },
    re = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `rename-thread` });
    },
    ie = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `archive-thread`, source: `native_accelerator` });
    },
    ae = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `copy-working-directory` });
    },
    oe = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `copy-session-id` });
    },
    se = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `copy-deeplink` });
    },
    ce = {
      ...b(`openCommandMenu`, 1),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await N(``);
      },
    },
    le = {
      ...b(`openCommandMenu`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await N();
      },
    },
    ue = { ...b(`searchFiles`), acceleratorWorksWhenHidden: !0, visible: !1, click: P },
    de = { ...b(`searchChats`), acceleratorWorksWhenHidden: !0, visible: !1, click: F },
    fe = {
      ...b(`openProcessManager`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await I(`openProcessManager`);
      },
    },
    pe = t.Jt.map((e) => ({
      ...b(e),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        await I(e);
      },
    })),
    me = { ...b(`copyConversationPath`), acceleratorWorksWhenHidden: !0, visible: !1, click: te },
    he = { ...b(`toggleThreadPin`), acceleratorWorksWhenHidden: !0, visible: !1, click: ne },
    ge = { ...b(`renameThread`), acceleratorWorksWhenHidden: !0, visible: !1, click: re },
    _e = { ...b(`archiveThread`), acceleratorWorksWhenHidden: !0, visible: !1, click: ie },
    ve = { ...b(`copyWorkingDirectory`), acceleratorWorksWhenHidden: !0, visible: !1, click: ae },
    ye = { ...b(`copySessionId`), acceleratorWorksWhenHidden: !0, visible: !1, click: oe },
    be = { ...b(`copyDeeplink`), acceleratorWorksWhenHidden: !0, visible: !1, click: se },
    xe = {
      label: r
        .V()
        .formatMessage({ messageId: J$, defaultMessage: Y$, values: { appName: a.app.getName() } }),
      click: (t, n) => {
        u1({ buildFlavor: e, parent: n instanceof a.BrowserWindow && !n.isDestroyed() ? n : null });
      },
    },
    Se = {
      ...b(`toggleSidebar`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-sidebar` });
      },
    },
    Ce = {
      ...b(`toggleBottomPanel`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-bottom-panel` });
      },
    },
    we = {
      ...b(`toggleTerminal`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-terminal` });
      },
    },
    Te = {
      ...b(`toggleFileTreePanel`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-file-tree-panel` });
      },
    },
    Ee = {
      ...b(`toggleBrowserPanel`),
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async () => {
        let e = await y();
        e &&
          _.sendMessageToWindow(e, {
            type: `toggle-browser-panel`,
            source: `manual`,
            initiator: `app_menu`,
          });
      },
    },
    De = {
      ...b(`openBrowserTab`),
      click: async () => {
        let e = await y();
        e &&
          _.sendMessageToWindow(e, {
            type: `open-browser-tab`,
            source: `manual`,
            initiator: `app_menu`,
          });
      },
    },
    Oe = (e) => g(e)?.getBrowserSidebarManager?.(),
    ke = (e) => {
      let t = _.getAppShellShortcutState(e.webContents.id);
      return t?.focusArea === `main` ? null : nF(t);
    },
    Ae = (e) => {
      let t = _.getAppShellShortcutState(e.webContents.id);
      if (t?.focusArea === `bottom-panel` && t.bottomPanelBrowserTabId == null) return null;
      let n =
        Oe(e)?.getFocusedVisibleBrowserTabPanel(e, a.webContents.getFocusedWebContents()) ?? null;
      if (n != null) return n;
      switch (t?.focusArea) {
        case `bottom-panel`:
          return `bottom`;
        case `right-panel`:
          return `right`;
        case `main`:
        case void 0:
          break;
      }
      return rF(t);
    },
    je = (e) =>
      e === `nextRecentThread` || e === `nextThread`
        ? `nextTab`
        : e === `previousRecentThread` || e === `previousThread`
          ? `previousTab`
          : null,
    Me = (e) => je(e) != null,
    Ne = (e, r) => {
      let i = je(e);
      return i != null && t.Ut({ commandId: i, isMacOS: n }).includes(r);
    },
    Pe = (e, t, n) => n?.triggeredByAccelerator === !0 && t != null && Ne(e, t),
    Fe = a.BrowserWindow.getFocusedWindow(),
    Ie = a.webContents.getFocusedWebContents(),
    Le = Fe != null && !Fe.isDestroyed() && !!Oe(Fe)?.canReloadActiveVisiblePage(Fe, Ie),
    Re = async (e = !1) => {
      let t = await y();
      if (!t) return;
      let n = Oe(t);
      if (n == null) return;
      let r = a.webContents.getFocusedWebContents();
      if (e) {
        n.reloadActiveVisiblePageWithOptions(t, { ignoreCache: !0 }, r);
        return;
      }
      n.reloadActiveVisiblePage(t, r);
    },
    ze = {
      ...b(`reloadBrowserPage`),
      enabled: Le,
      click: async () => {
        await Re();
      },
    },
    Be = {
      ...b(`hardReloadBrowserPage`),
      acceleratorWorksWhenHidden: !0,
      enabled: Le,
      visible: !1,
      click: async () => {
        await Re(!0);
      },
    },
    Ve = {
      label: `Reload Window`,
      accelerator: `Command+Control+R`,
      click: async () => {
        let e = await y();
        e && e.reload();
      },
    },
    He = {
      ...b(`toggleSidePanel`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-diff-panel` });
      },
    },
    Ue = vY({
      allowDevtools: i,
      accelerator: b(`toggleTraceRecording`).accelerator,
      windowManager: _,
      traceRecordingUploadOptions: v,
    }),
    We = {
      ...b(`findInThread`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `find-in-thread` });
      },
    },
    Ge = b(`previousThread`),
    Ke = {
      ...Ge,
      click: async (e, t, n) => {
        await I(`previousThread`, {
          allowAppShellTabShortcut: Pe(`previousThread`, Ge.accelerator, n),
        });
      },
    },
    qe = b(`previousThread`, 1),
    Je = {
      ...qe,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async (e, t, n) => {
        await I(`previousThread`, {
          allowAppShellTabShortcut: Pe(`previousThread`, qe.accelerator, n),
        });
      },
    },
    Ye = b(`nextThread`),
    Xe = {
      ...Ye,
      click: async (e, t, n) => {
        await I(`nextThread`, { allowAppShellTabShortcut: Pe(`nextThread`, Ye.accelerator, n) });
      },
    },
    Ze = b(`nextThread`, 1),
    Qe = {
      ...Ze,
      acceleratorWorksWhenHidden: !0,
      visible: !1,
      click: async (e, t, n) => {
        await I(`nextThread`, { allowAppShellTabShortcut: Pe(`nextThread`, Ze.accelerator, n) });
      },
    },
    $e = {
      label: `Toggle Debug Menu`,
      accelerator: `Alt+D`,
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-debug-modal` });
      },
    },
    et = {
      label: `Open Deeplink from Clipboard`,
      click: () => {
        p(a.clipboard.readText().trim()) ||
          a.dialog.showMessageBox({
            type: `info`,
            title: `Invalid Deeplink`,
            message: `Clipboard does not contain a valid codex:// deeplink.`,
            detail: `Copy a codex:// URL to the clipboard and try again.`,
          });
      },
    },
    tt = {
      label: `Toggle Query Devtools`,
      accelerator: `CmdOrCtrl+Alt+Y`,
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-query-devtools` });
      },
    },
    nt = {
      label: `Toggle React Scan`,
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `toggle-react-scan` });
      },
    },
    rt = async (e) => {
      let t = await y();
      t && _.sendMessageToWindow(t, { type: `step-window-zoom`, delta: e });
    },
    it = async () => {
      let e = await y();
      e && _.sendMessageToWindow(e, { type: `reset-window-zoom` });
    },
    at = () => {
      let e = a.BrowserWindow.getFocusedWindow();
      !e || e.isDestroyed() || e.setFullScreen(!e.isFullScreen());
    },
    ot = {
      ...b(`navigateBack`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `navigate-back` });
      },
    },
    st = {
      ...b(`navigateForward`),
      click: async () => {
        let e = await y();
        e && _.sendMessageToWindow(e, { type: `navigate-forward` });
      },
    },
    ct = {
      label: `Check for Updates…`,
      enabled: !0,
      click: () => {
        if ((v1().info(`Check for updates requested via menu.`), !u.hasUpdater())) {
          let e = u.getUnavailableReason() ?? `unknown`;
          (v1().warning(`Desktop updater unavailable; init likely skipped.`, {
            safe: { reason: e },
            sensitive: {},
          }),
            a.dialog.showMessageBox({
              type: `info`,
              title: `Updates Unavailable`,
              message: `Automatic updates are unavailable right now.`,
              detail: `Updater initialization skipped: ${e}`,
            }));
          return;
        }
        u.checkForUpdates();
      },
    },
    lt = [xe, { type: `separator` }, x];
  (c && lt.push(ct, { type: `separator` }),
    lt.push(
      { role: `services` },
      { type: `separator` },
      { role: `hide` },
      { role: `hideOthers` },
      { role: `unhide` },
      { type: `separator` },
      M,
      { role: `quit` },
    ));
  let ut = { label: `File`, id: t.Fa.file, submenu: [] },
    dt = { role: `appMenu`, submenu: lt },
    ft = { label: `View`, id: t.Fa.view, submenu: [] },
    pt = { role: `windowMenu`, id: t.Fa.window },
    mt = [
      ce,
      le,
      de,
      ue,
      ...(l.processManager ? [fe] : []),
      ...pe,
      ...(n ? [me] : []),
      he,
      ge,
      _e,
      ve,
      ye,
      be,
      { type: `separator` },
      Se,
      Ce,
      we,
      Te,
      ...(l.browserPane ? [De, Ee, ze, Be] : []),
      He,
      We,
      { type: `separator` },
      Ke,
      Je,
      Xe,
      Qe,
      ot,
      st,
      ...(s ? [Ve] : []),
    ],
    ht = [
      ...(o ? [$e, et] : []),
      ...(i ? [{ role: `toggleDevTools` }] : []),
      ...(i && t.Lo.supportsReactScan(e) ? [nt] : []),
      ...(t.Ji({ buildFlavor: e, isDev: !1 }) ? [tt] : []),
    ];
  ht.length > 0 && mt.push({ type: `separator` }, ...ht);
  let gt = [
    {
      label: `Zoom In`,
      accelerator: `CmdOrCtrl+Plus`,
      click: async () => {
        await rt(1);
      },
    },
    ...(n
      ? []
      : [
          {
            label: `Zoom In`,
            accelerator: `Ctrl+=`,
            acceleratorWorksWhenHidden: !0,
            visible: !1,
            click: async () => {
              await rt(1);
            },
          },
        ]),
    {
      label: `Zoom Out`,
      accelerator: `CmdOrCtrl+-`,
      click: async () => {
        await rt(-1);
      },
    },
    {
      label: `Actual Size`,
      accelerator: `CmdOrCtrl+0`,
      click: async () => {
        await it();
      },
    },
  ];
  (mt.push({ type: `separator` }, ...gt),
    n
      ? mt.push(
          { type: `separator` },
          {
            label: `Toggle Full Screen`,
            accelerator: `Command+Control+F`,
            visible: !1,
            acceleratorWorksWhenHidden: !0,
            click: at,
          },
        )
      : mt.push(
          { type: `separator` },
          { label: `Toggle Full Screen`, accelerator: `F11`, click: at },
        ),
    (ft.submenu = mt));
  let _t = [],
    vt = [
      ...(n ? [dt, ut] : [ut]),
      { role: `editMenu`, id: t.Fa.edit },
      ft,
      ...(n ? [pt] : []),
      {
        role: `help`,
        id: t.Fa.help,
        submenu: [
          {
            label: `Codex Documentation`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/app`);
            },
          },
          {
            label: `What's new`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/changelog`);
            },
          },
          {
            label: `Automations`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/app/automations`);
            },
          },
          {
            label: `Local Environments`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/app/local-environments`);
            },
          },
          {
            label: `Worktrees`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/app/worktrees`);
            },
          },
          {
            label: `Skills`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/skills`);
            },
          },
          {
            label: `Model Context Protocol`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/mcp`);
            },
          },
          {
            label: `Troubleshooting`,
            click: () => {
              a.shell.openExternal(`https://developers.openai.com/codex/app/troubleshooting`);
            },
          },
          { type: `separator` },
          { label: `Send Feedback`, click: ee },
          Ue,
          ..._t,
          { type: `separator` },
          {
            ...b(`showKeyboardShortcuts`),
            click: async () => {
              await I(`showKeyboardShortcuts`);
            },
          },
          ...(n ? [] : [{ type: `separator` }, xe]),
        ],
      },
    ],
    yt = a.Menu.buildFromTemplate(vt),
    bt = yt.getMenuItemById(t.Fa.file)?.submenu;
  if (bt) {
    let e = 0;
    if (
      (l.multiWindow && bt.insert(e++, new a.MenuItem(T)),
      bt.insert(e++, new a.MenuItem(S)),
      bt.insert(e++, new a.MenuItem(w)),
      bt.insert(e++, new a.MenuItem(C)),
      bt.insert(e++, new a.MenuItem(j)),
      bt.insert(e++, new a.MenuItem(O)),
      A.accelerator != null && bt.insert(e++, new a.MenuItem(A)),
      !n)
    ) {
      (bt.insert(e, new a.MenuItem({ type: `separator` })),
        bt.append(new a.MenuItem(x)),
        bt.append(new a.MenuItem({ type: `separator` })));
      let t = bt.items.findIndex((e) => e.role === `quit`);
      t >= 0
        ? bt.insert(t, new a.MenuItem(M))
        : (bt.append(new a.MenuItem(M)), bt.append(new a.MenuItem({ role: `quit` })));
    }
  }
  (a.Menu.setApplicationMenu(yt), yY(_));
}
var S1 = class {
    buildMenu;
    constructor({ buildMenu: e }) {
      this.buildMenu = e;
    }
    refresh() {
      this.buildMenu();
    }
  },
  C1 = t.Wr(`codex-deep-link-navigation`);
async function w1({
  window: e,
  route: n,
  globalState: r,
  gitManager: i,
  appServerClient: a,
  listPlugins: o,
  windowManager: s,
  navigateToRoute: c,
}) {
  switch (n.kind) {
    case `applyCodexAppConfig`:
      return;
    case `pluginInstall`: {
      let r;
      try {
        r = await T1({ listPlugins: o, route: n });
      } catch (t) {
        (C1.warning(`plugin_install_marketplace_lookup_failed`, {
          safe: { marketplaceName: n.marketplaceName, pluginName: n.pluginName },
          sensitive: { error: t },
        }),
          c(e, `/skills`, { initialTab: `plugins` }));
        return;
      }
      if (r == null) {
        c(e, `/skills`, { initialTab: `plugins` });
        return;
      }
      c(e, t.Vi({ marketplacePath: r, pluginId: n.pluginName, pluginName: n.pluginName }));
      return;
    }
    case `petInstall`:
      (s.showPrimaryWindow({ stealFocus: !0 }),
        s.sendMessageToWindow(e, {
          type: `open-pet-install-modal`,
          name: n.name,
          description: n.description,
          imageUrl: n.imageUrl,
        }));
      return;
    case `pluginDetail`:
      c(e, t.Vi(n));
      return;
    case `localPluginDetail`:
      c(
        e,
        t.Vi({
          hostId: n.hostId ?? `local`,
          marketplacePath: n.marketplacePath,
          pluginId: n.pluginName,
          pluginName: n.pluginName,
        }),
        { pluginDetailDeepLinkMode: n.openShareDialog ? `share` : `view` },
      );
      return;
    case `automations`:
      c(e, `/automations`, { automationMode: `create` });
      return;
    case `connectorOAuthCallback`:
      (s.showPrimaryWindow({ stealFocus: !0 }),
        s.sendMessageToWindow(e, {
          type: `connector-oauth-callback`,
          fullRedirectUrl: n.fullRedirectUrl,
          returnTo: n.returnTo ?? void 0,
        }));
      return;
    case `localConversation`:
      c(e, `/local/${n.conversationId}`);
      return;
    case `newThread`: {
      let t = await E1({ route: n, globalState: r, gitManager: i, appServerClient: a });
      (t != null && M1({ globalState: r, workspaceRoot: t, windowManager: s }),
        c(e, `/`, {
          focusComposerNonce: Date.now(),
          prefillPrompt: n.prompt,
          prefillCwd: t ?? void 0,
        }));
      return;
    }
    case `settings`:
      c(e, n.section == null ? `/settings` : `/settings/${n.section}`);
      return;
    case `skills`:
      c(e, `/skills`);
      return;
  }
}
async function T1({ listPlugins: e, route: t }) {
  if (e == null) return null;
  let { marketplaces: n } = await e({});
  return (
    n.find((e) =>
      e.path == null || e.name !== t.marketplaceName
        ? !1
        : e.plugins.some((e) => e.name === t.pluginName),
    )?.path ?? null
  );
}
async function E1({ route: e, globalState: t, gitManager: n, appServerClient: r }) {
  if (e.path != null) {
    let t = await D1(e.path);
    if (t != null) return t;
  }
  return e.originUrl == null
    ? null
    : O1({ targetOriginUrl: e.originUrl, globalState: t, gitManager: n, appServerClient: r });
}
async function D1(e) {
  let t = (0, s.resolve)(e);
  try {
    return (await (0, p.stat)(t)).isDirectory() ? t : null;
  } catch {
    return null;
  }
}
async function O1({ targetOriginUrl: e, globalState: n, gitManager: r, appServerClient: i }) {
  let a = k1(n);
  return a.length === 0
    ? null
    : A1({ targetOriginUrl: e, workspaceRoots: a, origins: await t.Q(a, r, t.ht(i)) });
}
function k1(e) {
  let t = r.s(e);
  return t.length > 0 ? t : r.i(e);
}
function A1({ targetOriginUrl: e, workspaceRoots: n, origins: r }) {
  let i = t.na(e),
    a = new Map();
  for (let e of r)
    e.originUrl != null &&
      (a.has(e.dir) || a.set(e.dir, e.originUrl), a.has(e.root) || a.set(e.root, e.originUrl));
  for (let t of n) {
    let n = a.get(t);
    if (n != null && j1({ targetOriginUrl: e, candidateOriginUrl: n, targetRepo: i })) return t;
  }
  return null;
}
function j1({ targetOriginUrl: e, candidateOriginUrl: n, targetRepo: r }) {
  if (n === e) return !0;
  if (r == null) return !1;
  let i = t.na(n);
  return i == null ? !1 : i.host === r.host && i.owner === r.owner && i.repo === r.repo;
}
function M1({ globalState: e, workspaceRoot: t, windowManager: n }) {
  let i = r.s(e);
  i.includes(t) ||
    (r.d(e, [t, ...i]), n.sendMessageToAllWindows({ type: `workspace-root-options-updated` }));
  let a = r.i(e);
  (a.length === 1 && a[0] === t) ||
    (r.u(e, [t]), n.sendMessageToAllWindows({ type: `active-workspace-roots-updated` }));
}
function N1({
  buildFlavor: e,
  globalState: t,
  errorReporter: i,
  sparkleManager: o,
  gitManager: s,
  getLocalAppServerClient: c,
  applyCodexAppConfig: l,
  getLocalAppServerConnection: u,
  windowServices: d,
  createFreshWindow: f,
  ensureWindow: p,
  getElectronMessageHandlerForWindow: m,
  allowDevtools: h,
  allowDebugMenu: g,
  enableSparkle: _,
  isMacOS: v,
  appVersion: y,
}) {
  let b = (e, t, n) => {
      d.sendMessageToWindow(e, { type: `navigate-to-route`, path: t, state: n });
    },
    x = async () => p(),
    S = r.D({
      app: a.app,
      isMacOS: v,
      ensurePrimaryWindowVisible: x,
      navigateToRoute: async (e, n) => {
        if (n.kind === `applyCodexAppConfig`) {
          await l(e);
          return;
        }
        return w1({
          window: e,
          route: n,
          globalState: t,
          gitManager: s,
          appServerClient: c(),
          listPlugins: n.kind === `pluginInstall` ? (e) => u().listPlugins(e) : void 0,
          windowManager: d.windowManager,
          navigateToRoute: b,
        });
      },
      initialArgv: process.argv,
      errorReporter: i,
    }),
    C = new S1({
      buildMenu: () => {
        x1({
          buildFlavor: e,
          isMacOS: v,
          allowDevtools: h,
          allowDebugMenu: g,
          allowWindowReload: e !== n.i.Prod,
          enableSparkle: _,
          desktopFeatureAvailability: Re(),
          sparkleManager: o,
          ensurePrimaryWindowVisible: x,
          createFreshWindow: f,
          queueCodexDeepLinkUrl: S.queueCodexDeepLinkUrl,
          navigateToRoute: b,
          collapseHotkeyWindowToHome: () => d.hotkeyWindowLifecycleManager.collapseToHome(),
          getElectronMessageHandlerForWindow: m,
          windowManager: d.windowManager,
          traceRecordingUploadOptions: {
            appVersion: y,
            buildFlavor: e,
            buildNumber: r.M.value,
            uploadFeedback: (e) => u().sendAppServerRequest(`feedback/upload`, e),
          },
        });
      },
    }),
    w = () => {
      C.refresh();
    };
  return {
    applicationMenuManager: C,
    deepLinks: S,
    navigateToRoute: b,
    refreshApplicationMenu: w,
    setDesktopFeatureAvailability: (e) => {
      Ve(e) && w();
    },
  };
}
var P1 = t.Gr(`settings-store`),
  F1 = `enter-behavior`,
  I1 = t.Hs.object({
    desktop: t.Hs.record(t.Hs.string(), t.Hs.unknown()).optional(),
  }).passthrough(),
  L1 = class {
    state;
    pendingLegacyMigrations = new Map();
    pendingWrites = new Map();
    configClient = null;
    persistQueued = Promise.resolve();
    constructor(e, t) {
      ((this.filePath = e),
        (this.globalState = t),
        (this.state = B1(this.filePath)),
        R1(this.state, this.pendingLegacyMigrations, this.globalState));
    }
    getStateFilePath() {
      return this.filePath;
    }
    async initialize(e) {
      this.configClient = e;
      let n = z1((await e.readConfig()).desktop),
        r = new Set();
      for (let e of t.di)
        if (!this.pendingWrites.has(e.key)) {
          if (n.has(e.key)) {
            (this.state.set(e.key, n.get(e.key)),
              this.pendingLegacyMigrations.delete(e.key) && r.add(e.key));
            continue;
          }
          this.pendingLegacyMigrations.has(e.key) || this.state.delete(e.key);
        }
      (U1(r, this.globalState), await this.persistPendingSettings());
    }
    get(e) {
      return this.state.get(e);
    }
    getEffective(e) {
      return this.get(e) ?? t.ai(e).default;
    }
    set(e, n) {
      let r = t.oi(e).parse(n);
      (this.state.set(e, r), this.pendingWrites.set(e, r), this.persistPendingSettings());
    }
    flush() {
      return this.persistQueued.then(() => this.persistPendingSettings());
    }
    persistPendingSettings() {
      let e = this.configClient;
      return (
        e == null ||
          (this.persistQueued = this.persistQueued
            .catch(() => void 0)
            .then(async () => {
              let n = new Map(this.pendingLegacyMigrations),
                r = new Map(this.pendingWrites),
                i = new Map(n);
              for (let [e, t] of r) i.set(e, t);
              if (i.size !== 0)
                try {
                  await e.batchWriteConfigValues({
                    edits: [...i.entries()].map(([e, n]) => {
                      let r = t.ui(t.oi(e), n);
                      return { keyPath: `desktop.${e}`, mergeStrategy: `replace`, value: r };
                    }),
                  });
                  for (let [e, t] of n)
                    this.pendingLegacyMigrations.get(e) === t &&
                      this.pendingLegacyMigrations.delete(e);
                  for (let [e, t] of r)
                    this.pendingWrites.get(e) === t && this.pendingWrites.delete(e);
                  U1(new Set(n.keys()), this.globalState);
                } catch (e) {
                  P1().warning(`Failed to persist desktop settings`, {
                    safe: {},
                    sensitive: { error: e },
                  });
                }
            })),
        this.persistQueued
      );
    }
  };
function R1(e, n, r) {
  let i = r.getStored(Vr);
  for (let a of t.di) {
    if (e.has(a.key)) continue;
    let o = H1(a, r, i);
    if (o === void 0) continue;
    let s = t.oi(a.key).safeParse(o);
    if (!s.success) {
      P1().warning(`Dropping invalid migrated setting`, { safe: { key: a.key }, sensitive: {} });
      continue;
    }
    (e.set(a.key, s.data), n.set(a.key, s.data));
  }
}
function z1(e) {
  return e == null ? new Map() : V1(e);
}
function B1(e) {
  if (!(0, l.existsSync)(e)) return new Map();
  try {
    return V1(I1.parse(t.Or((0, l.readFileSync)(e, `utf8`))).desktop ?? {});
  } catch (e) {
    return (
      P1().warning(`Failed to load desktop settings from config.toml`, {
        safe: {},
        sensitive: { error: e },
      }),
      new Map()
    );
  }
}
function V1(e) {
  let n = new Map();
  for (let [r, i] of Object.entries(e)) {
    let e = t.ai(r);
    if (e == null) continue;
    let a = t.oi(e.key).safeParse(t.li(t.oi(e.key), i));
    if (!a.success) {
      P1().warning(`Dropping invalid desktop setting`, { safe: { key: r }, sensitive: {} });
      continue;
    }
    n.set(e.key, a.data);
  }
  return n;
}
function H1(e, n, r) {
  let { hostStorage: i } = e;
  switch (i.kind) {
    case `configuration`: {
      let a = n.getStored(i.key);
      return a === void 0
        ? e.key === t._i.composerEnterBehavior.key &&
          r != null &&
          Object.prototype.hasOwnProperty.call(r, F1)
          ? r[F1]
          : e.key === t._i.keepRemoteControlAwakeWhilePluggedIn.key
            ? n.getStored(t._i.preventSleepWhileRunning.key)
            : void 0
        : a;
    }
    case `global-state`:
      return n.getStored(i.key);
    case `persisted-atom`:
      return r == null || !Object.prototype.hasOwnProperty.call(r, i.key)
        ? void 0
        : e.key === t.mi.defaultServiceTier.key
          ? t.xi(r)
          : r[i.key];
  }
}
function U1(e, n) {
  if (e.size === 0) return;
  let r = n.getStored(Vr),
    i = r ? { ...r } : null,
    a = !1;
  for (let r of e) {
    let e = t.ai(r);
    switch (
      (r === t._i.composerEnterBehavior.key &&
        i != null &&
        Object.prototype.hasOwnProperty.call(i, F1) &&
        (delete i[F1], (a = !0)),
      e.hostStorage.kind)
    ) {
      case `configuration`:
      case `global-state`:
        n.delete(e.hostStorage.key);
        break;
      case `persisted-atom`:
        i != null &&
          Object.prototype.hasOwnProperty.call(i, e.hostStorage.key) &&
          (delete i[e.hostStorage.key], (a = !0));
        break;
    }
  }
  a && n.set(Vr, Object.keys(i ?? {}).length === 0 ? void 0 : (i ?? void 0));
}
var W1 = `.codex-global-state.json`,
  G1 = `config.toml`;
function K1({ moduleDir: e }) {
  t.yr();
  let n = t.Mr(),
    r = (0, s.join)(e, `preload.js`),
    i = (0, s.join)(e, `..`, `..`),
    a = (0, s.join)(i, `..`),
    o = new ii((0, s.join)(n, W1)),
    c = new L1((0, s.join)(n, G1), o),
    l =
      process.platform === `win32` &&
      process.env.WSL_DISTRO_NAME == null &&
      c.getEffective(t._i.runCodexInWsl.key) === !0 &&
      t.Fr() != null;
  return (
    t.Rr(() => l),
    EM(c.getEffective(t.yi.theme.key) ?? `system`),
    { codexHome: n, preloadPath: r, desktopRoot: i, repoRoot: a, globalState: o, settingsStore: c }
  );
}
var q1 = `codex.sqliteBackfillProgress.title`,
  J1 = `Rebuilding conversation history`,
  Y1 = `codex.sqliteBackfillProgress.detail`,
  X1 = `This may take several minutes — keep the app open`;
async function Z1({ onQuit: e }) {
  let t = r.V(),
    n = t.formatMessage({ messageId: q1, defaultMessage: J1 }),
    i = t.formatMessage({ messageId: Y1, defaultMessage: X1 }),
    o = !1,
    s = !1,
    c = new a.BrowserWindow({
      width: 420,
      height: 176,
      resizable: !1,
      maximizable: !1,
      fullscreenable: !1,
      show: !1,
      title: n,
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        sandbox: !0,
        spellcheck: !1,
        devTools: !1,
      },
    });
  (c.setMenuBarVisibility(!1),
    c.on(`close`, (t) => {
      if (!(o || s)) {
        (t.preventDefault(), (s = !0));
        try {
          e();
        } finally {
          s = !1;
        }
      }
    }),
    c.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    c.webContents.on(`will-navigate`, (e) => {
      e.preventDefault();
    }));
  try {
    await c.loadURL(`data:text/html;charset=utf-8,${encodeURIComponent(Q1(n, i))}`);
  } catch (e) {
    throw ((o = !0), c.isDestroyed() || c.destroy(), e);
  }
  return (
    c.isDestroyed() || (c.center(), c.show(), c.focus()),
    () => {
      ((o = !0), c.isDestroyed() || c.destroy());
    }
  );
}
function Q1(e, t) {
  let n = (0, cB.default)(e);
  return `<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    :root { color-scheme: light dark; }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      background: Canvas;
      color: CanvasText;
      font-family: system-ui, sans-serif;
    }
    main { width: 100%; padding: 28px 32px; }
    h1 { margin: 0; font-size: 17px; font-weight: 600; }
    p { margin: 8px 0 0; color: GrayText; font-size: 13px; line-height: 1.45; }
    progress { width: 100%; margin-top: 22px; }
  </style>
</head>
<body>
  <main>
    <h1>${n}</h1>
    <p>${(0, cB.default)(t)}</p>
    <progress aria-label="${n}"></progress>
  </main>
</body>
</html>`;
}
function $1(e, r, i = t.Cs.Codex) {
  if (i === t.Cs.ChatGPT && e !== n.i.Agent && (r === `win32` || r === `darwin`))
    return `icon-chatgpt`;
  switch (e) {
    case n.i.Agent:
      return `icon-agent`;
    case n.i.Nightly:
      return `icon-nightly`;
    case n.i.InternalAlpha:
      return `icon-alpha`;
    case n.i.PublicBeta:
      return `icon-beta`;
    case n.i.Dev:
    case n.i.Prod:
      return `icon`;
  }
}
function e0(e) {
  let t;
  switch (e) {
    case n.i.Dev:
    case n.i.Prod:
      t = ``;
      break;
    case n.i.Nightly:
      t = `-nightly`;
      break;
    case n.i.InternalAlpha:
      t = `-alpha`;
      break;
    case n.i.PublicBeta:
      t = `-beta`;
      break;
    case n.i.Agent:
      return { "app-default": null, "codex-dark": null, "codex-light": null };
  }
  return {
    "app-default": null,
    "codex-dark": `icon-codex-dark${t}.png`,
    "codex-light": `icon-codex-light${t}.png`,
  };
}
var t0 = `DoubleCommand`,
  n0 = 6e4;
function r0({ globalState: e, windowManager: n, enabled: r }) {
  let i = e.getStored(`appshotHotkey`),
    a = i === void 0 ? t0 : i,
    o = null,
    s = () => ({
      supported: r && process.platform === `darwin`,
      configuredHotkey: a,
      isActive: o != null,
    }),
    c = () => {
      if ((o?.unregister(), (o = null), !r || process.platform !== `darwin` || a == null)) {
        t.Ur().info(`Appshot hotkey inactive`, {
          safe: { enabled: r, platform: process.platform, configured: a != null },
          sensitive: {},
        });
        return;
      }
      if (
        (t.Ur().info(`Registering appshot hotkey`, { safe: { hotkey: a }, sensitive: {} }),
        RO(a, { bareModifierTrigger: `immediatePress` }),
        (o = FO(
          a,
          {
            onPressed: () => {
              t.Ur().info(`Appshot hotkey pressed`, { safe: { hotkey: a }, sensitive: {} });
              let e = n.getPrimaryWindow();
              if (e == null || e.isDestroyed()) {
                t.Ur().info(`Appshot hotkey skipped`, {
                  safe: { reason: e == null ? `no-primary-window` : `primary-window-destroyed` },
                  sensitive: {},
                });
                return;
              }
              let r =
                n.wasPrimaryWindowFocusedWithin(e, n0) &&
                n.getAppShellShortcutState(e.webContents.id)?.canAcceptAppshotShortcut === !0;
              (r ||
                n.sendMessageToWindow(e, {
                  type: `navigate-to-route`,
                  path: `/`,
                  state: { focusComposerNonce: Date.now() },
                }),
                t
                  .Ur()
                  .info(`Dispatching appshot hotkey`, {
                    safe: { target: r ? `current-route` : `new-chat`, windowId: e.id },
                    sensitive: {},
                  }),
                n.sendMessageToWindow(e, { type: `appshot-shortcut` }));
            },
          },
          { bareModifierTrigger: `immediatePress` },
        )),
        o == null)
      )
        throw Error(`Unable to register shortcut: ${a}`);
      t.Ur().info(`Registered appshot hotkey`, { safe: { hotkey: a }, sensitive: {} });
    },
    l = (n) => {
      if (!r || process.platform !== `darwin`)
        return { success: !1, error: `Not supported.`, state: s() };
      if (n != null) {
        let e = LO(n);
        if (e != null) return { success: !1, error: e, state: s() };
      }
      let i = a;
      a = n;
      try {
        c();
      } catch (e) {
        a = i;
        try {
          c();
        } catch (e) {
          t.Ur().warning(`Failed to restore appshot hotkey after update failure`, {
            safe: {},
            sensitive: { rollbackError: e },
          });
        }
        return { success: !1, error: e instanceof Error ? e.message : String(e), state: s() };
      }
      return (e.set(`appshotHotkey`, a), { success: !0, state: s() });
    };
  try {
    c();
  } catch (e) {
    t.Ur().warning(`Failed to register appshot hotkey`, { safe: {}, sensitive: { error: e } });
  }
  return {
    rpc: { getState: s, setHotkey: l },
    dispose: () => {
      (o?.unregister(), (o = null));
    },
  };
}
var i0 = `avatar-overlay.node`,
  a0 = `avatar_overlay.node`,
  o0 = (0, C.createRequire)(__filename);
function s0(e) {
  let t = c0(e).find(l.existsSync);
  if (t == null) throw Error(`Avatar overlay native addon not found`);
  return u0(t);
}
function c0(e) {
  let t = l0(e, a0),
    n = Ze({ env: process.env, resourcesPath: process.resourcesPath });
  return n == null ? [t] : [s.default.join(n, `native`, i0), t];
}
function l0(e, t) {
  return s.default.join(
    e,
    `native`,
    `avatar-overlay-addon`,
    `artifacts`,
    `Release-${process.arch}`,
    t,
  );
}
function u0(e) {
  return o0(e);
}
function d0({ devAppPath: e, platform: n = process.platform } = {}) {
  if (n !== `darwin`) return null;
  let r = null,
    i = !1;
  return {
    animateSurface(e) {
      r?.animateCompositionSurface(e);
    },
    attachMaterial({
      activityStackDurationMs: n,
      activityStackOpacity: o,
      activityStackRect: s,
      mascotRect: c,
      rootWindow: l,
      surfaces: u,
    }) {
      i = !1;
      try {
        r ??= s0(e ?? a.app.getAppPath());
        let i = r.attachCompositionMaterial({
          activityStackDurationMs: n,
          activityStackOpacity: o,
          activityStackRect: s,
          mascotRect: c,
          root: f0(l),
          surfaces: u.map(({ cornerRadius: e, id: t, window: n }) => ({
            ...f0(n),
            cornerRadius: e,
            id: t,
          })),
        });
        return (
          t
            .Ur()
            .info(`Native pet material attachment completed`, {
              safe: { attached: i, surfaceCount: u.length },
            }),
          i || r.detachCompositionMaterial(),
          i
        );
      } catch (e) {
        return (
          r?.detachCompositionMaterial(),
          (r = null),
          (i = !0),
          t
            .Ur()
            .warning(`Failed to attach native pet material`, { safe: {}, sensitive: { error: e } }),
          !1
        );
      }
    },
    consumeActivityStackScrollDeltaY() {
      return r?.consumeCompositionActivityStackScrollDeltaY() ?? 0;
    },
    consumePointerDownSurfaceId() {
      return r?.consumeCompositionPointerDownSurfaceId() ?? null;
    },
    consumePointerUpSurfaceId() {
      return r?.consumeCompositionPointerUpSurfaceId() ?? null;
    },
    detach() {
      (r?.detachCompositionMaterial(), (r = null), (i = !1));
    },
    getCursorPosition() {
      if (i) return null;
      try {
        return ((r ??= s0(e ?? a.app.getAppPath())), r.getCursorPosition());
      } catch {
        return ((i = !0), null);
      }
    },
    dumpHierarchy() {
      return r?.dumpCompositionHierarchy() ?? null;
    },
    getPresentations() {
      return r?.getCompositionSurfacePresentations() ?? [];
    },
    getPointerSurfaceId() {
      return r?.getCompositionPointerSurfaceId() ?? null;
    },
    isAnimating() {
      return r?.isCompositionAnimating() ?? !1;
    },
    isPrimaryMouseButtonPressed() {
      if (i) return !1;
      try {
        return ((r ??= s0(e ?? a.app.getAppPath())), r.isPrimaryMouseButtonPressed());
      } catch {
        return ((i = !0), !1);
      }
    },
    orderSurfaces(e) {
      r?.orderCompositionSurfaces(e);
    },
    prepareDragFollowForOverlayMove(e) {
      r?.prepareCompositionDragFollowForOverlayMove(e);
    },
    presentNotificationStack(e) {
      r?.presentNotificationStack(e);
    },
    setNotificationStackExpanded(e) {
      r?.setNotificationStackExpanded(e);
    },
    setOverlayWindowPosition(n, o) {
      if (i) return !1;
      try {
        return (
          (r ??= s0(e ?? a.app.getAppPath())),
          r.setOverlayWindowPosition({ position: o, root: f0(n) })
        );
      } catch (e) {
        return (
          (i = !0),
          t
            .Ur()
            .warning(`Failed to position native pet window`, { safe: {}, sensitive: { error: e } }),
          !1
        );
      }
    },
    updateMaterial(e) {
      if (r == null) return !1;
      try {
        return (r.updateCompositionMaterial(e), !0);
      } catch (e) {
        return (
          r.detachCompositionMaterial(),
          (r = null),
          t
            .Ur()
            .warning(`Failed to update native pet material`, { safe: {}, sensitive: { error: e } }),
          !1
        );
      }
    },
  };
}
function f0(e) {
  return {
    contentBounds: e.getContentBounds(),
    mediaSourceId: typeof e.getMediaSourceId == `function` ? e.getMediaSourceId() : null,
    nativeWindowHandle:
      typeof e.getNativeWindowHandle == `function` ? e.getNativeWindowHandle() : null,
    title: e.getTitle(),
  };
}
var p0 = { width: 356, height: 320 },
  m0 = { width: 384, height: 400 },
  h0 = { top: 8, right: 28, bottom: 8, left: 0 },
  g0 = 64,
  _0 = 4,
  v0 = 12,
  y0 = 96;
function b0({
  anchor: e,
  displayBounds: t,
  mode: n = `native`,
  mascotSize: r,
  nativeMaterialAttached: i = !1,
  previousPlacement: a,
  traySize: o,
  viewportSize: s = n === `native` ? m0 : p0,
}) {
  let c = { width: Math.min(s.width, t.width), height: Math.min(s.height, t.height) },
    l = n === `native`,
    u = E0({ ...e, width: Math.min(r.width, t.width), height: Math.min(r.height, t.height) }, t),
    d =
      o == null
        ? null
        : {
            width: Math.min(o.width, Math.max(0, c.width - h0.left - h0.right)),
            height: Math.min(
              o.height,
              Math.max(0, c.height - u.height - h0.bottom - _0 - (l ? h0.top + v0 * 2 : 0)),
            ),
          },
    f = x0(u, t),
    p =
      d == null
        ? a
        : S0({
            anchor: u,
            displayBounds: t,
            mode: n,
            preferredPlacement: f,
            previousPlacement: a,
            traySize: d,
          }),
    m = d == null ? null : w0(C0(u, d, p, n), t, n),
    h = l
      ? D0({ anchor: u, displayBounds: t, placement: p, viewport: c })
      : O0({
          contentBounds: A0([T0(u), ...(m == null ? [] : [m])]),
          displayBounds: t,
          viewport: c,
        }),
    g = j0(u, h),
    _ =
      l && i
        ? {
            width: Math.min(Math.max(c.width, g.left + g.width + g0), t.width),
            height: Math.min(Math.max(c.height, g.top + g.height + g0), t.height),
          }
        : c,
    v = { ...h, width: _.width, height: _.height };
  return {
    anchor: u,
    mascot: g,
    placement: p,
    tray: m == null ? null : j0(m, v),
    viewport: _,
    windowBounds: v,
  };
}
function x0(e, t) {
  return `${N0(e) < N0(t) ? `bottom` : `top`}-${M0(e) < M0(t) ? `start` : `end`}`;
}
function S0({
  anchor: e,
  displayBounds: t,
  mode: n,
  preferredPlacement: r,
  previousPlacement: i,
  traySize: a,
}) {
  let o;
  o =
    n === `legacy`
      ? [`top-start`, `top-end`, `bottom-start`, `bottom-end`]
      : r.endsWith(`start`)
        ? [`top-start`, `bottom-start`]
        : [`top-end`, `bottom-end`];
  let s = o
    .map((o) => {
      let s = n === `native` ? o.startsWith(`top`) === i.startsWith(`top`) : o === i;
      return { placement: o, score: k0(C0(e, a, o, n), t) + (o === r ? 0 : 32) + (s ? -y0 : 0) };
    })
    .sort((e, t) => e.score - t.score)[0];
  if (s == null) throw Error(`Expected avatar overlay placement`);
  return s.placement;
}
function C0(e, t, n, r) {
  let i = n.startsWith(`top`);
  return r === `legacy`
    ? {
        x: n.endsWith(`end`) ? e.x + e.width - t.width : e.x,
        y: i ? e.y - t.height - _0 : e.y + e.height + _0,
        width: t.width,
        height: t.height,
      }
    : {
        x: e.x + (e.width - t.width) / 2,
        y: i ? e.y - h0.top - t.height - _0 : e.y + e.height + h0.bottom + _0,
        width: t.width,
        height: t.height,
      };
}
function w0(e, t, n) {
  let r = n === `native` ? v0 : 0;
  return {
    ...e,
    x: P0(e.x, t.x + r, t.x + t.width - e.width - r),
    y: P0(e.y, t.y + r, t.y + t.height - e.height - r),
  };
}
function T0(e) {
  return {
    x: e.x - h0.left,
    y: e.y - h0.top,
    width: e.width + h0.left + h0.right,
    height: e.height + h0.top + h0.bottom,
  };
}
function E0(e, t) {
  return {
    ...e,
    x: P0(e.x, t.x, t.x + t.width - e.width),
    y: P0(e.y, t.y, t.y + t.height - e.height - h0.bottom),
  };
}
function D0({ anchor: e, displayBounds: t, placement: n, viewport: r }) {
  return {
    x: P0(e.x + e.width / 2 - r.width / 2, t.x, t.x + t.width - r.width),
    y: P0(
      n.startsWith(`top`) ? e.y + e.height + h0.bottom - r.height : e.y - h0.top,
      t.y,
      t.y + t.height - r.height,
    ),
    width: r.width,
    height: r.height,
  };
}
function O0({ contentBounds: e, displayBounds: t, viewport: n }) {
  return {
    x: P0(e.x + e.width - n.width, t.x, t.x + t.width - n.width),
    y: P0(e.y + e.height - n.height, t.y, t.y + t.height - n.height),
    width: n.width,
    height: n.height,
  };
}
function k0(e, t) {
  let n = Math.max(0, t.x - e.x),
    r = Math.max(0, t.y - e.y),
    i = Math.max(0, e.x + e.width - t.x - t.width),
    a = Math.max(0, e.y + e.height - t.y - t.height);
  return n + r + i + a + (n + i) * e.height + (r + a) * e.width;
}
function A0(e) {
  let t = Math.min(...e.map((e) => e.x)),
    n = Math.min(...e.map((e) => e.y)),
    r = Math.max(...e.map((e) => e.x + e.width)),
    i = Math.max(...e.map((e) => e.y + e.height));
  return { x: t, y: n, width: r - t, height: i - n };
}
function j0(e, t) {
  return {
    left: Math.round(e.x - t.x),
    top: Math.round(e.y - t.y),
    width: Math.round(e.width),
    height: Math.round(e.height),
  };
}
function M0(e) {
  return e.x + e.width / 2;
}
function N0(e) {
  return e.y + e.height / 2;
}
function P0(e, t, n) {
  return t > n ? Math.round((t + n) / 2) : Math.min(Math.max(Math.round(e), t), n);
}
var F0 = m0.height + 64,
  I0 = m0.width + 64,
  L0 = 180,
  R0 = 1e4;
function z0({
  createWindow: e,
  getCursorScreenPoint: n,
  getSurfaceContentState: r,
  getOverlayBounds: i,
  nativeBridge: a,
  onBeforeSurfaceFocus: o,
  onNativeAttachmentFailed: s,
  onNativePresentationOwned: c,
  onPreparationUpdated: l,
  onSurfaceLifecycleFailed: u,
  onSurfaceLifecycleTransition: d,
  onSurfaceFailed: f,
  onSurfaceFocus: p,
  onSurfaceReadyTimeout: m,
  onActivityStackScrollOffsetPublished: h,
}) {
  let g = new Map(),
    _ = null,
    v = null,
    y = null,
    b = null,
    x = null,
    S = null,
    C = a == null,
    w = null,
    T = [],
    E = 0,
    D = !1,
    O = !1;
  return {
    consumePointerDownSurfaceId() {
      let e = a?.consumePointerDownSurfaceId() ?? null;
      return e === a?.getPointerSurfaceId() ? e : null;
    },
    consumePointerUpSurfaceId() {
      return a?.consumePointerUpSurfaceId() ?? null;
    },
    detach() {
      ((C = !0), (x = null), (b = null), (S = null), ne(), a?.detach(), ie());
    },
    getPreparation(e) {
      return g.get(e)?.preparation ?? null;
    },
    getPointerSurfaceId() {
      return a?.getPointerSurfaceId() ?? null;
    },
    getSurfaceId(e) {
      return e == null
        ? null
        : ([...g].find(([, t]) => t.window.webContents.id === e.id)?.[0] ?? null);
    },
    hideSurfacesImmediately() {
      ((_ = null), ne());
      for (let e of g.values()) e.visible && (ee(e, 0), (e.hasPresented = !1));
    },
    moveBackingCanvases() {
      for (let e of g.values()) e.window.setBounds(V0(i()));
    },
    publishActivityStackScrollOffset() {
      C || x != null || h(E);
    },
    setNotificationStackExpanded(e) {
      D = e;
    },
    setPointerInteractionSurface: oe,
    setSurfaceKeyboardInteraction: ae,
    surfaceReady: j,
    scrollActivityStack: k,
    prepareSurfaces(n, o) {
      if (C) return;
      ((D = o.isNotificationStackExpanded),
        D || ((x = null), (S = null)),
        (w = o.activityStackPresentation),
        (T = o.activityStackItems),
        (E = o.activityStackScrollOffset));
      let s =
        o.isNotificationStackExpanded &&
        (x === E || _?.presentsActivityStackScrollImmediately === !0);
      x = null;
      let c = new Set(),
        u = !1,
        d = new Set(),
        f = new Map();
      for (let p of n) {
        let n = p.id;
        d.add(n);
        let m = g.get(n);
        if (m == null) {
          if (!t.Bo.includes(n)) throw Error(`Missing avatar overlay composition surface: ${n}`);
          let a = e(n);
          (a.setBounds(V0(i())),
            a.setFocusable(!1),
            a.setIgnoreMouseEvents(!0, { forward: !0 }),
            a.setOpacity(0),
            a.showInactive(),
            (m = {
              activityStackPresentation: o.activityStackPresentation,
              attached: !1,
              hasPresented: !1,
              keyboardInteractive: !1,
              preparation: {
                acceptsPointerEvents: !1,
                backingRect: { height: 0, left: 0, top: 0, width: 0 },
                chromiumPresentationRect: { height: 0, left: 0, top: 0, width: 0 },
                contentState: r(n, o.contentState),
                cornerRadius: 0,
                id: n,
                nativeAttached: !1,
                opacity: 0,
                platterRect: { height: 0, left: 0, top: 0, width: 0 },
                presentationOffset: { x: 0, y: 0 },
                presentationRect: { height: 0, left: 0, top: 0, width: 0 },
                revision: 0,
              },
              visible: !1,
              window: a,
            }),
            g.set(n, m));
        }
        let h = r(n, o.contentState),
          v = !(0, TP.default)(m.preparation.contentState, h);
        if (
          s &&
          m.visible &&
          !v &&
          !X0(n, m.activityStackPresentation, o.activityStackPresentation)
        ) {
          let e = m.activityStackPresentation.slots.find(({ slotId: e }) => e === n),
            t = o.activityStackPresentation.slots.find(({ slotId: e }) => e === n);
          if (e != null && t != null) {
            if (
              ((m.activityStackPresentation = o.activityStackPresentation),
              G0(e.contentRect, t.contentRect) &&
                G0(e.presentationRect, t.presentationRect) &&
                G0(e.visibleRect, t.visibleRect))
            )
              continue;
            ((m.preparation = H0(m.preparation, e, t)), c.add(n));
            continue;
          }
        }
        let y = Z0(m.preparation, p);
        if (m.visible && !v && y) continue;
        if (m.visible && m.attached && !_?.waitingRevisions.has(n) && !v) {
          ((m.activityStackPresentation = o.activityStackPresentation),
            (m.preparation = {
              ...m.preparation,
              acceptsPointerEvents: p.acceptsPointerEvents,
              backingRect: p.rect,
              chromiumOverflowCornerRadius: p.chromiumOverflowCornerRadius,
              chromiumOverflowRect: p.chromiumOverflowRect,
              chromiumPresentationRect: p.chromiumPresentationRect,
              cornerRadius: p.cornerRadius,
              opacity: p.opacity,
              platterRect: p.platterRect,
              presentationOffset: p.presentationOffset,
              presentationRect: p.presentationRect,
              revision: m.preparation.revision + 1,
            }),
            c.add(n));
          continue;
        }
        m.visible &&
          X0(n, m.activityStackPresentation, o.activityStackPresentation) &&
          (s
            ? (ee(m, 0), (u = !0))
            : m.attached &&
              (a?.animateSurface({ ...B0(m.preparation, 0), acceptsPointerEvents: !1 }), (u = !0)));
        let b = m.preparation.revision + 1;
        (!m.visible || !y) && c.add(n);
        let x = {
          attached: m.attached,
          activityStackPresentation: o.activityStackPresentation,
          hasPresented: m.hasPresented,
          keyboardInteractive: m.keyboardInteractive,
          preparation: {
            acceptsPointerEvents: p.acceptsPointerEvents,
            backingRect: p.rect,
            chromiumOverflowCornerRadius: p.chromiumOverflowCornerRadius,
            chromiumOverflowRect: p.chromiumOverflowRect,
            chromiumPresentationRect: p.chromiumPresentationRect,
            contentState: h,
            cornerRadius: p.cornerRadius,
            id: n,
            nativeAttached: m.attached,
            opacity: p.opacity,
            platterRect: p.platterRect,
            presentationOffset: p.presentationOffset,
            presentationRect: p.presentationRect,
            revision: b,
          },
          visible: !0,
          window: m.window,
        };
        (g.set(n, x), f.set(n, b), l(m.window, x.preparation));
      }
      u && a?.presentNotificationStack(0);
      let p = new Set();
      for (let [e, t] of g) !d.has(e) && t.visible && p.add(e);
      if (_ != null) {
        for (let [e, t] of _.waitingRevisions) d.has(e) && !f.has(e) && f.set(e, t);
        for (let e of _.animatedSurfaceIds) d.has(e) && c.add(e);
      }
      if (c.size === 0 && p.size === 0 && O === D) {
        for (let [e, t] of f) _?.waitingRevisions.has(e) && _.waitingRevisions.set(e, t);
        return;
      }
      (f.size === 0 &&
        _ != null &&
        $0(d, _.visibleSurfaceIds) &&
        $0(p, _.hiddenSurfaceIds) &&
        D === _.notificationStackExpanded) ||
        ((_ = {
          animatedSurfaceIds: c,
          hiddenSurfaceIds: p,
          notificationStackExpanded: D,
          presentsActivityStackScrollImmediately: s,
          visibleSurfaceIds: d,
          waitingRevisions: f,
        }),
        te(),
        M());
    },
    surfaceFailed(e) {
      g.has(e) && (u?.(e), f?.(e), re());
    },
  };
  function k(e) {
    if (C || !D || w == null || !w.slots.some(({ visibleRect: e }) => e.height > 0)) return !1;
    let n = t.Go({
      contentHeight: w.contentHeight,
      deltaY: e,
      scrollOffset: S ?? E,
      viewportHeight: w.viewportRect.height,
    });
    return ((S = n), n === E || A(), !0);
  }
  function A() {
    if (C || w == null || _ != null || x != null || S == null) return;
    if (S === E) {
      S = null;
      return;
    }
    let e = K0({ currentPresentation: w, items: T, scrollOffset: E, targetScrollOffset: S }),
      n = t.Wo({ expanded: !0, items: T, scrollOffset: e, viewportRect: w.viewportRect });
    if (((E = e), !Y0(w, n))) {
      ((x = e), h(e));
      return;
    }
    (F(w, n), (w = n), A());
  }
  function j(e) {
    let t = g.get(e.id);
    if (!(t == null || !t.visible || e.revision !== t.preparation.revision))
      switch (e.phase) {
        case `mounted`:
          d?.(e.id, e.phase);
          return;
        case `painted-before-attach`:
          if (t.attached || _?.waitingRevisions.get(e.id) !== e.revision) return;
          if (
            (d?.(e.id, e.phase),
            (t.attached = a?.attachSurface(e.id, t.window, t.preparation.cornerRadius) ?? !1),
            !t.attached)
          ) {
            (u?.(e.id, `native-attached`), re());
            return;
          }
          (d?.(e.id, `native-attached`),
            (t.preparation = {
              ...t.preparation,
              nativeAttached: !0,
              revision: t.preparation.revision + 1,
            }),
            l(t.window, t.preparation),
            _?.waitingRevisions.set(e.id, t.preparation.revision),
            M());
          return;
        case `painted`:
          if (!t.attached || _?.waitingRevisions.get(e.id) !== e.revision) return;
          (_.waitingRevisions.delete(e.id), M());
      }
  }
  function M() {
    if (_ == null || _.waitingRevisions.size > 0) return;
    let e = _;
    ((_ = null), ne(), N(e));
  }
  function N(e) {
    a?.orderSurfaces(J0(w?.slots ?? []));
    let t = O !== e.notificationStackExpanded,
      n = !1,
      r = 0;
    for (let t of e.hiddenSurfaceIds) {
      let i = g.get(t);
      if (i != null) {
        let a = ee(i, e.presentsActivityStackScrollImmediately ? 0 : void 0);
        t.startsWith(`activity-slot-`) && ((n = !0), (r = Math.max(r, a)));
      }
    }
    for (let t of e.animatedSurfaceIds) {
      let i = g.get(t);
      if (i == null) continue;
      let a = P(
        i,
        e.presentsActivityStackScrollImmediately && i.preparation.id !== `composer` ? 0 : void 0,
      );
      t.startsWith(`activity-slot-`) && ((n = !0), (r = Math.max(r, a)));
    }
    (t && ((O = e.notificationStackExpanded), a?.setNotificationStackExpanded(O)),
      !t && n && a?.presentNotificationStack(r));
    for (let t of e.visibleSurfaceIds) {
      let e = g.get(t);
      e != null &&
        (e.window.setOpacity(1),
        e.keyboardInteractive
          ? (e.window.setFocusable(!0), e.window.setIgnoreMouseEvents(!1))
          : e.window.showInactive(),
        I(e));
    }
    (e.visibleSurfaceIds.size > 0 && c(), A());
  }
  function P(e, t) {
    let n = B0(e.preparation, t ?? (e.preparation.id === `composer` || e.hasPresented ? L0 : 0));
    return e.attached
      ? (a?.animateSurface(n),
        e.hasPresented || d?.(e.preparation.id, `presented`),
        (e.hasPresented = !0),
        n.durationMs)
      : (e.window.setIgnoreMouseEvents(!0, { forward: !0 }), e.window.setOpacity(0), 0);
  }
  function F(e, t) {
    for (let n of t.slots) {
      let r = e.slots.find(({ slotId: e }) => e === n.slotId),
        i = g.get(n.slotId);
      if (r == null || i == null || !i.attached) continue;
      ((i.preparation = H0(i.preparation, r, n)), (i.activityStackPresentation = t));
      let o = B0(i.preparation, 0);
      a?.animateSurface(o);
    }
    a?.presentNotificationStack(0);
  }
  function I(e) {
    if (e == null || !e.visible || !e.attached || e.preparation.id !== y) return;
    (e.window.show(),
      e.window.setFocusable(!0),
      e.window.setIgnoreMouseEvents(!1),
      (e.keyboardInteractive = !0));
    let t = !1,
      n = () => {
        t || !e.keyboardInteractive || ((t = !0), p?.(e.window));
      };
    (e.window.isFocused() || e.window.once(`focus`, n),
      o(),
      e.window.focus(),
      e.window.webContents.focus(),
      e.window.isFocused() && n(),
      (y = null));
  }
  function ee(e, t = L0) {
    ((e.visible = !1),
      ae(e.preparation.id, !1),
      (e.preparation = { ...e.preparation, revision: e.preparation.revision + 1 }));
    let n = { ...B0(e.preparation, t), acceptsPointerEvents: !1, opacity: 0 };
    return e.attached ? (a?.animateSurface(n), n.durationMs) : (e.window.setOpacity(0), 0);
  }
  function te() {
    (ne(),
      _?.waitingRevisions.size !== 0 &&
        (v = setTimeout(() => {
          if (_ != null) {
            m?.(_.waitingRevisions);
            for (let e of _.waitingRevisions.keys())
              u?.(e, g.get(e)?.attached === !0 ? `painted` : `painted-before-attach`);
          }
          re();
        }, R0)));
  }
  function ne() {
    v != null && (clearTimeout(v), (v = null));
  }
  function re() {
    ((C = !0), (x = null), (S = null), ne(), (_ = null), a?.detach(), ie(), s());
  }
  function ie() {
    let e = [...g.values()];
    g.clear();
    for (let t of e) t.window.isDestroyed() || t.window.close();
  }
  function ae(e, t) {
    let n = g.get(e);
    if (!t) {
      (y === e && (y = null), n != null && (n.keyboardInteractive = !1), se());
      return;
    }
    n?.keyboardInteractive || y === e || ((y = e), I(n));
  }
  function oe(e) {
    if (b === e) {
      let t = e == null ? null : g.get(e);
      t != null && ce(t);
      return;
    }
    ((b = e), se());
  }
  function se() {
    for (let [e, t] of g) {
      let n = e === b;
      (t.window.setFocusable(t.keyboardInteractive || n),
        n
          ? (t.window.setIgnoreMouseEvents(!1), ce(t))
          : t.window.setIgnoreMouseEvents(!0, { forward: !0 }));
    }
  }
  function ce(e) {
    let t = le(e);
    t != null &&
      e.window.webContents.sendInputEvent({ type: `mouseMove`, ...t, movementX: 0, movementY: 0 });
  }
  function le(e) {
    let t = n(),
      r = e.window.getContentBounds(),
      i = t.x - r.x - e.preparation.backingRect.left - e.preparation.presentationOffset.x,
      a = t.y - r.y - e.preparation.backingRect.top - e.preparation.presentationOffset.y;
    return i >= 0 && a >= 0 && i <= r.width && a <= r.height ? { x: i, y: a } : null;
  }
}
function B0(e, t) {
  let n = e.presentationRect.height < e.platterRect.height;
  return {
    acceptsPointerEvents: e.acceptsPointerEvents,
    backingRect: e.backingRect,
    chromiumOverflowCornerRadius: n ? void 0 : e.chromiumOverflowCornerRadius,
    chromiumOverflowRect: n || e.chromiumOverflowRect == null ? void 0 : Q0(e.chromiumOverflowRect),
    chromiumPresentationRect: Q0(e.chromiumPresentationRect),
    durationMs: t,
    id: e.id,
    opacity: e.opacity,
    platterRect: Q0(e.platterRect),
    presentationOffset: e.presentationOffset,
    presentationRect: Q0(e.presentationRect),
    revision: e.revision,
  };
}
function V0(e) {
  return { height: F0, width: I0, x: e.x - 32, y: e.y - 32 };
}
function H0(e, t, n) {
  let r = n.presentationRect.left - t.presentationRect.left,
    i = n.presentationRect.top - t.presentationRect.top,
    a = W0(e.backingRect, { x: r, y: i });
  return {
    ...e,
    acceptsPointerEvents: n.visibleRect.height > 0,
    backingRect: a,
    chromiumOverflowRect:
      e.chromiumOverflowRect == null ? void 0 : W0(e.chromiumOverflowRect, { x: r, y: i }),
    chromiumPresentationRect: a,
    opacity: n.visibleRect.height > 0 ? 1 : 0,
    platterRect: a,
    presentationOffset: { x: 0, y: 0 },
    presentationRect: U0(a, n.visibleRect, n.presentationRect),
    revision: e.revision + 1,
  };
}
function U0(e, t, n) {
  return {
    height: t.height,
    left: e.left + t.left - n.left,
    top: e.top + t.top - n.top,
    width: t.width,
  };
}
function W0(e, t) {
  return { ...e, left: e.left + t.x, top: e.top + t.y };
}
function G0(e, t) {
  return e.height === t.height && e.left === t.left && e.top === t.top && e.width === t.width;
}
function K0({ currentPresentation: e, items: n, scrollOffset: r, targetScrollOffset: i }) {
  let a = Math.sign(i - r),
    o = Math.abs(i - r);
  if (q0(e, t.Wo({ expanded: !0, items: n, scrollOffset: i, viewportRect: e.viewportRect })) <= 1)
    return i;
  let s = 0,
    c = Math.ceil(o);
  for (; s + 1 < c; ) {
    let i = Math.floor((s + c) / 2);
    q0(
      e,
      t.Wo({ expanded: !0, items: n, scrollOffset: r + a * i, viewportRect: e.viewportRect }),
    ) <= 1
      ? (s = i)
      : (c = i);
  }
  return r + a * Math.max(1, s);
}
function q0(e, t) {
  return t.slots.filter(
    ({ itemId: t, slotId: n }) => e.slots.find((e) => e.slotId === n)?.itemId !== t,
  ).length;
}
function J0(e) {
  let n = e.toSorted((e, t) => e.zIndex - t.zIndex).map(({ slotId: e }) => e),
    r = new Set(n);
  return [...t.Bo.filter((e) => !r.has(e)), ...n];
}
function Y0(e, t) {
  return (
    e.slots.length === t.slots.length &&
    e.slots.every(({ itemId: e, slotId: n }) =>
      t.slots.some((t) => t.itemId === e && t.slotId === n),
    )
  );
}
function X0(e, t, n) {
  let r = t.slots.find(({ slotId: t }) => t === e)?.itemId;
  return r != null && r !== n.slots.find(({ slotId: t }) => t === e)?.itemId;
}
function Z0(e, t) {
  return (
    e.acceptsPointerEvents === t.acceptsPointerEvents &&
    e.chromiumOverflowCornerRadius === t.chromiumOverflowCornerRadius &&
    ((e.chromiumOverflowRect == null && t.chromiumOverflowRect == null) ||
      (e.chromiumOverflowRect != null &&
        t.chromiumOverflowRect != null &&
        G0(e.chromiumOverflowRect, t.chromiumOverflowRect))) &&
    e.cornerRadius === t.cornerRadius &&
    e.opacity === t.opacity &&
    e.platterRect.height === t.platterRect.height &&
    e.platterRect.left === t.platterRect.left &&
    e.platterRect.top === t.platterRect.top &&
    e.platterRect.width === t.platterRect.width &&
    e.chromiumPresentationRect.height === t.chromiumPresentationRect.height &&
    e.chromiumPresentationRect.left === t.chromiumPresentationRect.left &&
    e.chromiumPresentationRect.top === t.chromiumPresentationRect.top &&
    e.chromiumPresentationRect.width === t.chromiumPresentationRect.width &&
    e.presentationOffset.x === t.presentationOffset.x &&
    e.presentationOffset.y === t.presentationOffset.y &&
    e.presentationRect.height === t.presentationRect.height &&
    e.presentationRect.left === t.presentationRect.left &&
    e.presentationRect.top === t.presentationRect.top &&
    e.presentationRect.width === t.presentationRect.width &&
    e.backingRect.height === t.rect.height &&
    e.backingRect.left === t.rect.left &&
    e.backingRect.top === t.rect.top &&
    e.backingRect.width === t.rect.width
  );
}
function Q0(e) {
  return { ...e, left: 32 + e.left, top: 32 + e.top };
}
function $0(e, t) {
  return e.size === t.size && [...e].every((e) => t.has(e));
}
var e2 = 180;
function t2({
  createWindow: e,
  getCursorScreenPoint: t,
  getOverlayBounds: n,
  nativeBridge: r,
  onBeforeSurfaceFocus: i,
  onNativeAttachmentFailed: a,
  onNativePresentationOwned: o,
  onPreparationUpdated: s,
  onSurfaceLifecycleFailed: c,
  onSurfaceLifecycleTransition: l,
  onSurfaceFailed: u,
  onSurfaceFocus: d,
  onSurfaceReadyTimeout: f,
  onActivityStackScrollOffsetPublished: p,
  overlayWindow: m,
}) {
  let h = null,
    g = !1,
    _ = null,
    v = null,
    y = !1,
    b,
    x = () => {
      y || ((y = !0), o());
    };
  return (
    (b = z0({
      createWindow: e,
      getCursorScreenPoint: t,
      getOverlayBounds: n,
      getSurfaceContentState: r2,
      nativeBridge:
        r == null
          ? null
          : {
              animateSurface: (e) => {
                r.animateSurface(e);
              },
              attachSurface: (e, t, n) =>
                !g || v == null
                  ? !1
                  : ((g = r.attachMaterial({
                      ...v,
                      rootWindow: m,
                      surfaces: [{ cornerRadius: n, id: e, window: t }],
                    })),
                    g),
              consumePointerDownSurfaceId: () => r.consumePointerDownSurfaceId(),
              consumePointerUpSurfaceId: () => r.consumePointerUpSurfaceId(),
              detach: () => {
                ((g = !1), (_ = null), (v = null), r.detach());
              },
              getPointerSurfaceId: () => r.getPointerSurfaceId(),
              orderSurfaces: (e) => {
                r.orderSurfaces(e);
              },
              presentNotificationStack: (e) => {
                r.presentNotificationStack(e);
              },
              setNotificationStackExpanded: (e) => {
                r.setNotificationStackExpanded(e);
              },
            },
      onBeforeSurfaceFocus: i,
      onNativeAttachmentFailed: a,
      onNativePresentationOwned: () => {
        if (_ != null && r != null) {
          if (((g = r.updateMaterial(_)), !g)) {
            (b.detach(), a());
            return;
          }
          ((v = _), (_ = null));
        }
        x();
      },
      onPreparationUpdated: s,
      onSurfaceLifecycleFailed: c,
      onSurfaceLifecycleTransition: l,
      onSurfaceFailed: u,
      onSurfaceFocus: d,
      onSurfaceReadyTimeout: f,
      onActivityStackScrollOffsetPublished: p,
    })),
    {
      ...b,
      prepareSurfaces(e, t) {
        if (r == null) return;
        let n = n2(e, t, 0),
          i = v != null && (v.activityStackRect == null) != (n.activityStackRect == null),
          o = {
            ...n,
            activityStackDurationMs:
              (h != null && h !== t.isNotificationStackExpanded) || i ? e2 : 0,
          };
        h = t.isNotificationStackExpanded;
        let s = g;
        if (
          (s && v?.activityStackRect == null && o.activityStackRect != null
            ? (_ = o)
            : ((_ = null),
              (g = s
                ? r.updateMaterial(o)
                : r.attachMaterial({ ...o, rootWindow: m, surfaces: [] })),
              g && (v = o)),
          !g)
        ) {
          (b.detach(), a());
          return;
        }
        (b.prepareSurfaces(e.surfaces, {
          activityStackItems: e.activityStackItems,
          activityStackPresentation: e.activityStackPresentation,
          activityStackScrollOffset: e.activityStackScrollOffset,
          contentState: t,
          isNotificationStackExpanded: t.isNotificationStackExpanded,
        }),
          e.surfaces.length === 0 && x());
      },
    }
  );
}
function n2(e, n, r) {
  let i = e.surfaces.filter(
      ({ id: e, opacity: t, presentationRect: n }) =>
        e !== `composer` && t > 0.01 && n.width > 0 && n.height > 0,
    ),
    a = null;
  if (n.isNotificationStackExpanded)
    for (let { presentationRect: e } of i)
      a =
        a == null
          ? e
          : {
              height: Math.max(a.top + a.height, e.top + e.height) - Math.min(a.top, e.top),
              left: Math.min(a.left, e.left),
              top: Math.min(a.top, e.top),
              width: Math.max(a.left + a.width, e.left + e.width) - Math.min(a.left, e.left),
            };
  else {
    let e = null,
      n = 0;
    for (let { platterRect: t, presentationRect: r } of i) {
      let i = r.top + r.height - t.height;
      ((e == null || i > e.top) && (e = { ...t, top: i }), (n = Math.max(n, t.width)));
    }
    e != null && (a = { height: t.Ko(i.length, e.height), left: e.left, top: e.top, width: n });
  }
  let o = 0;
  for (let e of i) e.opacity > o && (o = e.opacity);
  return {
    activityStackDurationMs: r,
    activityStackOpacity: a == null ? 0 : o,
    activityStackRect: a,
    mascotRect: n.layout.mascot,
  };
}
function r2(e, t) {
  if (e === `composer`) return t;
  let n = t.activityStackPresentation.slots.find(({ slotId: t }) => t === e),
    r = n?.itemId;
  return i2({
    ...t,
    activities: t.activities.filter(({ notification: e }) => e.id === r),
    activityStackPresentation: { ...t.activityStackPresentation, slots: n == null ? [] : [n] },
    expandedNotificationIds: r != null && t.expandedNotificationIds.includes(r) ? [r] : [],
    isQuickChatVisible: !1,
    pointerSurfaceId: t.pointerSurfaceId === e ? e : null,
    quickChatDraft: ``,
    quickChatResetRevision: 0,
  });
}
function i2(e) {
  let t = e.activityStackBackingLayoutHeight - e.activityStackVisibleLayoutHeight;
  return {
    ...e,
    activityStackVisibleLayoutHeight: e.activityStackBackingLayoutHeight,
    layout:
      e.layout.tray == null
        ? e.layout
        : {
            ...e.layout,
            tray: {
              ...e.layout.tray,
              height: e.layout.tray.height + t,
              top: e.layout.placement.startsWith(`top`) ? e.layout.tray.top - t : e.layout.tray.top,
            },
          },
  };
}
function a2(e) {
  let t = new Map();
  return {
    failed(n, r) {
      let i = t.get(n) ?? [],
        a = r ?? i.at(-1) ?? `created`;
      e({
        action: `detach-native-and-use-css-fallback`,
        failedPhase: a,
        id: n,
        trace: `${i.join(` -> `)} -> failed phase=${a} action=detach-native-and-use-css-fallback`,
      });
    },
    transition(n, r) {
      let i = r === `created` ? [] : (t.get(n) ?? []);
      i.includes(r) || (i.push(r), t.set(n, i), e({ id: n, trace: i.join(` -> `) }));
    },
  };
}
var o2 = `http://localhost:5175/`;
function s2(e) {
  let t = a.app.isPackaged
    ? new URL(`${r.P}/avatar-overlay-composition-surface.html`)
    : new URL(`/avatar-overlay-composition-surface.html`, process.env.ELECTRON_RENDERER_URL || o2);
  return (t.searchParams.set(`surfaceId`, e), t.toString());
}
function c2({
  onFailure: e,
  onLifecycleTransition: t,
  parentWindow: n,
  platform: r = process.platform,
  preloadPath: i,
  registerWindow: o,
  surfaceId: s,
  surfaceUrl: c,
}) {
  let l = new a.BrowserWindow({
    acceptFirstMouse: !0,
    backgroundColor: `#00000000`,
    ...(r === `darwin` ? { enableLargerThanScreen: !0 } : {}),
    focusable: !1,
    frame: !1,
    hasShadow: !1,
    height: m0.height,
    parent: n,
    resizable: !1,
    show: !1,
    skipTaskbar: !0,
    title: `Pet Surface ${s}`,
    transparent: !0,
    webPreferences: {
      backgroundThrottling: !1,
      contextIsolation: !0,
      nodeIntegration: !1,
      preload: i,
    },
    width: m0.width,
  });
  return (
    l.webContents.setWindowOpenHandler(() => ({ action: `deny` })),
    l.webContents.on(`will-navigate`, (e, t) => {
      t !== c && e.preventDefault();
    }),
    l.webContents.on(`will-frame-navigate`, (e) => {
      (!e.isMainFrame || e.url !== c) && e.preventDefault();
    }),
    l.webContents.on(`will-redirect`, (e) => {
      e.preventDefault();
    }),
    o(l),
    t?.(`created`),
    l.setAlwaysOnTop(!0, `floating`),
    l.webContents.setBackgroundThrottling(!1),
    l.webContents.on(`did-finish-load`, () => {
      t?.(`did-finish-load`);
    }),
    l.webContents.on(`did-fail-load`, e),
    l.webContents.on(`render-process-gone`, e),
    l.on(`closed`, e),
    t?.(`load-url`),
    l.loadURL(c).catch(e),
    l
  );
}
var l2 = 300,
  u2 = 32,
  d2 = 100,
  f2 = 8,
  p2 = class {
    coordinator = null;
    lifecycle = a2((e) => {
      if (e.failedPhase == null) {
        t.Ur().info(`Native pet composition lifecycle`, { safe: { id: e.id, trace: e.trace } });
        return;
      }
      t.Ur().warning(`Native pet composition lifecycle`, {
        safe: { action: e.action, id: e.id, phase: e.failedPhase, trace: e.trace },
      });
    });
    latestState = null;
    nativeMaterialAttached = !1;
    overlayWindow = null;
    pointerSurfaceExitTimer = null;
    pointerSurfaceHovered = !1;
    pointerSurfaceId = null;
    pointerSyncTimer = null;
    scrollSettleTimer = null;
    scrollSyncTimer = null;
    unavailable = !1;
    constructor(e, t, n) {
      ((this.windowManager = e), (this.nativeBridge = t), (this.onNativeMaterialStateChanged = n));
    }
    setOverlayWindow(e) {
      e !== this.overlayWindow && (this.detach(), (this.overlayWindow = e));
    }
    isEnabled() {
      return this.latestState != null;
    }
    isNativeMaterialAttached() {
      return this.nativeMaterialAttached;
    }
    getDevelopmentDiagnostics() {
      let e = this.latestState,
        n =
          e?.measurements.surfaces.map(({ id: e, opacity: t, presentationRect: n }) => ({
            id: e,
            opacity: t,
            presentationRect: n,
          })) ?? [];
      if (e != null && !e.contentState.isNotificationStackExpanded) {
        let r = e.measurements.surfaces
          .filter(
            ({ id: e, opacity: n, presentationRect: r }) =>
              t.Ho.some((t) => t === e) && n > 0.01 && r.height > 0,
          )
          .map(({ platterRect: e, presentationRect: t }) => ({
            ...e,
            top: t.top + t.height - e.height,
          }))
          .sort((e, t) => t.top - e.top)[0];
        r != null &&
          (n = n.flatMap((e) => {
            let n = t.Ho.findIndex((t) => t === e.id);
            if (n < 0) return [e];
            let i = t.Uo[n];
            return i == null ? [] : [{ ...e, presentationRect: t.qo(r, i) }];
          }));
      }
      return {
        desiredPresentations: n,
        hierarchy: this.nativeBridge?.dumpHierarchy() ?? null,
        mascotSize: this.latestState?.measurements.mascot ?? null,
        nativeAnimating: this.nativeBridge?.isAnimating() ?? !1,
        nativeMaterialAttached: this.nativeMaterialAttached,
        nativePresentations: this.nativeBridge?.getPresentations() ?? [],
      };
    }
    getCursorPosition() {
      return this.nativeBridge?.getCursorPosition() ?? null;
    }
    isPrimaryMouseButtonPressed() {
      return this.nativeBridge?.isPrimaryMouseButtonPressed() ?? !1;
    }
    moveBackingCanvases() {
      this.coordinator?.moveBackingCanvases();
    }
    prepareDragFollowForOverlayMove(e) {
      this.nativeBridge?.prepareDragFollowForOverlayMove(e);
    }
    setOverlayWindowPosition(e) {
      return this.overlayWindow == null || this.overlayWindow.isDestroyed()
        ? !1
        : (this.nativeBridge?.setOverlayWindowPosition(this.overlayWindow, e) ?? !1);
    }
    setState(e, t) {
      if (this.overlayWindow?.webContents.id === e) {
        if (t == null) {
          ((this.latestState = null), (this.unavailable = !1), this.detachCoordinator());
          return;
        }
        if (
          ((this.latestState = { contentState: t.contentState, measurements: t.measurements }),
          !(this.nativeBridge == null || this.unavailable))
        ) {
          if (this.coordinator == null) {
            this.ensureCoordinator();
            return;
          }
          this.prepareLatestState();
        }
      }
    }
    handleSurfaceReady(e, n) {
      let r = this.coordinator?.getSurfaceId({ id: e });
      (t
        .Ur()
        .info(`Native pet composition surface ready received`, {
          safe: {
            id: n.id,
            phase: n.phase,
            revision: n.revision,
            surfaceId: r ?? `none`,
            webContentsId: e,
          },
        }),
        r === n.id && this.coordinator?.surfaceReady(n));
    }
    handleSurfaceAction(e, t) {
      let n = this.overlayWindow?.webContents.id === e,
        r = n ? null : (this.coordinator?.getSurfaceId({ id: e }) ?? null);
      if (
        !(!n && r == null) &&
        !(
          t.type === `scroll-activity-stack` &&
          this.coordinator?.scrollActivityStack(t.deltaY) === !0
        )
      ) {
        switch (t.type) {
          case `activate-notification`:
            break;
          case `close-notification-stack`:
            this.coordinator?.setNotificationStackExpanded(!1);
            break;
          case `open-notification-stack`:
            this.coordinator?.setNotificationStackExpanded(!0);
            break;
          case `quick-chat-active-changed`:
            this.coordinator?.setSurfaceKeyboardInteraction(`composer`, t.isActive);
            break;
          case `run-notification-control`:
            switch (t.action.type) {
              case `open-follow-up`:
                r != null && this.coordinator?.setSurfaceKeyboardInteraction(r, !0);
                break;
              case `close-follow-up`:
              case `stop`:
                r != null && this.coordinator?.setSurfaceKeyboardInteraction(r, !1);
                break;
              case `submit-follow-up`:
                break;
            }
            break;
          case `activity-stack-scroll-offset-changed`:
          case `composition-pointer-surface-changed`:
          case `dismiss-notification`:
          case `notification-expansion-changed`:
          case `quick-chat-draft-changed`:
          case `quick-chat-surface-hover-changed`:
          case `quick-chat-visibility-changed`:
          case `run-notification-action`:
          case `scroll-activity-stack`:
          case `submit-question-option`:
          case `submit-quick-chat`:
            break;
        }
        n || this.sendActionToRoot(t);
      }
    }
    getSurfaceHost(e) {
      let t = this.coordinator?.getSurfaceId(e);
      return (
        t != null && this.lifecycle.transition(t, `connected`),
        t == null ? null : new m2(t, e.id, this)
      );
    }
    getPreparation(e) {
      return this.coordinator?.getPreparation(e) ?? null;
    }
    detach() {
      ((this.latestState = null), (this.unavailable = !1), this.detachCoordinator());
    }
    ensureCoordinator() {
      let e = this.overlayWindow;
      e == null ||
        e.isDestroyed() ||
        this.nativeBridge == null ||
        this.createCoordinator(e, this.nativeBridge);
    }
    createCoordinator(e, n) {
      let r = new Map(),
        i = null;
      ((i = t2({
        createWindow: (t) => {
          let n = c2({
            onFailure: () => {
              i?.surfaceFailed(t);
            },
            onLifecycleTransition: (e) => {
              this.lifecycle.transition(t, e);
            },
            parentWindow: e,
            preloadPath: (0, s.join)(__dirname, `avatar-overlay-composition-surface-preload.js`),
            registerWindow: (e) => {
              this.windowManager.registerAuxiliaryWindow(e, `avatarOverlay`);
            },
            surfaceId: t,
            surfaceUrl: s2(t),
          });
          return (r.set(t, n), n);
        },
        getCursorScreenPoint: () => a.screen.getCursorScreenPoint(),
        getOverlayBounds: () => this.overlayWindow?.getContentBounds() ?? { x: 0, y: 0 },
        nativeBridge: n,
        onBeforeSurfaceFocus: () => {
          a.app.focus({ steal: !0 });
        },
        onNativeAttachmentFailed: () => {
          (t.Ur().warning(`Native pet composition attachment failed`),
            (this.unavailable = !0),
            (this.nativeMaterialAttached = !1),
            this.onNativeMaterialStateChanged());
        },
        onNativePresentationOwned: () => {
          (t.Ur().info(`Native pet composition presentation owned`),
            this.setNativeMaterialAttached(!0));
        },
        onPreparationUpdated: (e, n) => {
          let i = r.get(n.id);
          i != null &&
            (t
              .Ur()
              .info(`Native pet composition preparation sent`, {
                safe: {
                  backingRect: n.backingRect,
                  backingWindowBounds: i.getContentBounds(),
                  chromiumPresentationRect: n.chromiumPresentationRect,
                  id: n.id,
                  nativeAttached: n.nativeAttached,
                  overlayBounds: this.overlayWindow?.getContentBounds() ?? null,
                  platterRect: n.platterRect,
                  presentationOffset: n.presentationOffset,
                  presentationRect: n.presentationRect,
                  revision: n.revision,
                  webContentsId: i.webContents.id,
                },
              }),
            i.webContents.send(t.Vo, n));
        },
        onSurfaceLifecycleFailed: (e, t) => {
          this.lifecycle.failed(e, t);
        },
        onSurfaceLifecycleTransition: (e, t) => {
          this.lifecycle.transition(e, t);
        },
        onSurfaceFocus: (e) => {
          e.webContents.send(t.Ro, void 0);
        },
        overlayWindow: e,
        onSurfaceFailed: (e) => {
          t.Ur().warning(`Native pet composition surface failed`, { safe: { id: e } });
        },
        onSurfaceReadyTimeout: (e) => {
          t.Ur().warning(`Native pet composition surface timed out`, {
            safe: { surfaces: [...e].map(([e, t]) => `${e}:${t}`).join(`,`) },
          });
          for (let [n, i] of e) {
            let e = r.get(n);
            e != null &&
              t
                .Ur()
                .warning(`Native pet composition pending window state`, {
                  safe: {
                    backgroundThrottling: e.webContents.getBackgroundThrottling(),
                    destroyed: e.isDestroyed(),
                    id: n,
                    loading: e.webContents.isLoading(),
                    opacity: e.getOpacity(),
                    revision: i,
                    visible: e.isVisible(),
                    webContentsId: e.webContents.id,
                  },
                });
          }
        },
        onActivityStackScrollOffsetPublished: (e) => {
          this.sendActionToRoot({ type: `activity-stack-scroll-offset-changed`, offset: e });
        },
      })),
        (this.coordinator = i),
        (this.scrollSyncTimer = setInterval(() => {
          let e = n.consumeActivityStackScrollDeltaY();
          e !== 0 &&
            i.scrollActivityStack(e) &&
            (this.scrollSettleTimer != null && clearTimeout(this.scrollSettleTimer),
            (this.scrollSettleTimer = setTimeout(() => {
              ((this.scrollSettleTimer = null), i.publishActivityStackScrollOffset());
            }, d2)),
            this.scrollSettleTimer.unref());
        }, f2)),
        this.scrollSyncTimer.unref());
      let o = (e, t) => {
          let n = r.get(e),
            o = i.getPreparation(e);
          if (n == null || o == null) return;
          let s = n.getContentBounds(),
            c = a.screen.getCursorScreenPoint(),
            l = c.x - s.x - o.backingRect.left - o.presentationOffset.x,
            u = c.y - s.y - o.backingRect.top - o.presentationOffset.y;
          n.webContents.sendInputEvent(
            t === `mouseMove`
              ? { movementX: 0, movementY: 0, type: t, x: l, y: u }
              : t === `mouseDown` || t === `mouseUp`
                ? { button: `left`, clickCount: 1, type: t, x: l, y: u }
                : { type: t, x: l, y: u },
          );
        },
        c = null,
        l = null;
      ((this.pointerSyncTimer = setInterval(() => {
        let e = i.getPointerSurfaceId();
        (c?.startsWith(`activity-slot-`) && c !== e && o(c, `mouseLeave`),
          (c = e),
          e !== this.pointerSurfaceId &&
            ((this.pointerSurfaceId = e),
            this.sendActionToRoot({ type: `composition-pointer-surface-changed`, surfaceId: e })),
          i.setPointerInteractionSurface(e),
          e == null
            ? this.pointerSurfaceHovered &&
              this.pointerSurfaceExitTimer == null &&
              (this.pointerSurfaceExitTimer = setTimeout(() => {
                ((this.pointerSurfaceExitTimer = null),
                  i.getPointerSurfaceId() ??
                    ((this.pointerSurfaceHovered = !1),
                    this.sendActionToRoot({
                      type: `quick-chat-surface-hover-changed`,
                      isHovered: !1,
                    })));
              }, l2))
            : (e.startsWith(`activity-slot-`) && o(e, `mouseMove`),
              this.pointerSurfaceExitTimer != null &&
                (clearTimeout(this.pointerSurfaceExitTimer), (this.pointerSurfaceExitTimer = null)),
              this.pointerSurfaceHovered ||
                ((this.pointerSurfaceHovered = !0),
                this.sendActionToRoot({
                  type: `quick-chat-surface-hover-changed`,
                  isHovered: !0,
                }))));
        let t = i.consumePointerDownSurfaceId();
        t === `composer`
          ? i.setSurfaceKeyboardInteraction(`composer`, !0)
          : t?.startsWith(`activity-slot-`) &&
              this.latestState != null &&
              !this.latestState.contentState.isNotificationStackExpanded &&
              this.latestState.contentState.activities.length > 1
            ? (i.setNotificationStackExpanded(!0),
              this.sendActionToRoot({ type: `open-notification-stack` }))
            : t?.startsWith(`activity-slot-`) && ((l = t), o(t, `mouseDown`));
        let n = i.consumePointerUpSurfaceId();
        l != null && n === l && (o(l, `mouseUp`), (l = null));
      }, u2)),
        this.pointerSyncTimer.unref(),
        this.prepareLatestState());
    }
    prepareLatestState() {
      this.latestState == null ||
        this.coordinator == null ||
        this.coordinator.prepareSurfaces(
          this.latestState.measurements,
          this.latestState.contentState,
        );
    }
    sendActionToRoot(e) {
      let t = this.overlayWindow;
      t == null ||
        t.isDestroyed() ||
        this.windowManager.sendMessageToWebContents(t.webContents, {
          type: `avatar-overlay-composition-action`,
          action: e,
        });
    }
    detachCoordinator() {
      (this.pointerSurfaceExitTimer != null &&
        (clearTimeout(this.pointerSurfaceExitTimer), (this.pointerSurfaceExitTimer = null)),
        this.pointerSurfaceHovered &&
          ((this.pointerSurfaceHovered = !1),
          this.sendActionToRoot({ type: `quick-chat-surface-hover-changed`, isHovered: !1 })),
        this.pointerSurfaceId != null &&
          ((this.pointerSurfaceId = null),
          this.sendActionToRoot({ type: `composition-pointer-surface-changed`, surfaceId: null })),
        this.pointerSyncTimer != null &&
          (clearInterval(this.pointerSyncTimer), (this.pointerSyncTimer = null)),
        this.scrollSyncTimer != null &&
          (clearInterval(this.scrollSyncTimer), (this.scrollSyncTimer = null)),
        this.scrollSettleTimer != null &&
          (clearTimeout(this.scrollSettleTimer), (this.scrollSettleTimer = null)),
        this.coordinator?.detach(),
        (this.coordinator = null),
        this.setNativeMaterialAttached(!1));
    }
    setNativeMaterialAttached(e) {
      this.nativeMaterialAttached !== e &&
        ((this.nativeMaterialAttached = e), this.onNativeMaterialStateChanged());
    }
  },
  m2 = class extends ig {
    constructor(e, t, n) {
      (super(), (this.id = e), (this.webContentsId = t), (this.host = n));
    }
    dispatchAction(e) {
      this.host.handleSurfaceAction(this.webContentsId, e);
    }
    getPreparation() {
      let e = this.host.getPreparation(this.id);
      if (e == null) throw Error(`Missing avatar overlay composition surface: ${this.id}`);
      return e;
    }
    surfaceReady(e) {
      if (e.id !== this.id) throw Error(`Unexpected avatar overlay composition surface: ${e.id}`);
      this.host.handleSurfaceReady(this.webContentsId, e);
    }
  },
  h2 = class {
    crossedDisplay = !1;
    hasMovementIntent = !1;
    hasMoved = !1;
    displayBounds;
    constructor(e, t, n, r) {
      ((this.cursorSource = e),
        (this.pointerAnchorX = t),
        (this.pointerAnchorY = n),
        (this.displayBounds = r));
    }
    getCursorPointForSource({ native: e, renderer: t }) {
      return this.cursorSource === `native` ? e : t;
    }
    recordMove(e) {
      ((this.hasMovementIntent = !0), (this.hasMoved = !0), this.updateDisplayBounds(e));
    }
    recordMovementIntent() {
      this.hasMovementIntent = !0;
    }
    updateDisplayBounds(e) {
      ((this.crossedDisplay ||= !g2(this.displayBounds, e)), (this.displayBounds = e));
    }
    shouldSuppressRendererThrow() {
      return this.cursorSource === `native` && (!this.hasMoved || this.crossedDisplay);
    }
  };
function g2(e, t) {
  return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
var _2 = 5e3,
  v2 = 250,
  y2 = 16,
  b2 = class {
    applying = !1;
    target = null;
    timer = null;
    constructor(e) {
      this.options = e;
    }
    clear() {
      (this.timer != null && (clearTimeout(this.timer), (this.timer = null)), (this.target = null));
    }
    position(e, t) {
      this.clear();
      let n = { bounds: { ...t }, retryUntilMs: Date.now() + (e.isVisible() ? v2 : _2), window: e };
      this.target = n;
      let r = this.apply(n);
      if (!this.options.retryEnabled) {
        this.clear();
        return;
      }
      r || this.schedule(n, y2);
    }
    handleWindowMove(e) {
      let t = this.target;
      return t == null || t.window !== e
        ? !1
        : this.applying
          ? !0
          : this.options.shouldCancelWindowMoveResync()
            ? (this.clear(), !1)
            : ((t.retryUntilMs = Date.now() + v2), this.schedule(t, 0), !0);
    }
    handleWindowShow(e) {
      let t = this.target;
      t != null && t.window === e && this.position(e, t.bounds);
    }
    apply(e) {
      this.applying = !0;
      try {
        return this.options.setPosition(e.bounds);
      } finally {
        this.applying = !1;
      }
    }
    schedule(e, t) {
      if (this.target !== e) return;
      this.timer != null && clearTimeout(this.timer);
      let n = e.retryUntilMs - Date.now();
      if (n <= 0) {
        this.timer = null;
        return;
      }
      this.timer = setTimeout(
        () => {
          if (
            ((this.timer = null),
            this.target !== e ||
              e.window !== this.options.getCurrentWindow() ||
              e.window.isDestroyed())
          ) {
            this.target === e && (this.target = null);
            return;
          }
          Date.now() >= e.retryUntilMs || this.apply(e) || this.schedule(e, y2);
        },
        Math.min(t, n),
      );
    }
  },
  x2 = `/avatar-overlay`,
  S2 = 24,
  C2 = 24,
  w2 = { width: 112, height: 121 },
  T2 = 80,
  E2 = 224,
  D2 = 192 / 208,
  O2 = { width: 276, height: 131 },
  k2 = { width: 351, height: 141 },
  A2 = 16,
  j2 = 0.88,
  M2 = 65,
  N2 = 900,
  P2 = 100,
  F2 = 1e4,
  I2 = 1e3,
  L2 = t.Gr(`avatar-overlay`),
  R2 = class {
    window = null;
    openingWindowPromise = null;
    anchor = B2({ x: 0, y: 0, ...p0 }, w2);
    displayBounds = null;
    displayId = null;
    detachedDisplay = null;
    detachedDisplayRestoreTimer = null;
    dragState = null;
    layout = null;
    layoutMode = `legacy`;
    hasDeferredLayout = !1;
    mascotSize = w2;
    momentumTimer = null;
    suppressNextRendererThrow = !1;
    movedWindowPersistTimer = null;
    mousePassthroughEnabled = !1;
    pendingRealtimeStart = null;
    pendingPresentation = null;
    placement = `top-end`;
    pointerInteractive = !1;
    realtimeStarter = null;
    realtimeStartInFlight = null;
    rendererReady = !1;
    mascotResizeState = null;
    ignoreMascotElementSizeUntilMs = 0;
    expectedMascotSizeAfterResize = null;
    pendingElementSizeRevision = null;
    isSuspended = !1;
    initialPresentationState = `waiting-for-layout`;
    windowStagedForNativePresentation = !1;
    resolutionKey = null;
    traySize = null;
    compositionHost;
    nativePositionController;
    nativeCompositionSupported;
    constructor(e, t, n = null) {
      ((this.windowManager = e),
        (this.globalState = t),
        (this.nativeCompositionSupported = n != null),
        (this.compositionHost = new p2(e, n, () => {
          let e = this.window;
          e != null &&
            (this.initialPresentationState === `waiting-for-native` &&
              (this.initialPresentationState = `ready`),
            this.applyLayout(e),
            this.showWindowIfReady(e));
        })),
        (this.nativePositionController = new b2({
          getCurrentWindow: () => this.window,
          retryEnabled: this.nativeCompositionSupported,
          setPosition: (e) => this.compositionHost.setOverlayWindowPosition(e),
          shouldCancelWindowMoveResync: () =>
            this.layoutMode === `native` && this.compositionHost.isPrimaryMouseButtonPressed(),
        })));
    }
    isOpen() {
      let e = this.window;
      return (
        e != null && !e.isDestroyed() && e.isVisible() && !this.windowStagedForNativePresentation
      );
    }
    getVisibleWebContents() {
      return this.isOpen() ? (this.window?.webContents ?? null) : null;
    }
    configureDevelopmentWindow({ mascotWidth: e, placement: t }) {
      let n = this.window;
      if (n == null || n.isDestroyed()) return;
      let r = this.getCurrentDisplay(),
        i = this.layoutMode === `native` ? r.workArea : r.bounds,
        a = $2(e);
      ((this.mascotSize = a),
        (this.placement = t),
        (this.anchor = {
          x: t.endsWith(`start`) ? i.x + S2 : i.x + i.width - a.width - S2,
          y: t.startsWith(`bottom`) ? i.y + S2 : i.y + i.height - a.height - S2,
          ...a,
        }),
        this.applyLayout(n, r));
    }
    getDevelopmentDiagnostics() {
      return {
        ...this.compositionHost.getDevelopmentDiagnostics(),
        layout: this.layout == null ? null : U2(this.layout),
      };
    }
    async toggle(e) {
      let t = this.window;
      if (t != null && !t.isDestroyed()) {
        (this.globalState.set(Ur, !1), t.close());
        return;
      }
      await this.open(e);
    }
    rpcFor(e) {
      return new z2(this, e);
    }
    async open(e) {
      let t = await this.ensureWindow();
      (this.globalState.set(Ur, !0), this.positionWindow(t, e), this.showWindowIfReady(t));
    }
    async restoreOpenState(e) {
      this.globalState.get(`electron-avatar-overlay-open`) === !0 && (await this.open(e));
    }
    handleRendererReady(e) {
      let t = this.window;
      t == null ||
        t.isDestroyed() ||
        t.webContents.id !== e ||
        ((this.rendererReady = !0),
        this.sendLayoutToRenderer(t),
        this.stageWindowForNativePresentation(t),
        this.showWindowIfReady(t));
    }
    registerRealtimeStarter(e, t, n) {
      let r = this.window;
      if (
        (r == null && this.openingWindowPromise == null) ||
        (r != null && (r.isDestroyed() || r.webContents.id !== e))
      )
        return;
      this.clearRealtimeStarter();
      let i = t.dup();
      ((this.realtimeStarter = { canCreateNewThread: n, start: i, webContentsId: e }),
        i.onRpcBroken(() => {
          this.realtimeStarter?.start === i && this.clearRealtimeStarter(e);
        }),
        this.startRealtimeIfReady());
    }
    unregisterRealtimeStarter(e) {
      this.isCurrentOverlayRenderer(e) && this.clearRealtimeStarter(e);
    }
    setPointerInteraction(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        ((this.pointerInteractive = t),
        this.movedWindowPersistTimer ?? this.applyPointerInteractivityPolicy());
    }
    setKeyboardInteraction(e, t) {
      let n = this.window;
      if (!(n == null || n.isDestroyed() || n.webContents.id !== e)) {
        if ((this.applyPointerInteractivityPolicy(), !t)) {
          n.setFocusable(!1);
          return;
        }
        (n.setFocusable(!0),
          n.show(),
          process.platform === `darwin` && a.app.focus({ steal: !0 }),
          n.focus(),
          n.webContents.focus(),
          this.windowManager.sendMessageToWebContents(n.webContents, {
            type: `avatar-overlay-keyboard-interaction-ready`,
          }));
      }
    }
    startDrag(e, t) {
      let n = this.window;
      if (n == null || n.isDestroyed() || n.webContents.id !== e) return;
      (this.cancelMomentum(),
        (this.suppressNextRendererThrow = !1),
        this.clearDetachedDisplayRestore());
      let r = this.getLayout(n);
      this.nativePositionController.clear();
      let i = V2(this.compositionHost.getCursorPosition()),
        o =
          t.pointerScreenX != null && t.pointerScreenY != null
            ? { x: t.pointerScreenX, y: t.pointerScreenY }
            : a.screen.getCursorScreenPoint(),
        s = i ?? o,
        c = t.pointerWindowX - r.mascot.left,
        l = t.pointerWindowY - r.mascot.top;
      this.dragState = new h2(
        i == null ? `renderer` : `native`,
        c,
        l,
        a.screen.getDisplayNearestPoint(s).bounds,
      );
    }
    moveDrag(e, t) {
      let n = this.window;
      if (n == null || n.isDestroyed() || n.webContents.id !== e || this.dragState == null) return;
      this.cancelMomentum();
      let r = this.dragState;
      r.recordMovementIntent();
      let i = a.screen.getCursorScreenPoint(),
        o = r.getCursorPointForSource({
          native: r.cursorSource === `native` ? V2(this.compositionHost.getCursorPosition()) : null,
          renderer: { x: t?.pointerScreenX ?? i.x, y: t?.pointerScreenY ?? i.y },
        });
      o != null && this.moveDragToPointer(n, o);
    }
    endDrag(e, t) {
      let n = this.window;
      if (n == null || n.isDestroyed() || n.webContents.id !== e) return;
      let r = this.dragState;
      if (r?.hasMovementIntent) {
        let e = a.screen.getCursorScreenPoint(),
          i = r.getCursorPointForSource({
            native:
              r.cursorSource === `native` ? V2(this.compositionHost.getCursorPosition()) : null,
            renderer: { x: t?.pointerScreenX ?? e.x, y: t?.pointerScreenY ?? e.y },
          });
        i != null && this.moveDragToPointer(n, i);
      }
      ((this.suppressNextRendererThrow = r?.shouldSuppressRendererThrow() ?? !1),
        (this.dragState = null),
        this.reclampWindowToVisibleDisplay({ shouldPersist: !0 }));
    }
    throwWithVelocity(e, t, n) {
      let r = this.window;
      if (
        r == null ||
        r.isDestroyed() ||
        r.webContents.id !== e ||
        !Number.isFinite(t) ||
        !Number.isFinite(n) ||
        (t === 0 && n === 0)
      )
        return;
      if (this.suppressNextRendererThrow) {
        this.suppressNextRendererThrow = !1;
        return;
      }
      this.cancelMomentum();
      let i = t,
        o = n,
        s = 0,
        c = () => {
          let t = this.window;
          if (t == null || t.isDestroyed() || t.webContents.id !== e) {
            this.cancelMomentum();
            return;
          }
          ((this.momentumTimer = null), (s += A2));
          let n = {
              ...this.anchor,
              x: this.anchor.x + (i * A2) / 1e3,
              y: this.anchor.y + (o * A2) / 1e3,
            },
            r = a.screen.getDisplayNearestPoint(Y2(n));
          if (
            ((this.anchor = n),
            this.applyLayout(t, r),
            this.anchor.x !== Math.round(n.x) && (i = 0),
            this.anchor.y !== Math.round(n.y) && (o = 0),
            (i *= j2),
            (o *= j2),
            s >= N2 || Math.hypot(i, o) < M2)
          ) {
            this.persistWindowBounds(t);
            return;
          }
          this.momentumTimer = setTimeout(c, A2);
        };
      this.momentumTimer = setTimeout(c, A2);
    }
    setCompositionState(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        (t != null &&
          this.layout != null &&
          !(0, TP.default)(t.contentState.layout, U2(this.layout))) ||
        (this.compositionHost.setState(e, t), t ?? this.sendLayoutToRenderer(n));
    }
    handleCompositionSurfaceAction(e, t) {
      this.compositionHost.handleSurfaceAction(e, t);
    }
    handleCompositionSurfaceReady(e, t) {
      this.compositionHost.handleSurfaceReady(e, t);
    }
    getCompositionSurfaceHost(e) {
      return this.compositionHost.getSurfaceHost(e);
    }
    startMascotResize(e, t) {
      let n = this.window;
      if (n == null || n.isDestroyed() || n.webContents.id !== e) return;
      (this.cancelMomentum(), this.clearDetachedDisplayRestore());
      let r = V2(this.compositionHost.getCursorPosition()),
        i = r ?? a.screen.getCursorScreenPoint(),
        o = $2(t ?? this.mascotSize.width);
      ((this.mascotResizeState = {
        cursorSource: r == null ? `electron` : `native`,
        displayBounds: a.screen.getDisplayNearestPoint(i).bounds,
        startAnchor: { ...this.anchor },
        startCursorScreenX: i.x,
        startMascotSize: o,
      }),
        (this.ignoreMascotElementSizeUntilMs = 0),
        (this.expectedMascotSizeAfterResize = null));
    }
    moveMascotResize(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.mascotResizeState == null ||
        this.resizeMascotToCurrentCursor(n, t);
    }
    endMascotResize(e, t) {
      let n = this.window;
      n == null ||
        n.isDestroyed() ||
        n.webContents.id !== e ||
        this.mascotResizeState == null ||
        (this.resizeMascotToCurrentCursor(n, t),
        (this.mascotResizeState = null),
        (this.ignoreMascotElementSizeUntilMs = Date.now() + I2),
        (this.expectedMascotSizeAfterResize = this.mascotSize),
        this.persistWindowBounds(n));
    }
    setElementSize(e, { elementSizeRevision: t, isTrayVisible: n, mascot: r, tray: i }) {
      let a = this.window;
      if (!(a == null || a.isDestroyed() || a.webContents.id !== e)) {
        if (
          (this.cancelMomentum(),
          (this.pendingElementSizeRevision = t ?? null),
          (this.layoutMode = n == null ? `native` : `legacy`),
          this.initialPresentationState === `waiting-for-layout` &&
            (this.initialPresentationState =
              this.layoutMode === `native` ? `waiting-for-native` : `ready`),
          this.mascotResizeState != null)
        ) {
          this.traySize = i;
          return;
        }
        if (this.expectedMascotSizeAfterResize != null)
          if (e4(r, this.expectedMascotSizeAfterResize))
            ((this.expectedMascotSizeAfterResize = null),
              (this.ignoreMascotElementSizeUntilMs = 0));
          else if (Date.now() < this.ignoreMascotElementSizeUntilMs) {
            this.traySize = i;
            return;
          } else
            ((this.expectedMascotSizeAfterResize = null),
              (this.ignoreMascotElementSizeUntilMs = 0));
        if (Date.now() < this.ignoreMascotElementSizeUntilMs && !e4(r, this.mascotSize)) {
          this.traySize = i;
          return;
        }
        if (
          ((this.ignoreMascotElementSizeUntilMs = 0),
          (this.mascotSize = r),
          (this.traySize = i),
          this.movedWindowPersistTimer != null)
        ) {
          this.hasDeferredLayout = !0;
          return;
        }
        (this.applyLatestElementSizes(a),
          this.stageWindowForNativePresentation(a),
          this.showWindowIfReady(a));
      }
    }
    async ensureWindow() {
      let e = this.window;
      return e != null && !e.isDestroyed()
        ? e
        : this.openingWindowPromise == null
          ? (this.clearRealtimeStarter(),
            (this.openingWindowPromise = this.createWindow().finally(() => {
              this.openingWindowPromise = null;
            })),
            this.openingWindowPromise)
          : this.openingWindowPromise;
    }
    async createWindow() {
      let e = await this.windowManager.createWindow({
        title: a.app.getName(),
        width: p0.width,
        height: p0.height,
        appearance: `avatarOverlay`,
        focusable: !1,
        show: !1,
        initialRoute: x2,
      });
      return (
        (this.window = e),
        this.compositionHost.setOverlayWindow(e),
        (this.rendererReady = this.windowManager.isWebContentsReady(e.webContents.id)),
        this.clearDetachedDisplayRestore(),
        (this.displayBounds = null),
        (this.displayId = null),
        (this.dragState = null),
        (this.hasDeferredLayout = !1),
        (this.layout = null),
        (this.layoutMode = `legacy`),
        (this.initialPresentationState = `waiting-for-layout`),
        (this.windowStagedForNativePresentation = !1),
        (this.mascotSize = w2),
        (this.mascotResizeState = null),
        (this.ignoreMascotElementSizeUntilMs = 0),
        (this.expectedMascotSizeAfterResize = null),
        (this.isSuspended = !1),
        (this.mousePassthroughEnabled = !1),
        (this.placement = `top-end`),
        (this.pointerInteractive = !1),
        (this.resolutionKey = null),
        (this.traySize = null),
        process.platform === `darwin`
          ? e.setVisibleOnAllWorkspaces(!0, {
              visibleOnFullScreen: !0,
              skipTransformProcessType: !0,
            })
          : e.setVisibleOnAllWorkspaces(!0),
        e.setAlwaysOnTop(!0, `floating`),
        e.setMenuBarVisibility(!1),
        this.addDisplayChangeListeners(),
        this.addPowerMonitorListeners(),
        e.once(`ready-to-show`, () => {
          this.showWindowIfReady(e);
        }),
        e.on(`close`, () => {
          this.window === e &&
            (this.cancelMomentum(),
            this.clearMovedWindowPersist(),
            this.nativePositionController.clear(),
            this.persistWindowBounds(e));
        }),
        e.on(`move`, () => {
          this.nativePositionController.handleWindowMove(e) ||
            (this.dragState ??
              (this.compositionHost.isNativeMaterialAttached() ||
                this.compositionHost.moveBackingCanvases(),
              this.layoutMode === `native` && this.rememberMovedWindow(e)));
        }),
        e.on(`will-move`, () => {
          (this.cancelMomentum(),
            this.clearDetachedDisplayRestore(),
            this.nativePositionController.clear());
        }),
        e.on(`show`, () => {
          this.nativePositionController.handleWindowShow(e);
        }),
        e.on(`closed`, () => {
          this.window === e &&
            (this.cancelMomentum(),
            this.clearMovedWindowPersist(),
            this.nativePositionController.clear(),
            (this.window = null),
            this.removeDisplayChangeListeners(),
            this.removePowerMonitorListeners(),
            (this.displayBounds = null),
            (this.displayId = null),
            (this.dragState = null),
            this.clearDetachedDisplayRestore(),
            (this.hasDeferredLayout = !1),
            (this.layout = null),
            (this.mascotResizeState = null),
            (this.ignoreMascotElementSizeUntilMs = 0),
            (this.expectedMascotSizeAfterResize = null),
            (this.isSuspended = !1),
            (this.pendingRealtimeStart = null),
            this.finishPendingPresentation(),
            this.clearRealtimeStarter(e.webContents.id),
            (this.realtimeStartInFlight = null),
            (this.rendererReady = !1),
            (this.windowStagedForNativePresentation = !1),
            (this.pointerInteractive = !1),
            (this.mousePassthroughEnabled = !1),
            this.compositionHost.setOverlayWindow(null),
            this.broadcastOpenState());
        }),
        e
      );
    }
    positionWindow(e, t) {
      if (e.isDestroyed()) return;
      let n = this.globalState.get(Wr),
        r = this.getInitialDisplay(e, t, n),
        i = K2(n, r);
      if (i != null) {
        (this.restoreBoundsForDisplay(i, r),
          this.applyLayout(e, r),
          (n?.byDisplayId == null || n.byResolution == null) && this.persistWindowBounds(e, r));
        return;
      }
      ((this.anchor = B2(this.layoutMode === `native` ? r.workArea : r.bounds, this.mascotSize)),
        this.applyLayout(e, r));
    }
    getInitialDisplay(e, t, n) {
      let r = n?.displayId;
      if (r != null) {
        let e = W2(r);
        if (e != null) return e;
      }
      let i = n?.displayBounds;
      if (i != null) {
        let e = a.screen.getDisplayMatching(i);
        if (t4(e.bounds, i)) return e;
      }
      let o = a.BrowserWindow.fromWebContents(t);
      return o == null || o.isDestroyed() || o === e
        ? a.screen.getDisplayNearestPoint(a.screen.getCursorScreenPoint())
        : a.screen.getDisplayMatching(o.getBounds());
    }
    restoreBoundsForDisplay(e, t) {
      let n = q2(e, t.bounds);
      ((this.anchor = n),
        (this.mascotSize = { width: n.width, height: n.height }),
        (this.placement = e.placement ?? this.placement));
    }
    resetToDefaultAnchor(e) {
      this.anchor = B2(this.layoutMode === `native` ? e.workArea : e.bounds, this.mascotSize);
    }
    switchToDisplay(e, t, n = !1) {
      let r = K2(this.globalState.get(Wr), t);
      (r == null ? this.resetToDefaultAnchor(t) : this.restoreBoundsForDisplay(r, t),
        this.applyLayout(e, t, n));
    }
    getCurrentDisplay() {
      if (this.displayId != null) {
        let e = W2(this.displayId);
        if (e != null) return e;
      }
      return a.screen.getDisplayNearestPoint(Y2(this.anchor));
    }
    addDisplayChangeListeners() {
      (a.screen.on(`display-added`, this.handleDisplayChanged),
        a.screen.on(`display-removed`, this.handleDisplayChanged),
        a.screen.on(`display-metrics-changed`, this.handleDisplayChanged));
    }
    removeDisplayChangeListeners() {
      (a.screen.off(`display-added`, this.handleDisplayChanged),
        a.screen.off(`display-removed`, this.handleDisplayChanged),
        a.screen.off(`display-metrics-changed`, this.handleDisplayChanged));
    }
    addPowerMonitorListeners() {
      (a.powerMonitor.on(`suspend`, this.handleSuspend),
        a.powerMonitor.on(`resume`, this.handleResume));
    }
    removePowerMonitorListeners() {
      (a.powerMonitor.off(`suspend`, this.handleSuspend),
        a.powerMonitor.off(`resume`, this.handleResume));
    }
    handleDisplayChanged = () => {
      if (this.isSuspended) return;
      if (this.dragState != null) {
        let e =
          this.dragState.cursorSource === `native`
            ? V2(this.compositionHost.getCursorPosition())
            : a.screen.getCursorScreenPoint();
        e != null && this.dragState.updateDisplayBounds(a.screen.getDisplayNearestPoint(e).bounds);
        return;
      }
      this.cancelMomentum();
      let e = this.window;
      if (e == null || e.isDestroyed() || this.restoreDetachedDisplay(e)) return;
      this.rememberDetachedDisplay();
      let t = this.getCurrentDisplay();
      if (
        H2(t.bounds) !== this.resolutionKey ||
        this.displayBounds == null ||
        !t4(t.bounds, this.displayBounds)
      ) {
        (this.switchToDisplay(e, t, !0), this.persistWindowBounds(e, t));
        return;
      }
      this.reclampWindowToVisibleDisplay({ shouldPersist: !0 });
    };
    handleSuspend = () => {
      let e = this.window;
      if (e == null || e.isDestroyed() || this.displayBounds == null) return;
      (this.cancelMomentum(), (this.isSuspended = !0));
      let t = this.getCurrentDisplay();
      this.persistWindowBounds(e, t);
    };
    handleResume = () => {
      this.isSuspended = !1;
      let e = this.window;
      if (e == null || e.isDestroyed() || this.restoreDetachedDisplay(e)) return;
      let t = this.displayId;
      if (t == null) return;
      let n = W2(t);
      if (n == null) {
        this.rememberDetachedDisplay();
        let t = this.getCurrentDisplay();
        (this.switchToDisplay(e, t, !0), this.persistWindowBounds(e, t));
        return;
      }
      (this.switchToDisplay(e, n, !0), this.persistWindowBounds(e, n));
    };
    rememberDetachedDisplay() {
      let e = this.displayId,
        t = this.displayBounds;
      e == null ||
        t == null ||
        W2(e) != null ||
        this.detachedDisplay?.id === e ||
        ((this.detachedDisplay = { bounds: t, id: e }),
        this.detachedDisplayRestoreTimer != null && clearTimeout(this.detachedDisplayRestoreTimer),
        (this.detachedDisplayRestoreTimer = setTimeout(() => {
          ((this.detachedDisplay = null), (this.detachedDisplayRestoreTimer = null));
        }, F2)));
    }
    restoreDetachedDisplay(e) {
      let t = this.detachedDisplay;
      if (t == null) return !1;
      let n = W2(t.id) ?? G2(t.bounds);
      if (n == null) return !1;
      let r = this.globalState.get(Wr)?.byDisplayId?.[String(t.id)];
      return (
        this.clearDetachedDisplayRestore(),
        r == null
          ? this.switchToDisplay(e, n, !0)
          : (this.restoreBoundsForDisplay(r, n), this.applyLayout(e, n, !0)),
        this.persistWindowBounds(e, n),
        !0
      );
    }
    clearDetachedDisplayRestore() {
      (this.detachedDisplayRestoreTimer != null &&
        (clearTimeout(this.detachedDisplayRestoreTimer), (this.detachedDisplayRestoreTimer = null)),
        (this.detachedDisplay = null));
    }
    reclampWindowToVisibleDisplay({ shouldPersist: e }) {
      let t = this.window;
      if (t == null || t.isDestroyed()) return;
      let n = this.getCurrentDisplay();
      (this.applyLayout(t, n), e && this.persistWindowBounds(t, n));
    }
    moveDragToPointer(e, t) {
      let n = this.dragState;
      if (n == null) return;
      let r = J2(t, n.displayBounds),
        i = a.screen.getDisplayMatching(r);
      (n.recordMove(i.bounds),
        (this.anchor = { ...this.anchor, x: t.x - n.pointerAnchorX, y: t.y - n.pointerAnchorY }),
        this.applyLayout(e, i, !1, !1));
    }
    resizeMascotToCurrentCursor(e, t) {
      let n = this.mascotResizeState;
      if (n == null) return;
      let r =
        n.cursorSource === `native`
          ? V2(this.compositionHost.getCursorPosition())
          : a.screen.getCursorScreenPoint();
      if (r == null && t == null) return;
      let i =
        r == null
          ? a.screen.getDisplayMatching(n.displayBounds)
          : a.screen.getDisplayMatching(J2(r, n.displayBounds));
      n.displayBounds = i.bounds;
      let o = $2(
        t ?? n.startMascotSize.width + (r?.x ?? n.startCursorScreenX) - n.startCursorScreenX,
      );
      ((this.mascotSize = o), (this.anchor = { ...n.startAnchor, ...o }), this.applyLayout(e, i));
    }
    cancelMomentum() {
      this.momentumTimer != null && (clearTimeout(this.momentumTimer), (this.momentumTimer = null));
    }
    clearMovedWindowPersist() {
      this.movedWindowPersistTimer != null &&
        (clearTimeout(this.movedWindowPersistTimer), (this.movedWindowPersistTimer = null));
    }
    rememberMovedWindow(e) {
      if (e.isDestroyed() || this.layout == null) return;
      let t = e.getContentBounds();
      this.anchor = {
        ...this.anchor,
        x: t.x + this.layout.mascot.left,
        y: t.y + this.layout.mascot.top,
      };
      let n = a.screen.getDisplayNearestPoint(Y2(this.anchor));
      ((this.displayBounds = n.bounds),
        (this.displayId = n.id),
        (this.resolutionKey = H2(n.bounds)),
        this.compositionHost.isPrimaryMouseButtonPressed() && this.clearDetachedDisplayRestore(),
        this.clearMovedWindowPersist(),
        (this.movedWindowPersistTimer = setTimeout(() => {
          if (
            ((this.movedWindowPersistTimer = null),
            this.compositionHost.isPrimaryMouseButtonPressed())
          ) {
            this.rememberMovedWindow(e);
            return;
          }
          (this.hasDeferredLayout &&
            ((this.hasDeferredLayout = !1), this.applyLatestElementSizes(e)),
            this.applyPointerInteractivityPolicy(),
            this.reclampWindowToVisibleDisplay({ shouldPersist: !0 }));
        }, P2)));
    }
    applyLatestElementSizes(e) {
      this.anchor = {
        ...this.anchor,
        width: this.mascotSize.width,
        height: this.mascotSize.height,
      };
      let t = this.pendingElementSizeRevision;
      ((this.pendingElementSizeRevision = null),
        this.applyLayout(e, this.getCurrentDisplay(), !1, !0, t));
    }
    applyLayout(e, t = this.getCurrentDisplay(), n = !1, r = !0, i = null) {
      if (e.isDestroyed()) return;
      let a = t.bounds;
      ((this.displayId = t.id), (this.resolutionKey = H2(a)), (this.displayBounds = a));
      let o = b0({
        anchor: this.anchor,
        displayBounds: this.layoutMode === `native` ? t.workArea : t.bounds,
        mode: this.layoutMode,
        mascotSize: this.mascotSize,
        nativeMaterialAttached: this.compositionHost.isNativeMaterialAttached(),
        previousPlacement: this.placement,
        traySize: this.traySize ?? (this.layoutMode === `native` ? k2 : O2),
      });
      ((this.anchor = o.anchor),
        (this.layout = o),
        (this.placement = o.placement),
        this.setWindowBounds(e, o.windowBounds, n, r),
        this.sendLayoutToRenderer(e, i));
    }
    getLayout(e) {
      if ((this.layout ?? this.applyLayout(e), this.layout == null))
        throw Error(`Expected avatar overlay layout`);
      return this.layout;
    }
    setWindowBounds(e, t, n, r) {
      if (e.isDestroyed()) return;
      let i = e.getContentBounds();
      if (t4(i, t)) {
        this.nativePositionController.position(e, t);
        return;
      }
      let a = i.width === t.width && i.height === t.height;
      (a && !r && this.compositionHost.prepareDragFollowForOverlayMove(t),
        e.setContentBounds(t, n && !this.nativeCompositionSupported),
        this.nativePositionController.position(e, t),
        (r || !a) && this.compositionHost.moveBackingCanvases());
    }
    sendLayoutToRenderer(e, t = null) {
      e.isDestroyed() ||
        this.layout == null ||
        !this.rendererReady ||
        this.windowManager.sendMessageToWebContents(e.webContents, {
          type: `avatar-overlay-layout-changed`,
          ...(t == null ? {} : { elementSizeRevision: t }),
          layout: U2(this.layout),
          nativeMaterialAttached: this.compositionHost.isNativeMaterialAttached(),
        });
    }
    persistWindowBounds(e, t = this.getCurrentDisplay()) {
      if (e.isDestroyed()) return;
      let n = t.bounds,
        r = {
          ...e.getContentBounds(),
          anchor: this.anchor,
          displayBounds: n,
          displayId: t.id,
          mascot: this.layout?.mascot,
          placement: this.placement,
          tray: this.layout?.tray,
        },
        i = this.globalState.get(Wr),
        a = H2(n);
      ((this.displayId = t.id),
        (this.resolutionKey = a),
        this.globalState.set(Wr, {
          ...r,
          byDisplayId: { ...i?.byDisplayId, [String(t.id)]: r },
          byResolution: { ...i?.byResolution, [a]: r },
        }));
    }
    showWindow(e) {
      if (e.isDestroyed()) return;
      let t = this.isOpen();
      ((this.windowStagedForNativePresentation &&= (e.setOpacity(1), !1)),
        e.moveTop(),
        e.showInactive(),
        !t && this.isOpen() && (this.finishPendingPresentation(), this.broadcastOpenState()));
    }
    showWindowIfReady(e) {
      !this.rendererReady ||
        this.initialPresentationState !== `ready` ||
        (this.showWindow(e), this.applyPointerInteractivityPolicy());
    }
    stageWindowForNativePresentation(e) {
      e.isDestroyed() ||
        !this.rendererReady ||
        this.initialPresentationState !== `waiting-for-native` ||
        this.windowStagedForNativePresentation ||
        ((this.windowStagedForNativePresentation = !0),
        e.setOpacity(0),
        e.moveTop(),
        e.showInactive(),
        this.applyPointerInteractivityPolicy());
    }
    broadcastOpenState() {
      this.windowManager.sendMessageToAllRegisteredWindows({
        type: `avatar-overlay-open-state-changed`,
        isOpen: this.isOpen(),
      });
    }
    startRealtimeIfReady() {
      let e = this.window,
        t = this.pendingRealtimeStart,
        n = this.realtimeStarter;
      if (
        t == null ||
        e == null ||
        e.isDestroyed() ||
        n == null ||
        n.webContentsId !== e.webContents.id ||
        (t.source !== `composer_button_existing_thread` && !n.canCreateNewThread)
      )
        return;
      this.pendingRealtimeStart = null;
      let r = n.start(t).catch((e) => {
        L2().error(`Failed to start realtime in the avatar overlay`, {
          safe: {},
          sensitive: { error: e },
        });
      });
      ((this.realtimeStartInFlight = r),
        r.finally(() => {
          this.realtimeStartInFlight === r && (this.realtimeStartInFlight = null);
        }));
    }
    async requestRealtimeStart(e, t) {
      if (
        !(t.source === `composer_button_existing_thread` && !t.isRealtimeVoiceThread) &&
        this.realtimeStartInFlight == null &&
        !(
          this.pendingRealtimeStart != null &&
          (t.source !== `composer_button_existing_thread` ||
            this.pendingRealtimeStart.source === `composer_button_existing_thread`)
        )
      ) {
        this.pendingRealtimeStart = t;
        try {
          await this.open(e);
        } catch (e) {
          throw ((this.pendingRealtimeStart = null), e);
        }
        (this.startRealtimeIfReady(), await this.waitForPresentation());
      }
    }
    waitForPresentation() {
      return this.isOpen()
        ? Promise.resolve()
        : ((this.pendingPresentation ??= t.Ia()), this.pendingPresentation.promise);
    }
    finishPendingPresentation() {
      (this.pendingPresentation?.resolve(void 0), (this.pendingPresentation = null));
    }
    clearRealtimeStarter(e) {
      let t = this.realtimeStarter;
      t == null ||
        (e != null && t.webContentsId !== e) ||
        ((this.realtimeStarter = null), t.start[Symbol.dispose]());
    }
    isCurrentOverlayRenderer(e) {
      let t = this.window;
      return t != null && !t.isDestroyed() && t.webContents.id === e;
    }
    applyPointerInteractivityPolicy() {
      let e = this.window;
      if (e == null || e.isDestroyed()) {
        this.mousePassthroughEnabled = !1;
        return;
      }
      let t = !this.pointerInteractive;
      if (this.mousePassthroughEnabled !== t) {
        if (((this.mousePassthroughEnabled = t), t)) {
          e.setIgnoreMouseEvents(!0, { forward: !0 });
          return;
        }
        (e.setIgnoreMouseEvents(!1), this.refreshCursorAtCurrentMousePosition(e));
      }
    }
    refreshCursorAtCurrentMousePosition(e) {
      if (e.isDestroyed()) return;
      let t = V2(this.compositionHost.getCursorPosition()) ?? a.screen.getCursorScreenPoint(),
        n = e.getContentBounds(),
        r = t.x - n.x,
        i = t.y - n.y;
      r < 0 ||
        i < 0 ||
        r > n.width ||
        i > n.height ||
        e.webContents.sendInputEvent({ type: `mouseMove`, x: r, y: i, movementX: 0, movementY: 0 });
    }
  },
  z2 = class extends ig {
    constructor(e, t) {
      (super(), (this.manager = e), (this.origin = t));
    }
    registerRealtimeStarter(e, t) {
      this.manager.registerRealtimeStarter(this.origin.id, e, t);
    }
    requestRealtimeStart(e) {
      return this.manager.requestRealtimeStart(this.origin, e);
    }
    unregisterRealtimeStarter() {
      this.manager.unregisterRealtimeStarter(this.origin.id);
    }
  };
function B2(e, t) {
  return {
    x: e.x + e.width - t.width - S2,
    y: e.y + e.height - t.height - S2,
    width: t.width,
    height: t.height,
  };
}
function V2(e) {
  return e == null ? null : { x: e.pointerScreenX, y: e.pointerScreenY };
}
function H2(e) {
  return `${e.width}x${e.height}`;
}
function U2(e) {
  return { mascot: e.mascot, placement: e.placement, tray: e.tray, viewport: e.viewport };
}
function W2(e) {
  return a.screen.getAllDisplays().find((t) => t.id === e) ?? null;
}
function G2(e) {
  let t = a.screen.getDisplayMatching(e);
  return t4(t.bounds, e) ? t : null;
}
function K2(e, t) {
  return e == null
    ? null
    : (e.byDisplayId?.[String(t.id)] ??
        e.byResolution?.[H2(t.bounds)] ??
        (e.byDisplayId == null && e.byResolution == null ? e : null));
}
function q2(e, t) {
  let n = e.mascot,
    r =
      e.anchor ??
      (n == null
        ? { x: e.x, y: e.y, width: e.width, height: e.height }
        : { x: e.x + n.left, y: e.y + n.top, width: n.width, height: n.height }),
    i = e.displayBounds;
  return i == null ? r : { ...r, x: r.x + t.x - i.x, y: r.y + t.y - i.y };
}
function J2(e, t) {
  let n = a.screen.getDisplayNearestPoint(e).bounds;
  return t4(n, t) ? n : X2(e, Z2(t, C2)) ? t : n;
}
function Y2(e) {
  return { x: e.x + e.width / 2, y: e.y + e.height / 2 };
}
function X2(e, t) {
  return e.x >= t.x && e.x < t.x + t.width && e.y >= t.y && e.y < t.y + t.height;
}
function Z2(e, t) {
  return { x: e.x - t, y: e.y - t, width: e.width + t * 2, height: e.height + t * 2 };
}
function Q2(e) {
  return Number.isFinite(e) ? Math.round(Math.min(E2, Math.max(T2, e))) : w2.width;
}
function $2(e) {
  let t = Q2(e);
  return { width: t, height: Math.ceil(t / D2) };
}
function e4(e, t) {
  return e.width === t.width && e.height === t.height;
}
function t4(e, t) {
  return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
var n4 = `transcription-history.jsonl`,
  r4 = t.$s({ id: t.nc(), createdAtMs: t.Qs(), text: t.nc() });
function i4() {
  let e = o4();
  if (!(0, l.existsSync)(e)) return [];
  try {
    let t = [];
    for (let n of (0, l.readFileSync)(e, `utf8`).split(/\r?\n/)) {
      let e = n.trim();
      if (e.length !== 0)
        try {
          let n = r4.safeParse(JSON.parse(e));
          n.success && t.push(n.data);
        } catch {}
    }
    return t.slice(-10).reverse();
  } catch (e) {
    return (
      t
        .Ur()
        .warning(`Failed to read global dictation history`, { safe: {}, sensitive: { error: e } }),
      []
    );
  }
}
function a4(e) {
  try {
    let t = o4();
    ((0, l.mkdirSync)((0, s.dirname)(t), { recursive: !0 }),
      (0, l.appendFileSync)(t, `${JSON.stringify(e)}\n`, `utf8`));
  } catch (e) {
    t.Ur().warning(`Failed to write global dictation history`, {
      safe: {},
      sensitive: { error: e },
    });
  }
}
function o4() {
  return (0, s.join)(t.Mr(), n4);
}
var s4 = 1e3,
  c4 = `codex-global-dictation-window.lock`,
  l4 = `owner.json`,
  u4 = 5e3,
  d4 = t.$s({ pid: t.Qs().int().positive(), token: t.nc().min(1) });
function f4({ force: e, lockPath: t = (0, s.join)((0, o.tmpdir)(), c4), onLost: n }) {
  let r = { pid: process.pid, token: (0, u.randomUUID)() };
  if (!p4(t, e)) return null;
  m4(t, r);
  let i = setInterval(() => {
    g4(t, r.token) || (clearInterval(i), n());
  }, s4);
  return (
    i.unref?.(),
    {
      dispose: () => {
        (clearInterval(i), h4(t, r.token));
      },
    }
  );
}
function p4(e, t) {
  try {
    return ((0, l.mkdirSync)(e, { recursive: !1 }), !0);
  } catch (e) {
    if (!S4(e)) throw e;
  }
  if (!t && !_4(e)) return !1;
  (0, l.rmSync)(e, { force: !0, recursive: !0 });
  try {
    return ((0, l.mkdirSync)(e, { recursive: !1 }), !0);
  } catch (e) {
    if (S4(e)) return !1;
    throw e;
  }
}
function m4(e, t) {
  (0, l.writeFileSync)(y4(e), `${JSON.stringify(t, null, 2)}\n`);
}
function h4(e, t) {
  g4(e, t) && (0, l.rmSync)(e, { force: !0, recursive: !0 });
}
function g4(e, t) {
  return v4(e)?.token === t;
}
function _4(e) {
  let t = v4(e);
  return t == null ? !b4(e) : !x4(t.pid);
}
function v4(e) {
  try {
    let t = (0, l.readFileSync)(y4(e), `utf8`);
    return d4.parse(JSON.parse(t));
  } catch {
    return null;
  }
}
function y4(e) {
  return (0, s.join)(e, l4);
}
function b4(e) {
  try {
    let t = (0, l.statSync)(e);
    return Date.now() - t.mtimeMs < u4;
  } catch {
    return !1;
  }
}
function x4(e) {
  try {
    return (process.kill(e, 0), !0);
  } catch (e) {
    return !C4(e);
  }
}
function S4(e) {
  return w4(e) === `EEXIST`;
}
function C4(e) {
  return w4(e) === `ESRCH`;
}
function w4(e) {
  return typeof e == `object` && e && `code` in e ? e.code : null;
}
var T4 = 150,
  E4 = 700,
  D4 = (0, c.promisify)(m.execFile);
async function O4(e) {
  let t = k4();
  a.clipboard.writeText(e);
  try {
    (await (0, _.setTimeout)(T4), await M4(), await (0, _.setTimeout)(E4));
  } finally {
    A4(t, e);
  }
}
function k4() {
  return {
    data: {
      text: a.clipboard.readText(),
      html: a.clipboard.readHTML(),
      rtf: a.clipboard.readRTF(),
      image: a.clipboard.readImage(),
      bookmark: a.clipboard.readBookmark().title,
    },
    formats: a.clipboard
      .availableFormats()
      .map((e) => ({ format: e, data: a.clipboard.readBuffer(e) })),
  };
}
function A4(e, t) {
  return a.clipboard.readText() === t ? (j4(e), !0) : !1;
}
function j4(e) {
  a.clipboard.write(e.data);
  let t = new Set(a.clipboard.availableFormats());
  for (let { format: n, data: r } of e.formats) t.has(n) || a.clipboard.writeBuffer(n, r);
}
async function M4() {
  switch (process.platform) {
    case `darwin`:
      if (!a.systemPreferences.isTrustedAccessibilityClient(!1))
        throw Error(`Accessibility permission is required to paste global dictation.`);
      await D4(`/usr/bin/osascript`, [
        `-e`,
        `tell application "System Events" to keystroke "v" using command down`,
      ]);
      return;
    case `win32`:
      await D4(`powershell.exe`, [
        `-STA`,
        `-NoProfile`,
        `-NonInteractive`,
        `-ExecutionPolicy`,
        `Bypass`,
        `-Command`,
        `Add-Type -AssemblyName System.Windows.Forms; [System.Windows.Forms.SendKeys]::SendWait('^v')`,
      ]);
      return;
    case `aix`:
    case `android`:
    case `cygwin`:
    case `freebsd`:
    case `haiku`:
    case `linux`:
    case `netbsd`:
    case `openbsd`:
    case `sunos`:
      throw Error(`Global dictation paste is not supported on this OS.`);
  }
}
var N4 = 16,
  P4 = { width: 720, height: 84 },
  F4 = class {
    recorderWindowId = null;
    recorderWindowPromise = null;
    rendererReadyPromise = null;
    rendererReadyResolve = null;
    rendererReadyWebContentsId = null;
    constructor(e) {
      this.windowManager = e;
    }
    prewarm() {
      this.ensureWindow();
    }
    async showIdle(e, t) {
      let n = await this.ensureWindow();
      return n == null || (await this.waitForRenderer(n), n.isDestroyed())
        ? !1
        : (this.positionWindow(n),
          n.setAlwaysOnTop(!0, `floating`),
          this.windowManager.sendMessageToWebContents(n.webContents, {
            type: `global-dictation-idle`,
            configuredHotkey: e,
            configuredToggleHotkey: t,
          }),
          n.showInactive(),
          this.applyPointerInteractivity(n, !0),
          !0);
    }
    async showAndStart(e) {
      let t = await this.ensureWindow();
      return t == null || (await this.waitForRenderer(t), t.isDestroyed())
        ? !1
        : (this.positionWindow(t),
          t.setAlwaysOnTop(!0, `floating`),
          this.windowManager.sendMessageToWebContents(t.webContents, {
            type: `global-dictation-start`,
            sessionId: e,
          }),
          t.showInactive(),
          !0);
    }
    sendStop(e) {
      let t = this.getWindow();
      if (t != null) {
        if (this.rendererReadyWebContentsId === t.webContents.id) {
          this.windowManager.sendMessageToWebContents(t.webContents, {
            type: `global-dictation-stop`,
            sessionId: e,
          });
          return;
        }
        this.waitForRenderer(t).then(() => {
          t.isDestroyed() ||
            this.windowManager.sendMessageToWebContents(t.webContents, {
              type: `global-dictation-stop`,
              sessionId: e,
            });
        });
      }
    }
    setPointerInteractive(e, t) {
      let n = this.getWindow();
      n == null || n.webContents.id !== e || this.applyPointerInteractivity(n, t);
    }
    setRendererReady(e) {
      let t = this.getWindow();
      t == null ||
        t.webContents.id !== e ||
        ((this.rendererReadyWebContentsId = e),
        this.rendererReadyResolve?.(),
        (this.rendererReadyResolve = null));
    }
    hide() {
      let e = this.getWindow();
      if (e != null) {
        e.hide();
        return;
      }
      this.recorderWindowPromise?.then((e) => {
        e == null || e.isDestroyed() || e.hide();
      });
    }
    close() {
      let e = this.getWindow(),
        t = this.recorderWindowPromise;
      if (
        ((this.recorderWindowId = null),
        (this.recorderWindowPromise = null),
        (this.rendererReadyWebContentsId = null),
        this.rendererReadyResolve?.(),
        (this.rendererReadyResolve = null),
        (this.rendererReadyPromise = null),
        e != null)
      ) {
        e.close();
        return;
      }
      t?.then((e) => {
        e == null || e.isDestroyed() || e.close();
      });
    }
    getFocusedInAppWindow() {
      let e = a.BrowserWindow.getFocusedWindow();
      return e == null || e.isDestroyed() || e.id === this.recorderWindowId ? null : e;
    }
    positionWindow(e) {
      let t = a.screen.getDisplayNearestPoint(a.screen.getCursorScreenPoint()).workArea;
      e.setBounds(
        {
          ...P4,
          x: Math.round(t.x + (t.width - P4.width) / 2),
          y: Math.max(t.y, t.y + t.height - P4.height - N4),
        },
        !1,
      );
    }
    async ensureWindow() {
      if (this.recorderWindowPromise == null) {
        let e = this.getWindow();
        if (e != null) return e;
        this.recorderWindowPromise = this.createWindow();
      }
      let e = await this.recorderWindowPromise;
      return e == null
        ? ((this.recorderWindowPromise = null), null)
        : e.isDestroyed()
          ? ((this.recorderWindowPromise = null), this.ensureWindow())
          : e;
    }
    async createWindow() {
      try {
        let e = await this.windowManager.createWindow({
          title: `Dictation`,
          ...P4,
          appearance: `globalDictation`,
          show: !1,
          initialRoute: t.Fi,
          focusable: !1,
        });
        return (
          (this.recorderWindowId = e.id),
          (this.rendererReadyPromise = new Promise((e) => {
            this.rendererReadyResolve = e;
          })),
          e.setAlwaysOnTop(!0, `floating`),
          this.positionWindow(e),
          this.applyPointerInteractivity(e, !1),
          e
        );
      } catch (e) {
        return (
          t
            .Ur()
            .warning(`Failed to create global dictation window`, {
              safe: {},
              sensitive: { error: e },
            }),
          null
        );
      }
    }
    async waitForRenderer(e) {
      this.rendererReadyWebContentsId !== e.webContents.id && (await this.rendererReadyPromise);
    }
    applyPointerInteractivity(e, t) {
      if (t) {
        e.setIgnoreMouseEvents(!1);
        return;
      }
      e.setIgnoreMouseEvents(!0, { forward: !0 });
    }
    getWindow() {
      if (this.recorderWindowId == null) return null;
      for (let e of a.BrowserWindow.getAllWindows())
        if (e.id === this.recorderWindowId && !e.isDestroyed()) return e;
      return null;
    }
  },
  I4 = {
    "global-dictation-enabled-changed": !0,
    "global-dictation-force-lock-changed": !0,
    "global-dictation-pet-renderer-ready": !0,
    "global-dictation-renderer-ready": !0,
    "global-dictation-stop-requested": !0,
    "global-dictation-pointer-interaction-changed": !0,
    "global-dictation-recording-stopped": !0,
    "global-dictation-dismiss": !0,
    "global-dictation-close": !0,
    "global-dictation-completed": !0,
    "global-dictation-record-history-item": !0,
    "global-dictation-failed": !0,
    "global-dictation-in-app-started": !0,
  },
  L4 = 150,
  R4 = class {
    isGateEnabled = !1;
    forceGlobalDictationLock;
    configuredHotkey;
    configuredToggleHotkey;
    keepVisible;
    registeredHotkey = null;
    registeredHotkeyRegistration = null;
    registeredToggleHotkey = null;
    registeredToggleHotkeyRegistration = null;
    activeSession = null;
    activeHotkeyReleaseWatcher = null;
    pendingInAppDictationStart = null;
    lifecycleLock = null;
    nativePetRendererWebContentsIds = new Set();
    windowController;
    history = [];
    hotkeyController = {
      getState: () => this.getHotkeyState(),
      setHotkey: (e) => this.setHotkey(e),
      setKeepVisible: (e) => this.setKeepVisible(e),
      setToggleHotkey: (e) => this.setToggleHotkey(e),
      syncCommandKeybindings: (e) => {
        this.syncCommandKeybindings(e);
      },
      getHistory: () => [...this.history],
      copyHistoryItem: (e) => this.copyHistoryItem(e),
    };
    constructor(e) {
      ((this.options = e),
        (this.windowController = new F4(e.windowManager)),
        (this.forceGlobalDictationLock =
          e.canForceGlobalDictationLock === !0 &&
          (e.forceGlobalDictationLock === !0 ||
            e.globalState.get(t.Fo.GLOBAL_DICTATION_FORCE_LOCK_DEBUG_ENABLED) === !0)),
        this.history.push(...i4()));
      let n = e.globalState.get(`globalDictationHotkey`),
        r = n != null && qO(n) == null ? n : null,
        i = e.globalState.get(`globalDictationToggleHotkey`),
        a = i != null && qO(i) == null ? i : null,
        o = pM(`globalDictationHold`),
        s = pM(`globalDictationToggle`);
      ((this.configuredHotkey = _M({ keymapHotkey: o, legacyHotkey: r })),
        (this.configuredToggleHotkey = _M({ keymapHotkey: s, legacyHotkey: a })),
        o.hasBinding && e.globalState.set(`globalDictationHotkey`, this.configuredHotkey ?? void 0),
        s.hasBinding &&
          e.globalState.set(`globalDictationToggleHotkey`, this.configuredToggleHotkey ?? void 0),
        z4(this.configuredHotkey, this.configuredToggleHotkey) &&
          ((this.configuredToggleHotkey = null),
          e.globalState.set(`globalDictationToggleHotkey`, void 0)),
        (this.keepVisible =
          e.globalState.getStored(t.Fo.GLOBAL_DICTATION_KEEP_VISIBLE) ??
          this.hasConfiguredHotkey()),
        this.applyLifecycleWithWarning(`startup`));
    }
    getHotkeyController() {
      return this.hotkeyController;
    }
    async handleMessage(e, t) {
      if (!H4(t)) return !1;
      if (t.type === `global-dictation-enabled-changed`)
        return (this.setGateEnabled(t.enabled), !0);
      if (t.type === `global-dictation-force-lock-changed`)
        return (this.setForceGlobalDictationLock(t.enabled), !0);
      if (t.type === `global-dictation-pet-renderer-ready`)
        return (this.setNativePetRendererReady(e.sender.id, t.ready), !0);
      if (t.type === `global-dictation-renderer-ready`)
        return (this.windowController.setRendererReady(e.sender.id), !0);
      if (!this.isGateEnabled) return !0;
      let n = this.activeSession,
        r = n?.id ?? null;
      switch (t.type) {
        case `global-dictation-pointer-interaction-changed`:
          return (this.windowController.setPointerInteractive(e.sender.id, t.isInteractive), !0);
        case `global-dictation-stop-requested`:
          return (
            t.sessionId === r &&
              (n?.type === `overlay` || (n?.type === `pet` && n.webContentsId === e.sender.id)) &&
              this.stopRecording(t.sessionId),
            !0
          );
        case `global-dictation-recording-stopped`:
          return (t.sessionId === r && this.clearHotkeyReleaseWatcher(), !0);
        case `global-dictation-dismiss`:
          return (
            t.sessionId === r &&
              (this.clearActiveSession(), this.clearHotkeyReleaseWatcher(), this.showIdleOrHide()),
            !0
          );
        case `global-dictation-close`:
          return (
            t.sessionId === r &&
              (this.clearActiveSession(),
              this.clearHotkeyReleaseWatcher(),
              this.windowController.close()),
            !0
          );
        case `global-dictation-completed`:
          return (t.sessionId === r && (await this.handleTranscript(t.sessionId, t.text)), !0);
        case `global-dictation-record-history-item`:
          return (this.recordHistoryText((0, u.randomUUID)(), t.text), !0);
        case `global-dictation-failed`:
          return t.sessionId !== r ||
            (this.clearHotkeyReleaseWatcher(), t.stage === `transcription`)
            ? !0
            : (this.clearActiveSession(), this.showIdleOrHide(), !0);
        case `global-dictation-in-app-started`:
          return (
            t.sessionId === this.pendingInAppDictationStart?.sessionId &&
              this.pendingInAppDictationStart.complete(!0),
            !0
          );
      }
    }
    dispose() {
      this.deactivateLifecycle();
    }
    getHotkeyState() {
      let e = B4() && this.isGateEnabled;
      return {
        supported: e,
        configuredHotkey: e ? this.configuredHotkey : null,
        configuredToggleHotkey: e ? this.configuredToggleHotkey : null,
        keepVisible: this.keepVisible,
      };
    }
    setKeepVisible(e) {
      let n = e && this.hasConfiguredHotkey();
      return this.keepVisible === n
        ? this.getHotkeyState()
        : ((this.keepVisible = n),
          this.options.globalState.set(t.Fo.GLOBAL_DICTATION_KEEP_VISIBLE, n),
          n
            ? (this.applyLifecycleWithWarning(`keep-visible-change`), this.getHotkeyState())
            : (this.activeSession?.type !== `overlay` && this.windowController.hide(),
              this.getHotkeyState()));
    }
    setHotkey(e) {
      return this.setHotkeyForMode(`hold`, e);
    }
    setToggleHotkey(e) {
      return this.setHotkeyForMode(`toggle`, e);
    }
    setHotkeyForMode(e, n) {
      if (!B4() || !this.isGateEnabled) return this.createMutationFailure(`Not supported.`);
      if (n != null) {
        let e = qO(n);
        if (e != null) return this.createMutationFailure(e);
        (zO(n), V4());
      }
      if (
        (e === `hold` && z4(n, this.configuredToggleHotkey)) ||
        (e === `toggle` && z4(this.configuredHotkey, n))
      )
        return this.createMutationFailure(`Hold and toggle dictation hotkeys must be different.`);
      let r = this.configuredHotkey,
        i = this.configuredToggleHotkey,
        a = this.keepVisible,
        o = this.hasConfiguredHotkey();
      e === `hold` ? (this.configuredHotkey = n) : (this.configuredToggleHotkey = n);
      let s = this.hasConfiguredHotkey();
      !o && s ? (this.keepVisible = !0) : s || (this.keepVisible = !1);
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.configuredHotkey = r),
          (this.configuredToggleHotkey = i),
          (this.keepVisible = a),
          this.applyLifecycleWithWarning(`hotkey-rollback`),
          this.createMutationFailure(e instanceof Error ? e.message : String(e))
        );
      }
      return (
        this.options.globalState.set(`globalDictationHotkey`, this.configuredHotkey ?? void 0),
        this.options.globalState.set(
          `globalDictationToggleHotkey`,
          this.configuredToggleHotkey ?? void 0,
        ),
        this.keepVisible !== a &&
          this.options.globalState.set(t.Fo.GLOBAL_DICTATION_KEEP_VISIBLE, this.keepVisible),
        { success: !0, state: this.getHotkeyState() }
      );
    }
    createMutationFailure(e) {
      return { success: !1, error: e, state: this.getHotkeyState() };
    }
    applyLifecycleOrThrow() {
      if (!this.isGateEnabled || !B4()) {
        this.deactivateLifecycle();
        return;
      }
      if (this.configuredHotkey == null && this.configuredToggleHotkey == null) {
        this.deactivateLifecycle();
        return;
      }
      if (!this.ensureLifecycleLock())
        throw Error(`Global dictation is already active in another Codex app.`);
      (this.configuredHotkey == null
        ? this.unregisterHotkey()
        : this.registerHotkeyOrThrow(this.configuredHotkey),
        this.configuredToggleHotkey == null
          ? this.unregisterToggleHotkey()
          : this.registerToggleHotkeyOrThrow(this.configuredToggleHotkey),
        this.keepVisible && this.activeSession?.type !== `overlay`
          ? this.showIdleWindow()
          : this.windowController.prewarm());
    }
    applyLifecycleWithWarning(e) {
      try {
        this.applyLifecycleOrThrow();
      } catch (n) {
        (t
          .Ur()
          .warning(`Failed to apply global dictation runtime state`, {
            safe: { reason: e },
            sensitive: { error: n },
          }),
          this.deactivateLifecycle());
      }
    }
    setGateEnabled(e) {
      this.isGateEnabled !== e &&
        ((this.isGateEnabled = e), this.applyLifecycleWithWarning(`gate-change`));
    }
    setForceGlobalDictationLock(e) {
      let t = this.options.canForceGlobalDictationLock === !0 && e;
      this.forceGlobalDictationLock !== t &&
        ((this.forceGlobalDictationLock = t),
        t || this.releaseLifecycleLock(),
        this.applyLifecycleWithWarning(`force-lock-change`));
    }
    syncCommandKeybindings({ holdHotkey: e, toggleHotkey: t }) {
      let n =
        e != null && e !== this.configuredHotkey
          ? e
          : t != null && t !== this.configuredToggleHotkey
            ? t
            : null;
      (n != null && (zO(n), V4()),
        this.options.globalState.set(`globalDictationHotkey`, e ?? void 0),
        this.options.globalState.set(`globalDictationToggleHotkey`, t ?? void 0),
        this.syncConfiguredHotkeys({ holdHotkey: e, toggleHotkey: t }));
    }
    syncConfiguredHotkeys({ holdHotkey: e, toggleHotkey: n }) {
      let r = this.hasConfiguredHotkey(),
        i = this.keepVisible;
      ((this.configuredHotkey = e), (this.configuredToggleHotkey = z4(e, n) ? null : n));
      let a = this.hasConfiguredHotkey();
      (!r && a ? (this.keepVisible = !0) : a || (this.keepVisible = !1),
        this.keepVisible !== i &&
          this.options.globalState.set(t.Fo.GLOBAL_DICTATION_KEEP_VISIBLE, this.keepVisible),
        this.applyLifecycleWithWarning(`command-keybinding-sync`));
    }
    hasConfiguredHotkey() {
      return this.configuredHotkey != null || this.configuredToggleHotkey != null;
    }
    deactivateLifecycle() {
      (this.unregisterHotkey(),
        this.unregisterToggleHotkey(),
        this.activeSession != null && this.stopRecording(this.activeSession.id),
        this.clearHotkeyReleaseWatcher(),
        this.pendingInAppDictationStart?.complete(!1),
        this.clearActiveSession(),
        this.windowController.close(),
        this.releaseLifecycleLock());
    }
    ensureLifecycleLock() {
      return this.lifecycleLock == null
        ? ((this.lifecycleLock = f4({
            force: this.forceGlobalDictationLock,
            onLost: () => {
              ((this.lifecycleLock = null), this.deactivateLifecycle());
            },
          })),
          this.lifecycleLock != null)
        : !0;
    }
    releaseLifecycleLock() {
      (this.lifecycleLock?.dispose(), (this.lifecycleLock = null));
    }
    registerHotkeyOrThrow(e) {
      if (this.registeredHotkey === e) return;
      let t = this.registeredHotkey,
        n = FO(e, {
          onPressed: () => {
            this.handleHoldHotkeyPressed();
          },
          onReleased: () => {
            this.handleHoldHotkeyReleased();
          },
        });
      if (n == null) throw Error(`Unable to register global dictation hotkey: ${e}`);
      (t != null && this.registeredHotkeyRegistration?.unregister(),
        (this.registeredHotkey = e),
        (this.registeredHotkeyRegistration = n));
    }
    unregisterHotkey() {
      this.registeredHotkey != null &&
        (this.registeredHotkeyRegistration?.unregister(),
        (this.registeredHotkey = null),
        (this.registeredHotkeyRegistration = null));
    }
    registerToggleHotkeyOrThrow(e) {
      if (this.registeredToggleHotkey === e) return;
      let t = this.registeredToggleHotkey,
        n = FO(
          e,
          {
            onPressed: () => {
              this.handleToggleHotkeyPressed();
            },
          },
          { bareModifierTrigger: `release` },
        );
      if (n == null) throw Error(`Unable to register global dictation toggle hotkey: ${e}`);
      (t != null && this.registeredToggleHotkeyRegistration?.unregister(),
        (this.registeredToggleHotkey = e),
        (this.registeredToggleHotkeyRegistration = n));
    }
    unregisterToggleHotkey() {
      this.registeredToggleHotkey != null &&
        (this.registeredToggleHotkeyRegistration?.unregister(),
        (this.registeredToggleHotkey = null),
        (this.registeredToggleHotkeyRegistration = null));
    }
    async handleHoldHotkeyPressed() {
      if (this.activeSession != null) return;
      let e = this.windowController.getFocusedInAppWindow();
      (e != null && (await this.tryStartInAppSession(e, `hold`))) ||
        (await this.startSession(`hold`));
    }
    async handleToggleHotkeyPressed() {
      let e = this.activeSession;
      if (e != null) {
        e.mode === `toggle` && this.stopRecording(e.id);
        return;
      }
      let t = this.windowController.getFocusedInAppWindow();
      (t != null && (await this.tryStartInAppSession(t, `toggle`))) ||
        (await this.startSession(`toggle`));
    }
    async startSession(e) {
      if (this.activeSession != null) return;
      let n = (0, u.randomUUID)(),
        r = this.getVisibleNativePetWebContents();
      if (r != null) {
        ((this.activeSession = { type: `pet`, id: n, webContentsId: r.id, mode: e }),
          e === `hold` && this.watchHoldHotkeyRelease(n),
          this.windowController.hide(),
          this.options.windowManager.sendMessageToWebContents(r, {
            type: `global-dictation-start`,
            sessionId: n,
          }));
        return;
      }
      this.activeSession = { type: `overlay`, id: n, mode: e };
      try {
        if (
          (e === `hold` && this.watchHoldHotkeyRelease(n),
          !(await this.windowController.showAndStart(n)))
        ) {
          (this.clearHotkeyReleaseWatcher(), this.clearActiveSession());
          return;
        }
        if (this.activeSession?.id !== n) return;
      } catch (e) {
        (this.clearHotkeyReleaseWatcher(),
          this.clearActiveSession(),
          t
            .Ur()
            .warning(`Failed to open global dictation window`, {
              safe: {},
              sensitive: { error: e },
            }));
      }
    }
    async tryStartInAppSession(e, t) {
      let n = (0, u.randomUUID)();
      return (
        (this.activeSession = { type: `inApp`, id: n, webContentsId: e.webContents.id, mode: t }),
        this.options.windowManager.sendMessageToWebContents(e.webContents, {
          type: `global-dictation-in-app-start`,
          sessionId: n,
        }),
        (await this.waitForInAppDictationStart(n))
          ? this.activeSession?.id === n
            ? (t === `hold` && this.watchHoldHotkeyRelease(n), !0)
            : !1
          : (this.activeSession?.id === n && this.clearActiveSession(), !1)
      );
    }
    waitForInAppDictationStart(e) {
      return new Promise((t) => {
        let n = setTimeout(() => {
          this.pendingInAppDictationStart?.complete(!1);
        }, L4);
        this.pendingInAppDictationStart = {
          sessionId: e,
          complete: (r) => {
            (this.pendingInAppDictationStart?.sessionId === e &&
              (this.pendingInAppDictationStart = null),
              clearTimeout(n),
              t(r));
          },
        };
      });
    }
    watchHoldHotkeyRelease(e) {
      if (this.registeredHotkeyRegistration?.handlesRelease) return;
      let n = this.registeredHotkey;
      if (n != null) {
        this.clearHotkeyReleaseWatcher();
        try {
          this.activeHotkeyReleaseWatcher = JO(n, () => {
            this.activeSession?.id === e && this.stopRecording(e);
          });
        } catch (n) {
          (t
            .Ur()
            .warning(`Failed to watch global dictation hotkey release`, {
              safe: {},
              sensitive: { error: n },
            }),
            this.stopRecording(e));
        }
      }
    }
    stopRecording(e) {
      this.clearHotkeyReleaseWatcher();
      let t = this.activeSession;
      if (t?.type === `inApp`) {
        (this.options.windowManager.sendMessageToWebContentsId(t.webContentsId, {
          type: `global-dictation-in-app-stop`,
          sessionId: e,
        }),
          this.clearActiveSession());
        return;
      }
      if (t?.type === `pet`) {
        this.options.windowManager.sendMessageToWebContentsId(t.webContentsId, {
          type: `global-dictation-stop`,
          sessionId: e,
        });
        return;
      }
      this.windowController.sendStop(e);
    }
    handleHoldHotkeyReleased() {
      let e = this.activeSession;
      e == null || e.mode !== `hold` || this.stopRecording(e.id);
    }
    clearHotkeyReleaseWatcher() {
      this.activeHotkeyReleaseWatcher != null &&
        (this.activeHotkeyReleaseWatcher.dispose(), (this.activeHotkeyReleaseWatcher = null));
    }
    clearActiveSession() {
      this.activeSession = null;
    }
    async showIdleWindow() {
      this.showNativePetIdleIfAvailable() ||
        (await this.windowController.showIdle(this.configuredHotkey, this.configuredToggleHotkey));
    }
    showIdleOrHide() {
      if (!this.showNativePetIdleIfAvailable()) {
        if (this.keepVisible) {
          this.showIdleWindow();
          return;
        }
        this.windowController.hide();
      }
    }
    showNativePetIdleIfAvailable() {
      let e = this.getVisibleNativePetWebContents();
      return e == null
        ? !1
        : (this.options.windowManager.sendMessageToWebContents(e, {
            type: `global-dictation-idle`,
            configuredHotkey: this.configuredHotkey,
            configuredToggleHotkey: this.configuredToggleHotkey,
          }),
          this.windowController.hide(),
          !0);
    }
    setNativePetRendererReady(e, t) {
      (t
        ? this.nativePetRendererWebContentsIds.add(e)
        : this.nativePetRendererWebContentsIds.delete(e),
        this.activeSession ?? this.showIdleOrHide());
    }
    getVisibleNativePetWebContents() {
      let e = this.options.getVisibleNativePetWebContents?.() ?? null;
      return e != null && this.nativePetRendererWebContentsIds.has(e.id) ? e : null;
    }
    async handleTranscript(e, n) {
      (this.clearHotkeyReleaseWatcher(), this.clearActiveSession(), this.showIdleOrHide());
      let r = this.recordHistoryText(e, n);
      if (r != null)
        try {
          await O4(`${r} `);
        } catch (e) {
          t.Ur().warning(`Global dictation paste failed`, { safe: {}, sensitive: { error: e } });
        }
    }
    recordHistoryText(e, t) {
      let n = t.trim();
      return n ? (this.recordHistory({ id: e, createdAtMs: Date.now(), text: n }), n) : null;
    }
    recordHistory(e) {
      (this.history.unshift(e), this.history.splice(10), a4(e));
    }
    copyHistoryItem(e) {
      let t = this.history.find((t) => t.id === e);
      return t == null || t.text.length === 0 ? !1 : (a.clipboard.writeText(t.text), !0);
    }
  };
function z4(e, t) {
  return e != null && t != null && e === t;
}
function B4() {
  return process.platform === `darwin` || process.platform === `win32`;
}
function V4() {
  process.platform === `darwin` && a.systemPreferences.isTrustedAccessibilityClient(!0);
}
function H4(e) {
  return e.type in I4;
}
function U4({
  currentBounds: e,
  desiredComposerTopOnScreenPx: t,
  minimumComposerTopInsetPx: n,
  restingComposerTopInsetPx: r,
  workArea: i,
}) {
  let a = Math.min(n, r),
    o = i.y,
    s = i.y + i.height - e.height,
    c = W4(t, o + a, s + r),
    l = W4(r, Math.max(a, c - s), Math.min(r, c - o));
  return { composerTopInsetPx: l, windowBounds: { ...e, y: c - l } };
}
function W4(e, t, n) {
  return Math.max(t, Math.min(e, n));
}
var G4 = 470,
  K4 = G4,
  q4 = 290,
  J4 = G4,
  Y4 = 640,
  X4 = 400,
  Z4 = 400,
  Q4 = 52,
  $4 = 110,
  e3 = 1200,
  t3 = class {
    logger = t.Wr(`hotkey-window-controller`);
    configuredWindowIds = new Set();
    mode = `hidden`;
    isDisposed = !1;
    lastVisibleSurface = `home`;
    homeWindow = null;
    homeWindowPromise = null;
    threadWindow = null;
    lastDetailRoute = null;
    lastDetailRouteState = void 0;
    threadWindowPromise = null;
    threadSize = { width: J4, height: Y4 };
    transitionInFlight = null;
    windowGeneration = 0;
    homePointerInteractive = !0;
    homeMousePassthroughEnabled = !1;
    homeComposerTopInsetPx = null;
    homeLayoutMetrics = null;
    homeDragState = null;
    primaryWindowMayBeDismissFocusFallback = !1;
    constructor(e, t = () => !0, n = {}) {
      ((this.windowManager = e), (this.shouldRetainWindows = t), (this.callbacks = n));
    }
    async toggleHotkey() {
      if (this.transitionInFlight) return;
      if (this.mode === `hidden`) {
        await this.showLastVisibleSurface();
        return;
      }
      let e = this.getWindowForCurrentMode();
      if (e != null && !e.isFocused()) {
        let t = this.lastDetailRoute;
        (this.mode === `threadVisible` &&
          t != null &&
          e === this.threadWindow &&
          this.navigateToDetailRoute(e, t, i3(t, this.lastDetailRouteState)),
          this.showAndFocusFromCurrentFocus(e));
        return;
      }
      this.hideAll();
    }
    async show() {
      if (this.transitionInFlight) return;
      let e = this.getWindowForCurrentMode();
      if (e != null) {
        this.showAndFocusFromCurrentFocus(e);
        return;
      }
      await this.showLastVisibleSurface();
    }
    prewarm() {
      this.prewarmWindows();
    }
    async openThread(e) {
      this.transitionInFlight || (await this.openDetailRoute(t.zi(e)));
    }
    async openDetailRoute(e, t) {
      if (!this.transitionInFlight) {
        if (((this.lastDetailRoute = e), (this.lastDetailRouteState = t), this.mode === `hidden`)) {
          await this.showThread(e, t);
          return;
        }
        if (this.mode === `threadVisible`) {
          let n = this.windowGeneration,
            r = await this.ensureThreadWindow(e);
          if (!this.isWindowGenerationCurrent(n)) return;
          (this.navigateToDetailRoute(r, e, t),
            this.showAndFocusFromCurrentFocus(r),
            (this.lastVisibleSurface = `thread`));
          return;
        }
        this.mode === `homeVisible` && (await this.startExpandTransition(e, t));
      }
    }
    async openHome(e = null) {
      if (!this.transitionInFlight) {
        if (this.mode === `hidden`) {
          await this.showHome(e);
          return;
        }
        if (this.mode === `homeVisible`) {
          let n = this.windowGeneration,
            r = await this.ensureHomeWindow();
          if (!this.isWindowGenerationCurrent(n)) return;
          (this.applyHotkeyWindowWindowPolicy(r),
            this.navigateToRoute(r, t.Ii, r3(e)),
            this.showAndFocusFromCurrentFocus(r),
            (this.lastVisibleSurface = `home`),
            this.applyHomeInteractivityPolicy());
          return;
        }
        this.mode === `threadVisible` && (await this.startCollapseTransition(e));
      }
    }
    async collapseToHome() {
      if (!this.transitionInFlight) {
        if (this.mode === `hidden`) {
          await this.showHome();
          return;
        }
        this.mode === `threadVisible` && (await this.startCollapseTransition());
      }
    }
    setHomePointerInteraction(e, t) {
      this.isHomeOrigin(e) &&
        (this.transitionInFlight ||
          (this.homePointerInteractive !== t.isInteractive &&
            ((this.homePointerInteractive = t.isInteractive),
            this.applyHomeInteractivityPolicy())));
    }
    setHomeLayout(e, t) {
      if (!this.isHomeOrigin(e) || !this.homeWindow) return;
      let n = {
        minimumComposerTopInsetPx: Math.max(0, Math.round(t.minimumComposerTopInsetPx)),
        restingComposerTopInsetPx: Math.max(0, Math.round(t.restingComposerTopInsetPx)),
      };
      if (
        this.homeLayoutMetrics?.minimumComposerTopInsetPx === n.minimumComposerTopInsetPx &&
        this.homeLayoutMetrics.restingComposerTopInsetPx === n.restingComposerTopInsetPx
      )
        return;
      let r = this.getWindowLayoutBounds(this.homeWindow),
        i = this.homeComposerTopInsetPx ?? n.restingComposerTopInsetPx;
      ((this.homeLayoutMetrics = n),
        this.layoutHomeForComposerTop(r.y + i, a.screen.getDisplayMatching(r).workArea));
    }
    startHomeDrag(e, t) {
      if (!this.isHomeOrigin(e) || !this.homeWindow) return;
      let n = this.homeLayoutMetrics;
      if (n == null) return;
      let r = this.homeComposerTopInsetPx ?? n.restingComposerTopInsetPx;
      this.homeDragState = {
        pointerComposerY: t.pointerWindowY - r,
        pointerWindowX: t.pointerWindowX,
      };
    }
    moveHomeDrag(e) {
      if (!this.isHomeOrigin(e) || !this.homeWindow || this.homeDragState == null) return;
      let t = a.screen.getCursorScreenPoint(),
        n = a.screen.getDisplayNearestPoint(t).workArea;
      this.layoutHomeForComposerTop(
        Math.round(t.y - this.homeDragState.pointerComposerY),
        n,
        Math.round(t.x - this.homeDragState.pointerWindowX),
      );
    }
    endHomeDrag(e) {
      this.isHomeOrigin(e) && ((this.homeDragState = null), this.applyHomeInteractivityPolicy());
    }
    layoutHomeForComposerTop(e, t, n) {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) return;
      let r = this.homeLayoutMetrics;
      if (r == null) return;
      let i = this.getWindowLayoutBounds(this.homeWindow),
        { composerTopInsetPx: a, windowBounds: o } = U4({
          currentBounds: n == null ? i : { ...i, x: n },
          desiredComposerTopOnScreenPx: e,
          minimumComposerTopInsetPx: r.minimumComposerTopInsetPx,
          restingComposerTopInsetPx: r.restingComposerTopInsetPx,
          workArea: t,
        });
      (o3(i, o) || this.setWindowLayoutBounds(this.homeWindow, o),
        this.homeComposerTopInsetPx !== a &&
          ((this.homeComposerTopInsetPx = a),
          this.windowManager.sendMessageToWindow(this.homeWindow, {
            type: `hotkey-window-home-composer-position-changed`,
            topInsetPx: a,
          })));
    }
    handleTransitionDone(e, t) {
      let n = this.transitionInFlight;
      if (n && n.id === t.transitionId) {
        if (n.stage === `awaiting-source-raised`) {
          if (t.step !== `raised` || e.id !== n.sourceWebContentsId) return;
          (this.clearTransitionTimeout(),
            this.swapWindowsAfterRaise(n),
            (n.stage = `awaiting-destination-lowered`),
            this.startTransitionTimeout(n));
          let r = this.getWindowByWebContentsId(n.destinationWebContentsId);
          r && this.sendTransition(r, n.id, `lower-curtain`);
          return;
        }
        n.stage === `awaiting-destination-lowered` &&
          t.step === `lowered` &&
          e.id === n.destinationWebContentsId &&
          this.finishTransition(n);
      }
    }
    hideAll() {
      (this.clearTransitionTimeout(),
        (this.transitionInFlight = null),
        this.resetHomeInteractivity());
      let e =
        process.platform === `darwin` && !this.primaryWindowMayBeDismissFocusFallback
          ? this.windowManager.getPrimaryWindows()
          : [];
      for (let t of e) t.setFocusable(!1);
      try {
        (this.homeWindow && !this.homeWindow.isDestroyed() && this.homeWindow.hide(),
          this.threadWindow && !this.threadWindow.isDestroyed() && this.threadWindow.hide(),
          (this.mode = `hidden`));
      } finally {
        for (let t of e) t.isDestroyed() || t.setFocusable(!0);
      }
    }
    closeAllWindows() {
      (this.clearTransitionTimeout(),
        (this.transitionInFlight = null),
        this.resetHomeInteractivity(),
        (this.windowGeneration += 1));
      let e = this.homeWindow,
        t = this.threadWindow;
      ((this.homeWindow = null),
        (this.homeWindowPromise = null),
        (this.homeComposerTopInsetPx = null),
        (this.homeLayoutMetrics = null),
        (this.homeDragState = null),
        (this.threadWindow = null),
        (this.threadWindowPromise = null),
        (this.mode = `hidden`),
        this.closeWindow(e),
        this.closeWindow(t));
    }
    dispose() {
      ((this.isDisposed = !0),
        this.closeAllWindows(),
        (this.lastDetailRoute = null),
        (this.lastDetailRouteState = void 0));
    }
    async prewarmWindows() {
      let e = this.windowGeneration,
        t = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(e)) return;
      let n = await this.ensureThreadWindow();
      this.isWindowGenerationCurrent(e) &&
        (t.isDestroyed() || t.hide(), n.isDestroyed() || n.hide());
    }
    async showHome(e = null, n = !1) {
      let r = this.windowGeneration;
      this.homePointerInteractive = !0;
      let i = this.homeWindow != null && !this.homeWindow.isDestroyed(),
        a = this.threadWindow != null && !this.threadWindow.isDestroyed(),
        o = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(r)) return;
      if (a && this.lastVisibleSurface === `thread`) this.alignHomeToThread();
      else if (!i) {
        let e = this.computeHomeBounds(this.getTargetDisplay());
        this.setWindowLayoutBounds(o, e);
      }
      (this.applyHotkeyWindowWindowPolicy(o),
        (!n || !i) && this.navigateToRoute(o, t.Ii, r3(e)),
        this.showAndFocusFromCurrentFocus(o),
        (this.mode = `homeVisible`),
        (this.lastVisibleSurface = `home`),
        this.applyHomeInteractivityPolicy());
      let s = await this.ensureThreadWindow();
      this.isWindowGenerationCurrent(r) && (this.alignThreadToHome(), s.hide());
    }
    async showThread(e, t) {
      let n = this.windowGeneration,
        r = e ?? this.lastDetailRoute,
        i = t ?? this.lastDetailRouteState;
      if (!r) return !1;
      let a = this.threadWindow != null && !this.threadWindow.isDestroyed(),
        o = await this.ensureThreadWindow(r);
      return this.isWindowGenerationCurrent(n)
        ? (a || this.setWindowLayoutBounds(o, this.computeThreadBounds(this.getTargetDisplay())),
          this.homeWindow &&
            !this.homeWindow.isDestroyed() &&
            this.lastVisibleSurface === `home` &&
            this.alignThreadToHome(),
          this.applyHotkeyWindowWindowPolicy(o),
          this.navigateToDetailRoute(o, r, i),
          this.showAndFocusFromCurrentFocus(o),
          this.homeWindow && !this.homeWindow.isDestroyed() && this.homeWindow.hide(),
          this.resetHomeInteractivity(),
          (this.mode = `threadVisible`),
          (this.lastVisibleSurface = `thread`),
          !0)
        : !1;
    }
    async showLastVisibleSurface() {
      (this.lastVisibleSurface === `thread` && (await this.showThread())) ||
        (await this.showHome(null, !0));
    }
    async startExpandTransition(e, t) {
      let n = this.windowGeneration,
        r = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      let i = await this.ensureThreadWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      ((this.lastDetailRoute = e),
        (this.lastDetailRouteState = t),
        this.alignThreadToHome(),
        this.applyHotkeyWindowWindowPolicy(r),
        this.applyHotkeyWindowWindowPolicy(i),
        this.navigateToDetailRoute(i, e, t),
        this.resetHomeInteractivity());
      let a = this.beginTransition({ kind: `expand`, sourceWindow: r, destinationWindow: i });
      (this.sendTransition(i, a, `raise-curtain`), this.sendTransition(r, a, `raise-curtain`));
    }
    async startCollapseTransition(e = null) {
      let n = this.windowGeneration,
        r = await this.ensureHomeWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      let i = await this.ensureThreadWindow();
      if (!this.isWindowGenerationCurrent(n)) return;
      (this.alignHomeToThread(),
        this.applyHotkeyWindowWindowPolicy(r),
        this.applyHotkeyWindowWindowPolicy(i),
        this.navigateToRoute(r, t.Ii, r3(e)),
        this.resetHomeInteractivity());
      let a = this.beginTransition({ kind: `collapse`, sourceWindow: i, destinationWindow: r });
      (this.sendTransition(r, a, `raise-curtain`), this.sendTransition(i, a, `raise-curtain`));
    }
    beginTransition({ kind: e, sourceWindow: t, destinationWindow: n }) {
      let r = (0, u.randomUUID)(),
        i = setTimeout(() => {
          this.handleTransitionTimeout(r);
        }, e3);
      return (
        i.unref(),
        (this.transitionInFlight = {
          id: r,
          kind: e,
          stage: `awaiting-source-raised`,
          sourceWebContentsId: t.webContents.id,
          destinationWebContentsId: n.webContents.id,
          timeout: i,
        }),
        r
      );
    }
    handleTransitionTimeout(e) {
      let t = this.transitionInFlight;
      !t ||
        t.id !== e ||
        (this.logger.warning(`Hotkey Window transition timed out; forcing completion`, {
          safe: { transitionId: e, stage: t.stage, kind: t.kind },
          sensitive: {},
        }),
        this.swapWindowsAfterRaise(t),
        this.finishTransition(t));
    }
    startTransitionTimeout(e) {
      (this.clearTransitionTimeout(),
        (e.timeout = setTimeout(() => {
          this.handleTransitionTimeout(e.id);
        }, e3)),
        e.timeout.unref());
    }
    clearTransitionTimeout() {
      this.transitionInFlight && clearTimeout(this.transitionInFlight.timeout);
    }
    swapWindowsAfterRaise(e) {
      let t = this.getWindowByWebContentsId(e.sourceWebContentsId),
        n = this.getWindowByWebContentsId(e.destinationWebContentsId);
      n &&
        (this.applyHotkeyWindowWindowPolicy(n),
        this.showAndFocus(n),
        t && !t.isDestroyed() && t.hide());
    }
    finishTransition(e) {
      this.clearTransitionTimeout();
      let t = this.getWindowByWebContentsId(e.sourceWebContentsId),
        n = this.getWindowByWebContentsId(e.destinationWebContentsId);
      (t && this.sendTransition(t, e.id, `commit`),
        n && this.sendTransition(n, e.id, `commit`),
        (this.transitionInFlight = null),
        (this.mode = e.kind === `expand` ? `threadVisible` : `homeVisible`),
        (this.lastVisibleSurface = e.kind === `expand` ? `thread` : `home`),
        this.applyHomeInteractivityPolicy());
    }
    async ensureHomeWindow() {
      if (this.homeWindow && !this.homeWindow.isDestroyed()) return this.homeWindow;
      if (this.homeWindowPromise != null) return this.homeWindowPromise;
      let e = this.windowGeneration,
        t = this.windowManager
          .createWindow({
            title: a.app.getName(),
            width: K4,
            height: q4,
            appearance: `hotkeyWindowHome`,
            show: !1,
            initialRoute: `/hotkey-window`,
          })
          .then((t) =>
            this.isDisposed || e !== this.windowGeneration
              ? (this.closeWindow(t), t)
              : (t.setMenuBarVisibility(!1),
                t.on(`focus`, () => {
                  this.callbacks.onWindowFocused?.(t.webContents.id);
                }),
                t.on(`blur`, () => {
                  this.handleHotkeyWindowBlurred(t.webContents.id);
                }),
                t.on(`closed`, () => {
                  if (
                    (this.configuredWindowIds.delete(t.id),
                    (this.homeMousePassthroughEnabled = !1),
                    (this.homePointerInteractive = !0),
                    this.homeWindow === t &&
                      ((this.homeWindow = null),
                      (this.homeComposerTopInsetPx = null),
                      (this.homeLayoutMetrics = null),
                      (this.homeDragState = null)),
                    !this.shouldRetainWindows())
                  ) {
                    this.closeAllWindows();
                    return;
                  }
                  this.mode !== `threadVisible` && (this.mode = `hidden`);
                }),
                (this.homeWindow = t),
                t),
          )
          .finally(() => {
            this.homeWindowPromise === t && (this.homeWindowPromise = null);
          });
      return ((this.homeWindowPromise = t), t);
    }
    async ensureThreadWindow(e) {
      if (this.threadWindow && !this.threadWindow.isDestroyed()) return this.threadWindow;
      if (this.threadWindowPromise != null) return this.threadWindowPromise;
      let t = this.windowGeneration,
        n = this.windowManager
          .createWindow({
            title: a.app.getName(),
            width: this.threadSize.width,
            height: this.threadSize.height,
            appearance: `hotkeyWindowThread`,
            show: !1,
            initialRoute: e ?? `/hotkey-window`,
          })
          .then((e) =>
            this.isDisposed || t !== this.windowGeneration
              ? (this.closeWindow(e), e)
              : (e.setMenuBarVisibility(!1),
                this.setWindowLayoutBounds(e, this.computeThreadBounds(this.getTargetDisplay())),
                e.setMinimumSize(X4, Z4),
                e.on(`resize`, () => {
                  if (e.isDestroyed()) return;
                  let t = this.getWindowLayoutBounds(e);
                  this.threadSize = {
                    width: Math.max(t.width, X4),
                    height: Math.max(t.height, Z4),
                  };
                }),
                e.on(`focus`, () => {
                  this.callbacks.onWindowFocused?.(e.webContents.id);
                }),
                e.on(`blur`, () => {
                  this.handleHotkeyWindowBlurred(e.webContents.id);
                }),
                e.on(`closed`, () => {
                  if (
                    (this.configuredWindowIds.delete(e.id),
                    this.threadWindow === e && (this.threadWindow = null),
                    !this.shouldRetainWindows())
                  ) {
                    this.closeAllWindows();
                    return;
                  }
                  this.mode !== `homeVisible` && (this.mode = `hidden`);
                }),
                (this.threadWindow = e),
                e),
          )
          .finally(() => {
            this.threadWindowPromise === n && (this.threadWindowPromise = null);
          });
      return ((this.threadWindowPromise = n), n);
    }
    applyHotkeyWindowWindowPolicy(e) {
      e.isDestroyed() ||
        (this.configuredWindowIds.has(e.id) ||
          (this.configuredWindowIds.add(e.id),
          process.platform === `darwin`
            ? e.setVisibleOnAllWorkspaces(!0, {
                visibleOnFullScreen: !0,
                skipTransformProcessType: !0,
              })
            : e.setVisibleOnAllWorkspaces(!0)),
        e.moveTop());
    }
    showAndFocus(e) {
      e.isDestroyed() ||
        (e.isMinimized() && e.restore(),
        this.applyHotkeyWindowWindowPolicy(e),
        e.show(),
        process.platform === `darwin` && a.app.focus({ steal: !0 }),
        e.focus());
    }
    showAndFocusFromCurrentFocus(e) {
      ((this.primaryWindowMayBeDismissFocusFallback = this.primaryWindowIsFocused()),
        this.showAndFocus(e));
    }
    handleHotkeyWindowBlurred(e) {
      (this.transitionInFlight ||
        (this.primaryWindowMayBeDismissFocusFallback = this.primaryWindowIsFocused()),
        this.callbacks.onWindowBlurred?.(e));
    }
    primaryWindowIsFocused() {
      return this.windowManager.getPrimaryWindows().some((e) => e.isFocused());
    }
    getVisibleWindowWebContentsId() {
      return this.getWindowForCurrentMode()?.webContents.id ?? null;
    }
    getTargetDisplay() {
      let e = a.screen.getCursorScreenPoint();
      return a.screen.getDisplayNearestPoint(e);
    }
    computeHomeBounds(e) {
      let t = e.workArea,
        n = Math.min(K4, t.width),
        r = Math.min(q4, t.height),
        i = this.computeThreadBounds(e);
      return {
        x: Math.round(t.x + (t.width - n) / 2),
        y: a3(Math.round(i.y + i.height - r), t.y, t.y + t.height - r),
        width: n,
        height: r,
      };
    }
    computeThreadBounds(e) {
      let t = e.workArea,
        n = Math.min(Math.max(this.threadSize.width, X4), t.width),
        r = Math.min(this.threadSize.height, t.height),
        i = Math.round(t.x + (t.width - n) / 2),
        a = Math.round(t.y + Q4);
      return {
        x: a3(i, t.x, t.x + t.width - n),
        y: a3(a, t.y, t.y + t.height - r),
        width: n,
        height: r,
      };
    }
    alignThreadToHome() {
      if (
        !this.homeWindow ||
        this.homeWindow.isDestroyed() ||
        !this.threadWindow ||
        this.threadWindow.isDestroyed()
      )
        return;
      let e = this.getWindowLayoutBounds(this.homeWindow),
        t = this.getWindowLayoutBounds(this.threadWindow),
        n = a.screen.getDisplayMatching(e).workArea,
        r = Math.round(e.x + (e.width - t.width) / 2),
        i = Math.round(e.y + e.height - t.height),
        o = a3(r, n.x, n.x + n.width - t.width),
        s = a3(i, n.y, n.y + n.height - t.height);
      this.setWindowLayoutPosition(this.threadWindow, o, s);
    }
    alignHomeToThread() {
      if (
        !this.homeWindow ||
        this.homeWindow.isDestroyed() ||
        !this.threadWindow ||
        this.threadWindow.isDestroyed()
      )
        return;
      let e = this.getWindowLayoutBounds(this.homeWindow),
        t = this.getWindowLayoutBounds(this.threadWindow),
        n = Math.max(t.width, X4),
        r = Math.round(t.x + (t.width - n) / 2),
        i = Math.round(t.y + t.height - e.height);
      (e.x === r && e.y === i && e.width === n) ||
        this.setWindowLayoutBounds(this.homeWindow, { ...e, x: r, y: i, width: n });
    }
    getWindowLayoutBounds(e) {
      return process.platform === `win32` ? e.getContentBounds() : e.getBounds();
    }
    setWindowLayoutBounds(e, t) {
      if (!e.isDestroyed()) {
        if (process.platform === `win32`) {
          e.setContentBounds(t, !1);
          return;
        }
        e.setBounds(t, !1);
      }
    }
    setWindowLayoutPosition(e, t, n) {
      if (e.isDestroyed()) return;
      let r = this.getWindowLayoutBounds(e);
      (r.x === t && r.y === n) || this.setWindowLayoutBounds(e, { ...r, x: t, y: n });
    }
    closeWindow(e) {
      !e || e.isDestroyed() || e.close();
    }
    isWindowGenerationCurrent(e) {
      return !this.isDisposed && e === this.windowGeneration;
    }
    resetHomeInteractivity() {
      ((this.homeDragState = null),
        (this.homePointerInteractive = !0),
        this.disableHomeMousePassthrough());
    }
    disableHomeMousePassthrough() {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) {
        this.homeMousePassthroughEnabled = !1;
        return;
      }
      this.homeMousePassthroughEnabled &&
        ((this.homeMousePassthroughEnabled = !1), this.homeWindow.setIgnoreMouseEvents(!1));
    }
    applyHomeInteractivityPolicy() {
      if (!this.homeWindow || this.homeWindow.isDestroyed()) {
        this.homeMousePassthroughEnabled = !1;
        return;
      }
      if (this.transitionInFlight || this.mode !== `homeVisible`) {
        this.disableHomeMousePassthrough();
        return;
      }
      let e = !this.homePointerInteractive && this.homeDragState == null;
      if (this.homeMousePassthroughEnabled !== e) {
        if (((this.homeMousePassthroughEnabled = e), e)) {
          this.homeWindow.setIgnoreMouseEvents(!0, { forward: !0 });
          return;
        }
        this.homeWindow.setIgnoreMouseEvents(!1);
      }
    }
    getWindowByWebContentsId(e) {
      return this.homeWindow &&
        !this.homeWindow.isDestroyed() &&
        this.homeWindow.webContents.id === e
        ? this.homeWindow
        : this.threadWindow &&
            !this.threadWindow.isDestroyed() &&
            this.threadWindow.webContents.id === e
          ? this.threadWindow
          : null;
    }
    navigateToRoute(e, t, n) {
      this.windowManager.sendMessageToWindow(e, { type: `navigate-to-route`, path: t, state: n });
    }
    navigateToDetailRoute(e, n, r) {
      this.navigateToRoute(e, n, r ?? (t.Ui(n) == null ? void 0 : n3()));
    }
    sendTransition(e, t, n) {
      this.windowManager.sendMessageToWindow(e, {
        type: `hotkey-window-transition`,
        transitionId: t,
        step: n,
        durationMs: $4,
      });
    }
    isHomeOrigin(e) {
      return (
        this.homeWindow != null &&
        !this.homeWindow.isDestroyed() &&
        this.homeWindow.webContents.id === e.id
      );
    }
    getWindowForCurrentMode() {
      let e =
        this.mode === `homeVisible`
          ? this.homeWindow
          : this.mode === `threadVisible`
            ? this.threadWindow
            : null;
      return e == null || e.isDestroyed() ? null : e;
    }
  };
function n3() {
  return { focusComposerNonce: Date.now() };
}
function r3(e) {
  return { focusComposerNonce: Date.now(), ...(e == null ? {} : { prefillCwd: e }) };
}
function i3(e, n) {
  return t.Ui(e) == null
    ? e === t.Li
      ? r3(
          typeof n == `object` && n && `prefillCwd` in n && typeof n.prefillCwd == `string`
            ? n.prefillCwd
            : null,
        )
      : n
    : n3();
}
function a3(e, t, n) {
  return e < t ? t : e > n ? n : e;
}
function o3(e, t) {
  return e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height;
}
var s3 = 300,
  c3 = class {
    rpcFor = (e) => ({
      getState: () => this.getState(),
      setEnabled: (e) => this.setHotkeyWindowGateEnabled(e),
      open: (e) => this.open(e),
      collapseToHome: () => this.collapseToHome(),
      dismiss: () => this.dismiss(),
      setHotkey: (e) => this.setHotkey(e),
      setDevOverrideEnabled: (e) => this.setDevOverrideEnabled(e),
      transitionDone: (t) => {
        this.hotkeyWindowController?.handleTransitionDone(e, t);
      },
      homePointerInteractionChanged: (t) => {
        this.hotkeyWindowController?.setHomePointerInteraction(e, t);
      },
      homeLayoutChanged: (t) => {
        this.hotkeyWindowController?.setHomeLayout(e, t);
      },
      homeDragStart: (t) => {
        this.hotkeyWindowController?.startHomeDrag(e, t);
      },
      homeDragMove: () => {
        this.hotkeyWindowController?.moveHomeDrag(e);
      },
      homeDragEnd: () => {
        this.hotkeyWindowController?.endHomeDrag(e);
      },
    });
    hotkeyWindowController = null;
    isHotkeyWindowGateEnabled = !1;
    isHotkeyWindowActive = !1;
    devHotkeyWindowHotkeyOverrideEnabled = !1;
    registeredHotkeyWindowHotkey = null;
    registeredHotkeyWindowHotkeyRegistration = null;
    configuredHotkeyWindowHotkey;
    lastBlurredWindowWebContentsId = null;
    lastBlurredAtMs = 0;
    constructor(e) {
      this.options = e;
      let t = e.globalState.get(`hotkeyWindowHotkey`),
        n = t != null && LO(t) == null ? t : null,
        r = pM(`hotkeyWindow`);
      ((this.configuredHotkeyWindowHotkey = _M({ keymapHotkey: r, legacyHotkey: n })),
        r.hasBinding &&
          e.globalState.set(`hotkeyWindowHotkey`, this.configuredHotkeyWindowHotkey ?? void 0));
    }
    getState() {
      return this.getHotkeyWindowHotkeyState();
    }
    async open({ path: e, prefillCwd: n }) {
      if (!(!this.isHotkeyWindowGateEnabled || !t.Gi(e))) {
        if (e === `/hotkey-window`) {
          if (this.configuredHotkeyWindowHotkey == null) {
            await this.ensureHotkeyWindowController().openDetailRoute(t.Hi(!1), l3(n ?? null));
            return;
          }
          await this.ensureHotkeyWindowController().openHome(n ?? null);
          return;
        }
        await this.ensureHotkeyWindowController().openDetailRoute(
          e,
          e === t.Hi(!1) ? l3(n ?? null) : void 0,
        );
      }
    }
    async collapseToHome() {
      if (this.isHotkeyWindowGateEnabled) {
        if (this.configuredHotkeyWindowHotkey == null) {
          await this.getHotkeyWindowController()?.openDetailRoute(t.Hi(!1), l3(null));
          return;
        }
        await this.getHotkeyWindowController()?.collapseToHome();
      }
    }
    dismiss() {
      this.getHotkeyWindowController()?.hideAll();
    }
    async setHotkey(e) {
      let n = this.setHotkeyWindowHotkey(e);
      return (
        n.success &&
          (await t.qt({
            commandId: `hotkeyWindow`,
            update: e == null ? { type: `clear` } : { type: `set`, accelerator: e },
          })),
        n
      );
    }
    setDevOverrideEnabled(e) {
      return this.setHotkeyWindowDevHotkeyOverride(e);
    }
    syncCommandKeybinding(e) {
      (e != null && e !== this.configuredHotkeyWindowHotkey && RO(e),
        (this.configuredHotkeyWindowHotkey = e),
        this.options.globalState.set(`hotkeyWindowHotkey`, e ?? void 0),
        this.applyLifecycleWithWarning(`command-keybinding-sync`));
    }
    dispose() {
      this.deactivateLifecycle();
    }
    hide() {
      this.getHotkeyWindowController()?.hideAll();
    }
    toggleVisibility() {
      if (!this.isHotkeyWindowGateEnabled) return !1;
      let e = this.ensureHotkeyWindowController();
      return this.shouldHideHotkeyWindowOnToggle(e) ? (e.hideAll(), !0) : (e.toggleHotkey(), !0);
    }
    show() {
      return this.isHotkeyWindowGateEnabled ? (this.ensureHotkeyWindowController().show(), !0) : !1;
    }
    ensureHotkeyWindowController() {
      return (
        (this.hotkeyWindowController ??= new t3(
          this.options.windowManager,
          () => this.configuredHotkeyWindowHotkey != null,
          {
            onWindowFocused: () => {
              this.clearRecentBlur();
            },
            onWindowBlurred: (e) => {
              this.recordBlur(e);
            },
          },
        )),
        this.hotkeyWindowController
      );
    }
    isHotkeyWindowEffectivelyEnabled() {
      return (
        this.isHotkeyWindowGateEnabled &&
        this.configuredHotkeyWindowHotkey != null &&
        (!this.options.isDevMode || this.devHotkeyWindowHotkeyOverrideEnabled)
      );
    }
    getHotkeyWindowHotkeyState() {
      return {
        supported: !0,
        configuredHotkey: this.configuredHotkeyWindowHotkey,
        isGateEnabled: this.isHotkeyWindowGateEnabled,
        isDevMode: this.options.isDevMode,
        isDevOverrideEnabled: this.devHotkeyWindowHotkeyOverrideEnabled,
        isActive: this.isHotkeyWindowActive,
      };
    }
    createHotkeyWindowMutationFailure(e) {
      return { success: !1, error: e, state: this.getHotkeyWindowHotkeyState() };
    }
    handleHotkeyWindowHotkeyPressed() {
      this.isHotkeyWindowActive && this.ensureHotkeyWindowController().toggleHotkey();
    }
    registerHotkeyWindowHotkeyOrThrow(e) {
      if (this.registeredHotkeyWindowHotkey === e) return;
      let t = this.registeredHotkeyWindowHotkey,
        n = FO(e, {
          onPressed: () => {
            this.handleHotkeyWindowHotkeyPressed();
          },
        });
      if (n == null) throw Error(`Unable to register hotkey window hotkey: ${e}`);
      (t != null && this.registeredHotkeyWindowHotkeyRegistration?.unregister(),
        (this.registeredHotkeyWindowHotkey = e),
        (this.registeredHotkeyWindowHotkeyRegistration = n));
    }
    unregisterHotkeyWindowHotkey() {
      this.registeredHotkeyWindowHotkey &&
        (this.registeredHotkeyWindowHotkeyRegistration?.unregister(),
        (this.registeredHotkeyWindowHotkey = null),
        (this.registeredHotkeyWindowHotkeyRegistration = null));
    }
    deactivateLifecycle() {
      (this.unregisterHotkeyWindowHotkey(),
        this.hotkeyWindowController?.dispose(),
        (this.hotkeyWindowController = null),
        (this.isHotkeyWindowActive = !1));
    }
    applyLifecycleOrThrow() {
      if (!this.isHotkeyWindowEffectivelyEnabled()) {
        this.deactivateLifecycle();
        return;
      }
      if (this.configuredHotkeyWindowHotkey == null)
        throw Error(`Hotkey Window hotkey is not configured.`);
      (this.registerHotkeyWindowHotkeyOrThrow(this.configuredHotkeyWindowHotkey),
        (this.isHotkeyWindowActive = !0),
        this.ensureHotkeyWindowController().prewarm());
    }
    applyLifecycleWithWarning(e) {
      try {
        this.applyLifecycleOrThrow();
      } catch (n) {
        (t
          .Ur()
          .warning(`Failed to apply hotkey window runtime state`, {
            safe: { reason: e },
            sensitive: { error: n },
          }),
          this.deactivateLifecycle());
      }
    }
    setHotkeyWindowGateEnabled(e) {
      this.isHotkeyWindowGateEnabled !== e &&
        ((this.isHotkeyWindowGateEnabled = e), this.applyLifecycleWithWarning(`gate-change`));
    }
    setHotkeyWindowHotkey(e) {
      if (e != null) {
        let t = LO(e);
        if (t != null) return this.createHotkeyWindowMutationFailure(t);
        RO(e);
      }
      let t = this.configuredHotkeyWindowHotkey;
      this.configuredHotkeyWindowHotkey = e;
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.configuredHotkeyWindowHotkey = t),
          this.applyLifecycleWithWarning(`hotkey-rollback`),
          this.createHotkeyWindowMutationFailure(e instanceof Error ? e.message : String(e))
        );
      }
      return (
        this.options.globalState.set(
          `hotkeyWindowHotkey`,
          this.configuredHotkeyWindowHotkey ?? void 0,
        ),
        { success: !0, state: this.getHotkeyWindowHotkeyState() }
      );
    }
    setHotkeyWindowDevHotkeyOverride(e) {
      if (!this.options.isDevMode)
        return this.createHotkeyWindowMutationFailure(
          `Dev override is only available in unpackaged builds.`,
        );
      if (this.devHotkeyWindowHotkeyOverrideEnabled === e)
        return { success: !0, state: this.getHotkeyWindowHotkeyState() };
      let t = this.devHotkeyWindowHotkeyOverrideEnabled;
      this.devHotkeyWindowHotkeyOverrideEnabled = e;
      try {
        this.applyLifecycleOrThrow();
      } catch (e) {
        return (
          (this.devHotkeyWindowHotkeyOverrideEnabled = t),
          this.applyLifecycleWithWarning(`dev-override-rollback`),
          this.createHotkeyWindowMutationFailure(e instanceof Error ? e.message : String(e))
        );
      }
      return { success: !0, state: this.getHotkeyWindowHotkeyState() };
    }
    getHotkeyWindowController() {
      return this.hotkeyWindowController;
    }
    shouldHideHotkeyWindowOnToggle(e) {
      let t = e.getVisibleWindowWebContentsId();
      return t == null || this.lastBlurredWindowWebContentsId !== t
        ? !1
        : Date.now() - this.lastBlurredAtMs <= s3;
    }
    recordBlur(e) {
      ((this.lastBlurredWindowWebContentsId = e), (this.lastBlurredAtMs = Date.now()));
    }
    clearRecentBlur() {
      ((this.lastBlurredWindowWebContentsId = null), (this.lastBlurredAtMs = 0));
    }
  };
function l3(e) {
  return { focusComposerNonce: Date.now(), ...(e == null ? {} : { prefillCwd: e }) };
}
function u3(e) {
  return Number.isInteger(e)
    ? e >= 4352 &&
        (e <= 4447 ||
          e === 9001 ||
          e === 9002 ||
          (11904 <= e && e <= 12871 && e !== 12351) ||
          (12880 <= e && e <= 19903) ||
          (19968 <= e && e <= 42182) ||
          (43360 <= e && e <= 43388) ||
          (44032 <= e && e <= 55203) ||
          (63744 <= e && e <= 64255) ||
          (65040 <= e && e <= 65049) ||
          (65072 <= e && e <= 65131) ||
          (65281 <= e && e <= 65376) ||
          (65504 <= e && e <= 65510) ||
          (110592 <= e && e <= 110593) ||
          (127488 <= e && e <= 127569) ||
          (131072 <= e && e <= 262141))
    : !1;
}
var d3 = 10,
  f3 =
    (e = 0) =>
    (t) =>
      `\u001B[${t + e}m`,
  p3 =
    (e = 0) =>
    (t) =>
      `\u001B[${38 + e};5;${t}m`,
  m3 =
    (e = 0) =>
    (t, n, r) =>
      `\u001B[${38 + e};2;${t};${n};${r}m`,
  h3 = {
    modifier: {
      reset: [0, 0],
      bold: [1, 22],
      dim: [2, 22],
      italic: [3, 23],
      underline: [4, 24],
      overline: [53, 55],
      inverse: [7, 27],
      hidden: [8, 28],
      strikethrough: [9, 29],
    },
    color: {
      black: [30, 39],
      red: [31, 39],
      green: [32, 39],
      yellow: [33, 39],
      blue: [34, 39],
      magenta: [35, 39],
      cyan: [36, 39],
      white: [37, 39],
      blackBright: [90, 39],
      gray: [90, 39],
      grey: [90, 39],
      redBright: [91, 39],
      greenBright: [92, 39],
      yellowBright: [93, 39],
      blueBright: [94, 39],
      magentaBright: [95, 39],
      cyanBright: [96, 39],
      whiteBright: [97, 39],
    },
    bgColor: {
      bgBlack: [40, 49],
      bgRed: [41, 49],
      bgGreen: [42, 49],
      bgYellow: [43, 49],
      bgBlue: [44, 49],
      bgMagenta: [45, 49],
      bgCyan: [46, 49],
      bgWhite: [47, 49],
      bgBlackBright: [100, 49],
      bgGray: [100, 49],
      bgGrey: [100, 49],
      bgRedBright: [101, 49],
      bgGreenBright: [102, 49],
      bgYellowBright: [103, 49],
      bgBlueBright: [104, 49],
      bgMagentaBright: [105, 49],
      bgCyanBright: [106, 49],
      bgWhiteBright: [107, 49],
    },
  };
Object.keys(h3.modifier);
var g3 = Object.keys(h3.color),
  _3 = Object.keys(h3.bgColor);
[...g3, ..._3];
function v3() {
  let e = new Map();
  for (let [t, n] of Object.entries(h3)) {
    for (let [t, r] of Object.entries(n))
      ((h3[t] = { open: `\u001B[${r[0]}m`, close: `\u001B[${r[1]}m` }),
        (n[t] = h3[t]),
        e.set(r[0], r[1]));
    Object.defineProperty(h3, t, { value: n, enumerable: !1 });
  }
  return (
    Object.defineProperty(h3, `codes`, { value: e, enumerable: !1 }),
    (h3.color.close = `\x1B[39m`),
    (h3.bgColor.close = `\x1B[49m`),
    (h3.color.ansi = f3()),
    (h3.color.ansi256 = p3()),
    (h3.color.ansi16m = m3()),
    (h3.bgColor.ansi = f3(d3)),
    (h3.bgColor.ansi256 = p3(d3)),
    (h3.bgColor.ansi16m = m3(d3)),
    Object.defineProperties(h3, {
      rgbToAnsi256: {
        value(e, t, n) {
          return e === t && t === n
            ? e < 8
              ? 16
              : e > 248
                ? 231
                : Math.round(((e - 8) / 247) * 24) + 232
            : 16 +
                36 * Math.round((e / 255) * 5) +
                6 * Math.round((t / 255) * 5) +
                Math.round((n / 255) * 5);
        },
        enumerable: !1,
      },
      hexToRgb: {
        value(e) {
          let t = /[a-f\d]{6}|[a-f\d]{3}/i.exec(e.toString(16));
          if (!t) return [0, 0, 0];
          let [n] = t;
          n.length === 3 && (n = [...n].map((e) => e + e).join(``));
          let r = Number.parseInt(n, 16);
          return [(r >> 16) & 255, (r >> 8) & 255, r & 255];
        },
        enumerable: !1,
      },
      hexToAnsi256: { value: (e) => h3.rgbToAnsi256(...h3.hexToRgb(e)), enumerable: !1 },
      ansi256ToAnsi: {
        value(e) {
          if (e < 8) return 30 + e;
          if (e < 16) return 90 + (e - 8);
          let t, n, r;
          if (e >= 232) ((t = ((e - 232) * 10 + 8) / 255), (n = t), (r = t));
          else {
            e -= 16;
            let i = e % 36;
            ((t = Math.floor(e / 36) / 5), (n = Math.floor(i / 6) / 5), (r = (i % 6) / 5));
          }
          let i = Math.max(t, n, r) * 2;
          if (i === 0) return 30;
          let a = 30 + ((Math.round(r) << 2) | (Math.round(n) << 1) | Math.round(t));
          return (i === 2 && (a += 60), a);
        },
        enumerable: !1,
      },
      rgbToAnsi: { value: (e, t, n) => h3.ansi256ToAnsi(h3.rgbToAnsi256(e, t, n)), enumerable: !1 },
      hexToAnsi: { value: (e) => h3.ansi256ToAnsi(h3.hexToAnsi256(e)), enumerable: !1 },
    }),
    h3
  );
}
var y3 = v3(),
  b3 = /^[\uD800-\uDBFF][\uDC00-\uDFFF]$/,
  x3 = [`\x1B`, ``],
  S3 = (e) => `${x3[0]}[${e}m`,
  C3 = (e, t, n) => {
    let r = [];
    e = [...e];
    for (let n of e) {
      let i = n;
      n.includes(`;`) && (n = n.split(`;`)[0][0] + `0`);
      let a = y3.codes.get(Number.parseInt(n, 10));
      if (a) {
        let n = e.indexOf(a.toString());
        n === -1 ? r.push(S3(t ? a : i)) : e.splice(n, 1);
      } else if (t) {
        r.push(S3(0));
        break;
      } else r.push(S3(i));
    }
    if (t && ((r = r.filter((e, t) => r.indexOf(e) === t)), n !== void 0)) {
      let e = S3(y3.codes.get(Number.parseInt(n, 10)));
      r = r.reduce((t, n) => (n === e ? [n, ...t] : [...t, n]), []);
    }
    return r.join(``);
  };
function w3(e, t, n) {
  let r = [...e],
    i = [],
    a = typeof n == `number` ? n : r.length,
    o = !1,
    s,
    c = 0,
    l = ``;
  for (let [u, d] of r.entries()) {
    let r = !1;
    if (x3.includes(d)) {
      let t = /\d[^m]*/.exec(e.slice(u, u + 18));
      ((s = t && t.length > 0 ? t[0] : void 0), c < a && ((o = !0), s !== void 0 && i.push(s)));
    } else o && d === `m` && ((o = !1), (r = !0));
    if (
      (!o && !r && c++,
      !b3.test(d) && u3(d.codePointAt()) && (c++, typeof n != `number` && a++),
      c > t && c <= a)
    )
      l += d;
    else if (c === t && !o && s !== void 0) l = C3(i);
    else if (c >= a) {
      l += C3(i, !0, s);
      break;
    }
  }
  return l;
}
function T3({ onlyFirst: e = !1 } = {}) {
  return RegExp(
    `(?:\\u001B\\][\\s\\S]*?(?:\\u0007|\\u001B\\u005C|\\u009C))|[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]`,
    e ? void 0 : `g`,
  );
}
var E3 = T3();
function D3(e) {
  if (typeof e != `string`) throw TypeError(`Expected a \`string\`, got \`${typeof e}\``);
  return e.replace(E3, ``);
}
function O3(e) {
  return (
    e === 161 ||
    e === 164 ||
    e === 167 ||
    e === 168 ||
    e === 170 ||
    e === 173 ||
    e === 174 ||
    (e >= 176 && e <= 180) ||
    (e >= 182 && e <= 186) ||
    (e >= 188 && e <= 191) ||
    e === 198 ||
    e === 208 ||
    e === 215 ||
    e === 216 ||
    (e >= 222 && e <= 225) ||
    e === 230 ||
    (e >= 232 && e <= 234) ||
    e === 236 ||
    e === 237 ||
    e === 240 ||
    e === 242 ||
    e === 243 ||
    (e >= 247 && e <= 250) ||
    e === 252 ||
    e === 254 ||
    e === 257 ||
    e === 273 ||
    e === 275 ||
    e === 283 ||
    e === 294 ||
    e === 295 ||
    e === 299 ||
    (e >= 305 && e <= 307) ||
    e === 312 ||
    (e >= 319 && e <= 322) ||
    e === 324 ||
    (e >= 328 && e <= 331) ||
    e === 333 ||
    e === 338 ||
    e === 339 ||
    e === 358 ||
    e === 359 ||
    e === 363 ||
    e === 462 ||
    e === 464 ||
    e === 466 ||
    e === 468 ||
    e === 470 ||
    e === 472 ||
    e === 474 ||
    e === 476 ||
    e === 593 ||
    e === 609 ||
    e === 708 ||
    e === 711 ||
    (e >= 713 && e <= 715) ||
    e === 717 ||
    e === 720 ||
    (e >= 728 && e <= 731) ||
    e === 733 ||
    e === 735 ||
    (e >= 768 && e <= 879) ||
    (e >= 913 && e <= 929) ||
    (e >= 931 && e <= 937) ||
    (e >= 945 && e <= 961) ||
    (e >= 963 && e <= 969) ||
    e === 1025 ||
    (e >= 1040 && e <= 1103) ||
    e === 1105 ||
    e === 8208 ||
    (e >= 8211 && e <= 8214) ||
    e === 8216 ||
    e === 8217 ||
    e === 8220 ||
    e === 8221 ||
    (e >= 8224 && e <= 8226) ||
    (e >= 8228 && e <= 8231) ||
    e === 8240 ||
    e === 8242 ||
    e === 8243 ||
    e === 8245 ||
    e === 8251 ||
    e === 8254 ||
    e === 8308 ||
    e === 8319 ||
    (e >= 8321 && e <= 8324) ||
    e === 8364 ||
    e === 8451 ||
    e === 8453 ||
    e === 8457 ||
    e === 8467 ||
    e === 8470 ||
    e === 8481 ||
    e === 8482 ||
    e === 8486 ||
    e === 8491 ||
    e === 8531 ||
    e === 8532 ||
    (e >= 8539 && e <= 8542) ||
    (e >= 8544 && e <= 8555) ||
    (e >= 8560 && e <= 8569) ||
    e === 8585 ||
    (e >= 8592 && e <= 8601) ||
    e === 8632 ||
    e === 8633 ||
    e === 8658 ||
    e === 8660 ||
    e === 8679 ||
    e === 8704 ||
    e === 8706 ||
    e === 8707 ||
    e === 8711 ||
    e === 8712 ||
    e === 8715 ||
    e === 8719 ||
    e === 8721 ||
    e === 8725 ||
    e === 8730 ||
    (e >= 8733 && e <= 8736) ||
    e === 8739 ||
    e === 8741 ||
    (e >= 8743 && e <= 8748) ||
    e === 8750 ||
    (e >= 8756 && e <= 8759) ||
    e === 8764 ||
    e === 8765 ||
    e === 8776 ||
    e === 8780 ||
    e === 8786 ||
    e === 8800 ||
    e === 8801 ||
    (e >= 8804 && e <= 8807) ||
    e === 8810 ||
    e === 8811 ||
    e === 8814 ||
    e === 8815 ||
    e === 8834 ||
    e === 8835 ||
    e === 8838 ||
    e === 8839 ||
    e === 8853 ||
    e === 8857 ||
    e === 8869 ||
    e === 8895 ||
    e === 8978 ||
    (e >= 9312 && e <= 9449) ||
    (e >= 9451 && e <= 9547) ||
    (e >= 9552 && e <= 9587) ||
    (e >= 9600 && e <= 9615) ||
    (e >= 9618 && e <= 9621) ||
    e === 9632 ||
    e === 9633 ||
    (e >= 9635 && e <= 9641) ||
    e === 9650 ||
    e === 9651 ||
    e === 9654 ||
    e === 9655 ||
    e === 9660 ||
    e === 9661 ||
    e === 9664 ||
    e === 9665 ||
    (e >= 9670 && e <= 9672) ||
    e === 9675 ||
    (e >= 9678 && e <= 9681) ||
    (e >= 9698 && e <= 9701) ||
    e === 9711 ||
    e === 9733 ||
    e === 9734 ||
    e === 9737 ||
    e === 9742 ||
    e === 9743 ||
    e === 9756 ||
    e === 9758 ||
    e === 9792 ||
    e === 9794 ||
    e === 9824 ||
    e === 9825 ||
    (e >= 9827 && e <= 9829) ||
    (e >= 9831 && e <= 9834) ||
    e === 9836 ||
    e === 9837 ||
    e === 9839 ||
    e === 9886 ||
    e === 9887 ||
    e === 9919 ||
    (e >= 9926 && e <= 9933) ||
    (e >= 9935 && e <= 9939) ||
    (e >= 9941 && e <= 9953) ||
    e === 9955 ||
    e === 9960 ||
    e === 9961 ||
    (e >= 9963 && e <= 9969) ||
    e === 9972 ||
    (e >= 9974 && e <= 9977) ||
    e === 9979 ||
    e === 9980 ||
    e === 9982 ||
    e === 9983 ||
    e === 10045 ||
    (e >= 10102 && e <= 10111) ||
    (e >= 11094 && e <= 11097) ||
    (e >= 12872 && e <= 12879) ||
    (e >= 57344 && e <= 63743) ||
    (e >= 65024 && e <= 65039) ||
    e === 65533 ||
    (e >= 127232 && e <= 127242) ||
    (e >= 127248 && e <= 127277) ||
    (e >= 127280 && e <= 127337) ||
    (e >= 127344 && e <= 127373) ||
    e === 127375 ||
    e === 127376 ||
    (e >= 127387 && e <= 127404) ||
    (e >= 917760 && e <= 917999) ||
    (e >= 983040 && e <= 1048573) ||
    (e >= 1048576 && e <= 1114109)
  );
}
function k3(e) {
  return e === 12288 || (e >= 65281 && e <= 65376) || (e >= 65504 && e <= 65510);
}
function A3(e) {
  return (
    (e >= 4352 && e <= 4447) ||
    e === 8986 ||
    e === 8987 ||
    e === 9001 ||
    e === 9002 ||
    (e >= 9193 && e <= 9196) ||
    e === 9200 ||
    e === 9203 ||
    e === 9725 ||
    e === 9726 ||
    e === 9748 ||
    e === 9749 ||
    (e >= 9776 && e <= 9783) ||
    (e >= 9800 && e <= 9811) ||
    e === 9855 ||
    (e >= 9866 && e <= 9871) ||
    e === 9875 ||
    e === 9889 ||
    e === 9898 ||
    e === 9899 ||
    e === 9917 ||
    e === 9918 ||
    e === 9924 ||
    e === 9925 ||
    e === 9934 ||
    e === 9940 ||
    e === 9962 ||
    e === 9970 ||
    e === 9971 ||
    e === 9973 ||
    e === 9978 ||
    e === 9981 ||
    e === 9989 ||
    e === 9994 ||
    e === 9995 ||
    e === 10024 ||
    e === 10060 ||
    e === 10062 ||
    (e >= 10067 && e <= 10069) ||
    e === 10071 ||
    (e >= 10133 && e <= 10135) ||
    e === 10160 ||
    e === 10175 ||
    e === 11035 ||
    e === 11036 ||
    e === 11088 ||
    e === 11093 ||
    (e >= 11904 && e <= 11929) ||
    (e >= 11931 && e <= 12019) ||
    (e >= 12032 && e <= 12245) ||
    (e >= 12272 && e <= 12287) ||
    (e >= 12289 && e <= 12350) ||
    (e >= 12353 && e <= 12438) ||
    (e >= 12441 && e <= 12543) ||
    (e >= 12549 && e <= 12591) ||
    (e >= 12593 && e <= 12686) ||
    (e >= 12688 && e <= 12773) ||
    (e >= 12783 && e <= 12830) ||
    (e >= 12832 && e <= 12871) ||
    (e >= 12880 && e <= 42124) ||
    (e >= 42128 && e <= 42182) ||
    (e >= 43360 && e <= 43388) ||
    (e >= 44032 && e <= 55203) ||
    (e >= 63744 && e <= 64255) ||
    (e >= 65040 && e <= 65049) ||
    (e >= 65072 && e <= 65106) ||
    (e >= 65108 && e <= 65126) ||
    (e >= 65128 && e <= 65131) ||
    (e >= 94176 && e <= 94180) ||
    (e >= 94192 && e <= 94198) ||
    (e >= 94208 && e <= 101589) ||
    (e >= 101631 && e <= 101662) ||
    (e >= 101760 && e <= 101874) ||
    (e >= 110576 && e <= 110579) ||
    (e >= 110581 && e <= 110587) ||
    e === 110589 ||
    e === 110590 ||
    (e >= 110592 && e <= 110882) ||
    e === 110898 ||
    (e >= 110928 && e <= 110930) ||
    e === 110933 ||
    (e >= 110948 && e <= 110951) ||
    (e >= 110960 && e <= 111355) ||
    (e >= 119552 && e <= 119638) ||
    (e >= 119648 && e <= 119670) ||
    e === 126980 ||
    e === 127183 ||
    e === 127374 ||
    (e >= 127377 && e <= 127386) ||
    (e >= 127488 && e <= 127490) ||
    (e >= 127504 && e <= 127547) ||
    (e >= 127552 && e <= 127560) ||
    e === 127568 ||
    e === 127569 ||
    (e >= 127584 && e <= 127589) ||
    (e >= 127744 && e <= 127776) ||
    (e >= 127789 && e <= 127797) ||
    (e >= 127799 && e <= 127868) ||
    (e >= 127870 && e <= 127891) ||
    (e >= 127904 && e <= 127946) ||
    (e >= 127951 && e <= 127955) ||
    (e >= 127968 && e <= 127984) ||
    e === 127988 ||
    (e >= 127992 && e <= 128062) ||
    e === 128064 ||
    (e >= 128066 && e <= 128252) ||
    (e >= 128255 && e <= 128317) ||
    (e >= 128331 && e <= 128334) ||
    (e >= 128336 && e <= 128359) ||
    e === 128378 ||
    e === 128405 ||
    e === 128406 ||
    e === 128420 ||
    (e >= 128507 && e <= 128591) ||
    (e >= 128640 && e <= 128709) ||
    e === 128716 ||
    (e >= 128720 && e <= 128722) ||
    (e >= 128725 && e <= 128728) ||
    (e >= 128732 && e <= 128735) ||
    e === 128747 ||
    e === 128748 ||
    (e >= 128756 && e <= 128764) ||
    (e >= 128992 && e <= 129003) ||
    e === 129008 ||
    (e >= 129292 && e <= 129338) ||
    (e >= 129340 && e <= 129349) ||
    (e >= 129351 && e <= 129535) ||
    (e >= 129648 && e <= 129660) ||
    (e >= 129664 && e <= 129674) ||
    (e >= 129678 && e <= 129734) ||
    e === 129736 ||
    (e >= 129741 && e <= 129756) ||
    (e >= 129759 && e <= 129770) ||
    (e >= 129775 && e <= 129784) ||
    (e >= 131072 && e <= 196605) ||
    (e >= 196608 && e <= 262141)
  );
}
function j3(e) {
  if (!Number.isSafeInteger(e)) throw TypeError(`Expected a code point, got \`${typeof e}\`.`);
}
function M3(e, { ambiguousAsWide: t = !1 } = {}) {
  return (j3(e), k3(e) || A3(e) || (t && O3(e)) ? 2 : 1);
}
var N3 = () =>
    /[#*0-9]\uFE0F?\u20E3|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26AA\u26B0\u26B1\u26BD\u26BE\u26C4\u26C8\u26CF\u26D1\u26E9\u26F0-\u26F5\u26F7\u26F8\u26FA\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2757\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B55\u3030\u303D\u3297\u3299]\uFE0F?|[\u261D\u270C\u270D](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\u270A\u270B](?:\uD83C[\uDFFB-\uDFFF])?|[\u23E9-\u23EC\u23F0\u23F3\u25FD\u2693\u26A1\u26AB\u26C5\u26CE\u26D4\u26EA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2795-\u2797\u27B0\u27BF\u2B50]|\u26D3\uFE0F?(?:\u200D\uD83D\uDCA5)?|\u26F9(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\u2764\uFE0F?(?:\u200D(?:\uD83D\uDD25|\uD83E\uDE79))?|\uD83C(?:[\uDC04\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]\uFE0F?|[\uDF85\uDFC2\uDFC7](?:\uD83C[\uDFFB-\uDFFF])?|[\uDFC4\uDFCA](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDFCB\uDFCC](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF43\uDF45-\uDF4A\uDF4C-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uDDE6\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF]|\uDDE7\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF]|\uDDE8\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF7\uDDFA-\uDDFF]|\uDDE9\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF]|\uDDEA\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA]|\uDDEB\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7]|\uDDEC\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE]|\uDDED\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA]|\uDDEE\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9]|\uDDEF\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5]|\uDDF0\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF]|\uDDF1\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE]|\uDDF2\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF]|\uDDF3\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF]|\uDDF4\uD83C\uDDF2|\uDDF5\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE]|\uDDF6\uD83C\uDDE6|\uDDF7\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC]|\uDDF8\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF]|\uDDF9\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF]|\uDDFA\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF]|\uDDFB\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA]|\uDDFC\uD83C[\uDDEB\uDDF8]|\uDDFD\uD83C\uDDF0|\uDDFE\uD83C[\uDDEA\uDDF9]|\uDDFF\uD83C[\uDDE6\uDDF2\uDDFC]|\uDF44(?:\u200D\uD83D\uDFEB)?|\uDF4B(?:\u200D\uD83D\uDFE9)?|\uDFC3(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDFF3\uFE0F?(?:\u200D(?:\u26A7\uFE0F?|\uD83C\uDF08))?|\uDFF4(?:\u200D\u2620\uFE0F?|\uDB40\uDC67\uDB40\uDC62\uDB40(?:\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDC73\uDB40\uDC63\uDB40\uDC74|\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F)?)|\uD83D(?:[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3]\uFE0F?|[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC](?:\uD83C[\uDFFB-\uDFFF])?|[\uDC6E-\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4\uDEB5](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD74\uDD90](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC25\uDC27-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE41\uDE43\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED8\uDEDC-\uDEDF\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB\uDFF0]|\uDC08(?:\u200D\u2B1B)?|\uDC15(?:\u200D\uD83E\uDDBA)?|\uDC26(?:\u200D(?:\u2B1B|\uD83D\uDD25))?|\uDC3B(?:\u200D\u2744\uFE0F?)?|\uDC41\uFE0F?(?:\u200D\uD83D\uDDE8\uFE0F?)?|\uDC68(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDC68\uDC69]\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?))?|\uDC69(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?[\uDC68\uDC69]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?|\uDC69\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?))|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFC-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFD-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFD\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFE]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])))?))?|\uDD75(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDE2E(?:\u200D\uD83D\uDCA8)?|\uDE35(?:\u200D\uD83D\uDCAB)?|\uDE36(?:\u200D\uD83C\uDF2B\uFE0F?)?|\uDE42(?:\u200D[\u2194\u2195]\uFE0F?)?|\uDEB6(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?)|\uD83E(?:[\uDD0C\uDD0F\uDD18-\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5\uDEC3-\uDEC5\uDEF0\uDEF2-\uDEF8](?:\uD83C[\uDFFB-\uDFFF])?|[\uDD26\uDD35\uDD37-\uDD39\uDD3C-\uDD3E\uDDB8\uDDB9\uDDCD\uDDCF\uDDD4\uDDD6-\uDDDD](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDDDE\uDDDF](?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD0D\uDD0E\uDD10-\uDD17\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCC\uDDD0\uDDE0-\uDDFF\uDE70-\uDE7C\uDE80-\uDE8A\uDE8E-\uDEC2\uDEC6\uDEC8\uDECD-\uDEDC\uDEDF-\uDEEA\uDEEF]|\uDDCE(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDDD1(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1|\uDDD1\u200D\uD83E\uDDD2(?:\u200D\uD83E\uDDD2)?|\uDDD2(?:\u200D\uD83E\uDDD2)?))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])))?))?|\uDEF1(?:\uD83C(?:\uDFFB(?:\u200D\uD83E\uDEF2\uD83C[\uDFFC-\uDFFF])?|\uDFFC(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFD-\uDFFF])?|\uDFFD(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])?|\uDFFE(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFD\uDFFF])?|\uDFFF(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFE])?))?)/g,
  P3 = new Intl.Segmenter(),
  F3 = /^\p{Default_Ignorable_Code_Point}$/u;
function I3(e, t = {}) {
  if (typeof e != `string` || e.length === 0) return 0;
  let { ambiguousIsNarrow: n = !0, countAnsiEscapeCodes: r = !1 } = t;
  if ((r || (e = D3(e)), e.length === 0)) return 0;
  let i = 0,
    a = { ambiguousAsWide: !n };
  for (let { segment: t } of P3.segment(e)) {
    let e = t.codePointAt(0);
    if (
      !(e <= 31 || (e >= 127 && e <= 159)) &&
      !((e >= 8203 && e <= 8207) || e === 65279) &&
      !(
        (e >= 768 && e <= 879) ||
        (e >= 6832 && e <= 6911) ||
        (e >= 7616 && e <= 7679) ||
        (e >= 8400 && e <= 8447) ||
        (e >= 65056 && e <= 65071)
      ) &&
      !(e >= 55296 && e <= 57343) &&
      !(e >= 65024 && e <= 65039) &&
      !F3.test(t)
    ) {
      if (N3().test(t)) {
        i += 2;
        continue;
      }
      i += M3(e, a);
    }
  }
  return i;
}
function L3(e, t, n) {
  if (e.charAt(t) === ` `) return t;
  let r = n ? 1 : -1;
  for (let n = 0; n <= 3; n++) {
    let i = t + n * r;
    if (e.charAt(i) === ` `) return i;
  }
  return t;
}
function R3(e, t, n = {}) {
  let { position: r = `end`, space: i = !1, preferTruncationOnSpace: a = !1 } = n,
    { truncationCharacter: o = `…` } = n;
  if (typeof e != `string`) throw TypeError(`Expected \`input\` to be a string, got ${typeof e}`);
  if (typeof t != `number`) throw TypeError(`Expected \`columns\` to be a number, got ${typeof t}`);
  if (t < 1) return ``;
  if (t === 1) return o;
  let s = I3(e);
  if (s <= t) return e;
  if (r === `start`) {
    if (a) {
      let n = L3(e, s - t + 1, !0);
      return o + w3(e, n, s).trim();
    }
    return (i === !0 && (o += ` `), o + w3(e, s - t + I3(o), s));
  }
  if (r === `middle`) {
    i === !0 && (o = ` ${o} `);
    let n = Math.floor(t / 2);
    if (a) {
      let r = L3(e, n),
        i = L3(e, s - (t - n) + 1, !0);
      return w3(e, 0, r) + o + w3(e, i, s).trim();
    }
    return w3(e, 0, n) + o + w3(e, s - (t - n) + I3(o), s);
  }
  if (r === `end`)
    return a ? w3(e, 0, L3(e, t - 1)) + o : (i === !0 && (o = ` ${o}`), w3(e, 0, t - I3(o)) + o);
  throw Error(
    `Expected \`options.position\` to be either \`start\`, \`middle\` or \`end\`, got ${r}`,
  );
}
function z3(e) {
  try {
    return (l.default.accessSync(e), !0);
  } catch {
    return !1;
  }
}
var B3 = class extends Error {
    constructor(e, t) {
      (super(`Max tries reached.`), (this.originalPath = e), (this.lastTriedPath = t));
    }
  },
  V3 = (e, t) => {
    let n = e.match(/^(?<filename>.*)\((?<index>\d+)\)$/),
      { filename: r, index: i } = n ? n.groups : { filename: e, index: 0 };
    return ((r = r.trim()), [`${r}${t}`, `${r} (${++i})${t}`]);
  },
  H3 = (e, t) => {
    let n = s.default.extname(e),
      r = s.default.dirname(e),
      [i, a] = t(s.default.basename(e, n), n);
    return [s.default.join(r, i), s.default.join(r, a)];
  };
function U3(e, { incrementer: t = V3, maxTries: n = 1 / 0 } = {}) {
  let r = 0,
    [i] = H3(e, t),
    a = e;
  for (;;) {
    if (!z3(a)) return a;
    if (++r > n) throw new B3(i, a);
    [i, a] = H3(a, t);
  }
}
var W3 = (e) =>
  e
    .replace(/&/g, `&amp;`)
    .replace(/"/g, `&quot;`)
    .replace(/'/g, `&#39;`)
    .replace(/</g, `&lt;`)
    .replace(/>/g, `&gt;`);
function G3(e, ...t) {
  if (typeof e == `string`) return W3(e);
  let n = e[0];
  for (let [r, i] of t.entries()) n = n + W3(String(i)) + e[r + 1];
  return n;
}
var K3 = class extends Error {
    constructor(e) {
      (super(`Missing a value for ${e ? `the placeholder: ${e}` : `a placeholder`}`, e),
        (this.name = `MissingValueError`),
        (this.key = e));
    }
  },
  q3 = class extends Error {
    constructor(e) {
      (super(`Missing filter: ${e}`), (this.name = `MissingFilterError`), (this.filterName = e));
    }
  };
function J3(
  e,
  t,
  { ignoreMissing: n = !1, transform: r = ({ value: e }) => e, filters: i = {} } = {},
) {
  if (typeof e != `string`)
    throw TypeError(`Expected a \`string\` in the first argument, got \`${typeof e}\``);
  if (typeof t != `object`)
    throw TypeError(
      `Expected an \`object\` or \`Array\` in the second argument, got \`${typeof t}\``,
    );
  let a = ``,
    o = ``;
  ((e = e.replace(/\\{/g, a)), (e = e.replace(/\\}/g, o)));
  let s = (e) => {
      let t = [],
        n = ``;
      for (let r = 0; r < e.length; r++)
        e[r] === `\\` && e[r + 1] === `.`
          ? ((n += `.`), r++)
          : e[r] === `.`
            ? (t.push(n), (n = ``))
            : (n += e[r]);
      return (t.push(n), t);
    },
    c = (e, a) => {
      let o = a.split(`|`).map((e) => e.trim()),
        c = o[0],
        l = o.slice(1),
        u = s(c),
        d = t;
      for (let e of u) d &&= d[e];
      for (let t of l) {
        let r = i[t];
        if (!r) {
          if (n) return e;
          throw new q3(t);
        }
        d !== void 0 && (d = r(d));
      }
      let f = r({ value: d, key: c });
      if (f === void 0) {
        if (n) return e;
        throw new K3(c);
      }
      return String(f);
    },
    l = `((\\d+|[a-z$_][\\w\\-.$\\\\]*)\\s*(?:\\|\\s*[a-z$_][\\w$]*\\s*)*)`,
    u = RegExp(`{{${l}}}`, `gi`),
    d = RegExp(`{${l}}`, `gi`);
  return (
    (e = e.replace(u, (...e) => G3(c(...e)))),
    (e = e.replace(d, c)),
    (e = e.replace(new RegExp(a, `g`), `{`)),
    (e = e.replace(new RegExp(o, `g`), `}`)),
    e
  );
}
var Y3 = e.i({ default: () => X3 }),
  X3,
  Z3 = e.r(() => {
    X3 = {
      "application/1d-interleaved-parityfec": { source: `iana` },
      "application/3gpdash-qoe-report+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/3gpp-ims+xml": { source: `iana`, compressible: !0 },
      "application/3gpphal+json": { source: `iana`, compressible: !0 },
      "application/3gpphalforms+json": { source: `iana`, compressible: !0 },
      "application/a2l": { source: `iana` },
      "application/ace+cbor": { source: `iana` },
      "application/ace+json": { source: `iana`, compressible: !0 },
      "application/ace-groupcomm+cbor": { source: `iana` },
      "application/ace-trl+cbor": { source: `iana` },
      "application/activemessage": { source: `iana` },
      "application/activity+json": { source: `iana`, compressible: !0 },
      "application/aif+cbor": { source: `iana` },
      "application/aif+json": { source: `iana`, compressible: !0 },
      "application/alto-cdni+json": { source: `iana`, compressible: !0 },
      "application/alto-cdnifilter+json": { source: `iana`, compressible: !0 },
      "application/alto-costmap+json": { source: `iana`, compressible: !0 },
      "application/alto-costmapfilter+json": { source: `iana`, compressible: !0 },
      "application/alto-directory+json": { source: `iana`, compressible: !0 },
      "application/alto-endpointcost+json": { source: `iana`, compressible: !0 },
      "application/alto-endpointcostparams+json": { source: `iana`, compressible: !0 },
      "application/alto-endpointprop+json": { source: `iana`, compressible: !0 },
      "application/alto-endpointpropparams+json": { source: `iana`, compressible: !0 },
      "application/alto-error+json": { source: `iana`, compressible: !0 },
      "application/alto-networkmap+json": { source: `iana`, compressible: !0 },
      "application/alto-networkmapfilter+json": { source: `iana`, compressible: !0 },
      "application/alto-propmap+json": { source: `iana`, compressible: !0 },
      "application/alto-propmapparams+json": { source: `iana`, compressible: !0 },
      "application/alto-tips+json": { source: `iana`, compressible: !0 },
      "application/alto-tipsparams+json": { source: `iana`, compressible: !0 },
      "application/alto-updatestreamcontrol+json": { source: `iana`, compressible: !0 },
      "application/alto-updatestreamparams+json": { source: `iana`, compressible: !0 },
      "application/aml": { source: `iana` },
      "application/andrew-inset": { source: `iana`, extensions: [`ez`] },
      "application/appinstaller": { compressible: !1, extensions: [`appinstaller`] },
      "application/applefile": { source: `iana` },
      "application/applixware": { source: `apache`, extensions: [`aw`] },
      "application/appx": { compressible: !1, extensions: [`appx`] },
      "application/appxbundle": { compressible: !1, extensions: [`appxbundle`] },
      "application/at+jwt": { source: `iana` },
      "application/atf": { source: `iana` },
      "application/atfx": { source: `iana` },
      "application/atom+xml": { source: `iana`, compressible: !0, extensions: [`atom`] },
      "application/atomcat+xml": { source: `iana`, compressible: !0, extensions: [`atomcat`] },
      "application/atomdeleted+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`atomdeleted`],
      },
      "application/atomicmail": { source: `iana` },
      "application/atomsvc+xml": { source: `iana`, compressible: !0, extensions: [`atomsvc`] },
      "application/atsc-dwd+xml": { source: `iana`, compressible: !0, extensions: [`dwd`] },
      "application/atsc-dynamic-event-message": { source: `iana` },
      "application/atsc-held+xml": { source: `iana`, compressible: !0, extensions: [`held`] },
      "application/atsc-rdt+json": { source: `iana`, compressible: !0 },
      "application/atsc-rsat+xml": { source: `iana`, compressible: !0, extensions: [`rsat`] },
      "application/atxml": { source: `iana` },
      "application/auth-policy+xml": { source: `iana`, compressible: !0 },
      "application/automationml-aml+xml": { source: `iana`, compressible: !0, extensions: [`aml`] },
      "application/automationml-amlx+zip": {
        source: `iana`,
        compressible: !1,
        extensions: [`amlx`],
      },
      "application/bacnet-xdd+zip": { source: `iana`, compressible: !1 },
      "application/batch-smtp": { source: `iana` },
      "application/bdoc": { compressible: !1, extensions: [`bdoc`] },
      "application/beep+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/bufr": { source: `iana` },
      "application/c2pa": { source: `iana` },
      "application/calendar+json": { source: `iana`, compressible: !0 },
      "application/calendar+xml": { source: `iana`, compressible: !0, extensions: [`xcs`] },
      "application/call-completion": { source: `iana` },
      "application/cals-1840": { source: `iana` },
      "application/captive+json": { source: `iana`, compressible: !0 },
      "application/cbor": { source: `iana` },
      "application/cbor-seq": { source: `iana` },
      "application/cccex": { source: `iana` },
      "application/ccmp+xml": { source: `iana`, compressible: !0 },
      "application/ccxml+xml": { source: `iana`, compressible: !0, extensions: [`ccxml`] },
      "application/cda+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/cdfx+xml": { source: `iana`, compressible: !0, extensions: [`cdfx`] },
      "application/cdmi-capability": { source: `iana`, extensions: [`cdmia`] },
      "application/cdmi-container": { source: `iana`, extensions: [`cdmic`] },
      "application/cdmi-domain": { source: `iana`, extensions: [`cdmid`] },
      "application/cdmi-object": { source: `iana`, extensions: [`cdmio`] },
      "application/cdmi-queue": { source: `iana`, extensions: [`cdmiq`] },
      "application/cdni": { source: `iana` },
      "application/ce+cbor": { source: `iana` },
      "application/cea": { source: `iana` },
      "application/cea-2018+xml": { source: `iana`, compressible: !0 },
      "application/cellml+xml": { source: `iana`, compressible: !0 },
      "application/cfw": { source: `iana` },
      "application/cid-edhoc+cbor-seq": { source: `iana` },
      "application/city+json": { source: `iana`, compressible: !0 },
      "application/city+json-seq": { source: `iana` },
      "application/clr": { source: `iana` },
      "application/clue+xml": { source: `iana`, compressible: !0 },
      "application/clue_info+xml": { source: `iana`, compressible: !0 },
      "application/cms": { source: `iana` },
      "application/cnrp+xml": { source: `iana`, compressible: !0 },
      "application/coap-eap": { source: `iana` },
      "application/coap-group+json": { source: `iana`, compressible: !0 },
      "application/coap-payload": { source: `iana` },
      "application/commonground": { source: `iana` },
      "application/concise-problem-details+cbor": { source: `iana` },
      "application/conference-info+xml": { source: `iana`, compressible: !0 },
      "application/cose": { source: `iana` },
      "application/cose-key": { source: `iana` },
      "application/cose-key-set": { source: `iana` },
      "application/cose-x509": { source: `iana` },
      "application/cpl+xml": { source: `iana`, compressible: !0, extensions: [`cpl`] },
      "application/csrattrs": { source: `iana` },
      "application/csta+xml": { source: `iana`, compressible: !0 },
      "application/cstadata+xml": { source: `iana`, compressible: !0 },
      "application/csvm+json": { source: `iana`, compressible: !0 },
      "application/cu-seeme": { source: `apache`, extensions: [`cu`] },
      "application/cwl": { source: `iana`, extensions: [`cwl`] },
      "application/cwl+json": { source: `iana`, compressible: !0 },
      "application/cwl+yaml": { source: `iana` },
      "application/cwt": { source: `iana` },
      "application/cybercash": { source: `iana` },
      "application/dart": { compressible: !0 },
      "application/dash+xml": { source: `iana`, compressible: !0, extensions: [`mpd`] },
      "application/dash-patch+xml": { source: `iana`, compressible: !0, extensions: [`mpp`] },
      "application/dashdelta": { source: `iana` },
      "application/davmount+xml": { source: `iana`, compressible: !0, extensions: [`davmount`] },
      "application/dca-rft": { source: `iana` },
      "application/dcd": { source: `iana` },
      "application/dec-dx": { source: `iana` },
      "application/dialog-info+xml": { source: `iana`, compressible: !0 },
      "application/dicom": { source: `iana`, extensions: [`dcm`] },
      "application/dicom+json": { source: `iana`, compressible: !0 },
      "application/dicom+xml": { source: `iana`, compressible: !0 },
      "application/dii": { source: `iana` },
      "application/dit": { source: `iana` },
      "application/dns": { source: `iana` },
      "application/dns+json": { source: `iana`, compressible: !0 },
      "application/dns-message": { source: `iana` },
      "application/docbook+xml": { source: `apache`, compressible: !0, extensions: [`dbk`] },
      "application/dots+cbor": { source: `iana` },
      "application/dpop+jwt": { source: `iana` },
      "application/dskpp+xml": { source: `iana`, compressible: !0 },
      "application/dssc+der": { source: `iana`, extensions: [`dssc`] },
      "application/dssc+xml": { source: `iana`, compressible: !0, extensions: [`xdssc`] },
      "application/dvcs": { source: `iana` },
      "application/eat+cwt": { source: `iana` },
      "application/eat+jwt": { source: `iana` },
      "application/eat-bun+cbor": { source: `iana` },
      "application/eat-bun+json": { source: `iana`, compressible: !0 },
      "application/eat-ucs+cbor": { source: `iana` },
      "application/eat-ucs+json": { source: `iana`, compressible: !0 },
      "application/ecmascript": { source: `apache`, compressible: !0, extensions: [`ecma`] },
      "application/edhoc+cbor-seq": { source: `iana` },
      "application/edi-consent": { source: `iana` },
      "application/edi-x12": { source: `iana`, compressible: !1 },
      "application/edifact": { source: `iana`, compressible: !1 },
      "application/efi": { source: `iana` },
      "application/elm+json": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/elm+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.cap+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
      },
      "application/emergencycalldata.comment+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.control+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.deviceinfo+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.ecall.msd": { source: `iana` },
      "application/emergencycalldata.legacyesn+json": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.providerinfo+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.serviceinfo+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.subscriberinfo+xml": { source: `iana`, compressible: !0 },
      "application/emergencycalldata.veds+xml": { source: `iana`, compressible: !0 },
      "application/emma+xml": { source: `iana`, compressible: !0, extensions: [`emma`] },
      "application/emotionml+xml": { source: `iana`, compressible: !0, extensions: [`emotionml`] },
      "application/encaprtp": { source: `iana` },
      "application/entity-statement+jwt": { source: `iana` },
      "application/epp+xml": { source: `iana`, compressible: !0 },
      "application/epub+zip": { source: `iana`, compressible: !1, extensions: [`epub`] },
      "application/eshop": { source: `iana` },
      "application/exi": { source: `iana`, extensions: [`exi`] },
      "application/expect-ct-report+json": { source: `iana`, compressible: !0 },
      "application/express": { source: `iana`, extensions: [`exp`] },
      "application/fastinfoset": { source: `iana` },
      "application/fastsoap": { source: `iana` },
      "application/fdf": { source: `iana`, extensions: [`fdf`] },
      "application/fdt+xml": { source: `iana`, compressible: !0, extensions: [`fdt`] },
      "application/fhir+json": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/fhir+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/fido.trusted-apps+json": { compressible: !0 },
      "application/fits": { source: `iana` },
      "application/flexfec": { source: `iana` },
      "application/font-sfnt": { source: `iana` },
      "application/font-tdpfr": { source: `iana`, extensions: [`pfr`] },
      "application/font-woff": { source: `iana`, compressible: !1 },
      "application/framework-attributes+xml": { source: `iana`, compressible: !0 },
      "application/geo+json": { source: `iana`, compressible: !0, extensions: [`geojson`] },
      "application/geo+json-seq": { source: `iana` },
      "application/geopackage+sqlite3": { source: `iana` },
      "application/geopose+json": { source: `iana`, compressible: !0 },
      "application/geoxacml+json": { source: `iana`, compressible: !0 },
      "application/geoxacml+xml": { source: `iana`, compressible: !0 },
      "application/gltf-buffer": { source: `iana` },
      "application/gml+xml": { source: `iana`, compressible: !0, extensions: [`gml`] },
      "application/gnap-binding-jws": { source: `iana` },
      "application/gnap-binding-jwsd": { source: `iana` },
      "application/gnap-binding-rotation-jws": { source: `iana` },
      "application/gnap-binding-rotation-jwsd": { source: `iana` },
      "application/gpx+xml": { source: `apache`, compressible: !0, extensions: [`gpx`] },
      "application/grib": { source: `iana` },
      "application/gxf": { source: `apache`, extensions: [`gxf`] },
      "application/gzip": { source: `iana`, compressible: !1, extensions: [`gz`] },
      "application/h224": { source: `iana` },
      "application/held+xml": { source: `iana`, compressible: !0 },
      "application/hjson": { extensions: [`hjson`] },
      "application/hl7v2+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/http": { source: `iana` },
      "application/hyperstudio": { source: `iana`, extensions: [`stk`] },
      "application/ibe-key-request+xml": { source: `iana`, compressible: !0 },
      "application/ibe-pkg-reply+xml": { source: `iana`, compressible: !0 },
      "application/ibe-pp-data": { source: `iana` },
      "application/iges": { source: `iana` },
      "application/im-iscomposing+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/index": { source: `iana` },
      "application/index.cmd": { source: `iana` },
      "application/index.obj": { source: `iana` },
      "application/index.response": { source: `iana` },
      "application/index.vnd": { source: `iana` },
      "application/inkml+xml": { source: `iana`, compressible: !0, extensions: [`ink`, `inkml`] },
      "application/iotp": { source: `iana` },
      "application/ipfix": { source: `iana`, extensions: [`ipfix`] },
      "application/ipp": { source: `iana` },
      "application/isup": { source: `iana` },
      "application/its+xml": { source: `iana`, compressible: !0, extensions: [`its`] },
      "application/java-archive": {
        source: `iana`,
        compressible: !1,
        extensions: [`jar`, `war`, `ear`],
      },
      "application/java-serialized-object": {
        source: `apache`,
        compressible: !1,
        extensions: [`ser`],
      },
      "application/java-vm": { source: `apache`, compressible: !1, extensions: [`class`] },
      "application/javascript": {
        source: `apache`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`js`],
      },
      "application/jf2feed+json": { source: `iana`, compressible: !0 },
      "application/jose": { source: `iana` },
      "application/jose+json": { source: `iana`, compressible: !0 },
      "application/jrd+json": { source: `iana`, compressible: !0 },
      "application/jscalendar+json": { source: `iana`, compressible: !0 },
      "application/jscontact+json": { source: `iana`, compressible: !0 },
      "application/json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`json`, `map`],
      },
      "application/json-patch+json": { source: `iana`, compressible: !0 },
      "application/json-seq": { source: `iana` },
      "application/json5": { extensions: [`json5`] },
      "application/jsonml+json": { source: `apache`, compressible: !0, extensions: [`jsonml`] },
      "application/jsonpath": { source: `iana` },
      "application/jwk+json": { source: `iana`, compressible: !0 },
      "application/jwk-set+json": { source: `iana`, compressible: !0 },
      "application/jwk-set+jwt": { source: `iana` },
      "application/jwt": { source: `iana` },
      "application/kpml-request+xml": { source: `iana`, compressible: !0 },
      "application/kpml-response+xml": { source: `iana`, compressible: !0 },
      "application/ld+json": { source: `iana`, compressible: !0, extensions: [`jsonld`] },
      "application/lgr+xml": { source: `iana`, compressible: !0, extensions: [`lgr`] },
      "application/link-format": { source: `iana` },
      "application/linkset": { source: `iana` },
      "application/linkset+json": { source: `iana`, compressible: !0 },
      "application/load-control+xml": { source: `iana`, compressible: !0 },
      "application/logout+jwt": { source: `iana` },
      "application/lost+xml": { source: `iana`, compressible: !0, extensions: [`lostxml`] },
      "application/lostsync+xml": { source: `iana`, compressible: !0 },
      "application/lpf+zip": { source: `iana`, compressible: !1 },
      "application/lxf": { source: `iana` },
      "application/mac-binhex40": { source: `iana`, extensions: [`hqx`] },
      "application/mac-compactpro": { source: `apache`, extensions: [`cpt`] },
      "application/macwriteii": { source: `iana` },
      "application/mads+xml": { source: `iana`, compressible: !0, extensions: [`mads`] },
      "application/manifest+json": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`webmanifest`],
      },
      "application/marc": { source: `iana`, extensions: [`mrc`] },
      "application/marcxml+xml": { source: `iana`, compressible: !0, extensions: [`mrcx`] },
      "application/mathematica": { source: `iana`, extensions: [`ma`, `nb`, `mb`] },
      "application/mathml+xml": { source: `iana`, compressible: !0, extensions: [`mathml`] },
      "application/mathml-content+xml": { source: `iana`, compressible: !0 },
      "application/mathml-presentation+xml": { source: `iana`, compressible: !0 },
      "application/mbms-associated-procedure-description+xml": { source: `iana`, compressible: !0 },
      "application/mbms-deregister+xml": { source: `iana`, compressible: !0 },
      "application/mbms-envelope+xml": { source: `iana`, compressible: !0 },
      "application/mbms-msk+xml": { source: `iana`, compressible: !0 },
      "application/mbms-msk-response+xml": { source: `iana`, compressible: !0 },
      "application/mbms-protection-description+xml": { source: `iana`, compressible: !0 },
      "application/mbms-reception-report+xml": { source: `iana`, compressible: !0 },
      "application/mbms-register+xml": { source: `iana`, compressible: !0 },
      "application/mbms-register-response+xml": { source: `iana`, compressible: !0 },
      "application/mbms-schedule+xml": { source: `iana`, compressible: !0 },
      "application/mbms-user-service-description+xml": { source: `iana`, compressible: !0 },
      "application/mbox": { source: `iana`, extensions: [`mbox`] },
      "application/media-policy-dataset+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpf`],
      },
      "application/media_control+xml": { source: `iana`, compressible: !0 },
      "application/mediaservercontrol+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mscml`],
      },
      "application/merge-patch+json": { source: `iana`, compressible: !0 },
      "application/metalink+xml": { source: `apache`, compressible: !0, extensions: [`metalink`] },
      "application/metalink4+xml": { source: `iana`, compressible: !0, extensions: [`meta4`] },
      "application/mets+xml": { source: `iana`, compressible: !0, extensions: [`mets`] },
      "application/mf4": { source: `iana` },
      "application/mikey": { source: `iana` },
      "application/mipc": { source: `iana` },
      "application/missing-blocks+cbor-seq": { source: `iana` },
      "application/mmt-aei+xml": { source: `iana`, compressible: !0, extensions: [`maei`] },
      "application/mmt-usd+xml": { source: `iana`, compressible: !0, extensions: [`musd`] },
      "application/mods+xml": { source: `iana`, compressible: !0, extensions: [`mods`] },
      "application/moss-keys": { source: `iana` },
      "application/moss-signature": { source: `iana` },
      "application/mosskey-data": { source: `iana` },
      "application/mosskey-request": { source: `iana` },
      "application/mp21": { source: `iana`, extensions: [`m21`, `mp21`] },
      "application/mp4": { source: `iana`, extensions: [`mp4`, `mpg4`, `mp4s`, `m4p`] },
      "application/mpeg4-generic": { source: `iana` },
      "application/mpeg4-iod": { source: `iana` },
      "application/mpeg4-iod-xmt": { source: `iana` },
      "application/mrb-consumer+xml": { source: `iana`, compressible: !0 },
      "application/mrb-publish+xml": { source: `iana`, compressible: !0 },
      "application/msc-ivr+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/msc-mixer+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/msix": { compressible: !1, extensions: [`msix`] },
      "application/msixbundle": { compressible: !1, extensions: [`msixbundle`] },
      "application/msword": { source: `iana`, compressible: !1, extensions: [`doc`, `dot`] },
      "application/mud+json": { source: `iana`, compressible: !0 },
      "application/multipart-core": { source: `iana` },
      "application/mxf": { source: `iana`, extensions: [`mxf`] },
      "application/n-quads": { source: `iana`, extensions: [`nq`] },
      "application/n-triples": { source: `iana`, extensions: [`nt`] },
      "application/nasdata": { source: `iana` },
      "application/news-checkgroups": { source: `iana`, charset: `US-ASCII` },
      "application/news-groupinfo": { source: `iana`, charset: `US-ASCII` },
      "application/news-transmission": { source: `iana` },
      "application/nlsml+xml": { source: `iana`, compressible: !0 },
      "application/node": { source: `iana`, extensions: [`cjs`] },
      "application/nss": { source: `iana` },
      "application/oauth-authz-req+jwt": { source: `iana` },
      "application/oblivious-dns-message": { source: `iana` },
      "application/ocsp-request": { source: `iana` },
      "application/ocsp-response": { source: `iana` },
      "application/octet-stream": {
        source: `iana`,
        compressible: !0,
        extensions: [
          `bin`,
          `dms`,
          `lrf`,
          `mar`,
          `so`,
          `dist`,
          `distz`,
          `pkg`,
          `bpk`,
          `dump`,
          `elc`,
          `deploy`,
          `exe`,
          `dll`,
          `deb`,
          `dmg`,
          `iso`,
          `img`,
          `msi`,
          `msp`,
          `msm`,
          `buffer`,
        ],
      },
      "application/oda": { source: `iana`, extensions: [`oda`] },
      "application/odm+xml": { source: `iana`, compressible: !0 },
      "application/odx": { source: `iana` },
      "application/oebps-package+xml": { source: `iana`, compressible: !0, extensions: [`opf`] },
      "application/ogg": { source: `iana`, compressible: !1, extensions: [`ogx`] },
      "application/ohttp-keys": { source: `iana` },
      "application/omdoc+xml": { source: `apache`, compressible: !0, extensions: [`omdoc`] },
      "application/onenote": {
        source: `apache`,
        extensions: [`onetoc`, `onetoc2`, `onetmp`, `onepkg`, `one`, `onea`],
      },
      "application/opc-nodeset+xml": { source: `iana`, compressible: !0 },
      "application/oscore": { source: `iana` },
      "application/oxps": { source: `iana`, extensions: [`oxps`] },
      "application/p21": { source: `iana` },
      "application/p21+zip": { source: `iana`, compressible: !1 },
      "application/p2p-overlay+xml": { source: `iana`, compressible: !0, extensions: [`relo`] },
      "application/parityfec": { source: `iana` },
      "application/passport": { source: `iana` },
      "application/patch-ops-error+xml": { source: `iana`, compressible: !0, extensions: [`xer`] },
      "application/pdf": { source: `iana`, compressible: !1, extensions: [`pdf`] },
      "application/pdx": { source: `iana` },
      "application/pem-certificate-chain": { source: `iana` },
      "application/pgp-encrypted": { source: `iana`, compressible: !1, extensions: [`pgp`] },
      "application/pgp-keys": { source: `iana`, extensions: [`asc`] },
      "application/pgp-signature": { source: `iana`, extensions: [`sig`, `asc`] },
      "application/pics-rules": { source: `apache`, extensions: [`prf`] },
      "application/pidf+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/pidf-diff+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/pkcs10": { source: `iana`, extensions: [`p10`] },
      "application/pkcs12": { source: `iana` },
      "application/pkcs7-mime": { source: `iana`, extensions: [`p7m`, `p7c`] },
      "application/pkcs7-signature": { source: `iana`, extensions: [`p7s`] },
      "application/pkcs8": { source: `iana`, extensions: [`p8`] },
      "application/pkcs8-encrypted": { source: `iana` },
      "application/pkix-attr-cert": { source: `iana`, extensions: [`ac`] },
      "application/pkix-cert": { source: `iana`, extensions: [`cer`] },
      "application/pkix-crl": { source: `iana`, extensions: [`crl`] },
      "application/pkix-pkipath": { source: `iana`, extensions: [`pkipath`] },
      "application/pkixcmp": { source: `iana`, extensions: [`pki`] },
      "application/pls+xml": { source: `iana`, compressible: !0, extensions: [`pls`] },
      "application/poc-settings+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/postscript": {
        source: `iana`,
        compressible: !0,
        extensions: [`ai`, `eps`, `ps`],
      },
      "application/ppsp-tracker+json": { source: `iana`, compressible: !0 },
      "application/private-token-issuer-directory": { source: `iana` },
      "application/private-token-request": { source: `iana` },
      "application/private-token-response": { source: `iana` },
      "application/problem+json": { source: `iana`, compressible: !0 },
      "application/problem+xml": { source: `iana`, compressible: !0 },
      "application/provenance+xml": { source: `iana`, compressible: !0, extensions: [`provx`] },
      "application/provided-claims+jwt": { source: `iana` },
      "application/prs.alvestrand.titrax-sheet": { source: `iana` },
      "application/prs.cww": { source: `iana`, extensions: [`cww`] },
      "application/prs.cyn": { source: `iana`, charset: `7-BIT` },
      "application/prs.hpub+zip": { source: `iana`, compressible: !1 },
      "application/prs.implied-document+xml": { source: `iana`, compressible: !0 },
      "application/prs.implied-executable": { source: `iana` },
      "application/prs.implied-object+json": { source: `iana`, compressible: !0 },
      "application/prs.implied-object+json-seq": { source: `iana` },
      "application/prs.implied-object+yaml": { source: `iana` },
      "application/prs.implied-structure": { source: `iana` },
      "application/prs.mayfile": { source: `iana` },
      "application/prs.nprend": { source: `iana` },
      "application/prs.plucker": { source: `iana` },
      "application/prs.rdf-xml-crypt": { source: `iana` },
      "application/prs.vcfbzip2": { source: `iana` },
      "application/prs.xsf+xml": { source: `iana`, compressible: !0, extensions: [`xsf`] },
      "application/pskc+xml": { source: `iana`, compressible: !0, extensions: [`pskcxml`] },
      "application/pvd+json": { source: `iana`, compressible: !0 },
      "application/qsig": { source: `iana` },
      "application/raml+yaml": { compressible: !0, extensions: [`raml`] },
      "application/raptorfec": { source: `iana` },
      "application/rdap+json": { source: `iana`, compressible: !0 },
      "application/rdf+xml": { source: `iana`, compressible: !0, extensions: [`rdf`, `owl`] },
      "application/reginfo+xml": { source: `iana`, compressible: !0, extensions: [`rif`] },
      "application/relax-ng-compact-syntax": { source: `iana`, extensions: [`rnc`] },
      "application/remote-printing": { source: `apache` },
      "application/reputon+json": { source: `iana`, compressible: !0 },
      "application/resolve-response+jwt": { source: `iana` },
      "application/resource-lists+xml": { source: `iana`, compressible: !0, extensions: [`rl`] },
      "application/resource-lists-diff+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`rld`],
      },
      "application/rfc+xml": { source: `iana`, compressible: !0 },
      "application/riscos": { source: `iana` },
      "application/rlmi+xml": { source: `iana`, compressible: !0 },
      "application/rls-services+xml": { source: `iana`, compressible: !0, extensions: [`rs`] },
      "application/route-apd+xml": { source: `iana`, compressible: !0, extensions: [`rapd`] },
      "application/route-s-tsid+xml": { source: `iana`, compressible: !0, extensions: [`sls`] },
      "application/route-usd+xml": { source: `iana`, compressible: !0, extensions: [`rusd`] },
      "application/rpki-checklist": { source: `iana` },
      "application/rpki-ghostbusters": { source: `iana`, extensions: [`gbr`] },
      "application/rpki-manifest": { source: `iana`, extensions: [`mft`] },
      "application/rpki-publication": { source: `iana` },
      "application/rpki-roa": { source: `iana`, extensions: [`roa`] },
      "application/rpki-signed-tal": { source: `iana` },
      "application/rpki-updown": { source: `iana` },
      "application/rsd+xml": { source: `apache`, compressible: !0, extensions: [`rsd`] },
      "application/rss+xml": { source: `apache`, compressible: !0, extensions: [`rss`] },
      "application/rtf": { source: `iana`, compressible: !0, extensions: [`rtf`] },
      "application/rtploopback": { source: `iana` },
      "application/rtx": { source: `iana` },
      "application/samlassertion+xml": { source: `iana`, compressible: !0 },
      "application/samlmetadata+xml": { source: `iana`, compressible: !0 },
      "application/sarif+json": { source: `iana`, compressible: !0 },
      "application/sarif-external-properties+json": { source: `iana`, compressible: !0 },
      "application/sbe": { source: `iana` },
      "application/sbml+xml": { source: `iana`, compressible: !0, extensions: [`sbml`] },
      "application/scaip+xml": { source: `iana`, compressible: !0 },
      "application/scim+json": { source: `iana`, compressible: !0 },
      "application/scvp-cv-request": { source: `iana`, extensions: [`scq`] },
      "application/scvp-cv-response": { source: `iana`, extensions: [`scs`] },
      "application/scvp-vp-request": { source: `iana`, extensions: [`spq`] },
      "application/scvp-vp-response": { source: `iana`, extensions: [`spp`] },
      "application/sdp": { source: `iana`, extensions: [`sdp`] },
      "application/secevent+jwt": { source: `iana` },
      "application/senml+cbor": { source: `iana` },
      "application/senml+json": { source: `iana`, compressible: !0 },
      "application/senml+xml": { source: `iana`, compressible: !0, extensions: [`senmlx`] },
      "application/senml-etch+cbor": { source: `iana` },
      "application/senml-etch+json": { source: `iana`, compressible: !0 },
      "application/senml-exi": { source: `iana` },
      "application/sensml+cbor": { source: `iana` },
      "application/sensml+json": { source: `iana`, compressible: !0 },
      "application/sensml+xml": { source: `iana`, compressible: !0, extensions: [`sensmlx`] },
      "application/sensml-exi": { source: `iana` },
      "application/sep+xml": { source: `iana`, compressible: !0 },
      "application/sep-exi": { source: `iana` },
      "application/session-info": { source: `iana` },
      "application/set-payment": { source: `iana` },
      "application/set-payment-initiation": { source: `iana`, extensions: [`setpay`] },
      "application/set-registration": { source: `iana` },
      "application/set-registration-initiation": { source: `iana`, extensions: [`setreg`] },
      "application/sgml": { source: `iana` },
      "application/sgml-open-catalog": { source: `iana` },
      "application/shf+xml": { source: `iana`, compressible: !0, extensions: [`shf`] },
      "application/sieve": { source: `iana`, extensions: [`siv`, `sieve`] },
      "application/simple-filter+xml": { source: `iana`, compressible: !0 },
      "application/simple-message-summary": { source: `iana` },
      "application/simplesymbolcontainer": { source: `iana` },
      "application/sipc": { source: `iana` },
      "application/slate": { source: `iana` },
      "application/smil": { source: `apache` },
      "application/smil+xml": { source: `iana`, compressible: !0, extensions: [`smi`, `smil`] },
      "application/smpte336m": { source: `iana` },
      "application/soap+fastinfoset": { source: `iana` },
      "application/soap+xml": { source: `iana`, compressible: !0 },
      "application/sparql-query": { source: `iana`, extensions: [`rq`] },
      "application/sparql-results+xml": { source: `iana`, compressible: !0, extensions: [`srx`] },
      "application/spdx+json": { source: `iana`, compressible: !0 },
      "application/spirits-event+xml": { source: `iana`, compressible: !0 },
      "application/sql": { source: `iana`, extensions: [`sql`] },
      "application/srgs": { source: `iana`, extensions: [`gram`] },
      "application/srgs+xml": { source: `iana`, compressible: !0, extensions: [`grxml`] },
      "application/sru+xml": { source: `iana`, compressible: !0, extensions: [`sru`] },
      "application/ssdl+xml": { source: `apache`, compressible: !0, extensions: [`ssdl`] },
      "application/sslkeylogfile": { source: `iana` },
      "application/ssml+xml": { source: `iana`, compressible: !0, extensions: [`ssml`] },
      "application/st2110-41": { source: `iana` },
      "application/stix+json": { source: `iana`, compressible: !0 },
      "application/stratum": { source: `iana` },
      "application/swid+cbor": { source: `iana` },
      "application/swid+xml": { source: `iana`, compressible: !0, extensions: [`swidtag`] },
      "application/tamp-apex-update": { source: `iana` },
      "application/tamp-apex-update-confirm": { source: `iana` },
      "application/tamp-community-update": { source: `iana` },
      "application/tamp-community-update-confirm": { source: `iana` },
      "application/tamp-error": { source: `iana` },
      "application/tamp-sequence-adjust": { source: `iana` },
      "application/tamp-sequence-adjust-confirm": { source: `iana` },
      "application/tamp-status-query": { source: `iana` },
      "application/tamp-status-response": { source: `iana` },
      "application/tamp-update": { source: `iana` },
      "application/tamp-update-confirm": { source: `iana` },
      "application/tar": { compressible: !0 },
      "application/taxii+json": { source: `iana`, compressible: !0 },
      "application/td+json": { source: `iana`, compressible: !0 },
      "application/tei+xml": { source: `iana`, compressible: !0, extensions: [`tei`, `teicorpus`] },
      "application/tetra_isi": { source: `iana` },
      "application/thraud+xml": { source: `iana`, compressible: !0, extensions: [`tfi`] },
      "application/timestamp-query": { source: `iana` },
      "application/timestamp-reply": { source: `iana` },
      "application/timestamped-data": { source: `iana`, extensions: [`tsd`] },
      "application/tlsrpt+gzip": { source: `iana` },
      "application/tlsrpt+json": { source: `iana`, compressible: !0 },
      "application/tm+json": { source: `iana`, compressible: !0 },
      "application/tnauthlist": { source: `iana` },
      "application/toc+cbor": { source: `iana` },
      "application/token-introspection+jwt": { source: `iana` },
      "application/toml": { source: `iana`, compressible: !0, extensions: [`toml`] },
      "application/trickle-ice-sdpfrag": { source: `iana` },
      "application/trig": { source: `iana`, extensions: [`trig`] },
      "application/trust-chain+json": { source: `iana`, compressible: !0 },
      "application/trust-mark+jwt": { source: `iana` },
      "application/trust-mark-delegation+jwt": { source: `iana` },
      "application/ttml+xml": { source: `iana`, compressible: !0, extensions: [`ttml`] },
      "application/tve-trigger": { source: `iana` },
      "application/tzif": { source: `iana` },
      "application/tzif-leap": { source: `iana` },
      "application/ubjson": { compressible: !1, extensions: [`ubj`] },
      "application/uccs+cbor": { source: `iana` },
      "application/ujcs+json": { source: `iana`, compressible: !0 },
      "application/ulpfec": { source: `iana` },
      "application/urc-grpsheet+xml": { source: `iana`, compressible: !0 },
      "application/urc-ressheet+xml": { source: `iana`, compressible: !0, extensions: [`rsheet`] },
      "application/urc-targetdesc+xml": { source: `iana`, compressible: !0, extensions: [`td`] },
      "application/urc-uisocketdesc+xml": { source: `iana`, compressible: !0 },
      "application/vc": { source: `iana` },
      "application/vc+cose": { source: `iana` },
      "application/vc+jwt": { source: `iana` },
      "application/vcard+json": { source: `iana`, compressible: !0 },
      "application/vcard+xml": { source: `iana`, compressible: !0 },
      "application/vemmi": { source: `iana` },
      "application/vividence.scriptfile": { source: `apache` },
      "application/vnd.1000minds.decision-model+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`1km`],
      },
      "application/vnd.1ob": { source: `iana` },
      "application/vnd.3gpp-prose+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-prose-pc3a+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-prose-pc3ach+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-prose-pc3ch+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-prose-pc8+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp-v2x-local-service-information": { source: `iana` },
      "application/vnd.3gpp.5gnas": { source: `iana` },
      "application/vnd.3gpp.5gsa2x": { source: `iana` },
      "application/vnd.3gpp.5gsa2x-local-service-information": { source: `iana` },
      "application/vnd.3gpp.5gsv2x": { source: `iana` },
      "application/vnd.3gpp.5gsv2x-local-service-information": { source: `iana` },
      "application/vnd.3gpp.access-transfer-events+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.bsf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.crs+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.current-location-discovery+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.gmop+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.gtpc": { source: `iana` },
      "application/vnd.3gpp.interworking-data": { source: `iana` },
      "application/vnd.3gpp.lpp": { source: `iana` },
      "application/vnd.3gpp.mc-signalling-ear": { source: `iana` },
      "application/vnd.3gpp.mcdata-affiliation-command+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-msgstore-ctrl-request+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-payload": { source: `iana` },
      "application/vnd.3gpp.mcdata-regroup+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-service-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-signalling": { source: `iana` },
      "application/vnd.3gpp.mcdata-ue-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcdata-user-profile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-affiliation-command+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-floor-request+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-location-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-mbms-usage-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-regroup+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-service-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-signed+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-ue-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-ue-init-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcptt-user-profile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-affiliation-command+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-location-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-mbms-usage-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-regroup+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-service-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-transmission-request+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-ue-config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mcvideo-user-profile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.mid-call+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.ngap": { source: `iana` },
      "application/vnd.3gpp.pfcp": { source: `iana` },
      "application/vnd.3gpp.pic-bw-large": { source: `iana`, extensions: [`plb`] },
      "application/vnd.3gpp.pic-bw-small": { source: `iana`, extensions: [`psb`] },
      "application/vnd.3gpp.pic-bw-var": { source: `iana`, extensions: [`pvb`] },
      "application/vnd.3gpp.pinapp-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.s1ap": { source: `iana` },
      "application/vnd.3gpp.seal-group-doc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-location-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-mbms-usage-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-network-qos-management-info+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.3gpp.seal-ue-config-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-unicast-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.seal-user-profile-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.sms": { source: `iana` },
      "application/vnd.3gpp.sms+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.srvcc-ext+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.srvcc-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.state-and-event-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.ussd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp.v2x": { source: `iana` },
      "application/vnd.3gpp.vae-info+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp2.bcmcsinfo+xml": { source: `iana`, compressible: !0 },
      "application/vnd.3gpp2.sms": { source: `iana` },
      "application/vnd.3gpp2.tcap": { source: `iana`, extensions: [`tcap`] },
      "application/vnd.3lightssoftware.imagescal": { source: `iana` },
      "application/vnd.3m.post-it-notes": { source: `iana`, extensions: [`pwn`] },
      "application/vnd.accpac.simply.aso": { source: `iana`, extensions: [`aso`] },
      "application/vnd.accpac.simply.imp": { source: `iana`, extensions: [`imp`] },
      "application/vnd.acm.addressxfer+json": { source: `iana`, compressible: !0 },
      "application/vnd.acm.chatbot+json": { source: `iana`, compressible: !0 },
      "application/vnd.acucobol": { source: `iana`, extensions: [`acu`] },
      "application/vnd.acucorp": { source: `iana`, extensions: [`atc`, `acutc`] },
      "application/vnd.adobe.air-application-installer-package+zip": {
        source: `apache`,
        compressible: !1,
        extensions: [`air`],
      },
      "application/vnd.adobe.flash.movie": { source: `iana` },
      "application/vnd.adobe.formscentral.fcdt": { source: `iana`, extensions: [`fcdt`] },
      "application/vnd.adobe.fxp": { source: `iana`, extensions: [`fxp`, `fxpl`] },
      "application/vnd.adobe.partial-upload": { source: `iana` },
      "application/vnd.adobe.xdp+xml": { source: `iana`, compressible: !0, extensions: [`xdp`] },
      "application/vnd.adobe.xfdf": { source: `apache`, extensions: [`xfdf`] },
      "application/vnd.aether.imp": { source: `iana` },
      "application/vnd.afpc.afplinedata": { source: `iana` },
      "application/vnd.afpc.afplinedata-pagedef": { source: `iana` },
      "application/vnd.afpc.cmoca-cmresource": { source: `iana` },
      "application/vnd.afpc.foca-charset": { source: `iana` },
      "application/vnd.afpc.foca-codedfont": { source: `iana` },
      "application/vnd.afpc.foca-codepage": { source: `iana` },
      "application/vnd.afpc.modca": { source: `iana` },
      "application/vnd.afpc.modca-cmtable": { source: `iana` },
      "application/vnd.afpc.modca-formdef": { source: `iana` },
      "application/vnd.afpc.modca-mediummap": { source: `iana` },
      "application/vnd.afpc.modca-objectcontainer": { source: `iana` },
      "application/vnd.afpc.modca-overlay": { source: `iana` },
      "application/vnd.afpc.modca-pagesegment": { source: `iana` },
      "application/vnd.age": { source: `iana`, extensions: [`age`] },
      "application/vnd.ah-barcode": { source: `apache` },
      "application/vnd.ahead.space": { source: `iana`, extensions: [`ahead`] },
      "application/vnd.airzip.filesecure.azf": { source: `iana`, extensions: [`azf`] },
      "application/vnd.airzip.filesecure.azs": { source: `iana`, extensions: [`azs`] },
      "application/vnd.amadeus+json": { source: `iana`, compressible: !0 },
      "application/vnd.amazon.ebook": { source: `apache`, extensions: [`azw`] },
      "application/vnd.amazon.mobi8-ebook": { source: `iana` },
      "application/vnd.americandynamics.acc": { source: `iana`, extensions: [`acc`] },
      "application/vnd.amiga.ami": { source: `iana`, extensions: [`ami`] },
      "application/vnd.amundsen.maze+xml": { source: `iana`, compressible: !0 },
      "application/vnd.android.ota": { source: `iana` },
      "application/vnd.android.package-archive": {
        source: `apache`,
        compressible: !1,
        extensions: [`apk`],
      },
      "application/vnd.anki": { source: `iana` },
      "application/vnd.anser-web-certificate-issue-initiation": {
        source: `iana`,
        extensions: [`cii`],
      },
      "application/vnd.anser-web-funds-transfer-initiation": {
        source: `apache`,
        extensions: [`fti`],
      },
      "application/vnd.antix.game-component": { source: `iana`, extensions: [`atx`] },
      "application/vnd.apache.arrow.file": { source: `iana` },
      "application/vnd.apache.arrow.stream": { source: `iana` },
      "application/vnd.apache.parquet": { source: `iana` },
      "application/vnd.apache.thrift.binary": { source: `iana` },
      "application/vnd.apache.thrift.compact": { source: `iana` },
      "application/vnd.apache.thrift.json": { source: `iana` },
      "application/vnd.apexlang": { source: `iana` },
      "application/vnd.api+json": { source: `iana`, compressible: !0 },
      "application/vnd.aplextor.warrp+json": { source: `iana`, compressible: !0 },
      "application/vnd.apothekende.reservation+json": { source: `iana`, compressible: !0 },
      "application/vnd.apple.installer+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mpkg`],
      },
      "application/vnd.apple.keynote": { source: `iana`, extensions: [`key`] },
      "application/vnd.apple.mpegurl": { source: `iana`, extensions: [`m3u8`] },
      "application/vnd.apple.numbers": { source: `iana`, extensions: [`numbers`] },
      "application/vnd.apple.pages": { source: `iana`, extensions: [`pages`] },
      "application/vnd.apple.pkpass": { compressible: !1, extensions: [`pkpass`] },
      "application/vnd.arastra.swi": { source: `apache` },
      "application/vnd.aristanetworks.swi": { source: `iana`, extensions: [`swi`] },
      "application/vnd.artisan+json": { source: `iana`, compressible: !0 },
      "application/vnd.artsquare": { source: `iana` },
      "application/vnd.astraea-software.iota": { source: `iana`, extensions: [`iota`] },
      "application/vnd.audiograph": { source: `iana`, extensions: [`aep`] },
      "application/vnd.autodesk.fbx": { extensions: [`fbx`] },
      "application/vnd.autopackage": { source: `iana` },
      "application/vnd.avalon+json": { source: `iana`, compressible: !0 },
      "application/vnd.avistar+xml": { source: `iana`, compressible: !0 },
      "application/vnd.balsamiq.bmml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`bmml`],
      },
      "application/vnd.balsamiq.bmpr": { source: `iana` },
      "application/vnd.banana-accounting": { source: `iana` },
      "application/vnd.bbf.usp.error": { source: `iana` },
      "application/vnd.bbf.usp.msg": { source: `iana` },
      "application/vnd.bbf.usp.msg+json": { source: `iana`, compressible: !0 },
      "application/vnd.bekitzur-stech+json": { source: `iana`, compressible: !0 },
      "application/vnd.belightsoft.lhzd+zip": { source: `iana`, compressible: !1 },
      "application/vnd.belightsoft.lhzl+zip": { source: `iana`, compressible: !1 },
      "application/vnd.bint.med-content": { source: `iana` },
      "application/vnd.biopax.rdf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.blink-idb-value-wrapper": { source: `iana` },
      "application/vnd.blueice.multipass": { source: `iana`, extensions: [`mpm`] },
      "application/vnd.bluetooth.ep.oob": { source: `iana` },
      "application/vnd.bluetooth.le.oob": { source: `iana` },
      "application/vnd.bmi": { source: `iana`, extensions: [`bmi`] },
      "application/vnd.bpf": { source: `iana` },
      "application/vnd.bpf3": { source: `iana` },
      "application/vnd.businessobjects": { source: `iana`, extensions: [`rep`] },
      "application/vnd.byu.uapi+json": { source: `iana`, compressible: !0 },
      "application/vnd.bzip3": { source: `iana` },
      "application/vnd.c3voc.schedule+xml": { source: `iana`, compressible: !0 },
      "application/vnd.cab-jscript": { source: `iana` },
      "application/vnd.canon-cpdl": { source: `iana` },
      "application/vnd.canon-lips": { source: `iana` },
      "application/vnd.capasystems-pg+json": { source: `iana`, compressible: !0 },
      "application/vnd.cendio.thinlinc.clientconf": { source: `iana` },
      "application/vnd.century-systems.tcp_stream": { source: `iana` },
      "application/vnd.chemdraw+xml": { source: `iana`, compressible: !0, extensions: [`cdxml`] },
      "application/vnd.chess-pgn": { source: `iana` },
      "application/vnd.chipnuts.karaoke-mmd": { source: `iana`, extensions: [`mmd`] },
      "application/vnd.ciedi": { source: `iana` },
      "application/vnd.cinderella": { source: `iana`, extensions: [`cdy`] },
      "application/vnd.cirpack.isdn-ext": { source: `iana` },
      "application/vnd.citationstyles.style+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`csl`],
      },
      "application/vnd.claymore": { source: `iana`, extensions: [`cla`] },
      "application/vnd.cloanto.rp9": { source: `iana`, extensions: [`rp9`] },
      "application/vnd.clonk.c4group": {
        source: `iana`,
        extensions: [`c4g`, `c4d`, `c4f`, `c4p`, `c4u`],
      },
      "application/vnd.cluetrust.cartomobile-config": { source: `iana`, extensions: [`c11amc`] },
      "application/vnd.cluetrust.cartomobile-config-pkg": {
        source: `iana`,
        extensions: [`c11amz`],
      },
      "application/vnd.cncf.helm.chart.content.v1.tar+gzip": { source: `iana` },
      "application/vnd.cncf.helm.chart.provenance.v1.prov": { source: `iana` },
      "application/vnd.cncf.helm.config.v1+json": { source: `iana`, compressible: !0 },
      "application/vnd.coffeescript": { source: `iana` },
      "application/vnd.collabio.xodocuments.document": { source: `iana` },
      "application/vnd.collabio.xodocuments.document-template": { source: `iana` },
      "application/vnd.collabio.xodocuments.presentation": { source: `iana` },
      "application/vnd.collabio.xodocuments.presentation-template": { source: `iana` },
      "application/vnd.collabio.xodocuments.spreadsheet": { source: `iana` },
      "application/vnd.collabio.xodocuments.spreadsheet-template": { source: `iana` },
      "application/vnd.collection+json": { source: `iana`, compressible: !0 },
      "application/vnd.collection.doc+json": { source: `iana`, compressible: !0 },
      "application/vnd.collection.next+json": { source: `iana`, compressible: !0 },
      "application/vnd.comicbook+zip": { source: `iana`, compressible: !1 },
      "application/vnd.comicbook-rar": { source: `iana` },
      "application/vnd.commerce-battelle": { source: `iana` },
      "application/vnd.commonspace": { source: `iana`, extensions: [`csp`] },
      "application/vnd.contact.cmsg": { source: `iana`, extensions: [`cdbcmsg`] },
      "application/vnd.coreos.ignition+json": { source: `iana`, compressible: !0 },
      "application/vnd.cosmocaller": { source: `iana`, extensions: [`cmc`] },
      "application/vnd.crick.clicker": { source: `iana`, extensions: [`clkx`] },
      "application/vnd.crick.clicker.keyboard": { source: `iana`, extensions: [`clkk`] },
      "application/vnd.crick.clicker.palette": { source: `iana`, extensions: [`clkp`] },
      "application/vnd.crick.clicker.template": { source: `iana`, extensions: [`clkt`] },
      "application/vnd.crick.clicker.wordbank": { source: `iana`, extensions: [`clkw`] },
      "application/vnd.criticaltools.wbs+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`wbs`],
      },
      "application/vnd.cryptii.pipe+json": { source: `iana`, compressible: !0 },
      "application/vnd.crypto-shade-file": { source: `iana` },
      "application/vnd.cryptomator.encrypted": { source: `iana` },
      "application/vnd.cryptomator.vault": { source: `iana` },
      "application/vnd.ctc-posml": { source: `iana`, extensions: [`pml`] },
      "application/vnd.ctct.ws+xml": { source: `iana`, compressible: !0 },
      "application/vnd.cups-pdf": { source: `iana` },
      "application/vnd.cups-postscript": { source: `iana` },
      "application/vnd.cups-ppd": { source: `iana`, extensions: [`ppd`] },
      "application/vnd.cups-raster": { source: `iana` },
      "application/vnd.cups-raw": { source: `iana` },
      "application/vnd.curl": { source: `iana` },
      "application/vnd.curl.car": { source: `apache`, extensions: [`car`] },
      "application/vnd.curl.pcurl": { source: `apache`, extensions: [`pcurl`] },
      "application/vnd.cyan.dean.root+xml": { source: `iana`, compressible: !0 },
      "application/vnd.cybank": { source: `iana` },
      "application/vnd.cyclonedx+json": { source: `iana`, compressible: !0 },
      "application/vnd.cyclonedx+xml": { source: `iana`, compressible: !0 },
      "application/vnd.d2l.coursepackage1p0+zip": { source: `iana`, compressible: !1 },
      "application/vnd.d3m-dataset": { source: `iana` },
      "application/vnd.d3m-problem": { source: `iana` },
      "application/vnd.dart": { source: `iana`, compressible: !0, extensions: [`dart`] },
      "application/vnd.data-vision.rdz": { source: `iana`, extensions: [`rdz`] },
      "application/vnd.datalog": { source: `iana` },
      "application/vnd.datapackage+json": { source: `iana`, compressible: !0 },
      "application/vnd.dataresource+json": { source: `iana`, compressible: !0 },
      "application/vnd.dbf": { source: `iana`, extensions: [`dbf`] },
      "application/vnd.dcmp+xml": { source: `iana`, compressible: !0, extensions: [`dcmp`] },
      "application/vnd.debian.binary-package": { source: `iana` },
      "application/vnd.dece.data": { source: `iana`, extensions: [`uvf`, `uvvf`, `uvd`, `uvvd`] },
      "application/vnd.dece.ttml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`uvt`, `uvvt`],
      },
      "application/vnd.dece.unspecified": { source: `iana`, extensions: [`uvx`, `uvvx`] },
      "application/vnd.dece.zip": { source: `iana`, extensions: [`uvz`, `uvvz`] },
      "application/vnd.denovo.fcselayout-link": { source: `iana`, extensions: [`fe_launch`] },
      "application/vnd.desmume.movie": { source: `iana` },
      "application/vnd.dir-bi.plate-dl-nosuffix": { source: `iana` },
      "application/vnd.dm.delegation+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dna": { source: `iana`, extensions: [`dna`] },
      "application/vnd.document+json": { source: `iana`, compressible: !0 },
      "application/vnd.dolby.mlp": { source: `apache`, extensions: [`mlp`] },
      "application/vnd.dolby.mobile.1": { source: `iana` },
      "application/vnd.dolby.mobile.2": { source: `iana` },
      "application/vnd.doremir.scorecloud-binary-document": { source: `iana` },
      "application/vnd.dpgraph": { source: `iana`, extensions: [`dpg`] },
      "application/vnd.dreamfactory": { source: `iana`, extensions: [`dfac`] },
      "application/vnd.drive+json": { source: `iana`, compressible: !0 },
      "application/vnd.ds-keypoint": { source: `apache`, extensions: [`kpxx`] },
      "application/vnd.dtg.local": { source: `iana` },
      "application/vnd.dtg.local.flash": { source: `iana` },
      "application/vnd.dtg.local.html": { source: `iana` },
      "application/vnd.dvb.ait": { source: `iana`, extensions: [`ait`] },
      "application/vnd.dvb.dvbisl+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.dvbj": { source: `iana` },
      "application/vnd.dvb.esgcontainer": { source: `iana` },
      "application/vnd.dvb.ipdcdftnotifaccess": { source: `iana` },
      "application/vnd.dvb.ipdcesgaccess": { source: `iana` },
      "application/vnd.dvb.ipdcesgaccess2": { source: `iana` },
      "application/vnd.dvb.ipdcesgpdd": { source: `iana` },
      "application/vnd.dvb.ipdcroaming": { source: `iana` },
      "application/vnd.dvb.iptv.alfec-base": { source: `iana` },
      "application/vnd.dvb.iptv.alfec-enhancement": { source: `iana` },
      "application/vnd.dvb.notif-aggregate-root+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.notif-container+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.notif-generic+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.notif-ia-msglist+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.notif-ia-registration-request+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.notif-ia-registration-response+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.dvb.notif-init+xml": { source: `iana`, compressible: !0 },
      "application/vnd.dvb.pfr": { source: `iana` },
      "application/vnd.dvb.service": { source: `iana`, extensions: [`svc`] },
      "application/vnd.dxr": { source: `iana` },
      "application/vnd.dynageo": { source: `iana`, extensions: [`geo`] },
      "application/vnd.dzr": { source: `iana` },
      "application/vnd.easykaraoke.cdgdownload": { source: `iana` },
      "application/vnd.ecdis-update": { source: `iana` },
      "application/vnd.ecip.rlp": { source: `iana` },
      "application/vnd.eclipse.ditto+json": { source: `iana`, compressible: !0 },
      "application/vnd.ecowin.chart": { source: `iana`, extensions: [`mag`] },
      "application/vnd.ecowin.filerequest": { source: `iana` },
      "application/vnd.ecowin.fileupdate": { source: `iana` },
      "application/vnd.ecowin.series": { source: `iana` },
      "application/vnd.ecowin.seriesrequest": { source: `iana` },
      "application/vnd.ecowin.seriesupdate": { source: `iana` },
      "application/vnd.efi.img": { source: `iana` },
      "application/vnd.efi.iso": { source: `iana` },
      "application/vnd.eln+zip": { source: `iana`, compressible: !1 },
      "application/vnd.emclient.accessrequest+xml": { source: `iana`, compressible: !0 },
      "application/vnd.enliven": { source: `iana`, extensions: [`nml`] },
      "application/vnd.enphase.envoy": { source: `iana` },
      "application/vnd.eprints.data+xml": { source: `iana`, compressible: !0 },
      "application/vnd.epson.esf": { source: `iana`, extensions: [`esf`] },
      "application/vnd.epson.msf": { source: `iana`, extensions: [`msf`] },
      "application/vnd.epson.quickanime": { source: `iana`, extensions: [`qam`] },
      "application/vnd.epson.salt": { source: `iana`, extensions: [`slt`] },
      "application/vnd.epson.ssf": { source: `iana`, extensions: [`ssf`] },
      "application/vnd.ericsson.quickcall": { source: `iana` },
      "application/vnd.erofs": { source: `iana` },
      "application/vnd.espass-espass+zip": { source: `iana`, compressible: !1 },
      "application/vnd.eszigno3+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`es3`, `et3`],
      },
      "application/vnd.etsi.aoc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.asic-e+zip": { source: `iana`, compressible: !1 },
      "application/vnd.etsi.asic-s+zip": { source: `iana`, compressible: !1 },
      "application/vnd.etsi.cug+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvcommand+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvdiscovery+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvprofile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvsad-bc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvsad-cod+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvsad-npvr+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvservice+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvsync+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.iptvueprofile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.mcid+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.mheg5": { source: `iana` },
      "application/vnd.etsi.overload-control-policy-dataset+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.etsi.pstn+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.sci+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.simservs+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.timestamp-token": { source: `iana` },
      "application/vnd.etsi.tsl+xml": { source: `iana`, compressible: !0 },
      "application/vnd.etsi.tsl.der": { source: `iana` },
      "application/vnd.eu.kasparian.car+json": { source: `iana`, compressible: !0 },
      "application/vnd.eudora.data": { source: `iana` },
      "application/vnd.evolv.ecig.profile": { source: `iana` },
      "application/vnd.evolv.ecig.settings": { source: `iana` },
      "application/vnd.evolv.ecig.theme": { source: `iana` },
      "application/vnd.exstream-empower+zip": { source: `iana`, compressible: !1 },
      "application/vnd.exstream-package": { source: `iana` },
      "application/vnd.ezpix-album": { source: `iana`, extensions: [`ez2`] },
      "application/vnd.ezpix-package": { source: `iana`, extensions: [`ez3`] },
      "application/vnd.f-secure.mobile": { source: `iana` },
      "application/vnd.familysearch.gedcom+zip": { source: `iana`, compressible: !1 },
      "application/vnd.fastcopy-disk-image": { source: `iana` },
      "application/vnd.fdf": { source: `apache`, extensions: [`fdf`] },
      "application/vnd.fdsn.mseed": { source: `iana`, extensions: [`mseed`] },
      "application/vnd.fdsn.seed": { source: `iana`, extensions: [`seed`, `dataless`] },
      "application/vnd.fdsn.stationxml+xml": {
        source: `iana`,
        charset: `XML-BASED`,
        compressible: !0,
      },
      "application/vnd.ffsns": { source: `iana` },
      "application/vnd.ficlab.flb+zip": { source: `iana`, compressible: !1 },
      "application/vnd.filmit.zfc": { source: `iana` },
      "application/vnd.fints": { source: `iana` },
      "application/vnd.firemonkeys.cloudcell": { source: `iana` },
      "application/vnd.flographit": { source: `iana`, extensions: [`gph`] },
      "application/vnd.fluxtime.clip": { source: `iana`, extensions: [`ftc`] },
      "application/vnd.font-fontforge-sfd": { source: `iana` },
      "application/vnd.framemaker": {
        source: `iana`,
        extensions: [`fm`, `frame`, `maker`, `book`],
      },
      "application/vnd.freelog.comic": { source: `iana` },
      "application/vnd.frogans.fnc": { source: `apache`, extensions: [`fnc`] },
      "application/vnd.frogans.ltf": { source: `apache`, extensions: [`ltf`] },
      "application/vnd.fsc.weblaunch": { source: `iana`, extensions: [`fsc`] },
      "application/vnd.fujifilm.fb.docuworks": { source: `iana` },
      "application/vnd.fujifilm.fb.docuworks.binder": { source: `iana` },
      "application/vnd.fujifilm.fb.docuworks.container": { source: `iana` },
      "application/vnd.fujifilm.fb.jfi+xml": { source: `iana`, compressible: !0 },
      "application/vnd.fujitsu.oasys": { source: `iana`, extensions: [`oas`] },
      "application/vnd.fujitsu.oasys2": { source: `iana`, extensions: [`oa2`] },
      "application/vnd.fujitsu.oasys3": { source: `iana`, extensions: [`oa3`] },
      "application/vnd.fujitsu.oasysgp": { source: `iana`, extensions: [`fg5`] },
      "application/vnd.fujitsu.oasysprs": { source: `iana`, extensions: [`bh2`] },
      "application/vnd.fujixerox.art-ex": { source: `iana` },
      "application/vnd.fujixerox.art4": { source: `iana` },
      "application/vnd.fujixerox.ddd": { source: `iana`, extensions: [`ddd`] },
      "application/vnd.fujixerox.docuworks": { source: `iana`, extensions: [`xdw`] },
      "application/vnd.fujixerox.docuworks.binder": { source: `iana`, extensions: [`xbd`] },
      "application/vnd.fujixerox.docuworks.container": { source: `iana` },
      "application/vnd.fujixerox.hbpl": { source: `iana` },
      "application/vnd.fut-misnet": { source: `iana` },
      "application/vnd.futoin+cbor": { source: `iana` },
      "application/vnd.futoin+json": { source: `iana`, compressible: !0 },
      "application/vnd.fuzzysheet": { source: `iana`, extensions: [`fzs`] },
      "application/vnd.ga4gh.passport+jwt": { source: `iana` },
      "application/vnd.genomatix.tuxedo": { source: `iana`, extensions: [`txd`] },
      "application/vnd.genozip": { source: `iana` },
      "application/vnd.gentics.grd+json": { source: `iana`, compressible: !0 },
      "application/vnd.gentoo.catmetadata+xml": { source: `iana`, compressible: !0 },
      "application/vnd.gentoo.ebuild": { source: `iana` },
      "application/vnd.gentoo.eclass": { source: `iana` },
      "application/vnd.gentoo.gpkg": { source: `iana` },
      "application/vnd.gentoo.manifest": { source: `iana` },
      "application/vnd.gentoo.pkgmetadata+xml": { source: `iana`, compressible: !0 },
      "application/vnd.gentoo.xpak": { source: `iana` },
      "application/vnd.geo+json": { source: `apache`, compressible: !0 },
      "application/vnd.geocube+xml": { source: `apache`, compressible: !0 },
      "application/vnd.geogebra.file": { source: `iana`, extensions: [`ggb`] },
      "application/vnd.geogebra.pinboard": { source: `iana` },
      "application/vnd.geogebra.slides": { source: `iana`, extensions: [`ggs`] },
      "application/vnd.geogebra.tool": { source: `iana`, extensions: [`ggt`] },
      "application/vnd.geometry-explorer": { source: `iana`, extensions: [`gex`, `gre`] },
      "application/vnd.geonext": { source: `iana`, extensions: [`gxt`] },
      "application/vnd.geoplan": { source: `iana`, extensions: [`g2w`] },
      "application/vnd.geospace": { source: `iana`, extensions: [`g3w`] },
      "application/vnd.gerber": { source: `iana` },
      "application/vnd.globalplatform.card-content-mgt": { source: `iana` },
      "application/vnd.globalplatform.card-content-mgt-response": { source: `iana` },
      "application/vnd.gmx": { source: `iana`, extensions: [`gmx`] },
      "application/vnd.gnu.taler.exchange+json": { source: `iana`, compressible: !0 },
      "application/vnd.gnu.taler.merchant+json": { source: `iana`, compressible: !0 },
      "application/vnd.google-apps.audio": {},
      "application/vnd.google-apps.document": { compressible: !1, extensions: [`gdoc`] },
      "application/vnd.google-apps.drawing": { compressible: !1, extensions: [`gdraw`] },
      "application/vnd.google-apps.drive-sdk": { compressible: !1 },
      "application/vnd.google-apps.file": {},
      "application/vnd.google-apps.folder": { compressible: !1 },
      "application/vnd.google-apps.form": { compressible: !1, extensions: [`gform`] },
      "application/vnd.google-apps.fusiontable": {},
      "application/vnd.google-apps.jam": { compressible: !1, extensions: [`gjam`] },
      "application/vnd.google-apps.mail-layout": {},
      "application/vnd.google-apps.map": { compressible: !1, extensions: [`gmap`] },
      "application/vnd.google-apps.photo": {},
      "application/vnd.google-apps.presentation": { compressible: !1, extensions: [`gslides`] },
      "application/vnd.google-apps.script": { compressible: !1, extensions: [`gscript`] },
      "application/vnd.google-apps.shortcut": {},
      "application/vnd.google-apps.site": { compressible: !1, extensions: [`gsite`] },
      "application/vnd.google-apps.spreadsheet": { compressible: !1, extensions: [`gsheet`] },
      "application/vnd.google-apps.unknown": {},
      "application/vnd.google-apps.video": {},
      "application/vnd.google-earth.kml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`kml`],
      },
      "application/vnd.google-earth.kmz": { source: `iana`, compressible: !1, extensions: [`kmz`] },
      "application/vnd.gov.sk.e-form+xml": { source: `apache`, compressible: !0 },
      "application/vnd.gov.sk.e-form+zip": { source: `iana`, compressible: !1 },
      "application/vnd.gov.sk.xmldatacontainer+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xdcf`],
      },
      "application/vnd.gpxsee.map+xml": { source: `iana`, compressible: !0 },
      "application/vnd.grafeq": { source: `iana`, extensions: [`gqf`, `gqs`] },
      "application/vnd.gridmp": { source: `iana` },
      "application/vnd.groove-account": { source: `iana`, extensions: [`gac`] },
      "application/vnd.groove-help": { source: `iana`, extensions: [`ghf`] },
      "application/vnd.groove-identity-message": { source: `iana`, extensions: [`gim`] },
      "application/vnd.groove-injector": { source: `iana`, extensions: [`grv`] },
      "application/vnd.groove-tool-message": { source: `iana`, extensions: [`gtm`] },
      "application/vnd.groove-tool-template": { source: `iana`, extensions: [`tpl`] },
      "application/vnd.groove-vcard": { source: `iana`, extensions: [`vcg`] },
      "application/vnd.hal+json": { source: `iana`, compressible: !0 },
      "application/vnd.hal+xml": { source: `iana`, compressible: !0, extensions: [`hal`] },
      "application/vnd.handheld-entertainment+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`zmm`],
      },
      "application/vnd.hbci": { source: `iana`, extensions: [`hbci`] },
      "application/vnd.hc+json": { source: `iana`, compressible: !0 },
      "application/vnd.hcl-bireports": { source: `iana` },
      "application/vnd.hdt": { source: `iana` },
      "application/vnd.heroku+json": { source: `iana`, compressible: !0 },
      "application/vnd.hhe.lesson-player": { source: `iana`, extensions: [`les`] },
      "application/vnd.hp-hpgl": { source: `iana`, extensions: [`hpgl`] },
      "application/vnd.hp-hpid": { source: `iana`, extensions: [`hpid`] },
      "application/vnd.hp-hps": { source: `iana`, extensions: [`hps`] },
      "application/vnd.hp-jlyt": { source: `iana`, extensions: [`jlt`] },
      "application/vnd.hp-pcl": { source: `iana`, extensions: [`pcl`] },
      "application/vnd.hp-pclxl": { source: `iana`, extensions: [`pclxl`] },
      "application/vnd.hsl": { source: `iana` },
      "application/vnd.httphone": { source: `iana` },
      "application/vnd.hydrostatix.sof-data": { source: `iana`, extensions: [`sfd-hdstx`] },
      "application/vnd.hyper+json": { source: `iana`, compressible: !0 },
      "application/vnd.hyper-item+json": { source: `iana`, compressible: !0 },
      "application/vnd.hyperdrive+json": { source: `iana`, compressible: !0 },
      "application/vnd.hzn-3d-crossword": { source: `iana` },
      "application/vnd.ibm.afplinedata": { source: `apache` },
      "application/vnd.ibm.electronic-media": { source: `iana` },
      "application/vnd.ibm.minipay": { source: `iana`, extensions: [`mpy`] },
      "application/vnd.ibm.modcap": {
        source: `apache`,
        extensions: [`afp`, `listafp`, `list3820`],
      },
      "application/vnd.ibm.rights-management": { source: `iana`, extensions: [`irm`] },
      "application/vnd.ibm.secure-container": { source: `iana`, extensions: [`sc`] },
      "application/vnd.iccprofile": { source: `iana`, extensions: [`icc`, `icm`] },
      "application/vnd.ieee.1905": { source: `iana` },
      "application/vnd.igloader": { source: `iana`, extensions: [`igl`] },
      "application/vnd.imagemeter.folder+zip": { source: `iana`, compressible: !1 },
      "application/vnd.imagemeter.image+zip": { source: `iana`, compressible: !1 },
      "application/vnd.immervision-ivp": { source: `iana`, extensions: [`ivp`] },
      "application/vnd.immervision-ivu": { source: `iana`, extensions: [`ivu`] },
      "application/vnd.ims.imsccv1p1": { source: `iana` },
      "application/vnd.ims.imsccv1p2": { source: `iana` },
      "application/vnd.ims.imsccv1p3": { source: `iana` },
      "application/vnd.ims.lis.v2.result+json": { source: `iana`, compressible: !0 },
      "application/vnd.ims.lti.v2.toolconsumerprofile+json": { source: `iana`, compressible: !0 },
      "application/vnd.ims.lti.v2.toolproxy+json": { source: `iana`, compressible: !0 },
      "application/vnd.ims.lti.v2.toolproxy.id+json": { source: `iana`, compressible: !0 },
      "application/vnd.ims.lti.v2.toolsettings+json": { source: `iana`, compressible: !0 },
      "application/vnd.ims.lti.v2.toolsettings.simple+json": { source: `iana`, compressible: !0 },
      "application/vnd.informedcontrol.rms+xml": { source: `iana`, compressible: !0 },
      "application/vnd.informix-visionary": { source: `apache` },
      "application/vnd.infotech.project": { source: `iana` },
      "application/vnd.infotech.project+xml": { source: `iana`, compressible: !0 },
      "application/vnd.innopath.wamp.notification": { source: `iana` },
      "application/vnd.insors.igm": { source: `iana`, extensions: [`igm`] },
      "application/vnd.intercon.formnet": { source: `iana`, extensions: [`xpw`, `xpx`] },
      "application/vnd.intergeo": { source: `iana`, extensions: [`i2g`] },
      "application/vnd.intertrust.digibox": { source: `iana` },
      "application/vnd.intertrust.nncp": { source: `iana` },
      "application/vnd.intu.qbo": { source: `iana`, extensions: [`qbo`] },
      "application/vnd.intu.qfx": { source: `iana`, extensions: [`qfx`] },
      "application/vnd.ipfs.ipns-record": { source: `iana` },
      "application/vnd.ipld.car": { source: `iana` },
      "application/vnd.ipld.dag-cbor": { source: `iana` },
      "application/vnd.ipld.dag-json": { source: `iana` },
      "application/vnd.ipld.raw": { source: `iana` },
      "application/vnd.iptc.g2.catalogitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.conceptitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.knowledgeitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.newsitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.newsmessage+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.packageitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.iptc.g2.planningitem+xml": { source: `iana`, compressible: !0 },
      "application/vnd.ipunplugged.rcprofile": { source: `iana`, extensions: [`rcprofile`] },
      "application/vnd.irepository.package+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`irp`],
      },
      "application/vnd.is-xpr": { source: `iana`, extensions: [`xpr`] },
      "application/vnd.isac.fcs": { source: `iana`, extensions: [`fcs`] },
      "application/vnd.iso11783-10+zip": { source: `iana`, compressible: !1 },
      "application/vnd.jam": { source: `iana`, extensions: [`jam`] },
      "application/vnd.japannet-directory-service": { source: `iana` },
      "application/vnd.japannet-jpnstore-wakeup": { source: `iana` },
      "application/vnd.japannet-payment-wakeup": { source: `iana` },
      "application/vnd.japannet-registration": { source: `iana` },
      "application/vnd.japannet-registration-wakeup": { source: `iana` },
      "application/vnd.japannet-setstore-wakeup": { source: `iana` },
      "application/vnd.japannet-verification": { source: `iana` },
      "application/vnd.japannet-verification-wakeup": { source: `iana` },
      "application/vnd.jcp.javame.midlet-rms": { source: `iana`, extensions: [`rms`] },
      "application/vnd.jisp": { source: `iana`, extensions: [`jisp`] },
      "application/vnd.joost.joda-archive": { source: `iana`, extensions: [`joda`] },
      "application/vnd.jsk.isdn-ngn": { source: `iana` },
      "application/vnd.kahootz": { source: `iana`, extensions: [`ktz`, `ktr`] },
      "application/vnd.kde.karbon": { source: `iana`, extensions: [`karbon`] },
      "application/vnd.kde.kchart": { source: `iana`, extensions: [`chrt`] },
      "application/vnd.kde.kformula": { source: `iana`, extensions: [`kfo`] },
      "application/vnd.kde.kivio": { source: `iana`, extensions: [`flw`] },
      "application/vnd.kde.kontour": { source: `iana`, extensions: [`kon`] },
      "application/vnd.kde.kpresenter": { source: `iana`, extensions: [`kpr`, `kpt`] },
      "application/vnd.kde.kspread": { source: `iana`, extensions: [`ksp`] },
      "application/vnd.kde.kword": { source: `iana`, extensions: [`kwd`, `kwt`] },
      "application/vnd.kdl": { source: `iana` },
      "application/vnd.kenameaapp": { source: `iana`, extensions: [`htke`] },
      "application/vnd.keyman.kmp+zip": { source: `iana`, compressible: !1 },
      "application/vnd.keyman.kmx": { source: `iana` },
      "application/vnd.kidspiration": { source: `iana`, extensions: [`kia`] },
      "application/vnd.kinar": { source: `iana`, extensions: [`kne`, `knp`] },
      "application/vnd.koan": { source: `iana`, extensions: [`skp`, `skd`, `skt`, `skm`] },
      "application/vnd.kodak-descriptor": { source: `iana`, extensions: [`sse`] },
      "application/vnd.las": { source: `iana` },
      "application/vnd.las.las+json": { source: `iana`, compressible: !0 },
      "application/vnd.las.las+xml": { source: `iana`, compressible: !0, extensions: [`lasxml`] },
      "application/vnd.laszip": { source: `iana` },
      "application/vnd.ldev.productlicensing": { source: `iana` },
      "application/vnd.leap+json": { source: `iana`, compressible: !0 },
      "application/vnd.liberty-request+xml": { source: `iana`, compressible: !0 },
      "application/vnd.llamagraphics.life-balance.desktop": { source: `iana`, extensions: [`lbd`] },
      "application/vnd.llamagraphics.life-balance.exchange+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`lbe`],
      },
      "application/vnd.logipipe.circuit+zip": { source: `iana`, compressible: !1 },
      "application/vnd.loom": { source: `iana` },
      "application/vnd.lotus-1-2-3": { source: `iana`, extensions: [`123`] },
      "application/vnd.lotus-approach": { source: `iana`, extensions: [`apr`] },
      "application/vnd.lotus-freelance": { source: `iana`, extensions: [`pre`] },
      "application/vnd.lotus-notes": { source: `iana`, extensions: [`nsf`] },
      "application/vnd.lotus-organizer": { source: `iana`, extensions: [`org`] },
      "application/vnd.lotus-screencam": { source: `iana`, extensions: [`scm`] },
      "application/vnd.lotus-wordpro": { source: `iana`, extensions: [`lwp`] },
      "application/vnd.macports.portpkg": { source: `iana`, extensions: [`portpkg`] },
      "application/vnd.mapbox-vector-tile": { source: `iana`, extensions: [`mvt`] },
      "application/vnd.marlin.drm.actiontoken+xml": { source: `iana`, compressible: !0 },
      "application/vnd.marlin.drm.conftoken+xml": { source: `iana`, compressible: !0 },
      "application/vnd.marlin.drm.license+xml": { source: `iana`, compressible: !0 },
      "application/vnd.marlin.drm.mdcf": { source: `iana` },
      "application/vnd.mason+json": { source: `iana`, compressible: !0 },
      "application/vnd.maxar.archive.3tz+zip": { source: `iana`, compressible: !1 },
      "application/vnd.maxmind.maxmind-db": { source: `iana` },
      "application/vnd.mcd": { source: `iana`, extensions: [`mcd`] },
      "application/vnd.mdl": { source: `iana` },
      "application/vnd.mdl-mbsdf": { source: `iana` },
      "application/vnd.medcalcdata": { source: `iana`, extensions: [`mc1`] },
      "application/vnd.mediastation.cdkey": { source: `iana`, extensions: [`cdkey`] },
      "application/vnd.medicalholodeck.recordxr": { source: `iana` },
      "application/vnd.meridian-slingshot": { source: `iana` },
      "application/vnd.mermaid": { source: `iana` },
      "application/vnd.mfer": { source: `iana`, extensions: [`mwf`] },
      "application/vnd.mfmp": { source: `iana`, extensions: [`mfm`] },
      "application/vnd.micro+json": { source: `iana`, compressible: !0 },
      "application/vnd.micrografx.flo": { source: `iana`, extensions: [`flo`] },
      "application/vnd.micrografx.igx": { source: `iana`, extensions: [`igx`] },
      "application/vnd.microsoft.portable-executable": { source: `iana` },
      "application/vnd.microsoft.windows.thumbnail-cache": { source: `iana` },
      "application/vnd.miele+json": { source: `iana`, compressible: !0 },
      "application/vnd.mif": { source: `iana`, extensions: [`mif`] },
      "application/vnd.minisoft-hp3000-save": { source: `iana` },
      "application/vnd.mitsubishi.misty-guard.trustweb": { source: `iana` },
      "application/vnd.mobius.daf": { source: `iana`, extensions: [`daf`] },
      "application/vnd.mobius.dis": { source: `iana`, extensions: [`dis`] },
      "application/vnd.mobius.mbk": { source: `iana`, extensions: [`mbk`] },
      "application/vnd.mobius.mqy": { source: `iana`, extensions: [`mqy`] },
      "application/vnd.mobius.msl": { source: `iana`, extensions: [`msl`] },
      "application/vnd.mobius.plc": { source: `iana`, extensions: [`plc`] },
      "application/vnd.mobius.txf": { source: `iana`, extensions: [`txf`] },
      "application/vnd.modl": { source: `iana` },
      "application/vnd.mophun.application": { source: `iana`, extensions: [`mpn`] },
      "application/vnd.mophun.certificate": { source: `iana`, extensions: [`mpc`] },
      "application/vnd.motorola.flexsuite": { source: `iana` },
      "application/vnd.motorola.flexsuite.adsi": { source: `iana` },
      "application/vnd.motorola.flexsuite.fis": { source: `iana` },
      "application/vnd.motorola.flexsuite.gotap": { source: `iana` },
      "application/vnd.motorola.flexsuite.kmr": { source: `iana` },
      "application/vnd.motorola.flexsuite.ttc": { source: `iana` },
      "application/vnd.motorola.flexsuite.wem": { source: `iana` },
      "application/vnd.motorola.iprm": { source: `iana` },
      "application/vnd.mozilla.xul+xml": { source: `iana`, compressible: !0, extensions: [`xul`] },
      "application/vnd.ms-3mfdocument": { source: `iana` },
      "application/vnd.ms-artgalry": { source: `iana`, extensions: [`cil`] },
      "application/vnd.ms-asf": { source: `iana` },
      "application/vnd.ms-cab-compressed": { source: `iana`, extensions: [`cab`] },
      "application/vnd.ms-color.iccprofile": { source: `apache` },
      "application/vnd.ms-excel": {
        source: `iana`,
        compressible: !1,
        extensions: [`xls`, `xlm`, `xla`, `xlc`, `xlt`, `xlw`],
      },
      "application/vnd.ms-excel.addin.macroenabled.12": { source: `iana`, extensions: [`xlam`] },
      "application/vnd.ms-excel.sheet.binary.macroenabled.12": {
        source: `iana`,
        extensions: [`xlsb`],
      },
      "application/vnd.ms-excel.sheet.macroenabled.12": { source: `iana`, extensions: [`xlsm`] },
      "application/vnd.ms-excel.template.macroenabled.12": { source: `iana`, extensions: [`xltm`] },
      "application/vnd.ms-fontobject": { source: `iana`, compressible: !0, extensions: [`eot`] },
      "application/vnd.ms-htmlhelp": { source: `iana`, extensions: [`chm`] },
      "application/vnd.ms-ims": { source: `iana`, extensions: [`ims`] },
      "application/vnd.ms-lrm": { source: `iana`, extensions: [`lrm`] },
      "application/vnd.ms-office.activex+xml": { source: `iana`, compressible: !0 },
      "application/vnd.ms-officetheme": { source: `iana`, extensions: [`thmx`] },
      "application/vnd.ms-opentype": { source: `apache`, compressible: !0 },
      "application/vnd.ms-outlook": { compressible: !1, extensions: [`msg`] },
      "application/vnd.ms-package.obfuscated-opentype": { source: `apache` },
      "application/vnd.ms-pki.seccat": { source: `apache`, extensions: [`cat`] },
      "application/vnd.ms-pki.stl": { source: `apache`, extensions: [`stl`] },
      "application/vnd.ms-playready.initiator+xml": { source: `iana`, compressible: !0 },
      "application/vnd.ms-powerpoint": {
        source: `iana`,
        compressible: !1,
        extensions: [`ppt`, `pps`, `pot`],
      },
      "application/vnd.ms-powerpoint.addin.macroenabled.12": {
        source: `iana`,
        extensions: [`ppam`],
      },
      "application/vnd.ms-powerpoint.presentation.macroenabled.12": {
        source: `iana`,
        extensions: [`pptm`],
      },
      "application/vnd.ms-powerpoint.slide.macroenabled.12": {
        source: `iana`,
        extensions: [`sldm`],
      },
      "application/vnd.ms-powerpoint.slideshow.macroenabled.12": {
        source: `iana`,
        extensions: [`ppsm`],
      },
      "application/vnd.ms-powerpoint.template.macroenabled.12": {
        source: `iana`,
        extensions: [`potm`],
      },
      "application/vnd.ms-printdevicecapabilities+xml": { source: `iana`, compressible: !0 },
      "application/vnd.ms-printing.printticket+xml": { source: `apache`, compressible: !0 },
      "application/vnd.ms-printschematicket+xml": { source: `iana`, compressible: !0 },
      "application/vnd.ms-project": { source: `iana`, extensions: [`mpp`, `mpt`] },
      "application/vnd.ms-tnef": { source: `iana` },
      "application/vnd.ms-visio.viewer": { extensions: [`vdx`] },
      "application/vnd.ms-windows.devicepairing": { source: `iana` },
      "application/vnd.ms-windows.nwprinting.oob": { source: `iana` },
      "application/vnd.ms-windows.printerpairing": { source: `iana` },
      "application/vnd.ms-windows.wsd.oob": { source: `iana` },
      "application/vnd.ms-wmdrm.lic-chlg-req": { source: `iana` },
      "application/vnd.ms-wmdrm.lic-resp": { source: `iana` },
      "application/vnd.ms-wmdrm.meter-chlg-req": { source: `iana` },
      "application/vnd.ms-wmdrm.meter-resp": { source: `iana` },
      "application/vnd.ms-word.document.macroenabled.12": { source: `iana`, extensions: [`docm`] },
      "application/vnd.ms-word.template.macroenabled.12": { source: `iana`, extensions: [`dotm`] },
      "application/vnd.ms-works": { source: `iana`, extensions: [`wps`, `wks`, `wcm`, `wdb`] },
      "application/vnd.ms-wpl": { source: `iana`, extensions: [`wpl`] },
      "application/vnd.ms-xpsdocument": { source: `iana`, compressible: !1, extensions: [`xps`] },
      "application/vnd.msa-disk-image": { source: `iana` },
      "application/vnd.mseq": { source: `iana`, extensions: [`mseq`] },
      "application/vnd.msgpack": { source: `iana` },
      "application/vnd.msign": { source: `iana` },
      "application/vnd.multiad.creator": { source: `iana` },
      "application/vnd.multiad.creator.cif": { source: `iana` },
      "application/vnd.music-niff": { source: `iana` },
      "application/vnd.musician": { source: `iana`, extensions: [`mus`] },
      "application/vnd.muvee.style": { source: `iana`, extensions: [`msty`] },
      "application/vnd.mynfc": { source: `iana`, extensions: [`taglet`] },
      "application/vnd.nacamar.ybrid+json": { source: `iana`, compressible: !0 },
      "application/vnd.nato.bindingdataobject+cbor": { source: `iana` },
      "application/vnd.nato.bindingdataobject+json": { source: `iana`, compressible: !0 },
      "application/vnd.nato.bindingdataobject+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`bdo`],
      },
      "application/vnd.nato.openxmlformats-package.iepd+zip": { source: `iana`, compressible: !1 },
      "application/vnd.ncd.control": { source: `iana` },
      "application/vnd.ncd.reference": { source: `iana` },
      "application/vnd.nearst.inv+json": { source: `iana`, compressible: !0 },
      "application/vnd.nebumind.line": { source: `iana` },
      "application/vnd.nervana": { source: `iana` },
      "application/vnd.netfpx": { source: `iana` },
      "application/vnd.neurolanguage.nlu": { source: `iana`, extensions: [`nlu`] },
      "application/vnd.nimn": { source: `iana` },
      "application/vnd.nintendo.nitro.rom": { source: `iana` },
      "application/vnd.nintendo.snes.rom": { source: `iana` },
      "application/vnd.nitf": { source: `iana`, extensions: [`ntf`, `nitf`] },
      "application/vnd.noblenet-directory": { source: `iana`, extensions: [`nnd`] },
      "application/vnd.noblenet-sealer": { source: `iana`, extensions: [`nns`] },
      "application/vnd.noblenet-web": { source: `iana`, extensions: [`nnw`] },
      "application/vnd.nokia.catalogs": { source: `iana` },
      "application/vnd.nokia.conml+wbxml": { source: `iana` },
      "application/vnd.nokia.conml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.iptv.config+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.isds-radio-presets": { source: `iana` },
      "application/vnd.nokia.landmark+wbxml": { source: `iana` },
      "application/vnd.nokia.landmark+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.landmarkcollection+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.n-gage.ac+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`ac`],
      },
      "application/vnd.nokia.n-gage.data": { source: `iana`, extensions: [`ngdat`] },
      "application/vnd.nokia.n-gage.symbian.install": { source: `apache`, extensions: [`n-gage`] },
      "application/vnd.nokia.ncd": { source: `iana` },
      "application/vnd.nokia.pcd+wbxml": { source: `iana` },
      "application/vnd.nokia.pcd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.nokia.radio-preset": { source: `iana`, extensions: [`rpst`] },
      "application/vnd.nokia.radio-presets": { source: `iana`, extensions: [`rpss`] },
      "application/vnd.novadigm.edm": { source: `iana`, extensions: [`edm`] },
      "application/vnd.novadigm.edx": { source: `iana`, extensions: [`edx`] },
      "application/vnd.novadigm.ext": { source: `iana`, extensions: [`ext`] },
      "application/vnd.ntt-local.content-share": { source: `iana` },
      "application/vnd.ntt-local.file-transfer": { source: `iana` },
      "application/vnd.ntt-local.ogw_remote-access": { source: `iana` },
      "application/vnd.ntt-local.sip-ta_remote": { source: `iana` },
      "application/vnd.ntt-local.sip-ta_tcp_stream": { source: `iana` },
      "application/vnd.oai.workflows": { source: `iana` },
      "application/vnd.oai.workflows+json": { source: `iana`, compressible: !0 },
      "application/vnd.oai.workflows+yaml": { source: `iana` },
      "application/vnd.oasis.opendocument.base": { source: `iana` },
      "application/vnd.oasis.opendocument.chart": { source: `iana`, extensions: [`odc`] },
      "application/vnd.oasis.opendocument.chart-template": { source: `iana`, extensions: [`otc`] },
      "application/vnd.oasis.opendocument.database": { source: `apache`, extensions: [`odb`] },
      "application/vnd.oasis.opendocument.formula": { source: `iana`, extensions: [`odf`] },
      "application/vnd.oasis.opendocument.formula-template": {
        source: `iana`,
        extensions: [`odft`],
      },
      "application/vnd.oasis.opendocument.graphics": {
        source: `iana`,
        compressible: !1,
        extensions: [`odg`],
      },
      "application/vnd.oasis.opendocument.graphics-template": {
        source: `iana`,
        extensions: [`otg`],
      },
      "application/vnd.oasis.opendocument.image": { source: `iana`, extensions: [`odi`] },
      "application/vnd.oasis.opendocument.image-template": { source: `iana`, extensions: [`oti`] },
      "application/vnd.oasis.opendocument.presentation": {
        source: `iana`,
        compressible: !1,
        extensions: [`odp`],
      },
      "application/vnd.oasis.opendocument.presentation-template": {
        source: `iana`,
        extensions: [`otp`],
      },
      "application/vnd.oasis.opendocument.spreadsheet": {
        source: `iana`,
        compressible: !1,
        extensions: [`ods`],
      },
      "application/vnd.oasis.opendocument.spreadsheet-template": {
        source: `iana`,
        extensions: [`ots`],
      },
      "application/vnd.oasis.opendocument.text": {
        source: `iana`,
        compressible: !1,
        extensions: [`odt`],
      },
      "application/vnd.oasis.opendocument.text-master": { source: `iana`, extensions: [`odm`] },
      "application/vnd.oasis.opendocument.text-master-template": { source: `iana` },
      "application/vnd.oasis.opendocument.text-template": { source: `iana`, extensions: [`ott`] },
      "application/vnd.oasis.opendocument.text-web": { source: `iana`, extensions: [`oth`] },
      "application/vnd.obn": { source: `iana` },
      "application/vnd.ocf+cbor": { source: `iana` },
      "application/vnd.oci.image.manifest.v1+json": { source: `iana`, compressible: !0 },
      "application/vnd.oftn.l10n+json": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.contentaccessdownload+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.contentaccessstreaming+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.cspg-hexbinary": { source: `iana` },
      "application/vnd.oipf.dae.svg+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.dae.xhtml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.mippvcontrolmessage+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.pae.gem": { source: `iana` },
      "application/vnd.oipf.spdiscovery+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.spdlist+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.ueprofile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oipf.userprofile+xml": { source: `iana`, compressible: !0 },
      "application/vnd.olpc-sugar": { source: `iana`, extensions: [`xo`] },
      "application/vnd.oma-scws-config": { source: `iana` },
      "application/vnd.oma-scws-http-request": { source: `iana` },
      "application/vnd.oma-scws-http-response": { source: `iana` },
      "application/vnd.oma.bcast.associated-procedure-parameter+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oma.bcast.drm-trigger+xml": { source: `apache`, compressible: !0 },
      "application/vnd.oma.bcast.imd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.bcast.ltkm": { source: `iana` },
      "application/vnd.oma.bcast.notification+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.bcast.provisioningtrigger": { source: `iana` },
      "application/vnd.oma.bcast.sgboot": { source: `iana` },
      "application/vnd.oma.bcast.sgdd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.bcast.sgdu": { source: `iana` },
      "application/vnd.oma.bcast.simple-symbol-container": { source: `iana` },
      "application/vnd.oma.bcast.smartcard-trigger+xml": { source: `apache`, compressible: !0 },
      "application/vnd.oma.bcast.sprov+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.bcast.stkm": { source: `iana` },
      "application/vnd.oma.cab-address-book+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.cab-feature-handler+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.cab-pcc+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.cab-subs-invite+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.cab-user-prefs+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.dcd": { source: `iana` },
      "application/vnd.oma.dcdc": { source: `iana` },
      "application/vnd.oma.dd2+xml": { source: `iana`, compressible: !0, extensions: [`dd2`] },
      "application/vnd.oma.drm.risd+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.group-usage-list+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.lwm2m+cbor": { source: `iana` },
      "application/vnd.oma.lwm2m+json": { source: `iana`, compressible: !0 },
      "application/vnd.oma.lwm2m+tlv": { source: `iana` },
      "application/vnd.oma.pal+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.detailed-progress-report+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.final-report+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.groups+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.invocation-descriptor+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.poc.optimized-progress-report+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.push": { source: `iana` },
      "application/vnd.oma.scidm.messages+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oma.xcap-directory+xml": { source: `iana`, compressible: !0 },
      "application/vnd.omads-email+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/vnd.omads-file+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/vnd.omads-folder+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/vnd.omaloc-supl-init": { source: `iana` },
      "application/vnd.onepager": { source: `iana` },
      "application/vnd.onepagertamp": { source: `iana` },
      "application/vnd.onepagertamx": { source: `iana` },
      "application/vnd.onepagertat": { source: `iana` },
      "application/vnd.onepagertatp": { source: `iana` },
      "application/vnd.onepagertatx": { source: `iana` },
      "application/vnd.onvif.metadata": { source: `iana` },
      "application/vnd.openblox.game+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`obgx`],
      },
      "application/vnd.openblox.game-binary": { source: `iana` },
      "application/vnd.openeye.oeb": { source: `iana` },
      "application/vnd.openofficeorg.extension": { source: `apache`, extensions: [`oxt`] },
      "application/vnd.openstreetmap.data+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`osm`],
      },
      "application/vnd.opentimestamps.ots": { source: `iana` },
      "application/vnd.openvpi.dspx+json": { source: `iana`, compressible: !0 },
      "application/vnd.openxmlformats-officedocument.custom-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.customxmlproperties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawing+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.chart+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.chartshapes+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramcolors+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramdata+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramlayout+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.drawingml.diagramstyle+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.extended-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.commentauthors+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.comments+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.handoutmaster+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.notesmaster+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.notesslide+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.presentation": {
        source: `iana`,
        compressible: !1,
        extensions: [`pptx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.presentation.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.presprops+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slide": {
        source: `iana`,
        extensions: [`sldx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slide+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slidelayout+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slidemaster+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slideshow": {
        source: `iana`,
        extensions: [`ppsx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slideshow.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.slideupdateinfo+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.tablestyles+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.tags+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.template": {
        source: `iana`,
        extensions: [`potx`],
      },
      "application/vnd.openxmlformats-officedocument.presentationml.template.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.presentationml.viewprops+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.calcchain+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.chartsheet+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.comments+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.connections+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.dialogsheet+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.externallink+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcachedefinition+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivotcacherecords+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.pivottable+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.querytable+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionheaders+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.revisionlog+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sharedstrings+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": {
        source: `iana`,
        compressible: !1,
        extensions: [`xlsx`],
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.sheetmetadata+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.table+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.tablesinglecells+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.template": {
        source: `iana`,
        extensions: [`xltx`],
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.template.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.usernames+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.volatiledependencies+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.theme+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.themeoverride+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.vmldrawing": { source: `iana` },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.comments+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document": {
        source: `iana`,
        compressible: !1,
        extensions: [`docx`],
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document.glossary+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.endnotes+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.fonttable+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.footer+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.footnotes+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.numbering+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.settings+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.styles+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.template": {
        source: `iana`,
        extensions: [`dotx`],
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.template.main+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-officedocument.wordprocessingml.websettings+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-package.core-properties+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-package.digital-signature-xmlsignature+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.openxmlformats-package.relationships+xml": {
        source: `iana`,
        compressible: !0,
      },
      "application/vnd.oracle.resource+json": { source: `iana`, compressible: !0 },
      "application/vnd.orange.indata": { source: `iana` },
      "application/vnd.osa.netdeploy": { source: `iana` },
      "application/vnd.osgeo.mapguide.package": { source: `iana`, extensions: [`mgp`] },
      "application/vnd.osgi.bundle": { source: `iana` },
      "application/vnd.osgi.dp": { source: `iana`, extensions: [`dp`] },
      "application/vnd.osgi.subsystem": { source: `iana`, extensions: [`esa`] },
      "application/vnd.otps.ct-kip+xml": { source: `iana`, compressible: !0 },
      "application/vnd.oxli.countgraph": { source: `iana` },
      "application/vnd.pagerduty+json": { source: `iana`, compressible: !0 },
      "application/vnd.palm": { source: `iana`, extensions: [`pdb`, `pqa`, `oprc`] },
      "application/vnd.panoply": { source: `iana` },
      "application/vnd.paos.xml": { source: `iana` },
      "application/vnd.patentdive": { source: `iana` },
      "application/vnd.patientecommsdoc": { source: `iana` },
      "application/vnd.pawaafile": { source: `iana`, extensions: [`paw`] },
      "application/vnd.pcos": { source: `iana` },
      "application/vnd.pg.format": { source: `iana`, extensions: [`str`] },
      "application/vnd.pg.osasli": { source: `iana`, extensions: [`ei6`] },
      "application/vnd.piaccess.application-licence": { source: `iana` },
      "application/vnd.picsel": { source: `iana`, extensions: [`efif`] },
      "application/vnd.pmi.widget": { source: `iana`, extensions: [`wg`] },
      "application/vnd.poc.group-advertisement+xml": { source: `iana`, compressible: !0 },
      "application/vnd.pocketlearn": { source: `iana`, extensions: [`plf`] },
      "application/vnd.powerbuilder6": { source: `iana`, extensions: [`pbd`] },
      "application/vnd.powerbuilder6-s": { source: `iana` },
      "application/vnd.powerbuilder7": { source: `iana` },
      "application/vnd.powerbuilder7-s": { source: `iana` },
      "application/vnd.powerbuilder75": { source: `iana` },
      "application/vnd.powerbuilder75-s": { source: `iana` },
      "application/vnd.preminet": { source: `iana` },
      "application/vnd.previewsystems.box": { source: `iana`, extensions: [`box`] },
      "application/vnd.procrate.brushset": { extensions: [`brushset`] },
      "application/vnd.procreate.brush": { extensions: [`brush`] },
      "application/vnd.procreate.dream": { extensions: [`drm`] },
      "application/vnd.proteus.magazine": { source: `iana`, extensions: [`mgz`] },
      "application/vnd.psfs": { source: `iana` },
      "application/vnd.pt.mundusmundi": { source: `iana` },
      "application/vnd.publishare-delta-tree": { source: `iana`, extensions: [`qps`] },
      "application/vnd.pvi.ptid1": { source: `iana`, extensions: [`ptid`] },
      "application/vnd.pwg-multiplexed": { source: `iana` },
      "application/vnd.pwg-xhtml-print+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xhtm`],
      },
      "application/vnd.qualcomm.brew-app-res": { source: `iana` },
      "application/vnd.quarantainenet": { source: `iana` },
      "application/vnd.quark.quarkxpress": {
        source: `iana`,
        extensions: [`qxd`, `qxt`, `qwd`, `qwt`, `qxl`, `qxb`],
      },
      "application/vnd.quobject-quoxdocument": { source: `iana` },
      "application/vnd.radisys.moml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit-conf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit-conn+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit-dialog+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-audit-stream+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-conf+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-base+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-fax-detect+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-fax-sendrecv+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-group+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-speech+xml": { source: `iana`, compressible: !0 },
      "application/vnd.radisys.msml-dialog-transform+xml": { source: `iana`, compressible: !0 },
      "application/vnd.rainstor.data": { source: `iana` },
      "application/vnd.rapid": { source: `iana` },
      "application/vnd.rar": { source: `iana`, extensions: [`rar`] },
      "application/vnd.realvnc.bed": { source: `iana`, extensions: [`bed`] },
      "application/vnd.recordare.musicxml": { source: `iana`, extensions: [`mxl`] },
      "application/vnd.recordare.musicxml+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`musicxml`],
      },
      "application/vnd.relpipe": { source: `iana` },
      "application/vnd.renlearn.rlprint": { source: `iana` },
      "application/vnd.resilient.logic": { source: `iana` },
      "application/vnd.restful+json": { source: `iana`, compressible: !0 },
      "application/vnd.rig.cryptonote": { source: `iana`, extensions: [`cryptonote`] },
      "application/vnd.rim.cod": { source: `apache`, extensions: [`cod`] },
      "application/vnd.rn-realmedia": { source: `apache`, extensions: [`rm`] },
      "application/vnd.rn-realmedia-vbr": { source: `apache`, extensions: [`rmvb`] },
      "application/vnd.route66.link66+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`link66`],
      },
      "application/vnd.rs-274x": { source: `iana` },
      "application/vnd.ruckus.download": { source: `iana` },
      "application/vnd.s3sms": { source: `iana` },
      "application/vnd.sailingtracker.track": { source: `iana`, extensions: [`st`] },
      "application/vnd.sar": { source: `iana` },
      "application/vnd.sbm.cid": { source: `iana` },
      "application/vnd.sbm.mid2": { source: `iana` },
      "application/vnd.scribus": { source: `iana` },
      "application/vnd.sealed.3df": { source: `iana` },
      "application/vnd.sealed.csf": { source: `iana` },
      "application/vnd.sealed.doc": { source: `iana` },
      "application/vnd.sealed.eml": { source: `iana` },
      "application/vnd.sealed.mht": { source: `iana` },
      "application/vnd.sealed.net": { source: `iana` },
      "application/vnd.sealed.ppt": { source: `iana` },
      "application/vnd.sealed.tiff": { source: `iana` },
      "application/vnd.sealed.xls": { source: `iana` },
      "application/vnd.sealedmedia.softseal.html": { source: `iana` },
      "application/vnd.sealedmedia.softseal.pdf": { source: `iana` },
      "application/vnd.seemail": { source: `iana`, extensions: [`see`] },
      "application/vnd.seis+json": { source: `iana`, compressible: !0 },
      "application/vnd.sema": { source: `iana`, extensions: [`sema`] },
      "application/vnd.semd": { source: `iana`, extensions: [`semd`] },
      "application/vnd.semf": { source: `iana`, extensions: [`semf`] },
      "application/vnd.shade-save-file": { source: `iana` },
      "application/vnd.shana.informed.formdata": { source: `iana`, extensions: [`ifm`] },
      "application/vnd.shana.informed.formtemplate": { source: `iana`, extensions: [`itp`] },
      "application/vnd.shana.informed.interchange": { source: `iana`, extensions: [`iif`] },
      "application/vnd.shana.informed.package": { source: `iana`, extensions: [`ipk`] },
      "application/vnd.shootproof+json": { source: `iana`, compressible: !0 },
      "application/vnd.shopkick+json": { source: `iana`, compressible: !0 },
      "application/vnd.shp": { source: `iana` },
      "application/vnd.shx": { source: `iana` },
      "application/vnd.sigrok.session": { source: `iana` },
      "application/vnd.simtech-mindmapper": { source: `iana`, extensions: [`twd`, `twds`] },
      "application/vnd.siren+json": { source: `iana`, compressible: !0 },
      "application/vnd.sketchometry": { source: `iana` },
      "application/vnd.smaf": { source: `iana`, extensions: [`mmf`] },
      "application/vnd.smart.notebook": { source: `iana` },
      "application/vnd.smart.teacher": { source: `iana`, extensions: [`teacher`] },
      "application/vnd.smintio.portals.archive": { source: `iana` },
      "application/vnd.snesdev-page-table": { source: `iana` },
      "application/vnd.software602.filler.form+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`fo`],
      },
      "application/vnd.software602.filler.form-xml-zip": { source: `iana` },
      "application/vnd.solent.sdkm+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`sdkm`, `sdkd`],
      },
      "application/vnd.spotfire.dxp": { source: `iana`, extensions: [`dxp`] },
      "application/vnd.spotfire.sfs": { source: `iana`, extensions: [`sfs`] },
      "application/vnd.sqlite3": { source: `iana` },
      "application/vnd.sss-cod": { source: `iana` },
      "application/vnd.sss-dtf": { source: `iana` },
      "application/vnd.sss-ntf": { source: `iana` },
      "application/vnd.stardivision.calc": { source: `apache`, extensions: [`sdc`] },
      "application/vnd.stardivision.draw": { source: `apache`, extensions: [`sda`] },
      "application/vnd.stardivision.impress": { source: `apache`, extensions: [`sdd`] },
      "application/vnd.stardivision.math": { source: `apache`, extensions: [`smf`] },
      "application/vnd.stardivision.writer": { source: `apache`, extensions: [`sdw`, `vor`] },
      "application/vnd.stardivision.writer-global": { source: `apache`, extensions: [`sgl`] },
      "application/vnd.stepmania.package": { source: `iana`, extensions: [`smzip`] },
      "application/vnd.stepmania.stepchart": { source: `iana`, extensions: [`sm`] },
      "application/vnd.street-stream": { source: `iana` },
      "application/vnd.sun.wadl+xml": { source: `iana`, compressible: !0, extensions: [`wadl`] },
      "application/vnd.sun.xml.calc": { source: `apache`, extensions: [`sxc`] },
      "application/vnd.sun.xml.calc.template": { source: `apache`, extensions: [`stc`] },
      "application/vnd.sun.xml.draw": { source: `apache`, extensions: [`sxd`] },
      "application/vnd.sun.xml.draw.template": { source: `apache`, extensions: [`std`] },
      "application/vnd.sun.xml.impress": { source: `apache`, extensions: [`sxi`] },
      "application/vnd.sun.xml.impress.template": { source: `apache`, extensions: [`sti`] },
      "application/vnd.sun.xml.math": { source: `apache`, extensions: [`sxm`] },
      "application/vnd.sun.xml.writer": { source: `apache`, extensions: [`sxw`] },
      "application/vnd.sun.xml.writer.global": { source: `apache`, extensions: [`sxg`] },
      "application/vnd.sun.xml.writer.template": { source: `apache`, extensions: [`stw`] },
      "application/vnd.sus-calendar": { source: `iana`, extensions: [`sus`, `susp`] },
      "application/vnd.svd": { source: `iana`, extensions: [`svd`] },
      "application/vnd.swiftview-ics": { source: `iana` },
      "application/vnd.sybyl.mol2": { source: `iana` },
      "application/vnd.sycle+xml": { source: `iana`, compressible: !0 },
      "application/vnd.syft+json": { source: `iana`, compressible: !0 },
      "application/vnd.symbian.install": { source: `apache`, extensions: [`sis`, `sisx`] },
      "application/vnd.syncml+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`xsm`],
      },
      "application/vnd.syncml.dm+wbxml": { source: `iana`, charset: `UTF-8`, extensions: [`bdm`] },
      "application/vnd.syncml.dm+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`xdm`],
      },
      "application/vnd.syncml.dm.notification": { source: `iana` },
      "application/vnd.syncml.dmddf+wbxml": { source: `iana` },
      "application/vnd.syncml.dmddf+xml": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`ddf`],
      },
      "application/vnd.syncml.dmtnds+wbxml": { source: `iana` },
      "application/vnd.syncml.dmtnds+xml": { source: `iana`, charset: `UTF-8`, compressible: !0 },
      "application/vnd.syncml.ds.notification": { source: `iana` },
      "application/vnd.tableschema+json": { source: `iana`, compressible: !0 },
      "application/vnd.tao.intent-module-archive": { source: `iana`, extensions: [`tao`] },
      "application/vnd.tcpdump.pcap": { source: `iana`, extensions: [`pcap`, `cap`, `dmp`] },
      "application/vnd.think-cell.ppttc+json": { source: `iana`, compressible: !0 },
      "application/vnd.tmd.mediaflex.api+xml": { source: `iana`, compressible: !0 },
      "application/vnd.tml": { source: `iana` },
      "application/vnd.tmobile-livetv": { source: `iana`, extensions: [`tmo`] },
      "application/vnd.tri.onesource": { source: `iana` },
      "application/vnd.trid.tpt": { source: `iana`, extensions: [`tpt`] },
      "application/vnd.triscape.mxs": { source: `iana`, extensions: [`mxs`] },
      "application/vnd.trueapp": { source: `iana`, extensions: [`tra`] },
      "application/vnd.truedoc": { source: `iana` },
      "application/vnd.ubisoft.webplayer": { source: `iana` },
      "application/vnd.ufdl": { source: `iana`, extensions: [`ufd`, `ufdl`] },
      "application/vnd.uic.osdm+json": { source: `iana`, compressible: !0 },
      "application/vnd.uiq.theme": { source: `iana`, extensions: [`utz`] },
      "application/vnd.umajin": { source: `iana`, extensions: [`umj`] },
      "application/vnd.unity": { source: `iana`, extensions: [`unityweb`] },
      "application/vnd.uoml+xml": { source: `iana`, compressible: !0, extensions: [`uoml`, `uo`] },
      "application/vnd.uplanet.alert": { source: `iana` },
      "application/vnd.uplanet.alert-wbxml": { source: `iana` },
      "application/vnd.uplanet.bearer-choice": { source: `iana` },
      "application/vnd.uplanet.bearer-choice-wbxml": { source: `iana` },
      "application/vnd.uplanet.cacheop": { source: `iana` },
      "application/vnd.uplanet.cacheop-wbxml": { source: `iana` },
      "application/vnd.uplanet.channel": { source: `iana` },
      "application/vnd.uplanet.channel-wbxml": { source: `iana` },
      "application/vnd.uplanet.list": { source: `iana` },
      "application/vnd.uplanet.list-wbxml": { source: `iana` },
      "application/vnd.uplanet.listcmd": { source: `iana` },
      "application/vnd.uplanet.listcmd-wbxml": { source: `iana` },
      "application/vnd.uplanet.signal": { source: `iana` },
      "application/vnd.uri-map": { source: `iana` },
      "application/vnd.valve.source.material": { source: `iana` },
      "application/vnd.vcx": { source: `iana`, extensions: [`vcx`] },
      "application/vnd.vd-study": { source: `iana` },
      "application/vnd.vectorworks": { source: `iana` },
      "application/vnd.vel+json": { source: `iana`, compressible: !0 },
      "application/vnd.veraison.tsm-report+cbor": { source: `iana` },
      "application/vnd.veraison.tsm-report+json": { source: `iana`, compressible: !0 },
      "application/vnd.verimatrix.vcas": { source: `iana` },
      "application/vnd.veritone.aion+json": { source: `iana`, compressible: !0 },
      "application/vnd.veryant.thin": { source: `iana` },
      "application/vnd.ves.encrypted": { source: `iana` },
      "application/vnd.vidsoft.vidconference": { source: `iana` },
      "application/vnd.visio": {
        source: `iana`,
        extensions: [`vsd`, `vst`, `vss`, `vsw`, `vsdx`, `vtx`],
      },
      "application/vnd.visionary": { source: `iana`, extensions: [`vis`] },
      "application/vnd.vividence.scriptfile": { source: `iana` },
      "application/vnd.vocalshaper.vsp4": { source: `iana` },
      "application/vnd.vsf": { source: `iana`, extensions: [`vsf`] },
      "application/vnd.wap.sic": { source: `iana` },
      "application/vnd.wap.slc": { source: `iana` },
      "application/vnd.wap.wbxml": { source: `iana`, charset: `UTF-8`, extensions: [`wbxml`] },
      "application/vnd.wap.wmlc": { source: `iana`, extensions: [`wmlc`] },
      "application/vnd.wap.wmlscriptc": { source: `iana`, extensions: [`wmlsc`] },
      "application/vnd.wasmflow.wafl": { source: `iana` },
      "application/vnd.webturbo": { source: `iana`, extensions: [`wtb`] },
      "application/vnd.wfa.dpp": { source: `iana` },
      "application/vnd.wfa.p2p": { source: `iana` },
      "application/vnd.wfa.wsc": { source: `iana` },
      "application/vnd.windows.devicepairing": { source: `iana` },
      "application/vnd.wmc": { source: `iana` },
      "application/vnd.wmf.bootstrap": { source: `iana` },
      "application/vnd.wolfram.mathematica": { source: `iana` },
      "application/vnd.wolfram.mathematica.package": { source: `iana` },
      "application/vnd.wolfram.player": { source: `iana`, extensions: [`nbp`] },
      "application/vnd.wordlift": { source: `iana` },
      "application/vnd.wordperfect": { source: `iana`, extensions: [`wpd`] },
      "application/vnd.wqd": { source: `iana`, extensions: [`wqd`] },
      "application/vnd.wrq-hp3000-labelled": { source: `iana` },
      "application/vnd.wt.stf": { source: `iana`, extensions: [`stf`] },
      "application/vnd.wv.csp+wbxml": { source: `iana` },
      "application/vnd.wv.csp+xml": { source: `iana`, compressible: !0 },
      "application/vnd.wv.ssp+xml": { source: `iana`, compressible: !0 },
      "application/vnd.xacml+json": { source: `iana`, compressible: !0 },
      "application/vnd.xara": { source: `iana`, extensions: [`xar`] },
      "application/vnd.xarin.cpj": { source: `iana` },
      "application/vnd.xecrets-encrypted": { source: `iana` },
      "application/vnd.xfdl": { source: `iana`, extensions: [`xfdl`] },
      "application/vnd.xfdl.webform": { source: `iana` },
      "application/vnd.xmi+xml": { source: `iana`, compressible: !0 },
      "application/vnd.xmpie.cpkg": { source: `iana` },
      "application/vnd.xmpie.dpkg": { source: `iana` },
      "application/vnd.xmpie.plan": { source: `iana` },
      "application/vnd.xmpie.ppkg": { source: `iana` },
      "application/vnd.xmpie.xlim": { source: `iana` },
      "application/vnd.yamaha.hv-dic": { source: `iana`, extensions: [`hvd`] },
      "application/vnd.yamaha.hv-script": { source: `iana`, extensions: [`hvs`] },
      "application/vnd.yamaha.hv-voice": { source: `iana`, extensions: [`hvp`] },
      "application/vnd.yamaha.openscoreformat": { source: `iana`, extensions: [`osf`] },
      "application/vnd.yamaha.openscoreformat.osfpvg+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`osfpvg`],
      },
      "application/vnd.yamaha.remote-setup": { source: `iana` },
      "application/vnd.yamaha.smaf-audio": { source: `iana`, extensions: [`saf`] },
      "application/vnd.yamaha.smaf-phrase": { source: `iana`, extensions: [`spf`] },
      "application/vnd.yamaha.through-ngn": { source: `iana` },
      "application/vnd.yamaha.tunnel-udpencap": { source: `iana` },
      "application/vnd.yaoweme": { source: `iana` },
      "application/vnd.yellowriver-custom-menu": { source: `iana`, extensions: [`cmp`] },
      "application/vnd.zul": { source: `iana`, extensions: [`zir`, `zirz`] },
      "application/vnd.zzazz.deck+xml": { source: `iana`, compressible: !0, extensions: [`zaz`] },
      "application/voicexml+xml": { source: `iana`, compressible: !0, extensions: [`vxml`] },
      "application/voucher-cms+json": { source: `iana`, compressible: !0 },
      "application/voucher-jws+json": { source: `iana`, compressible: !0 },
      "application/vp": { source: `iana` },
      "application/vp+cose": { source: `iana` },
      "application/vp+jwt": { source: `iana` },
      "application/vq-rtcpxr": { source: `iana` },
      "application/wasm": { source: `iana`, compressible: !0, extensions: [`wasm`] },
      "application/watcherinfo+xml": { source: `iana`, compressible: !0, extensions: [`wif`] },
      "application/webpush-options+json": { source: `iana`, compressible: !0 },
      "application/whoispp-query": { source: `iana` },
      "application/whoispp-response": { source: `iana` },
      "application/widget": { source: `iana`, extensions: [`wgt`] },
      "application/winhlp": { source: `apache`, extensions: [`hlp`] },
      "application/wita": { source: `iana` },
      "application/wordperfect5.1": { source: `iana` },
      "application/wsdl+xml": { source: `iana`, compressible: !0, extensions: [`wsdl`] },
      "application/wspolicy+xml": { source: `iana`, compressible: !0, extensions: [`wspolicy`] },
      "application/x-7z-compressed": { source: `apache`, compressible: !1, extensions: [`7z`] },
      "application/x-abiword": { source: `apache`, extensions: [`abw`] },
      "application/x-ace-compressed": { source: `apache`, extensions: [`ace`] },
      "application/x-amf": { source: `apache` },
      "application/x-apple-diskimage": { source: `apache`, extensions: [`dmg`] },
      "application/x-arj": { compressible: !1, extensions: [`arj`] },
      "application/x-authorware-bin": {
        source: `apache`,
        extensions: [`aab`, `x32`, `u32`, `vox`],
      },
      "application/x-authorware-map": { source: `apache`, extensions: [`aam`] },
      "application/x-authorware-seg": { source: `apache`, extensions: [`aas`] },
      "application/x-bcpio": { source: `apache`, extensions: [`bcpio`] },
      "application/x-bdoc": { compressible: !1, extensions: [`bdoc`] },
      "application/x-bittorrent": { source: `apache`, extensions: [`torrent`] },
      "application/x-blender": { extensions: [`blend`] },
      "application/x-blorb": { source: `apache`, extensions: [`blb`, `blorb`] },
      "application/x-bzip": { source: `apache`, compressible: !1, extensions: [`bz`] },
      "application/x-bzip2": { source: `apache`, compressible: !1, extensions: [`bz2`, `boz`] },
      "application/x-cbr": { source: `apache`, extensions: [`cbr`, `cba`, `cbt`, `cbz`, `cb7`] },
      "application/x-cdlink": { source: `apache`, extensions: [`vcd`] },
      "application/x-cfs-compressed": { source: `apache`, extensions: [`cfs`] },
      "application/x-chat": { source: `apache`, extensions: [`chat`] },
      "application/x-chess-pgn": { source: `apache`, extensions: [`pgn`] },
      "application/x-chrome-extension": { extensions: [`crx`] },
      "application/x-cocoa": { source: `nginx`, extensions: [`cco`] },
      "application/x-compress": { source: `apache` },
      "application/x-compressed": { extensions: [`rar`] },
      "application/x-conference": { source: `apache`, extensions: [`nsc`] },
      "application/x-cpio": { source: `apache`, extensions: [`cpio`] },
      "application/x-csh": { source: `apache`, extensions: [`csh`] },
      "application/x-deb": { compressible: !1 },
      "application/x-debian-package": { source: `apache`, extensions: [`deb`, `udeb`] },
      "application/x-dgc-compressed": { source: `apache`, extensions: [`dgc`] },
      "application/x-director": {
        source: `apache`,
        extensions: [`dir`, `dcr`, `dxr`, `cst`, `cct`, `cxt`, `w3d`, `fgd`, `swa`],
      },
      "application/x-doom": { source: `apache`, extensions: [`wad`] },
      "application/x-dtbncx+xml": { source: `apache`, compressible: !0, extensions: [`ncx`] },
      "application/x-dtbook+xml": { source: `apache`, compressible: !0, extensions: [`dtb`] },
      "application/x-dtbresource+xml": { source: `apache`, compressible: !0, extensions: [`res`] },
      "application/x-dvi": { source: `apache`, compressible: !1, extensions: [`dvi`] },
      "application/x-envoy": { source: `apache`, extensions: [`evy`] },
      "application/x-eva": { source: `apache`, extensions: [`eva`] },
      "application/x-font-bdf": { source: `apache`, extensions: [`bdf`] },
      "application/x-font-dos": { source: `apache` },
      "application/x-font-framemaker": { source: `apache` },
      "application/x-font-ghostscript": { source: `apache`, extensions: [`gsf`] },
      "application/x-font-libgrx": { source: `apache` },
      "application/x-font-linux-psf": { source: `apache`, extensions: [`psf`] },
      "application/x-font-pcf": { source: `apache`, extensions: [`pcf`] },
      "application/x-font-snf": { source: `apache`, extensions: [`snf`] },
      "application/x-font-speedo": { source: `apache` },
      "application/x-font-sunos-news": { source: `apache` },
      "application/x-font-type1": { source: `apache`, extensions: [`pfa`, `pfb`, `pfm`, `afm`] },
      "application/x-font-vfont": { source: `apache` },
      "application/x-freearc": { source: `apache`, extensions: [`arc`] },
      "application/x-futuresplash": { source: `apache`, extensions: [`spl`] },
      "application/x-gca-compressed": { source: `apache`, extensions: [`gca`] },
      "application/x-glulx": { source: `apache`, extensions: [`ulx`] },
      "application/x-gnumeric": { source: `apache`, extensions: [`gnumeric`] },
      "application/x-gramps-xml": { source: `apache`, extensions: [`gramps`] },
      "application/x-gtar": { source: `apache`, extensions: [`gtar`] },
      "application/x-gzip": { source: `apache` },
      "application/x-hdf": { source: `apache`, extensions: [`hdf`] },
      "application/x-httpd-php": { compressible: !0, extensions: [`php`] },
      "application/x-install-instructions": { source: `apache`, extensions: [`install`] },
      "application/x-ipynb+json": { compressible: !0, extensions: [`ipynb`] },
      "application/x-iso9660-image": { source: `apache`, extensions: [`iso`] },
      "application/x-iwork-keynote-sffkey": { extensions: [`key`] },
      "application/x-iwork-numbers-sffnumbers": { extensions: [`numbers`] },
      "application/x-iwork-pages-sffpages": { extensions: [`pages`] },
      "application/x-java-archive-diff": { source: `nginx`, extensions: [`jardiff`] },
      "application/x-java-jnlp-file": { source: `apache`, compressible: !1, extensions: [`jnlp`] },
      "application/x-javascript": { compressible: !0 },
      "application/x-keepass2": { extensions: [`kdbx`] },
      "application/x-latex": { source: `apache`, compressible: !1, extensions: [`latex`] },
      "application/x-lua-bytecode": { extensions: [`luac`] },
      "application/x-lzh-compressed": { source: `apache`, extensions: [`lzh`, `lha`] },
      "application/x-makeself": { source: `nginx`, extensions: [`run`] },
      "application/x-mie": { source: `apache`, extensions: [`mie`] },
      "application/x-mobipocket-ebook": { source: `apache`, extensions: [`prc`, `mobi`] },
      "application/x-mpegurl": { compressible: !1 },
      "application/x-ms-application": { source: `apache`, extensions: [`application`] },
      "application/x-ms-shortcut": { source: `apache`, extensions: [`lnk`] },
      "application/x-ms-wmd": { source: `apache`, extensions: [`wmd`] },
      "application/x-ms-wmz": { source: `apache`, extensions: [`wmz`] },
      "application/x-ms-xbap": { source: `apache`, extensions: [`xbap`] },
      "application/x-msaccess": { source: `apache`, extensions: [`mdb`] },
      "application/x-msbinder": { source: `apache`, extensions: [`obd`] },
      "application/x-mscardfile": { source: `apache`, extensions: [`crd`] },
      "application/x-msclip": { source: `apache`, extensions: [`clp`] },
      "application/x-msdos-program": { extensions: [`exe`] },
      "application/x-msdownload": {
        source: `apache`,
        extensions: [`exe`, `dll`, `com`, `bat`, `msi`],
      },
      "application/x-msmediaview": { source: `apache`, extensions: [`mvb`, `m13`, `m14`] },
      "application/x-msmetafile": { source: `apache`, extensions: [`wmf`, `wmz`, `emf`, `emz`] },
      "application/x-msmoney": { source: `apache`, extensions: [`mny`] },
      "application/x-mspublisher": { source: `apache`, extensions: [`pub`] },
      "application/x-msschedule": { source: `apache`, extensions: [`scd`] },
      "application/x-msterminal": { source: `apache`, extensions: [`trm`] },
      "application/x-mswrite": { source: `apache`, extensions: [`wri`] },
      "application/x-netcdf": { source: `apache`, extensions: [`nc`, `cdf`] },
      "application/x-ns-proxy-autoconfig": { compressible: !0, extensions: [`pac`] },
      "application/x-nzb": { source: `apache`, extensions: [`nzb`] },
      "application/x-perl": { source: `nginx`, extensions: [`pl`, `pm`] },
      "application/x-pilot": { source: `nginx`, extensions: [`prc`, `pdb`] },
      "application/x-pkcs12": { source: `apache`, compressible: !1, extensions: [`p12`, `pfx`] },
      "application/x-pkcs7-certificates": { source: `apache`, extensions: [`p7b`, `spc`] },
      "application/x-pkcs7-certreqresp": { source: `apache`, extensions: [`p7r`] },
      "application/x-pki-message": { source: `iana` },
      "application/x-rar-compressed": { source: `apache`, compressible: !1, extensions: [`rar`] },
      "application/x-redhat-package-manager": { source: `nginx`, extensions: [`rpm`] },
      "application/x-research-info-systems": { source: `apache`, extensions: [`ris`] },
      "application/x-sea": { source: `nginx`, extensions: [`sea`] },
      "application/x-sh": { source: `apache`, compressible: !0, extensions: [`sh`] },
      "application/x-shar": { source: `apache`, extensions: [`shar`] },
      "application/x-shockwave-flash": { source: `apache`, compressible: !1, extensions: [`swf`] },
      "application/x-silverlight-app": { source: `apache`, extensions: [`xap`] },
      "application/x-sql": { source: `apache`, extensions: [`sql`] },
      "application/x-stuffit": { source: `apache`, compressible: !1, extensions: [`sit`] },
      "application/x-stuffitx": { source: `apache`, extensions: [`sitx`] },
      "application/x-subrip": { source: `apache`, extensions: [`srt`] },
      "application/x-sv4cpio": { source: `apache`, extensions: [`sv4cpio`] },
      "application/x-sv4crc": { source: `apache`, extensions: [`sv4crc`] },
      "application/x-t3vm-image": { source: `apache`, extensions: [`t3`] },
      "application/x-tads": { source: `apache`, extensions: [`gam`] },
      "application/x-tar": { source: `apache`, compressible: !0, extensions: [`tar`] },
      "application/x-tcl": { source: `apache`, extensions: [`tcl`, `tk`] },
      "application/x-tex": { source: `apache`, extensions: [`tex`] },
      "application/x-tex-tfm": { source: `apache`, extensions: [`tfm`] },
      "application/x-texinfo": { source: `apache`, extensions: [`texinfo`, `texi`] },
      "application/x-tgif": { source: `apache`, extensions: [`obj`] },
      "application/x-ustar": { source: `apache`, extensions: [`ustar`] },
      "application/x-virtualbox-hdd": { compressible: !0, extensions: [`hdd`] },
      "application/x-virtualbox-ova": { compressible: !0, extensions: [`ova`] },
      "application/x-virtualbox-ovf": { compressible: !0, extensions: [`ovf`] },
      "application/x-virtualbox-vbox": { compressible: !0, extensions: [`vbox`] },
      "application/x-virtualbox-vbox-extpack": { compressible: !1, extensions: [`vbox-extpack`] },
      "application/x-virtualbox-vdi": { compressible: !0, extensions: [`vdi`] },
      "application/x-virtualbox-vhd": { compressible: !0, extensions: [`vhd`] },
      "application/x-virtualbox-vmdk": { compressible: !0, extensions: [`vmdk`] },
      "application/x-wais-source": { source: `apache`, extensions: [`src`] },
      "application/x-web-app-manifest+json": { compressible: !0, extensions: [`webapp`] },
      "application/x-www-form-urlencoded": { source: `iana`, compressible: !0 },
      "application/x-x509-ca-cert": { source: `iana`, extensions: [`der`, `crt`, `pem`] },
      "application/x-x509-ca-ra-cert": { source: `iana` },
      "application/x-x509-next-ca-cert": { source: `iana` },
      "application/x-xfig": { source: `apache`, extensions: [`fig`] },
      "application/x-xliff+xml": { source: `apache`, compressible: !0, extensions: [`xlf`] },
      "application/x-xpinstall": { source: `apache`, compressible: !1, extensions: [`xpi`] },
      "application/x-xz": { source: `apache`, extensions: [`xz`] },
      "application/x-zip-compressed": { extensions: [`zip`] },
      "application/x-zmachine": {
        source: `apache`,
        extensions: [`z1`, `z2`, `z3`, `z4`, `z5`, `z6`, `z7`, `z8`],
      },
      "application/x400-bp": { source: `iana` },
      "application/xacml+xml": { source: `iana`, compressible: !0 },
      "application/xaml+xml": { source: `apache`, compressible: !0, extensions: [`xaml`] },
      "application/xcap-att+xml": { source: `iana`, compressible: !0, extensions: [`xav`] },
      "application/xcap-caps+xml": { source: `iana`, compressible: !0, extensions: [`xca`] },
      "application/xcap-diff+xml": { source: `iana`, compressible: !0, extensions: [`xdf`] },
      "application/xcap-el+xml": { source: `iana`, compressible: !0, extensions: [`xel`] },
      "application/xcap-error+xml": { source: `iana`, compressible: !0 },
      "application/xcap-ns+xml": { source: `iana`, compressible: !0, extensions: [`xns`] },
      "application/xcon-conference-info+xml": { source: `iana`, compressible: !0 },
      "application/xcon-conference-info-diff+xml": { source: `iana`, compressible: !0 },
      "application/xenc+xml": { source: `iana`, compressible: !0, extensions: [`xenc`] },
      "application/xfdf": { source: `iana`, extensions: [`xfdf`] },
      "application/xhtml+xml": { source: `iana`, compressible: !0, extensions: [`xhtml`, `xht`] },
      "application/xhtml-voice+xml": { source: `apache`, compressible: !0 },
      "application/xliff+xml": { source: `iana`, compressible: !0, extensions: [`xlf`] },
      "application/xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`xml`, `xsl`, `xsd`, `rng`],
      },
      "application/xml-dtd": { source: `iana`, compressible: !0, extensions: [`dtd`] },
      "application/xml-external-parsed-entity": { source: `iana` },
      "application/xml-patch+xml": { source: `iana`, compressible: !0 },
      "application/xmpp+xml": { source: `iana`, compressible: !0 },
      "application/xop+xml": { source: `iana`, compressible: !0, extensions: [`xop`] },
      "application/xproc+xml": { source: `apache`, compressible: !0, extensions: [`xpl`] },
      "application/xslt+xml": { source: `iana`, compressible: !0, extensions: [`xsl`, `xslt`] },
      "application/xspf+xml": { source: `apache`, compressible: !0, extensions: [`xspf`] },
      "application/xv+xml": {
        source: `iana`,
        compressible: !0,
        extensions: [`mxml`, `xhvml`, `xvml`, `xvm`],
      },
      "application/yaml": { source: `iana` },
      "application/yang": { source: `iana`, extensions: [`yang`] },
      "application/yang-data+cbor": { source: `iana` },
      "application/yang-data+json": { source: `iana`, compressible: !0 },
      "application/yang-data+xml": { source: `iana`, compressible: !0 },
      "application/yang-patch+json": { source: `iana`, compressible: !0 },
      "application/yang-patch+xml": { source: `iana`, compressible: !0 },
      "application/yang-sid+json": { source: `iana`, compressible: !0 },
      "application/yin+xml": { source: `iana`, compressible: !0, extensions: [`yin`] },
      "application/zip": { source: `iana`, compressible: !1, extensions: [`zip`] },
      "application/zip+dotlottie": { extensions: [`lottie`] },
      "application/zlib": { source: `iana` },
      "application/zstd": { source: `iana` },
      "audio/1d-interleaved-parityfec": { source: `iana` },
      "audio/32kadpcm": { source: `iana` },
      "audio/3gpp": { source: `iana`, compressible: !1, extensions: [`3gpp`] },
      "audio/3gpp2": { source: `iana` },
      "audio/aac": { source: `iana`, extensions: [`adts`, `aac`] },
      "audio/ac3": { source: `iana` },
      "audio/adpcm": { source: `apache`, extensions: [`adp`] },
      "audio/amr": { source: `iana`, extensions: [`amr`] },
      "audio/amr-wb": { source: `iana` },
      "audio/amr-wb+": { source: `iana` },
      "audio/aptx": { source: `iana` },
      "audio/asc": { source: `iana` },
      "audio/atrac-advanced-lossless": { source: `iana` },
      "audio/atrac-x": { source: `iana` },
      "audio/atrac3": { source: `iana` },
      "audio/basic": { source: `iana`, compressible: !1, extensions: [`au`, `snd`] },
      "audio/bv16": { source: `iana` },
      "audio/bv32": { source: `iana` },
      "audio/clearmode": { source: `iana` },
      "audio/cn": { source: `iana` },
      "audio/dat12": { source: `iana` },
      "audio/dls": { source: `iana` },
      "audio/dsr-es201108": { source: `iana` },
      "audio/dsr-es202050": { source: `iana` },
      "audio/dsr-es202211": { source: `iana` },
      "audio/dsr-es202212": { source: `iana` },
      "audio/dv": { source: `iana` },
      "audio/dvi4": { source: `iana` },
      "audio/eac3": { source: `iana` },
      "audio/encaprtp": { source: `iana` },
      "audio/evrc": { source: `iana` },
      "audio/evrc-qcp": { source: `iana` },
      "audio/evrc0": { source: `iana` },
      "audio/evrc1": { source: `iana` },
      "audio/evrcb": { source: `iana` },
      "audio/evrcb0": { source: `iana` },
      "audio/evrcb1": { source: `iana` },
      "audio/evrcnw": { source: `iana` },
      "audio/evrcnw0": { source: `iana` },
      "audio/evrcnw1": { source: `iana` },
      "audio/evrcwb": { source: `iana` },
      "audio/evrcwb0": { source: `iana` },
      "audio/evrcwb1": { source: `iana` },
      "audio/evs": { source: `iana` },
      "audio/flac": { source: `iana` },
      "audio/flexfec": { source: `iana` },
      "audio/fwdred": { source: `iana` },
      "audio/g711-0": { source: `iana` },
      "audio/g719": { source: `iana` },
      "audio/g722": { source: `iana` },
      "audio/g7221": { source: `iana` },
      "audio/g723": { source: `iana` },
      "audio/g726-16": { source: `iana` },
      "audio/g726-24": { source: `iana` },
      "audio/g726-32": { source: `iana` },
      "audio/g726-40": { source: `iana` },
      "audio/g728": { source: `iana` },
      "audio/g729": { source: `iana` },
      "audio/g7291": { source: `iana` },
      "audio/g729d": { source: `iana` },
      "audio/g729e": { source: `iana` },
      "audio/gsm": { source: `iana` },
      "audio/gsm-efr": { source: `iana` },
      "audio/gsm-hr-08": { source: `iana` },
      "audio/ilbc": { source: `iana` },
      "audio/ip-mr_v2.5": { source: `iana` },
      "audio/isac": { source: `apache` },
      "audio/l16": { source: `iana` },
      "audio/l20": { source: `iana` },
      "audio/l24": { source: `iana`, compressible: !1 },
      "audio/l8": { source: `iana` },
      "audio/lpc": { source: `iana` },
      "audio/matroska": { source: `iana` },
      "audio/melp": { source: `iana` },
      "audio/melp1200": { source: `iana` },
      "audio/melp2400": { source: `iana` },
      "audio/melp600": { source: `iana` },
      "audio/mhas": { source: `iana` },
      "audio/midi": { source: `apache`, extensions: [`mid`, `midi`, `kar`, `rmi`] },
      "audio/midi-clip": { source: `iana` },
      "audio/mobile-xmf": { source: `iana`, extensions: [`mxmf`] },
      "audio/mp3": { compressible: !1, extensions: [`mp3`] },
      "audio/mp4": { source: `iana`, compressible: !1, extensions: [`m4a`, `mp4a`, `m4b`] },
      "audio/mp4a-latm": { source: `iana` },
      "audio/mpa": { source: `iana` },
      "audio/mpa-robust": { source: `iana` },
      "audio/mpeg": {
        source: `iana`,
        compressible: !1,
        extensions: [`mpga`, `mp2`, `mp2a`, `mp3`, `m2a`, `m3a`],
      },
      "audio/mpeg4-generic": { source: `iana` },
      "audio/musepack": { source: `apache` },
      "audio/ogg": { source: `iana`, compressible: !1, extensions: [`oga`, `ogg`, `spx`, `opus`] },
      "audio/opus": { source: `iana` },
      "audio/parityfec": { source: `iana` },
      "audio/pcma": { source: `iana` },
      "audio/pcma-wb": { source: `iana` },
      "audio/pcmu": { source: `iana` },
      "audio/pcmu-wb": { source: `iana` },
      "audio/prs.sid": { source: `iana` },
      "audio/qcelp": { source: `iana` },
      "audio/raptorfec": { source: `iana` },
      "audio/red": { source: `iana` },
      "audio/rtp-enc-aescm128": { source: `iana` },
      "audio/rtp-midi": { source: `iana` },
      "audio/rtploopback": { source: `iana` },
      "audio/rtx": { source: `iana` },
      "audio/s3m": { source: `apache`, extensions: [`s3m`] },
      "audio/scip": { source: `iana` },
      "audio/silk": { source: `apache`, extensions: [`sil`] },
      "audio/smv": { source: `iana` },
      "audio/smv-qcp": { source: `iana` },
      "audio/smv0": { source: `iana` },
      "audio/sofa": { source: `iana` },
      "audio/sp-midi": { source: `iana` },
      "audio/speex": { source: `iana` },
      "audio/t140c": { source: `iana` },
      "audio/t38": { source: `iana` },
      "audio/telephone-event": { source: `iana` },
      "audio/tetra_acelp": { source: `iana` },
      "audio/tetra_acelp_bb": { source: `iana` },
      "audio/tone": { source: `iana` },
      "audio/tsvcis": { source: `iana` },
      "audio/uemclip": { source: `iana` },
      "audio/ulpfec": { source: `iana` },
      "audio/usac": { source: `iana` },
      "audio/vdvi": { source: `iana` },
      "audio/vmr-wb": { source: `iana` },
      "audio/vnd.3gpp.iufp": { source: `iana` },
      "audio/vnd.4sb": { source: `iana` },
      "audio/vnd.audiokoz": { source: `iana` },
      "audio/vnd.celp": { source: `iana` },
      "audio/vnd.cisco.nse": { source: `iana` },
      "audio/vnd.cmles.radio-events": { source: `iana` },
      "audio/vnd.cns.anp1": { source: `iana` },
      "audio/vnd.cns.inf1": { source: `iana` },
      "audio/vnd.dece.audio": { source: `iana`, extensions: [`uva`, `uvva`] },
      "audio/vnd.digital-winds": { source: `iana`, extensions: [`eol`] },
      "audio/vnd.dlna.adts": { source: `iana` },
      "audio/vnd.dolby.heaac.1": { source: `iana` },
      "audio/vnd.dolby.heaac.2": { source: `iana` },
      "audio/vnd.dolby.mlp": { source: `iana` },
      "audio/vnd.dolby.mps": { source: `iana` },
      "audio/vnd.dolby.pl2": { source: `iana` },
      "audio/vnd.dolby.pl2x": { source: `iana` },
      "audio/vnd.dolby.pl2z": { source: `iana` },
      "audio/vnd.dolby.pulse.1": { source: `iana` },
      "audio/vnd.dra": { source: `iana`, extensions: [`dra`] },
      "audio/vnd.dts": { source: `iana`, extensions: [`dts`] },
      "audio/vnd.dts.hd": { source: `iana`, extensions: [`dtshd`] },
      "audio/vnd.dts.uhd": { source: `iana` },
      "audio/vnd.dvb.file": { source: `iana` },
      "audio/vnd.everad.plj": { source: `iana` },
      "audio/vnd.hns.audio": { source: `iana` },
      "audio/vnd.lucent.voice": { source: `iana`, extensions: [`lvp`] },
      "audio/vnd.ms-playready.media.pya": { source: `iana`, extensions: [`pya`] },
      "audio/vnd.nokia.mobile-xmf": { source: `iana` },
      "audio/vnd.nortel.vbk": { source: `iana` },
      "audio/vnd.nuera.ecelp4800": { source: `iana`, extensions: [`ecelp4800`] },
      "audio/vnd.nuera.ecelp7470": { source: `iana`, extensions: [`ecelp7470`] },
      "audio/vnd.nuera.ecelp9600": { source: `iana`, extensions: [`ecelp9600`] },
      "audio/vnd.octel.sbc": { source: `iana` },
      "audio/vnd.presonus.multitrack": { source: `iana` },
      "audio/vnd.qcelp": { source: `apache` },
      "audio/vnd.rhetorex.32kadpcm": { source: `iana` },
      "audio/vnd.rip": { source: `iana`, extensions: [`rip`] },
      "audio/vnd.rn-realaudio": { compressible: !1 },
      "audio/vnd.sealedmedia.softseal.mpeg": { source: `iana` },
      "audio/vnd.vmx.cvsd": { source: `iana` },
      "audio/vnd.wave": { compressible: !1 },
      "audio/vorbis": { source: `iana`, compressible: !1 },
      "audio/vorbis-config": { source: `iana` },
      "audio/wav": { compressible: !1, extensions: [`wav`] },
      "audio/wave": { compressible: !1, extensions: [`wav`] },
      "audio/webm": { source: `apache`, compressible: !1, extensions: [`weba`] },
      "audio/x-aac": { source: `apache`, compressible: !1, extensions: [`aac`] },
      "audio/x-aiff": { source: `apache`, extensions: [`aif`, `aiff`, `aifc`] },
      "audio/x-caf": { source: `apache`, compressible: !1, extensions: [`caf`] },
      "audio/x-flac": { source: `apache`, extensions: [`flac`] },
      "audio/x-m4a": { source: `nginx`, extensions: [`m4a`] },
      "audio/x-matroska": { source: `apache`, extensions: [`mka`] },
      "audio/x-mpegurl": { source: `apache`, extensions: [`m3u`] },
      "audio/x-ms-wax": { source: `apache`, extensions: [`wax`] },
      "audio/x-ms-wma": { source: `apache`, extensions: [`wma`] },
      "audio/x-pn-realaudio": { source: `apache`, extensions: [`ram`, `ra`] },
      "audio/x-pn-realaudio-plugin": { source: `apache`, extensions: [`rmp`] },
      "audio/x-realaudio": { source: `nginx`, extensions: [`ra`] },
      "audio/x-tta": { source: `apache` },
      "audio/x-wav": { source: `apache`, extensions: [`wav`] },
      "audio/xm": { source: `apache`, extensions: [`xm`] },
      "chemical/x-cdx": { source: `apache`, extensions: [`cdx`] },
      "chemical/x-cif": { source: `apache`, extensions: [`cif`] },
      "chemical/x-cmdf": { source: `apache`, extensions: [`cmdf`] },
      "chemical/x-cml": { source: `apache`, extensions: [`cml`] },
      "chemical/x-csml": { source: `apache`, extensions: [`csml`] },
      "chemical/x-pdb": { source: `apache` },
      "chemical/x-xyz": { source: `apache`, extensions: [`xyz`] },
      "font/collection": { source: `iana`, extensions: [`ttc`] },
      "font/otf": { source: `iana`, compressible: !0, extensions: [`otf`] },
      "font/sfnt": { source: `iana` },
      "font/ttf": { source: `iana`, compressible: !0, extensions: [`ttf`] },
      "font/woff": { source: `iana`, extensions: [`woff`] },
      "font/woff2": { source: `iana`, extensions: [`woff2`] },
      "image/aces": { source: `iana`, extensions: [`exr`] },
      "image/apng": { source: `iana`, compressible: !1, extensions: [`apng`] },
      "image/avci": { source: `iana`, extensions: [`avci`] },
      "image/avcs": { source: `iana`, extensions: [`avcs`] },
      "image/avif": { source: `iana`, compressible: !1, extensions: [`avif`] },
      "image/bmp": { source: `iana`, compressible: !0, extensions: [`bmp`, `dib`] },
      "image/cgm": { source: `iana`, extensions: [`cgm`] },
      "image/dicom-rle": { source: `iana`, extensions: [`drle`] },
      "image/dpx": { source: `iana`, extensions: [`dpx`] },
      "image/emf": { source: `iana`, extensions: [`emf`] },
      "image/fits": { source: `iana`, extensions: [`fits`] },
      "image/g3fax": { source: `iana`, extensions: [`g3`] },
      "image/gif": { source: `iana`, compressible: !1, extensions: [`gif`] },
      "image/heic": { source: `iana`, extensions: [`heic`] },
      "image/heic-sequence": { source: `iana`, extensions: [`heics`] },
      "image/heif": { source: `iana`, extensions: [`heif`] },
      "image/heif-sequence": { source: `iana`, extensions: [`heifs`] },
      "image/hej2k": { source: `iana`, extensions: [`hej2`] },
      "image/ief": { source: `iana`, extensions: [`ief`] },
      "image/j2c": { source: `iana` },
      "image/jaii": { source: `iana`, extensions: [`jaii`] },
      "image/jais": { source: `iana`, extensions: [`jais`] },
      "image/jls": { source: `iana`, extensions: [`jls`] },
      "image/jp2": { source: `iana`, compressible: !1, extensions: [`jp2`, `jpg2`] },
      "image/jpeg": { source: `iana`, compressible: !1, extensions: [`jpg`, `jpeg`, `jpe`] },
      "image/jph": { source: `iana`, extensions: [`jph`] },
      "image/jphc": { source: `iana`, extensions: [`jhc`] },
      "image/jpm": { source: `iana`, compressible: !1, extensions: [`jpm`, `jpgm`] },
      "image/jpx": { source: `iana`, compressible: !1, extensions: [`jpx`, `jpf`] },
      "image/jxl": { source: `iana`, extensions: [`jxl`] },
      "image/jxr": { source: `iana`, extensions: [`jxr`] },
      "image/jxra": { source: `iana`, extensions: [`jxra`] },
      "image/jxrs": { source: `iana`, extensions: [`jxrs`] },
      "image/jxs": { source: `iana`, extensions: [`jxs`] },
      "image/jxsc": { source: `iana`, extensions: [`jxsc`] },
      "image/jxsi": { source: `iana`, extensions: [`jxsi`] },
      "image/jxss": { source: `iana`, extensions: [`jxss`] },
      "image/ktx": { source: `iana`, extensions: [`ktx`] },
      "image/ktx2": { source: `iana`, extensions: [`ktx2`] },
      "image/naplps": { source: `iana` },
      "image/pjpeg": { compressible: !1, extensions: [`jfif`] },
      "image/png": { source: `iana`, compressible: !1, extensions: [`png`] },
      "image/prs.btif": { source: `iana`, extensions: [`btif`, `btf`] },
      "image/prs.pti": { source: `iana`, extensions: [`pti`] },
      "image/pwg-raster": { source: `iana` },
      "image/sgi": { source: `apache`, extensions: [`sgi`] },
      "image/svg+xml": { source: `iana`, compressible: !0, extensions: [`svg`, `svgz`] },
      "image/t38": { source: `iana`, extensions: [`t38`] },
      "image/tiff": { source: `iana`, compressible: !1, extensions: [`tif`, `tiff`] },
      "image/tiff-fx": { source: `iana`, extensions: [`tfx`] },
      "image/vnd.adobe.photoshop": { source: `iana`, compressible: !0, extensions: [`psd`] },
      "image/vnd.airzip.accelerator.azv": { source: `iana`, extensions: [`azv`] },
      "image/vnd.clip": { source: `iana` },
      "image/vnd.cns.inf2": { source: `iana` },
      "image/vnd.dece.graphic": { source: `iana`, extensions: [`uvi`, `uvvi`, `uvg`, `uvvg`] },
      "image/vnd.djvu": { source: `iana`, extensions: [`djvu`, `djv`] },
      "image/vnd.dvb.subtitle": { source: `iana`, extensions: [`sub`] },
      "image/vnd.dwg": { source: `iana`, extensions: [`dwg`] },
      "image/vnd.dxf": { source: `iana`, extensions: [`dxf`] },
      "image/vnd.fastbidsheet": { source: `iana`, extensions: [`fbs`] },
      "image/vnd.fpx": { source: `iana`, extensions: [`fpx`] },
      "image/vnd.fst": { source: `iana`, extensions: [`fst`] },
      "image/vnd.fujixerox.edmics-mmr": { source: `iana`, extensions: [`mmr`] },
      "image/vnd.fujixerox.edmics-rlc": { source: `iana`, extensions: [`rlc`] },
      "image/vnd.globalgraphics.pgb": { source: `iana` },
      "image/vnd.microsoft.icon": { source: `iana`, compressible: !0, extensions: [`ico`] },
      "image/vnd.mix": { source: `iana` },
      "image/vnd.mozilla.apng": { source: `iana` },
      "image/vnd.ms-dds": { compressible: !0, extensions: [`dds`] },
      "image/vnd.ms-modi": { source: `iana`, extensions: [`mdi`] },
      "image/vnd.ms-photo": { source: `apache`, extensions: [`wdp`] },
      "image/vnd.net-fpx": { source: `iana`, extensions: [`npx`] },
      "image/vnd.pco.b16": { source: `iana`, extensions: [`b16`] },
      "image/vnd.radiance": { source: `iana` },
      "image/vnd.sealed.png": { source: `iana` },
      "image/vnd.sealedmedia.softseal.gif": { source: `iana` },
      "image/vnd.sealedmedia.softseal.jpg": { source: `iana` },
      "image/vnd.svf": { source: `iana` },
      "image/vnd.tencent.tap": { source: `iana`, extensions: [`tap`] },
      "image/vnd.valve.source.texture": { source: `iana`, extensions: [`vtf`] },
      "image/vnd.wap.wbmp": { source: `iana`, extensions: [`wbmp`] },
      "image/vnd.xiff": { source: `iana`, extensions: [`xif`] },
      "image/vnd.zbrush.pcx": { source: `iana`, extensions: [`pcx`] },
      "image/webp": { source: `iana`, extensions: [`webp`] },
      "image/wmf": { source: `iana`, extensions: [`wmf`] },
      "image/x-3ds": { source: `apache`, extensions: [`3ds`] },
      "image/x-adobe-dng": { extensions: [`dng`] },
      "image/x-cmu-raster": { source: `apache`, extensions: [`ras`] },
      "image/x-cmx": { source: `apache`, extensions: [`cmx`] },
      "image/x-emf": { source: `iana` },
      "image/x-freehand": { source: `apache`, extensions: [`fh`, `fhc`, `fh4`, `fh5`, `fh7`] },
      "image/x-icon": { source: `apache`, compressible: !0, extensions: [`ico`] },
      "image/x-jng": { source: `nginx`, extensions: [`jng`] },
      "image/x-mrsid-image": { source: `apache`, extensions: [`sid`] },
      "image/x-ms-bmp": { source: `nginx`, compressible: !0, extensions: [`bmp`] },
      "image/x-pcx": { source: `apache`, extensions: [`pcx`] },
      "image/x-pict": { source: `apache`, extensions: [`pic`, `pct`] },
      "image/x-portable-anymap": { source: `apache`, extensions: [`pnm`] },
      "image/x-portable-bitmap": { source: `apache`, extensions: [`pbm`] },
      "image/x-portable-graymap": { source: `apache`, extensions: [`pgm`] },
      "image/x-portable-pixmap": { source: `apache`, extensions: [`ppm`] },
      "image/x-rgb": { source: `apache`, extensions: [`rgb`] },
      "image/x-tga": { source: `apache`, extensions: [`tga`] },
      "image/x-wmf": { source: `iana` },
      "image/x-xbitmap": { source: `apache`, extensions: [`xbm`] },
      "image/x-xcf": { compressible: !1 },
      "image/x-xpixmap": { source: `apache`, extensions: [`xpm`] },
      "image/x-xwindowdump": { source: `apache`, extensions: [`xwd`] },
      "message/bhttp": { source: `iana` },
      "message/cpim": { source: `iana` },
      "message/delivery-status": { source: `iana` },
      "message/disposition-notification": {
        source: `iana`,
        extensions: [`disposition-notification`],
      },
      "message/external-body": { source: `iana` },
      "message/feedback-report": { source: `iana` },
      "message/global": { source: `iana`, extensions: [`u8msg`] },
      "message/global-delivery-status": { source: `iana`, extensions: [`u8dsn`] },
      "message/global-disposition-notification": { source: `iana`, extensions: [`u8mdn`] },
      "message/global-headers": { source: `iana`, extensions: [`u8hdr`] },
      "message/http": { source: `iana`, compressible: !1 },
      "message/imdn+xml": { source: `iana`, compressible: !0 },
      "message/mls": { source: `iana` },
      "message/news": { source: `apache` },
      "message/ohttp-req": { source: `iana` },
      "message/ohttp-res": { source: `iana` },
      "message/partial": { source: `iana`, compressible: !1 },
      "message/rfc822": {
        source: `iana`,
        compressible: !0,
        extensions: [`eml`, `mime`, `mht`, `mhtml`],
      },
      "message/s-http": { source: `apache` },
      "message/sip": { source: `iana` },
      "message/sipfrag": { source: `iana` },
      "message/tracking-status": { source: `iana` },
      "message/vnd.si.simp": { source: `apache` },
      "message/vnd.wfa.wsc": { source: `iana`, extensions: [`wsc`] },
      "model/3mf": { source: `iana`, extensions: [`3mf`] },
      "model/e57": { source: `iana` },
      "model/gltf+json": { source: `iana`, compressible: !0, extensions: [`gltf`] },
      "model/gltf-binary": { source: `iana`, compressible: !0, extensions: [`glb`] },
      "model/iges": { source: `iana`, compressible: !1, extensions: [`igs`, `iges`] },
      "model/jt": { source: `iana`, extensions: [`jt`] },
      "model/mesh": { source: `iana`, compressible: !1, extensions: [`msh`, `mesh`, `silo`] },
      "model/mtl": { source: `iana`, extensions: [`mtl`] },
      "model/obj": { source: `iana`, extensions: [`obj`] },
      "model/prc": { source: `iana`, extensions: [`prc`] },
      "model/step": { source: `iana`, extensions: [`step`, `stp`, `stpnc`, `p21`, `210`] },
      "model/step+xml": { source: `iana`, compressible: !0, extensions: [`stpx`] },
      "model/step+zip": { source: `iana`, compressible: !1, extensions: [`stpz`] },
      "model/step-xml+zip": { source: `iana`, compressible: !1, extensions: [`stpxz`] },
      "model/stl": { source: `iana`, extensions: [`stl`] },
      "model/u3d": { source: `iana`, extensions: [`u3d`] },
      "model/vnd.bary": { source: `iana`, extensions: [`bary`] },
      "model/vnd.cld": { source: `iana`, extensions: [`cld`] },
      "model/vnd.collada+xml": { source: `iana`, compressible: !0, extensions: [`dae`] },
      "model/vnd.dwf": { source: `iana`, extensions: [`dwf`] },
      "model/vnd.flatland.3dml": { source: `iana` },
      "model/vnd.gdl": { source: `iana`, extensions: [`gdl`] },
      "model/vnd.gs-gdl": { source: `apache` },
      "model/vnd.gs.gdl": { source: `iana` },
      "model/vnd.gtw": { source: `iana`, extensions: [`gtw`] },
      "model/vnd.moml+xml": { source: `iana`, compressible: !0 },
      "model/vnd.mts": { source: `iana`, extensions: [`mts`] },
      "model/vnd.opengex": { source: `iana`, extensions: [`ogex`] },
      "model/vnd.parasolid.transmit.binary": { source: `iana`, extensions: [`x_b`] },
      "model/vnd.parasolid.transmit.text": { source: `iana`, extensions: [`x_t`] },
      "model/vnd.pytha.pyox": { source: `iana`, extensions: [`pyo`, `pyox`] },
      "model/vnd.rosette.annotated-data-model": { source: `iana` },
      "model/vnd.sap.vds": { source: `iana`, extensions: [`vds`] },
      "model/vnd.usda": { source: `iana`, extensions: [`usda`] },
      "model/vnd.usdz+zip": { source: `iana`, compressible: !1, extensions: [`usdz`] },
      "model/vnd.valve.source.compiled-map": { source: `iana`, extensions: [`bsp`] },
      "model/vnd.vtu": { source: `iana`, extensions: [`vtu`] },
      "model/vrml": { source: `iana`, compressible: !1, extensions: [`wrl`, `vrml`] },
      "model/x3d+binary": { source: `apache`, compressible: !1, extensions: [`x3db`, `x3dbz`] },
      "model/x3d+fastinfoset": { source: `iana`, extensions: [`x3db`] },
      "model/x3d+vrml": { source: `apache`, compressible: !1, extensions: [`x3dv`, `x3dvz`] },
      "model/x3d+xml": { source: `iana`, compressible: !0, extensions: [`x3d`, `x3dz`] },
      "model/x3d-vrml": { source: `iana`, extensions: [`x3dv`] },
      "multipart/alternative": { source: `iana`, compressible: !1 },
      "multipart/appledouble": { source: `iana` },
      "multipart/byteranges": { source: `iana` },
      "multipart/digest": { source: `iana` },
      "multipart/encrypted": { source: `iana`, compressible: !1 },
      "multipart/form-data": { source: `iana`, compressible: !1 },
      "multipart/header-set": { source: `iana` },
      "multipart/mixed": { source: `iana` },
      "multipart/multilingual": { source: `iana` },
      "multipart/parallel": { source: `iana` },
      "multipart/related": { source: `iana`, compressible: !1 },
      "multipart/report": { source: `iana` },
      "multipart/signed": { source: `iana`, compressible: !1 },
      "multipart/vnd.bint.med-plus": { source: `iana` },
      "multipart/voice-message": { source: `iana` },
      "multipart/x-mixed-replace": { source: `iana` },
      "text/1d-interleaved-parityfec": { source: `iana` },
      "text/cache-manifest": {
        source: `iana`,
        compressible: !0,
        extensions: [`appcache`, `manifest`],
      },
      "text/calendar": { source: `iana`, extensions: [`ics`, `ifb`] },
      "text/calender": { compressible: !0 },
      "text/cmd": { compressible: !0 },
      "text/coffeescript": { extensions: [`coffee`, `litcoffee`] },
      "text/cql": { source: `iana` },
      "text/cql-expression": { source: `iana` },
      "text/cql-identifier": { source: `iana` },
      "text/css": { source: `iana`, charset: `UTF-8`, compressible: !0, extensions: [`css`] },
      "text/csv": { source: `iana`, compressible: !0, extensions: [`csv`] },
      "text/csv-schema": { source: `iana` },
      "text/directory": { source: `iana` },
      "text/dns": { source: `iana` },
      "text/ecmascript": { source: `apache` },
      "text/encaprtp": { source: `iana` },
      "text/enriched": { source: `iana` },
      "text/fhirpath": { source: `iana` },
      "text/flexfec": { source: `iana` },
      "text/fwdred": { source: `iana` },
      "text/gff3": { source: `iana` },
      "text/grammar-ref-list": { source: `iana` },
      "text/hl7v2": { source: `iana` },
      "text/html": { source: `iana`, compressible: !0, extensions: [`html`, `htm`, `shtml`] },
      "text/jade": { extensions: [`jade`] },
      "text/javascript": {
        source: `iana`,
        charset: `UTF-8`,
        compressible: !0,
        extensions: [`js`, `mjs`],
      },
      "text/jcr-cnd": { source: `iana` },
      "text/jsx": { compressible: !0, extensions: [`jsx`] },
      "text/less": { compressible: !0, extensions: [`less`] },
      "text/markdown": { source: `iana`, compressible: !0, extensions: [`md`, `markdown`] },
      "text/mathml": { source: `nginx`, extensions: [`mml`] },
      "text/mdx": { compressible: !0, extensions: [`mdx`] },
      "text/mizar": { source: `iana` },
      "text/n3": { source: `iana`, charset: `UTF-8`, compressible: !0, extensions: [`n3`] },
      "text/parameters": { source: `iana`, charset: `UTF-8` },
      "text/parityfec": { source: `iana` },
      "text/plain": {
        source: `iana`,
        compressible: !0,
        extensions: [`txt`, `text`, `conf`, `def`, `list`, `log`, `in`, `ini`],
      },
      "text/provenance-notation": { source: `iana`, charset: `UTF-8` },
      "text/prs.fallenstein.rst": { source: `iana` },
      "text/prs.lines.tag": { source: `iana`, extensions: [`dsc`] },
      "text/prs.prop.logic": { source: `iana` },
      "text/prs.texi": { source: `iana` },
      "text/raptorfec": { source: `iana` },
      "text/red": { source: `iana` },
      "text/rfc822-headers": { source: `iana` },
      "text/richtext": { source: `iana`, compressible: !0, extensions: [`rtx`] },
      "text/rtf": { source: `iana`, compressible: !0, extensions: [`rtf`] },
      "text/rtp-enc-aescm128": { source: `iana` },
      "text/rtploopback": { source: `iana` },
      "text/rtx": { source: `iana` },
      "text/sgml": { source: `iana`, extensions: [`sgml`, `sgm`] },
      "text/shaclc": { source: `iana` },
      "text/shex": { source: `iana`, extensions: [`shex`] },
      "text/slim": { extensions: [`slim`, `slm`] },
      "text/spdx": { source: `iana`, extensions: [`spdx`] },
      "text/strings": { source: `iana` },
      "text/stylus": { extensions: [`stylus`, `styl`] },
      "text/t140": { source: `iana` },
      "text/tab-separated-values": { source: `iana`, compressible: !0, extensions: [`tsv`] },
      "text/troff": { source: `iana`, extensions: [`t`, `tr`, `roff`, `man`, `me`, `ms`] },
      "text/turtle": { source: `iana`, charset: `UTF-8`, extensions: [`ttl`] },
      "text/ulpfec": { source: `iana` },
      "text/uri-list": { source: `iana`, compressible: !0, extensions: [`uri`, `uris`, `urls`] },
      "text/vcard": { source: `iana`, compressible: !0, extensions: [`vcard`] },
      "text/vnd.a": { source: `iana` },
      "text/vnd.abc": { source: `iana` },
      "text/vnd.ascii-art": { source: `iana` },
      "text/vnd.curl": { source: `iana`, extensions: [`curl`] },
      "text/vnd.curl.dcurl": { source: `apache`, extensions: [`dcurl`] },
      "text/vnd.curl.mcurl": { source: `apache`, extensions: [`mcurl`] },
      "text/vnd.curl.scurl": { source: `apache`, extensions: [`scurl`] },
      "text/vnd.debian.copyright": { source: `iana`, charset: `UTF-8` },
      "text/vnd.dmclientscript": { source: `iana` },
      "text/vnd.dvb.subtitle": { source: `iana`, extensions: [`sub`] },
      "text/vnd.esmertec.theme-descriptor": { source: `iana`, charset: `UTF-8` },
      "text/vnd.exchangeable": { source: `iana` },
      "text/vnd.familysearch.gedcom": { source: `iana`, extensions: [`ged`] },
      "text/vnd.ficlab.flt": { source: `iana` },
      "text/vnd.fly": { source: `iana`, extensions: [`fly`] },
      "text/vnd.fmi.flexstor": { source: `iana`, extensions: [`flx`] },
      "text/vnd.gml": { source: `iana` },
      "text/vnd.graphviz": { source: `iana`, extensions: [`gv`] },
      "text/vnd.hans": { source: `iana` },
      "text/vnd.hgl": { source: `iana` },
      "text/vnd.in3d.3dml": { source: `iana`, extensions: [`3dml`] },
      "text/vnd.in3d.spot": { source: `iana`, extensions: [`spot`] },
      "text/vnd.iptc.newsml": { source: `iana` },
      "text/vnd.iptc.nitf": { source: `iana` },
      "text/vnd.latex-z": { source: `iana` },
      "text/vnd.motorola.reflex": { source: `iana` },
      "text/vnd.ms-mediapackage": { source: `iana` },
      "text/vnd.net2phone.commcenter.command": { source: `iana` },
      "text/vnd.radisys.msml-basic-layout": { source: `iana` },
      "text/vnd.senx.warpscript": { source: `iana` },
      "text/vnd.si.uricatalogue": { source: `apache` },
      "text/vnd.sosi": { source: `iana` },
      "text/vnd.sun.j2me.app-descriptor": { source: `iana`, charset: `UTF-8`, extensions: [`jad`] },
      "text/vnd.trolltech.linguist": { source: `iana`, charset: `UTF-8` },
      "text/vnd.vcf": { source: `iana` },
      "text/vnd.wap.si": { source: `iana` },
      "text/vnd.wap.sl": { source: `iana` },
      "text/vnd.wap.wml": { source: `iana`, extensions: [`wml`] },
      "text/vnd.wap.wmlscript": { source: `iana`, extensions: [`wmls`] },
      "text/vnd.zoo.kcl": { source: `iana` },
      "text/vtt": { source: `iana`, charset: `UTF-8`, compressible: !0, extensions: [`vtt`] },
      "text/wgsl": { source: `iana`, extensions: [`wgsl`] },
      "text/x-asm": { source: `apache`, extensions: [`s`, `asm`] },
      "text/x-c": { source: `apache`, extensions: [`c`, `cc`, `cxx`, `cpp`, `h`, `hh`, `dic`] },
      "text/x-component": { source: `nginx`, extensions: [`htc`] },
      "text/x-fortran": { source: `apache`, extensions: [`f`, `for`, `f77`, `f90`] },
      "text/x-gwt-rpc": { compressible: !0 },
      "text/x-handlebars-template": { extensions: [`hbs`] },
      "text/x-java-source": { source: `apache`, extensions: [`java`] },
      "text/x-jquery-tmpl": { compressible: !0 },
      "text/x-lua": { extensions: [`lua`] },
      "text/x-markdown": { compressible: !0, extensions: [`mkd`] },
      "text/x-nfo": { source: `apache`, extensions: [`nfo`] },
      "text/x-opml": { source: `apache`, extensions: [`opml`] },
      "text/x-org": { compressible: !0, extensions: [`org`] },
      "text/x-pascal": { source: `apache`, extensions: [`p`, `pas`] },
      "text/x-processing": { compressible: !0, extensions: [`pde`] },
      "text/x-sass": { extensions: [`sass`] },
      "text/x-scss": { extensions: [`scss`] },
      "text/x-setext": { source: `apache`, extensions: [`etx`] },
      "text/x-sfv": { source: `apache`, extensions: [`sfv`] },
      "text/x-suse-ymp": { compressible: !0, extensions: [`ymp`] },
      "text/x-uuencode": { source: `apache`, extensions: [`uu`] },
      "text/x-vcalendar": { source: `apache`, extensions: [`vcs`] },
      "text/x-vcard": { source: `apache`, extensions: [`vcf`] },
      "text/xml": { source: `iana`, compressible: !0, extensions: [`xml`] },
      "text/xml-external-parsed-entity": { source: `iana` },
      "text/yaml": { compressible: !0, extensions: [`yaml`, `yml`] },
      "video/1d-interleaved-parityfec": { source: `iana` },
      "video/3gpp": { source: `iana`, extensions: [`3gp`, `3gpp`] },
      "video/3gpp-tt": { source: `iana` },
      "video/3gpp2": { source: `iana`, extensions: [`3g2`] },
      "video/av1": { source: `iana` },
      "video/bmpeg": { source: `iana` },
      "video/bt656": { source: `iana` },
      "video/celb": { source: `iana` },
      "video/dv": { source: `iana` },
      "video/encaprtp": { source: `iana` },
      "video/evc": { source: `iana` },
      "video/ffv1": { source: `iana` },
      "video/flexfec": { source: `iana` },
      "video/h261": { source: `iana`, extensions: [`h261`] },
      "video/h263": { source: `iana`, extensions: [`h263`] },
      "video/h263-1998": { source: `iana` },
      "video/h263-2000": { source: `iana` },
      "video/h264": { source: `iana`, extensions: [`h264`] },
      "video/h264-rcdo": { source: `iana` },
      "video/h264-svc": { source: `iana` },
      "video/h265": { source: `iana` },
      "video/h266": { source: `iana` },
      "video/iso.segment": { source: `iana`, extensions: [`m4s`] },
      "video/jpeg": { source: `iana`, extensions: [`jpgv`] },
      "video/jpeg2000": { source: `iana` },
      "video/jpm": { source: `apache`, extensions: [`jpm`, `jpgm`] },
      "video/jxsv": { source: `iana` },
      "video/lottie+json": { source: `iana`, compressible: !0 },
      "video/matroska": { source: `iana` },
      "video/matroska-3d": { source: `iana` },
      "video/mj2": { source: `iana`, extensions: [`mj2`, `mjp2`] },
      "video/mp1s": { source: `iana` },
      "video/mp2p": { source: `iana` },
      "video/mp2t": { source: `iana`, extensions: [`ts`, `m2t`, `m2ts`, `mts`] },
      "video/mp4": { source: `iana`, compressible: !1, extensions: [`mp4`, `mp4v`, `mpg4`] },
      "video/mp4v-es": { source: `iana` },
      "video/mpeg": {
        source: `iana`,
        compressible: !1,
        extensions: [`mpeg`, `mpg`, `mpe`, `m1v`, `m2v`],
      },
      "video/mpeg4-generic": { source: `iana` },
      "video/mpv": { source: `iana` },
      "video/nv": { source: `iana` },
      "video/ogg": { source: `iana`, compressible: !1, extensions: [`ogv`] },
      "video/parityfec": { source: `iana` },
      "video/pointer": { source: `iana` },
      "video/quicktime": { source: `iana`, compressible: !1, extensions: [`qt`, `mov`] },
      "video/raptorfec": { source: `iana` },
      "video/raw": { source: `iana` },
      "video/rtp-enc-aescm128": { source: `iana` },
      "video/rtploopback": { source: `iana` },
      "video/rtx": { source: `iana` },
      "video/scip": { source: `iana` },
      "video/smpte291": { source: `iana` },
      "video/smpte292m": { source: `iana` },
      "video/ulpfec": { source: `iana` },
      "video/vc1": { source: `iana` },
      "video/vc2": { source: `iana` },
      "video/vnd.cctv": { source: `iana` },
      "video/vnd.dece.hd": { source: `iana`, extensions: [`uvh`, `uvvh`] },
      "video/vnd.dece.mobile": { source: `iana`, extensions: [`uvm`, `uvvm`] },
      "video/vnd.dece.mp4": { source: `iana` },
      "video/vnd.dece.pd": { source: `iana`, extensions: [`uvp`, `uvvp`] },
      "video/vnd.dece.sd": { source: `iana`, extensions: [`uvs`, `uvvs`] },
      "video/vnd.dece.video": { source: `iana`, extensions: [`uvv`, `uvvv`] },
      "video/vnd.directv.mpeg": { source: `iana` },
      "video/vnd.directv.mpeg-tts": { source: `iana` },
      "video/vnd.dlna.mpeg-tts": { source: `iana` },
      "video/vnd.dvb.file": { source: `iana`, extensions: [`dvb`] },
      "video/vnd.fvt": { source: `iana`, extensions: [`fvt`] },
      "video/vnd.hns.video": { source: `iana` },
      "video/vnd.iptvforum.1dparityfec-1010": { source: `iana` },
      "video/vnd.iptvforum.1dparityfec-2005": { source: `iana` },
      "video/vnd.iptvforum.2dparityfec-1010": { source: `iana` },
      "video/vnd.iptvforum.2dparityfec-2005": { source: `iana` },
      "video/vnd.iptvforum.ttsavc": { source: `iana` },
      "video/vnd.iptvforum.ttsmpeg2": { source: `iana` },
      "video/vnd.motorola.video": { source: `iana` },
      "video/vnd.motorola.videop": { source: `iana` },
      "video/vnd.mpegurl": { source: `iana`, extensions: [`mxu`, `m4u`] },
      "video/vnd.ms-playready.media.pyv": { source: `iana`, extensions: [`pyv`] },
      "video/vnd.nokia.interleaved-multimedia": { source: `iana` },
      "video/vnd.nokia.mp4vr": { source: `iana` },
      "video/vnd.nokia.videovoip": { source: `iana` },
      "video/vnd.objectvideo": { source: `iana` },
      "video/vnd.planar": { source: `iana` },
      "video/vnd.radgamettools.bink": { source: `iana` },
      "video/vnd.radgamettools.smacker": { source: `apache` },
      "video/vnd.sealed.mpeg1": { source: `iana` },
      "video/vnd.sealed.mpeg4": { source: `iana` },
      "video/vnd.sealed.swf": { source: `iana` },
      "video/vnd.sealedmedia.softseal.mov": { source: `iana` },
      "video/vnd.uvvu.mp4": { source: `iana`, extensions: [`uvu`, `uvvu`] },
      "video/vnd.vivo": { source: `iana`, extensions: [`viv`] },
      "video/vnd.youtube.yt": { source: `iana` },
      "video/vp8": { source: `iana` },
      "video/vp9": { source: `iana` },
      "video/webm": { source: `apache`, compressible: !1, extensions: [`webm`] },
      "video/x-f4v": { source: `apache`, extensions: [`f4v`] },
      "video/x-fli": { source: `apache`, extensions: [`fli`] },
      "video/x-flv": { source: `apache`, compressible: !1, extensions: [`flv`] },
      "video/x-m4v": { source: `apache`, extensions: [`m4v`] },
      "video/x-matroska": {
        source: `apache`,
        compressible: !1,
        extensions: [`mkv`, `mk3d`, `mks`],
      },
      "video/x-mng": { source: `apache`, extensions: [`mng`] },
      "video/x-ms-asf": { source: `apache`, extensions: [`asf`, `asx`] },
      "video/x-ms-vob": { source: `apache`, extensions: [`vob`] },
      "video/x-ms-wm": { source: `apache`, extensions: [`wm`] },
      "video/x-ms-wmv": { source: `apache`, compressible: !1, extensions: [`wmv`] },
      "video/x-ms-wmx": { source: `apache`, extensions: [`wmx`] },
      "video/x-ms-wvx": { source: `apache`, extensions: [`wvx`] },
      "video/x-msvideo": { source: `apache`, extensions: [`avi`] },
      "video/x-sgi-movie": { source: `apache`, extensions: [`movie`] },
      "video/x-smv": { source: `apache`, extensions: [`smv`] },
      "x-conference/x-cooltalk": { source: `apache`, extensions: [`ice`] },
      "x-shader/x-fragment": { compressible: !0 },
      "x-shader/x-vertex": { compressible: !0 },
    };
  }),
  Q3 = e.n((t, n) => {
    n.exports = (Z3(), e.a(Y3).default);
  }),
  $3 = e.n((e, t) => {
    var n = Q3();
    t.exports = function () {
      var e = {};
      return (
        Object.keys(n).forEach(function (t) {
          var r = n[t];
          r.extensions &&
            r.extensions.length > 0 &&
            r.extensions.forEach(function (n) {
              e[n] = t;
            });
        }),
        e
      );
    };
  }),
  e6 = e.n((e, t) => {
    var n = Object.prototype.toString;
    t.exports = function (e) {
      var t;
      return (
        n.call(e) === `[object Object]` &&
        ((t = Object.getPrototypeOf(e)), t === null || t === Object.getPrototypeOf({}))
      );
    };
  }),
  t6 = e.n((e, t) => {
    var n = e6();
    t.exports = function (e, t) {
      if (!n(e)) throw TypeError(`Expected a plain object`);
      ((t ||= {}), typeof t == `function` && (t = { compare: t }));
      var r = t.deep,
        i = [],
        a = [],
        o = function (e) {
          var s = i.indexOf(e);
          if (s !== -1) return a[s];
          var c = {},
            l = Object.keys(e).sort(t.compare);
          (i.push(e), a.push(c));
          for (var u = 0; u < l.length; u++) {
            var d = l[u],
              f = e[d];
            c[d] = r && n(f) ? o(f) : f;
          }
          return c;
        };
      return o(e);
    };
  }),
  n6 = e.n((e, t) => {
    var n = t6();
    ((t.exports.desc = function (e) {
      return n(e, function (e, t) {
        return t.length - e.length;
      });
    }),
      (t.exports.asc = function (e) {
        return n(e, function (e, t) {
          return e.length - t.length;
        });
      }));
  }),
  r6 = e.n((e, t) => {
    var n = $3(),
      r = n6();
    ((t.exports = (e) => {
      let t = r.desc(n()),
        i = Object.keys(t).filter((t) => e.endsWith(t));
      return i.length === 0 ? [] : i.map((e) => ({ ext: e, mime: t[e] }));
    }),
      (t.exports.mime = (e) => {
        let t = r.desc(n()),
          i = Object.keys(t).filter((n) => t[n] === e);
        return i.length === 0 ? [] : i.map((e) => ({ ext: e, mime: t[e] }));
      }));
  }),
  i6 = e.o(r6(), 1),
  a6 = class extends Error {},
  o6 = (e, t) => {
    let n = i6.default.mime(t);
    return n.length === 1 ? `${e}.${n[0].ext}` : e;
  };
function s6(e, t, n = () => {}) {
  let r = new Set(),
    i = 0,
    o = 0,
    c = 0,
    l = () => r.size,
    u = () => i / c;
  t = { showBadge: !0, showProgressBar: !0, ...t };
  let d = (f, p, m) => {
    (r.add(p), (c += p.getTotalBytes()));
    let g = a.BrowserWindow.fromWebContents(m);
    if (!g) throw Error(`Failed to get window from web contents.`);
    if (t.directory && !s.default.isAbsolute(t.directory))
      throw Error("The `directory` option must be an absolute path");
    let _ = t.directory ?? a.app.getPath(`downloads`),
      v;
    if (t.filename) v = s.default.join(_, t.filename);
    else {
      let e = p.getFilename(),
        n = s.default.extname(e) ? e : o6(e, p.getMimeType());
      v = t.overwrite ? s.default.join(_, n) : U3(s.default.join(_, n));
    }
    let y = t.errorMessage ?? `The download of {filename} was interrupted`;
    (t.saveAs ? p.setSaveDialogOptions({ defaultPath: v, ...t.dialogOptions }) : p.setSavePath(v),
      p.on(`updated`, () => {
        i = o;
        for (let e of r) i += e.getReceivedBytes();
        if (
          (t.showBadge &&
            [`darwin`, `linux`].includes(h.default.platform) &&
            (a.app.badgeCount = l()),
          !g.isDestroyed() && t.showProgressBar && g.setProgressBar(u()),
          typeof t.onProgress == `function`)
        ) {
          let e = p.getReceivedBytes(),
            n = p.getTotalBytes();
          t.onProgress({ percent: n ? e / n : 0, transferredBytes: e, totalBytes: n });
        }
        typeof t.onTotalProgress == `function` &&
          t.onTotalProgress({ percent: u(), transferredBytes: i, totalBytes: c });
      }),
      p.on(`done`, (u, f) => {
        if (
          ((o += p.getTotalBytes()),
          r.delete(p),
          t.showBadge &&
            [`darwin`, `linux`].includes(h.default.platform) &&
            (a.app.badgeCount = l()),
          !g.isDestroyed() && !l() && (g.setProgressBar(-1), (i = 0), (o = 0), (c = 0)),
          t.unregisterWhenDone && e.removeListener(`will-download`, d),
          f === `cancelled`)
        )
          (typeof t.onCancel == `function` && t.onCancel(p), n(new a6()));
        else if (f === `interrupted`) {
          let e = J3(y, { filename: s.default.basename(v) });
          n(Error(e));
        } else if (f === `completed`) {
          let e = p.getSavePath();
          (h.default.platform === `darwin` && a.app.dock.downloadFinished(e),
            t.openFolderWhenDone && a.shell.showItemInFolder(e),
            typeof t.onCompleted == `function` &&
              t.onCompleted({
                fileName: p.getFilename(),
                filename: p.getFilename(),
                path: e,
                fileSize: p.getReceivedBytes(),
                mimeType: p.getMimeType(),
                url: p.getURL(),
              }),
            n(null, p));
        }
      }),
      typeof t.onStarted == `function` && t.onStarted(p));
  };
  e.on(`will-download`, d);
}
async function c6(e, t, n) {
  return new Promise((r, i) => {
    ((n = { ...n, unregisterWhenDone: !0 }),
      s6(e.webContents.session, n, (e, t) => {
        e ? i(e) : r(t);
      }),
      e.webContents.downloadURL(t));
  });
}
if (typeof a.default == `string`) throw TypeError(`Not running in an Electron environment!`);
var { env: l6 } = process,
  u6 = `ELECTRON_IS_DEV` in l6,
  d6 = Number.parseInt(l6.ELECTRON_IS_DEV, 10) === 1,
  f6 = u6 ? d6 : !a.default.app.isPackaged,
  p6 = (e) => e.webContents ?? (e.id && e),
  m6 =
    (e) =>
    (t = {}) => (t.transform && !t.click && (e.transform = t.transform), e),
  h6 = (e) => {
    let t;
    return e
      .filter((e) => e !== void 0 && e !== !1 && e.visible !== !1 && e.visible !== ``)
      .filter((e, n, r) => {
        let i =
          e.type === `separator` && (!t || n === r.length - 1 || r[n + 1].type === `separator`);
        return ((t = i ? t : e), !i);
      });
  },
  g6 = (e, t) => {
    let n = (n, r) => {
      if (typeof t.shouldShowMenu == `function` && t.shouldShowMenu(n, r) === !1) return;
      let { editFlags: i } = r,
        o = r.selectionText.length > 0,
        s = !!r.linkURL,
        c = (e) => i[`can${e}`] && o,
        l = {
          separator: () => ({ type: `separator` }),
          learnSpelling: m6({
            id: `learnSpelling`,
            label: `&Learn Spelling`,
            visible: !!(r.isEditable && o && r.misspelledWord),
            click() {
              p6(e).session.addWordToSpellCheckerDictionary(r.misspelledWord);
            },
          }),
          lookUpSelection: m6({
            id: `lookUpSelection`,
            label: `Look Up “{selection}”`,
            visible: h.default.platform === `darwin` && o && !s,
            click() {
              h.default.platform === `darwin` && p6(e).showDefinitionForSelection();
            },
          }),
          searchWithGoogle: m6({
            id: `searchWithGoogle`,
            label: `&Search with Google`,
            visible: o,
            click() {
              let e = new URL(`https://www.google.com/search`);
              (e.searchParams.set(`q`, r.selectionText),
                a.default.shell.openExternal(e.toString()));
            },
          }),
          cut: m6({
            id: `cut`,
            label: `Cu&t`,
            enabled: c(`Cut`),
            visible: r.isEditable,
            click(t) {
              let n = p6(e);
              !t.transform && n
                ? n.cut()
                : ((r.selectionText = t.transform ? t.transform(r.selectionText) : r.selectionText),
                  a.default.clipboard.writeText(r.selectionText));
            },
          }),
          copy: m6({
            id: `copy`,
            label: `&Copy`,
            enabled: c(`Copy`),
            visible: r.isEditable || o,
            click(t) {
              let n = p6(e);
              !t.transform && n
                ? n.copy()
                : ((r.selectionText = t.transform ? t.transform(r.selectionText) : r.selectionText),
                  a.default.clipboard.writeText(r.selectionText));
            },
          }),
          paste: m6({
            id: `paste`,
            label: `&Paste`,
            enabled: i.canPaste,
            visible: r.isEditable,
            click(t) {
              let n = p6(e);
              if (t.transform) {
                let e = a.default.clipboard.readText(r.selectionText);
                ((e = t.transform ? t.transform(e) : e), n.insertText(e));
              } else n.paste();
            },
          }),
          selectAll: m6({
            id: `selectAll`,
            label: `Select &All`,
            click() {
              p6(e).selectAll();
            },
          }),
          saveImage: m6({
            id: `saveImage`,
            label: `Save I&mage`,
            visible: r.mediaType === `image`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL), c6(e, r.srcURL));
            },
          }),
          saveImageAs: m6({
            id: `saveImageAs`,
            label: `Sa&ve Image As…`,
            visible: r.mediaType === `image`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                c6(e, r.srcURL, { saveAs: !0 }));
            },
          }),
          saveVideo: m6({
            id: `saveVideo`,
            label: `Save Vide&o`,
            visible: r.mediaType === `video`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL), c6(e, r.srcURL));
            },
          }),
          saveVideoAs: m6({
            id: `saveVideoAs`,
            label: `Save Video& As…`,
            visible: r.mediaType === `video`,
            click(t) {
              ((r.srcURL = t.transform ? t.transform(r.srcURL) : r.srcURL),
                c6(e, r.srcURL, { saveAs: !0 }));
            },
          }),
          copyLink: m6({
            id: `copyLink`,
            label: `Copy Lin&k`,
            visible: r.linkURL.length > 0 && r.mediaType === `none`,
            click(e) {
              ((r.linkURL = e.transform ? e.transform(r.linkURL) : r.linkURL),
                a.default.clipboard.write({ bookmark: r.linkText, text: r.linkURL }));
            },
          }),
          saveLinkAs: m6({
            id: `saveLinkAs`,
            label: `Save Link As…`,
            visible: r.linkURL.length > 0 && r.mediaType === `none`,
            click(t) {
              ((r.linkURL = t.transform ? t.transform(r.linkURL) : r.linkURL),
                c6(e, r.linkURL, { saveAs: !0 }));
            },
          }),
          copyImage: m6({
            id: `copyImage`,
            label: `Cop&y Image`,
            visible: r.mediaType === `image`,
            click() {
              p6(e).copyImageAt(r.x, r.y);
            },
          }),
          copyImageAddress: m6({
            id: `copyImageAddress`,
            label: `C&opy Image Address`,
            visible: r.mediaType === `image`,
            click(e) {
              ((r.srcURL = e.transform ? e.transform(r.srcURL) : r.srcURL),
                a.default.clipboard.write({ bookmark: r.srcURL, text: r.srcURL }));
            },
          }),
          copyVideoAddress: m6({
            id: `copyVideoAddress`,
            label: `Copy Video Ad&dress`,
            visible: r.mediaType === `video`,
            click(e) {
              ((r.srcURL = e.transform ? e.transform(r.srcURL) : r.srcURL),
                a.default.clipboard.write({ bookmark: r.srcURL, text: r.srcURL }));
            },
          }),
          inspect: () => ({
            id: `inspect`,
            label: `I&nspect Element`,
            click() {
              (p6(e).inspectElement(r.x, r.y),
                p6(e).isDevToolsOpened() && p6(e).devToolsWebContents.focus());
            },
          }),
          services: () => ({
            id: `services`,
            label: `Services`,
            role: `services`,
            visible: h.default.platform === `darwin` && (r.isEditable || o),
          }),
        },
        u = typeof t.showInspectElement == `boolean` ? t.showInspectElement : f6,
        d = t.showSelectAll || (t.showSelectAll !== !1 && h.default.platform !== `darwin`);
      function f(t) {
        return {
          id: `dictionarySuggestions`,
          label: t,
          visible: !!(r.isEditable && o && r.misspelledWord),
          click(t) {
            p6(e).replaceMisspelling(t.label);
          },
        };
      }
      let p = [];
      o && r.misspelledWord && r.dictionarySuggestions.length > 0
        ? (p = r.dictionarySuggestions.map((e) => f(e)))
        : p.push({
            id: `dictionarySuggestions`,
            label: `No Guesses Found`,
            visible: !!(o && r.misspelledWord),
            enabled: !1,
          });
      let m = [
        p.length > 0 && l.separator(),
        ...p,
        l.separator(),
        t.showLearnSpelling !== !1 && l.learnSpelling(),
        l.separator(),
        t.showLookUpSelection !== !1 && l.lookUpSelection(),
        l.separator(),
        t.showSearchWithGoogle !== !1 && l.searchWithGoogle(),
        l.separator(),
        l.cut(),
        l.copy(),
        l.paste(),
        d && l.selectAll(),
        l.separator(),
        t.showSaveImage && l.saveImage(),
        t.showSaveImageAs && l.saveImageAs(),
        t.showCopyImage !== !1 && l.copyImage(),
        t.showCopyImageAddress && l.copyImageAddress(),
        t.showSaveVideo && l.saveVideo(),
        t.showSaveVideoAs && l.saveVideoAs(),
        t.showCopyVideoAddress && l.copyVideoAddress(),
        l.separator(),
        t.showCopyLink !== !1 && l.copyLink(),
        t.showSaveLinkAs && l.saveLinkAs(),
        l.separator(),
        u && l.inspect(),
        t.showServices && l.services(),
        l.separator(),
      ];
      if ((t.menu && (m = t.menu(l, r, e, p, n)), t.prepend)) {
        let i = t.prepend(l, r, e, n);
        Array.isArray(i) && m.unshift(...i);
      }
      if (t.append) {
        let i = t.append(l, r, e, n);
        Array.isArray(i) && m.push(...i);
      }
      m = h6(m);
      for (let e of m)
        if (
          (t.labels && t.labels[e.id] && (e.label = t.labels[e.id]),
          typeof e.label == `string` && e.label.includes(`{selection}`))
        ) {
          let t = typeof r.selectionText == `string` ? r.selectionText.trim() : ``;
          e.label = e.label.replace(`{selection}`, R3(t, 25).replaceAll(`&`, `&&`));
        }
      if (m.length > 0) {
        let n = a.default.Menu.buildFromTemplate(m);
        (typeof t.onShow == `function` && n.on(`menu-will-show`, t.onShow),
          typeof t.onClose == `function` && n.on(`menu-will-close`, t.onClose),
          n.popup(e));
      }
    };
    return (
      p6(e).on(`context-menu`, n),
      () => {
        e?.isDestroyed?.() || p6(e).removeListener(`context-menu`, n);
      }
    );
  };
function _6(e = {}) {
  if (h.default.type === `renderer`)
    throw Error(`Cannot use electron-context-menu in the renderer process!`);
  let t = !1,
    n = [],
    r = (r) => {
      if (t) return;
      let i = g6(r, e),
        a = () => {
          i();
        };
      (p6(r).once(`destroyed`, a), n.push(a));
    },
    i = () => {
      for (let e of n) e();
      ((n.length = 0), (t = !0));
    };
  if (e.window) {
    let t = e.window;
    if (p6(t) === void 0) {
      let e = () => {
        r(t);
      };
      return (
        (t.addEventListener ?? t.addListener)(`dom-ready`, e, { once: !0 }),
        n.push(() => {
          t.removeEventListener(`dom-ready`, e, { once: !0 });
        }),
        i
      );
    }
    return (r(t), i);
  }
  for (let e of a.default.BrowserWindow.getAllWindows()) r(e);
  let o = (e, t) => {
    r(t);
  };
  return (
    a.default.app.on(`browser-window-created`, o),
    n.push(() => {
      a.default.app.removeListener(`browser-window-created`, o);
    }),
    i
  );
}
var v6 = class {
    windowsByOwnerWebContentsId = new Map();
    originConversationIdsByOwnerWebContentsId = new Map();
    constructor(e) {
      this.options = e;
    }
    forgetWebContents(e) {
      this.originConversationIdsByOwnerWebContentsId.delete(e);
      let t = this.windowsByOwnerWebContentsId.get(e);
      (this.windowsByOwnerWebContentsId.delete(e), t != null && (t.isDestroyed() || t.close()));
    }
    getOwnerWebContentsId(e) {
      for (let [t, n] of this.windowsByOwnerWebContentsId)
        if (!n.isDestroyed() && n.webContents.id === e) return t;
      return null;
    }
    setOriginConversation(e, t) {
      (this.originConversationIdsByOwnerWebContentsId.set(e.id, t),
        this.sendOriginConversation(e.id));
    }
    async show(e) {
      let n = this.windowsByOwnerWebContentsId.get(e.id);
      if (n != null && !n.isDestroyed()) {
        (this.sendOriginConversation(e.id), this.options.showWindow(n, { stealFocus: !0 }));
        return;
      }
      let r = await this.options.createWindow({
        title: `Debug`,
        width: 920,
        height: 840,
        appearance: `secondary`,
        initialRoute: t.Pi,
        lockTitle: !0,
      });
      (this.windowsByOwnerWebContentsId.set(e.id, r), r.setMenuBarVisibility(!1), r.center());
      let i = () => {
          this.windowsByOwnerWebContentsId.get(e.id) === r &&
            this.windowsByOwnerWebContentsId.delete(e.id);
        },
        a = () => {
          (i(),
            this.originConversationIdsByOwnerWebContentsId.delete(e.id),
            r.isDestroyed() || r.close());
        };
      (r.on(`closed`, () => {
        (i(), e.removeListener(`destroyed`, a));
      }),
        e.once(`destroyed`, a),
        this.sendOriginConversation(e.id),
        this.options.showWindow(r, { stealFocus: !0 }));
    }
    sendOriginConversation(e) {
      let t = this.windowsByOwnerWebContentsId.get(e);
      t == null ||
        t.isDestroyed() ||
        this.options.sendMessageToWindow(t, {
          type: `debug-window-origin-conversation-changed`,
          conversationId: this.originConversationIdsByOwnerWebContentsId.get(e) ?? null,
        });
    }
  },
  y6 = `mcp_app_sandbox`,
  b6 = t.Gr(`mcp-app-sandbox`);
function x6(e, t, n = {}) {
  b6().info(`${y6}.${e}`, {
    safe: n.safe ?? {},
    sensitive: {
      origin: t.origin,
      ownerWebContentsId: t.ownerWebContents.id,
      sandboxId: t.sandboxId,
      ...n.sensitive,
    },
  });
}
function S6(e) {
  let t = O6(e);
  return t == null ? !0 : ![`about:`, `blob:`, `data:`, `http:`, `https:`].includes(t.protocol);
}
function C6(e, { allowDevtools: t }) {
  let n = O6(e);
  return (
    n != null &&
    ([`about:`, `blob:`, `data:`, `https:`, `wss:`].includes(n.protocol) ||
      (t && n.protocol === `devtools:`))
  );
}
function w6(e) {
  return (
    T6(e.frame) ||
    D6(e.url) ||
    D6(E6(e.requestHeaders, `Origin`)) ||
    D6(E6(e.requestHeaders, `Referer`))
  );
}
function T6(e) {
  return e != null && (D6(e.origin) || D6(e.url));
}
function E6(e, t) {
  let n = Object.keys(e).find((e) => e.toLowerCase() === t.toLowerCase());
  return n == null ? void 0 : e[n];
}
function D6(e) {
  let n = O6(e);
  return n != null && t.ua(n.hostname);
}
function O6(e) {
  if (e == null || e.length === 0) return null;
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
function k6(e) {
  return O6(e)?.protocol ?? `invalid`;
}
function A6(e, t, n) {
  let r = O6(t.sourceUrl),
    i = (e) => {
      let t = O6(e);
      return (
        t != null &&
        r != null &&
        t.origin === r.origin &&
        t.pathname === r.pathname &&
        t.search === r.search
      );
    },
    a = (e) => (e.isMainFrame === !0 ? !i(e.url) : S6(e.url)),
    o = (e, n) => {
      x6(e, t, {
        safe: {
          isMainFrame: n.isMainFrame,
          reason: n.isMainFrame === !0 ? `main_frame_url_mismatch` : `privileged_subframe_url`,
          urlProtocol: k6(n.url),
        },
        sensitive: { guestWebContentsId: n.guestWebContentsId, url: mP(n.url) },
      });
    },
    s = (t, n) => {
      let r = t.url ?? n;
      i(r) ||
        (o(`navigation_blocked`, { guestWebContentsId: e.id, isMainFrame: !0, url: r }),
        t.preventDefault());
    },
    c = (t) => {
      a({ isMainFrame: t.isMainFrame, url: t.url }) &&
        (o(`navigation_blocked`, {
          guestWebContentsId: e.id,
          isMainFrame: t.isMainFrame,
          url: t.url,
        }),
        t.preventDefault());
    },
    l = (t, n) => {
      let r = t.url ?? n;
      a({ isMainFrame: t.isMainFrame, url: r }) &&
        (o(`redirect_blocked`, { guestWebContentsId: e.id, isMainFrame: t.isMainFrame, url: r }),
        t.preventDefault());
    },
    u = (n, r, i, a, o, s, c) => {
      r !== -3 &&
        x6(`guest_load_failed`, t, {
          safe: { errorCode: r, isMainFrame: o, urlProtocol: k6(a) },
          sensitive: {
            errorDescription: i,
            frameProcessId: s,
            frameRoutingId: c,
            guestWebContentsId: e.id,
            validatedURL: mP(a),
          },
        });
    },
    d = (n, r, i) => {
      b6().error(`${y6}.preload_failed`, {
        safe: {},
        sensitive: { error: i, guestWebContentsId: e.id, preloadPath: r, sandboxId: t.sandboxId },
      });
    },
    f = (n, r) => {
      r.reason !== `clean-exit` &&
        x6(`render_process_gone`, t, {
          safe: { reason: r.reason },
          sensitive: { exitCode: r.exitCode, guestWebContentsId: e.id },
        });
    },
    p = () => {
      (n(),
        e.removeListener(`will-navigate`, s),
        e.removeListener(`will-frame-navigate`, c),
        e.removeListener(`will-redirect`, l),
        e.removeListener(`did-fail-load`, u),
        e.removeListener(`preload-error`, d),
        e.removeListener(`render-process-gone`, f),
        e.removeListener(`destroyed`, p));
    };
  (e.setWindowOpenHandler(
    ({ url: n }) => (
      x6(`popup_blocked`, t, {
        safe: { urlProtocol: k6(n) },
        sensitive: { guestWebContentsId: e.id, url: mP(n) },
      }),
      { action: `deny` }
    ),
  ),
    e.on(`will-navigate`, s),
    e.on(`will-frame-navigate`, c),
    e.on(`will-redirect`, l),
    e.on(`did-fail-load`, u),
    e.on(`preload-error`, d),
    e.on(`render-process-gone`, f),
    e.on(`destroyed`, p));
}
var j6 = /\b(?:Chrome|Chromium)\/(\d+)\./;
function M6(e, t) {
  return t.filter(Boolean).reduce((e, t) => e.replace(RegExp(`\\s${N6(t)}/[^\\s]+`, `g`), ``), e);
}
function N6(e) {
  return e.replace(/[.*+?^${}()|[\]\\]/g, `\\$&`);
}
function P6(e) {
  let t = j6.exec(e)?.[1];
  return t == null ? null : `"Chromium";v="${t}", "Google Chrome";v="${t}", "Not=A?Brand";v="24"`;
}
function F6() {
  return process.platform === `darwin`
    ? `"macOS"`
    : process.platform === `win32`
      ? `"Windows"`
      : `"Linux"`;
}
function I6(e, t) {
  let n = { ...e },
    r = M6(t.userAgent, t.excludedUserAgentProducts),
    i = P6(r);
  return (
    L6(n, `User-Agent`, r),
    L6(n, `Accept-Language`, t.acceptLanguage),
    i != null && L6(n, `sec-ch-ua`, i),
    L6(n, `sec-ch-ua-mobile`, `?0`),
    L6(n, `sec-ch-ua-platform`, F6()),
    n
  );
}
function L6(e, t, n) {
  let r = Object.keys(e).find((e) => e.toLowerCase() === t.toLowerCase());
  (r != null && r !== t && delete e[r], (e[t] = n));
}
var R6 = !1,
  z6 = new WeakSet();
function B6() {
  if (R6) return;
  let e = `session` in a ? a.session?.defaultSession : void 0;
  e?.webRequest != null &&
    ((R6 = !0),
    e.webRequest.onBeforeSendHeaders((t, n) => {
      let r = t.requestHeaders;
      n({
        requestHeaders: w6({ frame: t.frame, requestHeaders: r, url: t.url })
          ? I6(r, {
              acceptLanguage: H6(),
              excludedUserAgentProducts: [`Electron`, a.app.getName()],
              userAgent: e.getUserAgent(),
            })
          : r,
      });
    }));
}
function V6(e, { allowDevtools: n }) {
  let r = a.session.fromPartition(t.sa(e));
  return (
    zq(r),
    z6.has(r)
      ? r
      : (z6.add(r),
        r.setPermissionRequestHandler((t, n, r) => {
          (b6().info(`${y6}.permission_blocked`, {
            safe: { permission: n },
            sensitive: { guestWebContentsId: t.id, sandboxId: e },
          }),
            r(!1));
        }),
        r.setPermissionCheckHandler(() => !1),
        r.on(`will-download`, (t, n) => {
          (b6().info(`${y6}.download_blocked`, {
            safe: { urlProtocol: k6(n.getURL()) },
            sensitive: { sandboxId: e, url: mP(n.getURL()) },
          }),
            t.preventDefault(),
            n.cancel());
        }),
        r.webRequest.onBeforeRequest((t, r) => {
          let i = C6(t.url, { allowDevtools: n });
          (i ||
            b6().info(`${y6}.request_blocked`, {
              safe: { resourceType: t.resourceType, urlProtocol: k6(t.url) },
              sensitive: { guestWebContentsId: t.webContentsId, sandboxId: e, url: mP(t.url) },
            }),
            r({ cancel: !i }));
        }),
        r.webRequest.onBeforeSendHeaders((e, t) => {
          t({
            requestHeaders: I6(e.requestHeaders, {
              acceptLanguage: H6(),
              excludedUserAgentProducts: [`Electron`, a.app.getName()],
              userAgent: r.getUserAgent(),
            }),
          });
        }),
        r)
  );
}
function H6() {
  let e = a.app.getPreferredSystemLanguages();
  return (e.length > 0 ? e : [a.app.getLocale()])
    .map((e, t) => (t === 0 ? e : `${e};q=${Math.max(1 - t * 0.1, 0.1).toFixed(1)}`))
    .join(`,`);
}
var U6 = (0, s.join)(__dirname, `sandbox-preload.js`),
  W6 = 3e4,
  G6 = !1,
  K6 = new Map(),
  q6 = new Map();
function J6() {
  (B6(), X6());
}
function Y6(e, t, n) {
  if (n)
    for (let [n, r] of q6) {
      if (r.ownerWebContents.id !== e.id || r.sandboxId !== t) continue;
      let i = a.webContents.fromId(n);
      if (i == null || i.isDestroyed()) {
        q6.delete(n);
        continue;
      }
      i.openDevTools({ mode: `detach` });
      return;
    }
}
function X6() {
  G6 ||
    !(`ipcMain` in a) ||
    a.ipcMain?.on == null ||
    ((G6 = !0),
    a.ipcMain.on(Il, (e, n) => {
      let r = q6.get(e.sender.id);
      if (r == null) {
        b6().warning(`${y6}.init_rejected`, {
          safe: { reason: `unknown_guest` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      let i = Z6(n);
      if (i == null) {
        x6(`init_rejected`, r, { safe: { reason: `invalid_message` } });
        return;
      }
      if (r.origin !== i.origin) {
        x6(`init_rejected`, r, {
          safe: { reason: `origin_mismatch` },
          sensitive: { guestWebContentsId: e.sender.id, receivedOrigin: i.origin },
        });
        return;
      }
      if (r.initId !== i.initId) {
        x6(`init_rejected`, r, {
          safe: { reason: `init_id_mismatch` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (!t.la(i.portNames)) {
        x6(`init_rejected`, r, {
          safe: { receivedPortNameCount: i.portNames.length, reason: `invalid_port_names` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (e.ports.length !== i.portNames.length + 1) {
        x6(`init_rejected`, r, {
          safe: {
            expectedPortCount: i.portNames.length + 1,
            receivedPortCount: e.ports.length,
            reason: `port_count_mismatch`,
          },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      if (r.ownerWebContents.isDestroyed()) {
        x6(`init_rejected`, r, {
          safe: { reason: `owner_destroyed` },
          sensitive: { guestWebContentsId: e.sender.id },
        });
        return;
      }
      r.ownerWebContents.postMessage(
        Ll,
        {
          initId: i.initId,
          origin: i.origin,
          portNames: i.portNames,
          sandboxId: r.sandboxId,
          type: `init`,
        },
        e.ports,
      );
    }));
}
function Z6(e) {
  if (typeof e != `object` || !e) return null;
  let { initId: t, origin: n, portNames: r, type: i } = e;
  return i !== `init` ||
    typeof t != `string` ||
    t.length === 0 ||
    typeof n != `string` ||
    !Array.isArray(r)
    ? null
    : { initId: t, origin: n, portNames: r };
}
function Q6(e, n, { allowNonMcpAppSandboxWebviews: r = !1 } = {}) {
  let i = (i, a, o) => {
      let s = (t, n = {}) => {
          (b6().warning(`${y6}.attach_rejected`, {
            safe: t,
            sensitive: { ownerWebContentsId: e.id, ...n },
          }),
            i.preventDefault());
        },
        c = t.aa(o.partition);
      if (c == null) {
        r || s({ reason: `non_mcp_app_sandbox_webview` });
        return;
      }
      let l = o.src,
        u = t.ca(l, { requireSkybridge: !0 }),
        d = t.oa(l);
      if (!t.da(c)) {
        s({ reason: `invalid_sandbox_id` });
        return;
      }
      if (u == null) {
        s({ reason: `invalid_source_url`, sourceProtocol: k6(l) }, { sourceUrl: mP(l) });
        return;
      }
      if (d == null) {
        s({ reason: `invalid_init_id` });
        return;
      }
      let f = V6(c, { allowDevtools: n });
      ($6(f, { initId: d, origin: u, ownerWebContents: e, sandboxId: c, sourceUrl: l }),
        (a.session = f),
        (a.preload = U6),
        (a.nodeIntegration = !1),
        (a.nodeIntegrationInSubFrames = !1),
        (a.contextIsolation = !0),
        (a.disableDialogs = !0),
        (a.sandbox = !0),
        (a.webSecurity = !0),
        (a.plugins = !1),
        (a.allowRunningInsecureContent = !1),
        (a.devTools = n),
        (a.webviewTag = !1));
    },
    a = (n, r) => {
      let i = e8(r.session, e, t.oa(r.getURL()));
      if (i == null) {
        b6().warning(`${y6}.attach_unmatched`, {
          safe: { urlProtocol: k6(r.getURL()) },
          sensitive: { guestWebContentsId: r.id, url: mP(r.getURL()) },
        });
        return;
      }
      (q6.set(r.id, i),
        A6(r, i, () => {
          q6.delete(r.id);
        }));
    };
  (e.on(`will-attach-webview`, i),
    e.on(`did-attach-webview`, a),
    e.once(`destroyed`, () => {
      (e.removeListener(`will-attach-webview`, i),
        e.removeListener(`did-attach-webview`, a),
        n8(e));
    }));
}
function $6(e, t) {
  let n = setTimeout(() => {
      x6(`attach_expired`, t);
      let n = K6.get(e)?.find((e) => e.state === t);
      n != null && t8(e, n);
    }, W6),
    r = K6.get(e) ?? [];
  (r.push({ state: t, timeout: n }), K6.set(e, r));
}
function e8(e, t, n) {
  let r = K6.get(e),
    i = -1;
  r != null &&
    (i =
      n == null
        ? r.findIndex(({ state: e }) => e.ownerWebContents.id === t.id)
        : r.findIndex(({ state: e }) => e.ownerWebContents.id === t.id && e.initId === n));
  let a = r != null && i >= 0 ? (r.splice(i, 1)[0] ?? null) : null;
  return (
    a != null && clearTimeout(a.timeout),
    (r == null || r.length === 0) && K6.delete(e),
    a?.state ?? null
  );
}
function t8(e, t) {
  let n = K6.get(e);
  if (n == null) return;
  let r = n.filter((e) => e !== t);
  (clearTimeout(t.timeout), r.length === 0 ? K6.delete(e) : r.length !== n.length && K6.set(e, r));
}
function n8(e) {
  for (let [t, n] of K6) for (let r of n) r.state.ownerWebContents.id === e.id && t8(t, r);
}
function r8(e, t) {
  if (!gb(e)) return !1;
  try {
    return new URL(e).origin === t;
  } catch {
    return !1;
  }
}
function i8(e, t, n) {
  return n && a8(e, t, n) ? !0 : o8(e, t);
}
function a8(e, t, n) {
  let r = s8(e),
    i = s8(t);
  return r == null || i == null || !gb(e) || !gb(t) || r.origin !== n || i.origin !== n
    ? !1
    : r.pathname === i.pathname;
}
function o8(e, t) {
  let n = s8(e),
    r = s8(t);
  return n == null || r == null || n.protocol !== `file:` || r.protocol !== `file:`
    ? !1
    : n.pathname === r.pathname;
}
function s8(e) {
  try {
    return new URL(e);
  } catch {
    return null;
  }
}
var c8 = t.Gr(`window-manager`),
  l8 = `http://localhost:5175/`,
  u8 =
    process.platform === `darwin`
      ? `Reveal in Finder`
      : process.platform === `win32`
        ? `Open in Explorer`
        : `Open in File Manager`,
  d8 = `<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Codex</title>
  </head>
  <body>
    <main>
      <h1>Something went wrong…</h1>
    </main>
  </body>
</html>`,
  f8 = `#00000000`,
  p8 = `#000000`,
  m8 = `#f9f9f9`,
  h8 = 3840,
  g8 = 2160,
  _8 = 320,
  v8 = 140,
  y8 = 400,
  b8 = 400,
  x8 = 46,
  S8 = 14,
  C8 = 16,
  w8 = 36,
  T8 = `#1f1f1f`,
  E8 = `#ffffff`;
function D8(e) {
  return { x: C8, y: Math.round((x8 * e - S8) / 2) };
}
function O8(e = 1) {
  return {
    color: f8,
    symbolColor: a.nativeTheme.shouldUseDarkColors ? E8 : T8,
    height: Math.round(w8 * e),
  };
}
var k8 = class {
  browserCommentPopupWindowCreatedHandlers = new Set();
  readyWebContentsIds = new Set();
  pendingMessages = new Map();
  primaryWindows = new Set();
  primaryWindowLastActiveAtMs = new WeakMap();
  lastActivePrimaryWindow = null;
  windowAppearances = new Map();
  registeredWindowIds = new Set();
  registeredWebContentsIds = new Set();
  webContentsHostIds = new Map();
  opaqueWindowSurfaceModes = new Map();
  windowZooms = new Map();
  appShellShortcutStates = new Map();
  appShellCloseTabAccelerators = t.Ut({
    commandId: `closeTab`,
    isMacOS: process.platform === `darwin`,
  });
  debugWindowManager;
  isAppQuitting = !1;
  rendererRecoveryAttempts = new Set();
  constructor(e) {
    ((this.options = e),
      J6(),
      (this.debugWindowManager = this.options.allowDebugMenu
        ? new v6({
            createWindow: (e) => this.createWindow(e),
            sendMessageToWindow: (e, t) => this.sendMessageToWindow(e, t),
            showWindow: (e, t) => this.showWindow(e, t),
          })
        : null));
  }
  setOnMainProcessInspectorStateChanged(e) {
    this.options.onMainProcessInspectorStateChanged = e;
  }
  setQueueCodexDeepLinkUrl(e) {
    this.options.queueCodexDeepLinkUrl = e;
  }
  refreshAppShellCommandAccelerators() {
    this.appShellCloseTabAccelerators = t.Ut({
      commandId: `closeTab`,
      isMacOS: process.platform === `darwin`,
    });
  }
  queueCodexDeepLinkUrl(e, t) {
    return this.options.queueCodexDeepLinkUrl?.(e, t) === !0;
  }
  markWebContentsReady(e) {
    (this.readyWebContentsIds.add(e.id), this.flushPendingMessages(e));
  }
  forgetWebContents(e) {
    (this.readyWebContentsIds.delete(e),
      this.pendingMessages.delete(e),
      this.appShellShortcutStates.delete(e),
      this.debugWindowManager?.forgetWebContents(e));
  }
  isWebContentsReady(e) {
    return this.readyWebContentsIds.has(e);
  }
  setAppShellShortcutState(e, t) {
    this.appShellShortcutStates.set(e, t);
  }
  setWindowZoom(e, t) {
    let n = a.BrowserWindow.fromWebContents(e);
    n == null ||
      this.windowAppearances.get(n.id) !== `primary` ||
      (process.platform === `darwin`
        ? n.setWindowButtonPosition(D8(t))
        : (process.platform === `win32` || process.platform === `linux`) &&
          (this.windowZooms.set(n.id, t), n.setTitleBarOverlay(O8(t))));
  }
  getAppShellShortcutState(e) {
    return this.appShellShortcutStates.get(e) ?? null;
  }
  markAppQuitting() {
    this.isAppQuitting = !0;
    let e = this.getPrimaryWindow();
    e && this.persistPrimaryWindowBounds(e);
  }
  showPrimaryWindow(e) {
    let t = this.getPrimaryWindow();
    return this.showWindow(t, e);
  }
  showLastActivePrimaryWindow() {
    let e = this.lastActivePrimaryWindow;
    return e != null && !e.isDestroyed()
      ? this.showWindow(e)
      : ((this.lastActivePrimaryWindow = null), this.showWindow(this.getPrimaryWindow()));
  }
  isHotkeyWindowThread(e) {
    return this.windowAppearances.get(e.id) === `hotkeyWindowThread`;
  }
  showWindow(e, t) {
    if (!e) return !1;
    e.isMinimized() && e.restore();
    let n = t?.stealFocus === !0 && process.platform === `win32`;
    return (
      n && e.setAlwaysOnTop(!0),
      e.show(),
      t?.stealFocus === !0 && process.platform === `darwin` && a.app.focus({ steal: !0 }),
      e.focus(),
      n && e.setAlwaysOnTop(!1),
      !0
    );
  }
  sendMessageToWindow(e, t) {
    this.sendMessageToWebContents(e.webContents, t);
  }
  sendMessageToAllRegisteredWindows(e) {
    this.sendMessageToAllWindows(e);
  }
  sendMessageToAllWindows(e) {
    for (let t of a.BrowserWindow.getAllWindows())
      t.isDestroyed() || (this.registeredWindowIds.has(t.id) && this.sendMessageToWindow(t, e));
  }
  sendMessageToWebContents(e, t) {
    if (e.isDestroyed()) return;
    if (this.isWebContentsReady(e.id)) {
      e.send(R, t);
      return;
    }
    let n = this.pendingMessages.get(e.id) ?? [];
    (n.push(t), this.pendingMessages.set(e.id, n));
  }
  sendMessageToWebContentsId(e, t) {
    for (let n of a.BrowserWindow.getAllWindows())
      if (!(n.isDestroyed() || n.webContents.id !== e)) {
        this.sendMessageToWebContents(n.webContents, t);
        return;
      }
  }
  getPrimaryWindow() {
    let e = this.lastActivePrimaryWindow;
    if (e != null && !e.isDestroyed() && this.primaryWindows.has(e)) return e;
    let t = this.getPrimaryWindows(),
      n = t.at(-1) ?? null,
      r = -1;
    for (let e of t) {
      let t = this.primaryWindowLastActiveAtMs.get(e) ?? -1;
      t > r && ((n = e), (r = t));
    }
    return n
      ? ((this.lastActivePrimaryWindow = n), n)
      : ((this.lastActivePrimaryWindow = null), null);
  }
  getPrimaryWindows() {
    return this.getTrackedPrimaryWindows();
  }
  wasPrimaryWindowFocusedWithin(e, t) {
    if (!this.primaryWindows.has(e) || e.isDestroyed()) return !1;
    if (e.isFocused()) return !0;
    let n = this.primaryWindowLastActiveAtMs.get(e);
    return n != null && Date.now() - n <= t;
  }
  refreshWindowBackdrops() {
    for (let e of a.BrowserWindow.getAllWindows()) {
      if (e.isDestroyed() || !this.registeredWindowIds.has(e.id)) continue;
      let t = this.windowAppearances.get(e.id);
      t != null && this.applyWindowBackdrop(e, t, !0);
    }
  }
  hasRegisteredWebContents(e) {
    return this.registeredWebContentsIds.has(e.id);
  }
  registerAuxiliaryWindow(e, t) {
    (this.registerWindow(e, z, !1, t), this.options.onWindowRegistered?.(e));
  }
  getHostIdForWebContents(e) {
    return this.webContentsHostIds.get(e.id) ?? null;
  }
  isTrustedIpcSender(e, t) {
    return e.id != null && !this.registeredWebContentsIds.has(e.id)
      ? !1
      : M8({ sender: e, senderFrame: t, moduleDir: this.options.moduleDir });
  }
  isMainProcessInspectorEnabled() {
    return T.url() != null;
  }
  async toggleMainProcessInspector() {
    throw Error(`Feature unavailable.`);
  }
  openMcpAppSandboxDevtools(e, t) {
    Y6(e, t, this.options.allowMcpAppSandboxDevtools === !0);
  }
  async createWindow(e = {}) {
    let {
        title: n,
        width: i = 1280,
        height: o = 820,
        appearance: c = `primary`,
        show: u = !0,
        initialRoute: d,
        hostId: f = z,
        parent: p,
        focusable: m,
        lockTitle: h = !1,
      } = e,
      g = Date.now(),
      _ = c === `primary` ? z : f,
      v = c === `primary`,
      y = v ? this.restorePrimaryWindowBounds() : null,
      b = y?.width ?? i,
      x = y?.height ?? o,
      S = y?.x,
      C = y?.y,
      w = y?.isMaximized === !0,
      T = U8(c, this.getPrimaryMinimumSize()),
      E = this.shouldAlwaysUseOpaqueWindowSurface(c),
      D = q8({ appearance: c, opaqueWindowSurfaceEnabled: E, platform: process.platform }),
      O = H8(c),
      k = {
        preload: this.options.preloadPath,
        contextIsolation: !0,
        nodeIntegration: !1,
        spellcheck: !0,
        devTools: this.options.allowDevtools,
        ...(O ? { webviewTag: !0 } : {}),
      },
      { backgroundColor: A, backgroundMaterial: j } = W8({
        platform: process.platform,
        appearance: c,
        opaqueWindowSurfaceEnabled: E,
        prefersDarkColors: a.nativeTheme.shouldUseDarkColors,
      }),
      M = new a.BrowserWindow({
        width: b,
        height: x,
        ...(S === void 0 || C === void 0 ? {} : { x: S, y: C }),
        title: n ?? a.app.getName(),
        backgroundColor: A,
        show: u,
        ...(p == null ? {} : { parent: p }),
        ...(m == null ? {} : { focusable: m }),
        ...(process.platform === `win32` || process.platform === `linux`
          ? { autoHideMenuBar: !0 }
          : {}),
        ...(j == null ? {} : { backgroundMaterial: j }),
        ...D,
        ...(T == null ? {} : { minWidth: T.width, minHeight: T.height }),
        webPreferences: k,
      });
    this.applyWindowBackdrop(M, c, !0);
    let N = M.webContents,
      P = N.id;
    (O &&
      Q6(N, this.options.allowMcpAppSandboxDevtools === !0, {
        allowNonMcpAppSandboxWebviews: c === `primary`,
      }),
      M.webContents.once(`did-finish-load`, () => {
        (M.webContents.setZoomLevel(0),
          c8().info(`window main frame finished load`, {
            safe: {
              hostId: _,
              windowId: M.id,
              webContentsId: M.webContents.id,
              appearance: c,
              startupElapsedMs: Date.now() - g,
            },
          }));
      }),
      M.once(`ready-to-show`, () => {
        c8().info(`window ready-to-show`, {
          safe: {
            hostId: _,
            windowId: M.id,
            webContentsId: M.webContents.id,
            appearance: c,
            startupElapsedMs: Date.now() - g,
          },
        });
      }));
    let F = this.installApplicationMenuTitleBarOverlaySync(M, c),
      I = () => {
        this.applyWindowBackdrop(M, c, !1);
      };
    (process.platform === `darwin` && (M.on(`move`, I), M.on(`resize`, I)),
      (process.platform === `win32` || process.platform === `linux`) && M.removeMenu(),
      M.on(`closed`, () => {
        (F?.(),
          process.platform === `darwin` &&
            (M.removeListener(`move`, I), M.removeListener(`resize`, I)));
      }),
      (h ||
        c === `primary` ||
        c === `hud` ||
        c === `hotkeyWindowHome` ||
        c === `hotkeyWindowThread`) &&
        M.on(`page-title-updated`, (e) => {
          (e.preventDefault(), M.isDestroyed() || M.setTitle(M.getTitle()));
        }),
      this.installWebContentsDiagnostics(M),
      this.registerWindow(M, _, v, c),
      this.options.onWindowRegistered?.(M),
      v &&
        M.on(`close`, (e) => {
          this.persistPrimaryWindowBounds(M);
          let t = this.getPrimaryWindows().some((e) => e !== M);
          if (
            process.platform === `win32` &&
            !this.isAppQuitting &&
            this.options.canHideLastWindowToTray?.() === !0 &&
            !t
          ) {
            (e.preventDefault(), M.hide());
            return;
          }
          if (process.platform === `darwin` && !this.isAppQuitting && !t) {
            if (M.isFullScreen()) {
              (e.preventDefault(),
                M.once(`leave-full-screen`, () => {
                  M.isDestroyed() || M.hide();
                }),
                M.setFullScreen(!1));
              return;
            }
            (e.preventDefault(), M.hide());
          }
        }));
    let ee = () => {
      this.sendMessageToWindow(M, {
        type: `window-fullscreen-changed`,
        isFullScreen: M.isFullScreen(),
      });
    };
    (M.on(`enter-full-screen`, ee), M.on(`leave-full-screen`, ee), ee());
    let te = () => {
        (this.applyWindowBackdrop(M, c, !1),
          this.sendMessageToWindow(M, {
            type: `electron-window-focus-changed`,
            isFocused: M.isFocused(),
          }));
      },
      ne = eF({
        allowDebugMenu: this.options.allowDebugMenu,
        getCloseTabAccelerators: () => this.appShellCloseTabAccelerators,
        getState: () => this.appShellShortcutStates.get(P) ?? null,
        sendAction: (e) => {
          this.sendMessageToWebContents(N, e);
        },
      });
    (M.on(`focus`, te),
      M.on(`blur`, te),
      M.on(`show`, te),
      M.on(`hide`, te),
      N.on(`before-input-event`, ne),
      te(),
      w &&
        M.once(`ready-to-show`, () => {
          M.isDestroyed() || M.maximize();
        }));
    let re = N8();
    if (
      (M.webContents.setWindowOpenHandler(({ url: e, frameName: n }) =>
        t.ns(n)?.windowId === t.ts.BROWSER_COMMENT_POPUP &&
        this.browserCommentPopupWindowCreatedHandlers.size > 0 &&
        e === `about:blank` &&
        c === `primary` &&
        F8(M.webContents.getURL(), this.options.moduleDir)
          ? {
              action: `allow`,
              outlivesOpener: !1,
              overrideBrowserWindowOptions: this.createBrowserCommentPopupWindowOptions(M),
            }
          : re && r8(e, re)
            ? { action: `allow` }
            : (gb(e) &&
                a.shell.openExternal(e).catch((e) => {
                  this.options.errorReporter.reportNonFatal(e instanceof Error ? e : t.Oo(e), {
                    kind: `open-external-window`,
                  });
                }),
              { action: `deny` }),
      ),
      M.webContents.on(`did-create-window`, (e, n) => {
        let r = t.ns(n.frameName);
        if (
          !(
            r?.windowId !== t.ts.BROWSER_COMMENT_POPUP ||
            n.url !== `about:blank` ||
            c !== `primary`
          )
        ) {
          (e.setMenuBarVisibility(!1),
            e.setVisibleOnAllWorkspaces(!1),
            process.platform === `win32` && e.removeMenu(),
            e.hide(),
            this.installNativeContextMenu(e));
          for (let t of this.browserCommentPopupWindowCreatedHandlers)
            t({
              browserTabId: r.browserTabId,
              conversationId: r.conversationId,
              hostId: this.getHostIdForWebContents(M.webContents) ?? _,
              owner: M.webContents,
              sessionId: r.sessionId,
              window: e,
            });
        }
      }),
      M.on(`closed`, () => {
        (this.appShellShortcutStates.delete(P),
          !N.isDestroyed() && N.removeListener(`before-input-event`, ne));
      }),
      M.webContents.on(`will-navigate`, (e, n) => {
        i8(n, M.webContents.getURL(), re) ||
          (e.preventDefault(),
          gb(n) &&
            a.shell.openExternal(n).catch((e) => {
              this.options.errorReporter.reportNonFatal(e instanceof Error ? e : t.Oo(e), {
                kind: `open-external-navigation`,
              });
            }));
      }),
      M.webContents.on(`will-frame-navigate`, (e) => {
        !e.isMainFrame && L8(e.url) && e.preventDefault();
      }),
      M.webContents.on(`will-redirect`, (e, t) => {
        let n = e.url ?? t;
        !e.isMainFrame && L8(n) && e.preventDefault();
      }),
      this.installNativeContextMenu(M),
      !a.app.isPackaged)
    ) {
      let e = new URL(j8());
      return (
        d && e.searchParams.set(`initialRoute`, d),
        O &&
          this.options.allowMcpAppSandboxDevtools === !0 &&
          e.searchParams.set(`mcpAppSandboxDevtools`, `1`),
        M.loadURL(e.toString()),
        M
      );
    }
    let ie = (0, s.join)(r.I(this.options.moduleDir), `index.html`);
    if ((0, l.existsSync)(ie))
      try {
        return (
          await M.loadURL(
            r.F(d, { mcpAppSandboxDevtools: O && this.options.allowMcpAppSandboxDevtools === !0 }),
          ),
          M
        );
      } catch (e) {
        c8().warning(`Failed to load bundle at`, {
          safe: {},
          sensitive: { indexPath: ie, error: e },
        });
      }
    let ae = `data:text/html;charset=utf-8,${encodeURIComponent(d8)}`;
    return (await M.loadURL(ae), M);
  }
  async createPrimaryWindow(e = {}) {
    return this.createWindow({ ...e, appearance: `primary` });
  }
  addBrowserCommentPopupWindowCreatedHandler(e) {
    return (
      this.browserCommentPopupWindowCreatedHandlers.add(e),
      () => {
        this.browserCommentPopupWindowCreatedHandlers.delete(e);
      }
    );
  }
  createBrowserCommentPopupWindowOptions(e) {
    let n = `browserCommentPopup`,
      r = this.shouldAlwaysUseOpaqueWindowSurface(n),
      { backgroundColor: i, backgroundMaterial: o } = W8({
        platform: process.platform,
        appearance: n,
        opaqueWindowSurfaceEnabled: r,
        prefersDarkColors: a.nativeTheme.shouldUseDarkColors,
      });
    return {
      title: ``,
      width: 294,
      height: t.is,
      parent: e,
      show: !1,
      backgroundColor: i,
      ...(o == null ? {} : { backgroundMaterial: o }),
      ...q8({ appearance: n, opaqueWindowSurfaceEnabled: r, platform: process.platform }),
      webPreferences: {
        contextIsolation: !0,
        nodeIntegration: !1,
        spellcheck: !0,
        devTools: this.options.allowDevtools,
      },
    };
  }
  registerWindow(e, t, n, r) {
    let i = e.id,
      a = e.webContents.id;
    (this.windowAppearances.set(i, r),
      this.registeredWindowIds.add(i),
      this.registeredWebContentsIds.add(a),
      this.webContentsHostIds.set(a, t),
      n && this.trackPrimaryWindow(e),
      e.on(`closed`, () => {
        (this.forgetWebContents(a),
          this.windowAppearances.delete(i),
          this.registeredWindowIds.delete(i),
          this.registeredWebContentsIds.delete(a),
          this.webContentsHostIds.delete(a),
          this.opaqueWindowSurfaceModes.delete(i),
          this.windowZooms.delete(i),
          n &&
            (this.primaryWindows.delete(e),
            this.lastActivePrimaryWindow === e && (this.lastActivePrimaryWindow = null)));
      }));
  }
  trackPrimaryWindow(e) {
    (this.primaryWindows.add(e),
      e.isFocused()
        ? ((this.lastActivePrimaryWindow = e), this.primaryWindowLastActiveAtMs.set(e, Date.now()))
        : (this.lastActivePrimaryWindow ??= e),
      e.on(`focus`, () => {
        ((this.lastActivePrimaryWindow = e), this.primaryWindowLastActiveAtMs.set(e, Date.now()));
      }),
      e.on(`blur`, () => {
        this.primaryWindowLastActiveAtMs.set(e, Date.now());
      }));
  }
  getTrackedPrimaryWindows() {
    let e = [];
    for (let t of this.primaryWindows) {
      if (t.isDestroyed()) {
        this.primaryWindows.delete(t);
        continue;
      }
      e.push(t);
    }
    return this.primaryWindows.size === 0
      ? ((this.lastActivePrimaryWindow = null), [])
      : (this.lastActivePrimaryWindow?.isDestroyed() && (this.lastActivePrimaryWindow = null), e);
  }
  restorePrimaryWindowBounds() {
    let e = this.options.globalState.get(`electron-main-window-bounds`) ?? null;
    if (!e) return null;
    let t = this.clampPrimaryWindowBounds(e);
    return this.isWithinDisplayWorkArea(t) ? t : null;
  }
  getPrimaryMinimumSize() {
    return { width: 480, height: 600 };
  }
  syncPrimaryMinimumSize() {
    let e = this.getPrimaryWindows();
    if (e.length === 0) return;
    let t = this.getPrimaryMinimumSize();
    for (let n of e) {
      if ((n.setMinimumSize(t.width, t.height), n.isMaximized() || n.isFullScreen())) continue;
      let e = n.getNormalBounds(),
        r = Math.max(e.width, t.width),
        i = Math.max(e.height, t.height);
      (r === e.width && i === e.height) || n.setBounds({ ...e, width: r, height: i });
    }
    let n = this.getPrimaryWindow();
    n && this.persistPrimaryWindowBounds(n);
  }
  persistPrimaryWindowBounds(e) {
    if (e.isDestroyed()) return;
    let t = e.getNormalBounds(),
      n = this.getPrimaryMinimumSize(),
      r = {
        x: t.x,
        y: t.y,
        width: Math.max(t.width, n.width),
        height: Math.max(t.height, n.height),
        isMaximized: e.isMaximized(),
      };
    this.options.globalState.set(`electron-main-window-bounds`, r);
  }
  clampPrimaryWindowBounds(e) {
    let t = this.getPrimaryMinimumSize();
    return { ...e, width: Math.max(e.width, t.width), height: Math.max(e.height, t.height) };
  }
  flushPendingMessages(e) {
    let t = this.pendingMessages.get(e.id);
    if (!t || t.length === 0) {
      this.pendingMessages.delete(e.id);
      return;
    }
    if ((this.pendingMessages.delete(e.id), !e.isDestroyed())) for (let n of t) e.send(R, n);
  }
  isWithinDisplayWorkArea(e) {
    let t = a.screen.getAllDisplays();
    if (t.length === 0) return !0;
    let n = { x: e.x, y: e.y, width: e.width, height: e.height };
    return t.some((e) => {
      let t = e.workArea;
      return (
        t.x < n.x + n.width && t.x + t.width > n.x && t.y < n.y + n.height && t.y + t.height > n.y
      );
    });
  }
  installApplicationMenuTitleBarOverlaySync(e, t) {
    if ((process.platform !== `win32` && process.platform !== `linux`) || t !== `primary`) return;
    let n = () => {
      e.isDestroyed() || e.setTitleBarOverlay(O8(this.windowZooms.get(e.id)));
    };
    return (
      a.nativeTheme.on(`updated`, n),
      n(),
      () => {
        a.nativeTheme.off(`updated`, n);
      }
    );
  }
  isOpaqueWindowsEnabled() {
    return (
      (A8(this.options.settingsStore.getEffective(t.yi.theme.key) ?? `system`) === `light`
        ? this.options.settingsStore.getEffective(t.yi.lightChromeTheme.key)
        : this.options.settingsStore.getEffective(t.yi.darkChromeTheme.key)
      )?.opaqueWindows === !0
    );
  }
  shouldUseOpaqueWindowSurface(e, t, n) {
    let r = a.screen.getDisplayMatching(t);
    return (
      this.shouldAlwaysUseOpaqueWindowSurface(e) ||
      V8({ appearance: e, isFocused: n, platform: process.platform }) ||
      z8({ appearance: e, platform: process.platform, scaleFactor: r.scaleFactor, windowBounds: t })
    );
  }
  shouldAlwaysUseOpaqueWindowSurface(e) {
    return (
      B8({
        appearance: e,
        opaqueWindowsEnabled: this.isOpaqueWindowsEnabled(),
        platform: process.platform,
      }) ||
      (!vM() && !R8(e))
    );
  }
  applyWindowBackdrop(e, t, n) {
    let r = e.getBounds(),
      i = this.shouldUseOpaqueWindowSurface(t, r, e.isFocused());
    if (!n && this.opaqueWindowSurfaceModes.get(e.id) === i) return;
    let { backgroundColor: o, backgroundMaterial: s } = W8({
      platform: process.platform,
      appearance: t,
      opaqueWindowSurfaceEnabled: i,
      prefersDarkColors: a.nativeTheme.shouldUseDarkColors,
    });
    (e.setBackgroundColor(o),
      s != null && e.setBackgroundMaterial(s),
      process.platform === `darwin` && e.setVibrancy(G8(t, i)),
      this.opaqueWindowSurfaceModes.set(e.id, i),
      this.sendMessageToWindow(e, {
        type: `electron-window-opaque-surface-changed`,
        opaqueWindowSurfaceEnabled: i,
      }));
  }
  installNativeContextMenu(e) {
    let t = _6({
      window: e,
      prepend: (e, t) => {
        if (t.mediaType !== `image`) return [];
        let n = r.L(t.srcURL);
        return n == null
          ? []
          : [
              {
                label: u8,
                click: () => {
                  a.shell.showItemInFolder(n);
                },
              },
            ];
      },
      showInspectElement: this.options.allowInspectElement,
    });
    e.on(`closed`, () => {
      t();
    });
  }
  installWebContentsDiagnostics(e) {
    let t = e.webContents,
      n = e.id,
      r = t.id,
      i = this.options.errorReporter;
    (t.on(`render-process-gone`, (t, a) => {
      a.reason !== `clean-exit` &&
        (i.reportFatal(Error(`Renderer process gone (${a.reason})`), {
          tags: { errorType: `renderer-process-gone`, reason: a.reason },
          extra: { windowId: n, webContentsId: r, exitCode: a.exitCode },
        }),
        this.maybeRecoverFromRendererCrash(e, a.reason));
    }),
      t.on(`did-finish-load`, () => {
        this.rendererRecoveryAttempts.delete(r);
      }),
      t.on(`unresponsive`, () => {
        i.reportFatal(Error(`Renderer unresponsive`), {
          tags: { errorType: `renderer-unresponsive` },
          extra: { windowId: n, webContentsId: r, url: mP(t.getURL()) },
        });
      }),
      t.on(`did-fail-load`, (e, t, a, o, s, c, l) => {
        s &&
          t !== -3 &&
          i.reportFatal(Error(`Renderer did-fail-load`), {
            tags: { errorType: `renderer-did-fail-load` },
            extra: {
              windowId: n,
              webContentsId: r,
              errorCode: t,
              errorDescription: a,
              validatedURL: mP(o),
              frameProcessId: c,
              frameRoutingId: l,
            },
          });
      }));
  }
  maybeRecoverFromRendererCrash(e, t) {
    if (this.isAppQuitting || t === `clean-exit` || e.isDestroyed()) return;
    let n = e.webContents;
    n.isDestroyed() ||
      this.rendererRecoveryAttempts.has(n.id) ||
      (this.rendererRecoveryAttempts.add(n.id),
      setTimeout(() => {
        if (!e.isDestroyed() && !e.webContents.isDestroyed())
          try {
            e.webContents.reload();
          } catch (e) {
            c8().warning(`Failed to reload crashed window`, { safe: {}, sensitive: { error: e } });
          }
      }, 500));
  }
};
function A8(e) {
  return e === `system` ? (a.nativeTheme.shouldUseDarkColors ? `dark` : `light`) : e;
}
function j8() {
  return process.env.ELECTRON_RENDERER_URL || l8;
}
function M8({ sender: e, senderFrame: t, moduleDir: n }) {
  return t != null && t !== e.mainFrame ? !1 : F8((t ?? e.mainFrame).url, n);
}
function N8() {
  let e = j8();
  return gb(e) ? new URL(e).origin : null;
}
function P8(e) {
  return (0, s.join)(e, `..`, `..`, `webview`, `index.html`);
}
function F8(e, t) {
  if (e === `about:blank` || I8(e)) return !0;
  let n = N8();
  if (n && r8(e, n)) return !0;
  let r = (0, x.pathToFileURL)(P8(t)).toString();
  return !!e.startsWith(r);
}
function I8(e) {
  try {
    let t = new URL(e);
    return t.protocol === `app:` ? t.host === `-` || t.host === `` : !1;
  } catch {
    return !1;
  }
}
function L8(e) {
  try {
    return new URL(e).protocol === `app:`;
  } catch {
    return !1;
  }
}
function R8(e) {
  return (
    e === `avatarOverlay` ||
    e === `browserCommentPopup` ||
    e === `globalDictation` ||
    e === `hotkeyWindowHome` ||
    e === `hotkeyWindowThread`
  );
}
function z8({ appearance: e, platform: t, scaleFactor: n, windowBounds: r }) {
  if (t !== `darwin` || R8(e)) return !1;
  let i = r.width * n,
    a = r.height * n;
  return Math.max(i, a) >= h8 && Math.min(i, a) >= g8;
}
function B8({ appearance: e, opaqueWindowsEnabled: t, platform: n }) {
  return t && !R8(e) && (n === `darwin` || n === `win32`);
}
function V8({ appearance: e, isFocused: t, platform: n }) {
  return !t && !R8(e) && (n === `darwin` || n === `win32`);
}
function H8(e) {
  return e === `primary` || e === `hotkeyWindowThread` || e === `hud`;
}
function U8(e, t) {
  switch (e) {
    case `avatarOverlay`:
    case `browserCommentPopup`:
    case `globalDictation`:
      return null;
    case `primary`:
      return t;
    case `hotkeyWindowHome`:
      return { width: _8, height: v8 };
    case `hotkeyWindowThread`:
      return { width: y8, height: b8 };
    case `hud`:
    case `secondary`:
      return { width: 400, height: 400 };
  }
}
function W8({ platform: e, appearance: t, opaqueWindowSurfaceEnabled: n, prefersDarkColors: r }) {
  return n
    ? { backgroundColor: r ? p8 : m8, backgroundMaterial: e === `win32` ? `none` : null }
    : e === `win32` && !R8(t)
      ? { backgroundColor: f8, backgroundMaterial: `mica` }
      : { backgroundColor: f8, backgroundMaterial: null };
}
function G8(e, t) {
  return t || R8(e) ? null : `menu`;
}
function K8({
  alwaysOnTop: e,
  hasShadow: t = !0,
  platform: n,
  resizable: r,
  thickFrame: i,
  transparent: a = !0,
}) {
  return {
    frame: !1,
    transparent: a,
    hasShadow: t,
    resizable: r,
    minimizable: !1,
    maximizable: !1,
    fullscreenable: !1,
    skipTaskbar: !0,
    ...(e ? { alwaysOnTop: !0 } : {}),
    ...(n === `win32`
      ? { accentColor: !1, roundedCorners: !1, ...(i == null ? {} : { thickFrame: i }) }
      : {}),
    ...(n === `darwin` ? { type: `panel` } : {}),
  };
}
function q8({ appearance: e, opaqueWindowSurfaceEnabled: t, platform: n, windowZoom: r = 1 }) {
  switch (e) {
    case `browserCommentPopup`:
      return K8({ hasShadow: !1, platform: n, resizable: !1, thickFrame: !1, transparent: !0 });
    case `globalDictation`:
      return {
        ...K8({
          alwaysOnTop: !0,
          hasShadow: !1,
          platform: n,
          resizable: !1,
          thickFrame: !1,
          transparent: !0,
        }),
        ...(n === `darwin` ? { acceptFirstMouse: !0 } : {}),
      };
    case `avatarOverlay`:
      return {
        ...K8({ alwaysOnTop: !0, platform: n, resizable: !1, thickFrame: !1 }),
        ...(n === `darwin` ? { enableLargerThanScreen: !0 } : {}),
        hasShadow: !1,
      };
    case `hotkeyWindowHome`:
      return K8({ platform: n, resizable: !1, thickFrame: !1 });
    case `hotkeyWindowThread`:
      return K8({ platform: n, resizable: !0 });
    case `primary`:
      return n === `darwin`
        ? t
          ? { titleBarStyle: `hiddenInset`, trafficLightPosition: D8(r) }
          : { vibrancy: `menu`, titleBarStyle: `hiddenInset`, trafficLightPosition: D8(r) }
        : n === `win32` || n === `linux`
          ? { titleBarStyle: `hidden`, titleBarOverlay: O8(r) }
          : { titleBarStyle: `default` };
    case `secondary`:
      return n === `darwin`
        ? t
          ? { titleBarStyle: `default` }
          : { vibrancy: `menu`, titleBarStyle: `default` }
        : { titleBarStyle: `default` };
    case `hud`:
      return n === `darwin`
        ? t
          ? {
              titleBarStyle: `hiddenInset`,
              minimizable: !1,
              maximizable: !1,
              fullscreenable: !1,
              alwaysOnTop: !0,
              trafficLightPosition: { x: 10, y: 10 },
            }
          : {
              vibrancy: `menu`,
              titleBarStyle: `hiddenInset`,
              minimizable: !1,
              maximizable: !1,
              fullscreenable: !1,
              alwaysOnTop: !0,
              trafficLightPosition: { x: 10, y: 10 },
            }
        : {
            titleBarStyle: `default`,
            minimizable: !1,
            maximizable: !1,
            fullscreenable: !1,
            alwaysOnTop: !0,
          };
  }
}
var J8 = 2e3;
function Y8({
  appBrand: e,
  buildFlavor: r,
  allowDevtools: i,
  allowInspectElement: o,
  allowDebugMenu: c,
  errorReporter: u,
  globalState: d,
  settingsStore: f,
  desktopRoot: p,
  preloadPath: m,
  repoRoot: h,
  isMacOS: g,
  isWindows: _,
  isDevMode: v,
  canHideLastWindowToTray: y,
  disposables: b,
}) {
  let x = (0, s.join)(h, `electron`, `src`, `icons`),
    S = (e) => {
      if (!a.app.isPackaged) return null;
      let t = (0, s.join)(process.resourcesPath, e);
      return (0, l.existsSync)(t) ? t : null;
    },
    C = (e) => {
      let t = (0, s.join)(x, e);
      return (0, l.existsSync)(t) ? t : null;
    },
    w = (e) => S(e) ?? C(e),
    T = () => {
      switch (r) {
        case n.i.Dev:
        case n.i.Nightly:
        case n.i.InternalAlpha:
        case n.i.PublicBeta:
        case n.i.Prod:
          return f.get(t.yi.dockIconPreference.key) ?? `app-default`;
        case n.i.Agent:
          return `app-default`;
      }
    },
    E = () => {
      switch (r) {
        case n.i.Dev:
          return C(`icon-dev-outline.png`);
        case n.i.Agent:
          return w(`icon-agent.png`);
        case n.i.Nightly:
        case n.i.InternalAlpha:
        case n.i.PublicBeta:
        case n.i.Prod:
          return w(`${$1(r, `darwin`, e)}.png`);
      }
    },
    D = e0(r),
    O = (e, t) => {
      if (a.app.isPackaged && e === `app-default`) {
        let e = a.app.dock;
        e != null && Reflect.apply(e.setIcon.bind(e), e, [null]);
        return;
      }
      let n = (t == null ? null : w(t)) ?? E(),
        r = n == null ? a.nativeImage.createEmpty() : a.nativeImage.createFromPath(n);
      r.isEmpty() || a.app.dock?.setIcon(r);
    },
    k = () => {
      let e = T(),
        t = D[e];
      (O(e, t), ok({ preference: e, resourceName: t }));
    };
  g && k();
  let A = null,
    j = new k8({
      desktopRoot: p,
      iconDirectoryName: Yl,
      globalState: d,
      settingsStore: f,
      moduleDir: __dirname,
      preloadPath: m,
      repoRoot: h,
      allowDevtools: i,
      allowMcpAppSandboxDevtools: v,
      allowDebugMenu: c,
      allowInspectElement: o,
      errorReporter: u,
      canHideLastWindowToTray: y,
      onWindowRegistered: (e) => {
        A?.registerWindow(e);
      },
    }),
    M = new R2(j, d, d0()),
    N = new c3({ windowManager: j, globalState: d, isDevMode: v }),
    P = new R4({
      canForceGlobalDictationLock: v,
      forceGlobalDictationLock: v && d.get(t.Fo.GLOBAL_DICTATION_FORCE_LOCK_DEBUG_ENABLED) === !0,
      getVisibleNativePetWebContents: () => M.getVisibleWebContents(),
      windowManager: j,
      globalState: d,
    }),
    F = r0({ windowManager: j, globalState: d, enabled: !0 });
  b.add(() => {
    F.dispose();
  });
  let I = (e) => {
      if (!_) return;
      let t = setTimeout(() => {
        e.isDestroyed() || e.isVisible() || (e.show(), e.focus());
      }, J8);
      t.unref();
      let n = () => {
        clearTimeout(t);
      };
      (e.once(`show`, n), e.once(`closed`, n));
    },
    ee = async ({ show: e, initialRoute: t }) => {
      if (A == null) return null;
      let n = await j.createPrimaryWindow({
        title: a.app.getName(),
        show: e,
        initialRoute: t === `/` ? void 0 : t,
      });
      return (e || I(n), n);
    };
  return {
    windowManager: j,
    avatarOverlayManager: M,
    hotkeyWindowLifecycleManager: N,
    globalDictationLifecycleManager: P,
    appshotHotkeyController: F,
    updateDockIcon: k,
    setWindowContext: (e) => {
      A = e;
      for (let t of a.BrowserWindow.getAllWindows())
        t.isDestroyed() || !j.hasRegisteredWebContents(t.webContents) || e.registerWindow(t);
    },
    ensureWindow: async () => {
      let e = j.getPrimaryWindow();
      return e
        ? A == null
          ? null
          : (e.isMinimized() && e.restore(), e.show(), e.focus(), e.isDestroyed() ? null : e)
        : ee({ show: !1 });
    },
    createFreshWindow: async (e = `/`) => ee({ show: !0, initialRoute: e }),
    showLastActivePrimaryWindow: () => j.showLastActivePrimaryWindow(),
    markAppQuitting: () => {
      j.markAppQuitting();
    },
    isTrustedIpcSender: j.isTrustedIpcSender.bind(j),
    sendMessageToWindow: j.sendMessageToWindow.bind(j),
    sendMessageToAllRegisteredWindows: j.sendMessageToAllRegisteredWindows.bind(j),
    sendMessageToAllWindows: j.sendMessageToAllWindows.bind(j),
    getPrimaryWindow: j.getPrimaryWindow.bind(j),
    getContextForWebContents: (e) => (j.hasRegisteredWebContents(e) ? A : null),
  };
}
var X8 = 3e4,
  Z8 = class {
    totalInvocations = 0;
    rollingWindowCounter = new t.Ai({ windowMs: X8 });
    recordInvocation(e = Date.now()) {
      ((this.totalInvocations += 1), this.rollingWindowCounter.record(1, e));
    }
    getSnapshot(e = Date.now()) {
      let t = this.rollingWindowCounter.getSnapshot(e);
      return { totalInvocations: this.totalInvocations, invocationsLast30s: t.count };
    }
  };
function Q8(e) {
  return typeof e == `object` && !!e;
}
function $8(e) {
  return (
    e === `worktree-cleanup-inputs` ||
    e === `computer-use-invalidate-service-pid` ||
    e === `computer-use-service-pid` ||
    e === `codex-home` ||
    e === `platform-family` ||
    e === `platform-os` ||
    e === `process-start` ||
    e === `process-write` ||
    e === `process-resize` ||
    e === `process-terminate` ||
    e === `fs-read-file` ||
    e === `fs-write-file` ||
    e === `fs-create-directory` ||
    e === `fs-get-metadata` ||
    e === `fs-read-directory` ||
    e === `fs-remove` ||
    e === `fs-copy` ||
    e === `fs-watch` ||
    e === `fs-unwatch` ||
    e === `worker-exit`
  );
}
function e5(e) {
  return Q8(e)
    ? e.type === `worker-main-rpc-request` &&
        typeof e.workerId == `string` &&
        typeof e.requestId == `string` &&
        $8(e.method)
    : !1;
}
var t5 = class {
    worker = null;
    latestAuthUser = null;
    listeners = new Set();
    exitListeners = new Set();
    mainRpcHandler = null;
    exitedWorkers = new WeakSet();
    disposedWorkers = new WeakSet();
    constructor(e) {
      this.id = e;
    }
    dispose() {
      let e = this.worker;
      e &&
        ((this.worker = null),
        this.disposedWorkers.add(e),
        this.handleWorkerExit(e),
        e.terminate());
    }
    addListener(e) {
      return (
        this.listeners.add(e),
        () => {
          this.listeners.delete(e);
        }
      );
    }
    addExitListener(e) {
      return (
        this.exitListeners.add(e),
        () => {
          this.exitListeners.delete(e);
        }
      );
    }
    setMainRpcHandler(e) {
      this.mainRpcHandler = e;
    }
    postMessage(e) {
      this.ensureWorker().postMessage(e);
    }
    start() {
      this.ensureWorker();
    }
    setSentryUser(e, t, n, r) {
      this.latestAuthUser = { authMethod: e, userId: t, email: n, accountId: r };
      let i = this.worker;
      i &&
        i.postMessage({
          type: `worker-sentry-user-update`,
          authMethod: e,
          userId: t,
          email: n,
          accountId: r,
        });
    }
    emitAppEvent(e) {
      let t = this.worker;
      t && t.postMessage({ type: `worker-app-event`, event: e });
    }
    ensureWorker() {
      if (this.worker) return this.worker;
      let e = t.Wr(`worker-manager`),
        i = (0, s.join)(__dirname, `worker.js`),
        o = t.b(n.i.resolve()),
        c = a.app.isPackaged ? a.app.getAppPath() : process.cwd(),
        l = {
          workerId: this.id,
          sentryInitOptions: r.j,
          maxLogLevel: o,
          sentryRewriteFramesRoot: c,
          spawnInsideWsl: t.zr(),
        },
        u = new S.Worker(i, { name: this.id, workerData: l });
      return (
        u.on(`message`, (e) => {
          if (e5(e)) {
            this.handleMainRpcRequest(u, e);
            return;
          }
          this.listeners.forEach((t) => {
            t(e);
          });
        }),
        u.on(`error`, (t) => {
          e.warning(`Worker error`, { safe: {}, sensitive: { error: t } });
        }),
        u.on(`exit`, (t) => {
          (this.worker === u && (this.worker = null),
            this.handleWorkerExit(u),
            t !== 0 &&
              !this.disposedWorkers.has(u) &&
              e.warning(`Worker exited`, { safe: { code: t }, sensitive: {} }));
        }),
        u.unref(),
        (this.worker = u),
        this.latestAuthUser &&
          u.postMessage({
            type: `worker-sentry-user-update`,
            authMethod: this.latestAuthUser.authMethod,
            userId: this.latestAuthUser.userId,
            email: this.latestAuthUser.email,
            accountId: this.latestAuthUser.accountId,
          }),
        u
      );
    }
    async handleMainRpcRequest(e, n) {
      let r = t.Wr(`worker-manager`);
      if (n.workerId !== this.id) {
        r.warning(`Received main RPC request for wrong worker`, {
          safe: { expectedWorkerId: this.id, workerId: n.workerId, method: n.method },
        });
        return;
      }
      let i = this.mainRpcHandler;
      if (!i) {
        let t = {
          type: `worker-main-rpc-response`,
          workerId: this.id,
          requestId: n.requestId,
          method: n.method,
          result: {
            type: `error`,
            error: { message: `No main RPC handler configured for worker '${this.id}'` },
          },
        };
        e.postMessage(t);
        return;
      }
      try {
        let t = await i(n, this.createMainRpcHandlerContext(e)),
          r = {
            type: `worker-main-rpc-response`,
            workerId: this.id,
            requestId: n.requestId,
            method: n.method,
            result: { type: `ok`, value: t },
          };
        e.postMessage(r);
      } catch (t) {
        let r = {
          type: `worker-main-rpc-response`,
          workerId: this.id,
          requestId: n.requestId,
          method: n.method,
          result: {
            type: `error`,
            error: {
              message:
                t instanceof Error ? t.message : `Failed to handle main RPC request: ${String(t)}`,
            },
          },
        };
        e.postMessage(r);
      }
    }
    createMainRpcHandlerContext(e) {
      return {
        emitEvent: (t) => {
          let n = {
            type: `worker-main-rpc-event`,
            workerId: this.id,
            method: t.method,
            params: t.params,
          };
          e.postMessage(n);
        },
      };
    }
    handleWorkerExit(e) {
      if (this.exitedWorkers.has(e)) return;
      (this.exitedWorkers.add(e),
        this.exitListeners.forEach((e) => {
          e();
        }));
      let n = this.mainRpcHandler;
      if (!n) return;
      let r = t.Wr(`worker-manager`);
      n(
        {
          type: `worker-main-rpc-request`,
          workerId: this.id,
          requestId: `worker-exit`,
          method: `worker-exit`,
          params: {},
        },
        this.createMainRpcHandlerContext(e),
      ).catch((e) => {
        r.warning(`Worker exit cleanup failed`, { safe: {}, sensitive: { error: e } });
      });
    }
  },
  n5 = class {
    logger = t.Wr(`worker-bus-message-handler`);
    pending = new Map();
    pendingRequestFromHost = new Map();
    hostListeners = new Set();
    knownWebContents = new Set();
    workerManager;
    onWorkerRequestSent;
    constructor(e, t = {}) {
      ((this.workerId = e),
        (this.onWorkerRequestSent = t.onWorkerRequestSent ?? null),
        (this.workerManager = new t5(this.workerId)),
        this.workerManager.addListener((e) => {
          this.handleWorkerMessage(e);
        }));
    }
    async handleMessage(e, t) {
      if ((this.trackLifecycle(e), t.workerId !== this.workerId)) {
        this.logger.warning(`Received worker message for wrong worker`, {
          safe: { expectedWorkerId: this.workerId, workerId: t.workerId, type: t.type },
        });
        return;
      }
      switch (t.type) {
        case `worker-request`: {
          let n = t;
          (this.pending.set(n.request.id, e),
            this.onWorkerRequestSent?.(),
            this.workerManager.postMessage(n));
          return;
        }
        case `worker-request-cancel`:
          this.pending.delete(t.id);
          break;
      }
      this.workerManager.postMessage(t);
    }
    dispose() {
      let e = Error(`Worker bus disposed for '${this.workerId}'`);
      for (let t of this.pendingRequestFromHost.values()) t.reject(e);
      (this.pendingRequestFromHost.clear(), this.workerManager.dispose());
    }
    requestFromHost(e) {
      if (e.signal?.aborted) return Promise.reject(new DOMException(`Aborted`, `AbortError`));
      let n = t.Yr(`main-${(0, u.randomUUID)()}`),
        r = { id: n, method: e.method, params: e.params },
        i = { type: `worker-request`, workerId: this.workerId, request: r },
        a = new Promise((t, r) => {
          this.pendingRequestFromHost.set(n, {
            method: e.method,
            resolve: (e) => {
              t(e);
            },
            reject: r,
          });
        });
      try {
        (this.onWorkerRequestSent?.(), this.workerManager.postMessage(i));
      } catch (e) {
        return (
          this.pendingRequestFromHost.delete(n),
          Promise.reject(
            e instanceof Error ? e : Error(`Failed to post worker request: ${String(e)}`),
          )
        );
      }
      let o = e.signal;
      if (o) {
        let e = () => {
          o.removeEventListener(`abort`, e);
          let t = this.pendingRequestFromHost.get(n);
          t &&
            (this.pendingRequestFromHost.delete(n),
            this.workerManager.postMessage({
              type: `worker-request-cancel`,
              workerId: this.workerId,
              id: n,
            }),
            t.reject(new DOMException(`Aborted`, `AbortError`)));
        };
        o.addEventListener(`abort`, e);
        let t = () => {
          o.removeEventListener(`abort`, e);
        };
        a.then(t, t);
      }
      return a;
    }
    subscribeFromHost(e) {
      return (
        this.hostListeners.add(e),
        () => {
          this.hostListeners.delete(e);
        }
      );
    }
    subscribeWorkerExit(e) {
      return this.workerManager.addExitListener(e);
    }
    setSentryUser(e, t, n, r) {
      this.workerManager.setSentryUser(e, t, n, r);
    }
    emitAppEvent(e) {
      this.workerManager.emitAppEvent(e);
    }
    setMainRpcHandler(e) {
      this.workerManager.setMainRpcHandler(e);
    }
    startWorker() {
      this.workerManager.start();
    }
    trackLifecycle(e) {
      this.knownWebContents.has(e) ||
        (this.knownWebContents.add(e),
        e.once(`destroyed`, () => {
          (this.knownWebContents.delete(e), this.cleanupOrigin(e));
        }));
    }
    cleanupOrigin(e) {
      for (let [t, n] of this.pending.entries()) {
        if (n !== e) continue;
        this.pending.delete(t);
        let r = { type: `worker-request-cancel`, workerId: this.workerId, id: t };
        this.workerManager.postMessage(r);
      }
    }
    handleWorkerMessage(e) {
      if (e.workerId !== this.workerId) {
        this.logger.warning(`Received worker message for wrong worker`, {
          safe: { expectedWorkerId: this.workerId, workerId: e.workerId, type: e.type },
        });
        return;
      }
      if (e.type === `worker-response`) {
        let t = this.pendingRequestFromHost.get(e.response.id);
        if (t) {
          if ((this.pendingRequestFromHost.delete(e.response.id), t.method !== e.response.method)) {
            t.reject(Error(`Mismatched worker response method`));
            return;
          }
          if (e.response.result.type === `ok`) {
            t.resolve(e.response.result.value);
            return;
          }
          t.reject(Error(e.response.result.error.message));
          return;
        }
        let n = this.pending.get(e.response.id);
        if (!n) return;
        (this.pending.delete(e.response.id), this.send(n, e));
        return;
      }
      (this.hostListeners.forEach((t) => {
        t(e);
      }),
        this.broadcast(e));
    }
    send(e, t) {
      e.isDestroyed() || e.send(tu(this.workerId), t);
    }
    broadcast(e) {
      this.knownWebContents.forEach((t) => {
        this.send(t, e);
      });
    }
  };
function r5({ disposables: e, isTrustedIpcEvent: n }) {
  let r = new t.br(),
    i = new t.$(r),
    o = new Z8(),
    s = [],
    c = new n5(`git`, {
      onWorkerRequestSent: () => {
        o.recordInvocation();
      },
    }),
    l = new n5(`computer-use-capture`),
    u = new Set(),
    d = new Map(),
    f = null,
    p = null,
    m = (e, t, n, r) => {
      ((f = { authMethod: e, userId: t, email: n, accountId: r }),
        s.forEach((i) => {
          i(e, t, n, r);
        }),
        d.forEach((i) => {
          i.setSentryUser(e, t, n, r);
        }));
    },
    h = (e) => c.requestFromHost(e),
    g = async (e) => {
      let t = await l.requestFromHost(e);
      return (y(e.params.requestId, e.signal), t);
    },
    _ = (t, n) => {
      (e.add(n),
        s.push((e, t, r, i) => {
          n.setSentryUser(e, t, r, i);
        }),
        e.add(
          r.subscribe((e) => {
            n.emitAppEvent(e);
          }),
        ));
    },
    v = (e, t) => {
      (_(e, t),
        a.ipcMain.handle(eu(e), async (e, r) => {
          n(e) && (await t.handleMessage(e.sender, r));
        }));
    };
  return {
    appEvent: r,
    gitManager: i,
    gitWorkerInvocationSampler: o,
    requestComputerUseCaptureWorker: g,
    requestGitWorker: h,
    subscribeComputerUseCaptureWorkerEvent: (e, t) => {
      let n = (n) => {
        i5(n, e) && t(n);
      };
      return (
        u.add(n),
        () => {
          u.delete(n);
        }
      );
    },
    subscribeGitWorkerEvent: (e, t) =>
      c.subscribeFromHost((n) => {
        n.type === `worker-event` && a5(n.event, e) && t(n.event);
      }),
    updateWorkerSentryUser: m,
    registerWorkerBusMessageHandlers: () => {
      _(`computer-use-capture`, l);
      for (let e of t.Jr) {
        if (e === `git`) {
          v(e, c);
          continue;
        }
        v(e, new n5(e));
      }
    },
    setGitWorkerMainRpcHandler: (e) => {
      c.setMainRpcHandler(e);
    },
    setComputerUseCaptureWorkerMainRpcHandler: (e) => {
      ((p = e),
        l.setMainRpcHandler(e),
        d.forEach((t) => {
          t.setMainRpcHandler(e);
        }));
    },
    startComputerUseCaptureWorker: () => {
      l.startWorker();
    },
  };
  function y(e, t) {
    if (d.has(e)) return;
    let n = new n5(`computer-use-capture`);
    (p != null && n.setMainRpcHandler(p),
      d.set(e, n),
      f != null && n.setSentryUser(f.authMethod, f.userId, f.email, f.accountId));
    let i = r.subscribe((e) => {
        n.emitAppEvent(e);
      }),
      a = n.subscribeFromHost((t) => {
        t.type === `worker-event` &&
          (u.forEach((e) => {
            e(t.event);
          }),
          t.event.type === `update` &&
            t.event.requestId === e &&
            (t.event.update.type === `completed` || t.event.update.type === `failed`) &&
            l());
      }),
      o = n.subscribeWorkerExit(() => {
        c || (m(`update_worker_exited`), l());
      }),
      s = () => {
        l();
      };
    t?.addEventListener(`abort`, s, { once: !0 });
    let c = !1,
      l = () => {
        c ||
          ((c = !0),
          a(),
          o(),
          t?.removeEventListener(`abort`, s),
          i.dispose(),
          d.delete(e),
          n.dispose());
      },
      m = (t) => {
        u.forEach((n) => {
          n({ type: `update`, requestId: e, update: { type: `failed`, failureReason: t } });
        });
      };
    (n.startWorker(),
      n
        .requestFromHost({ method: `watchUpdates`, params: { requestId: e }, signal: t })
        .catch(() => {
          c || (m(`update_watch_failed`), l());
        }));
  }
}
function i5(e, t) {
  return e.type === t;
}
function a5(e, t) {
  return e.type === t;
}
var o5 = 3,
  s5 = 5.5,
  c5 = 1,
  l5 = null,
  u5 = null,
  d5 = !1,
  f5 = !1;
function p5() {
  return l5;
}
async function m5(e) {
  return process.platform !== `win32` && process.platform !== `darwin`
    ? null
    : ((d5 = !0),
      l5 ??
        u5 ??
        ((f5 ||=
          (a.app.on(`before-quit`, () => {
            h5();
          }),
          !0)),
        (u5 = (async () => {
          let t = await g5(e.buildFlavor, e.repoRoot),
            n = new a.Tray(t.defaultIcon);
          return d5
            ? (n.setToolTip(a.app.getName()),
              (l5 = new b$(
                n,
                e.onTrayButtonClick,
                e.onTrayMenuOpenMainWindow,
                e.onTrayMenuOpenNewThread,
                e.onTrayMenuOpenRecentThread,
                e.getChronicleSidecarControlState,
                e.toggleChronicleSidecar,
                t,
                r.V(),
                a.app.getName(),
              )),
              l5)
            : (n.destroy(), null);
        })().finally(() => {
          u5 = null;
        })),
        u5));
}
function h5() {
  ((d5 = !1), l5?.destroy(), (l5 = null));
}
async function g5(e, t) {
  if (process.platform === `darwin`) {
    let e = [
      ...(a.app.isPackaged ? [(0, s.join)(process.resourcesPath, `codexTemplate.png`)] : []),
      (0, s.join)(t, `electron`, `src`, `icons`, `codexTemplate.png`),
    ];
    for (let t of e) {
      let e = a.nativeImage.createFromPath(t);
      if (!e.isEmpty())
        return (e.setTemplateImage(!0), { defaultIcon: e, chronicleRunningIcon: _5(e) });
    }
    let n = await a.app.getFileIcon(process.execPath, { size: `normal` });
    return { defaultIcon: n, chronicleRunningIcon: _5(n) };
  }
  let n = process.platform === `win32` ? `.ico` : `.png`,
    r = $1(e, process.platform),
    i = [
      ...(a.app.isPackaged ? [(0, s.join)(process.resourcesPath, `${r}${n}`)] : []),
      (0, s.join)(t, `electron`, `src`, `icons`, `${r}${n}`),
    ];
  for (let e of i) {
    let t = a.nativeImage.createFromPath(e);
    if (!t.isEmpty()) return { defaultIcon: t, chronicleRunningIcon: null };
  }
  return {
    defaultIcon: await a.app.getFileIcon(process.execPath, {
      size: process.platform === `win32` ? `small` : `normal`,
    }),
    chronicleRunningIcon: null,
  };
}
function _5(e) {
  let t = e.getSize(),
    n = t.width,
    r = t.height;
  if (n <= 0 || r <= 0) return null;
  let i = a.nativeImage.createEmpty(),
    o = !1;
  for (let t of e.getScaleFactors()) {
    let a = v5(e, n, r, t);
    a != null && (i.addRepresentation(a), (o = !0));
  }
  return !o || i.isEmpty() ? null : (i.setTemplateImage(!0), i);
}
function v5(e, t, n, r) {
  let i = Buffer.from(e.toBitmap({ scaleFactor: r })),
    a = y5(i, t, n);
  if (a == null) return null;
  let { height: o, scaleFactor: s, width: c } = a,
    l = Math.min(c, o) / Math.min(t, n),
    u = o5 * l,
    d = s5 * l,
    f = c5 * l,
    p = c - u - f,
    m = o - u - f;
  for (let e = 0; e < o; e += 1)
    for (let t = 0; t < c; t += 1) {
      let n = Math.hypot(t + 0.5 - p, e + 0.5 - m),
        r = (e * c + t) * 4;
      (n <= d && (i[r + 3] = 0),
        n <= u && ((i[r] = 0), (i[r + 1] = 0), (i[r + 2] = 0), (i[r + 3] = 255)));
    }
  return { buffer: i, width: c, height: o, scaleFactor: s };
}
function y5(e, t, n) {
  let r = e.length / 4;
  if (!Number.isInteger(r) || r <= 0) return null;
  if (r === t * n) return { width: t, height: n, scaleFactor: 1 };
  let i = t / n,
    a = Math.round(Math.sqrt(r / i)),
    o = Math.round(a * i);
  return o * a === r ? { width: o, height: a, scaleFactor: o / t } : null;
}
function b5({
  exitImmediately: e,
  exitImmediatelyForUpdateInstall: t,
  quitForUpdateInstall: n,
  quitState: r,
}) {
  if (e) {
    t();
    return;
  }
  (r.allowQuitTemporarilyForUpdateInstall(), n());
}
var x5 = (0, c.promisify)(m.execFile),
  S5 = `--open-project`,
  C5 = `HKCU\\Software\\Classes\\Directory\\shell\\OpenProjectInCodex`,
  w5 = `${C5}\\command`;
function T5(e) {
  return [
    { key: C5, value: `Open project in Codex` },
    { key: C5, name: `Icon`, value: `"${e}",0` },
    { key: w5, value: `"${e}" ${S5} "%1"` },
  ];
}
async function E5({ key: e, name: t, value: n }) {
  await x5(`reg.exe`, [`add`, e, ...(t ? [`/v`, t] : [`/ve`]), `/d`, n, `/f`], { windowsHide: !0 });
}
async function D5({ isWindows: e, isPackaged: n, executablePath: r }) {
  if (!(!e || !n))
    try {
      await Promise.all(T5(r).map(E5));
    } catch (e) {
      t.Ur().warning(`Failed to register Windows folder context menu`, {
        safe: {},
        sensitive: { error: e },
      });
    }
}
var O5 = 2e3;
function k5({
  platform: e = process.platform,
  pid: t = process.pid,
  loadNativeAddon: n = r.A,
} = {}) {
  e === `win32` && j5(n).killProcessDescendants(t);
}
function A5({ delayMs: e = O5, loadNativeAddon: t = r.A, platform: n = process.platform } = {}) {
  n === `win32` && j5(t).armTerminateCurrentProcess(e);
}
function j5(e) {
  let t = e();
  if (t == null) throw Error(`Failed to load Windows updater addon.`);
  return t;
}
function M5(e) {
  return async (t) => {
    switch (t.method) {
      case `computer-use-invalidate-service-pid`:
        return (await e.invalidateManagedComputerUseService(t.params.processIdentifier), {});
      case `computer-use-service-pid`:
        return e.ensureManagedComputerUseService();
      case `worker-exit`:
        return {};
    }
  };
}
function N5(e) {
  return JSON.stringify([e.hostId, e.watchId]);
}
function P5(e) {
  let t = new Map(),
    n = new Map(),
    r = (t) => {
      let n = e.getAppServerClientForHost(t);
      if (!n) throw Error(`App server state for host ID ${t.hostId} not found`);
      return n;
    },
    i = async (e) => {
      let n = t.get(e);
      if (!n) throw Error(`Process session ${e} not found`);
      return n.session ? n.session : n.sessionPromise;
    },
    a = () => {
      let e = Array.from(n.values());
      n.clear();
      for (let t of e) t.session && t.session.dispose().catch(() => void 0);
    };
  return async (o, s) => {
    switch (o.method) {
      case `worker-exit`:
        return (a(), {});
      case `worktree-cleanup-inputs`:
        return e.getWorktreeCleanupInputsForHost(o.params);
      case `codex-home`: {
        let e = o.params;
        return r(e).codexHome();
      }
      case `platform-family`: {
        let e = o.params;
        return r(e).platformFamily();
      }
      case `platform-os`: {
        let e = o.params;
        return r(e).platformOs();
      }
      case `process-start`: {
        let e = o.params,
          n = r({ hostId: e.hostId }),
          { hostId: i, ...a } = e,
          c = n.startProcess({
            ...a,
            onStdoutDelta: (t) => {
              s.emitEvent({
                method: `process-output-delta`,
                params: {
                  processHandle: e.processHandle,
                  stream: `stdout`,
                  delta: {
                    chunk: new Uint8Array(t.chunk.buffer, t.chunk.byteOffset, t.chunk.byteLength),
                    capReached: t.capReached,
                  },
                },
              });
            },
            onStderrDelta: (t) => {
              s.emitEvent({
                method: `process-output-delta`,
                params: {
                  processHandle: e.processHandle,
                  stream: `stderr`,
                  delta: {
                    chunk: new Uint8Array(t.chunk.buffer, t.chunk.byteOffset, t.chunk.byteLength),
                    capReached: t.capReached,
                  },
                },
              });
            },
          }),
          l = { sessionPromise: c, session: null };
        t.set(e.processHandle, l);
        try {
          let e = await c;
          return ((l.session = e), await e.response);
        } finally {
          t.get(e.processHandle) === l && t.delete(e.processHandle);
        }
      }
      case `fs-read-file`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).readFile(t);
      }
      case `fs-write-file`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).writeFile(t);
      }
      case `fs-create-directory`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).createDirectory(t);
      }
      case `fs-get-metadata`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).getMetadata(t);
      }
      case `fs-read-directory`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).readDirectory(t);
      }
      case `fs-remove`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).remove(t);
      }
      case `fs-copy`: {
        let { hostId: e, ...t } = o.params;
        return r({ hostId: e }).copy(t);
      }
      case `fs-watch`: {
        let { hostId: e, watchId: t, ...i } = o.params,
          a = N5({ hostId: e, watchId: t }),
          c = r({ hostId: e }),
          l = { session: null };
        n.set(a, l);
        let u;
        try {
          u = await c.startFileWatch({
            ...i,
            watchId: t,
            onChange: (r) => {
              n.get(a) === l &&
                s.emitEvent({
                  method: `fs-watch-changed`,
                  params: { hostId: e, watchId: t, changedPaths: r.changedPaths },
                });
            },
          });
        } catch (e) {
          throw (n.get(a) === l && n.delete(a), e);
        }
        return (
          (l.session = u),
          n.get(a) === l
            ? (u.closed.then((r) => {
                n.get(a) === l &&
                  (n.delete(a),
                  s.emitEvent({
                    method: `fs-watch-closed`,
                    params: { hostId: e, watchId: t, reason: r.reason },
                  }));
              }),
              { path: u.path })
            : (u.dispose().catch(() => void 0), { path: u.path })
        );
      }
      case `fs-unwatch`: {
        let e = o.params,
          t = N5(e),
          r = n.get(t);
        return r ? (n.delete(t), r.session && (await r.session.dispose()), {}) : {};
      }
      case `process-write`: {
        let e = o.params,
          t = await i(e.processHandle),
          n =
            e.delta == null
              ? Buffer.alloc(0)
              : Buffer.from(e.delta.buffer, e.delta.byteOffset, e.delta.byteLength);
        return t.write(n, { closeStdin: e.closeStdin });
      }
      case `process-resize`: {
        let e = o.params;
        return (await i(e.processHandle)).resize(e.size);
      }
      case `process-terminate`: {
        let e = o.params;
        return (await i(e.processHandle)).terminate();
      }
    }
  };
}
function F5(e) {
  return e.replace(/\\/g, `/`);
}
function I5(e) {
  let n = t.rt().replace(/\\/g, `/`),
    i = r.s(e).map(F5),
    a = (e) => {
      let t = F5(e);
      return i.includes(t) ? !1 : t === n || t.startsWith(`${n}/`);
    },
    o = r.i(e),
    s = o.filter((e) => !a(e));
  s.length !== o.length && r.u(e, s);
  let c = r.s(e),
    l = c.filter((e) => !a(e));
  l.length !== c.length && r.d(e, l);
}
var L5 = { enabled: !1, running: !1, state: `disabled` };
function R5(e) {
  let t = (e instanceof Error ? e.message : String(e)).toLowerCase();
  return (
    t.includes(`sqlite state db`) ||
    t.includes(`state db backfill`) ||
    t.includes(`sqlite`) ||
    t.includes(`database`)
  );
}
async function z5() {
  let {
      startedAtMs: e,
      buildFlavor: i,
      desktopSentry: o,
      sparkleManager: c,
      setSparkleBridgeHandlers: l,
      setSecondInstanceArgsHandler: u,
    } = r.k(),
    d = t.b(i),
    f = n.i.shouldIncludeSparkle(i, process.platform, process.env),
    p = n.i.shouldIncludeUpdater(i, process.platform, process.env),
    m = n.i.allowDevtools(i),
    h = i === n.i.Dev || i === n.i.Agent,
    g = n.i.allowDebugMenu(i),
    _ = t.Mt((e, t, n) => {
      o.captureException(e, {
        level: t === `fatal` ? `fatal` : `error`,
        tags: n.tags,
        extra: n.extra,
      });
    }),
    v = a.app.getVersion(),
    y = n.a(),
    b = new t.Lt({
      source: `codex-desktop`,
      env: i,
      codexAppSessionId: r.N,
      platform: process.platform,
      buildInfo: { version: v, buildNumber: r.M.value },
      fetchImpl: (e, t) => a.net.fetch(e instanceof URL ? e.toString() : e, t),
      reportFailure: (e) => {
        _.reportNonFatal(`Datadog log sink failure`, {
          kind: `datadog-log-sink-failure`,
          tags: { failureType: e.type, reason: e.reason },
          extra: { failure: e },
        });
      },
    });
  r.x(_, b, r.N, d);
  let x = uu({ app: a.app }),
    S = du({ app: a.app }),
    C = t.Kr(`startup`),
    w = (t, n, r = {}) => {
      let i = Date.now();
      C.trace(t, { safe: { phaseElapsedMs: i - n, startupElapsedMs: i - e, ...r } });
    };
  w(`bootstrap handoff complete`, e, { appWhenReadyResolved: a.app.isReady() });
  let T = process.platform === `darwin`,
    E = process.platform === `win32`,
    D = !a.app.isPackaged,
    O = !1,
    k = new t.Rn();
  (k.add(x),
    k.add(S),
    t
      .Ur()
      .info(`Launching app`, {
        safe: {
          buildFlavor: i,
          nodeEnv: process.env.NODE_ENV,
          enableUpdater: p,
          enableSparkle: f,
          allowDevtools: m,
          allowInspectElement: h,
          allowDebugMenu: g,
          platform: process.platform,
          packaged: a.app.isPackaged,
          agentRunId: process.env.CODEX_ELECTRON_AGENT_RUN_ID?.trim() || null,
        },
      }));
  let A = Date.now();
  (await a.app.whenReady(),
    w(`main app.whenReady resolved`, A),
    D && ((A = Date.now()), w(`react devtools extension loaded=${await _d()}`, A)),
    (A = Date.now()),
    r.R(r.I(__dirname)),
    w(`registered app protocol`, A),
    (A = Date.now()),
    await Z_(),
    w(`shell environment hydrated`, A));
  let j = K1({ moduleDir: __dirname });
  await r.B(j.repoRoot);
  let M = Y8({
      appBrand: r.H(),
      buildFlavor: i,
      allowDevtools: m,
      allowDebugMenu: g,
      allowInspectElement: h,
      errorReporter: _,
      globalState: j.globalState,
      settingsStore: j.settingsStore,
      desktopRoot: j.desktopRoot,
      preloadPath: j.preloadPath,
      repoRoot: j.repoRoot,
      isMacOS: T,
      isWindows: E,
      isDevMode: D,
      canHideLastWindowToTray: () => O,
      disposables: k,
    }),
    N = !1,
    P = () => (N ? M.ensureWindow() : Promise.resolve(null)),
    F = (e) => (N ? M.createFreshWindow(e) : Promise.resolve(null)),
    I = (e) => M.isTrustedIpcSender(e.sender, e.senderFrame ?? null);
  (U$({
    buildFlavor: i,
    getContextForWebContents: M.getContextForWebContents,
    isTrustedIpcEvent: I,
    usesOwlAppShell: y,
  }),
    a.ipcMain.on(Ul, (e) => {
      if (!I(e)) {
        e.returnValue = `light`;
        return;
      }
      e.returnValue = a.nativeTheme.shouldUseDarkColors ? `dark` : `light`;
    }));
  let ee = () => {
    let e = a.nativeTheme.shouldUseDarkColors ? `dark` : `light`;
    M.windowManager.refreshWindowBackdrops();
    for (let t of a.BrowserWindow.getAllWindows()) t.isDestroyed() || t.webContents.send(Kl, e);
  };
  (a.nativeTheme.on(`updated`, ee),
    k.add(() => {
      a.nativeTheme.off(`updated`, ee);
    }));
  let te = B$(),
    ne = null,
    re = (e) => {
      if (e?.quitImmediately === !1) {
        te.allowQuitTemporarilyForUpdateInstall();
        return;
      }
      (te.allowQuitTemporarilyForUpdateInstall(), a.app.quit());
    },
    ie = r5({ disposables: k, isTrustedIpcEvent: I }),
    ae = [],
    oe = null,
    se = null,
    ce = (e) => !1,
    le = Cd({
      ensureWindow: P,
      showPrimaryWindow: (e) => N && M.windowManager.showPrimaryWindow(e),
    }),
    ue = l$({
      app: a.app,
      globalState: j.globalState,
      settingsStore: j.settingsStore,
      windowServices: M,
      repoRoot: j.repoRoot,
      errorReporter: _,
      desktopSentry: o,
      datadogLogger: b,
      maxLogLevel: d,
      buildFlavor: i,
      sparkleManager: c,
      avatarOverlayManager: M.avatarOverlayManager,
      hotkeyWindowLifecycleManager: M.hotkeyWindowLifecycleManager,
      hasActiveComputerUseTurn: (e) => ce(e),
      globalDictationHotkeyController: M.globalDictationLifecycleManager.getHotkeyController(),
      appshotHotkeyController: M.appshotHotkeyController,
      gitManager: ie.gitManager,
      requestComputerUseCaptureWorker: ie.requestComputerUseCaptureWorker,
      requestGitWorker: ie.requestGitWorker,
      refreshApplicationMenu: () => {
        se?.();
      },
      subscribeComputerUseCaptureWorkerEvent: ie.subscribeComputerUseCaptureWorkerEvent,
      subscribeGitWorkerEvent: ie.subscribeGitWorkerEvent,
      gitWorkerInvocationSampler: ie.gitWorkerInvocationSampler,
      updateWorkerSentryUser: ie.updateWorkerSentryUser,
      appEvent: ie.appEvent,
      onAppServerSqliteRecovery: le.onRecovery,
      onCompletedThreadsChanged: (e) => {
        ((ae = e), oe?.setCompletedThreads(e));
      },
    }),
    de = ue.getWindowContext();
  l({
    onDownloadProgressChanged: () => {
      de.broadcastAppUpdateState();
    },
    onInstallProgressChanged: () => {
      E && de.broadcastAppUpdateState();
    },
    onUpdateReadyChanged: () => {
      de.broadcastAppUpdateState();
    },
    onUpdateLifecycleStateChanged: () => {
      de.broadcastAppUpdateState();
    },
    onRelaunchNoticeChanged: () => {
      de.broadcastAppUpdateState();
    },
    onInstallUpdatesRequested: (e) => {
      re(e);
    },
    isTrustedIpcEvent: I,
  });
  let fe = () => de.appServerConnectionRegistry.getConnection(z),
    pe = N1({
      buildFlavor: i,
      globalState: j.globalState,
      errorReporter: _,
      sparkleManager: c,
      gitManager: ie.gitManager,
      getLocalAppServerClient: () => de.getAppServerClient(),
      applyCodexAppConfig: async (e) => {
        try {
          await Rc(e, await de.applyCodexAppConfigFromDisk(j.codexHome));
        } catch (n) {
          (t
            .Ur()
            .warning(`Failed to apply Codex app config from deeplink`, {
              safe: {},
              sensitive: { error: n },
            }),
            await Rc(e, { status: `unexpected-error`, configPath: Ic(j.codexHome), error: n }));
        }
      },
      getLocalAppServerConnection: fe,
      windowServices: M,
      createFreshWindow: F,
      ensureWindow: P,
      getElectronMessageHandlerForWindow: ue.getElectronMessageHandlerForWindow,
      allowDevtools: m,
      allowDebugMenu: g,
      enableSparkle: f,
      isMacOS: T,
      appVersion: v,
    });
  se = pe.refreshApplicationMenu;
  let me = () => {
    pe.refreshApplicationMenu();
  };
  (ue.setOnBrowserSidebarStateChanged(() => {
    pe.refreshApplicationMenu();
  }),
    a.app.on(`browser-window-focus`, me),
    k.add(() => {
      a.app.off(`browser-window-focus`, me);
    }),
    M.windowManager.setOnMainProcessInspectorStateChanged(() => {
      pe.refreshApplicationMenu();
    }),
    M.windowManager.setQueueCodexDeepLinkUrl(pe.deepLinks.queueCodexDeepLinkUrl),
    ie.setGitWorkerMainRpcHandler(
      P5({
        getAppServerClientForHost: ue.getAppServerClientForHost,
        getWorktreeCleanupInputsForHost: ue.getWorktreeCleanupInputsForHost,
      }),
    ),
    ie.setComputerUseCaptureWorkerMainRpcHandler(
      M5({
        ensureManagedComputerUseService: () => {
          if (ne == null) throw Error(`Managed Computer Use service is unavailable`);
          return ne.ensureServicePid();
        },
        invalidateManagedComputerUseService: (e) =>
          ne == null ? Promise.resolve() : ne.invalidateServicePid(e),
      }),
    ),
    ie.registerWorkerBusMessageHandlers(),
    T && n.i.isInternal(i) && ie.startComputerUseCaptureWorker(),
    (A = Date.now()),
    pe.deepLinks.registerProtocolClient(),
    r.t(r.h(process.argv)),
    w(`registered deep link protocol`, A));
  let he = (e) => {
      (e.isMinimized() && e.restore(), e.show(), e.focus());
    },
    ge = async () => {
      if (N)
        try {
          M.hotkeyWindowLifecycleManager.hide();
          let e = M.getPrimaryWindow() ?? (await F(`/`));
          if (e == null) return;
          he(e);
        } catch (e) {
          _.reportNonFatal(e instanceof Error ? e : `Failed to open window on second instance`, {
            kind: `second-instance-open-window-failed`,
          });
        }
    };
  u((e) => {
    let t = r.t(r.h(e));
    if (pe.deepLinks.queueProcessArgs(e)) {
      t && ge();
      return;
    }
    if (t) {
      ge();
      return;
    }
    ge();
  });
  let _e = async (e, t) => {
    if (!N) return;
    M.hotkeyWindowLifecycleManager.hide();
    let n = M.getPrimaryWindow(),
      r = n ?? (await F(e));
    r != null &&
      (ue.desktopNotificationManager.dismissByNavigationPath(e),
      n != null && t.navigateExistingWindow && pe.navigateToRoute(r, e),
      he(r));
  };
  T &&
    ((oe = new Mu((e) => _e(e, { navigateExistingWindow: !0 }), r.V())),
    oe.setCompletedThreads(ae));
  let ve = async () => {
      O = !0;
      try {
        await m5({
          buildFlavor: i,
          repoRoot: j.repoRoot,
          onTrayButtonClick: async () => {
            M.hotkeyWindowLifecycleManager.show() || (await P());
          },
          onTrayMenuOpenMainWindow: () => _e(`/`, { navigateExistingWindow: !1 }),
          onTrayMenuOpenNewThread: () => _e(`/`, { navigateExistingWindow: !0 }),
          onTrayMenuOpenRecentThread: (e) => _e(e, { navigateExistingWindow: !0 }),
          getChronicleSidecarControlState: () =>
            de.appServerConnectionRegistry
              .getMaybeConnection(`local`)
              ?.getChronicleSidecarControlState() ?? L5,
          toggleChronicleSidecar: async () => {
            let e = de.appServerConnectionRegistry.getMaybeConnection(z);
            return e == null
              ? L5
              : e.getChronicleSidecarControlState().running
                ? e.pauseChronicleSidecar()
                : e.resumeChronicleSidecar();
          },
        });
      } catch (e) {
        ((O = !1),
          _.reportNonFatal(e instanceof Error ? e : `Failed to set up tray`, {
            kind: `tray-setup-failed`,
            tags: { errorType: `tray-setup-failed` },
          }),
          P());
      }
    },
    ye = async (e) => {
      if (T) {
        if (((O = e), !e)) {
          h5();
          return;
        }
        await ve();
      }
    };
  E && ve();
  let be = Fn({
    codexCliPath: Rn({ isPackaged: a.app.isPackaged, repoRoot: j.repoRoot }).codexCliPath,
    codexHome: j.codexHome,
    onAnalyticsEvent: (e) => {
      let t = M.getPrimaryWindow();
      t != null &&
        M.windowManager.sendMessageToWebContents(t.webContents, {
          type: `computer-use-analytics-event`,
          event: e,
        });
    },
  });
  ((ce = be.hasActiveTurn),
    k.add(Ml({ appServerConnection: fe(), closeActiveTurn: be.closeActiveTurn })));
  let xe = Tc({
      appVersion: v,
      buildFlavor: i,
      codexHome: j.codexHome,
      getLocalAppServerConnection: fe,
      globalState: j.globalState,
      isPackaged: a.app.isPackaged,
      onSkillsMayHaveChanged: () => {
        M.sendMessageToAllRegisteredWindows({
          type: `ipc-broadcast`,
          method: `query-cache-invalidate`,
          sourceClientId: `desktop`,
          version: t.es(`query-cache-invalidate`),
          params: { queryKey: [`skills`] },
        });
      },
      onReconcileComplete: () => {
        (M.sendMessageToAllRegisteredWindows({
          type: `ipc-broadcast`,
          method: `query-cache-invalidate`,
          sourceClientId: `desktop`,
          version: t.es(`query-cache-invalidate`),
          params: { queryKey: [`plugins`] },
        }),
          be.refresh());
      },
      repoRoot: j.repoRoot,
      trashItem: (e) => a.shell.trashItem(e),
    }),
    Se = () => {
      xe.reconcileExternalPluginState(`focus`);
    };
  if (
    (a.app.on(`browser-window-focus`, Se),
    k.add(() => {
      a.app.off(`browser-window-focus`, Se);
    }),
    T)
  ) {
    try {
      await Qc({ sourceServiceAppPath: Hc(), codexHome: j.codexHome });
    } catch (e) {
      t.Ur().warning(`Failed to sync canonical Computer Use service app during startup`, {
        safe: {},
        sensitive: { error: e },
      });
    }
    if (((ne = new Al(Zc(j.codexHome))), k.add(ne), n.i.isInternal(i))) {
      delete process.env[In];
      try {
        let e = await fd({
          services: new Map([
            [
              `computer-use`,
              async () => {
                if (ne == null) throw Error(`Managed Computer Use service is unavailable`);
                return (await ne.ensureServicePid(), {});
              },
            ],
          ]),
        });
        ((process.env[In] = e.pipePath),
          k.add(() => {
            (process.env.NODE_REPL_HOST_SERVICES_PIPE_PATH === e.pipePath && delete process.env[In],
              e.dispose());
          }));
      } catch (e) {
        (delete process.env[In],
          t
            .Ur()
            .warning(`Failed to start node_repl host services native pipe`, {
              safe: {},
              sensitive: { error: e },
            }));
      }
    }
  }
  let Ce = Je({
    setBrowserUseNativePipeEnabled: (e) => {
      de.getBrowserSessionRegistry().setBrowserUseNativePipeEnabled(e);
    },
  });
  k.add(() => {
    (Ce.dispose(), be.dispose());
  });
  let we = (e) => {
    let n = He(e);
    if (
      (pe.setDesktopFeatureAvailability(n),
      xe.setDesktopFeatureAvailability(n),
      Ce.setDesktopFeatureAvailability(n),
      be.setDesktopFeatureAvailability(n),
      ne != null)
    ) {
      let e = Re(),
        n = e.appshotsEnabled,
        r = e.computerUse && e.computerUseNodeRepl;
      ne.setEnabledFeatures({ appshotsEnabled: n, nodeReplEnabled: r }).catch((e) => {
        t.Ur().warning(`Failed to reconcile managed Computer Use service`, {
          safe: { appshotsEnabled: n, nodeReplEnabled: r },
          sensitive: { error: e },
        });
      });
    }
  };
  (H$({
    desktopSentry: o,
    hotkeyWindowLifecycleManager: M.hotkeyWindowLifecycleManager,
    globalDictationLifecycleManager: M.globalDictationLifecycleManager,
    codexHome: j.codexHome,
    nativeContextMenuIconSearchRoots: [
      (0, s.join)(j.repoRoot, `webview`, `public`),
      (0, s.join)(j.desktopRoot, `webview`),
    ],
    getContextForWebContents: M.getContextForWebContents,
    getPrimaryWindow: M.getPrimaryWindow,
    showPrimaryWindow: (e) => N && M.windowManager.showPrimaryWindow(e),
    sendMessageToWindow: M.sendMessageToWindow,
    ensureWindow: P,
    createFreshWindow: F,
    setDesktopFeatureAvailability: we,
    setAutomaticBackgroundDownloadsEnabled: (e) => {
      c.setAutomaticBackgroundDownloadsEnabled(e);
    },
    setRealtimeSessionRecordingEnabled: ue.setRealtimeSessionRecordingEnabled,
    restoreAvatarOverlayOpenState: (e) => M.avatarOverlayManager.restoreOpenState(e),
    getAvatarOverlayCompositionSurfaceHost: (e) =>
      M.avatarOverlayManager.getCompositionSurfaceHost(e),
    reloadBundledPlugins: () => xe.reloadBundledPlugins(),
    setMacMenuBarEnabled: ye,
    navigateToRoute: pe.navigateToRoute,
    getDockMenuController: () => oe,
    getTrayController: p5,
    isTrustedIpcEvent: I,
  }),
    W$({
      isWindows: E,
      disableQuitConfirmationPrompt: process.env.CODEX_ELECTRON_DISABLE_QUIT_CONFIRMATION === `1`,
      quitState: te,
      windows: M,
      applicationMenuManager: pe.applicationMenuManager,
      ensureWindow: P,
      hotkeyWindowLifecycleManager: M.hotkeyWindowLifecycleManager,
      globalDictationLifecycleManager: M.globalDictationLifecycleManager,
      globalState: j.globalState,
      settingsStore: j.settingsStore,
      flushAndDisposeContext: ue.flushAndDisposeContext,
      disposables: k,
      appEvent: ie.appEvent,
      errorReporter: _,
    }),
    (A = Date.now()),
    I5(j.globalState));
  try {
    await de.applyCodexAppConfigFromDisk(j.codexHome);
  } catch (e) {
    t.Ur().warning(`Failed to apply Codex app config during startup`, {
      safe: {},
      sensitive: { error: e },
    });
  }
  A = Date.now();
  let Te = de.appServerConnectionRegistry.getConnection(z),
    Ee = () => {};
  if (
    !(await r.y({
      databasePath: r.g,
      onBackfillWait: async () => {
        try {
          Ee = await Z1({
            onQuit: () => {
              (te.markQuitApproved(), a.app.quit());
            },
          });
        } catch (e) {
          t.Ur().warning(`Failed to show SQLite backfill progress window`, {
            safe: {},
            sensitive: { error: e },
          });
        }
      },
      open: async () => {
        (await Te.connect(), await le.enable());
      },
      shouldHandleError: R5,
    }))
  )
    return;
  w(`local app-server sqlite initialized`, A);
  try {
    await j.settingsStore.initialize({
      readConfig: () => Te.getUserSavedConfiguration(),
      batchWriteConfigValues: (e) => Te.sendAppServerRequest(`config/batchWrite`, e),
    });
  } catch (e) {
    t.Ur().warning(`Failed to initialize desktop settings`, { safe: {}, sensitive: { error: e } });
  }
  re = (e) => {
    if (e?.quitImmediately === !1) {
      te.allowQuitTemporarilyForUpdateInstall({ allowWithoutPrompt: !0, skipDrainBeforeQuit: !0 });
      return;
    }
    b5({
      exitImmediately: E && a.app.isPackaged,
      exitImmediatelyForUpdateInstall: () => {
        B5({
          runCleanupBeforeImmediateExit: () => {
            (h5(),
              V$({
                hotkeyWindowLifecycleManager: M.hotkeyWindowLifecycleManager,
                globalDictationLifecycleManager: M.globalDictationLifecycleManager,
                flushAndDisposeContext: ue.flushAndDisposeContext,
                disposables: k,
              }));
          },
          markAppQuitting: M.markAppQuitting,
        });
      },
      quitForUpdateInstall: () => {
        a.app.quit();
      },
      quitState: te,
    });
  };
  let De = Gi({
      appVersion: v,
      appServerConnectionRegistry: de.appServerConnectionRegistry,
      getDesktopFeatureAvailability: Re,
      gitManager: ie.gitManager,
      globalState: j.globalState,
      settingsStore: j.settingsStore,
      appServerClient: de.getAppServerClient(),
      worktreeLifecycle: {
        requestCleanup: () => {
          de.getMessageHandler().getWorktreeService().requestCleanup();
        },
        setOwnerThread: ({ conversationId: e, worktree: t }) =>
          de
            .getMessageHandler()
            .getWorktreeService()
            .setOwnerThread({ conversationId: e, worktree: t }),
      },
      requestDynamicToolsForThreadStart: () =>
        de.getMessageHandler().requestDynamicToolsForThreadStart(null, !0),
      isPackaged: a.app.isPackaged,
      repoRoot: j.repoRoot,
    }),
    Oe = qe({
      appEvent: ie.appEvent,
      appServerClient: de.getAppServerClient(),
      appServerConnectionRegistry: de.appServerConnectionRegistry,
      globalState: j.globalState,
      settingsStore: j.settingsStore,
    });
  (de.getMessageHandler().setAutomationSchedulerController(De),
    k.add(() => {
      (De.dispose(), Oe.dispose());
    }),
    w(`window context initialized`, A));
  let ke = Re(),
    Ae = He(ke);
  (Be(ke, Ae) && we(Ae),
    (A = Date.now()),
    await D5({ isWindows: E, isPackaged: a.app.isPackaged, executablePath: process.execPath }),
    w(`windows folder context menu registered`, A, { isWindows: E }),
    (A = Date.now()),
    pe.refreshApplicationMenu(),
    w(`application menu refreshed`, A),
    (A = Date.now()),
    await xe.waitForPendingReconcile(),
    w(`bundled plugins reconcile checked`, A, { isMacOS: T }),
    (A = Date.now()));
  let je = await M.ensureWindow();
  (je &&
    (k.add(
      r.r({
        ensureWindow: M.ensureWindow,
        errorReporter: _,
        globalState: j.globalState,
        isMacOS: T,
        windowManager: M.windowManager,
      }),
    ),
    he(je)),
    (N = !0),
    Ee(),
    w(`window ensured`, A, { windowVisible: je?.isVisible() ?? !1 }),
    (A = Date.now()),
    await pe.deepLinks.flushPendingDeepLinks(),
    w(`pending deep links flushed`, A),
    w(`startup complete`, e));
}
function B5({ markAppQuitting: e, runCleanupBeforeImmediateExit: n }) {
  let r;
  try {
    e();
    try {
      k5();
    } catch (e) {
      r ??= e;
    }
    try {
      A5();
    } catch (e) {
      r ??= e;
    }
    n();
    for (let e of a.BrowserWindow.getAllWindows()) e.isDestroyed() || e.destroy();
    if (r != null)
      throw r instanceof Error
        ? r
        : Error(typeof r == `string` ? r : `Windows update cleanup failed.`);
  } catch (e) {
    t.Ur().warning(`Immediate update exit cleanup failed before process exit`, {
      safe: {},
      sensitive: { error: e },
    });
  } finally {
    a.app.exit(0);
  }
}
exports.runMainAppStartup = z5;
//# sourceMappingURL=main.js.map
